---
phase: 42-site-wide-toast-notification-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Views/Shared/_Toasts.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.GroupPicker.cshtml
  - QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.cshtml
  - QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.Mobile.cshtml
  - QuestBoard.Service/Extensions/ControllerExtensions.cs
  - QuestBoard.Service/Controllers/Admin/AccountController.cs
  - QuestBoard.Service/wwwroot/js/site.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A shared _Toasts.cshtml partial renders Success/Error/Warning/Info/GoldReceived toasts from TempData, top-right, with the correct per-type color, icon, and autohide behavior"
    - "All 5 layouts render the shared toast partial, so any view under any layout can surface a flash message as a toast"
    - "Success and Info toasts auto-hide after 5000ms; Error and Warning toasts are sticky (manual dismiss only)"
    - "Controllers can set an Info-type flash via a new RedirectWithInfo extension method matching the existing sibling helpers"
    - "AccountController's Profile-page flash writes use the standard Success/Error/Warning/Info keys, so the shared partial reads one consistent key scheme with no dual-scheme branching"
    - "The toast-init script runs once site-wide from site.js, so toasts show without any per-view inline init script"
  artifacts:
    - path: "QuestBoard.Service/Views/Shared/_Toasts.cshtml"
      provides: "Shared TempData-driven toast partial for all 4 generic types plus the bespoke GoldReceived block"
      contains: "toast-container position-fixed top-0 end-0"
    - path: "QuestBoard.Service/Extensions/ControllerExtensions.cs"
      provides: "RedirectWithInfo helper setting TempData[\"Info\"]"
      contains: "RedirectWithInfo"
    - path: "QuestBoard.Service/wwwroot/js/site.js"
      provides: "Consolidated site-wide toast-init inside the existing DOMContentLoaded listener"
      contains: "new bootstrap.Toast"
  key_links:
    - from: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      to: "QuestBoard.Service/Views/Shared/_Toasts.cshtml"
      via: "<partial name=\"_Toasts\" /> after @RenderBody()"
      pattern: "partial name=\"_Toasts\""
    - from: "QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.cshtml"
      to: "QuestBoard.Service/Views/Shared/_Toasts.cshtml"
      via: "<partial name=\"_Toasts\" /> resolves to the root Shared partial from the Platform area"
      pattern: "partial name=\"_Toasts\""
    - from: "QuestBoard.Service/Controllers/Admin/AccountController.cs"
      to: "QuestBoard.Service/Views/Shared/_Toasts.cshtml"
      via: "TempData[\"Success\"|\"Error\"|\"Info\"] keys read by the shared partial"
      pattern: "TempData\\[\"(Success|Error|Info)\"\\]"
---

<objective>
Build the foundation for the site-wide toast redesign: create the shared `_Toasts.cshtml` partial, wire it into all 5 layouts, add the `RedirectWithInfo` helper, standardize AccountController's outlier TempData keys, and consolidate the toast-init JS into site.js. After this plan, every layout renders toasts from a single source of truth — but the per-view local flash markup still exists (removed in Wave 2). This is the enabling wave; the Wave 2 migration plans depend on it.

Purpose: Eliminate ~24 duplicated flash-message markup blocks and 4 duplicated init scripts by centralizing rendering into one partial + one init script (D-01, D-02). Add the new Info type (D-05) and its controller helper. Fix the two latent AccountController key-mismatch bugs as a side effect of key standardization.

Output: `_Toasts.cshtml` partial, 5 layouts wired with `<partial name="_Toasts" />`, `RedirectWithInfo` in ControllerExtensions, standardized AccountController keys, consolidated toast-init in site.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-CONTEXT.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
These symbols/files are CREATED by Phase 42 — exclude them from source-drift verification (they will not exist in the pre-phase codebase):

- **`QuestBoard.Service/Views/Shared/_Toasts.cshtml`** — NEW shared Razor partial (created in this plan).
- **`RedirectWithInfo`** — NEW extension method in `ControllerExtensions.cs` (created in this plan).
- **`<partial name="_Toasts" />`** — NEW markup line added to all 5 layouts (this plan).
- **Toast-init block inside `site.js`'s existing `DOMContentLoaded` listener** — NEW lines added to the existing listener (this plan). No new listener is registered.
- **`TempData["Info"]`** as a rendered flash key — NEW key surfaced by `RedirectWithInfo` and by AccountController's renamed `Edit` email-change path.

Downstream Wave 2 plans (02–05) only DELETE local markup and reference these already-created artifacts.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create shared _Toasts.cshtml partial with all 4 generic types plus GoldReceived</name>
  <files>QuestBoard.Service/Views/Shared/_Toasts.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shop/Index.cshtml (lines 450-478 — the existing Success/Error toast reference markup this partial ports verbatim)
    - QuestBoard.Service/Views/Shop/Details.cshtml (lines 79-98 — the existing GoldReceived "+X gp" bespoke toast block to reproduce exactly, including the `<small class="text-muted">@TempData["Success"]</small>` echo sub-line)
    - .planning/phases/42-site-wide-toast-notification-redesign/42-UI-SPEC.md (Color, Interaction/Dismiss Behavior, and Icons tables — the binding per-type contract)
  </read_first>
  <action>
    Create `QuestBoard.Service/Views/Shared/_Toasts.cshtml` as a plain Razor partial (D-02 — no `@model`, no View Component). Root element is the exact container string from the Shop reference: a `div` with classes `toast-container position-fixed top-0 end-0 p-3` and inline `style="z-index: 1055;"` (D-10).

    Inside, render five independent `@if (TempData["<key>"] != null)` blocks, one per key, in this order: `Success`, `Error`, `Warning`, `Info`, `GoldReceived`. Each generic block is a `div.toast.show` with `role="alert"`, a `div.toast-header` with a SOLID colored background bar (Bootstrap semantic `bg-*` class per type — not a subtle left-border-accent style, per D-11) + FontAwesome icon + `<strong class="me-auto">` label + a `btn-close` dismiss button, and a `div.toast-body` rendering the message via plain `@TempData["<key>"]` interpolation. Multiple simultaneous blocks stack vertically inside the single `.toast-container` via Bootstrap's default toast-container flow — no custom stacking/offset CSS (D-13).

    Per-type contract (from UI-SPEC):
    - Success: header `bg-success text-white`, icon `fas fa-check-circle`, label `Success`, `btn-close btn-close-white`, `data-bs-autohide="true"` + `data-bs-delay="5000"` (D-04).
    - Error: header `bg-danger text-white`, icon `fas fa-exclamation-circle`, label `Error`, `btn-close btn-close-white`, STICKY — omit `data-bs-autohide` and `data-bs-delay` entirely (do not set autohide to a false value; absence is the sticky signal) (D-04).
    - Warning: header `bg-warning text-dark`, icon `fas fa-exclamation-triangle` (deliberately the triangle, distinct from Error's circle — D-12), label `Warning`, plain `btn-close` (dark, since header is light), STICKY — omit autohide/delay attributes (D-04).
    - Info: header `bg-info text-white`, icon `fas fa-info-circle`, label `Info`, `btn-close btn-close-white`, `data-bs-autohide="true"` + `data-bs-delay="5000"` (D-05, D-04).
    - GoldReceived: reproduce the Shop Details bespoke block byte-for-byte — `div.toast.show.gold-toast`, header `bg-warning text-dark` with icon `fas fa-coins`, label `Gold Received!`, plain `btn-close`, `data-bs-autohide="true"` + `data-bs-delay="6000"` (keep 6000, do not change to 5000). Body renders the `fa-coins fa-2x text-warning` icon, a `<strong class="fs-5">+@TempData["GoldReceived"] gp</strong>`, and the `<small class="text-muted">@TempData["Success"]</small>` echo sub-line. Preserve this dual-render: the generic Success toast fires AND its text is echoed inside GoldReceived (per Claude's Discretion lean, confirmed in UI-SPEC).

    Use only plain `@TempData[...]` interpolation — never `@Html.Raw(...)` — to preserve Razor's default HTML-encoding (the existing XSS protection). Do NOT include any `@section Scripts` or inline `<script>` in this partial; init lives in site.js (Task 5).
  </action>
  <verify>
    <automated>bash -c "grep -c 'toast-container position-fixed top-0 end-0' QuestBoard.Service/Views/Shared/_Toasts.cshtml | grep -qx 1 && grep -q 'fa-info-circle' QuestBoard.Service/Views/Shared/_Toasts.cshtml && grep -q 'fa-exclamation-triangle' QuestBoard.Service/Views/Shared/_Toasts.cshtml && grep -q 'GoldReceived' QuestBoard.Service/Views/Shared/_Toasts.cshtml && ! grep -q 'Html.Raw' QuestBoard.Service/Views/Shared/_Toasts.cshtml && echo PARTIAL_OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `QuestBoard.Service/Views/Shared/_Toasts.cshtml` exists.
    - Source contains the exact string `toast-container position-fixed top-0 end-0 p-3` exactly once, with `style="z-index: 1055;"`.
    - Source contains all four generic keys `TempData["Success"]`, `TempData["Error"]`, `TempData["Warning"]`, `TempData["Info"]` and the `TempData["GoldReceived"]` key, each inside its own `@if (... != null)` block.
    - Success header has `bg-success text-white` + `fa-check-circle`; Error has `bg-danger text-white` + `fa-exclamation-circle`; Warning has `bg-warning text-dark` + `fa-exclamation-triangle`; Info has `bg-info text-white` + `fa-info-circle`.
    - Success and Info blocks contain `data-bs-delay="5000"`; Error and Warning blocks contain NO `data-bs-autohide` and NO `data-bs-delay` attribute; GoldReceived block contains `data-bs-delay="6000"`.
    - GoldReceived block contains `+@TempData["GoldReceived"] gp` and the `<small class="text-muted">@TempData["Success"]</small>` echo line.
    - Source contains no `@Html.Raw` and no `@section Scripts`/`<script>` tag.
  </acceptance_criteria>
  <done>The shared toast partial exists and renders all 4 generic types plus GoldReceived with the correct per-type color/icon/autohide contract, no inline script, no Html.Raw.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the shared partial into all 5 layouts</name>
  <files>QuestBoard.Service/Views/Shared/_Layout.cshtml, QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml, QuestBoard.Service/Views/Shared/_Layout.GroupPicker.cshtml, QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.cshtml, QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (around line 205, the `@RenderBody()` line, and line 220, the site.js `<script>` include)
    - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml (around line 174 `@RenderBody()`, line 188 site.js)
    - QuestBoard.Service/Views/Shared/_Layout.GroupPicker.cshtml (around line 28 `@RenderBody()`, line 34 site.js)
    - QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.cshtml (around line 43 `@RenderBody()`, line 49 site.js)
    - QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.Mobile.cshtml (around line 61 `@RenderBody()`, line 75 site.js)
    - .planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md (Critical Discrepancy #1 and #3 — why the Platform pair must be included, and that _Layout.Platform.Mobile.cshtml is dead code but still gets the partial for future-proofing)
  </read_first>
  <action>
    Add the single line `<partial name="_Toasts" />` to each of the 5 layout files, placed immediately AFTER the `@RenderBody()` call (so the toast container sits at the end of the page body, matching where Shop's local container currently sits relative to content), and before the closing footer/scripts. Use the exact tag `<partial name="_Toasts" />`.

    All 5 layouts must be wired — the root trio (`_Layout.cshtml`, `_Layout.Mobile.cshtml`, `_Layout.GroupPicker.cshtml` per D-01/D-03) AND the Platform-area pair (`_Layout.Platform.cshtml`, `_Layout.Platform.Mobile.cshtml`). The Platform pair is REQUIRED even though CONTEXT.md's literal text said "3 layouts": the Platform-area views (Group/Members, Group/Index, Users/Index and their mobile pairs — all in scope for Wave 2 migration) resolve to `_Layout.Platform.cshtml` via `Areas/Platform/Views/_ViewStart.cshtml`, NOT the root layouts. Omitting them would strip those views' alert markup in Wave 2 with no toast container to replace it (a functional regression). `_Layout.Platform.Mobile.cshtml` is currently dead code (its `_ViewStart.cshtml` never selects it) — still wire it for completeness/future-proofing; do NOT attempt to fix the layout-selection bug (out of scope).

    The `<partial name="_Toasts" />` reference resolves to `Views/Shared/_Toasts.cshtml` from every layout including the Platform area (partial name resolution walks up to the root Shared folder), so no area-relative path is needed.
  </action>
  <verify>
    <automated>bash -c "n=$(grep -rl 'partial name=\"_Toasts\"' QuestBoard.Service/Views/Shared/_Layout.cshtml QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml QuestBoard.Service/Views/Shared/_Layout.GroupPicker.cshtml QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.cshtml QuestBoard.Service/Areas/Platform/Views/Shared/_Layout.Platform.Mobile.cshtml | wc -l); [ \"$n\" = \"5\" ] && echo FIVE_LAYOUTS_OK || echo \"ONLY_$n\""</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 named layout files contains the exact string `<partial name="_Toasts" />` at least once.
    - In each file, the `<partial name="_Toasts" />` line appears after the `@RenderBody()` line (not before it).
    - No layout file was otherwise structurally altered (only the one partial line added).
  </acceptance_criteria>
  <done>All 5 layouts render `<partial name="_Toasts" />` after `@RenderBody()`; the Platform-area pair is included per RESEARCH Discrepancy #1.</done>
</task>

<task type="auto">
  <name>Task 3: Add RedirectWithInfo helper to ControllerExtensions.cs</name>
  <files>QuestBoard.Service/Extensions/ControllerExtensions.cs</files>
  <read_first>
    - QuestBoard.Service/Extensions/ControllerExtensions.cs (the full file — RedirectWithSuccess/Error/Warning are the exact one-line sibling pattern to mirror)
  </read_first>
  <action>
    Add an `internal static IActionResult RedirectWithInfo(this Controller controller, string action, string message)` method that delegates to the existing `RedirectWithMessage(action, "Info", message)`, following the exact one-line expression-body pattern of `RedirectWithSuccess`/`RedirectWithError`/`RedirectWithWarning`. Place it immediately after `RedirectWithWarning`. Give it an XML `<summary>` doc comment in the same style as its siblings ("Sets TempData Info and redirects to the given action."). Do NOT embed any GSD/phase/decision reference in the comment (project rule).
  </action>
  <verify>
    <automated>bash -c "grep -q 'RedirectWithInfo' QuestBoard.Service/Extensions/ControllerExtensions.cs && grep -q 'RedirectWithMessage(action, \"Info\", message)' QuestBoard.Service/Extensions/ControllerExtensions.cs && echo INFO_HELPER_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `ControllerExtensions.cs` contains a `RedirectWithInfo` method with signature `(this Controller controller, string action, string message)` returning `IActionResult`.
    - Its body delegates to `RedirectWithMessage(action, "Info", message)`.
    - It has an XML doc `<summary>` comment; the comment contains no phase number, decision ID, or requirement ID.
  </acceptance_criteria>
  <done>`RedirectWithInfo` exists and sets `TempData["Info"]` via the shared helper, matching the sibling method pattern.</done>
</task>

<task type="auto">
  <name>Task 4: Standardize AccountController TempData keys to Success/Error/Info</name>
  <files>QuestBoard.Service/Controllers/Admin/AccountController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/AccountController.cs (the full file — the outlier writes are at approx lines 251, 257, 273, 275, 280, 304; note which actions redirect to Login vs Profile)
    - .planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md (Critical Discrepancy #2 and Pitfall 2 — the ConfirmEmailChange Error path and Edit InfoMessage path currently render nothing; after this rename they correctly show for the first time — this is intentional, not a regression)
  </read_first>
  <action>
    Rename the three outlier TempData keys used in AccountController's Profile-page actions to the standard scheme so `_Toasts.cshtml` reads one consistent key set (Claude's Discretion lean, confirmed in UI-SPEC): `TempData["SuccessMessage"]` → `TempData["Success"]`, `TempData["ErrorMessage"]` → `TempData["Error"]`, `TempData["InfoMessage"]` → `TempData["Info"]`. Apply to every occurrence in this file (approx: `Edit` action's email-change confirmation InfoMessage; `Edit`'s "Profile updated successfully!" SuccessMessage; `ConfirmEmailChange`'s SuccessMessage/ErrorMessage; the password-change SuccessMessage). Do NOT touch the already-standard `TempData["Success"]`/`TempData["Error"]` writes in the ForgotPassword/reset actions (approx lines 71, 102, 114) — those are already correct.

    Note the two latent-bug fixes this surfaces (call them out in the SUMMARY as intentional, per Pitfall 2): (a) `ConfirmEmailChange` redirects to `Login`, which reads the standard `Success`/`Error` keys — after this rename its messages will actually appear (they were silently dropped before); (b) `Edit`'s email-change `Info` message will render an Info toast on Profile for the first time. Both are the previously-broken paths becoming visible — expected, not scope creep.

    Do not change any message strings, action logic, or redirect targets — key renames only.
  </action>
  <verify>
    <automated>bash -c "! grep -Eq 'TempData\[\"(SuccessMessage|ErrorMessage|InfoMessage)\"\]' QuestBoard.Service/Controllers/Admin/AccountController.cs && grep -q 'TempData\[\"Info\"\]' QuestBoard.Service/Controllers/Admin/AccountController.cs && echo KEYS_STANDARDIZED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `AccountController.cs` contains ZERO occurrences of `TempData["SuccessMessage"]`, `TempData["ErrorMessage"]`, or `TempData["InfoMessage"]`.
    - The former `InfoMessage` write now sets `TempData["Info"]` (grep confirms `TempData["Info"]` present).
    - No message string literal was changed and no redirect target (`RedirectToAction` argument) was changed.
  </acceptance_criteria>
  <done>AccountController writes only the standard `Success`/`Error`/`Info` keys; the two previously-silent paths now resolve to keys the shared partial reads.</done>
</task>

<task type="auto">
  <name>Task 5: Consolidate toast-init into the existing site.js DOMContentLoaded listener</name>
  <files>QuestBoard.Service/wwwroot/js/site.js</files>
  <read_first>
    - QuestBoard.Service/wwwroot/js/site.js (lines 272-304 — the existing `document.addEventListener('DOMContentLoaded', function() { ... })` block to EXTEND, not duplicate)
    - QuestBoard.Service/Views/Shop/Index.cshtml (lines 481-489 — the exact toast-init snippet being centralized)
    - .planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md (Pattern 2 and Pitfall 3 — extend the existing listener; leave Shop's showMerchantToast untouched)
  </read_first>
  <action>
    Inside site.js's EXISTING `document.addEventListener('DOMContentLoaded', function() { ... })` block (the one near lines 272-304), add the generic toast bulk-init logic: select all `.toast` elements via `document.querySelectorAll('.toast')`, and for each construct a `new bootstrap.Toast(el)` and call `.show()`. This is the exact snippet currently duplicated in the 4 Shop `@section Scripts` blocks. Do NOT register a second `DOMContentLoaded` listener — append to the existing one so there is a single init path site-wide.

    This is the only generic init call site after this phase. Shop's `showMerchantToast()` (which creates its own dynamic toast at click-time) is untouched and lives elsewhere (Shop/Index.cshtml) — it keeps its own `new bootstrap.Toast(newToast)` call. Wave 2's Shop migration (Plan 02) removes the 4 now-redundant inline init copies.
  </action>
  <verify>
    <automated>bash -c "grep -q \"querySelectorAll('.toast')\" QuestBoard.Service/wwwroot/js/site.js && grep -q 'new bootstrap.Toast' QuestBoard.Service/wwwroot/js/site.js && [ \"$(grep -c \"addEventListener('DOMContentLoaded'\" QuestBoard.Service/wwwroot/js/site.js)\" = \"1\" ] && echo SITEJS_INIT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `site.js` contains `document.querySelectorAll('.toast')` and `new bootstrap.Toast(` inside a DOMContentLoaded handler.
    - `site.js` contains exactly ONE `addEventListener('DOMContentLoaded'` registration (the init was appended to the existing listener, not added as a second one).
    - The existing datetime/masonry logic in that listener is preserved (not deleted).
  </acceptance_criteria>
  <done>site.js's single DOMContentLoaded listener now bulk-initializes all `.toast` elements site-wide; no second listener was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Razor view (TempData) | Server-set flash-message strings cross into rendered HTML |
| Server-rendered HTML → Browser DOM | Toast markup + bootstrap.Toast client init |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure / Tampering | `_Toasts.cshtml` rendering `@TempData[...]` | mitigate | Use plain `@TempData[...]` interpolation only (Razor auto-HTML-encodes); never `@Html.Raw`. Task 1 acceptance criteria greps for absence of `Html.Raw`. |
| T-42-02 | Tampering | Toast message content originates from controller-set strings, not raw user input | accept | Message strings are server-authored constants/interpolations already present pre-phase; no new user-input path is introduced by relocating rendering. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — this phase installs zero packages; Bootstrap 5.3.0 / FontAwesome 6.4.0 are pre-existing CDN includes (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Build: `dotnet build` succeeds (Razor partial + C# extension + controller edits compile). If the build fails on locked output files, the app is running under the VS debugger — ask the user to stop it (Shift+F5) and retry.
- `_Toasts.cshtml` exists and passes Task 1 grep gate.
- All 5 layouts contain `<partial name="_Toasts" />` after `@RenderBody()` (Task 2 gate).
- `RedirectWithInfo` present (Task 3 gate); AccountController has zero `*Message` outlier keys (Task 4 gate); site.js has a single DOMContentLoaded listener with toast bulk-init (Task 5 gate).
- Manual (per 42-VALIDATION.md): load one view under each of the 5 layouts with a TempData key set and confirm a toast renders top-right with the correct color/icon; confirm Success/Info auto-hide at 5000ms and Error/Warning stay sticky. NOTE: Wave 2 has not yet removed local markup, so some views may show BOTH the old banner and the new toast at this point — that is expected mid-migration and resolves after Plans 02–05.
- Intentional new-visible behavior (call out in SUMMARY, not a regression): an expired `ConfirmEmailChange` link now shows an Error toast on Login; an email-change request from `Edit` now shows an Info toast on Profile.
</verification>

<success_criteria>
- Shared `_Toasts.cshtml` partial renders all 4 generic toast types + GoldReceived with the correct per-type color/icon/autohide contract and no `@Html.Raw`.
- All 5 layouts render the shared partial.
- `RedirectWithInfo` helper exists; AccountController uses only standardized keys.
- Toast-init is consolidated into site.js's single existing DOMContentLoaded listener.
- `dotnet build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/42-site-wide-toast-notification-redesign/42-01-SUMMARY.md` when done. In it, explicitly call out the two intentional new-visible behaviors from Task 4 (ConfirmEmailChange Error toast on Login; Edit email-change Info toast on Profile) as fixed latent bugs, NOT scope creep.
</output>
