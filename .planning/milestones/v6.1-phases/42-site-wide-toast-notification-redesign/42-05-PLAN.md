---
phase: 42-site-wide-toast-notification-redesign
plan: 05
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - QuestBoard.Service/Views/Admin/Users.cshtml
  - QuestBoard.Service/Views/Quest/Manage.cshtml
  - QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The Admin Users page and the Quest Manage views (desktop + mobile) no longer render their own alert-banner flash markup; flash messages render as toasts via the shared partial in the root layouts"
    - "A DM managing a quest and an admin managing users still see their Success/Warning/Error messages, now as top-right toasts"
  artifacts:
    - path: "QuestBoard.Service/Views/Admin/Users.cshtml"
      provides: "Admin user-management page with alert banners removed, relying on shared toast partial"
  key_links:
    - from: "QuestBoard.Service/Views/Admin/Users.cshtml"
      to: "QuestBoard.Service/Views/Shared/_Toasts.cshtml"
      via: "Success/Error/Warning flash rendering delegated to the shared partial in the root layout (Plan 01)"
      pattern: "TempData"
---

<objective>
Migrate the remaining 3 root-layout alert-banner views — `Admin/Users.cshtml` and `Quest/Manage.cshtml` (+ mobile) — off their local `alert alert-dismissible` flash markup onto the shared toast partial (D-08). These resolve to the root layouts (`_Layout.cshtml`/`_Layout.Mobile.cshtml`), wired with `<partial name="_Toasts" />` in Plan 01.

Purpose: Complete the alert-banner migration for the last root views not covered by the Account (Plan 04) or Shop (Plan 02) groups. After this plan, no view in the app renders a local TempData `alert-dismissible` banner (the whole app is on the shared toast partial).

Output: 3 root views with local alert banners removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-CONTEXT.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan CREATES no new symbols — it only DELETES local `alert alert-dismissible` flash blocks from 3 root views. It DEPENDS on `_Toasts.cshtml` + the root-layout `<partial name="_Toasts" />` wiring (both created in Plan 01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove alert banners from Admin Users page</name>
  <files>QuestBoard.Service/Views/Admin/Users.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Admin/Users.cshtml (lines 21-46 — the Success/Error/Warning alert-dismissible blocks inside `card-body`)
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (confirm the shared toast partial line is present from Plan 01)
  </read_first>
  <action>
    In `Admin/Users.cshtml`, remove the three local flash blocks: `@if (TempData["Success"] != null) {...}`, `@if (TempData["Error"] != null) {...}`, and `@if (TempData["Warning"] != null) {...}`, each wrapping an `<div class="alert alert-... alert-dismissible fade show">` (full `@if` wrapper, inner alert div, icon element, `@TempData[...]`, and dismiss button). These keys are read by the shared partial, so messages now render as top-right toasts via the root layout.

    Preserve the `modern-card`/`modern-card-header`/`modern-card-body` structure, the "Create User" header button, the users table, and the per-row action buttons/forms. Do not touch `asp-validation-*` markup. Remove only the TempData alert-banner blocks.
  </action>
  <verify>
    <automated>bash -c '! grep -q "alert-dismissible" QuestBoard.Service/Views/Admin/Users.cshtml && grep -q "modern-card" QuestBoard.Service/Views/Admin/Users.cshtml && echo ADMIN_USERS_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `Admin/Users.cshtml` contains no `alert-dismissible` string.
    - It contains no `@if (TempData["Success"|"Error"|"Warning"] != null)` block wrapping an `alert` div.
    - The `modern-card` structure, "Create User" button, users table, and action forms are unchanged.
  </acceptance_criteria>
  <done>The Admin Users page has its three local alert banners removed; flash messages render via the shared toast partial.</done>
</task>

<task type="auto">
  <name>Task 2: Remove alert banners from Quest Manage (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Quest/Manage.cshtml, QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Manage.cshtml (its TempData alert-dismissible block region)
    - QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml (its TempData alert-dismissible block region)
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (confirm the shared toast partial line is present from Plan 01)
  </read_first>
  <action>
    In both `Quest/Manage.cshtml` and `Quest/Manage.Mobile.cshtml`, remove every local `@if (TempData["Success"|"Warning"|"Error"] != null) { <div class="alert alert-... alert-dismissible fade show" ...>...</div> }` flash block (full `@if` wrapper, inner alert div, icon, `@TempData[...]`, dismiss button). The shared partial renders the same keys as toasts after removal.

    Preserve all quest-management content: the quest list/cards, finalize/close/reopen actions, date-voting UI, and any `asp-validation-*` markup (D-09). Remove only the TempData alert-banner blocks.
  </action>
  <verify>
    <automated>bash -c 'for f in QuestBoard.Service/Views/Quest/Manage.cshtml QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml; do grep -q "alert-dismissible" "$f" && { echo "BANNER_REMAINS:$f"; exit 1; }; done; echo QUEST_MANAGE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - Neither `Quest/Manage.cshtml` nor `Quest/Manage.Mobile.cshtml` contains `alert-dismissible`.
    - Neither file contains a TempData `@if` block wrapping an `alert` div for `Success`/`Error`/`Warning`.
    - Quest-management content (quest cards, action buttons, date-voting UI) is unchanged.
  </acceptance_criteria>
  <done>Quest Manage (desktop + mobile) has its local alert banners removed; flash messages render via the shared toast partial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Razor view (TempData) | Admin/Quest action results (Success/Warning/Error) cross into rendered HTML — now via the shared partial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Information Disclosure | Removed alert banners replaced by shared partial | mitigate | Shared partial (Plan 01) uses plain `@TempData[...]` (no `@Html.Raw`); this plan only deletes local banners. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — zero package installs in this phase. |
</threat_model>

<verification>
- Build: `dotnet build` succeeds (Razor-only edits). If build fails on locked files, stop the VS debugger (Shift+F5) and retry.
- Grep gates for Tasks 1 and 2 pass (no `alert-dismissible` in the 3 views).
- Phase-wide final check (after all Wave 2 plans): grep the entire `QuestBoard.Service` for `alert-dismissible` scoped to TempData flash usage — no TempData-driven flash banner should remain (form-validation `asp-validation-summary` markup is unrelated and stays).
- Manual (per 42-VALIDATION.md): perform a quest management action (finalize/close/reopen) and an admin user action; confirm the resulting message appears as a top-right toast on both desktop and mobile.
</verification>

<success_criteria>
- Admin Users and Quest Manage (desktop + mobile) no longer render local alert banners.
- Their Success/Warning/Error messages still surface, now as toasts.
- `dotnet build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/42-site-wide-toast-notification-redesign/42-05-SUMMARY.md` when done.
</output>
