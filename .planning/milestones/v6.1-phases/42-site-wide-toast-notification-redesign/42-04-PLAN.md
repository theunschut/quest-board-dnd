---
phase: 42-site-wide-toast-notification-redesign
plan: 04
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - QuestBoard.Service/Views/Account/Login.cshtml
  - QuestBoard.Service/Views/Account/Login.Mobile.cshtml
  - QuestBoard.Service/Views/Account/ForgotPassword.cshtml
  - QuestBoard.Service/Views/Account/ForgotPassword.Mobile.cshtml
  - QuestBoard.Service/Views/Account/Profile.cshtml
  - QuestBoard.Service/Views/Account/Profile.Mobile.cshtml
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The Account views (Login, ForgotPassword, Profile — desktop + mobile) no longer render local alert-banner flash markup; flash messages render as toasts via the shared partial in the root layouts"
    - "Profile's flash blocks that read the old SuccessMessage key are removed; after Plan 01's key standardization the controller writes Success/Info, which the shared partial renders as toasts"
    - "Login.Mobile, which previously showed no flash message at all, now surfaces Success/Error flashes as toasts via the shared partial (a latent gap closed)"
  artifacts:
    - path: "QuestBoard.Service/Views/Account/Profile.cshtml"
      provides: "Profile page with local SuccessMessage alert banner removed, relying on shared toast partial"
  key_links:
    - from: "QuestBoard.Service/Views/Account/Profile.cshtml"
      to: "QuestBoard.Service/Views/Shared/_Toasts.cshtml"
      via: "Success/Info flash rendering delegated to the shared partial after AccountController key standardization (Plan 01)"
      pattern: "TempData"
---

<objective>
Migrate the 6 Account views (Login, ForgotPassword, Profile — desktop + mobile) off their local flash-message markup onto the shared toast partial (D-08). These resolve to the root layouts (`_Layout.cshtml`/`_Layout.Mobile.cshtml`), wired with `<partial name="_Toasts" />` in Plan 01. Profile's blocks use the OLD `SuccessMessage`/`InfoMessage` keys — those keys were standardized to `Success`/`Info` in AccountController by Plan 01, so removing Profile's local banners lets the shared partial render the now-standard-keyed messages.

Purpose: Complete the Account-area migration, including the Profile page that carried the outlier key scheme. Also closes a latent gap: `Login.Mobile.cshtml` currently renders no flash markup at all, so it silently dropped Login flashes on mobile — after this migration the shared partial surfaces them.

Output: 6 Account views with local flash markup removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-CONTEXT.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan CREATES no new symbols — it only DELETES local flash markup from 6 Account views. It DEPENDS on `_Toasts.cshtml` + the root-layout `<partial name="_Toasts" />` wiring AND the AccountController key standardization (Success/Info), all delivered in Plan 01. `Login.Mobile.cshtml` gaining a visible flash is not a new artifact — it is the shared partial (Plan 01) now rendering for a view that previously had none.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove flash banners from Login and ForgotPassword (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Account/Login.cshtml, QuestBoard.Service/Views/Account/Login.Mobile.cshtml, QuestBoard.Service/Views/Account/ForgotPassword.cshtml, QuestBoard.Service/Views/Account/ForgotPassword.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Account/Login.cshtml (lines 18-33 — the Success/Error alert-dismissible blocks to remove)
    - QuestBoard.Service/Views/Account/Login.Mobile.cshtml (currently has NO alert/TempData markup — confirm there is nothing to remove; do not add markup, the shared partial handles it)
    - QuestBoard.Service/Views/Account/ForgotPassword.cshtml (lines 23-46 — Success/Warning/Error alert blocks)
    - QuestBoard.Service/Views/Account/ForgotPassword.Mobile.cshtml (its TempData alert block region)
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (confirm the shared toast partial line is present from Plan 01)
  </read_first>
  <action>
    In `Login.cshtml` and `ForgotPassword.cshtml` (+ `ForgotPassword.Mobile.cshtml`): remove every local `@if (TempData["Success"|"Warning"|"Error"] != null) { <div class="alert alert-... alert-dismissible fade show" ...>...</div> }` flash block (full `@if` wrapper, inner alert div, icon, `@TempData[...]`, dismiss button). These keys are already read by the shared partial, so the messages now render as toasts via the root layout.

    For `Login.Mobile.cshtml`: this file currently has NO TempData/alert markup at all (verified — it silently showed nothing). Do NOT add any markup. Removing nothing is correct; the shared partial in `_Layout.Mobile.cshtml` will now surface Login's `Success`/`Error` flashes on mobile for the first time — note this closed gap in the SUMMARY as an intentional improvement (not a regression).

    Do NOT touch `asp-validation-summary` / field-level `asp-validation-for` markup (D-09) — the Login and ForgotPassword forms have validation summaries that must remain. Preserve all form fields, buttons, and layout structure. Remove only TempData flash-banner blocks.
  </action>
  <verify>
    <automated>bash -c 'for f in QuestBoard.Service/Views/Account/Login.cshtml QuestBoard.Service/Views/Account/Login.Mobile.cshtml QuestBoard.Service/Views/Account/ForgotPassword.cshtml QuestBoard.Service/Views/Account/ForgotPassword.Mobile.cshtml; do grep -Eq "TempData\[\"(Success|Error|Warning)\"\].*alert-dismissible|alert-dismissible" "$f" && { echo "BANNER_REMAINS:$f"; exit 1; }; done; echo LOGIN_FORGOT_OK'</automated>
  </verify>
  <acceptance_criteria>
    - None of the 4 files contains `alert-dismissible`.
    - `Login.cshtml` and `ForgotPassword.cshtml`/`.Mobile.cshtml` no longer contain a TempData `@if` block wrapping an `alert` div.
    - `Login.Mobile.cshtml` is unchanged if it had no flash markup (no markup added).
    - `asp-validation-summary` markup in Login/ForgotPassword forms is still present; all form fields and buttons are unchanged.
  </acceptance_criteria>
  <done>Login and ForgotPassword (desktop + mobile) have local flash banners removed; validation summaries preserved; Login.Mobile now surfaces flashes via the shared partial.</done>
</task>

<task type="auto">
  <name>Task 2: Remove flash banners from Profile (desktop + mobile), including the old SuccessMessage-keyed block</name>
  <files>QuestBoard.Service/Views/Account/Profile.cshtml, QuestBoard.Service/Views/Account/Profile.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Account/Profile.cshtml (lines 80-86 — the `@if (TempData["SuccessMessage"] != null)` alert-success block; check for any Error/Info blocks too)
    - QuestBoard.Service/Views/Account/Profile.Mobile.cshtml (lines 10-14 — the `@if (TempData["SuccessMessage"] != null)` block)
    - QuestBoard.Service/Controllers/Admin/AccountController.cs (confirm Plan 01 renamed the Profile actions' keys to Success/Info — so the controller no longer writes SuccessMessage/InfoMessage)
    - .planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md (Pitfall 2 — Profile's Info/Error paths were previously never rendered; after Plan 01's rename + this removal they render as toasts for the first time)
  </read_first>
  <action>
    In `Profile.cshtml` and `Profile.Mobile.cshtml`: remove the local flash-message blocks that read the OLD outlier keys — `@if (TempData["SuccessMessage"] != null) { <div class="alert ...">...</div> }` and any `ErrorMessage`/`InfoMessage` equivalents (full `@if` wrapper, inner alert div, icon, interpolation, dismiss button). Plan 01 already renamed AccountController's Profile-action writes to the standard `Success`/`Error`/`Info` keys, which the shared partial reads — so after removing these local `*Message`-keyed banners, Profile's flash messages render as toasts via the root layout's shared partial.

    Because the controller now writes `Success`/`Info` (not `SuccessMessage`/`InfoMessage`), leaving the old `*Message`-keyed banners would render nothing anyway (dead markup). Removing them is required for a clean migration. This surfaces the previously-silent Info toast on email-change requests (RESEARCH Pitfall 2) — intentional, note in SUMMARY.

    Do NOT touch `asp-validation-*` markup, profile form fields, avatar/photo section, or the `modern-card` structure — remove only the TempData flash-banner blocks.
  </action>
  <verify>
    <automated>bash -c 'for f in QuestBoard.Service/Views/Account/Profile.cshtml QuestBoard.Service/Views/Account/Profile.Mobile.cshtml; do grep -Eq "SuccessMessage|ErrorMessage|InfoMessage|alert-dismissible" "$f" && { echo "BANNER_REMAINS:$f"; exit 1; }; done; echo PROFILE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - Neither `Profile.cshtml` nor `Profile.Mobile.cshtml` contains `SuccessMessage`, `ErrorMessage`, `InfoMessage`, or `alert-dismissible`.
    - The profile form, avatar/photo section, and `modern-card` structure are unchanged.
    - `asp-validation-*` markup (if any) is preserved.
  </acceptance_criteria>
  <done>Profile (desktop + mobile) has its old `*Message`-keyed flash banners removed; flash messages now render as toasts via the shared partial reading the standardized keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Razor view (TempData) | Account flash strings (Success/Error/Warning/Info) cross into rendered HTML — now via the shared partial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04-01 | Information Disclosure | Removed alert banners replaced by shared partial | mitigate | Shared partial (Plan 01) uses plain `@TempData[...]` (no `@Html.Raw`); this plan only deletes local banners. |
| T-42-04-02 | Repudiation / silent-drop | Profile's `*Message`-keyed banners + controller-key mismatch | mitigate | Plan 01 standardized the controller keys; this plan removes the now-dead `*Message` banners so the standardized keys render — closing the previously-silent Info/Error path (RESEARCH Pitfall 2). |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — zero package installs in this phase. |
</threat_model>

<verification>
- Build: `dotnet build` succeeds (Razor-only edits). If build fails on locked files, stop the VS debugger (Shift+F5) and retry.
- Grep gates for Tasks 1 and 2 pass (no `alert-dismissible`; no `SuccessMessage`/`ErrorMessage`/`InfoMessage` in Profile views).
- Manual (per 42-VALIDATION.md): (a) trigger a Login flash (e.g. after password reset) on both desktop and mobile and confirm a toast appears — mobile is the newly-closed gap; (b) request an email change from Profile and confirm an Info toast appears (previously silent — RESEARCH Pitfall 2, intentional); (c) update the profile and confirm the Success toast appears; (d) confirm the ForgotPassword confirmation message appears as a toast.
</verification>

<success_criteria>
- All 6 Account views no longer render local flash banners.
- Profile's old `*Message`-keyed banners are removed; standardized-key messages render via the shared partial.
- Login.Mobile now surfaces flashes as toasts (gap closed).
- Validation summaries preserved.
- `dotnet build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/42-site-wide-toast-notification-redesign/42-04-SUMMARY.md` when done. Call out the Login.Mobile gap-closure and the Profile Info-toast-now-visible behavior as intentional improvements, not regressions.
</output>
