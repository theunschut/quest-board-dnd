---
phase: 69-contact-fields
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - QuestBoard.Service/Views/Contacts/Details.cshtml
  - QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml
  - QuestBoard.Service/wwwroot/css/contact-detail.mobile.css
autonomous: true
requirements: [CONTACTMD-01, CONTACTMD-02]
must_haves:
  truths:
    - "Contact Description renders as sanitized formatted HTML on Contact Details desktop and mobile"
    - "The always-visible Add Note form shows the full toolbar + Preview immediately (per D-02)"
    - "Editing a note shows the toolbar + Preview; each note renders via its own Html.Markdown call so one author's unclosed formatting never bleeds into another note (CONTACTMD-02)"
    - "Opening one note's editor auto-collapses any other currently-open note editor and reverts its unsaved text to the saved value (per D-03)"
    - "The old whitespace-preserving inline styles are removed from Description and each note's view container — multi-line text shows without doubled spacing"
  artifacts:
    - "QuestBoard.Service/Views/Contacts/Details.cshtml (Description Html.Markdown + Notes editors + lazy-init/D-03 registry JS)"
    - "QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml (identical Notes + Description render)"
    - "QuestBoard.Service/wwwroot/css/contact-detail.mobile.css (li added to Description catch-all and .note-item scope-out)"
  key_links:
    - "Per-note edit editor uses ElementId=Text_{note.Id} but FieldName stays 'Text' so EditNote model binding still resolves viewModel.Text"
    - "EasyMDE for a note is created only on first Edit-button click, after the container is shown — never inside display:none"
    - "noteEditors Map keyed by the .note-item data-note-id drives D-03 auto-collapse and unsaved-text revert"
    - "Details.cshtml / Details.Mobile.cshtml load _QuestFormScripts.cshtml (first read pages in the app to do so) so window.markdownAntiforgeryToken + EasyMDE are present"
---

<objective>
Complete CONTACTMD-01 (Description read-side) and deliver all of CONTACTMD-02 (Notes) on the two Contact Details views. This plan does everything that lives on the Details page: render Description via `Html.Markdown()`, wire the Add Note and per-note Edit forms to the shared editor, render each note independently via `Html.Markdown()`, and replace the note-toggle script with the D-03 auto-collapse + lazy-init registry. It also adds the two `li` companion CSS edits the mobile card needs for Markdown lists.

The Notes surface is the only genuinely new work in this phase (per-note multi-instance inline editors on one shared page — no prior field did this). It relies on plan 69-01's `ElementId` property and `display:none`-skip guard, hence Wave 2.

Purpose: Users can read formatted Description and Notes, author notes with the full toolbar, and edit notes one-at-a-time with unsaved changes safely discarded on switch.
Output: Both Details views rendering formatted Contact content, per-note independent rendering, D-03 exclusivity, and the mobile CSS list fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-contact-fields/69-CONTEXT.md
@.planning/phases/69-contact-fields/69-RESEARCH.md
@.planning/phases/69-contact-fields/69-PATTERNS.md
@.planning/phases/69-contact-fields/69-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by THIS plan (source-grounding allowlist for reviewers — new, not drift):

- Per-note DOM ids of the form `Text_{note.Id}` on each note's edit-form textarea/label (produced by passing `ElementId = $"Text_{note.Id}"` to `_MarkdownEditor`). The posted field name stays `Text`.
- `noteEditors` — a client-side `Map` (noteId → `{ instance, originalText }`) declared in each Details view's `@section Scripts`.
- `collapseNote(noteItem)` — a client-side function in each Details view's `@section Scripts` that reverts a note's EasyMDE value to its saved text and hides its edit form.
- CSS selector extensions in `contact-detail.mobile.css`: `li` added to the `.contact-detail-card` paragraph catch-all and to the `.contact-detail-card .note-item` scope-out rule.

Consumes from plan 69-01: `MarkdownEditorViewModel.ElementId`, the `_MarkdownEditor.cshtml` id override, and the `markdown-editor.js` visibility guard.

New files: none. New C#: none. No new packages, no migrations, no controller changes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Render Contact Description as Markdown on Details (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Contacts/Details.cshtml, QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Contacts/Details.cshtml — top `@using` block (only ContactViewModels today); Description block lines 88-97 (the `<p class="mb-0" style="…">@Model.Description</p>` inside `.card-body.modern-card-body`)
    - QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml — Description block lines 74-85 (identical shape inside `.contact-detail-card`)
    - QuestBoard.Service/Views/Characters/Details.cshtml — lines 1-3 (`@using QuestBoard.Service.Extensions` for `Html.Markdown`, plus the CharacterViewModels using) and lines 148-154 (the shipped `Html.Markdown(Model.Description)` read pattern)
    - .planning/phases/69-contact-fields/69-UI-SPEC.md — "Read-view restructuring" copy row and the heading-color findings (no new CSS needed for Description desktop; consistent gold-with-shadow is the accepted look)
  </read_first>
  <action>
    In both Details views, add `@using QuestBoard.Service.Extensions` to the top `@using` block so `@Html.Markdown(...)` resolves (this namespace is not in _ViewImports; confirmed present in Character's Details.cshtml, absent in Contact's). Also add `@using QuestBoard.Service.ViewModels.Shared` now — Task 2 needs it and doing both usings here keeps the header edit in one place.

    Replace the Description render so `Model.Description`'s Markdown becomes formatted HTML:
    - Keep the existing `@if (!string.IsNullOrEmpty(Model.Description)) { … } else { <p class="text-muted mb-0">No description provided.</p> }` guard exactly (the else branch and its copy are unchanged).
    - In the truthy branch, replace the single `<p class="mb-0" style="…whitespace-preserving…">@Model.Description</p>` with `@Html.Markdown(Model.Description)`. Drop the whitespace-preserving inline style attribute entirely (do not move it) — the global `.markdown-content { white-space: normal }` rule now governs paragraph spacing. `Html.Markdown` emits block-level HTML (headings/lists/blockquotes) that cannot legally sit inside a `<p>`, so the `<p>` wrapper must go; the helper already wraps output in its own `<div class="markdown-content">`, so no extra wrapper element is required. Preserve the surrounding card structure (the existing `<h4>Description</h4>` header on desktop and `.contact-section-heading` "Description" header on mobile stay as-is — do not add a duplicate inline `<label>`).

    Do not touch the portrait card, Actions card, or the Notes card in this task.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds.
    - Both files call the read helper for Description: `grep -c 'Html.Markdown(Model.Description)' QuestBoard.Service/Views/Contacts/Details.cshtml` returns 1 and the same for Details.Mobile.cshtml.
    - Both files import the extensions namespace: `grep -c 'QuestBoard.Service.Extensions' <each Details file>` returns 1.
    - The Description no-value copy is preserved: `grep -c 'No description provided.' <each Details file>` returns 1.
    - The whitespace-preserving inline style is gone from the Description container (verified together with note containers in Task 2's acceptance criteria).
  </acceptance_criteria>
  <done>Contact Description renders as sanitized formatted HTML on both Details views; the empty-state copy and card headers are unchanged; the solution builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Add Note + per-note Edit editors and per-note Markdown rendering (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Contacts/Details.cshtml, QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Contacts/Details.cshtml — Add Note form lines 106-114; per-note `@foreach` block lines 120-157 (note-view-text `<p>` at 145, `.note-edit-form` with `<textarea name="Text">@note.Text</textarea>` at 147-155); `@section Scripts` at 179-201
    - QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml — Add Note form lines 91-99; per-note `@foreach` lines 105-142 (note-view-text `<p>` at 130, edit form 132-140); `@section Scripts` at 163-185
    - QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs — `ContactNoteViewModel.Text` is `[Required]` + `[StringLength(2000)]`; `Notes` is `List<ContactNoteViewModel>`
    - QuestBoard.Service/Controllers/Contacts/ContactsController.cs — `AddNote(int contactId, ContactNoteViewModel viewModel, …)` and `EditNote(int id, int contactId, ContactNoteViewModel viewModel, …)`: both bind the bare, unprefixed `Text` form field. This is why the posted field name MUST stay exactly `"Text"`.
    - .planning/phases/69-contact-fields/69-RESEARCH.md — "Unknown 1" (ElementId vs FieldName), and Code Examples for the Add/Edit note wiring
    - .planning/phases/69-contact-fields/69-UI-SPEC.md — "Add Note field" and "Edit Note field" copy rows (labels, placeholder, required)
  </read_first>
  <action>
    Convert the three note surfaces in each Details view to the shared editor + per-note independent Markdown render. Both Details views carry byte-for-byte-identical note markup; apply the same edits to each.

    (1) Add Note form (single, always-visible, top of the Notes card): replace the bare `<textarea name="Text" …placeholder="Add a note about this contact...">` with a render of `_MarkdownEditor` passing `FieldName = "Text"`, `Value = null`, `Label = "Add a Note"`, `Required = true`, `Placeholder = "Add a note about this contact..."`. Omit `ElementId` (single instance on the page → falls back to id `"Text"`). Keep the surrounding `<form asp-action="AddNote">`, its hidden `contactId`, and the Add Note submit button row unchanged. Because this form uses `asp-action`, the antiforgery token is already emitted.

    (2) Per-note view text (inside the `@foreach`): replace `<p class="mb-0 mt-2 note-view-text" style="…whitespace-preserving…">@note.Text</p>` with `<div class="mb-0 mt-2 note-view-text">@Html.Markdown(note.Text)</div>`. Keep the class `note-view-text` exactly — both the mobile CSS scope-out rules and the note-toggle script select by that class. Drop the whitespace-preserving inline style (do not move it). One `Html.Markdown(note.Text)` call PER note inside the loop — never concatenate note text into a single string; this per-note isolation is what satisfies CONTACTMD-02's independent-rendering constraint.

    (3) Per-note Edit form (inside the `@foreach`): replace `<textarea name="Text" …>@note.Text</textarea>` with a render of `_MarkdownEditor` passing `FieldName = "Text"` (MUST stay literally "Text" — EditNote binds the unprefixed field; making FieldName per-note-unique would silently fail `[Required]` and lose the edit), `ElementId = $"Text_{note.Id}"` (per-note-unique DOM id — this is the sole reason `ElementId` was added in 69-01), `Value = note.Text`, `Label = "Edit Note"`, `Required = true`. Keep the surrounding `<form asp-action="EditNote">`, its hidden `id`/`contactId`, the `note-edit-form` class, the `style="display: none;"`, and the Cancel/Save button row unchanged.

    (4) Load the editor assets on the Details pages (first read pages in the app to need them): inside each view's existing `@section Scripts`, add `@{ await Html.RenderPartialAsync("~/Views/Quest/_QuestFormScripts.cshtml"); }` as the FIRST statement, above the existing note-toggle `<script>`. This supplies EasyMDE + `window.markdownAntiforgeryToken` + markdown-editor.js. (The note-toggle `<script>` itself is rewritten in Task 3 — leave it in place for now so this task builds.)

    The `@using QuestBoard.Service.ViewModels.Shared` import was already added in Task 1. Do not alter the note author/timestamp row, the Delete-note form, or the empty-state block.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj && dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~ContactsControllerIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds.
    - `ContactsControllerIntegrationTests` pass (AddNote/EditNote binding + authorization unregressed — this is the guard that the `name="Text"` POST contract is intact).
    - Per-note render is present in both files: `grep -c 'Html.Markdown(note.Text)' QuestBoard.Service/Views/Contacts/Details.cshtml` returns 1 and same for Details.Mobile.cshtml.
    - The per-note-unique id override is present in both files: `grep -c 'ElementId = \$"Text_{note.Id}"' <each Details file>` returns 1.
    - The posted field name is preserved (never made per-note-unique): `grep -c 'FieldName = "Text"' <each Details file>` returns 2 (Add Note + per-note Edit).
    - Both Details files load the editor scripts: `grep -c '_QuestFormScripts.cshtml' <each Details file>` returns 1.
    - The `note-view-text` class is retained on the new `<div>`: `grep -c 'note-view-text' <each Details file>` returns at least 2 (view container + script/CSS references still resolve).
    - No whitespace-preserving inline style remains on either Details view: `grep -c 'pre-wrap' <each Details file>` returns 0.
  </acceptance_criteria>
  <done>Add Note and every per-note Edit form render the shared EasyMDE editor; each note renders independently via its own Html.Markdown call; the posted field name stays "Text" while each note's DOM id is unique; both Details views load _QuestFormScripts; build and Contacts integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the note-toggle script with the D-03 auto-collapse + lazy-init registry, and add the mobile list CSS edits</name>
  <files>QuestBoard.Service/Views/Contacts/Details.cshtml, QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml, QuestBoard.Service/wwwroot/css/contact-detail.mobile.css</files>
  <read_first>
    - QuestBoard.Service/Views/Contacts/Details.cshtml — current note-toggle `<script>` at 180-200 (independent-toggle, no exclusivity, no editor lifecycle)
    - QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml — current note-toggle `<script>` at 164-184 (byte-identical to desktop)
    - .planning/phases/69-contact-fields/69-RESEARCH.md — "Unknown 3 (resolved): D-03 auto-collapse instance registry" (the authoritative design: `noteEditors` Map, `collapseNote`, lazy-init after the container is shown) and "Unknown 2" (init only while visible)
    - .planning/phases/69-contact-fields/69-PATTERNS.md — the "Target — replace with D-03 auto-collapse + lazy-init registry" script block (concrete pattern to implement)
    - .planning/phases/69-contact-fields/69-UI-SPEC.md — "Mobile Description" and "Mobile Notes" typography findings: the two required `li` companion edits to contact-detail.mobile.css
    - QuestBoard.Service/wwwroot/css/contact-detail.mobile.css — the `.contact-detail-card p, .contact-detail-card a, .contact-detail-card span:not(.badge)` catch-all (~lines 77-82) and the `.contact-detail-card .note-item …` scope-out block (~lines 87-92)
  </read_first>
  <action>
    Rewrite the note-toggle script in BOTH Details views (they stay byte-for-byte identical) to add editor lazy-init and D-03 exclusivity, and add the two mobile `li` CSS edits.

    Script rewrite (inside each `@section Scripts`, replacing the current `.note-item` toggle `<script>` — keep the `_QuestFormScripts` render from Task 2 above it):
    - Declare `const noteEditors = new Map();` mapping note id → `{ instance, originalText }`.
    - Define `collapseNote(noteItem)`: read `noteItem.dataset.noteId`; if `noteEditors` has an entry, call `entry.instance.value(entry.originalText)` to revert the visible CodeMirror content to the saved text (this is the unsaved-text discard); then hide `.note-edit-form` and restore `.note-view-text` + `.note-view-actions` display.
    - For each `.note-item`: on its `.note-edit-btn` click — FIRST walk every OTHER `.note-item` whose `.note-edit-form` is currently visible (`style.display !== 'none'`) and call `collapseNote(other)` on it (this is D-03: only one note editor open at a time; opening one collapses and discards any other). THEN hide this note's view text/actions and set its `.note-edit-form` display to block. THEN, only if `noteEditors` does not yet have this note's id, call `initMarkdownEditor(textarea, window.markdownAntiforgeryToken)` on this note's `.markdown-editor-textarea` and store `{ instance, originalText: textarea.value }` in `noteEditors`. Init happens AFTER the form is shown so EasyMDE never measures a `display:none` container (this is why 69-01's eager loop skips hidden textareas — the note ones are lazily created here instead).
    - `.note-cancel-btn` click → `collapseNote(noteItem)` (reuses the same revert-and-hide path).
    Capturing `originalText` from `textarea.value` at first-init is safe: nothing has edited it yet, so it equals the server-rendered `@note.Text`.

    CSS edits to contact-detail.mobile.css (defensive-correctness list rendering, matching the Character WR-02 precedent):
    - Add `li` to the mobile Description catch-all so a Markdown list in Description matches the same full-opacity gold as surrounding paragraphs: extend `.contact-detail-card p, .contact-detail-card a, .contact-detail-card span:not(.badge)` to also include `.contact-detail-card li`.
    - Add `li` to the `.note-item` scope-out block so a list inside a note inherits the note card's plain treatment rather than the parchment catch-all: extend the existing `.contact-detail-card .note-item, .contact-detail-card .note-item p, .contact-detail-card .note-item span:not(.badge)` selector to also include `.contact-detail-card .note-item li`.
    Do not otherwise change the two rules' declarations (color/text-shadow values stay as-is).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds (Razor views compile).
    - The registry + collapse function exist in both Details files: `grep -c 'noteEditors' <each Details file>` returns at least 3 (declare + has + set usages) and `grep -c 'function collapseNote' <each Details file>` returns 1.
    - Lazy-init is wired through the shared initializer: `grep -c 'initMarkdownEditor(textarea, window.markdownAntiforgeryToken)' <each Details file>` returns 1.
    - The unsaved-text revert is present: `grep -c 'instance.value(' <each Details file>` returns 1 (inside collapseNote).
    - The two mobile CSS list edits are present: `grep -c '.contact-detail-card li' QuestBoard.Service/wwwroot/css/contact-detail.mobile.css` returns 1 and `grep -c '.contact-detail-card .note-item li' QuestBoard.Service/wwwroot/css/contact-detail.mobile.css` returns 1.
    - Behavior assertion (verified in the plan 69-03 checkpoint, not automatable here): opening note B's editor while note A's editor is open collapses A and discards A's unsaved text; each note's toolbar renders at full width on first open (no zero-width CodeMirror).
  </acceptance_criteria>
  <done>Both Details views run the D-03 registry: exactly one note editor is open at a time, switching discards unsaved text, and each note's EasyMDE instance is lazily created only when first shown. Mobile CSS renders Markdown list items with correct contrast in both Description and note cards. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contact Description / Note free-text → Details render | Untrusted stored free-text is converted to HTML and shown to every group member viewing the contact |
| Browser → POST /markdown/preview | Per-note and Add-Note Preview toggles send untrusted text for server-side render — now reachable from Details pages for the first time |
| Multiple note edit `<form>`s on one page | Client-side DOM id space shared across N per-note editors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-04 | Tampering | Stored XSS via Description + Note free-text rendered on Details | high | mitigate | All render goes through `Html.Markdown()` → `IMarkdownService.RenderToHtml(..., MarkdownRenderTarget.Web)` (Markdig + HtmlSanitizer Web profile), reused verbatim — same sanitizing choke point already unit-tested in Phase 65 and verified for Character (68-SECURITY.md). No `Html.Raw()` is introduced anywhere in these view edits; per-note rendering is one isolated `Html.Markdown(note.Text)` call per note. |
| T-69-05 | Spoofing | CSRF on POST /markdown/preview now reachable from Details.cshtml / Details.Mobile.cshtml | high | mitigate | Endpoint already enforces `[Authorize]` + `[ValidateAntiForgeryToken]`; `_QuestFormScripts.cshtml` added to both Details views calls `Antiforgery.GetAndStoreTokens` and sets `window.markdownAntiforgeryToken`, which markdown-editor.js sends as the `RequestVerificationToken` header. No new endpoint. |
| T-69-06 | Tampering | Per-note editor DOM id collision misleading which note is edited | low | mitigate | `ElementId = $"Text_{note.Id}"` makes each editor's DOM id + `<label for>` unique; the POST target is decided by which `<form asp-action="EditNote">` (with its own hidden `id`/`contactId`) is submitted, not by the textarea id — so this was never server-exploitable, but the unique id removes user-facing ambiguity. `name="Text"` stays constant so binding is correct. |
| T-69-07 | Information Disclosure | EasyMDE CDN assets now downloaded by every Details visitor (Add Note is visible to any group member) | low | accept | Functional cost, not a leak — no contact data crosses a new boundary; the Add Note form was already visible to any authenticated group member. Recorded for awareness per RESEARCH Pitfall 5. |
| T-69-SC | Tampering | npm/NuGet installs | low | accept | No new packages — EasyMDE/Markdig/Ganss.Xss reused verbatim (slopcheck `[OK]`, Phase 66/68). No install task in this plan. |
</threat_model>

<verification>
- `dotnet build` clean; `dotnet test --filter FullyQualifiedName~ContactsControllerIntegrationTests` green.
- Grep assertions across all three tasks pass.
- Manual behavior checks (formal UAT in 69-03): Description + notes render formatted; unclosed `**bold` in one note does not bleed into the next; D-03 collapse discards unsaved text; note toolbar renders full-width on first open; mobile 320px toolbar fits one row; Markdown list contrast reads correctly in Description and note cards.
- Index views (`Index.cshtml` / `Index.Mobile.cshtml`) are intentionally NOT modified, honoring D-01 (Index is satisfied by click-through to Details; no per-card Description preview).
</verification>

<success_criteria>
- CONTACTMD-01 read half complete: Description renders as formatted HTML on Details desktop + mobile.
- CONTACTMD-02 complete: Add Note and per-note Edit use the shared editor; each note renders independently; D-03 exclusivity with unsaved-text discard works; per-note EasyMDE is lazy-created only when shown.
- Whitespace-preserving inline styles removed; mobile list CSS edits in place.
- D-01 honored (Index untouched); D-02 honored (full toolbar on Add Note immediately); D-03 honored (single open editor).
- Build + Contacts integration tests pass.
</success_criteria>

<output>
Create `.planning/phases/69-contact-fields/69-02-SUMMARY.md` when done.
</output>
