---
phase: 69-contact-fields
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs
  - QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml
  - QuestBoard.Service/wwwroot/js/markdown-editor.js
  - QuestBoard.Service/Views/Contacts/Create.cshtml
  - QuestBoard.Service/Views/Contacts/Create.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Edit.cshtml
  - QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml
autonomous: true
requirements: [CONTACTMD-01]
must_haves:
  truths:
    - "Editing Contact Description on Create, Create.Mobile, Edit, and Edit.Mobile shows the shared Markdown toolbar (Bold, Italic, Heading, List, Link, Blockquote) plus a Preview toggle"
    - "MarkdownEditorViewModel accepts an optional ElementId that overrides the derived DOM id while the posted field name stays FieldName"
    - "markdown-editor.js eager-init skips any textarea whose container is display:none, leaving it for a later reveal handler to lazy-init"
  artifacts:
    - "QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs (new ElementId property)"
    - "QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml (ElementId-aware id derivation)"
    - "QuestBoard.Service/wwwroot/js/markdown-editor.js (visibility guard on eager-init loop)"
    - "QuestBoard.Service/Views/Contacts/Create.cshtml, Create.Mobile.cshtml, Edit.cshtml, Edit.Mobile.cshtml (Description wired to _MarkdownEditor + _QuestFormScripts loader)"
  key_links:
    - "_MarkdownEditor renders textarea name=@Model.FieldName; ElementId changes only the id attribute so Description POST binding is unaffected"
    - "_QuestFormScripts.cshtml supplies EasyMDE CDN assets + window.markdownAntiforgeryToken on each write form"
---

<objective>
Add the one piece of shared infrastructure Contact Notes need (an optional `ElementId` override on `MarkdownEditorViewModel` + a visibility guard in the eager-init loop), then mechanically wire the shared Markdown editor into Contact Description on all four write forms (Create, Create.Mobile, Edit, Edit.Mobile) — a byte-for-byte repeat of Character's Phase 68 pattern.

This is the Wave-1 foundation of CONTACTMD-01: it makes the shared partial/JS safe to render as multiple hidden instances on one page (consumed by plan 69-02's Notes work) and delivers the Description write half of CONTACTMD-01. The read half (Details rendering) and all Notes work land in 69-02.

Purpose: Deliver Contact Description authoring UI and unblock the per-note multi-instance editor infrastructure without touching any existing single-instance call site's behavior.
Output: Additive `ElementId` property, ElementId-aware `_MarkdownEditor.cshtml`, a `display:none`-skipping eager-init loop, and four Contact write forms rendering the EasyMDE editor for Description.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-contact-fields/69-CONTEXT.md
@.planning/phases/69-contact-fields/69-RESEARCH.md
@.planning/phases/69-contact-fields/69-PATTERNS.md
@.planning/phases/69-contact-fields/69-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by THIS plan (source-grounding allowlist for reviewers — these are new, not drift):

- `MarkdownEditorViewModel.ElementId` — new optional `public string? ElementId { get; set; }` property on `QuestBoard.Service.ViewModels.Shared.MarkdownEditorViewModel`. Every existing call site omits it (implicitly null), preserving current behavior.
- `_MarkdownEditor.cshtml` `elementId` local — now derived as `Model.ElementId ?? Model.FieldName.Replace('.', '_')` (behavior change to an existing local, no new public symbol).
- `markdown-editor.js` eager-init loop — now guards each textarea with an `offsetParent === null` visibility check before calling `initMarkdownEditor`. `initMarkdownEditor(textarea, antiforgeryToken)` signature and body are unchanged and still return the constructed EasyMDE instance.

New files: none. New classes/controllers/endpoints: none. No new packages, no migrations.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add ElementId override to MarkdownEditorViewModel + _MarkdownEditor.cshtml, and a visibility guard to markdown-editor.js</name>
  <files>QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs, QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml, QuestBoard.Service/wwwroot/js/markdown-editor.js</files>
  <read_first>
    - QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs (current 5 properties: FieldName, Value, Label, Required, Placeholder)
    - QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml (current `elementId` derivation at the top; textarea uses `name="@Model.FieldName"` and `id="@elementId"`)
    - QuestBoard.Service/wwwroot/js/markdown-editor.js (current DOMContentLoaded loop over `.markdown-editor-textarea`; `initMarkdownEditor` returns `new EasyMDE(...)`)
    - .planning/phases/69-contact-fields/69-RESEARCH.md — "Unknown 1" (why FieldName must stay constant for POST binding) and "Unknown 2" (why hidden-container init must be skipped)
    - .planning/phases/69-contact-fields/69-PATTERNS.md — the three "Required change" excerpts for these exact three files
  </read_first>
  <action>
    Make three small, additive changes to shared editor infrastructure. This is the only infrastructure touched this phase; all other work is call-site wiring.

    (a) MarkdownEditorViewModel.cs — add one nullable property `public string? ElementId { get; set; }` after `Placeholder`. Document with a plain-language comment that it overrides the derived DOM id when multiple instances of the same FieldName render on one page (e.g. per-note inline editors), and that leaving it null falls back to the current FieldName-derived behavior. Do NOT reference phase/plan numbers or requirement IDs in the comment (per CLAUDE.md). Do not change or reorder the existing five properties.

    (b) _MarkdownEditor.cshtml — change the single `elementId` derivation line so it prefers `Model.ElementId` when set, falling back to `Model.FieldName.Replace('.', '_')`. The result is `var elementId = Model.ElementId ?? Model.FieldName.Replace('.', '_');`. Leave the entire rest of the partial byte-for-byte identical — the `<textarea>` MUST keep `name="@Model.FieldName"` (unchanged), only its `id="@elementId"` gains the override behavior. The `<label for="@elementId">` and validation span stay as-is.

    (c) markdown-editor.js — in the `DOMContentLoaded` loop only, add an early return that skips any textarea whose `offsetParent` is null (the standard signal for a `display:none` ancestor) before calling `initMarkdownEditor`. Add a plain-language inline comment stating the hidden textarea is intentionally left for its reveal handler to lazy-init. Do NOT modify `initMarkdownEditor` itself (the EasyMDE constructor, toolbar list, and `/markdown/preview` fetch wiring stay exactly as-is) — it must keep returning the constructed instance so a later caller can retain it.

    Net effect: every existing single-instance, always-visible editor (Quest Description, Character Description/Backstory, and Contact Description wired in Task 2) is unaffected — they pass ElementId=null implicitly and are visible at load (`offsetParent !== null`), so they eager-init exactly as today.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds with no errors.
    - `grep -c 'public string? ElementId' QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs` returns 1.
    - `grep -c 'Model.ElementId ??' QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml` returns 1.
    - `grep -c 'name="@Model.FieldName"' QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml` returns 1 (the posted field name derivation was NOT changed).
    - `grep -c 'offsetParent' QuestBoard.Service/wwwroot/js/markdown-editor.js` returns 1.
    - `grep -c 'return new EasyMDE' QuestBoard.Service/wwwroot/js/markdown-editor.js` returns 1 (initMarkdownEditor still returns the instance).
    - No phase/plan/requirement identifiers appear in the new comments (`grep -nE 'CONTACTMD|Phase 69|69-0|D-0[0-9]' QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs QuestBoard.Service/wwwroot/js/markdown-editor.js` returns nothing).
  </acceptance_criteria>
  <done>MarkdownEditorViewModel exposes an optional ElementId; _MarkdownEditor.cshtml uses it for the id only (FieldName still drives name); markdown-editor.js skips display:none textareas on eager-init; the solution compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Markdown editor into Contact Description on all four write forms</name>
  <files>QuestBoard.Service/Views/Contacts/Create.cshtml, QuestBoard.Service/Views/Contacts/Create.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Edit.cshtml, QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Contacts/Create.cshtml (Description block ~lines 52-56; `@section Scripts` loads cropper + image-crop only)
    - QuestBoard.Service/Views/Contacts/Create.Mobile.cshtml (Description block ~lines 46-50; same Scripts shape)
    - QuestBoard.Service/Views/Contacts/Edit.cshtml (Description block ~lines 60-64; Edit textarea has no placeholder today)
    - QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml (Description block ~lines 53-57)
    - QuestBoard.Service/Views/Characters/Edit.cshtml — lines 1-3 (the `@using QuestBoard.Service.ViewModels.Shared` import), lines 117-124 (the `_MarkdownEditor` partial call for Description), line 197 (`_QuestFormScripts.cshtml` in `@section Scripts`) — this is the exact shipped analog to copy
    - .planning/phases/69-contact-fields/69-UI-SPEC.md — "Description editor field (all 4 write forms)" copy row (Label "Description", Placeholder "Brief contact description", Required=false)
  </read_first>
  <action>
    In each of the four Contact write-form views, replace the existing plain Description block (a `<div class="mb-3">` wrapping a `<textarea>` model-bound to Description via the `asp-for` tag helper) with a render of the shared `_MarkdownEditor` partial, and ensure each view loads the editor scripts. Mirror Character's Edit.cshtml exactly.

    For each of the four files:
    1. Add `@using QuestBoard.Service.ViewModels.Shared` to the top `@using` block (currently only `@using QuestBoard.Service.ViewModels.ContactViewModels` is present — this namespace is NOT in _ViewImports, so the per-view import is required to reference `MarkdownEditorViewModel`).
    2. Replace the Description field block with a call to `Html.RenderPartialAsync("_MarkdownEditor", new MarkdownEditorViewModel { ... })` passing: `FieldName = "Description"`, `Value = Model.Description`, `Label = "Description"`, `Required = false`, `Placeholder = "Brief contact description"`. Omit `ElementId` (single instance — falls back to id `"Description"`). Use the exact `@{ await Html.RenderPartialAsync(...); }` form Character's Edit.cshtml uses. Applying the same placeholder to the two Edit forms (which have none today) is intentional — it closes the pre-existing 2-of-4 placeholder inconsistency as a byproduct, matching Character's Phase 68 precedent.
    3. In each view's existing `@section Scripts` block, add `@{ await Html.RenderPartialAsync("~/Views/Quest/_QuestFormScripts.cshtml"); }` as the FIRST statement inside the block, above the existing cropper/image-crop `<script>` tags. This partial loads the EasyMDE CDN CSS/JS, sets `window.markdownAntiforgeryToken`, and includes markdown-editor.js — all required for the Description editor to initialize. Do NOT remove or reorder the existing cropper `<script src=...>` / `initImageCrop(...)` lines; the crop UI must keep working.

    Do not touch Name/TownCity/SubLocation/image fields. The Description editor's toolbar-disable-during-preview, 44px touch targets, and paragraph-break tooltip are all inherited from the shared partial + global CSS — no per-view styling is added.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj && dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~ContactsControllerIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds.
    - Existing `ContactsControllerIntegrationTests` pass (no regression to AddNote/EditNote/Details authorization from the view edits).
    - Each of the four files contains the partial call: `grep -rc 'RenderPartialAsync("_MarkdownEditor"' QuestBoard.Service/Views/Contacts/Create.cshtml QuestBoard.Service/Views/Contacts/Create.Mobile.cshtml QuestBoard.Service/Views/Contacts/Edit.cshtml QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml` returns 1 for each file.
    - Each of the four files imports the shared namespace: `grep -rc 'QuestBoard.Service.ViewModels.Shared' <same four files>` returns 1 for each.
    - Each of the four files loads the editor scripts: `grep -rc '_QuestFormScripts.cshtml' <same four files>` returns 1 for each.
    - `FieldName = "Description"` appears in each of the four files (posted field name preserved for model binding).
    - The old plain Description textarea is gone: `grep -c 'asp-for="Description"' <each file>` returns 0 for all four.
  </acceptance_criteria>
  <done>All four Contact write forms render the shared EasyMDE editor for Description with the correct label/placeholder/required flag, load _QuestFormScripts, keep the crop UI intact, compile, and pass the existing Contacts integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → ContactsController (Create/Edit POST) | Authenticated group member submits untrusted Description free-text |
| Browser → MarkdownController (POST /markdown/preview) | Editor Preview toggle sends untrusted Description text for server-side render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Tampering | Contact Description free-text (write side) | high | mitigate | Write forms only collect/store raw text; no rendering sink is added here. All display is sanitized by `IMarkdownService.RenderToHtml(..., Web)` in plan 69-02. No `Html.Raw` introduced on any write form. Server-side `[StringLength(2000)]` on `ContactViewModel.Description` is unchanged. |
| T-69-02 | Spoofing | POST /markdown/preview reachable from Contact write forms | medium | mitigate | Endpoint already enforces `[Authorize]` + `[ValidateAntiForgeryToken]`; `window.markdownAntiforgeryToken` is supplied by `_QuestFormScripts.cshtml` added to all four forms. No new endpoint or auth change. |
| T-69-03 | Tampering | `_MarkdownEditor.cshtml` id-override change affecting existing call sites | low | accept | `ElementId` is additive and null at every existing call site; the `name` attribute derivation is untouched, so no existing form's POST binding changes. Verified by acceptance grep asserting `name="@Model.FieldName"` unchanged. |
| T-69-SC | Tampering | npm/NuGet installs | low | accept | No new packages this phase — Markdig/Ganss.Xss/EasyMDE reused verbatim (slopcheck `[OK]` recorded in Phase 66/68). No install task exists in this plan. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` is clean.
- `dotnet test --filter FullyQualifiedName~ContactsControllerIntegrationTests` is green (authorization regression guard).
- Grep assertions in each task's acceptance_criteria pass.
- No existing single-instance editor call site (Quest/Character) changed behavior: `_MarkdownEditor.cshtml` still emits `name="@Model.FieldName"`; markdown-editor.js still eager-inits visible textareas.
</verification>

<success_criteria>
- MarkdownEditorViewModel exposes an optional `ElementId`; `_MarkdownEditor.cshtml` uses it for the DOM id only.
- markdown-editor.js skips `display:none` textareas during eager init.
- Contact Create/Create.Mobile/Edit/Edit.Mobile render the shared Markdown editor for Description with toolbar + Preview, loading `_QuestFormScripts.cshtml`.
- Build passes; Contacts integration tests pass; crop UI still functions on all four forms.
</success_criteria>

<output>
Create `.planning/phases/69-contact-fields/69-01-SUMMARY.md` when done.
</output>
