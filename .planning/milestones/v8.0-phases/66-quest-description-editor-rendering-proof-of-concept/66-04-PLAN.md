---
phase: 66-quest-description-editor-rendering-proof-of-concept
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/wwwroot/js/markdown-editor.js
  - QuestBoard.Service/wwwroot/css/markdown-editor.css
  - QuestBoard.Service/wwwroot/css/markdown-content.css
  - QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs
  - QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml
  - QuestBoard.Service/wwwroot/js/site.js
autonomous: true
requirements: [EDITOR-01, EDITOR-02, EDITOR-03, EDITOR-05, EDITOR-06]
user_setup: []

must_haves:
  truths:
    - "A textarea marked with class markdown-editor-textarea is upgraded to an EasyMDE editor with a Bold/Italic/Heading/List/Link/Blockquote/Preview toolbar"
    - "Clicking Preview fetches server-rendered HTML from /markdown/preview and EasyMDE disables the rest of the toolbar while active"
    - "EasyMDE toolbar buttons are at least 44px with 0 gap and 0 wrapper padding so 7 fit one row at 320px; toolbar chrome keeps EasyMDE's default appearance, no restyle (D-03)"
    - "An info-circle icon next to the field label carries the exact blank-line-paragraph hint text for a Bootstrap tooltip, placed next to the label per D-08, wording locked by D-09"
    - "Rendered .markdown-content headings collapse to one restrained tier and use parchment-gold color on the dark description box"
  artifacts:
    - path: "QuestBoard.Service/wwwroot/js/markdown-editor.js"
      provides: "Class-selector-driven EasyMDE init with async server preview"
      contains: "querySelectorAll"
    - path: "QuestBoard.Service/wwwroot/css/markdown-editor.css"
      provides: "44px touch-target + 0-gap toolbar override"
      contains: "min-width: 44px"
    - path: "QuestBoard.Service/wwwroot/css/markdown-content.css"
      provides: "Rendered-Markdown typography (heading collapse, blockquote, gold color)"
      contains: ".markdown-content"
    - path: "QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml"
      provides: "Shared write-side partial (label + info hint + editor textarea + validation)"
      contains: "markdown-editor-textarea"
    - path: "QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs"
      provides: "Partial contract usable across all 3 write forms' differing field names"
      contains: "FieldName"
  key_links:
    - from: "QuestBoard.Service/wwwroot/js/markdown-editor.js"
      to: "/markdown/preview"
      via: "previewRender fetch with RequestVerificationToken header"
      pattern: "markdown/preview"
    - from: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      to: "font-awesome v4-shims + markdown css"
      via: "global link tags next to the existing FA6 link"
      pattern: "v4-shims"
    - from: "QuestBoard.Service/wwwroot/js/site.js"
      to: "bootstrap.Tooltip"
      via: "one-time DOMContentLoaded init over data-bs-toggle tooltip elements"
      pattern: "bootstrap.Tooltip"
---

<objective>
Build the shared, reusable Markdown editor client assets that all three write forms (66-05) and every future field phase (67–70) consume: the EasyMDE init module, the shared editor partial + its view model, the touch-target/fit CSS, the rendered-content typography CSS, the Font Awesome v4-shim for EasyMDE's default icons, and the app-wide Bootstrap tooltip init for the paragraph-break hint.

Purpose: Centralize every editor mechanic (toolbar, server preview, disable-during-preview, 44px targets, hint) in one place so form wiring (66-05) and read rendering (66-06) are pure consumption. Editor chrome stays EasyMDE-default per D-03 — only sizing/fit is overridden.
Output: markdown-editor.js, markdown-editor.css, markdown-content.css, MarkdownEditorViewModel, _MarkdownEditor.cshtml, FA v4-shim + CSS links in both layouts, tooltip init in site.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-UI-SPEC.md

<interfaces>
<!-- Locked facts the executor must honor. -->
- EasyMDE toolbar action names (verified): Blockquote is internally "quote", NOT "blockquote". Use the array ["bold","italic","heading","unordered-list","link","quote","preview"].
- previewRender(plainText, previewElement) is async; fetch /markdown/preview, set previewElement.innerHTML, and return previewElement.innerHTML or a loading placeholder synchronously.
- asp-for generates different ids per form (Description vs Quest_Description) — target editors by CLASS (.markdown-editor-textarea), never by id.
- EasyMDE default buttons are 30px (below 44px); its default icons are FA4-era (fa fa-bold) needing the FA6 v4-shim to render.
- FA v4-shim CDN (SRI computed 2026-07-09): url https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/v4-shims.min.css ; integrity sha384-TjXU13dTMPo+5ZlOUI1IGXvpmajjoetPqbUJqTx+uZ1bGwylKHNEItuVe/mg/H6l ; crossorigin anonymous. Re-verify the hash if the stylesheet fails to load.
- Tooltip wording (D-09, exact): "Supports Markdown formatting. Leave a blank line between paragraphs."
- Existing FA6 link is _Layout.cshtml line 13 / _Layout.Mobile.cshtml equivalent; the site-css link block ends around line 22 (image-crop.css).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: markdown-editor.js (EasyMDE init + async server preview) and markdown-editor.css (touch-target + fit)</name>
  <files>QuestBoard.Service/wwwroot/js/markdown-editor.js, QuestBoard.Service/wwwroot/css/markdown-editor.css</files>
  <read_first>
    - QuestBoard.Service/wwwroot/js/image-crop.js (file-level comment convention + safe-no-op-if-markup-absent init shape to mirror)
    - QuestBoard.Service/wwwroot/css/image-crop.css (narrow-scope "not restyling the widget" comment convention to mirror)
    - QuestBoard.Service/wwwroot/css/mobile.css (existing global 44px rule that does NOT reach .editor-toolbar button, so this override is additive)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md (Pattern 1 exact EasyMDE config + Pitfall 4)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-UI-SPEC.md (Spacing exceptions: 44px, 0 gap, 0 wrapper padding)
  </read_first>
  <action>
    Create markdown-editor.js (no-module, no-bundler, matching site.js) with a file-level why-comment mirroring image-crop.js. Define initMarkdownEditor(textarea, antiforgeryToken) returning a new EasyMDE instance configured with autoDownloadFontAwesome false, spellChecker false, status false, and toolbar array ["bold","italic","heading","unordered-list","link","quote","preview"] — use the literal string quote for Blockquote (blockquote silently renders no button). Add an async previewRender(plainText, previewElement) that POSTs to /markdown/preview with headers Content-Type application/json and RequestVerificationToken set to antiforgeryToken, body JSON.stringify of an object with a markdown property equal to plainText, then sets previewElement.innerHTML to the response text; on failure sets it to the literal markup p with class text-danger reading "Preview failed to load."; and synchronously returns previewElement.innerHTML or a "Loading preview" placeholder. On DOMContentLoaded, run document.querySelectorAll('.markdown-editor-textarea').forEach and init each element with window.markdownAntiforgeryToken — a safe no-op when no matching elements exist. Do NOT use getElementById.
    Create markdown-editor.css with a comment header mirroring image-crop.css (not restyling the widget, only touch-target sizing and single-row fit for the locked requirement). Include ONLY: a rule setting .editor-toolbar button to min-width 44px, min-height 44px, margin 0; and a rule setting .editor-toolbar to padding 0. Sizing/spacing only — no color, border, or icon changes (D-03). The 0 margin/padding is required so 7 x 44px = 308px fits inside a 320px viewport.
  </action>
  <verify>
    <automated>grep -q "querySelectorAll" QuestBoard.Service/wwwroot/js/markdown-editor.js && grep -q "markdown/preview" QuestBoard.Service/wwwroot/js/markdown-editor.js && grep -q '"quote"' QuestBoard.Service/wwwroot/js/markdown-editor.js && grep -q "min-width: 44px" QuestBoard.Service/wwwroot/css/markdown-editor.css</automated>
  </verify>
  <acceptance_criteria>
    - markdown-editor.js contains the toolbar array with the literal `"quote"` (not `"blockquote"`) and `"preview"`
    - markdown-editor.js contains `fetch` to `/markdown/preview` with a `RequestVerificationToken` header and reads `window.markdownAntiforgeryToken`
    - markdown-editor.js initializes via `querySelectorAll('.markdown-editor-textarea')`, not `getElementById`
    - markdown-editor.css contains `.editor-toolbar button` with `min-width: 44px`, `min-height: 44px`, `margin: 0` and `.editor-toolbar` with `padding: 0`; contains no color/border/icon declarations
  </acceptance_criteria>
  <done>The EasyMDE init module and touch-target CSS exist; any .markdown-editor-textarea will become a 7-button editor with a server-round-trip preview and 44px single-row toolbar once wired.</done>
</task>

<task type="auto">
  <name>Task 2: markdown-content.css (rendered-output typography) + FA v4-shim and CSS links in both layouts</name>
  <files>QuestBoard.Service/wwwroot/css/markdown-content.css, QuestBoard.Service/Views/Shared/_Layout.cshtml, QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/quests.css (the .quest-description-box rule — copy its exact text color #F4E4BC and its text-shadow value for the heading override; the global h1-h6 rule this must scope-override)
    - QuestBoard.Service/wwwroot/css/site.css (global h1-h6 color rule around line 586 that .markdown-content headings must override in-scope)
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (the FA6 <link> at line 13 and the site-css <link> block through line 22)
    - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml (its equivalent head <link> block)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-UI-SPEC.md (Typography + Color tables — exact heading/blockquote/link decisions)
  </read_first>
  <action>
    Create markdown-content.css scoping ALL rules under .markdown-content: body text at 1rem, weight 400, line-height 1.6; strong at weight 700; h1 through h6 collapsed to a single tier at 1.25rem, weight 700, line-height 1.2, color #F4E4BC, reusing the exact text-shadow value found on .quest-description-box (read quests.css) — this scoped rule intentionally overrides the global site.css h1-h6 color so markdown headings are not near-invisible dark-brown on the dark overlay box; blockquote with border-left 3px solid rgba(255, 193, 7, 0.5), padding-left 12px, font-style italic. Do NOT add any a / a:hover rule — links keep Bootstrap default styling per UI-SPEC.
    In _Layout.cshtml, immediately after the existing Font Awesome 6.4.0 all.min.css link (line 13), add the FA v4-shims link (url, integrity sha384-TjXU13dTMPo+5ZlOUI1IGXvpmajjoetPqbUJqTx+uZ1bGwylKHNEItuVe/mg/H6l, crossorigin anonymous) so EasyMDE's default fa fa-* icons render. Then in the site-stylesheet block (after the image-crop.css link near line 22), add two links: ~/css/markdown-editor.css and ~/css/markdown-content.css, both with asp-append-version true. Make the identical three additions in _Layout.Mobile.cshtml's head. Re-verify the v4-shims SRI hash if the icons render as blank/tofu boxes at verification time.
  </action>
  <verify>
    <automated>grep -q "v4-shims" QuestBoard.Service/Views/Shared/_Layout.cshtml && grep -q "v4-shims" QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml && grep -q "markdown-content.css" QuestBoard.Service/Views/Shared/_Layout.cshtml && grep -q "markdown-editor.css" QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml && grep -q ".markdown-content" QuestBoard.Service/wwwroot/css/markdown-content.css</automated>
  </verify>
  <acceptance_criteria>
    - markdown-content.css contains `.markdown-content h1` (or a grouped `.markdown-content h1, ...h6`) with `color: #F4E4BC` and a `text-shadow`
    - markdown-content.css contains `.markdown-content blockquote` with `border-left` using `rgba(255, 193, 7, 0.5)`
    - markdown-content.css contains no `a {` / `a:hover` override
    - Both _Layout.cshtml and _Layout.Mobile.cshtml contain a `v4-shims` link with the given `integrity` hash, plus `~/css/markdown-editor.css` and `~/css/markdown-content.css` links
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>Rendered Markdown has restrained, theme-consistent typography; EasyMDE's default icons have their FA6 compatibility shim; both new stylesheets load globally on desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 3: MarkdownEditorViewModel + _MarkdownEditor.cshtml partial + Bootstrap tooltip init in site.js</name>
  <files>QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs, QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml, QuestBoard.Service/wwwroot/js/site.js</files>
  <read_first>
    - QuestBoard.Service/ViewModels/CharacterViewModels/ (an existing ViewModel file for the exact namespace/style convention this project uses)
    - QuestBoard.Service/Views/Quest/Create.cshtml (the existing Description field block: label + required asterisk + textarea + asp-validation-for — the markup this partial replaces)
    - QuestBoard.Service/wwwroot/js/site.js (the existing DOMContentLoaded block around line 273 and the toast-init querySelectorAll('.toast') pattern near line 306 to mirror)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md (_MarkdownEditor pattern assignment + tooltip markup guidance)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md (D-08/D-09 hint placement and exact wording)
  </read_first>
  <action>
    Create MarkdownEditorViewModel.cs in QuestBoard.Service/ViewModels/Shared (namespace QuestBoard.Service.ViewModels.Shared) with properties: string FieldName (the model-binding name, e.g. "Description" or "Quest.Description"), string? Value, string Label, bool Required, string? Placeholder.
    Create _MarkdownEditor.cshtml as a strongly-typed partial with model MarkdownEditorViewModel. Compute the element id as FieldName with dots replaced by underscores (matching ASP.NET Core's id generation). Render, inside a div with class mb-3: a label (for the computed id) containing the Label text, then an info-circle icon i with classes fas fa-info-circle text-warning ms-1, attribute data-bs-toggle="tooltip", and title exactly "Supports Markdown formatting. Leave a blank line between paragraphs."; then, only when Required, a span with class text-danger containing an asterisk. Then a textarea with name set to FieldName, id set to the computed id, classes form-control markdown-editor-textarea, rows 4, placeholder set to Placeholder, and the Value as its inner content. Then a validation span with class text-danger, data-valmsg-for set to FieldName, and data-valmsg-replace true (this reproduces asp-validation-for so server-side validation errors still display; client-side unobtrusive validation on the original textarea is moot because EasyMDE hides it, and server-side [Required] remains authoritative). Do NOT use asp-for on the textarea — the explicit name/id is what lets one partial serve all three forms' differing field names.
    In site.js, add a one-time Bootstrap tooltip init: within a DOMContentLoaded handler (either extend the existing block near line 273 or add a sibling handler modeled on the toast-init block), iterate document.querySelectorAll('[data-bs-toggle="tooltip"]') and construct a new bootstrap.Tooltip for each element. This is the app's first tooltip usage; Bootstrap's JS bundle is already loaded.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service && grep -q "markdown-editor-textarea" QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml && grep -q "Leave a blank line between paragraphs" QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml && grep -q "bootstrap.Tooltip" QuestBoard.Service/wwwroot/js/site.js && grep -q "FieldName" QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs</automated>
  </verify>
  <acceptance_criteria>
    - MarkdownEditorViewModel.cs declares FieldName, Value, Label, Required, Placeholder in namespace `QuestBoard.Service.ViewModels.Shared`
    - _MarkdownEditor.cshtml renders a `textarea` with `name="@Model.FieldName"`, class `markdown-editor-textarea`, and NO `asp-for`
    - _MarkdownEditor.cshtml contains an `i` with `data-bs-toggle="tooltip"` and the exact title `Supports Markdown formatting. Leave a blank line between paragraphs.`
    - _MarkdownEditor.cshtml contains a validation span with `data-valmsg-for="@Model.FieldName"`
    - site.js contains `bootstrap.Tooltip` iterating `[data-bs-toggle="tooltip"]`
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>The shared editor partial (label + tooltip hint + editor-ready textarea + validation) and its view model exist, and Bootstrap tooltips initialize app-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | Third-party EasyMDE + FA v4-shim assets loaded from public CDNs |
| browser preview fetch → server | previewRender sends author text to /markdown/preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-09 | Tampering (rendered preview HTML) | previewElement.innerHTML | mitigate | Preview HTML comes only from /markdown/preview, which returns Phase-65-sanitized output; the client never renders raw author input, only the server's sanitized response |
| T-66-SC | Tampering (supply chain) | EasyMDE + FA v4-shims CDN assets | mitigate | Both loaded with SRI integrity hashes + crossorigin (fail-closed on tampering), version-pinned. EasyMDE slopcheck [OK] and FA v4-shim SRI verified in 66-RESEARCH.md — not [ASSUMED]/[SUS], no blocking checkpoint. EasyMDE CDN script/link tags themselves are added in 66-05 |
| T-66-10 | Spoofing (CSRF on preview) | previewRender fetch | mitigate | Sends the RequestVerificationToken header sourced from window.markdownAntiforgeryToken (set per-form in 66-05); endpoint enforces [ValidateAntiForgeryToken] (66-02) |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` succeeds
- grep confirms: `"quote"` toolbar entry, `markdown/preview` fetch, `querySelectorAll('.markdown-editor-textarea')`, `min-width: 44px`, `v4-shims` in both layouts, exact tooltip wording, `bootstrap.Tooltip`
- markdown-editor.css contains no color/border/icon declarations (D-03 sizing-only)
</verification>

<success_criteria>
- EasyMDE init module, editor-chrome CSS, rendered-content CSS, shared partial + view model all exist
- FA v4-shim + both stylesheets load globally in desktop and mobile layouts
- Bootstrap tooltips initialize app-wide with the exact locked hint wording
- No restyling of EasyMDE chrome beyond sizing (D-03)
</success_criteria>

<output>
Create `.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-04-SUMMARY.md` when done
</output>
