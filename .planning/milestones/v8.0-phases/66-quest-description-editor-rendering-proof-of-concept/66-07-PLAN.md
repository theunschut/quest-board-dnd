---
phase: 66-quest-description-editor-rendering-proof-of-concept
plan: 07
type: execute
wave: 3
depends_on: [66-03, 66-05, 66-06]
files_modified: []
autonomous: false
requirements: [EDITOR-01, EDITOR-02, EDITOR-03, EDITOR-04, EDITOR-05, EDITOR-06, QUESTMD-01]
user_setup: []

must_haves:
  truths:
    - "The Quest Description editor shows a 7-control toolbar whose icons actually render (Font Awesome v4-shim working), and the buttons insert Markdown syntax at the cursor/selection"
    - "Preview toggles to rendered HTML matching saved output, with the rest of the toolbar genuinely disabled while active"
    - "At a 320px viewport the toolbar fits one row with 44px+ targets and no overflow/scroll"
    - "The info-circle hint next to the Description label shows the exact blank-line-paragraph wording"
    - "Quest Description renders formatted on Details and Manage, plain-text-stripped on the board card, and as formatted HTML in the Quest Finalized email"
  artifacts:
    - path: "QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml"
      provides: "The editor surface being visually verified"
      contains: "markdown-editor-textarea"
    - path: "QuestBoard.Service/Views/Quest/Details.cshtml"
      provides: "The read surface being visually verified"
      contains: "Html.Markdown"
  key_links:
    - from: "human verifier"
      to: "full write→read→email loop"
      via: "manual browser check at desktop + 320px mobile"
      pattern: "Html.Markdown"
---

<objective>
Human verification gate for the proof-of-concept loop. Everything in 66-01 through 66-06 is automated where this codebase has test tooling; the remaining EDITOR-01/02/03/05/06 behaviors and the FA-icon/44px/320px-fit questions are, per 66-RESEARCH.md, manual-only (no JS test tooling exists here). This plan has the operator confirm the write→read→email loop in a real browser before the phase closes.

Purpose: Close the requirements that only a rendered browser can prove, and surface the one known risk (7×44px toolbar at 320px, 66-RESEARCH.md Open Question #1) as a blocking check rather than a silent workaround.
Output: Operator sign-off (or a recorded blocking finding to feed gap closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-UI-SPEC.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the Quest Description editor (desktop + 320px mobile)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-UI-SPEC.md (Spacing exceptions + Copywriting: expected toolbar/hint behavior)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md (Pitfall 3 FA icons, Pitfall 4/5 44px + 320px fit, Open Question #1)
  </read_first>
  <action>
    Guide the operator through the how-to-verify steps below in a real browser (desktop + a 320px DevTools viewport) and block on the resume-signal. This task writes no code — it confirms the manual-only EDITOR-01/02/03/05/06 behaviors and the FA-icon/44px/320px-fit questions that this codebase has no automated tooling to check. If the 320px toolbar overflows, record it as a blocking finding for gap closure rather than shrinking buttons below 44px or dropping a button.
  </action>
  <what-built>
    EasyMDE editor wired into Quest Description on all three write forms (Create/Edit/Follow-Up, desktop + mobile) with a 7-control toolbar (Bold, Italic, Heading, List, Link, Blockquote, Preview), a server round-trip Preview (POST /markdown/preview), an info-circle paragraph-break hint next to the Description label, and a 44px/0-gap touch-target CSS override. FA6 v4-shim added so EasyMDE's default icons render.
  </what-built>
  <how-to-verify>
    1. Start the app: `dotnet run --project QuestBoard.Service` and sign in.
    2. Open Create Quest (desktop browser). Confirm the Description field shows the EasyMDE toolbar with SEVEN visible icon buttons — none rendered as blank/tofu boxes (this proves the FA v4-shim works; if any icon is a blank square, re-verify the v4-shims SRI/link per 66-RESEARCH.md Pitfall 3).
    3. Select some text and click Bold, then Italic, then Heading, then List, then Link, then Blockquote — confirm each inserts the corresponding Markdown syntax around the selection or at the cursor.
    4. Click Preview — confirm the pane switches to rendered HTML and the OTHER toolbar buttons become non-clickable (not merely greyed): try clicking Bold while in Preview and confirm nothing happens. Toggle Preview off again.
    5. Hover (desktop) / tap (mobile) the info-circle icon next to the "Description" label — confirm the tooltip reads exactly: "Supports Markdown formatting. Leave a blank line between paragraphs."
    6. Open Chrome DevTools device toolbar at a 320px-wide viewport (iPhone SE preset). Reload the mobile Create Quest form. Confirm all 7 toolbar buttons sit in ONE row with NO horizontal scroll/overflow and each button is at least ~44px tall/wide. If it overflows, DO NOT shrink buttons below 44px or drop a button — report it as a blocking finding for gap closure (66-RESEARCH.md Open Question #1 / UI-SPEC).
    7. Confirm the same editor appears on Edit Quest and Create Follow-Up (the form that previously had no editor wiring), desktop and mobile.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all 7 items above pass, or records the specific failing check (especially any 320px overflow).</human-check>
  </verify>
  <acceptance_criteria>
    - All 7 toolbar icons render as glyphs (no blank/tofu boxes) — EDITOR-01
    - Each formatting button inserts Markdown syntax at the selection/cursor — EDITOR-01
    - Preview toggles to rendered HTML and genuinely disables the rest of the toolbar — EDITOR-02, EDITOR-03
    - The hint tooltip shows the exact locked wording — EDITOR-05
    - At 320px the 7 buttons fit one row with 44px+ targets and no overflow — EDITOR-06 (or a blocking finding is recorded)
    - The editor is present on Create, Edit, and Follow-Up (desktop + mobile) — QUESTMD-01 (write half)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe the failing check (especially any 320px overflow) to route to gap closure.</resume-signal>
  <done>Operator has confirmed the editor behaviors on desktop and 320px mobile, or recorded a blocking finding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Description rendering on board card, Details, Manage, and the Quest Finalized email</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md (D-06 board card plain text; D-02 Manage collapsed-by-default)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-UI-SPEC.md (Typography for rendered .markdown-content)
  </read_first>
  <action>
    Guide the operator through the how-to-verify steps below and block on the resume-signal. This task writes no code — it confirms the read + email render surfaces (board card plain text per D-06, Details/Manage formatted HTML, preview-matches-saved per EDITOR-04, and the Quest Finalized email). Any mismatch routes to gap closure.
  </action>
  <what-built>
    Read-side rendering: Details (desktop + mobile) and a new collapsed-by-default Manage section render Description as formatted HTML via Html.Markdown; the desktop board card shows Description as Markdown-stripped plain text feeding poster selection; the Quest Finalized email renders Description as formatted HTML (images stripped). EDITOR-04 is guaranteed by construction (preview and page display both use RenderToHtml Web).
  </what-built>
  <how-to-verify>
    1. Create (or edit) a One-Shot quest with a Markdown Description that uses a heading, a bold phrase, a bullet list, and a blockquote. Save it.
    2. Board card (desktop /quests): confirm the Description on the card is PLAIN TEXT — heading/bold/list markers stripped, no `#`/`**`/`- ` symbols showing — matching today's plain look (D-06).
    3. Quest Details (desktop + mobile): confirm the Description renders as FORMATTED HTML — visible heading, bold, bullets, blockquote — with parchment-gold heading color on the dark box, and NO doubled/blank-line spacing between paragraphs. Confirm the Rewards field (if set) still shows its plain-text line breaks (unchanged).
    4. Quest Manage: confirm a "Description" section (scroll icon + chevron) is present and COLLAPSED by default; expand it and confirm the same formatted HTML.
    5. Back on the editor, type the same Markdown and click Preview — confirm the preview looks identical to the saved Details rendering (EDITOR-04).
    6. Email: as SuperAdmin, open /EmailPreview/QuestFinalized and confirm the Description paragraph renders as formatted HTML (no raw `**`/`#` artifacts). Note: the automated test in 66-03 already proves bold→<strong> and image-stripping; real Outlook/Gmail rendering is deliberately Phase 71.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms board card is plain text, Details/Manage render formatted HTML with no doubled spacing, preview matches saved output, and the Quest Finalized email preview shows formatted HTML.</human-check>
  </verify>
  <acceptance_criteria>
    - Board card Description is Markdown-stripped plain text — QUESTMD-01 (card), D-06
    - Details (desktop + mobile) renders formatted HTML with no doubled spacing; Rewards unchanged — QUESTMD-01 (Details)
    - Manage has a collapsed-by-default Description section that expands to formatted HTML — QUESTMD-01 (Manage), D-02
    - Preview visually matches the saved Details rendering — EDITOR-04
    - Quest Finalized email preview renders Description as formatted HTML — ROADMAP criterion #5
  </acceptance_criteria>
  <resume-signal>Type "approved" if all render surfaces are correct, or describe the mismatch to route to gap closure.</resume-signal>
  <done>Operator has confirmed all Description render surfaces (card, Details, Manage, email) or recorded a mismatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → rendered app | Human visual inspection is the verification surface; no new code or input boundary introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-15 | (verification only) | Manual browser check | accept | This plan writes no code and introduces no new trust boundary; it confirms the mitigations built in 66-01…66-06 hold in a real browser. Any failure routes to gap closure rather than being worked around |
</threat_model>

<verification>
- Operator confirms both checkpoint tasks (or records a blocking finding)
- The 320px toolbar-fit result is explicitly recorded (pass or blocking finding), never silently worked around
</verification>

<success_criteria>
- All manual-only EDITOR requirements visually confirmed on desktop and 320px mobile
- Full write→read→email loop confirmed for Quest Description
- Any 320px overflow surfaced as a blocking finding, not papered over
</success_criteria>

<output>
Create `.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-07-SUMMARY.md` when done
</output>
