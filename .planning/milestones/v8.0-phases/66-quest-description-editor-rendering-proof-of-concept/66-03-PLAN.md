---
phase: 66-quest-description-editor-rendering-proof-of-concept
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.IntegrationTests/Emails/QuestFinalizedMarkdownRenderTests.cs
  - QuestBoard.Service/Components/Emails/QuestFinalized.razor
autonomous: true
requirements: [QUESTMD-01]
user_setup: []

must_haves:
  truths:
    - "The Quest Finalized email renders a Markdown Quest Description as formatted HTML (bold/lists/headings), not raw Markdown syntax"
    - "Images in Quest Description are stripped from the email body (MarkdownRenderTarget.Email)"
  artifacts:
    - path: "QuestBoard.Service/Components/Emails/QuestFinalized.razor"
      provides: "IMarkdownService-rendered Description in the email body"
      contains: "MarkdownRenderTarget.Email"
    - path: "QuestBoard.IntegrationTests/Emails/QuestFinalizedMarkdownRenderTests.cs"
      provides: "Automated proof the email renders Description markdown as HTML with images stripped"
      min_lines: 25
  key_links:
    - from: "QuestBoard.Service/Components/Emails/QuestFinalized.razor"
      to: "IMarkdownService"
      via: "@inject IMarkdownService + MarkupString RenderToHtml(QuestDescription, Email)"
      pattern: "@inject IMarkdownService"
---

<objective>
Wire the shared `IMarkdownService` into the Quest Finalized email so Quest Description renders as formatted HTML (images stripped) instead of HTML-encoded raw Markdown — the email half of the proof-of-concept write→read→email loop (ROADMAP Phase 66 success criterion #5).

Purpose: Proves the Blazor-email adapter half of the "one core, two adapters" architecture — the first `@inject` anywhere in `Components/Emails/`. Only Quest Finalized is wired this phase; Session Reminder and Waitlist Promoted are Phase 67 (EMAILMD-01).
Output: `QuestFinalized.razor` renders Description via `RenderToHtml(..., MarkdownRenderTarget.Email)`, plus an integration test proving it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md

<interfaces>
<!-- Shipped by Phase 65. Do not modify. -->
From QuestBoard.Domain/Interfaces/IMarkdownService.cs:
- string RenderToHtml(string? markdown, MarkdownRenderTarget target) — Email target strips <img>; registered AddSingleton (resolves from any scope).

Email render harness (verified this session):
- QuestBoard.Service/Services/RazorEmailRenderService.cs: `Task<string> RenderAsync<TComponent>(Dictionary<string, object?> parameters) where TComponent : IComponent`, registered as IEmailRenderService.
- HtmlRenderer is built from the real app IServiceProvider, so @inject of a singleton resolves.
- QuestFinalized.razor parameters (see EmailPreviewController.QuestFinalized): QuestTitle, DmName, QuestDate, QuestDescription, ConfirmedPlayerNames, QuestUrl, ChallengeRating, AppUrl.
- QuestFinalized.razor line 41 currently: `<p style="...">@QuestDescription</p>` (HTML-encoded raw text).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing integration test — Quest Finalized email renders Description markdown as HTML, images stripped (RED)</name>
  <files>QuestBoard.IntegrationTests/Emails/QuestFinalizedMarkdownRenderTests.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs (the full parameter dictionary for RenderAsync<QuestFinalized> — mirror it, overriding only QuestDescription)
    - QuestBoard.Service/Services/RazorEmailRenderService.cs (RenderAsync signature + IEmailRenderService)
    - QuestBoard.IntegrationTests/Controllers/QuestFinalizedEditTests.cs (WebApplicationFactoryBase fixture usage + IClassFixture pattern in this project)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md (QuestFinalized.razor pattern assignment)
  </read_first>
  <action>
    Create `QuestFinalizedMarkdownRenderTests.cs` in a new `QuestBoard.IntegrationTests/Emails/` folder, using the existing `WebApplicationFactoryBase` fixture via `IClassFixture`. In the test: create a DI scope from `factory.Services`, resolve `IEmailRenderService`, and call `RenderAsync<QuestBoard.Service.Components.Emails.QuestFinalized>(...)` with a parameter dictionary mirroring EmailPreviewController.QuestFinalized (all required parameters populated) EXCEPT set `QuestDescription` to a Markdown test string that combines bold and an image, e.g. `"**bold description** with an ![logo](http://example.com/x.png) image"`.
    Assert: (1) the returned HTML contains `<strong>bold description</strong>` (formatted, not raw); (2) the returned HTML does NOT contain the literal `**bold description**` (raw syntax gone); (3) the returned HTML does NOT contain `<img` (Email target strips images). Assertions (1) will FAIL against the current HTML-encoded `@QuestDescription`.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter QuestFinalizedMarkdownRenderTests 2>&1 | grep -q "Failed"</automated>
  </verify>
  <acceptance_criteria>
    - QuestFinalizedMarkdownRenderTests.cs resolves `IEmailRenderService` and calls `RenderAsync<...QuestFinalized>`
    - Test asserts presence of `<strong>bold description</strong>` and absence of `<img`
    - Test FAILS (RED) against the current raw-@QuestDescription email
  </acceptance_criteria>
  <done>The email-render test is written and failing because Description is still HTML-encoded raw Markdown.</done>
</task>

<task type="auto">
  <name>Task 2: Wire IMarkdownService into QuestFinalized.razor (GREEN)</name>
  <files>QuestBoard.Service/Components/Emails/QuestFinalized.razor</files>
  <read_first>
    - QuestBoard.Service/Components/Emails/QuestFinalized.razor (current @QuestDescription usage at line 41 + top-of-file @using directives)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md (exact @inject/MarkupString target for this file)
    - QuestBoard.IntegrationTests/Emails/QuestFinalizedMarkdownRenderTests.cs (assertions this must satisfy)
  </read_first>
  <action>
    Add `@using QuestBoard.Domain.Interfaces` and `@inject IMarkdownService MarkdownService` to the top of `QuestFinalized.razor` (the first `@inject` in `Components/Emails/`). Replace the `@QuestDescription` interpolation inside the description `<p>` (line 41) with `@((MarkupString)MarkdownService.RenderToHtml(QuestDescription, MarkdownRenderTarget.Email))`. Keep the existing `<p style="...">` wrapper and all inline styles unchanged (email-client-safety styling is out of scope until Phase 71). Do NOT change the component's parameters or any caller (QuestFinalizedEmailJob / EmailPreviewController pass QuestDescription unchanged).
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter QuestFinalizedMarkdownRenderTests</automated>
  </verify>
  <acceptance_criteria>
    - QuestFinalized.razor contains `@inject IMarkdownService MarkdownService`
    - QuestFinalized.razor contains `(MarkupString)MarkdownService.RenderToHtml(QuestDescription, MarkdownRenderTarget.Email)`
    - QuestFinalized.razor no longer contains a bare `@QuestDescription` interpolation in the description paragraph
    - `dotnet test QuestBoard.IntegrationTests --filter QuestFinalizedMarkdownRenderTests` exits 0
  </acceptance_criteria>
  <done>The Quest Finalized email renders Description as sanitized formatted HTML with images stripped; the render test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored Quest Description → email HTML | Author Markdown is rendered into an email body delivered to recipients' mail clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-07 | Tampering/Info Disclosure (XSS in email) | QuestFinalized.razor Description render | mitigate | Renders via `RenderToHtml(..., Email)` — the Phase-65 email sanitizer profile (strips `<img>` and scripts); `(MarkupString)` outputs only already-sanitized HTML, never raw author input |
| T-66-08 | Information Disclosure (remote image / tracking pixel) | Description image embed | mitigate | Email target strips `<img>`; asserted by the `does NOT contain <img` test |
</threat_model>

<verification>
- `dotnet test QuestBoard.IntegrationTests --filter QuestFinalizedMarkdownRenderTests` exits 0
- `grep -c "@QuestDescription" QuestBoard.Service/Components/Emails/QuestFinalized.razor` shows the bare interpolation is gone from the description paragraph
</verification>

<success_criteria>
- Quest Finalized email renders Description markdown as formatted HTML
- Images stripped from the email body
- No caller or parameter contract changed
</success_criteria>

<output>
Create `.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-03-SUMMARY.md` when done
</output>
