---
phase: 66-quest-description-editor-rendering-proof-of-concept
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.IntegrationTests/Controllers/MarkdownControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Helpers/AntiForgeryHelper.cs
  - QuestBoard.Service/Controllers/MarkdownController.cs
autonomous: true
requirements: [EDITOR-02, EDITOR-04]
user_setup: []

must_haves:
  truths:
    - "An authenticated user can POST Markdown text to /markdown/preview and receive back the exact sanitized HTML that IMarkdownService.RenderToHtml(text, Web) produces for the same input"
    - "The preview endpoint rejects requests without a valid antiforgery token"
  artifacts:
    - path: "QuestBoard.Service/Controllers/MarkdownController.cs"
      provides: "POST /markdown/preview action"
      contains: "markdown/preview"
    - path: "QuestBoard.IntegrationTests/Controllers/MarkdownControllerIntegrationTests.cs"
      provides: "Header-based antiforgery + JSON body round-trip coverage"
      min_lines: 30
  key_links:
    - from: "QuestBoard.Service/Controllers/MarkdownController.cs"
      to: "IMarkdownService.RenderToHtml"
      via: "delegates preview rendering to the same Web-target render used by page display"
      pattern: "RenderToHtml\\(.*MarkdownRenderTarget.Web"
    - from: "MarkdownController"
      to: "antiforgery"
      via: "[ValidateAntiForgeryToken] on the POST action"
      pattern: "ValidateAntiForgeryToken"
---

<objective>
Add the server round-trip preview endpoint `POST /markdown/preview` that the EasyMDE editor calls to render live previews, and prove the round trip with an integration test that establishes this codebase's first header-based-antiforgery + JSON-body test helper.

Purpose: EDITOR-02 requires Preview to render server-side (not a second client parser), and EDITOR-04 requires that preview to byte-match saved page display — guaranteed by construction because the endpoint calls the identical `RenderToHtml(text, MarkdownRenderTarget.Web)` used by `Html.Markdown()` on Details/Manage.
Output: `MarkdownController.Preview`, a new header-token helper in the integration-test project, and `MarkdownControllerIntegrationTests`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md

<interfaces>
<!-- Shipped by Phase 65. Do not modify. -->
From QuestBoard.Domain/Interfaces/IMarkdownService.cs:
- string RenderToHtml(string? markdown, MarkdownRenderTarget target = MarkdownRenderTarget.Web)
- enum MarkdownRenderTarget { Web, Email }

App antiforgery facts (66-RESEARCH.md Pitfall 7):
- Program.cs has NO explicit AddAntiforgery config — ASP.NET Core default HeaderName "RequestVerificationToken" is active.
- Existing header-based fetch() calls (QuestController.Delete/RemovePlayerSignup) already send this header. No Program.cs change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add header-token helper + failing MarkdownController integration test (RED)</name>
  <files>QuestBoard.IntegrationTests/Helpers/AntiForgeryHelper.cs, QuestBoard.IntegrationTests/Controllers/MarkdownControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Helpers/AntiForgeryHelper.cs (existing form-field token extraction; add a header-oriented method, do not break the existing one)
    - QuestBoard.IntegrationTests/Controllers/QuestReminderTests.cs (integration-test harness shape: WebApplicationFactory usage, authenticated client setup, antiforgery-required POST shape)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md (Wave 0 Gaps + Code Examples — the endpoint contract and antiforgery-header pattern)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md (No Analog Found note: this establishes a NEW header-token helper, not just a new test using existing helpers)
  </read_first>
  <action>
    Add a helper method to `AntiForgeryHelper.cs` (alongside the existing form-field extractor) that obtains a request-verification token usable as the `RequestVerificationToken` HTTP header for a JSON POST — extract the token from a GET on an authenticated page (the same cookie/token pairing the existing helper uses) and expose it plus the cookie for reuse. Do not modify or remove the existing form-based method.
    Create `MarkdownControllerIntegrationTests.cs` using the project's existing authenticated `WebApplicationFactory` client pattern. Write tests that will FAIL until the controller exists: (1) POST `/markdown/preview` with `Content-Type: application/json`, body `{"markdown":"**bold**"}`, and the `RequestVerificationToken` header returns HTTP 200 and a body containing `<strong>bold</strong>`; (2) the same POST WITHOUT the antiforgery header returns HTTP 400; (3) an anonymous (unauthenticated) POST is redirected/denied (not 200). Assert the 200-body equals what `IMarkdownService.RenderToHtml("**bold**", MarkdownRenderTarget.Web)` returns for the same input (resolve the service from the factory's services or assert the `<strong>bold</strong>` substring at minimum) to lock EDITOR-04's "preview matches saved render" guarantee at the integration boundary.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.IntegrationTests 2>&1 | grep -Eq "error|Build FAILED" || dotnet test QuestBoard.IntegrationTests --filter MarkdownControllerIntegrationTests 2>&1 | grep -q "Failed"</automated>
  </verify>
  <acceptance_criteria>
    - AntiForgeryHelper.cs contains a new method returning a header-usable token (existing form-field method still present)
    - MarkdownControllerIntegrationTests.cs contains a POST to `/markdown/preview` with a `RequestVerificationToken` header and an assertion for `<strong>bold</strong>`
    - MarkdownControllerIntegrationTests.cs contains a no-token case asserting HTTP 400
    - The new tests FAIL (RED) because MarkdownController does not yet exist (the endpoint 404s / route unmapped)
  </acceptance_criteria>
  <done>Header-token helper exists and the preview round-trip integration tests are written and failing for the right reason (endpoint not yet implemented).</done>
</task>

<task type="auto">
  <name>Task 2: Implement MarkdownController.Preview (GREEN)</name>
  <files>QuestBoard.Service/Controllers/MarkdownController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs (small focused controller shape: primary-constructor DI, class-level [Authorize], `Content(html, "text/html")` return convention)
    - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs (the existing [HttpPost]/[HttpDelete] + [ValidateAntiForgeryToken] header-antiforgery actions RevokeSignup/RemovePlayerSignup)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md (Code Examples → MarkdownController)
    - QuestBoard.IntegrationTests/Controllers/MarkdownControllerIntegrationTests.cs (the tests this must satisfy)
  </read_first>
  <action>
    Create `QuestBoard.Service/Controllers/MarkdownController.cs` in namespace `QuestBoard.Service.Controllers`, class-level `[Authorize]` (any authenticated group member — NOT DungeonMasterOnly/AdminOnly; several of the 9 future fields are player-authored, per 66-RESEARCH.md ASVS V4), primary-constructor DI injecting `IMarkdownService`. Define a `public record PreviewRequest(string? Markdown);`. Add a `[HttpPost]` `[Route("markdown/preview")]` `[ValidateAntiForgeryToken]` action `Preview([FromBody] PreviewRequest request)` that returns `Content(markdownService.RenderToHtml(request.Markdown, MarkdownRenderTarget.Web), "text/html")`. Use `MarkdownRenderTarget.Web` only — never `.Email` (66-RESEARCH.md anti-pattern: Email strips images and is a different surface the author never sees; EDITOR-04 requires matching the page display, which is Web). Do NOT add any AddAntiforgery configuration to Program.cs — the default header name is already active.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter MarkdownControllerIntegrationTests</automated>
  </verify>
  <acceptance_criteria>
    - MarkdownController.cs contains `[Route("markdown/preview")]`, `[HttpPost]`, `[ValidateAntiForgeryToken]`, and class-level `[Authorize]`
    - MarkdownController.cs contains `RenderToHtml(request.Markdown, MarkdownRenderTarget.Web)` and does NOT contain `MarkdownRenderTarget.Email`
    - `git diff --name-only` does NOT include QuestBoard.Service/Program.cs (no antiforgery config change)
    - `dotnet test QuestBoard.IntegrationTests --filter MarkdownControllerIntegrationTests` exits 0 (all three cases pass)
  </acceptance_criteria>
  <done>`POST /markdown/preview` returns sanitized Web-target HTML for authenticated token-bearing requests, 400 without a token, and denies anonymous requests; all integration tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /markdown/preview | Untrusted, arbitrary-length author text crosses into a new server POST endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03 | Spoofing/Tampering (CSRF) | POST /markdown/preview | mitigate | `[ValidateAntiForgeryToken]` + the app's default header-based antiforgery convention; no-token 400 case is an explicit integration-test assertion |
| T-66-04 | Tampering/Info Disclosure (reflected/self-XSS) | Preview response HTML | mitigate | Endpoint renders via the sanitized `RenderToHtml(..., Web)` — same sanitizer as page display; never a raw Markdig call. Asserted by the `does NOT contain MarkdownRenderTarget.Email` and `<strong>bold</strong>` (sanitized-structure) checks |
| T-66-05 | Elevation of Privilege (cross-group leak) | Preview endpoint | accept | Endpoint takes raw text only — no entity IDs, no DB access, no cross-group data reachable |
| T-66-06 | Denial of Service (large payload) | Preview body | accept | Inherits Markdig nesting-depth guard via RenderToHtml; no explicit size cap (no locked requirement; carried-forward Phase 65 discretion item). Noted, not mitigated |
</threat_model>

<verification>
- `dotnet test QuestBoard.IntegrationTests --filter MarkdownControllerIntegrationTests` exits 0
- No modification to QuestBoard.Service/Program.cs
- Endpoint uses `MarkdownRenderTarget.Web` exclusively
</verification>

<success_criteria>
- Authenticated + token-bearing POST returns sanitized HTML matching the page-display render
- Missing-token POST returns 400; anonymous POST denied
- No new antiforgery configuration added to Program.cs
</success_criteria>

<output>
Create `.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-02-SUMMARY.md` when done
</output>
