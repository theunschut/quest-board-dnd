---
phase: 66-quest-description-editor-rendering-proof-of-concept
plan: 05
type: execute
wave: 2
depends_on: [66-02, 66-04]
files_modified:
  - QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml
  - QuestBoard.Service/Views/Quest/Create.cshtml
  - QuestBoard.Service/Views/Quest/Create.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Edit.cshtml
  - QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml
  - QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml
autonomous: true
requirements: [EDITOR-01, EDITOR-02, EDITOR-06, QUESTMD-01]
user_setup: []

must_haves:
  truths:
    - "The Quest Description field on Create, Edit, and Follow-Up forms (desktop and mobile) renders the EasyMDE toolbar + editor instead of a plain textarea"
    - "The editor's Preview button reaches /markdown/preview with a valid antiforgery token"
    - "Follow-Up forms, which previously had no shared quest-form script include, now load the editor wiring too"
  artifacts:
    - path: "QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml"
      provides: "EasyMDE CDN assets + markdown-editor.js include + antiforgery token exposure to JS"
      contains: "markdownAntiforgeryToken"
    - path: "QuestBoard.Service/Views/Quest/Create.cshtml"
      provides: "Description field via shared editor partial"
      contains: "_MarkdownEditor"
    - path: "QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml"
      provides: "Description field via shared editor partial + editor script include"
      contains: "_MarkdownEditor"
  key_links:
    - from: "QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml"
      to: "markdown-editor.js"
      via: "EasyMDE CDN + token + module include, in EasyMDE-before-init order"
      pattern: "markdown-editor.js"
    - from: "QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml"
      to: "_QuestFormScripts"
      via: "RenderPartialAsync inside its existing @section Scripts block"
      pattern: "_QuestFormScripts"
---

<objective>
Swap the plain Quest Description textarea for the shared `_MarkdownEditor` partial on all six write-form views (Create/Edit/Follow-Up × desktop/mobile), and generalize `_QuestFormScripts.cshtml` to load the EasyMDE CDN assets, the `markdown-editor.js` init module, and the per-form antiforgery token — including bringing the two Follow-Up forms (which today have no shared editor wiring) onto the same setup.

Purpose: Delivers the write half of QUESTMD-01 (editor on all three forms, both breakpoints) and EDITOR-01/02/06 in-place, using the assets from 66-04 and the endpoint from 66-02. No new mechanics — pure wiring.
Output: `_QuestFormScripts.cshtml` wires EasyMDE; all six forms render the Description editor via the partial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md

<interfaces>
<!-- Provided by 66-04 (shared assets) and 66-02 (endpoint). -->
- Partial: Views/Shared/_MarkdownEditor.cshtml, model QuestBoard.Service.ViewModels.Shared.MarkdownEditorViewModel { string FieldName; string? Value; string Label; bool Required; string? Placeholder; }.
- markdown-editor.js reads window.markdownAntiforgeryToken and upgrades every .markdown-editor-textarea on DOMContentLoaded.
- Endpoint: POST /markdown/preview ([Authorize], [ValidateAntiForgeryToken]).
- Antiforgery is globally injected in Views/_ViewImports.cshtml as `Antiforgery`. Token retrieval convention: `var tokens = Antiforgery.GetAndStoreTokens(ViewContext.HttpContext);` then send `tokens.RequestToken`.
- EasyMDE CDN (SRI computed 2026-07-09; re-verify if script load hard-fails):
    CSS  https://cdn.jsdelivr.net/npm/easymde@2.21.0/dist/easymde.min.css  integrity sha384-ZoLYv3S+AsZX+zhbN1D1+WPpc8f+DmLfxfgw+qn0Nq8wJPOYQQXEW5ZrRhcGozlG  crossorigin anonymous
    JS   https://cdn.jsdelivr.net/npm/easymde@2.21.0/dist/easymde.min.js   integrity sha384-mTM6vzy+/UiHrMBClNGViM9qEv0/26iCGqpJKhSzdnjrxbKjO3vkT62ujXQ8B5iv  crossorigin anonymous
- Field-name facts: Create/Create.Mobile/CreateFollowUp/CreateFollowUp.Mobile bind top-level `Description` (asp-for="Description"); Edit/Edit.Mobile bind `Quest.Description`.
- CreateFollowUp(.Mobile) do NOT include _QuestFormScripts; they have their OWN inline @section Scripts (addDate/removeDate). Only one @section Scripts is allowed per view.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize _QuestFormScripts.cshtml to load EasyMDE assets + token + markdown-editor.js</name>
  <files>QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml (current dates-only content to preserve)
    - QuestBoard.Service/Views/Characters/Create.cshtml (the Cropper.js CDN+SRI script-include precedent + ~/js include with asp-append-version)
    - QuestBoard.Service/Views/_ViewImports.cshtml (confirm the global Antiforgery injection this file will use)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md (Shared write-form JS partial + Antiforgery patterns)
  </read_first>
  <action>
    Extend _QuestFormScripts.cshtml WITHOUT removing its existing quest-form date-validation script. Add, in this order so EasyMDE is defined before init and the token exists before DOMContentLoaded: (1) the EasyMDE CDN stylesheet link (url/integrity/crossorigin above); (2) the EasyMDE CDN script (url/integrity/crossorigin above); (3) a small inline script that sets window.markdownAntiforgeryToken to the request token — obtain it in a Razor code block via `var tokens = Antiforgery.GetAndStoreTokens(ViewContext.HttpContext);` and emit `tokens.RequestToken` into the assignment; (4) a script include for ~/js/markdown-editor.js with asp-append-version true. Do not add any per-form JS here — markdown-editor.js self-initializes every .markdown-editor-textarea. Keep the existing @using line and the date-validation block intact.
  </action>
  <verify>
    <automated>grep -q "easymde" QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml && grep -q "markdownAntiforgeryToken" QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml && grep -q "markdown-editor.js" QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml && grep -q "GetAndStoreTokens" QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml && grep -q "proposed-dates" QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - _QuestFormScripts.cshtml contains the EasyMDE CDN CSS link and JS script, each with the given `integrity` hash and `crossorigin`
    - _QuestFormScripts.cshtml sets `window.markdownAntiforgeryToken` from `Antiforgery.GetAndStoreTokens(...)` and includes `~/js/markdown-editor.js`
    - The EasyMDE JS script appears BEFORE the markdown-editor.js include in file order
    - The pre-existing date-validation block (references `proposed-dates`) is still present
  </acceptance_criteria>
  <done>The shared quest-form script partial now loads EasyMDE, exposes the antiforgery token to JS, and pulls in the init module, while keeping the existing date-validation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Swap Description textarea for the editor partial on Create/Edit (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Quest/Create.cshtml, QuestBoard.Service/Views/Quest/Create.Mobile.cshtml, QuestBoard.Service/Views/Quest/Edit.cshtml, QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Create.cshtml (current Description block lines ~28-32 + existing _QuestFormScripts include)
    - QuestBoard.Service/Views/Quest/Create.Mobile.cshtml (current Description block ~35-42)
    - QuestBoard.Service/Views/Quest/Edit.cshtml (current Description block ~30-34; binds Quest.Description)
    - QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml (current Description block ~44-48)
    - QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml (the partial contract to invoke)
  </read_first>
  <action>
    In each of the four views, add `@using QuestBoard.Service.ViewModels.Shared` near the top, then replace the entire Description form-group div (label + required asterisk + textarea + asp-validation-for) with a partial invocation of _MarkdownEditor passing a MarkdownEditorViewModel whose FieldName is the exact model-binding expression that view used (Create/Create.Mobile: "Description"; Edit/Edit.Mobile: "Quest.Description"), Value is the same model expression the textarea bound (Model.Description, or Model.Quest?.Description for Edit), Label "Description", Required true, and Placeholder copied verbatim from that view's existing textarea placeholder. Do NOT alter the Rewards textarea or any other field — Rewards stays plain text until Phase 67. These four views already include _QuestFormScripts, so no script change is needed here.
  </action>
  <verify>
    <automated>for f in Create Create.Mobile Edit Edit.Mobile; do grep -q "_MarkdownEditor" "QuestBoard.Service/Views/Quest/$f.cshtml" || exit 1; done; ! grep -Eq 'textarea asp-for="Description"' QuestBoard.Service/Views/Quest/Create.cshtml && ! grep -Eq 'textarea asp-for="Quest.Description"' QuestBoard.Service/Views/Quest/Edit.cshtml && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Each of Create.cshtml, Create.Mobile.cshtml, Edit.cshtml, Edit.Mobile.cshtml references `_MarkdownEditor` and declares `@using QuestBoard.Service.ViewModels.Shared`
    - Create.cshtml passes `FieldName = "Description"`; Edit.cshtml passes `FieldName = "Quest.Description"`
    - No `<textarea asp-for="Description">` remains in Create.cshtml and no `<textarea asp-for="Quest.Description">` remains in Edit.cshtml
    - The Rewards textarea is unchanged in all four views
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>Quest Description on all four Create/Edit forms renders through the shared editor partial with the correct per-form binding name; Rewards untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Swap Description on Follow-Up forms + include _QuestFormScripts inside their existing Scripts block</name>
  <files>QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml, QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml (Description block ~34-38; the existing @section Scripts block at ~128 with inline addDate/removeDate)
    - QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml (its Description block ~39-43 and its own Scripts block)
    - QuestBoard.Service/Views/Quest/Create.cshtml (how _QuestFormScripts is rendered via RenderPartialAsync)
    - QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml (the partial contract)
  </read_first>
  <action>
    In both Follow-Up views: add `@using QuestBoard.Service.ViewModels.Shared`, then replace the Description form-group div (label + required asterisk + textarea asp-for="Description" + validation) with a _MarkdownEditor partial invocation (FieldName "Description", Value Model.Description, Label "Description", Required true, Placeholder copied from the existing textarea). Do NOT touch the Rewards field. Then, because these views already have their OWN single @section Scripts block (and Razor allows only one), append `@{ await Html.RenderPartialAsync("_QuestFormScripts"); }` at the END of that existing @section Scripts block (after the addDate/removeDate script) so EasyMDE wiring loads. The shared partial's date-validation logic is safely inert or appropriate here (it only acts when a #proposed-dates container is present, which Follow-Up has); multiple submit listeners coexist without conflict.
  </action>
  <verify>
    <automated>grep -q "_MarkdownEditor" QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml && grep -q "_MarkdownEditor" QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml && grep -q "_QuestFormScripts" QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml && grep -q "_QuestFormScripts" QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Both CreateFollowUp.cshtml and CreateFollowUp.Mobile.cshtml reference `_MarkdownEditor` and pass `FieldName = "Description"`
    - Both contain exactly one `@section Scripts` block, which now renders `_QuestFormScripts` at its end (existing addDate/removeDate script retained)
    - No `<textarea asp-for="Description">` remains in either Follow-Up view; Rewards unchanged
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>Both Follow-Up forms render the Description editor and load the shared EasyMDE wiring they previously lacked; the write editor now appears on all three form families, desktop and mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write form → server | Antiforgery token minted per form and consumed by the preview fetch |
| CDN → browser | EasyMDE CDN assets loaded on the write forms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-11 | Spoofing (CSRF) | window.markdownAntiforgeryToken → /markdown/preview | mitigate | Token minted via Antiforgery.GetAndStoreTokens per rendered form; the preview endpoint enforces [ValidateAntiForgeryToken] (66-02) |
| T-66-SC | Tampering (supply chain) | EasyMDE CDN CSS/JS | mitigate | Version-pinned + SRI integrity + crossorigin (fail-closed); slopcheck [OK] in 66-RESEARCH.md — not [ASSUMED]/[SUS], no blocking checkpoint |
| T-66-12 | Tampering (stored XSS on save) | Quest Description persisted raw | accept | Raw Markdown is stored verbatim (unchanged column); all XSS defense is at render time via the Phase-65 sanitizer, not at input — matches the milestone architecture |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` succeeds
- All six write views reference `_MarkdownEditor`; no `<textarea asp-for="Description">` or `<textarea asp-for="Quest.Description">` remains for the Description field
- `_QuestFormScripts.cshtml` loads EasyMDE (SRI), exposes the token, and includes markdown-editor.js; the Follow-Up forms render `_QuestFormScripts`
- Full visual/behavioral confirmation is performed in 66-07 (manual UI checkpoint)
</verification>

<success_criteria>
- Quest Description shows the EasyMDE toolbar + editor on Create/Edit/Follow-Up, desktop and mobile
- Preview reaches /markdown/preview with a valid token
- Rewards and all other fields unchanged
</success_criteria>

<output>
Create `.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-05-SUMMARY.md` when done
</output>
