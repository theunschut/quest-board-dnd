---
phase: 71-email-safety-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 71-02
files_modified: []
autonomous: false
requirements:
  - EMAILMD-02
  - EMAILMD-03
user_setup:
  - service: real email clients
    why: "EMAILMD-02/EMAILMD-03 can only be closed by opening the sent emails in the real clients — Outlook's Word rendering engine and Gmail's rendering cannot be simulated by any browser preview or headless tool (D-05)."
    dashboard_config:
      - task: "Have access to a real Outlook desktop client AND a real Gmail webmail account (or arrange the second one). Per 71-CONTEXT.md the operator may currently have ready access to only one — the checkpoint surfaces this and allows splitting into two sessions."
        location: "Operator's own Outlook desktop + Gmail webmail inbox"
      - task: "Have the application running against the existing Postfix/Resend relay so the 3 test emails can actually send (the relay lives on the Linux deployment host, not the Windows dev box)."
        location: "Deployment host / running app instance"
must_haves:
  truths:
    - "3 real test emails (Quest Finalized, Session Reminder, Waitlist Promoted) with a structured-Markdown Description reach the operator's inbox via the existing relay (D-05/D-06)"
    - "The operator confirms, in real Outlook desktop and real Gmail webmail, visible bullets, intact heading/blockquote styling, no clipping, and a working read-more link (EMAILMD-02, EMAILMD-03)"
  artifacts:
    - "Throwaway test quest + signup/finalization/waitlist state (created to trigger the 3 dispatches, cleaned up after)"
  key_links:
    - "The 3 emails are triggered through the real dispatch path (QuestFinalizedEmailJob / SessionReminderJob or DailyReminderJob / QuestWaitlistPromotedEmailJob) via the existing Postfix/Resend relay — no new send infrastructure (D-05)"
---

<objective>
Close EMAILMD-02 and EMAILMD-03 by the only method that can actually prove them: send 3 real test emails to the operator's inbox and have the operator open each in real Outlook desktop and real Gmail webmail. Automated unit tests (Plan 01) prove the styling/truncation logic; browser preview (Plan 02) proves it renders in a browser — but neither can surface the two Outlook-Word-engine defects this phase exists to fix (invisible bullets, silent overflow clipping). This plan is the live, blocking human-verification gate (D-05), with Claude creating the throwaway test quest and triggering the dispatches (D-06).

Purpose: Final acceptance for the phase's two requirements against real clients.

Output: A verified rendering report from the operator (per client, per template) and a cleaned-up throwaway test quest.
</objective>

<artifacts_this_phase_produces>
No source symbols or files are produced by this plan. It creates transient runtime state only:
- A throwaway test quest (with a structured-Markdown Description) plus the signup / finalization / waitlist state needed to legitimately trigger the 3 dispatches — created then deleted/cleaned up after verification.
No new files, no new packages, no DB schema/migration changes.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-email-safety-hardening/71-CONTEXT.md
@.planning/phases/71-email-safety-hardening/71-RESEARCH.md
@.planning/phases/71-email-safety-hardening/71-VALIDATION.md
@QuestBoard.Domain/Interfaces/IQuestEmailDispatcher.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the throwaway test quest and trigger the 3 email dispatches</name>
  <files>(no source files — runtime data + email dispatch only)</files>
  <read_first>
    - .planning/phases/71-email-safety-hardening/71-RESEARCH.md — "Test-Email Trigger Mechanism" (Option 1 real UI/business flow recommended; Option 2 direct IBackgroundJobClient.Enqueue for speed); "Environment Availability" (SMTP relay lives on the Linux deployment host)
    - .planning/phases/71-email-safety-hardening/71-CONTEXT.md — D-05 (real send via existing relay, no new infra), D-06 (Claude sets up the test quest), and the single-client-access constraint
    - QuestBoard.Domain/Interfaces/IQuestEmailDispatcher.cs — the finalize / date-changed / waitlist-promoted dispatch signatures
    - QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs — the `SampleMarkdownDescription` const added in Plan 02 (reuse the same structured sample so the sent emails match what was dev-loop-previewed)
  </read_first>
  <action>
    Confirm the app is running against the existing Postfix/Resend relay (per operator memory the relay is on the Linux deployment host — this send cannot originate from the Windows dev box unless configured; coordinate with the operator on where the send runs). No new send infrastructure may be added (D-05).

    Create a throwaway test quest whose Quest Description is a structured-Markdown sample (reuse Plan 02's `SampleMarkdownDescription`: at least one heading, an unordered list, a blockquote, long enough to exceed the 5-block / 650-char truncation budget so D-02/D-03/D-04 truncation is exercised). Prefer Option 1 (drive the real app flow) for production fidelity: create the quest, add a signup/vote, finalize it (triggers `QuestFinalizedEmailJob`), drive the reminder path (`SessionReminderJob`/`DailyReminderJob`) — e.g. a proposed date of "tomorrow" or the DM manual-resend action — and set up a full-then-freed-seat scenario so a waitlisted player is auto-promoted (triggers `QuestWaitlistPromotedEmailJob`). Direct `IBackgroundJobClient.Enqueue<TJob>(...)` (Option 2, mirroring the existing AdminController enqueue pattern) is acceptable only for rapid iteration, not for the final acceptance send, because it bypasses the real dedup/guard logic.

    Address each of the 3 emails to the operator's own inbox. Confirm all 3 dispatched successfully (e.g. Hangfire dashboard shows the 3 jobs succeeded) and that the operator received all 3. Note the throwaway quest id so it can be cleaned up in the checkpoint's resume step.
  </action>
  <acceptance_criteria>
    - A throwaway test quest exists with a structured-Markdown Description (heading + list + blockquote) that exceeds the truncation budget
    - All 3 target emails (Quest Finalized, Session Reminder, Waitlist Promoted) are dispatched via the existing relay and land in the operator's inbox
    - No new send infrastructure was added (existing Postfix/Resend relay only)
    - The throwaway quest id is recorded for post-verification cleanup
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms all 3 emails (Quest Finalized, Session Reminder, Waitlist Promoted) arrived in their inbox. Automated dispatch confirmation via the Hangfire dashboard (3 succeeded jobs) supports this but inbox receipt is the acceptance signal.</human-check>
  </verify>
  <done>3 real test emails carrying structured-Markdown Quest Description content are sent via the existing relay and received by the operator, ready to be opened in the real clients.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Outlook desktop + Gmail webmail rendering verification (D-05)</name>
  <files>(none — human verification of the emails sent in Task 1)</files>
  <read_first>
    - .planning/phases/71-email-safety-hardening/71-CONTEXT.md — D-05 (blocking live human-verify) and the single-client-access constraint
    - .planning/phases/71-email-safety-hardening/71-UI-SPEC.md — expected heading/list/blockquote/link styling and the truncation read-more copy, so the operator knows what "correct" looks like
  </read_first>
  <action>
    Pause for the operator to open the 3 test emails from Task 1 in real Outlook desktop AND real Gmail webmail and confirm the 6 checks below. This is a blocking gate (D-05) — do not auto-advance and do not substitute a browser preview. If only one client is currently available, verify that one and hold EMAILMD-02 open until the second client is confirmed. After approval, clean up the throwaway test quest from Task 1.
  </action>
  <acceptance_criteria>
    - All 3 emails confirmed in BOTH real Outlook desktop and real Gmail webmail (or the second client explicitly recorded as still-pending, keeping EMAILMD-02 open)
    - Visible list markers in Outlook desktop (MSO fallback bullet present if native rendering fails)
    - Intact heading (upright, larger) and blockquote (gold left border) styling; brown underlined links; correct bold/italic
    - No silent clipping — long Description ends with a working "…continue reading on the quest board" link to the quest URL
  </acceptance_criteria>
  <verify>
    <human-check>Operator opens all 3 emails in real Outlook desktop and real Gmail webmail and reports pass/fail against the 6 checks; "approved" only when both clients pass for all 3 templates.</human-check>
  </verify>
  <done>Operator has confirmed all 6 checks in both real clients for all 3 templates (or explicitly deferred the second client), and the throwaway test quest is cleaned up.</done>
  <what-built>
    RenderEmailHtml (Plan 01) now injects email-safe inline styles + an Outlook bullet-visibility fallback on every heading/list/blockquote/inline element of Quest Description, and truncates over-budget content at a block boundary with a "…continue reading on the quest board" link. The 3 quest templates (Plan 02) call it and no longer use the Outlook-incompatible scroll wrapper. Task 1 sent 3 real test emails with structured-Markdown content to your inbox.
  </what-built>
  <how-to-verify>
    For EACH of the 3 emails (Quest Finalized, Session Reminder, Waitlist Promoted), open it in BOTH real Outlook desktop AND real Gmail webmail and confirm:
    1. Headings render with visual hierarchy — larger, bold, upright (not italic) — distinct from the italic flavor text.
    2. Bullet / numbered lists show a VISIBLE marker in front of each item (this is the primary Outlook-desktop risk — if bullets are missing in Outlook, report it; the MSO fallback should show a "•").
    3. Blockquotes show the gold left border and indented italic text.
    4. Links inside the Description are brown and underlined; bold/italic inline text renders correctly.
    5. No content is silently cut off at the bottom of the 840px poster card. If the Description was long, it ends with the "…continue reading on the quest board" link (not a hard clip, not a scrollbar).
    6. Clicking "…continue reading on the quest board" opens the quest (same URL as the Row 6 "View Quest Details" button).

    Access constraint (from 71-CONTEXT.md): if you currently have ready access to only one of Outlook desktop / Gmail webmail, verify the one you have now and explicitly note the other as still-pending — do NOT let EMAILMD-02 be marked closed until BOTH clients are confirmed. The Outlook AND Gmail halves may be done in two separate sessions.

    Tuning note (from RESEARCH Open Question 1): the truncation budget (5 blocks / 650 chars) is a starting heuristic. If content clips too early or spills past the card in a client, report the observed fit — the `maxTopLevelBlocks`/`maxPlainTextChars` parameters can be tuned without a structural code change (and SessionReminder's extra "The Adventure:" label may warrant a slightly smaller budget).

    After verification, clean up: delete the throwaway test quest (and any test signup/waitlist state) recorded in Task 1.
  </how-to-verify>
  <resume-signal>Reply "approved" if all 6 checks pass in BOTH Outlook desktop and Gmail webmail. Otherwise describe per-client/per-template what rendered wrong (missing bullets, clipped content, broken styling, budget too tight/loose) so a gap-closure plan or a budget/MSO-fallback tune can be scheduled. If only one client was available, reply which client passed and that the other is pending.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test quest data → real email send | Throwaway, non-sensitive placeholder content is sent only to the operator's own inbox via the existing relay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-07 | Information Disclosure | throwaway test quest + real email send | low | accept | Test quest uses non-sensitive placeholder content; emails are addressed only to the operator's own inbox; the throwaway quest and its signup/waitlist state are cleaned up after verification. |
| T-71-SC | Tampering | package installs | low | accept | No new packages or send infrastructure this phase — the existing Postfix/Resend relay is reused. |
</threat_model>

<verification>
- Full suite `dotnet test` remains green (carried from Plans 01/02) before this live gate
- D-05 live human verification: both real Outlook desktop AND real Gmail webmail confirmed for all 3 templates — this, together with the green suite, is what closes EMAILMD-02 and EMAILMD-03
- Throwaway test quest cleaned up after verification
</verification>

<success_criteria>
The operator has opened all 3 quest emails in real Outlook desktop and real Gmail webmail and confirmed visible list markers, intact heading/blockquote styling, no silent clipping (long content ends with a working "…continue reading on the quest board" link), closing EMAILMD-02 and EMAILMD-03; the throwaway test quest is cleaned up.
</success_criteria>

<output>
Create `.planning/phases/71-email-safety-hardening/71-03-SUMMARY.md` when done.
</output>
