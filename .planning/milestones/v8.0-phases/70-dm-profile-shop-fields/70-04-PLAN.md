---
phase: 70-dm-profile-shop-fields
plan: 04
type: execute
wave: 2
depends_on: [70-01, 70-02, 70-03]
files_modified: []
autonomous: false
requirements: [PROFILEMD-01, PROFILEMD-02]
must_haves:
  truths:
    - "dotnet build is clean; all six write forms load _QuestFormScripts; all four read views render Description/Bio via Html.Markdown; both teaser surfaces use ExtractPlainText; no inline pre-wrap remains in any edited read container"
    - "A human confirms the Bio and Description editors work on desktop and at a true 320px mobile viewport, the required-field 20-char regression still fires, the Preview toggle round-trips, and rendered headings are legible on every read surface (including the Shop Details modal and DM Profile mobile)"
  artifacts:
    - "Automated wiring/render gate output (build + Shop integration tests + grep assertions across all Wave-1 files)"
    - "Recorded human-verification outcome in 70-04-SUMMARY.md"
  key_links:
    - "All Wave-1 plans (70-01, 70-02, 70-03) are complete and committed before this verification runs"
---

<objective>
Close Phase 70 by proving both requirements end-to-end. First an automated gate re-asserts the full wiring across every file the three Wave-1 plans touched (build + the Shop Details/Index render tests + a grep sweep). Then a single blocking human-verification checkpoint confirms the things automation cannot see: live editor behavior on desktop and a true 320px mobile viewport, the required-field regression, Preview round-trip fidelity, and heading legibility on each read surface — including the two styling surfaces no generic selector was written against (the Shop Details Bootstrap modal and the DM Profile mobile glass card).

Purpose: Guarantee PROFILEMD-01 and PROFILEMD-02 are actually delivered (not just compiled) before the phase is marked complete.
Output: A green automated gate and a recorded human sign-off (or a concrete defect list to feed back into a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-dm-profile-shop-fields/70-CONTEXT.md
@.planning/phases/70-dm-profile-shop-fields/70-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan creates NO source symbols — it is verification only. It produces `70-04-SUMMARY.md` recording the automated-gate result and the human-verification outcome. No new files, classes, endpoints, migrations, or packages.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Automated wiring + render gate across all Wave-1 changes</name>
  <files>QuestBoard.Service/Views/DungeonMaster/EditProfile.cshtml, QuestBoard.Service/Views/DungeonMaster/EditProfile.Mobile.cshtml, QuestBoard.Service/Views/DungeonMaster/Profile.cshtml, QuestBoard.Service/Views/DungeonMaster/Profile.Mobile.cshtml, QuestBoard.Service/Views/ShopManagement/Create.cshtml, QuestBoard.Service/Views/ShopManagement/Create.Mobile.cshtml, QuestBoard.Service/Views/ShopManagement/Edit.cshtml, QuestBoard.Service/Views/ShopManagement/Edit.Mobile.cshtml, QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml, QuestBoard.Service/Views/Shop/Details.Mobile.cshtml, QuestBoard.Service/Views/Shop/Index.cshtml, QuestBoard.Service/Views/ShopManagement/Index.cshtml, QuestBoard.Service/wwwroot/js/markdown-editor.js</files>
  <read_first>
    - .planning/phases/70-dm-profile-shop-fields/70-01-SUMMARY.md, 70-02-SUMMARY.md, 70-03-SUMMARY.md (what each Wave-1 plan actually landed)
    - .planning/phases/70-dm-profile-shop-fields/70-CONTEXT.md — the Integration Points list (every surface that had to change) and the "Confirmed out of scope" note (Shop/Index.Mobile.cshtml has no teaser to change)
  </read_first>
  <action>
    Run the automated gate and confirm every Wave-1 change is present and coherent. This task modifies no source; it only verifies. If any check fails, do NOT patch it here — record the failure so it is fixed in its owning Wave-1 plan (or a gap-closure plan) and re-run.

    1. Build the service: `dotnet build QuestBoard.Service/QuestBoard.Service.csproj`.
    2. Run the Shop render guard: `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~ShopControllerIntegrationTests"` (exercises `/Shop` Index and `/Shop/Details/{id}` — the desktop teaser and the `_ShopItemDetailsContent` render sink).
    3. Parse the editor JS: `node --check QuestBoard.Service/wwwroot/js/markdown-editor.js`.
    4. Run the grep sweep in the acceptance criteria to confirm all six write forms load the editor scripts, all four read views render via Html.Markdown, both teaser surfaces use ExtractPlainText, and no inline pre-wrap remains in the edited read containers.

    Confirm no GSD identifiers leaked into source per CLAUDE.md across the edited files.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj && dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~ShopControllerIntegrationTests" && node --check QuestBoard.Service/wwwroot/js/markdown-editor.js</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds; `ShopControllerIntegrationTests` pass; `node --check` on markdown-editor.js passes.
    - All six write forms load the editor scripts: `grep -rc '_QuestFormScripts.cshtml' QuestBoard.Service/Views/DungeonMaster/EditProfile.cshtml QuestBoard.Service/Views/DungeonMaster/EditProfile.Mobile.cshtml QuestBoard.Service/Views/ShopManagement/Create.cshtml QuestBoard.Service/Views/ShopManagement/Create.Mobile.cshtml QuestBoard.Service/Views/ShopManagement/Edit.cshtml QuestBoard.Service/Views/ShopManagement/Edit.Mobile.cshtml` returns 1 for each file.
    - All four read views render via Html.Markdown: `grep -c 'Html.Markdown(Model.Bio)' QuestBoard.Service/Views/DungeonMaster/Profile.cshtml` and `.../Profile.Mobile.cshtml` each return 1; `grep -c 'Html.Markdown(Model.Description)' QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml` and `.../Shop/Details.Mobile.cshtml` each return 1.
    - Both teaser surfaces strip Markdown: `grep -c 'ExtractPlainText' QuestBoard.Service/Views/Shop/Index.cshtml` returns at least 1; `grep -c 'DescriptionTeaser' QuestBoard.Service/Views/ShopManagement/Index.cshtml` returns 4.
    - No inline pre-wrap remains in the three edited read containers: `grep -c 'white-space: pre-wrap' QuestBoard.Service/Views/DungeonMaster/Profile.cshtml QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml QuestBoard.Service/Views/Shop/Details.Mobile.cshtml` returns 0 for each.
    - The editor instance handle is exposed: `grep -c 'textarea.easyMDE = initMarkdownEditor' QuestBoard.Service/wwwroot/js/markdown-editor.js` returns 1.
    - No render sink uses Html.Raw: `grep -rc 'Html.Raw' QuestBoard.Service/Views/DungeonMaster/Profile.cshtml QuestBoard.Service/Views/DungeonMaster/Profile.Mobile.cshtml QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml QuestBoard.Service/Views/Shop/Details.Mobile.cshtml` returns 0 for each.
    - No GSD identifiers in edited source: `grep -rnE 'PROFILEMD|Phase 70|70-0[0-9]|D-0[0-9]' QuestBoard.Service/Views/DungeonMaster QuestBoard.Service/Views/ShopManagement QuestBoard.Service/Views/Shop QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml QuestBoard.Service/wwwroot/js/markdown-editor.js` returns nothing.
  </acceptance_criteria>
  <done>Build is clean, Shop integration tests pass, markdown-editor.js parses, and every grep assertion confirms the wiring/render/teaser/pre-wrap state across all Wave-1 files, with no GSD identifiers or Html.Raw in source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies the live Bio + Description loop (desktop + 320px mobile)</name>
  <files></files>
  <read_first>
    - .planning/phases/70-dm-profile-shop-fields/70-UI-SPEC.md (Spacing note: re-confirm the 320px one-row toolbar on EditProfile.Mobile Bio AND Shop Create.Mobile/Edit.Mobile Description; Typography "Heading color" bullets for the DM Profile mobile glass card and the Shop Description mobile — the two surfaces no generic selector was written against; Color "Required-field asterisk" for the first required field this milestone shows)
    - .planning/phases/70-dm-profile-shop-fields/70-CONTEXT.md (the "verify, don't redesign" notes for the light-card heading contrast and the DM Profile mobile bespoke-class card; the required-field regression-check reminder)
  </read_first>
  <action>
    Start the app, then present the numbered verification steps below to the operator and PAUSE for their confirmation. This task changes no code; it gates Phase 70 closure on a human confirming the rendered result at both desktop and a true 320px mobile viewport. If the operator reports a scoped, operator-directed CSS/markup issue, it may be fixed in-session (mirroring 66-07 / 67-05 / 68-03 / 69-03); anything larger is routed to a gap-closure pass.
  </action>
  <what-built>
    Phase 70 wired the shared Markdown editor + rendering into DM Profile Bio (PROFILEMD-01) and Shop Item Description (PROFILEMD-02). Automated build, the Shop Details/Index render tests, and a grep sweep all pass. This checkpoint confirms the live behavior automation cannot observe: editor UX on desktop and a true 320px mobile viewport, the required-field 20-char regression, Preview round-trip fidelity, and heading legibility on every read surface — including the two surfaces no generic selector was written against (the Shop Details Bootstrap modal and the DM Profile mobile glass card).
  </what-built>
  <how-to-verify>
    Run the app (`dotnet run --project QuestBoard.Service`) and, signed in as a DM, verify each item. For mobile checks, use browser dev-tools at a 320px-wide viewport (or the emulator) so the mobile `.Mobile.cshtml` views load.

    DM Profile Bio (PROFILEMD-01):
    1. Edit DM Profile (desktop): the Bio field shows the EasyMDE toolbar (Bold, Italic, Heading, List, Link, Blockquote) + Preview, with the info tooltip and the "Introduce yourself..." helper text still below it. Type a heading, a bullet list, and a blockquote; click Preview — the rendered result matches what saves. Save.
    2. View DM Profile (desktop): the About card shows the Bio as formatted HTML — heading, bullets, and blockquote render; no raw `#`/`*`/`>` characters; no doubled blank-line spacing; headings are legible against the card.
    3. Repeat Edit + View at 320px mobile: the toolbar fits in one row with no horizontal scroll; the mobile About card renders the formatted Bio legibly (gold text on the dark glass card).

    Shop Item Description (PROFILEMD-02):
    4. Create a Shop item (desktop): the Description field shows the same editor WITH a red required asterisk. Enter under 20 characters and submit — the "at least 20 characters" alert still fires (required-field regression). Then enter valid formatted Markdown (heading + list) and save successfully.
    5. Edit that item (desktop): the editor is pre-filled with the saved Markdown and shows the placeholder behavior + red asterisk; Preview round-trips; save works.
    6. View the item on Shop Details (desktop): Description renders as formatted HTML in its card; headings legible. Open the same item via the Details modal (from the Shop grid) — the modal shows the same formatted render.
    7. Shop Index (desktop, customer view): the browsing-card teaser shows clean plain text (no leaking Markdown syntax), truncated (~120 chars).
    8. Shop Management dashboard (desktop): the item-list teasers in all three sections show clean plain text (no syntax), truncated (~50 chars).
    9. Repeat the Shop Description create/edit + Details view at 320px mobile: toolbar fits one row; mobile Details renders the formatted Description legibly (muted parchment text).

    Security spot-check:
    10. Type a `<script>`-style payload into a Bio or Description, save, and confirm it renders inert (escaped or stripped), NOT executed (stored-XSS sanitizer spot-check).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms steps 1-10 across desktop and 320px mobile, or reports specific failing step numbers with observed behavior.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms steps 1-10 above, OR reports specific issues.
    - Any issue the operator raises is captured for a gap-closure pass; a scoped, operator-directed "fix now" CSS/markup issue may be corrected in-session (mirroring 66-07 / 67-05 / 68-03 / 69-03).
  </acceptance_criteria>
  <resume-signal>Type "approved" if every surface behaves as described, or list the specific surface(s) and defect(s) to fix so they can be routed into a gap-closure plan.</resume-signal>
  <done>Operator has signed off on the live Bio + Description loop across desktop and 320px mobile (including the required-field regression, Preview round-trip, modal render, and mobile heading legibility), or issues are captured for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced — this plan is verification only. The relevant boundaries (untrusted Bio/Description → Html.Markdown sanitizing render sink; → /markdown/preview) are established and mitigated in plans 70-01/70-02/70-03.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-11 | Tampering (XSS) | End-to-end render of Bio/Description across all surfaces | high | mitigate | The human-verify step includes typing formatting syntax and confirming it renders as intended HTML (not raw text and not executed markup); the automated gate asserts no `Html.Raw` on any render sink. The sanitizing pipeline (Phase 65) remains the control. |
| T-70-SC | Tampering | npm/NuGet installs | low | accept | No new packages this phase — nothing installed in any Phase-70 plan. |
</threat_model>

<verification>
- Automated gate (Task 1): build clean, Shop integration tests green, markdown-editor.js parses, all grep assertions pass.
- Human checkpoint (Task 2): explicit "approved" covering all nine steps, or a defect list.
- Any defect found routes to a gap-closure plan targeting the owning Wave-1 file(s); this plan does not patch source.
</verification>

<success_criteria>
- PROFILEMD-01 confirmed: Bio editor + formatted render work on DM Profile desktop and mobile.
- PROFILEMD-02 confirmed: Description editor (with required asterisk + 20-char regression) + formatted render work on Shop Create/Edit and Shop Details desktop (page + modal) and mobile, with clean plain-text teasers on Shop Index and the Management dashboard.
- Automated gate green; human sign-off recorded.
</success_criteria>

<output>
Create `.planning/phases/70-dm-profile-shop-fields/70-04-SUMMARY.md` when done, recording the automated-gate result and the human-verification outcome.
</output>
