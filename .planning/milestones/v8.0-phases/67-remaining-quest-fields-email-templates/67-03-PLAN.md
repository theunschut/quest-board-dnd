---
phase: 67-remaining-quest-fields-email-templates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Views/QuestLog/EditRecap.cshtml
  - QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml
autonomous: true
requirements: [QUESTMD-02, QUESTMD-03]
user_setup: []

must_haves:
  truths:
    - "A user editing a Quest Recap (EditRecap form, desktop or mobile) sees the same Markdown editor/toolbar/preview as Quest Description"
    - "Recap renders as formatted HTML on QuestLog Details (desktop and mobile), and the 'No recap yet' empty state stays plain text"
    - "Rewards renders as formatted HTML on QuestLog Details (desktop and mobile), including a newly added Rewards section on the mobile view that never had one (D-03)"
  artifacts:
    - path: "QuestBoard.Service/Views/QuestLog/EditRecap.cshtml"
      provides: "Recap edited through _MarkdownEditor, with a newly added _QuestFormScripts include"
      contains: "_MarkdownEditor"
    - path: "QuestBoard.Service/Views/QuestLog/Details.cshtml"
      provides: "Rewards and Recap both rendered via Html.Markdown()"
      contains: "Html.Markdown(Model.Quest.Recap)"
    - path: "QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml"
      provides: "Recap via Html.Markdown() plus a newly added Rewards section (D-03)"
      contains: "Html.Markdown(Model.Quest.Rewards)"
  key_links:
    - from: "QuestBoard.Service/Views/QuestLog/EditRecap.cshtml"
      to: "_QuestFormScripts (EasyMDE CDN + markdown-editor.js)"
      via: "newly added @section Scripts block"
      pattern: "_QuestFormScripts"
    - from: "QuestBoard.Service/Views/QuestLog/Details.cshtml"
      to: "IMarkdownService (via Html.Markdown extension)"
      via: "Html.Markdown(Model.Quest.Recap)"
      pattern: "Html\\.Markdown\\(Model\\.Quest\\.Recap\\)"
---

<objective>
Complete QUESTMD-03 (Recap) and the QuestLog slice of QUESTMD-02 (Rewards): wire the shared Markdown editor into the Recap edit forms, render Recap and Rewards as formatted HTML on the QuestLog Details views, and backfill the Rewards section that the QuestLog **mobile** Details view never had (decision D-03).

Purpose: QuestLog is the completed-quest surface. Recap only exists here (Manage is for active quests), so this plan owns the entire Recap read+write path plus the QuestLog Rewards reads. Both QuestLog Details files carry both a Rewards and a Recap edit, so they are grouped here to keep file ownership exclusive from plan 67-02.
Output: Two Recap write forms wired to the editor (with the scripts include they previously lacked), and two QuestLog Details views rendering both fields as Markdown.

Note: the `.markdown-content { white-space: normal; }` pre-wrap override that these QuestLog Rewards/Recap boxes rely on is delivered by plan 67-02 (same Wave 1). Do NOT edit `markdown-content.css` or `quests.css` here — both QuestLog Details files render the out-of-scope "Original Quest Description" block through the shared `.quest-description-box` / `.recap-display-box` classes, which must keep their `white-space: pre-wrap`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md

<interfaces>
<!-- Editor partial for the write forms; read helper for the display views. -->

Editor invocation (Recap uses lowercase FieldName to match the form's existing raw `name="recap"` binding):
`@{ await Html.RenderPartialAsync("_MarkdownEditor", new MarkdownEditorViewModel { FieldName = "recap", Value = Model.Recap, Label = "Recap", Required = false, Placeholder = "Describe what happened during this quest..." }); }`

`EditRecapViewModel.Recap` is bound by the raw `name="recap"` convention (NOT `asp-for`). Keeping `FieldName = "recap"` (lowercase) preserves model binding — changing it breaks POST.

Read helper: `@Html.Markdown(Model.Quest.Recap)` / `@Html.Markdown(Model.Quest.Rewards)` — wraps output in `.markdown-content` automatically.

Scripts include (loads EasyMDE CDN CSS/JS, the antiforgery token, and `markdown-editor.js` which self-inits every `.markdown-editor-textarea`):
`@section Scripts { @{ await Html.RenderPartialAsync("_QuestFormScripts"); } }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire _MarkdownEditor into the Recap edit forms (desktop + mobile) and add the scripts include</name>
  <files>QuestBoard.Service/Views/QuestLog/EditRecap.cshtml, QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/QuestLog/EditRecap.cshtml (current: plain `<textarea id="recap" name="recap" rows="10">@Model.Recap</textarea>` + a `<small>` encouragement line; NO `@section Scripts` block exists)
    - QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml (same shape, `rows="6"`, also no `@section Scripts`)
    - QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml
    - QuestBoard.Service/Views/Quest/_QuestFormScripts.cshtml (what the new scripts include loads; note its internal date-validation JS is scoped by `form[action*="/Quest/"]` and no-ops harmlessly on the `/QuestLog/EditRecap` form)
    - QuestBoard.Service/ViewModels/QuestLogViewModels/EditRecapViewModel.cs (confirm `Recap` binds via `name="recap"`)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md (section "Recap write forms")
    - .planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md (Copywriting Contract row "Recap editor field")
  </read_first>
  <action>
    In both `EditRecap.cshtml` and `EditRecap.Mobile.cshtml`:

    1. Replace the plain Recap `<div class="mb-3">` block (the `<label for="recap">` + `<textarea id="recap" name="recap">` — but NOT the trailing `<small>` encouragement line) with a `_MarkdownEditor` partial call: `FieldName = "recap"` (lowercase — do NOT change this, it must match the `name="recap"` model binding), `Value = Model.Recap`, `Label = "Recap"`, `Required = false`, `Placeholder = "Describe what happened during this quest..."`. This replaces the old instructional label "Write or update the session recap:" with the noun label "Recap" per the UI-SPEC.

    2. Keep the encouragement line `<small class="form-text text-muted">Share the story of this adventure with your players!</small>` as a sibling element placed immediately AFTER the partial call (the partial has no slot for it). Do not drop it.

    3. Add a new `@section Scripts { @{ await Html.RenderPartialAsync("_QuestFormScripts"); } }` block to each file (neither currently has one). Without it, `markdown-editor.js` never loads and the Recap textarea renders as a plain unstyled box with no toolbar/preview.

    4. Add `@using QuestBoard.Service.ViewModels.Shared` at the top of each file if not already present.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds.
    - `EditRecap.cshtml` and `EditRecap.Mobile.cshtml` each contain a `_MarkdownEditor` call with `FieldName = "recap"` and `Value = Model.Recap`, and no longer contain `<textarea ... name="recap"`.
    - Each file contains exactly one `@section Scripts` block that includes `_QuestFormScripts`.
    - The `Share the story of this adventure with your players!` `<small>` line is still present in each file, positioned after the partial call.
    - `FieldName` is the lowercase string `"recap"` (not `"Recap"`) in both files.
  </acceptance_criteria>
  <done>Both Recap edit forms use the shared editor with a functioning toolbar/preview (scripts loaded), the encouragement copy is preserved, and model binding is intact via the lowercase `recap` field name.</done>
</task>

<task type="auto">
  <name>Task 2: Render Rewards + Recap via Html.Markdown() on QuestLog Details (desktop + mobile), including the D-03 mobile Rewards backfill</name>
  <files>QuestBoard.Service/Views/QuestLog/Details.cshtml, QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/QuestLog/Details.cshtml (Rewards block ~lines 52-61 `.quest-description-box`; Recap block ~lines 116-128 `.recap-display-box` with a `No recap has been written...` empty branch; the "Original Quest Description" block ~lines 44-50 is OUT OF SCOPE — leave it raw)
    - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml (Recap block ~lines 91-104 `.recap-display-box`; has NO Rewards block at all — you are ADDING one per D-03; the "Original Quest Description" block ~lines 42-48 uses `.recap-display-box` and is OUT OF SCOPE — leave it raw)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md (decision D-03; the "Pre-wrap CSS scoping" note)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md (sections for `QuestLog/Details.cshtml` and Flagged Discrepancy 2)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md (Copywriting Contract — Recap empty-state body copy)
  </read_first>
  <action>
    In `Details.cshtml`:
    - Rewards: change `<div class="quest-description-box">@Model.Quest.Rewards</div>` to render `@Html.Markdown(Model.Quest.Rewards)`. Keep the `@if (!string.IsNullOrWhiteSpace(Model.Quest.Rewards))` guard, the `<hr />`, the `fa-coins` heading, and the `.quest-description-box` class.
    - Recap: change the `<div class="recap-display-box">@Model.Quest.Recap</div>` (inside the `@if (!string.IsNullOrWhiteSpace(Model.Quest.Recap))` branch) to render `@Html.Markdown(Model.Quest.Recap)`. Leave the `else` branch `<p class="text-muted">No recap has been written for this quest yet.</p>` exactly as-is — it stays plain text, outside the Markdown pipeline. Leave the "Original Quest Description" block (also `.quest-description-box`) completely untouched — it is out of scope and must keep rendering raw text with pre-wrap.

    In `Details.Mobile.cshtml`:
    - Recap: change `<div class="recap-display-box">@Model.Quest.Recap</div>` to `@Html.Markdown(Model.Quest.Recap)`, keeping the guard and the plain-text `No recap has been written for this quest yet.` empty branch unchanged. Leave the "Original Quest Description" block (`.recap-display-box`) untouched — out of scope, keeps pre-wrap.
    - Rewards (D-03 backfill): this file has NO Rewards section. ADD one, mirroring the desktop `QuestLog/Details.cshtml` Rewards block adapted to this file's mobile card layout: a blank-guarded (`@if (!string.IsNullOrWhiteSpace(Model.Quest.Rewards))`) section with a `<hr />`, an `<h6><i class="fas fa-coins text-warning me-2"></i>Rewards</h6>` heading, and `@Html.Markdown(Model.Quest.Rewards)` for the body. Place it between the "Original Quest Description" section and the "Adventurers"/"Session Recap" sections, matching desktop's Description → Rewards ordering. Since this Rewards markup never existed as plain text here, no pre-wrap-removal step applies.

    Do NOT edit `markdown-content.css` or `quests.css` in this plan (the pre-wrap override is owned by 67-02).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds.
    - `Details.cshtml` contains both `Html.Markdown(Model.Quest.Rewards)` and `Html.Markdown(Model.Quest.Recap)`, and still contains the raw "Original Quest Description" block rendering `@Model.Quest.Description` (unchanged, no `Html.Markdown` on Description).
    - `Details.cshtml` still contains the plain-text `No recap has been written for this quest yet.` empty-state line.
    - `Details.Mobile.cshtml` contains `Html.Markdown(Model.Quest.Recap)` and a NEW `Html.Markdown(Model.Quest.Rewards)` block with a `fas fa-coins` heading and a `!string.IsNullOrWhiteSpace(Model.Quest.Rewards)` guard.
    - `Details.Mobile.cshtml` still renders the raw "Original Quest Description" (`@Model.Quest.Description`) unchanged.
    - Neither `markdown-content.css` nor `quests.css` appears in this plan's `git diff --name-only`.
  </acceptance_criteria>
  <done>QuestLog Details renders both Rewards and Recap as Markdown on desktop and mobile; the mobile Rewards gap (D-03) is closed; out-of-scope Original Description blocks and the Recap empty state are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → EditRecap POST | User-authored Recap free-text crosses into model binding (unchanged from today; editor is client chrome only). |
| stored Recap/Rewards text → rendered HTML on QuestLog Details | User Markdown converted to HTML via `Html.Markdown()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering / Information Disclosure (stored XSS) | `Html.Markdown(Model.Quest.Recap)` / `Html.Markdown(Model.Quest.Rewards)` on QuestLog Details | mitigate | Rendering routes through `IMarkdownService.RenderToHtml(..., Web)` (HtmlSanitizer, Phase 65, 261 tests) — blocks `<script>`, `javascript:` links, attribute injection. Output is a sanitized `HtmlString`, never raw user input. |
| T-67-03-A | Elevation of Privilege (authorization) | `EditRecap` GET/POST | accept | No authorization change this phase. `CanEditRecap` was already broadened to "any group member" in Phase 63 and is unchanged; this plan only swaps the textarea widget. |
| T-67-SC | Tampering (supply chain) | EasyMDE / FA v4-shim CDN | accept | No new installs; assets reused verbatim from Phase 66 (slopcheck `[OK]`). |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` is green.
- Recap edits via the editor on both EditRecap forms; both now include `_QuestFormScripts`.
- QuestLog Details renders Rewards + Recap as Markdown on desktop and mobile; mobile Rewards section added (D-03).
- Out-of-scope Original Description blocks remain raw-text with pre-wrap; Recap empty state remains plain text.
- Live editor + rendering + text-color checks happen in 67-05 (EditRecap's editor is nested in `.modern-card`, so the Phase 66 CSS-leak fix is verified there).
</verification>

<success_criteria>
- QUESTMD-03 read+write is complete; the QuestLog slice of QUESTMD-02 is complete including the D-03 mobile backfill.
- No shared CSS class rule was altered; out-of-scope Description keeps line-break preservation.
</success_criteria>

<output>
Create `.planning/phases/67-remaining-quest-fields-email-templates/67-03-SUMMARY.md` when done.
</output>
