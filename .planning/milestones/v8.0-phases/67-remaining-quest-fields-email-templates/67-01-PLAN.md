---
phase: 67-remaining-quest-fields-email-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Views/Quest/Create.cshtml
  - QuestBoard.Service/Views/Quest/Create.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Edit.cshtml
  - QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml
  - QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml
autonomous: true
requirements: [QUESTMD-02]
user_setup: []

must_haves:
  truths:
    - "A user editing Quest Rewards on Create, Edit, or Follow-Up (desktop or mobile) sees the same Markdown toolbar/preview/paragraph-hint as Quest Description"
    - "The Follow-Up mobile form now has a Rewards field (it was missing entirely before this phase)"
    - "No Rewards field is marked required (no red asterisk) — Rewards is optional"
  artifacts:
    - path: "QuestBoard.Service/Views/Quest/Create.cshtml"
      provides: "Rewards field rendered through the shared _MarkdownEditor partial"
      contains: "_MarkdownEditor"
    - path: "QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml"
      provides: "Newly added Rewards field via _MarkdownEditor (D-02 mobile-parity backfill)"
      contains: "FieldName = \"Rewards\""
  key_links:
    - from: "QuestBoard.Service/Views/Quest/Edit.cshtml"
      to: "MarkdownEditorViewModel"
      via: "RenderPartialAsync _MarkdownEditor with FieldName \"Quest.Rewards\""
      pattern: "FieldName = \"Quest.Rewards\""
    - from: "QuestBoard.Service/Views/Quest/Create.cshtml"
      to: "_QuestFormScripts (EasyMDE CDN + markdown-editor.js)"
      via: "existing @section Scripts include (already present for Description)"
      pattern: "_QuestFormScripts"
---

<objective>
Wire the shared Markdown editor (`_MarkdownEditor.cshtml`) into the Quest **Rewards** field on all six quest write forms, mechanically repeating what Phase 66 did for Quest Description. Five forms already have a plain `<textarea asp-for="Rewards">`; the sixth (`CreateFollowUp.Mobile.cshtml`) is missing the Rewards field entirely and gets it added in the same task (decision D-02, mobile-parity lesson from Phases 43/54/61).

Purpose: Satisfies the write-side of QUESTMD-02 — users get the toolbar, Preview toggle, and paragraph-break hint on Rewards exactly as they do on Description, with zero new editor infrastructure.
Output: Six updated write-form views; Rewards editing reaches feature parity with Description editing across desktop and mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md

<interfaces>
<!-- The shared editor partial and its view model. Use these directly — do not hand-roll a textarea/toolbar/hint. -->

From QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs:
```csharp
public class MarkdownEditorViewModel
{
    public string FieldName { get; set; }   // model-binding name: "Rewards" or "Quest.Rewards"
    public string? Value { get; set; }       // current field value
    public string Label { get; set; }        // "Rewards"
    public bool Required { get; set; }        // false for Rewards
    public string? Placeholder { get; set; }
}
```

Invocation (mirrors each file's existing Description partial call a few lines above the Rewards block):
`@{ await Html.RenderPartialAsync("_MarkdownEditor", new MarkdownEditorViewModel { FieldName = "...", Value = ..., Label = "Rewards", Required = false, Placeholder = "..." }); }`

The partial (`Views/Shared/_MarkdownEditor.cshtml`) already renders label + info-icon tooltip ("Supports Markdown formatting. Leave a blank line between paragraphs.") + `<textarea class="markdown-editor-textarea">` + validation span. `markdown-editor.js` (loaded by `_QuestFormScripts.cshtml`) self-initializes every `.markdown-editor-textarea` on `DOMContentLoaded`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire _MarkdownEditor into Rewards on Create/Edit (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Quest/Create.cshtml, QuestBoard.Service/Views/Quest/Create.Mobile.cshtml, QuestBoard.Service/Views/Quest/Edit.cshtml, QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Create.cshtml (see the existing Description `_MarkdownEditor` call and the plain Rewards `<textarea>` a few lines below it — replace the latter, mirror the former)
    - QuestBoard.Service/Views/Quest/Create.Mobile.cshtml
    - QuestBoard.Service/Views/Quest/Edit.cshtml
    - QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml
    - QuestBoard.Service/Views/Shared/_MarkdownEditor.cshtml (the partial you are calling)
    - QuestBoard.Service/ViewModels/Shared/MarkdownEditorViewModel.cs
    - .planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md (section "Rewards write forms" — the per-file FieldName/Value table)
  </read_first>
  <action>
    In each of the four files, replace the existing plain Rewards block (the `<div class="mb-3">` containing `<label asp-for="Rewards">`, `<textarea asp-for="Rewards">`, and `<span asp-validation-for="Rewards">`) with a `Html.RenderPartialAsync("_MarkdownEditor", new MarkdownEditorViewModel { ... })` call, placed in the exact same position, mirroring the Description partial call already present in each file.

    Use these exact property values per file:
    - Create.cshtml and Create.Mobile.cshtml: `FieldName = "Rewards"`, `Value = Model.Rewards`
    - Edit.cshtml and Edit.Mobile.cshtml: `FieldName = "Quest.Rewards"`, `Value = Model.Quest.Rewards`
    All four: `Label = "Rewards"`, `Required = false`, `Placeholder = "Describe the loot, gold, or other rewards for this quest..."`.

    `Required = false` is mandatory — `Rewards` is `public string? Rewards` with no `[Required]` attribute on `QuestViewModel`, unlike Description. Do NOT add a `<span class="text-danger">*</span>` asterisk.

    Ensure `@using QuestBoard.Service.ViewModels.Shared` is present at the top of each file (it already is on every form Phase 66 touched — confirm, add only if genuinely missing). Do NOT add or modify any `@section Scripts` block: all four files already include `_QuestFormScripts` (Description required it in Phase 66), which loads the EasyMDE CDN assets and `markdown-editor.js`. Adding it again would double-load EasyMDE.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds with no Razor compile errors.
    - `Create.cshtml` and `Create.Mobile.cshtml` each contain a `_MarkdownEditor` partial call with `FieldName = "Rewards"` and `Value = Model.Rewards`, and no longer contain `<textarea asp-for="Rewards"`.
    - `Edit.cshtml` and `Edit.Mobile.cshtml` each contain a `_MarkdownEditor` partial call with `FieldName = "Quest.Rewards"` and `Value = Model.Quest.Rewards`, and no longer contain `<textarea asp-for="Rewards"`.
    - None of the four files gained a new `@section Scripts` block or a second `_QuestFormScripts` include (grep count of `_QuestFormScripts` per file is unchanged from before the edit).
    - No `text-danger">*` asterisk was added adjacent to any Rewards label.
  </acceptance_criteria>
  <done>All four Create/Edit forms render Rewards through `_MarkdownEditor` (optional, no asterisk); build is green; scripts includes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Rewards editor into Follow-Up desktop, and backfill the missing Follow-Up mobile Rewards field (D-02)</name>
  <files>QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml, QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml (has an existing plain Rewards block to replace, plus the Description `_MarkdownEditor` call to mirror)
    - QuestBoard.Service/Views/Quest/CreateFollowUp.Mobile.cshtml (Description is wired via `_MarkdownEditor` at lines ~40-47, then jumps straight to ChallengeRating — there is NO Rewards field to replace; you are ADDING one)
    - QuestBoard.Service/ViewModels/QuestViewModels/FollowUpQuestViewModel.cs (confirm `Rewards` is `string?`, no `[Required]`, and is not pre-populated from the original quest)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md (decision D-02)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md (section "CreateFollowUp.Mobile.cshtml (D-02 — Rewards field missing entirely)")
  </read_first>
  <action>
    In `CreateFollowUp.cshtml`: replace the existing plain Rewards `<div class="mb-3">` block (label + `<textarea asp-for="Rewards">` + validation span) with a `_MarkdownEditor` partial call — `FieldName = "Rewards"`, `Value = Model.Rewards`, `Label = "Rewards"`, `Required = false`, `Placeholder = "Describe the loot, gold, or other rewards for this quest..."` — in the same position, mirroring the Description partial call already in the file.

    In `CreateFollowUp.Mobile.cshtml`: this file has no Rewards field at all (Phase 59 asymmetry). ADD a `_MarkdownEditor` partial call with the identical property values above, inserted between the existing Description partial call and the ChallengeRating block, matching where desktop `CreateFollowUp.cshtml` places Rewards (Description → Rewards → ChallengeRating). The new mobile field starts blank because `FollowUpQuestViewModel.Rewards` is never copied from the original quest — this matches desktop's existing behavior and is out of scope to change.

    Neither file needs a scripts change: both already include `_QuestFormScripts` in their `@section Scripts` block (Description's editor needed it). Confirm `@using QuestBoard.Service.ViewModels.Shared` is present at the top of both files.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds.
    - `CreateFollowUp.cshtml` contains a `_MarkdownEditor` call with `FieldName = "Rewards"` and `Value = Model.Rewards`, and no longer contains `<textarea asp-for="Rewards"`.
    - `CreateFollowUp.Mobile.cshtml` now contains a `_MarkdownEditor` call with `FieldName = "Rewards"` and `Value = Model.Rewards`, positioned after the Description `_MarkdownEditor` call and before the `ChallengeRating` input.
    - Both files still contain exactly one `_QuestFormScripts` include each (no new duplicate).
    - No required asterisk added to the Follow-Up Rewards label on either file.
  </acceptance_criteria>
  <done>Follow-Up desktop uses the editor for Rewards; Follow-Up mobile has a new blank Rewards editor field at the correct position; build is green; the desktop/mobile Rewards asymmetry is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Quest write forms (Create/Edit/Follow-Up POST) | User-authored Rewards free-text crosses into server model binding. This boundary already existed pre-Markdown; this plan only changes the client-side editing widget, not the server intake. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01-W | Tampering (stored XSS) | Rewards free-text submitted from these forms | mitigate | No new server surface introduced here. Rewards is persisted as raw text exactly as today; XSS is neutralized on the READ side by `IMarkdownService`'s HtmlSanitizer pipeline (RENDER-01, Phase 65, 261 unit tests green), delivered in plans 67-02/67-03. The editor is client-only chrome. |
| T-67-SC | Tampering (supply chain) | EasyMDE / FA v4-shim CDN assets | accept | No new package installs this phase. EasyMDE 2.21.0 + FA v4-shims are reused verbatim from Phase 66 (slopcheck `[OK]`, 2026-07-09), same CDN URLs + SRI hashes, no version change. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` is green after both tasks.
- All six write forms (`Create`, `Create.Mobile`, `Edit`, `Edit.Mobile`, `CreateFollowUp`, `CreateFollowUp.Mobile`) render Rewards via `_MarkdownEditor`.
- No form gained a duplicate `_QuestFormScripts` include.
- Live toolbar/preview behavior is confirmed later in 67-05 (human-verify checkpoint), including the 320px one-row toolbar check for the Rewards editor specifically.
</verification>

<success_criteria>
- The write-side of QUESTMD-02 is complete: every quest write form edits Rewards with the same toolbar + Preview + paragraph-hint as Description.
- The Follow-Up mobile Rewards gap (D-02) is closed.
</success_criteria>

<output>
Create `.planning/phases/67-remaining-quest-fields-email-templates/67-01-SUMMARY.md` when done.
</output>
