---
phase: 34.2
slug: performance-architecture-fix-tech-debt-refactors-questcontro
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-01
---

# Phase 34.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Source note:** No dedicated RESEARCH.md exists for Phase 34.2 (research was skipped — Phase 34's `34-RESEARCH.md` already validated this exact scope in depth: Tech Debt, Performance Bottlenecks, Fragile Areas, Scaling Limits, Dependencies at Risk, and the 2 remaining Test Coverage Gaps items). Test Infrastructure below is carried over from `.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-RESEARCH.md`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit v3 (`xunit.v3` 3.2.2, `xunit.runner.visualstudio` 3.1.5), `Microsoft.NET.Test.Sdk` 18.7.0 |
| **Config file** | `QuestBoard.IntegrationTests/xunit.runner.json` |
| **Quick run command** | `dotnet test QuestBoard.UnitTests` |
| **Full suite command** | `dotnet test` |
| **Estimated runtime** | ~60 seconds (191 tests as of Phase 33 close, plus whatever Phase 34/34.1 and this phase add) |

---

## Sampling Rate

- **After every task commit:** Run `dotnet build`; `dotnet test QuestBoard.UnitTests` for logic changes (service extraction, retry policy, null-guard)
- **After every plan wave:** Run `dotnet test` (full suite) — the follow-up quest, tenant-isolation, and query-filter tests are especially sensitive to regressions
- **Before `/gsd-verify-work`:** Full suite must be green, including `TenantIsolationTests.cs` (must still pass — SuperAdmin's intentional `ActiveGroupId = null` must not trip the new null-guard)
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

No REQ-IDs are mapped to this phase — mapped to `CONCERNS.md` items / CONTEXT.md decisions instead. Task IDs are TBD until the planner assigns them.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | Tech Debt: QuestController/AdminController selective service extraction (D-01/D-02/D-03) | — | Behavior-preserving refactor — no user-facing change | build + full suite | `dotnet build && dotnet test` | ✅ QuestController.cs/AdminController.cs (modified), QuestService.cs (modified) | ⬜ pending |
| TBD | TBD | TBD | Tech Debt: follow-up quest two-phase consolidation into QuestService method (D-04) | — | Create+update happens transactionally; orphan cleanup on failure still works | integration | Existing `QuestController_CreateFollowUp_*` tests must still pass; new test for the consolidated method's failure path | ⚠️ existing tests + net-new failure-path test | ⬜ pending |
| TBD | TBD | TBD | Tech Debt: `DateTime.Now` → `UtcNow` in `ShopSeedService` | — | Seed dates use UTC | build | `dotnet build` | N/A — 2-line change | ⬜ pending |
| TBD | TBD | TBD | Performance: composite index on `Quests(IsFinalized, FinalizedDate)` (D-07) | — | Migration applies cleanly; query result set unchanged | integration | Existing `DailyReminderJob`/reminder tests must still pass unmodified after `dotnet ef migrations add` | ✅ existing tests cover behavior | ⬜ pending |
| TBD | TBD | TBD | Performance: Shop Item Queries projection (D-08) | — | List views exclude image BLOBs; detail views still load them | integration | Existing shop-listing tests must still pass; verify no missing-image regression on detail view | ✅ existing tests + manual spot-check | ⬜ pending |
| TBD | TBD | TBD | Fragile Areas: Hangfire scope-management helper (`HangfireJobHelper.RunInScopeAsync`) | — | All jobs still resolve scoped services correctly | build + full suite | `dotnet test` (existing Hangfire job tests must still pass after refactor to use the helper) | ✅ existing job tests | ⬜ pending |
| TBD | TBD | TBD | Fragile Areas: Query Filter documentation comments (`OnModelCreating`) | — | N/A — comment-only, no filter behavior change | build | `dotnet build` | N/A | ⬜ pending |
| TBD | TBD | TBD | Fragile Areas: AutoMapper enum-cast validation test | — | Enum casts at `EntityProfile.cs` boundaries deserialize correctly for known values | unit | New test asserting round-trip cast correctness for each cast site | ❌ Wave 0 — net-new test | ⬜ pending |
| TBD | TBD | TBD | Scaling Limits: `ActiveGroupId` null-guard (`InvalidOperationException` for unexpected-null) | T-34.2-01 (ASVS V4) | Throws only for unexpected-null; SuperAdmin's intentional null still works | unit + integration | `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~TenantIsolationTests"` must still pass; new test for the guard itself | ❌ Wave 0 — net-new test, high care needed not to break TENANT-01/02 | ⬜ pending |
| TBD | TBD | TBD | Scaling Limits: Hangfire `AutomaticRetryAttribute` retry policy | — | Jobs retry with exponential backoff (1/2/4/8/16s) on transient failure | build | `dotnet build`; existing job tests must still pass (retry policy is additive config, not behavior change to job logic) | ✅ existing job tests | ⬜ pending |
| TBD | TBD | TBD | Scaling Limits: `Forbid()` defense-in-depth — documentation only (D-06) | — | N/A — no code change this phase | build | `dotnet build` | N/A — doc note only | ⬜ pending |
| TBD | TBD | TBD | Performance: Hangfire job-queue batching — documentation only (D-09) | — | N/A — no code change this phase | build | `dotnet build` | N/A — doc note only | ⬜ pending |
| TBD | TBD | TBD | Dependencies at Risk: Identity/Resend SMTP documentation | — | N/A — comment/doc only | build | `dotnet build` | N/A | ⬜ pending |
| TBD | TBD | TBD | Test Coverage Gaps: Hangfire retry behavior test | — | Job retries on failure per `AutomaticRetryAttribute`, moves to Failed after max attempts | unit | New test mocking a job dependency to throw, verifying Hangfire retry behavior | ❌ Wave 0 — explicitly the deliverable | ⬜ pending |
| TBD | TBD | TBD | Test Coverage Gaps: Group Query Filter enforcement test | T-34.2-01 (ASVS V4) | `ActiveGroupId = null` or `= 999` (nonexistent) → empty result set | integration | New test using `WebApplicationFactoryBase`'s existing `IActiveGroupContext` test stub | ❌ Wave 0 — explicitly the deliverable | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New test: AutoMapper enum-cast round-trip validation
- [ ] New test: `ActiveGroupId` null-guard behavior (must not break existing SuperAdmin-null-is-valid tests in `TenantIsolationTests.cs`)
- [ ] New test: follow-up quest consolidated service method's failure/rollback path
- [ ] New test: Hangfire job retry behavior (Test Coverage Gap item)
- [ ] New test: Group Query Filter enforcement — null/nonexistent `ActiveGroupId` returns empty (Test Coverage Gap item, reuse `WebApplicationFactoryBase`'s existing stub)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `QuestController`/`AdminController` behavior unchanged after service extraction | D-01 | Full behavioral regression across many actions is better covered by the existing integration test suite + a manual click-through than a single new automated check | Run full `dotnet test` suite; manually click through Create/Edit/Delete quest, follow-up quest creation, and Admin user management flows in the running app |
| Shop list view no longer loads image BLOBs unnecessarily | D-08 | Confirming a query no longer loads a column requires either DB query-plan inspection or a manual timing/observation check, not a simple assertion | Compare shop list page load before/after the projection change (dev tools network tab or a quick timing log) |
| `Forbid()` and Hangfire-batching documentation notes are accurate and discoverable | D-06/D-09 | Documentation quality/placement is a human judgment call | Read the added note in `ARCHITECTURE.md`/`CONCERNS.md`, confirm it clearly states current safety net + deferred future work |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
