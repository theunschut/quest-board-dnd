---
phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro
plan: 01
subsystem: api
tags: [aspnet-core-mvc, ef-core, quest-controller, quest-service, refactor]

# Dependency graph
requires:
  - phase: 34-codebase-cleanup-and-security-hardening-remove-unused-code-s
    provides: comment-tag cleanup baseline on QuestController.cs/QuestService.cs (no D-xx/phase tags to strip before this refactor)
  - phase: 34.1-security-bugs-fix-known-bugs-and-security-considerations-ite
    provides: confirms this plan's file scope doesn't collide with Phase 34.1's AdminController/Program.cs work
provides:
  - QuestService.CreateFollowUpQuestWithDetailsAsync — single transactional method for follow-up create+apply+rollback
  - Thinned QuestController.CreateFollowUp action (validate → delegate → redirect/re-render)
  - ShopSeedService seed timestamps now UTC-consistent
affects: [34.2-05 (adds the failure-path regression test for the new service method)]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Domain service composes existing service methods (CreateFollowUpQuestAsync + UpdateQuestPropertiesWithNotificationsAsync) rather than duplicating their internals", "Rollback failure isolated in a nested try/catch so it never masks the primary exception"]

key-files:
  created: []
  modified:
    - QuestBoard.Domain/Services/QuestService.cs
    - QuestBoard.Domain/Interfaces/IQuestService.cs
    - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs
    - QuestBoard.Domain/Services/ShopSeedService.cs

key-decisions:
  - "Rollback logic wraps RemoveAsync in its own try/catch that swallows secondary failures, guaranteeing the primary exception from UpdateQuestPropertiesWithNotificationsAsync always re-throws to the controller"
  - "No new ILogger dependency added to QuestService for rollback-failure logging — out of scope per plan; primary-exception propagation is the correctness guarantee instead"

patterns-established:
  - "Composing-method pattern: new transactional Domain methods call existing service methods rather than re-implementing their logic, keeping create/update/rollback each a single source of truth"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-07-02
---

# Phase 34.2 Plan 01: Consolidate Follow-Up Quest Create+Rollback Summary

**Moved the fragile two-phase follow-up-quest create-then-update-with-rollback logic out of `QuestController.CreateFollowUp` into a single `QuestService.CreateFollowUpQuestWithDetailsAsync` transactional method, and fixed `ShopSeedService`'s `DateTime.Now` → `DateTime.UtcNow` tech debt.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-02T08:03:01Z (approx, per session start)
- **Completed:** 2026-07-02
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- `QuestService` now exposes `CreateFollowUpQuestWithDetailsAsync`, composing `CreateFollowUpQuestAsync` and `UpdateQuestPropertiesWithNotificationsAsync` with rollback-on-failure via `RemoveAsync`, guaranteeing no orphaned shell quest survives a failed update
- `QuestController.CreateFollowUp` is now a thin action: guard clauses/validation unchanged, single delegating call to the new service method, `catch (InvalidOperationException)` re-renders the form
- `ShopSeedService`'s "Ancient Dragon Scale Armor" seed item now uses `DateTime.UtcNow` for both `AvailableFrom` and `AvailableUntil`, consistent with the rest of the codebase

## Task Commits

Each task was committed atomically:

1. **Task 1: Add CreateFollowUpQuestWithDetailsAsync to QuestService + interface** - `124129c` (feat)
2. **Task 2: Thin QuestController.CreateFollowUp to delegate to the new service method** - `fe696b3` (refactor)
3. **Task 3: Replace DateTime.Now with DateTime.UtcNow in ShopSeedService** - `ace6282` (fix)

**Plan metadata:** (recorded in final commit)

## Files Created/Modified
- `QuestBoard.Domain/Services/QuestService.cs` - Added `CreateFollowUpQuestWithDetailsAsync`, composing the shell-create + apply-details + rollback-on-failure sequence
- `QuestBoard.Domain/Interfaces/IQuestService.cs` - Added the matching interface contract with a plain XML `<summary>` doc comment (no ID tags)
- `QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` - Thinned `CreateFollowUp`: removed the two-phase business logic block, replaced with a single delegating call + `catch (InvalidOperationException)`
- `QuestBoard.Domain/Services/ShopSeedService.cs` - `DateTime.Now.AddDays(7)`/`DateTime.Now.AddDays(30)` → `DateTime.UtcNow.AddDays(...)` for the legendary item's availability window

## Decisions Made
- Rollback failure inside `CreateFollowUpQuestWithDetailsAsync` is isolated in a nested `try`/`catch` around the `RemoveAsync` cleanup call so a secondary exception during cleanup can never replace or mask the primary exception from the failed update — the primary exception always propagates via the outer `throw;`.
- No `ILogger` dependency was added to `QuestService` for rollback-failure logging (per plan instruction — out of scope for this method; the correctness guarantee is that the primary exception always surfaces to the caller).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `CreateFollowUpQuestWithDetailsAsync` is a single testable unit; Plan 05 (later in this phase) adds the failure-path regression test asserting rollback behavior.
- All existing tests (62 unit tests, 201 integration tests including the `QuestController_CreateFollowUp_*` suite) pass unchanged against the delegating action, confirming zero behavior change to the follow-up quest creation flow.
- No blockers for subsequent plans in this phase.

---
*Phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro*
*Completed: 2026-07-02*

## Self-Check: PASSED

All created/modified files confirmed present on disk; all three task commits (124129c, fe696b3, ace6282) confirmed present in git history.
