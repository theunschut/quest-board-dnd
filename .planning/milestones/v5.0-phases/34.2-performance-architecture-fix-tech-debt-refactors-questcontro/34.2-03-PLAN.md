---
phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Repository/Entities/QuestBoardContext.cs
  - QuestBoard.Repository/Migrations
  - QuestBoard.Repository/ShopRepository.cs
  - QuestBoard.Service/Program.cs
autonomous: true
requirements: []
concerns_items:
  - "Performance: composite index on Quests(IsFinalized, FinalizedDate) (D-07)"
  - "Performance: Shop Item Queries projection — exclude image BLOB from list views (D-08)"
  - "Scaling Limits: Hangfire AutomaticRetryAttribute retry policy (not UseAutoRetry)"
  - "Fragile Areas: Query Filter documentation comments in OnModelCreating"

must_haves:
  truths:
    - "A composite index on Quests(IsFinalized, FinalizedDate) exists via an EF Core migration and applies cleanly on startup"
    - "Shop list-view queries no longer eagerly load image BLOB data that only detail views need; detail views still load full data"
    - "Hangfire jobs retry transient failures with exponential backoff (5 attempts, 1/2/4/8/16s) via a global AutomaticRetryAttribute registered in Program.cs, inside the non-Testing guard"
    - "OnModelCreating has descriptive comments explaining why QuestEntity/ShopItemEntity are filtered and UserEntity is intentionally not, with no ID/phase tags"
  artifacts:
    - path: "QuestBoard.Repository/Entities/QuestBoardContext.cs"
      provides: "Composite HasIndex on Quests(IsFinalized, FinalizedDate) + documented query-filter block"
      contains: "IsFinalized, q.FinalizedDate"
    - path: "QuestBoard.Service/Program.cs"
      provides: "Global Hangfire AutomaticRetryAttribute retry policy"
      contains: "AutomaticRetryAttribute"
    - path: "QuestBoard.Repository/ShopRepository.cs"
      provides: "List-view queries projected to exclude unnecessary BLOB loading"
      contains: "GetPublishedItemsAsync"
  key_links:
    - from: "QuestBoard.Repository/Migrations"
      to: "QuestBoard.Repository/Entities/QuestBoardContext.cs"
      via: "AddQuestFinalizedDateIndex migration generated from the new HasIndex call"
      pattern: "AddQuestFinalizedDateIndex"
    - from: "QuestBoard.Service/Program.cs"
      to: "Hangfire"
      via: "GlobalJobFilters.Filters.Add(new AutomaticRetryAttribute{...}) inside the non-Testing Hangfire block"
      pattern: "GlobalJobFilters.Filters.Add"
---

<objective>
Land the Repository/config performance and reliability fixes as one disjoint plan:

1. **Composite index (D-07)** — add `HasIndex(q => new { q.IsFinalized, q.FinalizedDate })` on `QuestEntity` in `OnModelCreating`, generate the `AddQuestFinalizedDateIndex` migration. Implement unconditionally regardless of current query time (D-07 is the scale-readiness lever).
2. **Shop projection (D-08)** — exclude unnecessary BLOB loading from shop **list** queries while leaving detail queries intact. Implement unconditionally (D-08). Because `ShopItemEntity` has no direct image column (verify against the live file), scope the fix to whatever BLOB-bearing navigation the list queries currently pull transitively — see Task 2.
3. **Hangfire retry (Scaling Limits)** — add a global `AutomaticRetryAttribute` (5 attempts, 1/2/4/8/16s backoff) in `Program.cs`, inside the existing `if (!builder.Environment.IsEnvironment("Testing"))` Hangfire block. Use `AutomaticRetryAttribute`, NOT the nonexistent `UseAutoRetry` API (Phase 34 RESEARCH.md correction).
4. **Query-filter docs (Fragile Areas)** — add descriptive comments in `OnModelCreating` explaining the per-entity filter decisions (comment-only, no filter-behavior change; do NOT add a UserEntity filter).

Purpose: Close the Performance Bottleneck, Scaling Limit, and Fragile-Area items whose files live in the Repository/config tier.
Output: A new EF migration, a projected shop list query, a Hangfire retry filter, and clarifying query-filter comments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/34.2-performance-architecture-fix-tech-debt-refactors-questcontro/34.2-CONTEXT.md
@.planning/phases/34.2-performance-architecture-fix-tech-debt-refactors-questcontro/34.2-PATTERNS.md

# Source files this plan modifies (read before editing)
@QuestBoard.Repository/Entities/QuestBoardContext.cs
@QuestBoard.Repository/ShopRepository.cs
@QuestBoard.Repository/Entities/ShopItemEntity.cs
@QuestBoard.Service/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add composite index on Quests(IsFinalized, FinalizedDate) + generate migration</name>
  <files>QuestBoard.Repository/Entities/QuestBoardContext.cs, QuestBoard.Repository/Migrations</files>
  <read_first>
    - QuestBoard.Repository/Entities/QuestBoardContext.cs lines 84-87, 189-191, 196-198, 201-203 — the 4 existing composite `HasIndex` calls; copy this fluent style. The unique ones use `.IsUnique()`; the new one is NON-unique (query-performance only, no `.IsUnique()`).
    - QuestBoard.Repository/Entities/QuestBoardContext.cs lines 205-210 — the nearby FK config comments embed stale `(D-10, GROUP-03)` ID tags. Per Phase 34's comment sweep + 34.2-PATTERNS.md research correction #8, the NEW comment for this index must NOT introduce any `(D-xx)` / phase / requirement-ID tag — write a plain descriptive comment.
    - CLAUDE.md "Entity Framework" — migrations are added from `QuestBoard.Service/` via `dotnet ef migrations add <Name> --project ../QuestBoard.Repository`; auto-applied on startup via `context.Database.Migrate()` (no manual `database update`).
    - QuestBoard.Repository/Migrations/ — existing migration file naming (`<timestamp>_<Name>.cs` + `.Designer.cs`); `QuestBoardContextModelSnapshot.cs` is regenerated by the tooling.
  </read_first>
  <action>
    In `OnModelCreating`, alongside the other `modelBuilder.Entity<...>().HasIndex(...)` calls (e.g. after the ReminderLog index around line 191, or near the QuestEntity FK config), add:
    a plain descriptive comment (e.g. "Speeds up session-reminder / finalized-quest sweep queries") followed by
    `modelBuilder.Entity<QuestEntity>().HasIndex(q => new { q.IsFinalized, q.FinalizedDate });` — NON-unique (do NOT add `.IsUnique()`). Do NOT put any `(D-xx)` / phase / requirement-ID text in the comment.

    Then generate the migration. Run from the `QuestBoard.Service/` directory:
    `dotnet ef migrations add AddQuestFinalizedDateIndex --project ../QuestBoard.Repository`
    This creates `QuestBoard.Repository/Migrations/<timestamp>_AddQuestFinalizedDateIndex.cs` (+ `.Designer.cs`) and updates `QuestBoardContextModelSnapshot.cs`. Confirm the generated `Up()` calls `CreateIndex(...)` on the `Quests` table over `IsFinalized` and `FinalizedDate` (and `Down()` drops it). Do NOT hand-edit the generated migration beyond what the tooling produces; do NOT edit any prior migration file (append-only per Phase 34 RESEARCH.md Pitfall 2).

    If the build has locked output files (Visual Studio debugger running), STOP and report per CLAUDE.md — do not force.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoardContext.OnModelCreating` contains `modelBuilder.Entity<QuestEntity>().HasIndex(q => new { q.IsFinalized, q.FinalizedDate });` with NO `.IsUnique()`.
    - The accompanying comment contains no `(D-`, `Phase`, or requirement-ID token.
    - A new migration `<timestamp>_AddQuestFinalizedDateIndex.cs` exists whose `Up()` creates an index on `Quests(IsFinalized, FinalizedDate)` and whose `Down()` drops it.
    - `QuestBoardContextModelSnapshot.cs` reflects the new index; no prior migration file was edited.
    - `dotnet build QuestBoard.Repository` succeeds.
  </acceptance_criteria>
  <done>A non-unique composite index on Quests(IsFinalized, FinalizedDate) exists via a clean EF migration that auto-applies on startup.</done>
</task>

<task type="auto">
  <name>Task 2: Project shop list queries to avoid unnecessary BLOB loading (D-08)</name>
  <files>QuestBoard.Repository/ShopRepository.cs</files>
  <read_first>
    - QuestBoard.Repository/ShopRepository.cs (whole file) — 7 query methods. List-returning methods: `GetAllAsync` (14-19, `.Include(CreatedByDm)` + `.Include(Transactions)`), `GetPublishedItemsAsync` (21-31), `GetItemsByStatusAsync` (33-42), `GetItemsByTypeAsync` (44-54), `GetItemsByDmAsync` (66-74), `GetPagedPublishedItemsAsync` (76-118). Detail method: `GetItemWithDetailsAsync` (56-64, `.Include(Transactions).ThenInclude(User)`).
    - QuestBoard.Repository/Entities/ShopItemEntity.cs (whole file) — CONFIRM: `ShopItemEntity` has NO image `byte[]` column of its own. Its navigations are `CreatedByDm` (UserEntity), `Group` (GroupEntity), `Transactions`. The BLOB concern from CONCERNS.md is transitive/adjacent — the actionable list-view over-fetch here is the `.Include(si => si.Transactions)` chain pulled by `GetAllAsync`, which list callers do not need. `UserEntity` has no image BLOB; DM/character images live on separate entities not loaded by these shop queries.
    - CONCERNS.md "Performance → Shop Item Queries Not Optimized" (lines 198-212) — the suggested approach is scoping `.Include`/`.ThenInclude` so list views load less than detail views. D-08 says implement unconditionally.
  </read_first>
  <action>
    Based on the confirmed entity shape, implement the minimal, behavior-safe list-view projection fix: remove the eager `.Include(si => si.Transactions)` from `GetAllAsync` (line 15) — list views render Name/Price/Type/Status and do not need each item's full transaction collection; only the detail view (`GetItemWithDetailsAsync`) needs `.Include(si => si.Transactions).ThenInclude(t => t.User)`, which stays. This eliminates the largest unnecessary related-collection load from the list path while leaving detail loading intact (D-08 intent: list views load less than detail views).

    Before removing the include, verify no caller of `GetAllAsync` reads `ShopItem.Transactions` for list rendering — if a caller (e.g. a management list view) genuinely needs a transaction count or similar, keep a projected/count-only load rather than the full `.Include`, OR leave that specific method and instead confirm the other list methods (`GetPublishedItemsAsync`, `GetItemsByStatusAsync`, `GetItemsByTypeAsync`, `GetItemsByDmAsync`, `GetPagedPublishedItemsAsync`) already omit `Transactions` (they do, per the read). If verification shows `GetAllAsync`'s callers require `Transactions`, do NOT remove it; instead document (plain comment, no ID tag) that the remaining list methods are already BLOB/collection-lean and the projection concern is satisfied. Prefer the smallest change that provably reduces list-view over-fetch without breaking a consumer.

    Do NOT add `.AsNoTracking()` or `.AsSplitQuery()` speculatively (behavior/perf change beyond scope). Do NOT introduce any `// D-xx` / phase / requirement-ID comment. Add a plain comment on the affected method explaining that list views intentionally omit the transaction collection that only the detail view loads.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository</automated>
  </verify>
  <acceptance_criteria>
    - A shop list-returning query no longer eagerly loads a related collection that only the detail view requires (either `GetAllAsync` drops `.Include(si => si.Transactions)`, OR — if a caller needs it — a documented confirmation that the remaining list methods are already lean).
    - `GetItemWithDetailsAsync` still includes `.Include(si => si.Transactions).ThenInclude(t => t.User)` (detail view unchanged).
    - No `.AsNoTracking()`/`.AsSplitQuery()` added; no caller of the changed method is broken (verified by build + existing shop tests in the wave gate).
    - No `// D-`, `// Phase`, or requirement-ID comment introduced.
    - `dotnet build QuestBoard.Repository` succeeds.
  </acceptance_criteria>
  <done>Shop list queries load only what list views render; detail-view loading is untouched; no consumer is broken.</done>
</task>

<task type="auto">
  <name>Task 3: Register global Hangfire AutomaticRetryAttribute retry policy in Program.cs</name>
  <files>QuestBoard.Service/Program.cs</files>
  <read_first>
    - QuestBoard.Service/Program.cs lines 216-248 — the `if (!builder.Environment.IsEnvironment("Testing"))` block wrapping `AddHangfire(...)` and `AddHangfireServer(...)`. The retry filter MUST go inside this guard so it is never registered in the Testing environment (where Hangfire is skipped and a no-op dispatcher is used).
    - PATTERNS.md "Program.cs — Hangfire global retry filter" and Phase 34 RESEARCH.md "Code Examples → Hangfire global retry configuration" — the confirmed API: `GlobalJobFilters.Filters.Add(new AutomaticRetryAttribute { Attempts = 5, DelaysInSeconds = new[] { 1, 2, 4, 8, 16 } });`. `UseAutoRetry` is NOT a real Hangfire API — do NOT use it.
    - Confirm `using Hangfire;` is already present at the top of Program.cs (the file already calls `AddHangfire`/`AddHangfireServer`); `AutomaticRetryAttribute` and `GlobalJobFilters` live in the `Hangfire` namespace.
  </read_first>
  <action>
    Inside the existing `if (!builder.Environment.IsEnvironment("Testing"))` block (after the `AddHangfire(...)` registration, around line 236, before or after `AddHangfireServer`), add exactly once:
    `GlobalJobFilters.Filters.Add(new AutomaticRetryAttribute { Attempts = 5, DelaysInSeconds = new[] { 1, 2, 4, 8, 16 } });`
    with a plain descriptive comment (e.g. "Exponential backoff for transient job failures — 5 attempts over 1/2/4/8/16 seconds"). Do NOT add a `(D-xx)` / phase / requirement-ID comment.

    Verify there is no pre-existing `GlobalJobFilters.Filters.Add(...)` call elsewhere (there is none per PATTERNS.md — add it once). Ensure `using Hangfire;` resolves `AutomaticRetryAttribute`/`GlobalJobFilters`; add the using only if missing. Do NOT place the call outside the non-Testing guard (it would register in the test host, which does not run Hangfire).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - `Program.cs` contains exactly one `GlobalJobFilters.Filters.Add(new AutomaticRetryAttribute { Attempts = 5, DelaysInSeconds = new[] { 1, 2, 4, 8, 16 } });`.
    - The call is inside the `if (!builder.Environment.IsEnvironment("Testing"))` Hangfire block (not in the Testing path, not at top level).
    - The code uses `AutomaticRetryAttribute` (grep confirms NO `UseAutoRetry` anywhere).
    - No `(D-`, `Phase`, or requirement-ID comment introduced.
    - `dotnet build QuestBoard.Service` succeeds.
  </acceptance_criteria>
  <done>Hangfire jobs retry transient failures 5 times with 1/2/4/8/16s exponential backoff via a global filter registered only in non-Testing environments.</done>
</task>

<task type="auto">
  <name>Task 4: Document the per-entity query-filter decisions in OnModelCreating</name>
  <files>QuestBoard.Repository/Entities/QuestBoardContext.cs</files>
  <read_first>
    - QuestBoard.Repository/Entities/QuestBoardContext.cs lines 233-249 — the current query-filter block. Line 233 has `// TENANT-03: Global query filters for group isolation` (a stale ID-tagged comment — Phase 34's sweep targets it, but this plan may improve the surrounding descriptive comments per the Fragile-Areas item). Lines 236-237 already carry the CRITICAL "do not capture into a local var" landmine comment (KEEP — D-08 useful comment). Line 248-249 already documents why UserEntity is excluded (KEEP/extend).
    - ARCHITECTURE.md "Anti-Patterns → Query Filter Application Inconsistent" and CONCERNS.md "Fragile Areas → Query Filter Application Inconsistent" — the fix is comment-only: explain why QuestEntity/ShopItemEntity are filtered and why UserEntity (and Character/PlayerSignup) are NOT. Do NOT add a filter to any currently-unfiltered entity.
  </read_first>
  <action>
    Improve the descriptive comments around the `HasQueryFilter` block (comment-only — no code/filter change):
    - Above the `QuestEntity`/`ShopItemEntity` `HasQueryFilter` calls, ensure a plain comment states that these two entities are group-scoped and that `null` ActiveGroupId means "see all" (SuperAdmin / seeding).
    - Preserve the existing CRITICAL "do NOT capture ActiveGroupId into a local var" comment verbatim (lines 236-237) — it is a genuine landmine warning (D-08).
    - Preserve/extend the existing "UserEntity intentionally excluded — HasQueryFilter breaks Identity" comment (lines 248-249). Optionally add one line noting that other entities (Character, PlayerSignup) are also unfiltered because they are reached only through group-scoped Quests/ShopItems.
    - When rewriting the top-of-block comment, do NOT reintroduce the `TENANT-03` tag or any `(D-xx)` / phase / requirement-ID text — write plain prose only.

    Make NO change to the `HasQueryFilter` lambdas themselves. Do NOT add `HasQueryFilter` to `UserEntity`, `CharacterEntity`, or `PlayerSignupEntity` (would break Identity / correctness — ARCHITECTURE.md landmine). This task is purely the Fragile-Areas documentation deliverable; the runtime-behavior null-guard is Plan 05 (added at a read-site, NOT in this lambda).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository</automated>
  </verify>
  <acceptance_criteria>
    - The query-filter block has plain descriptive comments explaining why QuestEntity/ShopItemEntity are filtered and why UserEntity is not.
    - The CRITICAL "do not capture into a local var" landmine comment is preserved.
    - No `HasQueryFilter` lambda is modified; no filter is added to UserEntity/CharacterEntity/PlayerSignupEntity.
    - No new `TENANT-`, `(D-`, `Phase`, or requirement-ID comment token is introduced by this task.
    - `dotnet build QuestBoard.Repository` succeeds.
  </acceptance_criteria>
  <done>OnModelCreating clearly documents the intentional per-entity filter decisions with no behavior change and no ID tags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request/job → EF Core query filter | ActiveGroupId scopes Quest/ShopItem reads; the filter is a safety net, not the primary boundary |
| Hangfire job → external mail relay | Transient upstream failures (SMTP/Resend) must degrade via bounded retry, not silent loss |
| DB schema migration → running app | The new index migration auto-applies on startup; must be additive and reversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-31 | Information Disclosure | EF Core Global Query Filter (ASVS V4 Access Control) | accept (unchanged) + document | Task 4 documents the per-entity filter decisions so a future dev does not accidentally add a UserEntity filter (breaking Identity) or forget to scope a new entity; no runtime behavior change. The runtime null-guard is added in Plan 05. |
| T-34.2-32 | Denial of Service | Hangfire transient job failures | mitigate | Task 3 adds bounded exponential-backoff retry (5 attempts, 1/2/4/8/16s) so transient SMTP/Resend/DB blips recover instead of exhausting default immediate retries; bounded so no retry storm. |
| T-34.2-33 | Tampering / Availability | Index migration on production schema | mitigate | Additive non-unique index with a reversible `Down()`; generated by tooling, no data change, applies via existing `Database.Migrate()`. |
| T-34.2-SC | Tampering | NuGet installs | mitigate | No new packages; `AutomaticRetryAttribute` ships with the already-installed Hangfire 1.8.23. Phase 34 RESEARCH.md confirmed a clean vulnerability scan. |
</threat_model>

<verification>
- `dotnet build` succeeds across the solution.
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Shop"` still green (list/detail shop queries behavior-preserving).
- `dotnet test` (full suite) green after the wave — reminder/finalize tests must still pass unmodified after the index migration.
- Manual (D-08): compare shop list page load before/after; confirm no missing-image regression on the detail view.
- Manual: confirm the new migration's `Up()` creates the index and applies cleanly on the dev SQL Server on next startup.
</verification>

<success_criteria>
- Non-unique composite index on Quests(IsFinalized, FinalizedDate) via a clean, reversible migration.
- Shop list queries provably lean vs. detail queries (D-08), no consumer broken.
- Global Hangfire `AutomaticRetryAttribute` (5×, 1/2/4/8/16s) registered only in non-Testing environments.
- Query-filter decisions documented; no filter-behavior change; no ID tags introduced.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (Plan 03)

New symbols / migration:
- EF migration `AddQuestFinalizedDateIndex` (new `<timestamp>_AddQuestFinalizedDateIndex.cs` + `.Designer.cs`) creating the non-unique composite index on `Quests(IsFinalized, FinalizedDate)`
- Global Hangfire retry filter: `GlobalJobFilters.Filters.Add(new AutomaticRetryAttribute { Attempts = 5, DelaysInSeconds = new[] { 1, 2, 4, 8, 16 } })` in `Program.cs`

Modified symbols / code paths:
- `QuestBoardContext.OnModelCreating` — new `HasIndex` on QuestEntity + improved query-filter documentation comments
- `ShopRepository` list query (`GetAllAsync` or documented-lean confirmation) — reduced list-view over-fetch
- `QuestBoardContextModelSnapshot.cs` — regenerated by tooling

New test classes/methods: none (the Hangfire-retry-behavior test and group-filter enforcement test land in Plan 05, which depends on this plan)
</artifacts_produced>

<output>
Create `.planning/phases/34.2-performance-architecture-fix-tech-debt-refactors-questcontro/34.2-03-SUMMARY.md` when done
</output>
