---
phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro
plan: 04
subsystem: infra
tags: [hangfire, background-jobs, service-scope, documentation, tech-debt]

# Dependency graph
requires:
  - phase: 34.2-03
    provides: Composite index and shop-item projection performance fixes; established the wave's build/test baseline this plan builds on
provides:
  - HangfireJobHelper.RunInScopeAsync — static scope + group-context wrapper shared by all Hangfire jobs
  - Documentation record of every deferred/documentation-only decision from 34.2-CONTEXT.md (D-06, D-09, dependency notes, enum convention)
affects: [34.2-05 (enum round-trip validation test references the CONVENTIONS.md note added here)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "HangfireJobHelper.RunInScopeAsync(scopeFactory, groupId, action) — centralizes the create-scope + optional SetGroupId + delegate-invoke + dispose sequence for all Hangfire jobs"

key-files:
  created:
    - QuestBoard.Service/Jobs/HangfireJobHelper.cs
  modified:
    - QuestBoard.Service/Jobs/SessionReminderJob.cs
    - QuestBoard.Service/Jobs/DailyReminderJob.cs
    - QuestBoard.Service/Jobs/QuestFinalizedEmailJob.cs
    - .planning/codebase/ARCHITECTURE.md
    - .planning/codebase/CONCERNS.md
    - .planning/codebase/CONVENTIONS.md

key-decisions:
  - "HangfireJobHelper.RunInScopeAsync only calls SetGroupId when groupId is non-null, preserving DailyReminderJob's intentional cross-group sweep (ActiveGroupId=null) while guaranteeing SetGroupId always runs before any repository resolution in the group-scoped jobs"
  - "Forbid() defense-in-depth (D-06) and Hangfire batching (D-09) remain documentation-only — no code implementing either was written, per the mandatory scope exclusion"

patterns-established:
  - "Hangfire job scope management: call HangfireJobHelper.RunInScopeAsync instead of hand-rolling CreateAsyncScope()+SetGroupId in each job"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-07-02
---

# Phase 34.2 Plan 04: Hangfire Job Helper + Deferred-Decision Documentation Summary

**Formalized the repeated Hangfire scope/group-context pattern into `HangfireJobHelper.RunInScopeAsync` and recorded every documentation-only decision (Forbid() deferral, Hangfire fan-out rationale, Identity/Resend dependency notes, enum convention) with zero behavior change.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-07-02T08:21:43Z
- **Completed:** 2026-07-02T08:25:39Z
- **Tasks:** 2
- **Files modified:** 7 (1 created, 6 modified)

## Accomplishments
- Created `HangfireJobHelper.RunInScopeAsync` — a single static wrapper for the scope-create + optional-`SetGroupId` + delegate-invoke + `await using` dispose sequence every Hangfire job previously repeated inline
- Refactored `SessionReminderJob`, `DailyReminderJob`, and `QuestFinalizedEmailJob` to use the helper with zero behavior change — same services resolved in the same order, same SetGroupId-before-repository guarantee, same fan-out loop
- Recorded documentation-only notes for D-06 (Forbid() deferred hardening), D-09 (Hangfire fan-out is intentional, batching is monitored future work), the Identity-email-routing/Resend-SPOF dependency notes, and the enum append-only/no-reorder convention

## Task Commits

Each task was committed atomically:

1. **Task 1: Create HangfireJobHelper and refactor the three jobs to use it** - `ceb99c2` (refactor)
2. **Task 2: Write the documentation-only notes (Forbid() D-06, Hangfire batching D-09, dependencies, enum convention)** - `a7919f6` (docs)

**Plan metadata:** _(added in this same commit sequence — see final commit below)_

## Files Created/Modified
- `QuestBoard.Service/Jobs/HangfireJobHelper.cs` - New internal static class; `RunInScopeAsync(IServiceScopeFactory, int?, Func<IServiceProvider, Task>)` creates the async scope, conditionally sets group context, invokes the delegate, disposes the scope
- `QuestBoard.Service/Jobs/SessionReminderJob.cs` - `ExecuteAsync` body moved into the `HangfireJobHelper.RunInScopeAsync` delegate; services resolved from the delegate's `IServiceProvider`
- `QuestBoard.Service/Jobs/QuestFinalizedEmailJob.cs` - Same refactor as SessionReminderJob
- `QuestBoard.Service/Jobs/DailyReminderJob.cs` - Uses the helper with `groupId: null` to preserve its cross-group sweep; per-quest `Enqueue<SessionReminderJob>` fan-out loop unchanged
- `.planning/codebase/ARCHITECTURE.md` - New "Deferred Hardening / Scaling Notes" section (Forbid() deferral, Hangfire fan-out rationale, dependency notes); fixed a stale line reference to the SetGroupId call site
- `.planning/codebase/CONCERNS.md` - Appended "documented as deferred, not implemented" status lines to the tenant-isolation and Hangfire-batching items
- `.planning/codebase/CONVENTIONS.md` - New "Enum Handling" section recording the append-only/no-reorder rule

## Decisions Made
- `SetGroupId` is only invoked inside the helper when `groupId is not null` — this is what lets `DailyReminderJob` share the same helper as the two group-scoped jobs without accidentally overriding its intentional cross-group sweep
- Kept all genuinely useful behavioral comments (Yes/Maybe-voter explanation, FinalizedDate-timezone note) verbatim; only removed the now-redundant inline scope-management comments and the stray `RESEARCH.md Pitfall 1` reference (replaced with plain prose per CLAUDE.md's Code Comments rule)
- Removed the now-unused `using QuestBoard.Service.Services;` import from `SessionReminderJob.cs` and `QuestFinalizedEmailJob.cs` since `ActiveGroupContextService` is now only referenced inside `HangfireJobHelper`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed a stale ARCHITECTURE.md line reference**
- **Found during:** Task 2 (documentation notes)
- **Issue:** The existing "Missing Group Context in Background Jobs" anti-pattern entry pointed to `QuestFinalizedEmailJob.cs:31-32` for the `SetGroupId` call — after Task 1's refactor, that call now lives inside `HangfireJobHelper.cs`, not at that line in the job file
- **Fix:** Updated the reference to point at the correct current line for the `groupId` parameter and noted the `SetGroupId` call is now centralized in `HangfireJobHelper.cs`
- **Files modified:** `.planning/codebase/ARCHITECTURE.md`
- **Verification:** Manual read-through confirms the reference now matches the refactored code
- **Committed in:** `a7919f6` (Task 2 commit)

**2. [Rule 1 - Bug] Removed unused `using` directives left over from the refactor**
- **Found during:** Task 1 (job refactor)
- **Issue:** After moving the `ActiveGroupContextService` resolution into `HangfireJobHelper`, `SessionReminderJob.cs` and `QuestFinalizedEmailJob.cs` no longer referenced `QuestBoard.Service.Services` directly, leaving a dead `using`
- **Fix:** Removed the unused `using QuestBoard.Service.Services;` line from both files
- **Files modified:** `QuestBoard.Service/Jobs/SessionReminderJob.cs`, `QuestBoard.Service/Jobs/QuestFinalizedEmailJob.cs`
- **Verification:** `dotnet build QuestBoard.Service` still succeeds with 0 warnings after removal
- **Committed in:** `ceb99c2` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - bug/cleanup, no scope change)
**Impact on plan:** Both fixes are small correctness/cleanliness items discovered as a direct consequence of the planned refactor. No scope creep — no code implementing Forbid() or batching was written, matching the plan's mandatory exclusions.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Known Stubs
None.

## Threat Flags
None - this plan introduced no new network endpoints, auth paths, file access patterns, or schema changes. `HangfireJobHelper` centralizes an existing trust-boundary control (group-context setup before repository access) rather than introducing new surface.

## Next Phase Readiness
- All three Hangfire jobs share one scope/group-context helper; the "Fragile Areas" item for manual scope management is closed
- Every documentation-only decision from 34.2-CONTEXT.md that this plan owned (D-06, D-09, dependency notes, enum convention) is recorded in the codebase docs
- Plan 05 can proceed to add the enum round-trip validation test and null-guard test referenced by the CONVENTIONS.md note added here
- Full solution build: 0 errors, 0 warnings. Unit tests: 62/62 passed. Integration tests: 201/201 passed.

---
*Phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro*
*Completed: 2026-07-02*

## Self-Check: PASSED

- FOUND: `QuestBoard.Service/Jobs/HangfireJobHelper.cs`
- FOUND: `.planning/codebase/ARCHITECTURE.md`
- FOUND: commit `ceb99c2`
- FOUND: commit `a7919f6`
