---
phase: 26-namespace-rename
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - "(none — verification, commit, and documented manual production step only)"
autonomous: false
requirements: [RENAME-04]
user_setup:
  - service: production-server
    why: "Renamed DLL (QuestBoard.Service.dll) requires the systemd unit ExecStart path to be updated before the first deploy from this branch, or the service fails to start"
    dashboard_config:
      - task: "Update systemd ExecStart DLL path and reload"
        location: "Production Linux server: /etc/systemd/system/questboard.service — change EuphoriaInn.Service.dll to QuestBoard.Service.dll, then run systemctl daemon-reload"

must_haves:
  truths:
    - "All 191 existing tests pass with zero failures after the rename"
    - "No tracked non-planning file contains the EuphoriaInn token (final confirmation)"
    - "The rename lands as a single atomic commit on the milestone/v5-multi-tenancy branch"
    - "The production systemd ExecStart manual step is documented and surfaced before deploy"
  artifacts:
    - path: ".planning/phases/26-namespace-rename/26-02-SUMMARY.md"
      provides: "Record of test results, commit SHA, and the pending manual production step"
      min_lines: 10
  key_links:
    - from: "QuestBoard.slnx"
      to: "QuestBoard.UnitTests + QuestBoard.IntegrationTests"
      via: "dotnet test runs both test projects against the renamed assemblies"
      pattern: "Passed.*Failed:\\s*0"
---

<objective>
Verify the namespace rename produced zero behavior change: run the full 191-test suite as the hard gate (RENAME-04), confirm a final grep shows no `EuphoriaInn` token remains anywhere in tracked non-planning files, then create the single atomic commit (D-05) on the `milestone/v5-multi-tenancy` branch. Surface the production systemd `ExecStart` manual step (Pitfall 4 / Open Question 1) as a blocking pre-deploy gate so the live service does not fail to start after the next deploy.

Purpose: Prove the rename is non-behavioral (the phase's core success criterion) and protect the live deployment from the DLL-name change.
Output: Green test suite, a single `refactor: rename EuphoriaInn -> QuestBoard` commit, and a documented production server gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-namespace-rename/26-CONTEXT.md
@.planning/phases/26-namespace-rename/26-RESEARCH.md
@.planning/phases/26-namespace-rename/26-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces no new source symbols. It produces:
- A single git commit on `milestone/v5-multi-tenancy`: `refactor: rename EuphoriaInn -> QuestBoard` (D-05)
- `.planning/phases/26-namespace-rename/26-02-SUMMARY.md`
- A documented (not executed) manual production-server step: updating `/etc/systemd/system/questboard.service` ExecStart from `EuphoriaInn.Service.dll` to `QuestBoard.Service.dll`. The systemd service NAME (`questboard`) is unchanged — only the DLL path inside ExecStart changes.

Newly-referenced assembly name (for drift exclusion): `QuestBoard.Service.dll` (was `EuphoriaInn.Service.dll`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full test gate and final grep-clean verification</name>
  <files>QuestBoard.slnx (test target — no edits)</files>
  <read_first>
    - .planning/phases/26-namespace-rename/26-RESEARCH.md (Verified dotnet test gate command; Validation Architecture — Phase Requirements -> Test Map; Pitfalls 1-3 — what test failures would indicate)
    - .planning/phases/26-namespace-rename/26-PATTERNS.md (Post-Replace Verification Commands — steps 1, 2, 4)
    - .planning/phases/26-namespace-rename/26-01-SUMMARY.md (confirm build was green and no commit was made)
  </read_first>
  <action>
    Run `dotnet test QuestBoard.slnx --verbosity normal` from the repo root. The result must show 0 failures (the suite is currently 191 passing). If any test fails, the failure mode maps directly to a known pitfall: an `InvalidOperationException: No entity type with name 'EuphoriaInn.Repository.Entities.*' was found` means a migration Designer.cs / ModelSnapshot FQN was missed (Pitfall 1) — re-check `QuestBoard.Repository/Migrations/`; an `InaccessibleDueToProtectionLevel` build/test error means the `InternalsVisibleTo` attribute was not updated (Pitfall 2) — re-check `QuestBoard.Domain/Properties/AssemblyInfo.cs`; a `FileNotFoundException` for `mobile.css` or a path containing `EuphoriaInn.Service` means an integration-test directory-path string literal was missed (Pitfall 3) — re-check `MobileCssTests.cs` and `QuestFinalizeTests.cs`. Fix the specific file (complete the missed token replacement), re-run the build (`dotnet build QuestBoard.slnx --no-incremental`) and the test suite until all-green. Then run the two final grep scans to confirm zero `EuphoriaInn` remains: one across the five project directories for `.cs`/`.cshtml`/`.razor`, and one across `.github Dockerfile create-migration.sh CLAUDE.md README.md docs`. Both must return empty. Do NOT commit in this task — Task 2 owns the commit.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.slnx --verbosity normal && test -z "$(grep -rl 'EuphoriaInn' QuestBoard.Domain QuestBoard.Repository QuestBoard.Service QuestBoard.UnitTests QuestBoard.IntegrationTests --include='*.cs' --include='*.cshtml' --include='*.razor' 2>/dev/null)" && echo TESTS_AND_GREP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.slnx --verbosity normal` reports `Failed: 0` (all 191 tests pass) — satisfies RENAME-04 and the phase "zero behavior change" criterion
    - `grep -rl 'EuphoriaInn' QuestBoard.Domain QuestBoard.Repository QuestBoard.Service QuestBoard.UnitTests QuestBoard.IntegrationTests --include='*.cs' --include='*.cshtml' --include='*.razor'` returns empty
    - `grep -r 'EuphoriaInn' .github Dockerfile create-migration.sh CLAUDE.md README.md docs` returns empty
    - The verify command prints `TESTS_AND_GREP_OK`
  </acceptance_criteria>
  <done>All 191 tests pass with zero failures; final grep confirms zero EuphoriaInn token in source and in config/CI/docs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of zero behavior change before commit</name>
  <action>Pause for human verification. Present the test-gate result and the local-run steps below, then wait for the resume signal before proceeding to the atomic commit. Do not commit until the human approves.</action>
  <what-built>
    The entire codebase has been renamed from EuphoriaInn.* to QuestBoard.*: five directories, the solution file, five .csproj files, all 343 tracked source/config/CI/doc files, and 53 EF migration files (patched in-place per D-01). dotnet build succeeded and all 191 tests passed. No commit has been made yet.
  </what-built>
  <how-to-verify>
    1. Confirm the test gate output from Task 1 showed `Failed: 0` for the full suite (191 passing).
    2. Run the app locally to confirm it starts and the quest board loads: `dotnet run --project QuestBoard.Service` then open the site in a browser. Confirm the home/quest board page renders (migrations auto-apply on startup against your local SQL Server).
    3. Spot-check that the UI shows no broken pages — the core loop (DM posts a quest, player can view it) should work exactly as before. There is no visible "EuphoriaInn" text in the UI (confirmed in CONTEXT D-03), so the page content should look identical to before the rename.
    4. Confirm via `git status --short` that directory renames appear as `R` (renames), not delete+add — this proves git history is preserved.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to the atomic commit, or describe any broken behavior to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Single atomic commit and document the production systemd manual step</name>
  <files>(git commit only — no file edits beyond the SUMMARY this plan already writes)</files>
  <read_first>
    - .planning/phases/26-namespace-rename/26-CONTEXT.md (D-05 — single atomic commit covering all changes; exact commit message intent)
    - .planning/phases/26-namespace-rename/26-RESEARCH.md (Runtime State Inventory — systemd ExecStart; Pitfall 4; Open Question 1 — manual server step timing)
    - CLAUDE.md (Branching — never commit to main; milestone/v5-multi-tenancy is the working branch)
  </read_first>
  <action>
    First confirm the current branch is `milestone/v5-multi-tenancy` (per CLAUDE.md, never commit to main). Stage ALL rename changes — the directory renames, the .csproj/.slnx renames, every patched source/config/CI/doc file, and the patched migration files — and create ONE commit (D-05): `refactor: rename EuphoriaInn -> QuestBoard`. The body should note this is a non-behavioral mechanical rename, that all 191 tests pass, and that migration files were patched in-place to preserve the deployed `__EFMigrationsHistory`. Do not split into multiple commits. After committing, surface the production-server manual step prominently in the SUMMARY and in the return to the orchestrator: before the first deploy from this branch, an admin must SSH to the production server and update `/etc/systemd/system/questboard.service` so ExecStart points to `QuestBoard.Service.dll` (currently `EuphoriaInn.Service.dll`), then run `systemctl daemon-reload`. The systemd service NAME stays `questboard`; only the DLL path changes. This is a blocking pre-deploy gate (Pitfall 4): skipping it makes `systemctl start questboard` fail silently because the old DLL path no longer exists. This step is NOT executed here — it is documented as a required out-of-band action.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q 'rename EuphoriaInn' && test "$(git rev-parse --abbrev-ref HEAD)" = "milestone/v5-multi-tenancy" && test -z "$(git status --porcelain)" && echo COMMIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit exists with a subject matching `refactor: rename EuphoriaInn -> QuestBoard`
    - The commit is on branch `milestone/v5-multi-tenancy`, not `main`
    - `git status --porcelain` is empty after the commit (all rename changes staged and committed together — D-05)
    - The SUMMARY documents the production systemd ExecStart manual step as a blocking pre-deploy gate with the exact path `/etc/systemd/system/questboard.service` and the `QuestBoard.Service.dll` target
    - The verify command prints `COMMIT_OK`
  </acceptance_criteria>
  <done>One atomic commit on milestone/v5-multi-tenancy renames the codebase; the production systemd manual step is documented as a blocking pre-deploy gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renamed build output → production server | The DLL name changed to QuestBoard.Service.dll; the systemd unit on the live server still references the old name until manually updated |
| developer → main branch | CLAUDE.md branch protection: this commit must land on milestone/v5-multi-tenancy, never main |

## STRIDE Threat Register

ASVS Level 1. No new auth/input/crypto surface — this plan only runs tests, commits, and documents a manual step. No "high" severity threats.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Denial of Service | Production systemd unit (questboard.service) | mitigate | Document the ExecStart DLL-path update as a BLOCKING pre-deploy gate (Pitfall 4); without it `systemctl start questboard` fails silently after deploy. Surfaced in SUMMARY + orchestrator return |
| T-26-05 | Tampering | Commit to wrong branch | mitigate | Task 3 verifies `HEAD == milestone/v5-multi-tenancy` before accepting the commit; CLAUDE.md forbids commits to main |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH confirms zero packages added; legitimacy audit not applicable |
</threat_model>

<verification>
1. `dotnet test QuestBoard.slnx --verbosity normal` reports `Failed: 0` (RENAME-04)
2. Final grep across source + config/CI/docs returns empty
3. Human confirms the running app behaves identically (zero behavior change)
4. Exactly one commit `refactor: rename EuphoriaInn -> QuestBoard` exists on milestone/v5-multi-tenancy (D-05)
5. Production systemd manual step documented as a blocking pre-deploy gate (Pitfall 4)
</verification>

<success_criteria>
- All 191 tests pass with zero failures (RENAME-04, zero behavior change)
- Final grep confirms no EuphoriaInn token remains anywhere in tracked non-planning files
- The rename is a single atomic commit on milestone/v5-multi-tenancy (D-05)
- The production systemd ExecStart manual step is documented as a blocking pre-deploy gate
</success_criteria>

<output>
Create `.planning/phases/26-namespace-rename/26-02-SUMMARY.md` when done. Record: test pass count, the commit SHA + subject, and a prominent "MANUAL PRE-DEPLOY GATE" section detailing the systemd ExecStart update on the production server.
</output>
