---
phase: 34-codebase-cleanup-and-security-hardening-remove-unused-code-s
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Domain/Interfaces/IActiveGroupContext.cs
  - QuestBoard.Domain/Interfaces/IQuestService.cs
  - QuestBoard.Domain/Interfaces/IQuestRepository.cs
  - QuestBoard.Domain/Interfaces/IUserService.cs
  - QuestBoard.Domain/Interfaces/IUserRepository.cs
  - QuestBoard.Domain/Interfaces/IIdentityService.cs
  - QuestBoard.Domain/Interfaces/IQuestEmailDispatcher.cs
  - QuestBoard.Domain/Interfaces/IReminderJobDispatcher.cs
  - QuestBoard.Domain/Interfaces/IBaseService.cs
  - QuestBoard.Domain/Interfaces/IBaseRepository.cs
  - QuestBoard.Domain/Interfaces/IEmailService.cs
  - QuestBoard.Domain/Interfaces/IEmailRenderService.cs
  - QuestBoard.Domain/Interfaces/IShopService.cs
  - QuestBoard.Domain/Interfaces/IShopRepository.cs
  - QuestBoard.Domain/Interfaces/IShopSeedService.cs
  - QuestBoard.Domain/Interfaces/ICharacterService.cs
  - QuestBoard.Domain/Interfaces/ICharacterRepository.cs
  - QuestBoard.Domain/Interfaces/IPlayerSignupService.cs
  - QuestBoard.Domain/Interfaces/IPlayerSignupRepository.cs
  - QuestBoard.Domain/Interfaces/IGroupService.cs
  - QuestBoard.Domain/Interfaces/IGroupRepository.cs
  - QuestBoard.Domain/Interfaces/IDungeonMasterProfileService.cs
  - QuestBoard.Domain/Interfaces/IDungeonMasterProfileRepository.cs
  - QuestBoard.Domain/Interfaces/IReminderLogRepository.cs
  - QuestBoard.Domain/Interfaces/ITradeItemRepository.cs
  - QuestBoard.Domain/Interfaces/IUserTransactionRepository.cs
autonomous: true
requirements: []
tags: [cleanup, xml-docs, interfaces, d-06, d-07, d-08]

must_haves:
  truths:
    - "Every public method on the 26 Domain interfaces has a <summary>-only XML doc comment"
    - "Existing partial-coverage docs (IQuestRepository, IQuestService, IActiveGroupContext) have their embedded (D-xx)/(Phase NN) tags stripped while substantive prose is kept"
    - "The solution builds after doc additions"
  artifacts:
    - path: "QuestBoard.Domain/Interfaces/IActiveGroupContext.cs"
      provides: "Cleaned <summary> — phase reference stripped, 'null = see all' kept"
      contains: "/// <summary>"
  key_links:
    - from: "26 Domain interfaces"
      to: "their implementations in QuestBoard.Domain/Services + QuestBoard.Repository"
      via: "read each impl to write an accurate <summary> (do not guess) — D-07"
      pattern: "/// <summary>"
---

<objective>
Backfill `<summary>`-only XML doc comments on all 26 public interfaces in `QuestBoard.Domain/Interfaces/` (D-07), and strip embedded `(D-xx)` / `(Phase NN)` ID references from the 3 partial-coverage Domain interfaces' existing docs (D-06/D-08). This is the Domain half of the XML-doc backfill; the Repository half is 34-05. Interface files are disjoint from every other Wave-1 plan, so this runs fully in parallel.

Purpose: Replace the "unhelpful ID-tag comment" convention on the public contract surface with proper `<summary>` docs — the exact replacement D-07 asks for, extending the `IQuestRepository` exemplar to every Domain interface.
Output: 26 documented Domain interface files, embedded ID-tags removed, solution compiles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-CONTEXT.md
@.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@QuestBoard.Domain/Interfaces/IQuestRepository.cs
</context>

<artifacts_this_phase_produces>
This plan MODIFIES (adds XML doc comments; no new symbols, no signature changes):
- 26 existing Domain interface files listed in `files_modified` — each gains `<summary>` docs; 3 (IQuestRepository, IQuestService, IActiveGroupContext) also have embedded ID-tags stripped from prose.

No interfaces are added or removed. No method signatures change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Clean partial-coverage docs + document core Domain interfaces (13 files)</name>
  <files>QuestBoard.Domain/Interfaces/IActiveGroupContext.cs, QuestBoard.Domain/Interfaces/IQuestService.cs, QuestBoard.Domain/Interfaces/IQuestRepository.cs, QuestBoard.Domain/Interfaces/IUserService.cs, QuestBoard.Domain/Interfaces/IUserRepository.cs, QuestBoard.Domain/Interfaces/IIdentityService.cs, QuestBoard.Domain/Interfaces/IQuestEmailDispatcher.cs, QuestBoard.Domain/Interfaces/IReminderJobDispatcher.cs, QuestBoard.Domain/Interfaces/IBaseService.cs, QuestBoard.Domain/Interfaces/IBaseRepository.cs, QuestBoard.Domain/Interfaces/IEmailService.cs, QuestBoard.Domain/Interfaces/IEmailRenderService.cs, QuestBoard.Domain/Interfaces/IShopService.cs</files>
  <read_first>
    - Each of the 13 interface files in this task's files field
    - The implementation behind each interface (e.g. QuestBoard.Domain/Services/QuestService.cs for IQuestService, QuestBoard.Repository/QuestRepository.cs for the Domain IQuestRepository, QuestBoard.Service/Services/ActiveGroupContextService.cs for IActiveGroupContext) — read the impl to write an accurate summary, do NOT guess behavior
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-PATTERNS.md §XML Doc Comment Backfill (the IQuestRepository exemplar, the IQuestService multi-tag-strip example, the IActiveGroupContext phase-reference-strip example, and the "net-new doc" structure rule)
    - .planning/codebase/CONVENTIONS.md §Comments (XML-doc pattern: <summary>-only, no <param>/<returns>)
  </read_first>
  <action>
    For the 3 partial-coverage interfaces first: in `IQuestRepository.cs` strip the trailing `(D-08)` from the `GetQuestsForTomorrowAllGroupsAsync` summary prose (keep the sentence), and add `<summary>` docs to its other undocumented methods; in `IQuestService.cs` strip the inline `(D-01, D-02)`, `(D-03)`, `(D-04)`, `(D-05, D-06, D-07)`, `(D-08)` references from the existing `CreateFollowUpQuestAsync`/`GetQuestsByDungeonMasterAsync` summaries (keep every substantive sentence) and document its remaining ~10 undocumented methods; in `IActiveGroupContext.cs` strip the `(Phase 28 temporary state; Phase 29 adds IsSuperAdmin)` phase reference, keeping the substantive `Null means "see all records"` meaning. For all 13 interfaces, add a `<summary>`-only XML doc to every public member that lacks one, following the codebase convention: one-line purpose statement, plus an optional second line ONLY for a non-obvious side-effect / gotcha / scope note (e.g. "bypasses the group filter", "requires X precondition"). Do NOT add `<param>` or `<returns>` tags — the codebase convention is `<summary>`-only (verified). Do NOT change any method signature, add/remove members, or alter code. CRITICAL D-06 discipline: the new `<summary>` prose must NOT contain any GSD requirement ID, D-xx tag, or phase number — describe behavior in plain language only.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: every `public` member in each of the 13 files is immediately preceded by a `/// <summary>` block (no undocumented public members remain).
    - Source assertion: `grep -nE '///.*([A-Z]{2,12}-[0-9]{1,3}|Phase [0-9])' <each of the 13 files>` returns zero matches (no ID/phase tokens survive inside doc comments).
    - Source assertion (preserve check): `grep -c 'system-wide sweep' QuestBoard.Domain/Interfaces/IQuestRepository.cs` returns >= 1 (the GetQuestsForTomorrowAllGroupsAsync prose survived, only the tag was stripped).
    - Behavior assertion: `dotnet build` succeeds.
  </acceptance_criteria>
  <done>The 3 partial-coverage docs are tag-free and complete, the other 10 core interfaces are fully documented, no ID tokens remain in any doc, and the solution compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Document remaining Domain interfaces (13 files: shop/character/signup/group/DM-profile/reminder/trade/transaction)</name>
  <files>QuestBoard.Domain/Interfaces/IShopRepository.cs, QuestBoard.Domain/Interfaces/IShopSeedService.cs, QuestBoard.Domain/Interfaces/ICharacterService.cs, QuestBoard.Domain/Interfaces/ICharacterRepository.cs, QuestBoard.Domain/Interfaces/IPlayerSignupService.cs, QuestBoard.Domain/Interfaces/IPlayerSignupRepository.cs, QuestBoard.Domain/Interfaces/IGroupService.cs, QuestBoard.Domain/Interfaces/IGroupRepository.cs, QuestBoard.Domain/Interfaces/IDungeonMasterProfileService.cs, QuestBoard.Domain/Interfaces/IDungeonMasterProfileRepository.cs, QuestBoard.Domain/Interfaces/IReminderLogRepository.cs, QuestBoard.Domain/Interfaces/ITradeItemRepository.cs, QuestBoard.Domain/Interfaces/IUserTransactionRepository.cs</files>
  <read_first>
    - Each of the 13 interface files in this task's files field
    - The implementation behind each (e.g. QuestBoard.Domain/Services/ShopService.cs, CharacterService.cs, PlayerSignupService.cs, GroupService.cs, DungeonMasterProfileService.cs, and the matching repository impls in QuestBoard.Repository/) — read the impl to write accurate summaries
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-PATTERNS.md §XML Doc Comment Backfill (net-new doc structure rule)
  </read_first>
  <action>
    These 13 interfaces have ZERO existing XML doc coverage. For each public member, read its implementation and add a `<summary>`-only XML doc following the codebase convention: one-line purpose, optional second line for a non-obvious gotcha/side-effect/scope note. No `<param>`/`<returns>` tags. Write the summary from the actual implementation behavior — do not guess or paraphrase the method name. For generic base-derived interfaces (e.g. `IShopRepository : IBaseRepository<...>`), document only the members declared on THIS interface (the base `IBaseRepository`/`IBaseService` members are documented in Task 1); do not re-document inherited members. Do NOT change signatures or code. D-06 discipline: no ID/phase tokens in any new doc prose.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: every `public` member declared on each of the 13 files is immediately preceded by a `/// <summary>` block.
    - Source assertion: `grep -nE '///.*([A-Z]{2,12}-[0-9]{1,3}|Phase [0-9])' <each of the 13 files>` returns zero matches.
    - Behavior assertion: `dotnet build` succeeds.
  </acceptance_criteria>
  <done>All 13 remaining Domain interfaces have complete <summary> docs with no ID tokens, and the solution compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interface contract prose → maintainer understanding | Inaccurate docs mislead future maintainers about security-relevant behavior (e.g. which methods bypass the group filter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Information Disclosure | Docs on group-filter-bypassing methods (e.g. GetQuestsForTomorrowAllGroupsAsync, ASVS V4 Access Control) | mitigate | Summaries are written FROM the implementation (read_first requires reading each impl); the cross-group-sweep scope note is explicitly preserved so future callers know it bypasses tenant isolation — improves, not degrades, the access-control invariant's documentation. |
| T-34-07 | Tampering | Interface files (signatures) | accept | Only `<summary>` comment lines are added; no signature, member, or code change. Build gate catches any accidental structural edit. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this doc-only plan. |
</threat_model>

<verification>
- `dotnet build QuestBoard.slnx -c Debug` succeeds.
- Every public member across all 26 Domain interfaces has a `<summary>` block.
- No `///`-line ID/phase tokens survive in any of the 26 files.
- `grep -c 'system-wide sweep' QuestBoard.Domain/Interfaces/IQuestRepository.cs` >= 1 (D-08 preserve verified).
</verification>

<success_criteria>
- All 26 Domain interfaces have complete <summary>-only XML docs.
- Partial-coverage docs (IQuestRepository, IQuestService, IActiveGroupContext) are tag-free with prose intact.
- No ID/phase tokens in any interface doc; solution compiles.
</success_criteria>

<output>
Create `.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-04-SUMMARY.md` when done.
</output>
