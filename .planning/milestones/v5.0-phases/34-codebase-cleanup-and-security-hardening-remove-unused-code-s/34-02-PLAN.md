---
phase: 34-codebase-cleanup-and-security-hardening-remove-unused-code-s
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Domain/Services/QuestService.cs
  - QuestBoard.Repository/Entities/QuestBoardContext.cs
  - QuestBoard.Service/Program.cs
  - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs
  - QuestBoard.Service/Controllers/Admin/AdminController.cs
  - QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs
  - QuestBoard.Service/Jobs/SessionReminderJob.cs
  - QuestBoard.Service/Jobs/QuestFinalizedEmailJob.cs
  - QuestBoard.Service/Middleware/GroupSessionMiddleware.cs
autonomous: true
requirements: []
tags: [cleanup, comments, d-06, d-08]

must_haves:
  truths:
    - "No ID/phase-reference comment tags (e.g. EMAIL-04, D-06, Phase 30-04, TENANT-03, WR-03) remain in the 9 non-test source implementation files"
    - "Genuinely useful 'why'/landmine comments (e.g. QuestService.RemoveAsync manual-cleanup-order note) are left completely untouched"
    - "The solution still builds after comment removal"
  artifacts:
    - path: "QuestBoard.Domain/Services/QuestService.cs"
      provides: "ID-tags stripped, RemoveAsync business-logic comment preserved"
      contains: "Manual cleanup"
  key_links:
    - from: "comment-cleanup sweep"
      to: "9 non-test implementation files"
      via: "strip PREFIX-NN / (D-xx) / Phase-NN substrings, keep substantive prose (D-06 + D-08)"
      pattern: "[A-Z]{2,12}-[0-9]"
---

<objective>
Strip low-value ID-referencing and phase-number-referencing comments (D-06) from the 9 NON-TEST source implementation files, while preserving genuinely useful business-logic / landmine comments (D-08). This plan owns implementation files only (services, context, Program.cs, controllers, jobs, middleware) — interface files are handled by 34-04, test files by 34-03, so there is zero file overlap with the other Wave-1 plans.

Purpose: Remove the "unhelpful, unread-later" GSD-ID comments the user explicitly wants gone from production source, without deleting the "why" context that has standalone value.
Output: 9 implementation files with ID-tags removed and substantive comments intact; solution still compiles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-CONTEXT.md
@.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan MODIFIES (no new symbols created):
- 9 existing implementation files listed in `files_modified` — comment text only, no code-behavior change.

No classes, interfaces, or test files are created or deleted.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Strip ID/phase-reference comments from Domain + Repository source (2 files)</name>
  <files>QuestBoard.Domain/Services/QuestService.cs, QuestBoard.Repository/Entities/QuestBoardContext.cs</files>
  <read_first>
    - QuestBoard.Domain/Services/QuestService.cs (the ~8 ID-tagged comments AND the RemoveAsync manual-cleanup comment that must be preserved)
    - QuestBoard.Repository/Entities/QuestBoardContext.cs (the TENANT-03 and D-08 comment tags near the query-filter config)
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-PATTERNS.md §Comment Cleanup — ID/Phase-Reference Stripping (Cases 1-4, incl. Case 4 the QuestService.RemoveAsync preserve-example)
    - .planning/codebase/CONVENTIONS.md §Comments (which comments count as "keep")
  </read_first>
  <action>
    Apply the D-06/D-08 comment-cleanup decision rule from PATTERNS.md §Shared Patterns to both files. The rule: strip only the `PREFIX-NN` / `(D-xx)` / `Phase NN` ID-shaped substring from each comment; if a colon-delimited description follows the ID on the same line, KEEP that description verbatim; if a comment's ONLY content is the ID reference (e.g. a bare `References: TENANT-03, D-03` line), delete the whole line. In `QuestService.cs` the ID-tagged comments to clean include the EMAIL-04, D-06 (x2), D-11, D-01..D-04, D-04, D-03, D-05..D-07 references (~8 occurrences). CRITICAL D-08 preserve rule: the manual-cleanup-order comment inside `RemoveAsync()` ("Manual cleanup required since Quest->PlayerSignup is NoAction...") is a business-logic explanation with NO ID prefix — leave it completely untouched. In `QuestBoardContext.cs` clean the `// TENANT-03: Global query filters for group isolation` and `D-08` tags near the `HasQueryFilter` config, but KEEP the explanatory text about WHICH entities are/aren't filtered and WHY (that "why" context is D-08-preserved). Do NOT change any code, filter behavior, or the query-filter predicate — comment text only.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nE '(^|[^A-Za-z])(D-[0-9]{1,2}|EMAIL-[0-9]|TENANT-[0-9]|Phase [0-9])' QuestBoard.Domain/Services/QuestService.cs QuestBoard.Repository/Entities/QuestBoardContext.cs` returns zero comment-line matches.
    - Source assertion (preserve check): `grep -c 'Manual cleanup' QuestBoard.Domain/Services/QuestService.cs` returns 1 — the RemoveAsync business-logic comment survives.
    - Behavior assertion: `dotnet build` succeeds (comment edits must not corrupt syntax).
  </acceptance_criteria>
  <done>Both Domain/Repository source files have ID-tags removed, the RemoveAsync preserve-comment is intact, and the solution compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Strip ID/phase-reference comments from Service-layer source (7 files: Program.cs, controllers, jobs, middleware)</name>
  <files>QuestBoard.Service/Program.cs, QuestBoard.Service/Controllers/QuestBoard/QuestController.cs, QuestBoard.Service/Controllers/Admin/AdminController.cs, QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs, QuestBoard.Service/Jobs/SessionReminderJob.cs, QuestBoard.Service/Jobs/QuestFinalizedEmailJob.cs, QuestBoard.Service/Middleware/GroupSessionMiddleware.cs</files>
  <read_first>
    - Each of the 7 files listed in this task's files field (read each once, extract every ID-tagged comment in one pass)
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-RESEARCH.md §Comment Cleanup Worklist — Non-test source (per-file tag inventory: Program.cs PWFLOW-06/PWFLOW-04/EMAIL-RATE/SESSION-01/D-09; QuestController D-08/D-11/D-01..D-04/FOLLOW-03/WR-03; AdminController EMAIL-RATE-02/03; GroupController MGMT-02..06; SessionReminderJob D-09; QuestFinalizedEmailJob D-09; GroupSessionMiddleware WR-03 (31-REVIEW))
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-PATTERNS.md §Comment-Cleanup Decision Rule (D-06/D-08)
  </read_first>
  <action>
    Apply the same D-06/D-08 decision rule to all 7 Service-layer files. Strip the ID-shaped prefix/substring (`PWFLOW-06`, `PWFLOW-04`, `EMAIL-RATE-01..04`, `SESSION-01`, `SESSION-02`, `D-09`, `D-08`, `D-11`, `D-01..D-04`, `FOLLOW-03`, `WR-03`, `WR-03 (31-REVIEW)`, `MGMT-02`, `MGMT-03`, `MGMT-04a`, `MGMT-04b`, `MGMT-05/06`) from each comment; keep any substantive description that follows the ID on the same line; delete bare ID-only lines entirely. In `Program.cs`, several ID tags sit next to genuinely useful config-intent comments (e.g. the rate-limiter and session-cache setup blocks) — keep the WHAT/WHY of the config, drop only the requirement-ID prefix. Do NOT alter any code, DI registration, middleware ordering, cron config, or rate-limit values — comment text only. Do NOT introduce any new ID-referencing comments (D-06 discipline applies to edits too). Note: `GroupSessionMiddleware.cs` line has `WR-03 (31-REVIEW)` — strip both the `WR-03` and the `(31-REVIEW)` code-review-finding reference, keeping any behavioral note that follows.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -rnE '(^|[^A-Za-z])(D-[0-9]{1,2}|PWFLOW-[0-9]|EMAIL-RATE-[0-9]|SESSION-[0-9]|MGMT-[0-9]|WR-[0-9]|FOLLOW-[0-9]|31-REVIEW)' QuestBoard.Service/Program.cs QuestBoard.Service/Controllers QuestBoard.Service/Jobs QuestBoard.Service/Middleware/GroupSessionMiddleware.cs QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs` returns zero comment-line matches.
    - Behavior assertion: `dotnet build` succeeds with zero errors.
    - Behavior assertion: `dotnet test QuestBoard.UnitTests` still passes (comment edits change no logic).
  </acceptance_criteria>
  <done>All 7 Service-layer files have ID/phase/review-finding tags removed, useful config comments retained, and the solution compiles + unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment text → compiled behavior | Comments do not execute, but a malformed edit (unterminated block comment, accidentally commenting out a code line) could silently change behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | Query-filter config comments in QuestBoardContext.cs (ASVS V4 Access Control) | mitigate | Only comment TEXT is edited; the `HasQueryFilter` predicate and the intentional UserEntity-exclusion are NOT touched. Build + unit-test gate proves tenant-isolation behavior is unchanged. |
| T-34-04 | Tampering | Rate-limiter / session-cache config comments in Program.cs | accept | Comment-only edits near security config; no config value, DI registration, or middleware order changes. Build gate catches any accidental code edit. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs in this comment-only plan. |
</threat_model>

<verification>
- `dotnet build QuestBoard.slnx -c Debug` succeeds.
- `dotnet test QuestBoard.UnitTests` passes.
- Post-edit grep for ID-tag patterns across the 9 files returns zero comment matches.
- `grep -c 'Manual cleanup' QuestBoard.Domain/Services/QuestService.cs` returns 1 (D-08 preserve verified).
</verification>

<success_criteria>
- All 9 non-test source implementation files have GSD-ID/phase/review-finding comment tags stripped.
- All D-08 "why"/landmine comments preserved (RemoveAsync example verified).
- Solution builds; unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-02-SUMMARY.md` when done.
</output>
