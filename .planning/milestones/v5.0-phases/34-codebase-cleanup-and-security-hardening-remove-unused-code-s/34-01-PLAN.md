---
phase: 34-codebase-cleanup-and-security-hardening-remove-unused-code-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/ViewModels/AccountViewModels/RegisterViewModel.cs
autonomous: true
requirements: []
tags: [cleanup, dead-code, dependency-scan, security]

must_haves:
  truths:
    - "The solution builds successfully with RegisterViewModel deleted (zero broken references)"
    - "A dependency vulnerability scan has been run across all 5 projects and its clean result is captured as evidence"
  artifacts:
    - path: ".planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-01-SUMMARY.md"
      provides: "Deletion confirmation + captured dependency-scan output as D-09/D-10 evidence"
  key_links:
    - from: "AccountController / Account views"
      to: "RegisterViewModel (deleted)"
      via: "confirmed zero references before deletion (D-05)"
      pattern: "RegisterViewModel"
---

<objective>
Remove the one confirmed-dead type (`RegisterViewModel`, D-04/D-05) and run the D-09/D-10 dependency vulnerability scan, capturing its clean output as closing evidence. This is the smallest, most independent slice of the Phase 34 mechanical-cleanup workstream.

Purpose: Close the "dead code" and "dependency scan" deliverables of the cleanup phase with hard evidence, not assertion.
Output: `RegisterViewModel.cs` deleted; a captured `dotnet list package --vulnerable` transcript in the plan summary proving the vulnerability class is closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-CONTEXT.md
@.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan CREATES:
- `.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-01-SUMMARY.md` — deletion confirmation + captured scan output

This plan DELETES:
- `QuestBoard.Service/ViewModels/AccountViewModels/RegisterViewModel.cs` — the class file

No new classes, interfaces, or test files are created by this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify-then-delete RegisterViewModel (D-04/D-05)</name>
  <files>QuestBoard.Service/ViewModels/AccountViewModels/RegisterViewModel.cs</files>
  <read_first>
    - QuestBoard.Service/ViewModels/AccountViewModels/RegisterViewModel.cs (the file being deleted)
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-CONTEXT.md §Dead Code Removal (D-04, D-05) and §Known Landmines (verify zero references before deleting)
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-RESEARCH.md §Dead Code Removal Worklist (confirmed-dead table)
  </read_first>
  <action>
    First re-verify D-05 compliance: run a reference search for the identifier `RegisterViewModel` across every `.cs` and `.cshtml` file in all 5 projects (prefer RIP `FindReferences` on the `RegisterViewModel` symbol per CLAUDE.md's RIP Lookup Protocol; fall back to a repo-wide grep for `RegisterViewModel` if RIP is unavailable). The ONLY expected match is the class's own `public class RegisterViewModel` declaration inside the file being deleted. If ANY other reference is found (a controller action, a `@model RegisterViewModel` directive in a view, an AutoMapper `CreateMap`, or a test), STOP and do not delete — instead record the blocker in the summary, because D-05 forbids removing merely-suspected dead code. If zero external references are confirmed, delete the file `QuestBoard.Service/ViewModels/AccountViewModels/RegisterViewModel.cs` entirely (whole-file removal, not emptying it). This was deliberately kept "unused but harmless" in Phase 30-02; D-04 explicitly names it for removal now.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the file `QuestBoard.Service/ViewModels/AccountViewModels/RegisterViewModel.cs` no longer exists on disk.
    - Behavior assertion: `dotnet build` succeeds with zero errors (a broken reference to the deleted type would fail compilation — the compiler is the dead-code test per RESEARCH §Validation Architecture).
    - Reference assertion: a post-deletion grep for `RegisterViewModel` across `*.cs`/`*.cshtml` returns zero matches.
  </acceptance_criteria>
  <done>RegisterViewModel.cs is deleted and the full solution compiles with no unresolved references.</done>
</task>

<task type="auto">
  <name>Task 2: Run dependency vulnerability scan and capture clean-result evidence (D-09/D-10)</name>
  <files>.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-CONTEXT.md §Security Audit Method (D-09, D-10)
    - .planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-RESEARCH.md §Package Legitimacy Audit (live scan results — expected CLEAN across all 5 projects) and §Summary point 1 (D-09/D-10 require no remediation, only the scan + documented clean result)
  </read_first>
  <action>
    Run `dotnet list package --vulnerable --include-transitive` at the solution root so it reports for all 5 projects (Domain, Repository, Service, UnitTests, IntegrationTests). Capture the full stdout verbatim. RESEARCH verified this is currently CLEAN (zero vulnerable packages). Then apply D-10's conditional logic to whatever the scan actually returns at execution time: (a) if the scan is clean, record the transcript in the summary as the "vulnerability class is closed" evidence artifact — no code change; (b) if the scan surprisingly reports a vulnerable package with a non-breaking fix version available, upgrade that package to the fixed version in the owning `.csproj` and re-run the scan to confirm it clears; (c) if a vulnerable package would require a BREAKING upgrade, do NOT force the upgrade mid-cleanup — instead document the package name, current version, advisory, and required breaking version in the summary as a follow-up item (D-10). Do not run `--outdated`/`--deprecated` remediation — those were confirmed clean by RESEARCH and D-10 scopes action to the `--vulnerable` result only. Do not add any GSD-ID-referencing code comments as part of this task (D-06 discipline).
  </action>
  <verify>
    <automated>dotnet list package --vulnerable --include-transitive</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `dotnet list package --vulnerable --include-transitive` completes for all 5 projects and its verbatim output is pasted into `34-01-SUMMARY.md` under a "Dependency Scan Evidence" heading.
    - Behavior assertion: if the captured output contains no vulnerable packages, the summary states the vulnerability class is closed; if any vulnerable package appears, the summary records either the applied non-breaking upgrade (with a passing re-scan) or the documented breaking-upgrade deferral per D-10.
  </acceptance_criteria>
  <done>The dependency scan has been run and its result captured as evidence; any vulnerable finding is either remediated (non-breaking) or explicitly documented (breaking).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/dependency supply chain → compiled app | Third-party NuGet packages (and their transitive deps) are executed inside the app process; a vulnerable dependency is an untrusted-code entry point |
| deleted dead code → live surface | Removing an unreferenced type must not silently remove a still-wired input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering / Elevation of Privilege | Third-party NuGet packages (ASVS V13 Malicious Code/Dependency) | mitigate | Run `dotnet list package --vulnerable --include-transitive` (D-09) across all 5 projects; upgrade non-breaking fixes, document breaking ones (D-10). Currently CLEAN per RESEARCH. |
| T-34-02 | Information Disclosure | RegisterViewModel deletion | accept | Type confirmed zero external references (D-05) before deletion; compiler build gate proves no live input path was removed. No PII/secret handled by this ViewModel. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package-manager installs performed this phase (cleanup pass adds no dependencies per RESEARCH §Package Legitimacy Audit); scan is verification-only. |
</threat_model>

<verification>
- `dotnet build QuestBoard.slnx -c Debug` succeeds after deletion.
- `dotnet list package --vulnerable --include-transitive` output captured in the summary.
- Post-deletion `RegisterViewModel` grep returns zero matches across `*.cs`/`*.cshtml`.
</verification>

<success_criteria>
- RegisterViewModel.cs deleted; solution compiles.
- Dependency scan run; clean result (or remediation/deferral) documented as evidence.
</success_criteria>

<output>
Create `.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-01-SUMMARY.md` when done.
</output>
