---
phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che
plan: 01
subsystem: auth
tags: [authorization, group-role, superadmin, userservice]

# Dependency graph
requires:
  - phase: 34.2-performance-architecture
    provides: RequireActiveGroupId() seam on IActiveGroupContext, established shared-helper extraction precedent (ControllerExtensions)
provides:
  - GetEffectiveGroupRoleAsync(ClaimsPrincipal, int) on IUserService/UserService — single shared SuperAdmin-bypass + GetGroupRoleAsync helper
affects: [34.3-02, 34.3-03, 34.3-04 — controller migration plans that inject IUserService and call this helper]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Domain-layer role-resolution helper that centralizes SuperAdmin bypass + GetGroupRoleAsync delegation, replacing per-call-site duplication"]

key-files:
  created:
    - QuestBoard.UnitTests/Services/UserServiceTests.cs
  modified:
    - QuestBoard.Domain/Interfaces/IUserService.cs
    - QuestBoard.Domain/Services/UserService.cs

key-decisions:
  - "GetEffectiveGroupRoleAsync takes no owner/currentUser parameter — ownership checks stay inline at each downstream call site, only role resolution is centralized"
  - "SuperAdmin bypass string copied verbatim (\"SuperAdmin\") from AdminHandler.cs to avoid a mistyped/divergent claim check"
  - "Non-SuperAdmin path delegates to the existing instance method GetGroupRoleAsync (not a direct repository call) to keep a single source of truth for the underlying lookup"

patterns-established:
  - "Effective-role helpers on IUserService follow the Get...Async naming convention already used by GetGroupRoleAsync/GetGroupRoleByIdAsync"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-07-02
---

# Phase 34.3 Plan 01: Shared GetEffectiveGroupRoleAsync Helper Summary

**Added `GetEffectiveGroupRoleAsync(ClaimsPrincipal, int)` on IUserService/UserService — a single reusable SuperAdmin-bypass + GetGroupRoleAsync helper that will replace ~23 duplicated inline `IsInRole("Admin")`/`IsInRoleAsync` checks in downstream plans.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-07-02T11:50:00Z
- **Completed:** 2026-07-02T11:55:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `IUserService.GetEffectiveGroupRoleAsync` declared with an XML doc explaining the SuperAdmin bypass, matching the existing `GetGroupRoleAsync`/`GetGroupRoleByIdAsync` signature convention
- `UserService.GetEffectiveGroupRoleAsync` implemented: SuperAdmin principals resolve to `GroupRole.Admin` with zero DB calls; all other principals delegate to the existing `GetGroupRoleAsync(user, groupId)` instance method
- 5 unit tests added (`UserServiceTests.cs`, a new file — no prior UserService test fixture existed) locking in the SuperAdmin bypass (with an explicit `DidNotReceive()` assertion proving no identity/repository call happens), the Admin/DungeonMaster pass-through, and both null-membership and null-identity-lookup cases

## Task Commits

Each task was committed atomically:

1. **Task 1: Declare and implement GetEffectiveGroupRoleAsync on IUserService / UserService** - `b36f70a` (feat)
2. **Task 2: Unit-test the SuperAdmin bypass and null-membership behavior** - `222bfca` (test)

**Plan metadata:** pending (docs: complete plan)

## Files Created/Modified
- `QuestBoard.Domain/Interfaces/IUserService.cs` - Added `GetEffectiveGroupRoleAsync(ClaimsPrincipal, int)` declaration with XML doc
- `QuestBoard.Domain/Services/UserService.cs` - Added implementation: SuperAdmin bypass returns `GroupRole.Admin`, otherwise delegates to `GetGroupRoleAsync`
- `QuestBoard.UnitTests/Services/UserServiceTests.cs` - New test fixture (NSubstitute + FluentAssertions, mirroring `QuestServiceTests.cs` conventions) with 5 tests covering the bypass, Admin/DungeonMaster/null pass-through, and null-identity-lookup short-circuit

## Decisions Made
- No prior `UserServiceTests.cs` file existed in the codebase; created a new one mirroring the existing `QuestServiceTests.cs` fixture style (NSubstitute for mocking, FluentAssertions for assertions, xUnit `[Fact]`) rather than Moq, since the codebase's actual convention (confirmed by reading the sibling test file) uses NSubstitute — the plan's suggested "Moq per the codebase convention" note did not match the real fixture style found on inspection.
- Added a fifth test (null `IIdentityService.GetUserIdAsync` result, distinct from a null repository membership lookup) beyond the plan's four listed behaviors, since both null paths are reachable and both needed independent coverage of the "returns null" contract.

## Deviations from Plan

None - plan executed exactly as written. The Moq-vs-NSubstitute note above is a documentation correction (the plan said "whatever mocking framework the existing fixture uses (Moq per the codebase convention)" — the actual codebase convention is NSubstitute, confirmed by reading `QuestServiceTests.cs` before writing tests), not a deviation from any acceptance criterion.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
`GetEffectiveGroupRoleAsync` is available on `IUserService` for injection by all downstream controllers. Plans 34.3-02, 34.3-03, and 34.3-04 (the ~23 inline call-site migrations across `QuestController`, `QuestLogController`, `DungeonMasterController`, and `Admin/AccountController`) can now call this helper directly. No blockers.

---
*Phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che*
*Completed: 2026-07-02*

## Self-Check: PASSED

All created/modified files verified present on disk; both task commits (`b36f70a`, `222bfca`) verified present in git log.
