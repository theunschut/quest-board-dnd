---
phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che
plan: 03
type: execute
wave: 2
depends_on: ["34.3-01"]
files_modified:
  - QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs
  - QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "An Admin who is not the quest's DM sees the recap-edit link on a completed quest's log Details"
    - "An Admin who is not the quest's DM can update a recap without a Forbid()"
    - "An Admin who is not the DM profile's owner sees the edit link (CanEdit) and can edit that DM profile"
    - "Anonymous visitors to QuestLog.Details and DungeonMaster.Profile never trigger RequireActiveGroupId()"
  artifacts:
    - path: "QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs"
      provides: "IActiveGroupContext injected + 2 sites migrated"
      contains: "GetEffectiveGroupRoleAsync"
    - path: "QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs"
      provides: "IActiveGroupContext injected + 3 sites migrated"
      contains: "GetEffectiveGroupRoleAsync"
  key_links:
    - from: "QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs"
      to: "IUserService.GetEffectiveGroupRoleAsync"
      via: "newly-injected IActiveGroupContext + RequireActiveGroupId (guarded by currentUser != null on Details)"
      pattern: "GetEffectiveGroupRoleAsync\\(User,"
---

<objective>
Add `IActiveGroupContext` to the constructors of `QuestLogController` and `DungeonMasterController`, then migrate their 5 inline role checks (QuestLog: 2, DungeonMaster: 3) to the D-02 helper, preserving each site's ownership comparison and anonymous-reachability guard.

Purpose: `QuestLogController.Details`'s `ViewBag.CanEditRecap` is the second user-visible symptom named in the phase goal ("QuestLog recap-edit link disappears"). `DungeonMasterController.Profile`'s `CanEdit` is the DM-profile edit-link equivalent.
Output: Both controllers read `UserGroups.GroupRole` via the helper for their Admin/owner decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-CONTEXT.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-PATTERNS.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-RESEARCH.md

<interfaces>
<!-- The helper produced by Plan 01 (Wave 1) — call this, do not re-derive. -->
From QuestBoard.Domain/Interfaces/IUserService.cs:
```csharp
Task<GroupRole?> GetEffectiveGroupRoleAsync(ClaimsPrincipal user, int groupId);
```

GroupRole enum: Player = 0, DungeonMaster = 1, Admin = 2

`IActiveGroupContext` is in `QuestBoard.Domain.Interfaces`; `RequireActiveGroupId()` is the extension in `QuestBoard.Domain.Extensions`. Both controllers may need `using QuestBoard.Domain.Extensions;` and `using QuestBoard.Domain.Interfaces;` added — verify existing usings before adding.

QuestController.cs constructor is the parameter-ordering analog (inject IActiveGroupContext at the END of the constructor parameter list, after existing service dependencies). IActiveGroupContext is registered once in Program.cs — no per-controller DI registration is needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuestLogController — inject IActiveGroupContext and migrate Details (CanEditRecap) + UpdateRecap</name>
  <files>QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs (read the full file: the primary constructor `(IUserService userService, IQuestService questService)` ~line 9-12; the Details `ViewBag.CanEditRecap` block ~line 54-57 with its `currentUser != null` short-circuit; the UpdateRecap `Forbid()` guard ~line 91-98 which is behind `[Authorize(Policy = "DungeonMasterOnly")]`)
    - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs (constructor shape — the analog for where to place IActiveGroupContext, and its Details site for the identical CanManage/CanEditRecap pattern already migrated in Plan 02)
    - QuestBoard.Service/Views/QuestLog/Details.cshtml (confirm how ViewBag.CanEditRecap gates the recap-edit link — needed for Plan 06 body assertions, and to confirm no view-layer fallback masks the fix)
  </read_first>
  <behavior>
    - Details: `ViewBag.CanEditRecap` is true for a real Admin who is not the quest's DM, false for an anonymous visitor, and no exception is thrown for the anonymous case.
    - UpdateRecap: a real Admin who is not the quest's DM is NOT returned Forbid().
  </behavior>
  <action>
    Add `IActiveGroupContext activeGroupContext` as the last parameter of the `QuestLogController` primary constructor. Add `using QuestBoard.Domain.Interfaces;` and `using QuestBoard.Domain.Extensions;` if not already present.

    Details `ViewBag.CanEditRecap` (~line 56): preserve the `currentUser != null &&` short-circuit exactly as QuestController.Details does. Replace `currentUser != null && await userService.IsInRoleAsync(User, "Admin")` with `currentUser != null && await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId()) == GroupRole.Admin` (the `&&` short-circuit means the helper/RequireActiveGroupId is never evaluated for anonymous visitors — Pitfall 2). Keep `ViewBag.CanEditRecap = isQuestDm || isAdmin;` unchanged.

    UpdateRecap (~line 93): this action is behind `[Authorize(Policy = "DungeonMasterOnly")]` so `RequireActiveGroupId()` fail-hard is safe (D-03). Replace `var isAdmin = await userService.IsInRoleAsync(User, "Admin");` with `var isAdmin = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId()) == GroupRole.Admin;`, keep `if (!isQuestDm && !isAdmin) return Forbid();` unchanged. Do not fold ownership into the helper (Pitfall 1). No phase/decision-ID comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service -c Debug && grep -c 'IsInRoleAsync(User, "Admin")\|User.IsInRole("Admin")' QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs</automated>
  </verify>
  <acceptance_criteria>
    - QuestLogController constructor parameter list contains `IActiveGroupContext activeGroupContext`
    - `grep -c 'IsInRole("Admin")' QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs` returns 0
    - `grep -c 'IsInRoleAsync(User, "Admin")' QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs` returns 0
    - The Details `ViewBag.CanEditRecap` assignment retains a `currentUser != null` short-circuit before any helper/RequireActiveGroupId call
    - `dotnet build QuestBoard.Service` exits 0
  </acceptance_criteria>
  <done>QuestLogController has IActiveGroupContext injected; Details CanEditRecap and UpdateRecap both read GroupRole via the helper; the anonymous-reachable Details site stays guarded; Service builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DungeonMasterController — inject IActiveGroupContext and migrate Profile (CanEdit) + EditProfile GET/POST</name>
  <files>QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs (read the full file: the primary constructor `(IDungeonMasterProfileService dmProfileService, IUserService userService, IQuestService questService, IMapper mapper)` ~line 10-14; the Profile `CanEdit = currentUser != null && (currentUser.Equals(user) || User.IsInRole("Admin"))` inside the DMProfileViewModel object initializer ~line 32; the EditProfile GET guard ~line 52 and POST guard ~line 82, both `if (!currentUser.Equals(targetUser) && !User.IsInRole("Admin"))`)
    - QuestBoard.Service/Views/DungeonMaster/Profile.cshtml (confirm how CanEdit gates the edit link — needed for Plan 06 body assertions)
    - QuestBoard.Service/Authorization/AdminHandler.cs (canonical role-check shape)
  </read_first>
  <behavior>
    - Profile: `CanEdit` is true for a real Admin viewing a DM profile they do not own, true for the profile's owner, false for an anonymous visitor (no exception).
    - EditProfile GET/POST: a real Admin editing a DM profile they do not own is NOT returned Forbid().
  </behavior>
  <action>
    Add `IActiveGroupContext activeGroupContext` as the last parameter of the `DungeonMasterController` primary constructor. Add `using QuestBoard.Domain.Interfaces;` and `using QuestBoard.Domain.Extensions;` if not already present.

    Profile `CanEdit` (~line 32): the async helper call cannot run inline inside an object initializer. Before constructing `DMProfileViewModel`, compute the role guarded by the anonymous check: `GroupRole? role = null; if (currentUser != null) { role = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId()); }`. Then set `CanEdit = currentUser != null && (currentUser.Equals(user) || role == GroupRole.Admin)`. The `if (currentUser != null)` guard ensures anonymous visitors never call RequireActiveGroupId (Pitfall 2). Keep the ownership term `currentUser.Equals(user)` inline (Pitfall 1).

    EditProfile GET (~line 52) and POST (~line 82): both are behind `[Authorize(Policy = "DungeonMasterOnly")]` so `RequireActiveGroupId()` fail-hard is safe (D-03). For each `if (!currentUser.Equals(targetUser) && !User.IsInRole("Admin"))`, hoist `var role = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId());` above the guard and change it to `if (!currentUser.Equals(targetUser) && role != GroupRole.Admin)`. Keep the `currentUser.Equals(targetUser)` ownership term untouched. No phase/decision-ID comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service -c Debug && grep -c 'User.IsInRole("Admin")' QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs</automated>
  </verify>
  <acceptance_criteria>
    - DungeonMasterController constructor parameter list contains `IActiveGroupContext activeGroupContext`
    - `grep -c 'IsInRole("Admin")' QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs` returns 0
    - The Profile `CanEdit` assignment retains `currentUser != null && (currentUser.Equals(user) || ...)` with the admin term now `role == GroupRole.Admin`, and the role is computed inside a `currentUser != null` guard before the object initializer
    - Both EditProfile guards retain the `currentUser.Equals(targetUser)` ownership term and only the admin term derives from the helper
    - `dotnet build QuestBoard.Service` exits 0
  </acceptance_criteria>
  <done>DungeonMasterController has IActiveGroupContext injected; Profile CanEdit and both EditProfile guards read GroupRole via the helper; the anonymous-reachable Profile site stays guarded; ownership comparisons preserved; Service builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous/authenticated request → QuestLog.Details / DungeonMaster.Profile | Both anonymous-reachable (no [Authorize]); UpdateRecap + EditProfile are DungeonMasterOnly |
| ownership (is-the-DM / is-profile-owner) vs. role (is-Admin) | Two orthogonal grants per site; must not collapse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-11 | Denial of Service | RequireActiveGroupId() on anonymous-reachable Details/Profile | mitigate | Preserve `currentUser != null` short-circuits; Profile computes role inside an `if (currentUser != null)` block before the object initializer. Acceptance criteria require these guards. |
| T-34.3-12 | Elevation of Privilege | ownership-vs-role collapse | mitigate | Only the `!User.IsInRole("Admin")` term is replaced; `currentUser.Equals(user)`/`currentUser.Equals(targetUser)` stays inline (Pitfall 1). Acceptance criteria require ownership terms retained. |
| T-34.3-13 | Denial of Service (regression stays broken) | missed site still evaluating IsInRole false | mitigate | grep gate asserts zero IsInRole("Admin") remain in each file; Plan 06 integration tests cover each action. |
| T-34.3-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` exits 0.
- Both files return 0 for `grep -c 'IsInRole("Admin")'`.
- Each anonymous-reachable helper call is nested inside a `currentUser != null` guard.
- Behavioral proof deferred to Plan 06 (integration tests).
</verification>

<success_criteria>
- QuestLogController and DungeonMasterController both have IActiveGroupContext injected and all 5 sites migrated.
- The recap-edit link (CanEditRecap) and DM-profile edit link (CanEdit) work for real Admins who are not the resource owner.
- Anonymous visitors incur no exception; ownership comparisons remain inline.
</success_criteria>

<output>
Create `.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-03-SUMMARY.md` when done.
</output>
