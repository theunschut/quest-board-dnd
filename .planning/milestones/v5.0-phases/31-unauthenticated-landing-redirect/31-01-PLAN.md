---
phase: 31-unauthenticated-landing-redirect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs
  - QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs
  - QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs
autonomous: true
requirements: [UX-04]
must_haves:
  truths:
    - "An unauthenticated request to /Calendar is redirected (302) to the login page"
    - "An unauthenticated request to /QuestLog is redirected (302) to the login page"
    - "An unauthenticated request to /DungeonMaster/Profile/{id} is redirected (302) to the login page"
  artifacts:
    - path: "QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs"
      provides: "Class-level [Authorize] on CalendarController (D-01)"
      contains: "[Authorize]"
    - path: "QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs"
      provides: "Class-level [Authorize] on QuestLogController (D-01)"
      contains: "[Authorize]"
    - path: "QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs"
      provides: "DM profile actions no longer publicly accessible (D-02)"
  key_links:
    - from: "CalendarController"
      to: "ASP.NET Identity auth challenge"
      via: "[Authorize] class attribute triggers redirect to /Account/Login for unauthenticated users"
      pattern: "\\[Authorize\\]"
---

<objective>
Lock down `CalendarController`, `QuestLogController`, and `DungeonMasterController` so unauthenticated visitors can no longer reach group-scoped content. Add class-level `[Authorize]` to Calendar and QuestLog (per D-01), and remove the two `[AllowAnonymous]` overrides on the DM profile actions (per D-02) so the existing class-level `[Authorize]` on `DungeonMasterController` takes effect.

Purpose: Close the access-control gaps where group-private data was reachable without login — aligns these controllers with the existing `ShopController`/`GuildMembersController` pattern and the group-isolation model from Phases 28-30.
Output: Three modified controller files. No views, no services, no schema changes. Tests for these controllers are updated in plan 31-04 (Wave 3), not here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-unauthenticated-landing-redirect/31-CONTEXT.md
@.planning/phases/31-unauthenticated-landing-redirect/31-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan (31-01) produces NO new symbols. It modifies existing controllers only:
- `CalendarController` — gains class-level `[Authorize]` attribute
- `QuestLogController` — gains class-level `[Authorize]` attribute
- `DungeonMasterController` — loses two `[AllowAnonymous]` attributes (on `Profile` GET and `GetDMProfilePicture` GET)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add class-level [Authorize] to CalendarController and QuestLogController</name>
  <files>QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs, QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs (current state — no auth attribute today)
    - QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs (current state — no auth attribute today)
    - QuestBoard.Service/Controllers/Shop/ShopController.cs (reference pattern: class-level [Authorize] above the class declaration)
  </read_first>
  <action>
    In `CalendarController.cs`: add `using Microsoft.AspNetCore.Authorization;` to the using block if absent, then place a `[Authorize]` attribute on the line directly above `public class CalendarController(IQuestService questService) : Controller`. Do not add any policy argument — bare `[Authorize]` matches D-01 ("No global fallback policy") and the ShopController reference pattern.
    In `QuestLogController.cs`: the `using Microsoft.AspNetCore.Authorization;` import already exists (the file references `[Authorize]` nowhere yet but imports it). Place a bare `[Authorize]` attribute on the line directly above `public class QuestLogController(` declaration. Do not modify any individual action attributes.
    Do NOT add `[Authorize]` to any action that already has its own attribute. Do NOT change `HomeController` or `QuestController` in this task — those are handled in plan 31-02.
  </action>
  <verify>
    <automated>cd QuestBoard.Service && dotnet build --no-restore 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\[Authorize\]" QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs` shows the attribute on the line immediately preceding the `public class CalendarController` declaration
    - `grep -n "\[Authorize\]" QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs` shows the attribute on the line immediately preceding the `public class QuestLogController` declaration
    - Neither file contains `[Authorize(Policy` at class level (bare `[Authorize]` only)
    - `dotnet build` for QuestBoard.Service reports "Build succeeded" with zero errors
  </acceptance_criteria>
  <done>Both CalendarController and QuestLogController carry a bare class-level [Authorize]; the project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove [AllowAnonymous] from DungeonMasterController profile actions</name>
  <files>QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs (class already has [Authorize] at line 9; two actions carry [AllowAnonymous])
  </read_first>
  <action>
    Remove the `[AllowAnonymous]` attribute line from the `Profile(int id, ...)` GET action (currently at line ~17, directly below its `[HttpGet]`). Remove the `[AllowAnonymous]` attribute line from the `GetDMProfilePicture(int id, ...)` GET action. These are the ONLY two `[AllowAnonymous]` attributes in the file — after removal, the file must contain zero occurrences of `AllowAnonymous`. Do NOT alter the class-level `[Authorize]` (line 9) — once the exemptions are gone, it governs both actions, which is the intent of D-02. Do NOT touch the `EditProfile` GET/POST actions (they keep `[Authorize(Policy = "DungeonMasterOnly")]`). If the `using Microsoft.AspNetCore.Authorization;` import is now the only consumer of `AllowAnonymous`, leave the import in place — `[Authorize]` still uses that namespace.
  </action>
  <verify>
    <automated>grep -c "AllowAnonymous" QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AllowAnonymous" QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs` returns `0`
    - The class-level `[Authorize]` attribute above `public class DungeonMasterController(` is still present (`grep -n "\[Authorize\]" ...` shows it)
    - Both `[Authorize(Policy = "DungeonMasterOnly")]` attributes on the EditProfile actions remain unchanged
    - `dotnet build` for QuestBoard.Service reports "Build succeeded" with zero errors
  </acceptance_criteria>
  <done>DungeonMasterController has no [AllowAnonymous] attributes; class-level [Authorize] now covers Profile and GetDMProfilePicture; project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → MVC controller | An anonymous HTTP request reaching a group-scoped controller action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | CalendarController / QuestLogController (previously open) | mitigate | Class-level `[Authorize]` forces ASP.NET Identity auth challenge → 302 to /Account/Login for anonymous requests |
| T-31-02 | Information Disclosure | DungeonMasterController.Profile / GetDMProfilePicture (previously `[AllowAnonymous]`) | mitigate | Remove `[AllowAnonymous]`; existing class-level `[Authorize]` makes DM profiles group-private |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md confirms zero external dependencies added |
</threat_model>

<verification>
- `dotnet build` succeeds with zero errors.
- `grep -c "AllowAnonymous" QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs` returns 0.
- Behavioral verification (302 for unauthenticated Calendar/QuestLog/DMProfile) is covered by the updated integration tests in plan 31-04; this plan only guarantees the source attributes and a clean build.
</verification>

<success_criteria>
- CalendarController and QuestLogController each carry a bare class-level `[Authorize]`.
- DungeonMasterController contains no `[AllowAnonymous]` attributes.
- The solution builds with no errors.
</success_criteria>

<output>
Create `.planning/phases/31-unauthenticated-landing-redirect/31-01-SUMMARY.md` when done.
</output>
