---
phase: 27-group-schema-foundation
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - QuestBoard.Repository/Migrations/AddGroupSchema.cs
  - QuestBoard.Repository/Migrations/QuestBoardContextModelSnapshot.cs
  - QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs
autonomous: false
requirements: [GROUP-04, GROUP-05, GROUP-06]
must_haves:
  truths:
    - "A single migration named AddGroupSchema exists in QuestBoard.Repository/Migrations and is the latest migration in the model snapshot"
    - "The migration Up() creates Groups and UserGroups tables, adds GroupId to Quests and ShopItems, seeds the EuphoriaInn group as Id=1, updates all existing Quests/ShopItems to GroupId=1, seeds UserGroups from AspNetUserRoles using highest-role logic, and deletes Player/DungeonMaster/Admin rows from AspNetUserRoles"
    - "TestDataHelper.CreateTestQuestAsync and CreateShopItemAsync set GroupId = 1 on the entities they create"
    - "All 191 existing integration tests pass after the model and test-factory changes"
  artifacts:
    - path: "QuestBoard.Repository/Migrations/AddGroupSchema.cs"
      provides: "Single atomic migration covering all 8 schema + data steps (D-02)"
      contains: "class AddGroupSchema"
    - path: "QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs"
      provides: "Factory methods setting GroupId = 1 on Quest and ShopItem"
      contains: "GroupId = 1"
  key_links:
    - from: "QuestBoard.Repository/Migrations/AddGroupSchema.cs"
      to: "QuestBoard.Repository/Entities/QuestBoardContext.cs"
      via: "migration scaffolded from the EF model defined in plan 01 (DbSets + OnModelCreating)"
      pattern: "CreateTable.*Groups"
    - from: "QuestBoard.Repository/Migrations/AddGroupSchema.cs"
      to: "AspNetUserRoles / AspNetRoles"
      via: "raw SQL reads AspNetUserRoles joined to AspNetRoles to seed UserGroups, then deletes the role rows"
      pattern: "INSERT INTO UserGroups"
    - from: "QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs"
      to: "QuestBoard.Repository/Entities/QuestEntity.cs"
      via: "CreateTestQuestAsync sets the non-nullable GroupId added in plan 01"
      pattern: "GroupId = 1"
---

<objective>
Generate the single atomic `AddGroupSchema` EF Core migration that performs all 8 schema-and-data steps (D-02) in the FK-safe order, then update the integration-test factory so the non-nullable `GroupId` from plan 01 is populated in InMemory test data, then run the full 191-test suite as the wave gate.

This plan delivers GROUP-04 (seed EuphoriaInn as GroupId=1), GROUP-05 (seed all users into UserGroups with their highest current role), and GROUP-06 (delete Player/DungeonMaster/Admin rows from AspNetUserRoles). It depends on plan 01 having the complete EF model in place — the migration is scaffolded from that model state.

Purpose: Produce the production data migration that auto-applies on startup (`context.Database.Migrate()`) and seeds all existing EuphoriaInn data into GroupId=1 with zero data loss, while keeping all 191 integration tests green (the InMemory provider builds schema from model state via `EnsureCreated()` and never runs the migration SQL, so the test factory must set `GroupId = 1` itself).
Output: 1 new migration file (`AddGroupSchema.cs` + regenerated `QuestBoardContextModelSnapshot.cs` + the generated `.Designer.cs`), and a modified `TestDataHelper.cs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-group-schema-foundation/27-CONTEXT.md
@.planning/phases/27-group-schema-foundation/27-RESEARCH.md
@.planning/phases/27-group-schema-foundation/27-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 02)

New files:
- `QuestBoard.Repository/Migrations/<timestamp>_AddGroupSchema.cs` — single migration class `AddGroupSchema` with `Up()` and `Down()`
- `QuestBoard.Repository/Migrations/<timestamp>_AddGroupSchema.Designer.cs` — generated designer (EF tooling output)

Modified symbols:
- `QuestBoardContextModelSnapshot` — regenerated to include Groups, UserGroups, Quests.GroupId, ShopItems.GroupId, indexes, FKs
- `TestDataHelper.CreateTestQuestAsync` — quest initializer gains `GroupId = 1`
- `TestDataHelper.CreateShopItemAsync` — item initializer gains `GroupId = 1`

Note: the generated migration filename carries a timestamp prefix (e.g. `20260629XXXXXX_AddGroupSchema.cs`); `files_modified` lists the logical name. The migration class name MUST be `AddGroupSchema`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Update TestDataHelper to set GroupId = 1 on Quest and ShopItem factories</name>
  <files>QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs (the `CreateTestQuestAsync` quest initializer and the `CreateShopItemAsync` item initializer)
    - QuestBoard.Repository/Entities/QuestEntity.cs (confirms non-nullable `int GroupId` added in plan 01)
    - QuestBoard.Repository/Entities/ShopItemEntity.cs (confirms non-nullable `int GroupId` added in plan 01)
  </read_first>
  <action>
    In `QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs`, add `GroupId = 1` to the object initializer of the `QuestEntity` created in `CreateTestQuestAsync` (alongside `TotalPlayerCount = 4` / `CreatedAt = DateTime.UtcNow`), and add `GroupId = 1` to the object initializer of the `ShopItemEntity` created in `CreateShopItemAsync` (alongside `CreatedByDmId` / `CreatedAt = DateTime.UtcNow`).

    Rationale (research Pitfall 3 + D-01): the InMemory provider builds schema from EF model state via `EnsureCreated()` and never runs the migration SQL, so the non-nullable `GroupId` defaults to 0 unless the factory sets it. Setting `GroupId = 1` keeps test data consistent with the seeded EuphoriaInn group and prevents wrong results in the Phase 28 query-filter tests. Do NOT change `SeedRolesAsync` — integration tests seed Player/DungeonMaster/Admin via `RoleManager.CreateAsync` directly and must keep doing so (the migration's AspNetUserRoles delete only affects the real SQL Server path, not InMemory).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj 2>&1 | grep -v '^#' | grep -c "error CS"</automated>
  </verify>
  <acceptance_criteria>
    - `TestDataHelper.cs` `CreateTestQuestAsync` quest initializer contains `GroupId = 1`
    - `TestDataHelper.cs` `CreateShopItemAsync` item initializer contains `GroupId = 1`
    - `SeedRolesAsync` is unchanged (still creates Admin/DungeonMaster/Player via RoleManager)
    - `dotnet build QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj` reports 0 `error CS` lines (count is 0)
  </acceptance_criteria>
  <done>Both factory methods set GroupId = 1 and the IntegrationTests project compiles. SeedRolesAsync untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and author the atomic AddGroupSchema migration (8 FK-safe steps)</name>
  <files>QuestBoard.Repository/Migrations/AddGroupSchema.cs, QuestBoard.Repository/Migrations/QuestBoardContextModelSnapshot.cs</files>
  <read_first>
    - QuestBoard.Repository/Migrations/20260626190255_AddReminderLog.cs (CreateTable + ForeignKey + CreateIndex unique pattern, and the `.Annotation("SqlServer:Identity", "1, 1")` Id column form)
    - QuestBoard.Repository/Migrations/20250704211037_ConvertIsDungeonMasterToRoles.cs (raw `migrationBuilder.Sql(@"...")` against AspNetUserRoles/AspNetRoles; confirms role IDs Player=1, DungeonMaster=2, Admin=3)
    - QuestBoard.Repository/Entities/QuestBoardContext.cs (model state the migration scaffolds from — DbSets and OnModelCreating from plan 01)
    - .planning/phases/27-group-schema-foundation/27-RESEARCH.md (Pattern 3 seeding SQL, Pitfall 1 step ordering, Pitfall 2 IDENTITY_INSERT, Pitfall 5 Down() reversal)
  </read_first>
  <action>
    From the `QuestBoard.Service/` directory, generate the migration scaffold: `dotnet ef migrations add AddGroupSchema --project ../QuestBoard.Repository`. This produces the timestamped `*_AddGroupSchema.cs`, its `.Designer.cs`, and regenerates `QuestBoardContextModelSnapshot.cs`. The migration class name MUST be `AddGroupSchema`.

    Then author the `Up()` body so the operations run in this FK-safe order (research Pitfall 1 — populate data before adding the FK constraint; all within one atomic migration per D-02). The scaffold will already contain CreateTable/AddColumn/AddForeignKey for the model changes; reorder and insert the data-seeding `migrationBuilder.Sql(...)` calls so the final order is:
    1. CreateTable `Groups` (Id identity, Name nvarchar(100), CreatedAt datetime2) + unique index on Name.
    2. CreateTable `UserGroups` (Id identity, UserId int, GroupId int, GroupRole int) + FKs to AspNetUsers (cascade) and Groups (cascade) + unique index on (UserId, GroupId).
    3. AddColumn `Quests.GroupId` (int, nullable:false, defaultValue:0) — fills existing rows with 0 temporarily (research Pattern 1).
    4. AddColumn `ShopItems.GroupId` (int, nullable:false, defaultValue:0).
    5. Seed EuphoriaInn with explicit Id=1 using raw SQL wrapped in IDENTITY_INSERT (research Pitfall 2): `SET IDENTITY_INSERT Groups ON; INSERT INTO Groups (Id, Name, CreatedAt) VALUES (1, 'EuphoriaInn', GETUTCDATE()); SET IDENTITY_INSERT Groups OFF;` (per GROUP-04, D-02 step 5).
    6. `migrationBuilder.Sql("UPDATE Quests SET GroupId = 1");` and `migrationBuilder.Sql("UPDATE ShopItems SET GroupId = 1");` — all existing shared-resource rows now point to EuphoriaInn (per GROUP-04, D-01).
    7. AddForeignKey `Quests.GroupId` → `Groups(Id)` with `onDelete: ReferentialAction.NoAction`, AFTER rows are populated (per D-10, GROUP-03). The scaffold may emit this as part of CreateTable-time config or as a separate AddForeignKey; ensure it executes after step 6.
    8. AddForeignKey `ShopItems.GroupId` → `Groups(Id)` with `onDelete: ReferentialAction.NoAction`, after step 6 (per D-10).
    9. Seed UserGroups from AspNetUserRoles with highest-role logic via raw SQL (research Pattern 3, per GROUP-05, D-03, D-04). Use a LEFT JOIN from AspNetUsers so users with no role row get GroupRole=0/Player (D-04 — an INNER JOIN would silently drop them). Role IDs Player=1, DungeonMaster=2, Admin=3 (verified in ConvertIsDungeonMasterToRoles); GroupRole enum values Player=0, DungeonMaster=1, Admin=2 (D-05). The CASE/MAX expression maps highest source role to the GroupRole int: Admin→2, DungeonMaster→1, else→0 (D-03 Admin > DungeonMaster > Player).
    10. Delete Player/DungeonMaster/Admin rows from AspNetUserRoles via raw SQL joined to AspNetRoles on role name IN ('Player','DungeonMaster','Admin') (per GROUP-06, D-02 step 8). SuperAdmin rows (none yet — added in Phase 29) are untouched.

    Author the `Down()` to reverse all steps in reverse order (research Pitfall 5): DropForeignKey + DropColumn `ShopItems.GroupId`; DropForeignKey + DropColumn `Quests.GroupId`; DropTable `UserGroups`; DropTable `Groups`. Re-inserting the deleted AspNetUserRoles rows from UserGroups is the open question in RESEARCH.md — implement the AspNetUserRoles re-insert in Down() (INSERT INTO AspNetUserRoles selecting from UserGroups before the UserGroups table is dropped, mapping GroupRole int back to role IDs Player=0→1, DungeonMaster=1→2, Admin=2→3). If full reversibility proves impractical, document explicitly in a `Down()` comment that data restoration is best-effort and schema rollback is the guaranteed contract.

    Do not introduce a fenced literal that a later negative grep checks for; this action describes the SQL by concept. The executor writes the actual SQL in the migration file.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj 2>&1 | grep -v '^#' | grep -c "error CS"</automated>
  </verify>
  <acceptance_criteria>
    - A file `QuestBoard.Repository/Migrations/*_AddGroupSchema.cs` exists with `public partial class AddGroupSchema : Migration`
    - The migration `Up()` contains `CreateTable` for `Groups` and for `UserGroups`
    - The migration `Up()` contains `AddColumn<int>` for `GroupId` on `Quests` and on `ShopItems` with `defaultValue: 0`
    - The migration `Up()` contains `IDENTITY_INSERT Groups ON` and inserts `'EuphoriaInn'` with explicit Id 1
    - The migration `Up()` contains `UPDATE Quests SET GroupId = 1` and `UPDATE ShopItems SET GroupId = 1`
    - The migration `Up()` contains `INSERT INTO UserGroups` reading from `AspNetUserRoles` via a LEFT JOIN to AspNetUsers and a delete of Player/DungeonMaster/Admin from `AspNetUserRoles`
    - The Quest→Groups and ShopItem→Groups foreign keys use `ReferentialAction.NoAction`; the UserGroups FKs to AspNetUsers and Groups use cascade
    - `Down()` drops UserGroups and Groups and the two GroupId columns/FKs
    - `QuestBoardContextModelSnapshot.cs` includes `GroupEntity`, `UserGroupEntity`, and `GroupId` on Quests/ShopItems
    - `dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj` reports 0 `error CS` lines (count is 0)
  </acceptance_criteria>
  <done>The single atomic `AddGroupSchema` migration exists with all 8 ordered steps in Up(), a reverse Down(), the model snapshot is regenerated, and the Repository project builds. The migration is authored but not yet applied to a real database (apply + data verification is plan 03).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full integration test suite gate (191 tests)</name>
  <action>Pause for human verification. Run the full integration test suite and present the pass/fail result, then wait for the resume signal before completing the plan.</action>
  <what-built>The complete EF model (plan 01), the AddGroupSchema migration, and the TestDataHelper GroupId=1 updates. Running the full suite confirms the non-nullable GroupId and the new entities did not break any of the 191 existing integration tests (the InMemory provider builds schema from the model, so this exercises the model state, not the migration SQL).</what-built>
  <how-to-verify>
    1. From the solution root run: `dotnet test`
    2. Confirm the run reports 191 passed, 0 failed (the established baseline from Phase 26).
    3. If any test fails with an FK or GroupId-related error, the most likely cause is a test path that creates a QuestEntity or ShopItemEntity outside TestDataHelper without setting GroupId — search the IntegrationTests project for direct `new QuestEntity` / `new ShopItemEntity` usages and set `GroupId = 1` there too.
  </how-to-verify>
  <resume-signal>Type "approved" once `dotnet test` reports 191 passed / 0 failed, or describe the failing tests.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → SQL Server | The AddGroupSchema migration runs at app startup (`context.Database.Migrate()`) and executes raw SQL against Identity tables. The SQL is author-controlled (no user input interpolated), so injection surface is zero, but the data-transform correctness is the trust concern. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering / Integrity | Data seeding (UserGroups from AspNetUserRoles) | mitigate | LEFT JOIN ensures no user is dropped (D-04); MAX(CASE) highest-role logic ensures deterministic role assignment for multi-role users (D-03); explicit GroupId=1 UPDATE before FK add prevents orphan rows (research Pitfall 1). Verified in plan 03 by row-count + spot-check. |
| T-27-04 | Denial of Service | FK constraint added before data populated | mitigate | Step ordering in Up() populates GroupId=1 before AddForeignKey (research Pitfall 1) — prevents migration apply failure that would block startup. |
| T-27-05 | Repudiation / Recoverability | Down() loses deleted AspNetUserRoles | mitigate | Down() re-inserts AspNetUserRoles from UserGroups before dropping the table; if impractical, the schema-only rollback contract is documented in a Down() comment (RESEARCH.md open question 1). |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — RESEARCH.md Package Legitimacy Audit confirms zero new packages. The only tool run is `dotnet ef`, already present (RESEARCH.md A3). |
</threat_model>

<verification>
- `dotnet build` (full solution) exits 0.
- `dotnet test` reports 191 passed / 0 failed.
- Exactly one new migration named `AddGroupSchema` exists under `QuestBoard.Repository/Migrations/`.
- `QuestBoardContextModelSnapshot.cs` reflects Groups, UserGroups, and GroupId on Quests/ShopItems.
- The migration is NOT yet applied to a real database in this plan (apply happens in plan 03 / on next `dotnet run`).
</verification>

<success_criteria>
- GROUP-04 satisfied: migration Up() seeds EuphoriaInn as GroupId=1 and updates all existing Quests/ShopItems to GroupId=1.
- GROUP-05 satisfied: migration Up() seeds UserGroups for all users with highest current role from AspNetUserRoles (LEFT JOIN, no user dropped).
- GROUP-06 satisfied: migration Up() deletes Player/DungeonMaster/Admin rows from AspNetUserRoles.
- All 191 integration tests pass; the InMemory schema accepts the new entities and the non-nullable GroupId.
</success_criteria>

<output>
Create `.planning/phases/27-group-schema-foundation/27-02-SUMMARY.md` when done.
</output>
