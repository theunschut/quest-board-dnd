---
phase: 27-group-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Domain/Enums/GroupRole.cs
  - QuestBoard.Domain/Models/Group.cs
  - QuestBoard.Domain/Models/UserGroup.cs
  - QuestBoard.Repository/Entities/GroupEntity.cs
  - QuestBoard.Repository/Entities/UserGroupEntity.cs
  - QuestBoard.Repository/Entities/QuestEntity.cs
  - QuestBoard.Repository/Entities/ShopItemEntity.cs
  - QuestBoard.Repository/Entities/UserEntity.cs
  - QuestBoard.Repository/Entities/QuestBoardContext.cs
  - QuestBoard.Repository/Automapper/EntityProfile.cs
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03]
must_haves:
  truths:
    - "Solution compiles with the new GroupRole enum, GroupEntity, UserGroupEntity, and domain models present"
    - "QuestEntity and ShopItemEntity carry a non-nullable int GroupId FK property and a Group navigation property"
    - "UserEntity exposes a UserGroups navigation collection without adding any AspNetUsers column"
    - "QuestBoardContext registers Groups and UserGroups DbSets and configures the (UserId, GroupId) unique index, the Groups.Name unique index, and all four FK delete behaviors"
    - "EntityProfile maps UserGroupEntity.GroupRole int to/from the GroupRole enum"
  artifacts:
    - path: "QuestBoard.Domain/Enums/GroupRole.cs"
      provides: "GroupRole enum Player=0, DungeonMaster=1, Admin=2"
      contains: "enum GroupRole"
    - path: "QuestBoard.Repository/Entities/GroupEntity.cs"
      provides: "Groups table entity (Id, Name, CreatedAt, UserGroups nav)"
      contains: "class GroupEntity"
    - path: "QuestBoard.Repository/Entities/UserGroupEntity.cs"
      provides: "UserGroups junction entity (Id, UserId, GroupId, GroupRole int, nav props)"
      contains: "class UserGroupEntity"
    - path: "QuestBoard.Domain/Models/Group.cs"
      provides: "Group domain model"
      contains: "class Group"
    - path: "QuestBoard.Domain/Models/UserGroup.cs"
      provides: "UserGroup domain model with GroupRole enum property"
      contains: "class UserGroup"
  key_links:
    - from: "QuestBoard.Repository/Entities/UserGroupEntity.cs"
      to: "QuestBoard.Repository/Entities/UserEntity.cs"
      via: "ForeignKey(UserId) navigation to UserEntity; UserEntity.UserGroups collects them"
      pattern: "ICollection<UserGroupEntity>"
    - from: "QuestBoard.Repository/Entities/QuestBoardContext.cs"
      to: "QuestBoard.Repository/Entities/UserGroupEntity.cs"
      via: "DbSet<UserGroupEntity> + OnModelCreating relationship and index config"
      pattern: "HasIndex.*UserId.*GroupId.*IsUnique"
    - from: "QuestBoard.Repository/Automapper/EntityProfile.cs"
      to: "QuestBoard.Domain/Enums/GroupRole.cs"
      via: "int GroupRole cast to/from GroupRole enum in CreateMap"
      pattern: "GroupRole\\)src.GroupRole"
---

<objective>
Add the multi-group EF Core model: the `GroupRole` enum (Domain), `GroupEntity` and `UserGroupEntity` (Repository), `Group` and `UserGroup` domain models, the non-nullable `GroupId` FK on `QuestEntity` and `ShopItemEntity`, the `UserGroups` navigation on `UserEntity`, the DbSet registrations and `OnModelCreating` relationship/index configuration in `QuestBoardContext`, and the `UserGroupEntity` ↔ `UserGroup` enum mapping in `EntityProfile`.

This plan delivers GROUP-01 (GroupEntity table), GROUP-02 (UserGroups junction table), and GROUP-03 (GroupId FK on the two shared-resource entities) at the model level. It produces NO migration and NO data seeding — that is plan 02. This is a pure model-and-mapping change verified by a green `dotnet build`.

Purpose: Establish the EF model state that the AddGroupSchema migration (plan 02) will scaffold from. Per CONTEXT.md the EF model is the single source of truth for both the SQL Server migration and the InMemory integration-test schema.
Output: 5 new files (enum, 2 entities, 2 domain models) and 5 modified files (QuestEntity, ShopItemEntity, UserEntity, QuestBoardContext, EntityProfile), all compiling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-group-schema-foundation/27-CONTEXT.md
@.planning/phases/27-group-schema-foundation/27-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 01)

New files:
- `QuestBoard.Domain/Enums/GroupRole.cs` — `public enum GroupRole { Player = 0, DungeonMaster = 1, Admin = 2 }`
- `QuestBoard.Repository/Entities/GroupEntity.cs` — `class GroupEntity : IEntity` with `int Id`, `string Name`, `DateTime CreatedAt`, `ICollection<UserGroupEntity> UserGroups`
- `QuestBoard.Repository/Entities/UserGroupEntity.cs` — `class UserGroupEntity : IEntity` with `int Id`, `int UserId`, `int GroupId`, `int GroupRole`, `UserEntity User` nav, `GroupEntity Group` nav
- `QuestBoard.Domain/Models/Group.cs` — `class Group : IModel` with `int Id`, `string Name`, `DateTime CreatedAt`
- `QuestBoard.Domain/Models/UserGroup.cs` — `class UserGroup : IModel` with `int Id`, `int UserId`, `int GroupId`, `GroupRole GroupRole`

Modified symbols:
- `QuestEntity.GroupId` (int) + `QuestEntity.Group` (GroupEntity nav)
- `ShopItemEntity.GroupId` (int) + `ShopItemEntity.Group` (GroupEntity nav)
- `UserEntity.UserGroups` (`ICollection<UserGroupEntity>`)
- `QuestBoardContext.Groups` (`DbSet<GroupEntity>`) + `QuestBoardContext.UserGroups` (`DbSet<UserGroupEntity>`)
- `EntityProfile`: `CreateMap<UserGroupEntity, UserGroup>` and `CreateMap<UserGroup, UserGroupEntity>` with GroupRole int↔enum conversion
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add GroupRole enum, Group/UserGroup entities and domain models, and GroupId FK / nav properties</name>
  <files>QuestBoard.Domain/Enums/GroupRole.cs, QuestBoard.Domain/Models/Group.cs, QuestBoard.Domain/Models/UserGroup.cs, QuestBoard.Repository/Entities/GroupEntity.cs, QuestBoard.Repository/Entities/UserGroupEntity.cs, QuestBoard.Repository/Entities/QuestEntity.cs, QuestBoard.Repository/Entities/ShopItemEntity.cs, QuestBoard.Repository/Entities/UserEntity.cs</files>
  <read_first>
    - QuestBoard.Domain/Enums/Role.cs (enum placement and namespace `QuestBoard.Domain.Enums`)
    - QuestBoard.Domain/Models/IModel.cs (domain model marker interface — requires `int Id { get; set; }`)
    - QuestBoard.Domain/Models/User.cs (root-namespace domain model reference: `QuestBoard.Domain.Models`)
    - QuestBoard.Repository/Entities/IEntity.cs (entity marker interface — requires `int Id`)
    - QuestBoard.Repository/Entities/PlayerDateVoteEntity.cs (junction entity reference: `[Key]`/`[DatabaseGenerated(Identity)]` Id + `[ForeignKey(nameof(...))]` virtual nav props)
    - QuestBoard.Repository/Entities/QuestEntity.cs (FK + nav pattern: `int DungeonMasterId` + `[ForeignKey(nameof(DungeonMasterId))] public virtual UserEntity DungeonMaster`)
    - QuestBoard.Repository/Entities/ShopItemEntity.cs (FK pattern: `int CreatedByDmId` + `[ForeignKey] virtual UserEntity CreatedByDm`)
    - QuestBoard.Repository/Entities/UserEntity.cs (IdentityUser&lt;int&gt; nav-collection pattern: `public virtual ICollection<QuestEntity> Quests { get; set; } = [];`)
  </read_first>
  <action>
    Create `QuestBoard.Domain/Enums/GroupRole.cs` in namespace `QuestBoard.Domain.Enums`: a public enum `GroupRole` with explicit values `Player = 0`, `DungeonMaster = 1`, `Admin = 2` (per D-05).

    Create `QuestBoard.Repository/Entities/GroupEntity.cs` in namespace `QuestBoard.Repository.Entities`, attributed `[Table("Groups")]`, implementing `IEntity`: `int Id` with `[Key]` + `[DatabaseGenerated(DatabaseGeneratedOption.Identity)]`; `string Name` with `[Required]` + `[StringLength(100)]` defaulting to `string.Empty`; `DateTime CreatedAt` defaulting to `DateTime.UtcNow`; and `public virtual ICollection<UserGroupEntity> UserGroups { get; set; } = [];` (per GROUP-01, D-07-style nav consistency, D-08 name uniqueness enforced later in QuestBoardContext).

    Create `QuestBoard.Repository/Entities/UserGroupEntity.cs` in namespace `QuestBoard.Repository.Entities`, attributed `[Table("UserGroups")]`, implementing `IEntity`, following the PlayerDateVoteEntity pattern (auto-increment Id PK plus a unique index configured in QuestBoardContext — NOT a composite key, per D-06 and the research anti-pattern note): `int Id` with `[Key]` + `[DatabaseGenerated(DatabaseGeneratedOption.Identity)]`; `int UserId` with `[Required]`; `int GroupId` with `[Required]`; `int GroupRole` with `[Required]` (stored as int per D-05 — no enum type on the entity); `[ForeignKey(nameof(UserId))] public virtual UserEntity User { get; set; } = null!;`; `[ForeignKey(nameof(GroupId))] public virtual GroupEntity Group { get; set; } = null!;` (per GROUP-02, D-06).

    Create `QuestBoard.Domain/Models/Group.cs` in namespace `QuestBoard.Domain.Models`, implementing `IModel`: `int Id`, `string Name` (default `string.Empty`), `DateTime CreatedAt`.

    Create `QuestBoard.Domain/Models/UserGroup.cs` in namespace `QuestBoard.Domain.Models`, implementing `IModel`, importing `QuestBoard.Domain.Enums`: `int Id`, `int UserId`, `int GroupId`, and `GroupRole GroupRole` (the enum type — the domain model is where the enum lives, per D-05).

    Modify `QuestBoard.Repository/Entities/QuestEntity.cs`: add `public int GroupId { get; set; }` and `[ForeignKey(nameof(GroupId))] public virtual GroupEntity Group { get; set; } = null!;` following the existing DungeonMasterId/DungeonMaster FK pattern (per GROUP-03, D-01 non-nullable int).

    Modify `QuestBoard.Repository/Entities/ShopItemEntity.cs`: add `public int GroupId { get; set; }` and `[ForeignKey(nameof(GroupId))] public virtual GroupEntity Group { get; set; } = null!;` following the existing CreatedByDmId/CreatedByDm FK pattern (per GROUP-03, D-01).

    Modify `QuestBoard.Repository/Entities/UserEntity.cs`: add ONLY the navigation collection `public virtual ICollection<UserGroupEntity> UserGroups { get; set; } = [];` (per D-07). Add NO scalar columns — `UserEntity : IdentityUser<int>` maps to AspNetUsers and a navigation property is the only safe change (research Pitfall 4).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj 2>&1 | grep -v '^#' | grep -c "error CS"</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoard.Domain/Enums/GroupRole.cs` contains `enum GroupRole` with `Player = 0`, `DungeonMaster = 1`, `Admin = 2`
    - `QuestBoard.Repository/Entities/GroupEntity.cs` contains `[Table("Groups")]`, `class GroupEntity : IEntity`, and `ICollection<UserGroupEntity> UserGroups`
    - `QuestBoard.Repository/Entities/UserGroupEntity.cs` contains `[Table("UserGroups")]`, `public int GroupRole { get; set; }` (int, not the enum), and two `[ForeignKey(...)]` virtual nav properties
    - `QuestEntity.cs` contains `public int GroupId { get; set; }` and `public virtual GroupEntity Group`
    - `ShopItemEntity.cs` contains `public int GroupId { get; set; }` and `public virtual GroupEntity Group`
    - `UserEntity.cs` contains `ICollection<UserGroupEntity> UserGroups` and adds no `[StringLength]`/scalar property beyond what already existed
    - `QuestBoard.Domain/Models/UserGroup.cs` contains `public GroupRole GroupRole { get; set; }` (the enum type)
    - `dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj` reports 0 `error CS` lines (count is 0)
  </acceptance_criteria>
  <done>The enum, both entities, both domain models, and the FK/nav property additions exist and the Repository project compiles with zero errors. No DbSet or OnModelCreating wiring yet (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Register DbSets, configure relationships/indexes in QuestBoardContext, and add EntityProfile GroupRole mapping</name>
  <files>QuestBoard.Repository/Entities/QuestBoardContext.cs, QuestBoard.Repository/Automapper/EntityProfile.cs</files>
  <read_first>
    - QuestBoard.Repository/Entities/QuestBoardContext.cs (current DbSets and the full OnModelCreating body — existing `HasIndex(...).IsUnique()` on PlayerDateVoteEntity and ReminderLogEntity, and the NoAction/Cascade `HasOne(...).WithMany(...).HasForeignKey(...).OnDelete(...)` patterns)
    - QuestBoard.Repository/Automapper/EntityProfile.cs (existing int↔enum CreateMap patterns — see ShopItem Type/Rarity/Status: `.ForMember(dest => dest.Type, opt => opt.MapFrom(src => (int)src.Type))` one way and `(ItemType)src.Type` the other)
    - QuestBoard.Repository/Entities/GroupEntity.cs (the entity created in Task 1)
    - QuestBoard.Repository/Entities/UserGroupEntity.cs (the entity created in Task 1)
    - QuestBoard.Domain/Models/UserGroup.cs (the domain model created in Task 1)
  </read_first>
  <action>
    In `QuestBoard.Repository/Entities/QuestBoardContext.cs`, add two DbSet properties alongside the existing ones: `public DbSet<GroupEntity> Groups { get; set; }` and `public DbSet<UserGroupEntity> UserGroups { get; set; }`.

    In `OnModelCreating` (after `base.OnModelCreating(modelBuilder)` and consistent with the existing configuration blocks), add the following six configurations:
    - `modelBuilder.Entity<GroupEntity>().HasIndex(g => g.Name).IsUnique();` (per D-08, GROUP-01)
    - `modelBuilder.Entity<UserGroupEntity>().HasIndex(ug => new { ug.UserId, ug.GroupId }).IsUnique();` (per D-06, GROUP-02 — mirrors the PlayerDateVoteEntity composite unique index)
    - Quest → Group FK with NoAction: `modelBuilder.Entity<QuestEntity>().HasOne(q => q.Group).WithMany().HasForeignKey(q => q.GroupId).OnDelete(DeleteBehavior.NoAction);` (per D-10, GROUP-03)
    - ShopItem → Group FK with NoAction: `modelBuilder.Entity<ShopItemEntity>().HasOne(si => si.Group).WithMany().HasForeignKey(si => si.GroupId).OnDelete(DeleteBehavior.NoAction);` (per D-10)
    - UserGroup → User FK with Cascade: `modelBuilder.Entity<UserGroupEntity>().HasOne(ug => ug.User).WithMany(u => u.UserGroups).HasForeignKey(ug => ug.UserId).OnDelete(DeleteBehavior.Cascade);` (per D-09)
    - UserGroup → Group FK with Cascade: `modelBuilder.Entity<UserGroupEntity>().HasOne(ug => ug.Group).WithMany(g => g.UserGroups).HasForeignKey(ug => ug.GroupId).OnDelete(DeleteBehavior.Cascade);` (per D-09)

    In `QuestBoard.Repository/Automapper/EntityProfile.cs`, add two maps following the ShopItem int↔enum pattern: `CreateMap<UserGroupEntity, UserGroup>().ForMember(dest => dest.GroupRole, opt => opt.MapFrom(src => (GroupRole)src.GroupRole));` and `CreateMap<UserGroup, UserGroupEntity>().ForMember(dest => dest.GroupRole, opt => opt.MapFrom(src => (int)src.GroupRole));`. Ensure `QuestBoard.Domain.Enums` and `QuestBoard.Domain.Models` are imported (they already are — `GroupRole` is in Enums, `UserGroup` in Models). Optionally add `CreateMap<GroupEntity, Group>().ReverseMap();` for symmetry (no enum conversion needed for Group).
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -v '^#' | grep -c "error CS"</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoardContext.cs` contains `DbSet<GroupEntity> Groups` and `DbSet<UserGroupEntity> UserGroups`
    - `QuestBoardContext.cs` OnModelCreating contains `HasIndex(g => g.Name).IsUnique()`
    - `QuestBoardContext.cs` OnModelCreating contains `HasIndex(ug => new { ug.UserId, ug.GroupId }).IsUnique()`
    - `QuestBoardContext.cs` contains a Quest→Group and a ShopItem→Group relationship configured with `OnDelete(DeleteBehavior.NoAction)`
    - `QuestBoardContext.cs` contains a UserGroup→User and a UserGroup→Group relationship configured with `OnDelete(DeleteBehavior.Cascade)`
    - `EntityProfile.cs` contains `CreateMap<UserGroupEntity, UserGroup>` with `(GroupRole)src.GroupRole` and `CreateMap<UserGroup, UserGroupEntity>` with `(int)src.GroupRole`
    - `dotnet build` (whole solution) reports 0 `error CS` lines (count is 0)
  </acceptance_criteria>
  <done>QuestBoardContext registers both new DbSets and configures the two unique indexes and four FK delete behaviors; EntityProfile maps the GroupRole int↔enum boundary; the whole solution builds with zero errors. The EF model is now complete and ready for migration scaffolding (plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no input-processing surface — it is C# model classes, EF configuration, and AutoMapper mapping. No HTTP endpoint, no user input, no auth decision is introduced. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | UserGroupEntity (UserId, GroupId) uniqueness | mitigate | Database unique index on `(UserId, GroupId)` configured in OnModelCreating — prevents duplicate membership rows at the DB layer (research "Don't Hand-Roll"). |
| T-27-02 | Information Disclosure | GroupRole stored as int | accept | GroupRole is not consumed for any access decision until Phase 29; no data exposure in this phase. Low severity per security contribution (pure schema phase). |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — RESEARCH.md Package Legitimacy Audit confirms zero new packages. No supply-chain surface. |
</threat_model>

<verification>
- `dotnet build` (full solution) exits 0 with zero `error CS` lines.
- All 5 new files exist at the paths listed in `files_modified`.
- `UserEntity.cs` gained only a navigation collection, no scalar column (confirm no new `[StringLength]`/column property versus the pre-plan version).
- No migration file is created in this plan (migration is plan 02 — verify `QuestBoard.Repository/Migrations/` has no new `*_AddGroupSchema.cs`).
</verification>

<success_criteria>
- GROUP-01 satisfied at model level: `GroupEntity` with Id/Name/CreatedAt exists and is registered as a DbSet with a unique Name index.
- GROUP-02 satisfied at model level: `UserGroupEntity` with UserId/GroupId/GroupRole(int) exists, registered as a DbSet, with a `(UserId, GroupId)` unique index and nav properties to User and Group.
- GROUP-03 satisfied at model level: non-nullable `int GroupId` FK on both `QuestEntity` and `ShopItemEntity` with NoAction delete behavior.
- Solution compiles; EntityProfile maps the GroupRole int↔enum boundary.
</success_criteria>

<output>
Create `.planning/phases/27-group-schema-foundation/27-01-SUMMARY.md` when done.
</output>
