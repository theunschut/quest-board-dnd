---
phase: 30-group-ux-admin-user-creation
plan: 05
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03", "30-04"]
files_modified:
  - QuestBoard.IntegrationTests/Controllers/GroupPickerControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, MGMT-07, REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "A single-group non-SuperAdmin user is redirected away from the picker"
    - "A multi-group user receives the picker page"
    - "A SuperAdmin receives the picker page with the Platform option"
    - "Selecting a group persists ActiveGroupId in session for subsequent requests"
    - "GET and POST /Account/Register return 404"
    - "An admin creating a user assigns them to the active group and queues a confirmation email"
    - "The full test suite is green before the phase is considered done"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/GroupPickerControllerIntegrationTests.cs"
      provides: "Integration coverage for UX-01..UX-04"
      contains: "GroupPicker"
    - path: "QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs"
      provides: "CreateUser coverage for MGMT-07, REG-02, REG-03"
      contains: "CreateUser"
  key_links:
    - from: "QuestBoard.IntegrationTests/Controllers/GroupPickerControllerIntegrationTests.cs"
      to: "QuestBoard.Service/Controllers/GroupPickerController.cs"
      via: "HTTP GET /groups/pick and POST /GroupPicker/SelectGroup"
      pattern: "groups/pick|SelectGroup"
---

<objective>
Add and update automated tests covering the Phase 30 behaviors, then gate on the full suite.

Purpose: Nyquist requires automated proof for every phase behavior. RESEARCH.md flags concrete test work: a new GroupPickerController integration suite (UX-01..UX-04), updates to the existing Register tests which now must assert 404 (REG-01, RESEARCH.md Pitfall 4), and CreateUser coverage in the admin suite (MGMT-07, REG-02, REG-03). This plan ends with the full-suite green gate that marks the phase verifiable.

Output: `GroupPickerControllerIntegrationTests.cs` (new); updated `AccountControllerIntegrationTests.cs` (Register → 404); updated `AdminControllerIntegrationTests.cs` (CreateUser). Full `dotnet test` passes.

Covers UX-01, UX-02, UX-03, UX-04, UX-05 (nav display assertion), MGMT-07, REG-01, REG-02, REG-03. (MGMT-08 promote/demote is already covered by existing Phase 29 admin tests; this plan confirms they still pass post `?? 1` removal.)

This plan has a blocking human-verify checkpoint at the end (autonomous: false) per the phase's human-in-the-loop convention for UX-bearing phases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-group-ux-admin-user-creation/30-RESEARCH.md
@.planning/phases/30-group-ux-admin-user-creation/30-CONTEXT.md
</context>

<artifacts_this_phase_produces>
**Plan 30-05 creates/updates these test artifacts:**

| File | Change |
|------|--------|
| `GroupPickerControllerIntegrationTests.cs` | new — single-group redirect, multi-group picker, SuperAdmin picker, session persistence |
| `AccountControllerIntegrationTests.cs` | Register_Get / Register_Post tests updated to assert 404; mismatched-password Register test removed |
| `AdminControllerIntegrationTests.cs` | new CreateUser tests — group assignment + auth gating |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create GroupPickerControllerIntegrationTests (UX-01..UX-04)</name>
  <files>QuestBoard.IntegrationTests/Controllers/GroupPickerControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs (IClassFixture<WebApplicationFactoryBase> pattern, CreateNonRedirectingClient usage, status-code assertions)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs (CreateAuthenticatedClientWithUserAsync seeds a UserGroups row for group 1; CreateAuthenticatedSuperAdminClientAsync; the auth header format; how to create a user in multiple groups)
    - QuestBoard.IntegrationTests/WebApplicationFactoryBase.cs (MutableGroupContext default ActiveGroupId = 1; TestDatabase seeding of Group 1; helper methods for creating clients)
    - QuestBoard.Service/Controllers/GroupPickerController.cs (Index GET route /groups/pick or /GroupPicker; SelectGroup POST; session keys written)
    - QuestBoard.Domain/Enums/GroupRole.cs (role values for seeding memberships)
  </read_first>
  <action>
    Create `GroupPickerControllerIntegrationTests` as `IClassFixture<WebApplicationFactoryBase>`, mirroring the constructor/fixture style of `AdminControllerIntegrationTests`. Use a non-redirecting client so 302 redirects are observable. Tests to add:

    - `Index_WhenNotAuthenticated_ShouldRedirectToLogin`: GET `/GroupPicker/Index` (or `/groups/pick`) unauthenticated → assert 302/401 (the `[Authorize]` gate).
    - `Index_WhenSingleGroupUser_ShouldRedirectAwayFromPicker` (UX-01): create an authenticated non-SuperAdmin user who is a member of exactly one group (the default helper seeds group 1) → GET the picker → assert a 302 redirect (NOT a 200 picker page). Assert the redirect Location is not the picker itself.
    - `Index_WhenMultiGroupUser_ShouldReturnPickerPage` (UX-02): create a user and add a SECOND UserGroups membership (seed a second group via the QuestBoardContext in a scope, then add a UserGroups row for the user) → GET the picker → assert 200 and the response body contains the "Select Your Group" heading and a `SelectGroup` form.
    - `Index_WhenSuperAdmin_ShouldReturnPickerWithPlatformOption` (UX-03): use `CreateAuthenticatedSuperAdminClientAsync` → GET the picker → assert 200 and body contains "Go to Platform" and a link/href to `/platform`.
    - `SelectGroup_ShouldPersistActiveGroupInSession` (UX-04): POST to `SelectGroup` with a valid `groupId` (and anti-forgery token — follow the existing POST-with-token helper pattern in the test project; if the project's tests post without CSRF because the Test auth scheme/test env disables anti-forgery validation, follow that established convention) → assert a 302 redirect → then issue a follow-up authenticated request on the same client/session to a page whose content depends on the selected group and assert the selection took effect (or assert the redirect target is the returnUrl/Home). Keep the assertion within what the test infrastructure can observe; if cookie-session round-tripping is not supported by the test client, assert the SelectGroup redirect and group lookup instead and note the limitation in a comment.

    Use FluentAssertions and `TestContext.Current.CancellationToken` consistent with the existing suite. Make user names/emails unique (the helper already appends a GUID suffix).
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~GroupPickerController"</automated>
  </verify>
  <acceptance_criteria>
    - `GroupPickerControllerIntegrationTests.cs` exists with at least: unauthenticated-redirect, single-group-redirect (UX-01), multi-group-picker (UX-02), SuperAdmin-picker (UX-03), and select-group-session (UX-04) tests
    - The single-group test asserts a redirect (no 200 picker page) and the multi-group test asserts a 200 containing a `SelectGroup` form
    - The SuperAdmin test asserts the response contains a Platform option
    - `dotnet test --filter "FullyQualifiedName~GroupPickerController"` passes
  </acceptance_criteria>
  <done>GroupPickerController behaviors UX-01..UX-04 have passing integration tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update Register tests to assert 404 and add AdminController CreateUser tests</name>
  <files>QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs (Register_Get_ShouldReturnSuccessStatusCode, Register_Post_WithValidData_ShouldCreateUser, Register_Post_WithMismatchedPasswords_ShouldReturnError — the three Register tests that now break)
    - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs (CreateAuthenticatedAdminClientAsync usage, status-code assertion style, how existing admin POST tests are structured)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs (CreateAuthenticatedAdminClientAsync seeds Admin GroupRole in group 1; CreateAuthenticatedClientWithUserAsync for a Player who should be forbidden)
    - QuestBoard.Service/Controllers/Admin/AdminController.cs (CreateUser GET/POST routes — /Admin/CreateUser)
    - .planning/phases/30-group-ux-admin-user-creation/30-RESEARCH.md (§Common Pitfalls Pitfall 4 — Register tests; §Phase Requirements → Test Map)
  </read_first>
  <action>
    In `AccountControllerIntegrationTests.cs` (REG-01): update `Register_Get_ShouldReturnSuccessStatusCode` to assert `HttpStatusCode.NotFound` for GET `/Account/Register` (rename it to reflect 404, e.g. `Register_Get_ShouldReturnNotFound`). Update or remove `Register_Post_WithValidData_ShouldCreateUser` and `Register_Post_WithMismatchedPasswords_ShouldReturnError`: convert `Register_Post_WithValidData` to assert POST `/Account/Register` returns `HttpStatusCode.NotFound`, and remove the mismatched-passwords test (the action no longer exists). Do not leave any test that GETs `/Account/Register` expecting 200 or that expects a created user from the public route.

    In `AdminControllerIntegrationTests.cs` add CreateUser coverage:
    - `CreateUser_WhenNotAdmin_ShouldBeForbidden`: a Player-role authenticated client GETs `/Admin/CreateUser` → assert 403/redirect/unauthorized (AdminOnly gate), matching the existing `Index_WhenNotAdmin` assertion style.
    - `CreateUser_Get_WhenAdmin_ShouldReturnForm`: an admin client GETs `/Admin/CreateUser` → assert 200 and body contains a `CreateUser` form and the Group Role select.
    - `CreateUser_Post_WhenAdmin_CreatesUserInActiveGroup` (MGMT-07, REG-02, REG-03): an admin client POSTs valid CreateUser form data (unique email, name, password ≥ 8 chars, a GroupRole) → assert a 302 redirect to Users. Then verify via a service scope (resolve `QuestBoardContext` / `IUserService`) that the created user exists and has a UserGroups row in group 1 (the admin's active group under MutableGroupContext default) with the chosen GroupRole. Assert email confirmation was triggered to the extent the Testing env allows — the Testing environment uses a NoOp background job client, so assert the user creation + group assignment succeeded (the enqueue call path is exercised; deep email assertion is out of scope for the NoOp dispatcher). Follow the existing test project's convention for posting forms with/without anti-forgery tokens.

    Keep all new tests using unique emails (helper appends GUID) and `TestContext.Current.CancellationToken`.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~AccountController|FullyQualifiedName~AdminController"</automated>
  </verify>
  <acceptance_criteria>
    - No test in `AccountControllerIntegrationTests.cs` expects `/Account/Register` to return 200 or to create a user; the Register GET test asserts `HttpStatusCode.NotFound`
    - `AdminControllerIntegrationTests.cs` contains a `CreateUser_Post_WhenAdmin_CreatesUserInActiveGroup` test that asserts the created user has a UserGroups membership in the active group with the chosen GroupRole (REG-02)
    - `AdminControllerIntegrationTests.cs` contains a CreateUser auth-gating test (non-admin forbidden) and a CreateUser GET-form test
    - `dotnet test --filter "FullyQualifiedName~AccountController|FullyQualifiedName~AdminController"` passes
  </acceptance_criteria>
  <done>Register routes are proven to 404 and admin user creation is proven to assign group + role.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full-suite green gate</name>
  <files>QuestBoard.IntegrationTests/Controllers/GroupPickerControllerIntegrationTests.cs</files>
  <read_first>
    - .planning/phases/30-group-ux-admin-user-creation/30-RESEARCH.md (§Validation Architecture — Sampling Rate, Phase gate: full suite green)
    - .planning/STATE.md (current baseline: 219/219 tests pass at Phase 29 completion)
  </read_first>
  <action>
    Run the complete test suite and ensure it is green. The Phase 29 baseline was 219 passing tests; Phase 30 removes 1–2 Register tests and adds GroupPicker + CreateUser tests, so the final count differs but MUST be all-passing. Investigate and fix any failure caused by Phase 30 changes:
    - If an existing admin/account test fails due to the login-redirect change (a test that asserted login lands on Home/returnUrl), update it to expect the redirect to `/groups/pick` (or to follow the redirect chain).
    - If a test fails due to `?? 1` removal returning a 302-to-GroupPicker on null active group, set the test's group context appropriately (MutableGroupContext default is 1) per RESEARCH.md Pitfall 3.
    Do not weaken assertions to force green — fix the test to reflect the new correct behavior, or fix the product code if a genuine regression is found.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.sln</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.sln` reports zero failing tests
    - No test was disabled, skipped, or had its assertion weakened to achieve green (failures are fixed by correcting expectations to the new behavior or fixing real regressions)
  </acceptance_criteria>
  <done>The entire test suite passes with the Phase 30 behavior in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify the Phase 30 group-UX loop end to end</name>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps to the user, wait for the resume-signal, and do not proceed until the user types "approved" or reports issues to address.</action>
  <what-built>
    The full Phase 30 group-UX loop: login now routes through a group picker (`/groups/pick`), single-group users skip the picker, multi-group users and SuperAdmins choose a group, the active group name + Switch Group link appear in the nav, self-registration is gone (Register → 404), and group admins can create users from /Admin/CreateUser that land in their active group with a confirmation email.
  </what-built>
  <how-to-verify>
    1. Run the app (`dotnet run --project QuestBoard.Service`) and log in as a user who belongs to exactly one group → confirm you land on the quest board directly (no picker shown). [UX-01]
    2. Log in as a user in multiple groups (or as SuperAdmin) → confirm the group picker page renders cards; SuperAdmin additionally sees a "Go to Platform" button. [UX-02, UX-03]
    3. Click a group card → confirm you enter the quest board and the user dropdown (desktop) shows the group name; open the mobile/offcanvas menu → confirm the same group switch item. Click it → confirm you return to the picker. [UX-04, UX-05]
    4. Visit `/Account/Register` directly → confirm a 404. Confirm the login page no longer shows "Create Account". [REG-01]
    5. As a group admin, go to /Admin/Users → Create User, submit a new account with a chosen role → confirm success message, the user appears in the Users list with the right role, and (if email is configured) a confirmation email is queued. [MGMT-07, REG-02, REG-03]
    6. As a group admin, promote/demote a user → confirm the role changes correctly within the active group. [MGMT-08]
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Tests exercise the real HTTP pipeline through WebApplicationFactory; no production trust boundary is changed by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-16 | Tampering (false-green tests) | test suite | mitigate | Task 3 forbids weakening/skip; failures must be fixed by correcting behavior expectations or real regressions; full-suite gate is the phase verifier |
| T-30-17 | Repudiation (untested security paths) | CreateUser auth gate, Register removal | mitigate | Explicit tests assert AdminOnly gating on CreateUser and 404 on Register, proving the access-control changes from plans 30-02/30-03 |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan |
</threat_model>

<verification>
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~GroupPickerController"` passes (UX-01..UX-04)
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~AccountController|FullyQualifiedName~AdminController"` passes (REG-01, MGMT-07, REG-02, REG-03)
- `dotnet test QuestBoard.sln` fully green (phase gate)
- Blocking human-verify checkpoint approves the end-to-end UX
</verification>

<success_criteria>
- Automated tests prove UX-01, UX-02, UX-03, UX-04, MGMT-07, REG-01, REG-02, REG-03
- MGMT-08 confirmed still-passing via existing Phase 29 admin tests after `?? 1` removal
- Full suite green; no skipped or weakened tests
- Human verification confirms the complete group-UX loop and admin user creation
</success_criteria>

<output>
Create `.planning/phases/30-group-ux-admin-user-creation/30-05-SUMMARY.md` when done
</output>
