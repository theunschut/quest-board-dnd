---
phase: 37-navigation-access-control
plan: 03
type: execute
wave: 2
depends_on: [37-01, 37-02]
files_modified:
  - QuestBoard.Service/Views/Shared/_Layout.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, ACCESS-01]
must_haves:
  truths:
    - "In a campaign group, both desktop and mobile nav hide Calendar, Shop, Manage Shop, Edit My Profile, and Players (D-01 — allowlist polarity: show only when confirmed OneShot, not a Campaign blocklist)"
    - "Guild member directory and Quest Log stay visible for all board types (unchanged) (D-05)"
    - "In a one-shot group, all nav items render exactly as before this phase (no regression)"
    - "Calendar is hidden for anonymous (logged-out) visitors in both layouts (D-04)"
    - "The Email Stats nav link only renders for SuperAdmin in both layouts"
    - "SuperAdmin's nav gating follows whatever group is currently active — no special-casing for the SuperAdmin role in the OneShot allowlist (D-02)"
    - "When no active group is set (anonymous or authenticated-no-group), the five allowlisted items are hidden, consistent with GetBoardTypeAsync returning null (D-03)"
  artifacts:
    - path: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      provides: "OneShot allowlist nav gating (desktop) + D-04 Calendar auth wrap + SuperAdmin Email Stats link gate"
      contains: "GetBoardTypeAsync"
    - path: "QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml"
      provides: "OneShot allowlist nav gating (mobile) + D-04 Calendar auth wrap + SuperAdmin Email Stats link gate"
      contains: "GetBoardTypeAsync"
  key_links:
    - from: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      to: "IActiveGroupContext.GetBoardTypeAsync"
      via: "@inject IActiveGroupContext + @await"
      pattern: "GetBoardTypeAsync"
    - from: "QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml"
      to: "IActiveGroupContext.GetBoardTypeAsync"
      via: "@inject IActiveGroupContext + @await"
      pattern: "GetBoardTypeAsync"
---

<objective>
Apply the OneShot allowlist nav gating to both layouts, fix the D-04 anonymous-Calendar leak, and gate the Email Stats nav link to SuperAdmin — turning Plan 01's `LayoutNavigationTests` from RED to GREEN. This is the single-owner edit of `_Layout.cshtml` and `_Layout.Mobile.cshtml`; it consumes the `GetBoardTypeAsync` contract from Plan 01 and points the Email Stats link at the SuperAdmin gate established in Plan 02.

Purpose: NAV-01..06 require campaign groups to see a stripped-down nav; ACCESS-01's cosmetic half requires the Email Stats link to disappear for Admin-but-not-SuperAdmin. Both concerns live only in the two layout files, so they are done together to keep exclusive file ownership.
Output: Both layouts gate the five campaign-hidden items behind confirmed-OneShot, keep Guild Members / Quest Log always visible, wrap Calendar in an authenticated check, and show Email Stats only to SuperAdmin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-navigation-access-control/37-CONTEXT.md
@.planning/phases/37-navigation-access-control/37-RESEARCH.md
@.planning/phases/37-navigation-access-control/37-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from Plan 01 output + codebase. -->

New context member available after Plan 01 (QuestBoard.Domain/Interfaces/IActiveGroupContext.cs):
```csharp
Task<BoardType?> GetBoardTypeAsync(CancellationToken token = default);
```
Returns null when no active group (anonymous / no-group-picked / unknown group) — per D-03. `BoardType` enum (QuestBoard.Domain.Enums) has values OneShot, Campaign.

Both layouts currently begin (lines 1-3) with:
```csharp
@using QuestBoard.Domain.Interfaces
@using QuestBoard.Service.Constants
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
```
Add to BOTH: `@using QuestBoard.Domain.Enums` and `@inject IActiveGroupContext ActiveGroupContext`.

DESKTOP _Layout.cshtml landmarks (per Pattern Map):
- Admin dropdown gate (lines 40-72): `@if ((await AuthorizationService.AuthorizeAsync(User, "AdminOnly")).Succeeded)`; inside it a SuperAdmin-only "Background Jobs" link already uses `@if (User.IsInRole("SuperAdmin"))` (line ~57); the Email Stats `<li>` (lines 65-69) is currently ungated inside the AdminOnly dropdown.
- DM dropdown gate (lines 74-100): `@if ((await AuthorizationService.AuthorizeAsync(User, "DungeonMasterOnly")).Succeeded)`; contains Create Quest (keep ungated), Manage Shop (ShopManagement/Index, lines 87-91), Edit My Profile (DungeonMaster/EditProfile, lines 92-96).
- Standalone items (lines 102-123): Shop (Shop/Index, 103-107), Quest Log (QuestLog/Index, 108-112 — KEEP), Guild Members (GuildMembers/Index, 113-117 — KEEP), Players (Players/Index, 118-122).
- Calendar (lines 126-131): rendered UNCONDITIONALLY in the `<ul class="navbar-nav ms-auto">` group, OUTSIDE the `@if (User.Identity?.IsAuthenticated == true)` block that wraps the profile dropdown.

MOBILE _Layout.Mobile.cshtml landmarks (per Pattern Map):
- DM block (lines 67-85): flat `<li>` list inside `@if ((await AuthorizationService.AuthorizeAsync(User, "DungeonMasterOnly")).Succeeded)` — Create Quest (keep), Manage Shop (ShopManagement/Index), Edit My Profile (DungeonMaster/EditProfile).
- Standalone (lines 87-107): Shop (88-92), Quest Log (93-97 — KEEP), Guild Members (98-102 — KEEP), Players (103-107).
- Calendar (lines 110-115): unconditional, with a `@* Calendar — available to all *@` comment, sitting OUTSIDE both the earlier IsAuthenticated block (closes ~line 108) and the later profile IsAuthenticated block (starts ~line 118).
- The Admin/Email Stats dropdown equivalent in mobile: locate the Email Stats mobile nav link (same AdminOnly region) and apply the same SuperAdmin gate.

SuperAdmin nav-link gate pattern already in use one line above Email Stats (Background Jobs):
```csharp
@if (User.IsInRole("SuperAdmin")) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate _Layout.cshtml (desktop) — OneShot allowlist, D-04 Calendar wrap, SuperAdmin Email Stats link</name>
  <files>QuestBoard.Service/Views/Shared/_Layout.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (full current file — you are editing in place; confirm exact line positions of each landmark before editing, as line numbers may have shifted)
    - QuestBoard.Domain/Interfaces/IActiveGroupContext.cs (the GetBoardTypeAsync member added by Plan 01)
    - QuestBoard.Service/Views/Shared/_ViewImports.cshtml (confirm which usings/injects are already global vs. need adding to this file)
    - .planning/phases/37-navigation-access-control/37-PATTERNS.md (the _Layout.cshtml section with exact current markup for each gated item)
  </read_first>
  <behavior>
    - Campaign active group: Calendar, Shop, Manage Shop, Edit My Profile, Players all absent from rendered desktop nav.
    - OneShot active group: all five items present (no regression).
    - No active group / anonymous: the five items absent (null BoardType → `== OneShot` false); Calendar additionally absent because logged out (D-04).
    - Guild Members and Quest Log present regardless of board type (D-05 — untouched).
    - Email Stats link present only when `User.IsInRole("SuperAdmin")`; absent for Admin-not-SuperAdmin.
  </behavior>
  <action>
    Add `@using QuestBoard.Domain.Enums` and `@inject IActiveGroupContext ActiveGroupContext` to the file header. Near the top of the nav render (before the first gated item), compute once: `@{ var activeBoardType = await ActiveGroupContext.GetBoardTypeAsync(); }` — reuse this single local for all gates; do NOT call GetBoardTypeAsync per item.

    Wrap each of these `<li>` blocks in `@if (activeBoardType == BoardType.OneShot) { ... }` — the allowlist shape (show only when confirmed OneShot), NOT the blocklist `!= BoardType.Campaign` (which would wrongly show items for null): Shop (Shop/Index), Players (Players/Index), Manage Shop (ShopManagement/Index, inside the existing DungeonMasterOnly dropdown), Edit My Profile (DungeonMaster/EditProfile, inside the DM dropdown). Leave Create Quest, Quest Log, and Guild Members UNGATED.

    Calendar (currently unconditional in the `ms-auto` group): nest its `<li>` inside BOTH `@if (User.Identity?.IsAuthenticated == true)` (D-04) AND `@if (activeBoardType == BoardType.OneShot)` (NAV-01). Combine into a single `&&` condition or nested ifs — functionally identical; nest IsAuthenticated outer to match the surrounding block structure. This effectively moves Calendar's markup into the same authenticated block as the profile dropdown.

    Email Stats link (currently ungated inside the AdminOnly dropdown): wrap its `<li>` in `@if (User.IsInRole("SuperAdmin")) { ... }` — the exact same guard already used one block above for "Background Jobs". Do NOT change the Admin dropdown's own outer AdminOnly gate.

    Do not add any GSD/phase/requirement IDs to Razor comments (per CLAUDE.md). Update or remove any now-inaccurate "available to all" comments so they don't lie about the new gating.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - _Layout.cshtml contains `@inject IActiveGroupContext ActiveGroupContext` and `@using QuestBoard.Domain.Enums`
    - _Layout.cshtml computes `await ActiveGroupContext.GetBoardTypeAsync()` exactly once (grep for `GetBoardTypeAsync` returns exactly 1 match)
    - _Layout.cshtml contains `activeBoardType == BoardType.OneShot` and does NOT contain `!= BoardType.Campaign` (grep confirms allowlist polarity)
    - The Calendar `<li>` is nested under both `User.Identity?.IsAuthenticated == true` and `activeBoardType == BoardType.OneShot`
    - The Email Stats `<li>` is wrapped in `@if (User.IsInRole("SuperAdmin"))`
    - Quest Log and Guild Members `<li>` blocks are NOT wrapped in any BoardType condition
    - `dotnet build QuestBoard.Service` exits 0
  </acceptance_criteria>
  <done>Desktop nav shows the five campaign-hidden items only for confirmed OneShot, hides Calendar for anonymous users, keeps Guild Members/Quest Log always visible, and shows Email Stats only to SuperAdmin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate _Layout.Mobile.cshtml — mirror desktop gating and turn LayoutNavigationTests green</name>
  <files>QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml (full current file — flat offcanvas list, no dropdowns; confirm exact line positions before editing)
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (the desktop version just edited in Task 1 — mirror its gating decisions exactly for behavioral parity)
    - .planning/phases/37-navigation-access-control/37-PATTERNS.md (the _Layout.Mobile.cshtml section with exact current markup)
  </read_first>
  <behavior>
    Identical visible behavior to the desktop layout for all board-type and role combinations: same five items hidden in Campaign, same items visible in OneShot, Calendar hidden for anonymous, Guild Members/Quest Log always visible, Email Stats link SuperAdmin-only. After this task, `LayoutNavigationTests` (Plan 01) is fully GREEN for both desktop and mobile user agents.
  </behavior>
  <action>
    Add `@using QuestBoard.Domain.Enums` and `@inject IActiveGroupContext ActiveGroupContext` to the header. Compute `@{ var activeBoardType = await ActiveGroupContext.GetBoardTypeAsync(); }` once near the top of the nav.

    Since the mobile layout is a flat `<li>` list (no nested dropdown), wrap each `<li>` directly in `@if (activeBoardType == BoardType.OneShot) { ... }`: Shop (Shop/Index), Players (Players/Index), Manage Shop (ShopManagement/Index, inside the DungeonMasterOnly block), Edit My Profile (DungeonMaster/EditProfile, inside the DM block). Leave Create Quest, Quest Log, and Guild Members ungated.

    Calendar (currently unconditional with the `@* Calendar — available to all *@` comment, structurally outside both IsAuthenticated blocks): wrap/move its `<li>` so it is gated by `User.Identity?.IsAuthenticated == true && activeBoardType == BoardType.OneShot`. Update or remove the now-inaccurate `@* Calendar — available to all *@` comment.

    Email Stats mobile link (in the AdminOnly region): wrap in `@if (User.IsInRole("SuperAdmin"))`, mirroring the desktop guard and the mobile Background Jobs guard if one exists.

    Do not add GSD/phase/requirement IDs to any comment.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~LayoutNavigationTests"</automated>
  </verify>
  <acceptance_criteria>
    - _Layout.Mobile.cshtml contains `@inject IActiveGroupContext ActiveGroupContext` and `@using QuestBoard.Domain.Enums`
    - _Layout.Mobile.cshtml computes `await ActiveGroupContext.GetBoardTypeAsync()` exactly once
    - _Layout.Mobile.cshtml contains `activeBoardType == BoardType.OneShot` and does NOT contain `!= BoardType.Campaign`
    - The Calendar `<li>` is gated by both `IsAuthenticated` and `activeBoardType == BoardType.OneShot`; the `@* Calendar — available to all *@` comment is removed or corrected
    - The Email Stats mobile `<li>` is wrapped in `@if (User.IsInRole("SuperAdmin"))`
    - `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~LayoutNavigationTests"` exits 0 — all NAV-01..06, OneShot regression, and D-04 tests GREEN
  </acceptance_criteria>
  <done>Mobile nav gating matches desktop exactly; the full LayoutNavigationTests suite (both user agents) passes, confirming NAV-01..06 and D-04 across desktop and mobile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify nav gating across board types, roles, and layouts</name>
  <what-built>
    Board-type-aware nav gating in both desktop and mobile layouts: campaign groups now hide Calendar, Shop, Manage Shop, Edit My Profile, and Players while keeping Guild Members and Quest Log; Calendar is hidden for logged-out visitors; the Email Stats link shows only to SuperAdmin. All automated LayoutNavigationTests are green.
  </what-built>
  <action>
    Present the running app to the user for manual verification. This is a pause point — do not proceed until the user approves. Automated coverage (LayoutNavigationTests) is already green; this checkpoint confirms the visual/interactive result across the OneShot/Campaign/anonymous/Admin/SuperAdmin matrix in a real browser, which the integration tests approximate but do not fully reproduce (dropdown expansion, offcanvas rendering).
  </action>
  <how-to-verify>
    1. Run `dotnet run --project QuestBoard.Service` and open the app in a desktop browser.
    2. Log in as a DM/player whose active group is a ONE-SHOT group. Confirm the nav shows Calendar, Shop, Manage Shop (DM dropdown), Edit My Profile (DM dropdown), Players, Guild Members, and Quest Log — i.e. exactly as before this phase.
    3. Switch active group to a CAMPAIGN group (or log in to one). Confirm Calendar, Shop, Manage Shop, Edit My Profile, and Players are all GONE, while Guild Members and Quest Log remain.
    4. Log out entirely. Confirm the Calendar link is NOT visible to the anonymous visitor (D-04).
    5. Narrow the browser to mobile width (or use device emulation) and open the mobile offcanvas nav. Repeat steps 2-4 — behavior must be identical to desktop.
    6. Log in as an Admin who is NOT a SuperAdmin. Confirm the "Email Stats" link is absent from the Admin dropdown (desktop) and mobile nav. Log in as a SuperAdmin and confirm "Email Stats" IS present.
  </how-to-verify>
  <verify>
    <human-check>User confirms all six nav-visibility checks pass across desktop and mobile.</human-check>
  </verify>
  <resume-signal>Type "approved" if all six checks pass, or describe which nav item is wrong (visible when it should be hidden, or vice versa) and in which layout/role.</resume-signal>
  <done>User has verified nav gating is correct for OneShot, Campaign, and anonymous states in both desktop and mobile layouts, and Email Stats visibility is SuperAdmin-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → SSR nav render | Nav visibility is derived server-side from the active group's BoardType and the user's roles. Nav hiding is COSMETIC only — it does not by itself block direct URL access to Calendar/Shop/etc. (out of scope per REQUIREMENTS.md; only ACCESS-01/EmailStats gets server-side enforcement, done in Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Elevation of Privilege | Nav hiding mistaken for an access-control boundary on Calendar/Shop/etc. | accept | Intentional per phase scope (RESEARCH.md Pitfall 5): NAV-01..06 are cosmetic; those controllers were never BoardType-gated and are out of scope to gate here. The one requirement needing real enforcement (ACCESS-01/EmailStats) is enforced server-side in Plan 02. |
| T-37-01 | Elevation of Privilege | Email Stats nav link visible to non-SuperAdmin | mitigate | Link gated on `User.IsInRole("SuperAdmin")` in both layouts; the authoritative server-side gate lives in Plan 02's controller attribute. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` exits 0.
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~LayoutNavigationTests"` exits 0 (all NAV + D-04 tests green).
- Both layouts use `activeBoardType == BoardType.OneShot` (allowlist), never `!= BoardType.Campaign` (grep both files).
- `grep asp-controller="Calendar"` in both layouts confirms Calendar sits inside an IsAuthenticated + OneShot gate.
- Human-verify checkpoint approved for desktop + mobile across OneShot/Campaign/anonymous/Admin/SuperAdmin.
</verification>

<success_criteria>
- Campaign groups hide Calendar, Shop, Manage Shop, Edit My Profile, Players in both layouts; Guild Members and Quest Log stay.
- One-shot groups render all nav items exactly as before (no regression).
- Calendar hidden for anonymous visitors in both layouts.
- Email Stats nav link renders only for SuperAdmin in both layouts.
- LayoutNavigationTests fully green; human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/37-navigation-access-control/37-03-SUMMARY.md` when done
</output>
