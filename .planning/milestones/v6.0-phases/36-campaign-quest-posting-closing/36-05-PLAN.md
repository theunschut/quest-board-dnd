---
phase: 36-campaign-quest-posting-closing
plan: 05
type: execute
wave: 4
depends_on: [36-03]
files_modified:
  - QuestBoard.Service/Views/QuestLog/Index.cshtml
  - QuestBoard.Service/Views/QuestLog/Index.Mobile.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml
autonomous: false
requirements: [CQUEST-05]
user_setup: []
must_haves:
  truths:
    - "A closed campaign quest appears as a Quest Log card immediately after closing, with the completed date shown from ClosedDate (not a blank/Unknown date)"
    - "Quest Log cards for campaign entries show no CR badge and no 'Adventurers: N' line"
    - "The Session Recap (add/edit + badge) works for closed campaign quests exactly as for one-shot"
    - "One-shot Quest Log cards render exactly as before (CR badge, Adventurers count, FinalizedDate)"
  artifacts:
    - path: "QuestBoard.Service/Views/QuestLog/Index.cshtml"
      provides: "BoardType-conditional CR-badge + Adventurers-line removal; ClosedDate fallback for the completed date"
      contains: "BoardType"
    - path: "QuestBoard.Service/Views/QuestLog/Details.cshtml"
      provides: "BoardType-conditional CR/Adventurers removal; Recap UI unchanged; ClosedDate display"
      contains: "BoardType"
  key_links:
    - from: "QuestLog/Index.cshtml card"
      to: "ViewBag.BoardType (from QuestLogController.Index)"
      via: "@if (boardType != BoardType.Campaign) around CR badge + Adventurers meta-item; ClosedDate ?? FinalizedDate for the date"
      pattern: "BoardType.Campaign"
    - from: "QuestLog/Details.cshtml"
      to: "Quest.Recap / UpdateRecap"
      via: "existing recap add/edit UI continues to render for closed campaign quests"
      pattern: "Recap"
---

<objective>
Apply the campaign simplification to the Quest Log views (desktop + mobile): drop the CR badge and "Adventurers: N" line for campaign-closed entries, show the completed date from `ClosedDate` (with `FinalizedDate` fallback for one-shot), and confirm the Session Recap UI keeps working for closed campaign quests — all gated on `ViewBag.BoardType`, one-shot untouched.

Purpose: This is the Quest Log display side of CQUEST-05. Plan 02/03 already made a closed campaign quest appear in `GetCompletedQuestsAsync` immediately (no next-day wait) and fixed the `QuestLogController.Details`/`UpdateRecap` guards to admit closed quests. This plan makes those entries render cleanly: no CR badge, no signup-count line (D-07), and a real completed date (a campaign quest has `FinalizedDate == null`, so the existing `quest.FinalizedDate?.ToString(...) ?? "Unknown Date"` would show "Unknown Date" — it must fall back to `ClosedDate`). Recap (D-08) applies to campaign quests identically and requires no change beyond confirming the existing markup still renders under the new guards.
Output: Four edited Quest Log Razor views and a human-verify checkpoint confirming the campaign Quest Log entry + recap flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-campaign-quest-posting-closing/36-UI-SPEC.md
@.planning/phases/36-campaign-quest-posting-closing/36-PATTERNS.md

<interfaces>
<!-- ViewBag.BoardType is set by QuestLogController.Index/Details (Plan 03). BoardType enum: OneShot=0, Campaign=1. -->
<!-- Views must add @using QuestBoard.Domain.Enums and read (BoardType)ViewBag.BoardType. -->

<!-- QuestLog/Index.cshtml current card markup -->
<!--   completed date (~19-20): var questDate = quest.FinalizedDate?.ToString("MMMM dd, yyyy") ?? "Unknown Date"; -->
<!--   CR badge (~27-30): <span class="cr-badge"><i class="fas fa-dice-d20 me-1"></i> CR @quest.ChallengeRating</span> -->
<!--   Adventurers meta-item (~45-48): <div class="meta-item">...<strong>Adventurers:</strong> @selectedPlayers.Count</div> -->
<!--   Recap badge (~64-69): renders when !string.IsNullOrWhiteSpace(quest.Recap) — leave as-is -->
<!--   Empty state (~78-84): "No Completed Quests Yet" — no copy change needed per UI-SPEC -->

<!-- QuestLog/Details.cshtml: CR badge + Adventurers list + Recap add/edit UI. Recap UI unchanged; CR/Adventurers wrapped for campaign. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quest Log Index + Details campaign simplification (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/QuestLog/Index.cshtml, QuestBoard.Service/Views/QuestLog/Index.Mobile.cshtml, QuestBoard.Service/Views/QuestLog/Details.cshtml, QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/QuestLog/Index.cshtml (completed-date var ~19-20; CR badge ~27-30; Adventurers meta-item ~45-48; recap badge ~64-69; empty state ~78-84)
    - QuestBoard.Service/Views/QuestLog/Index.Mobile.cshtml (equivalent card markup)
    - QuestBoard.Service/Views/QuestLog/Details.cshtml (CR badge, Adventurers/players list, Recap add/edit UI)
    - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml (equivalent sections)
    - .planning/phases/36-campaign-quest-posting-closing/36-UI-SPEC.md (Additional Contract Notes D-07; Copywriting Contract — no empty-state change, no new error state)
  </read_first>
  <action>
    In `QuestLog/Index.cshtml`: add `@using QuestBoard.Domain.Enums` at the top if not present and capture `var boardType = (BoardType)ViewBag.BoardType;`. Change the completed-date computation so campaign entries use `ClosedDate`: `var questDate = (quest.FinalizedDate ?? quest.ClosedDate)?.ToString("MMMM dd, yyyy") ?? "Unknown Date";` (a one-shot quest has `FinalizedDate`; a campaign quest has only `ClosedDate` — this yields a real date for both and preserves one-shot output exactly since `FinalizedDate` is non-null there). Wrap the CR-badge `<span class="cr-badge">...</span>` (~27-30) in `@if (boardType != BoardType.Campaign) { ... }` and wrap the Adventurers `.meta-item` (~45-48) in `@if (boardType != BoardType.Campaign) { ... }` (D-07 — full removal; `.quest-log-meta` reflows via its flex-column gap). Leave the recap badge, description preview, footer, and empty state unchanged (per UI-SPEC: no empty-state copy change).

    In `QuestLog/Details.cshtml`: capture `boardType` the same way; wrap the CR badge and the "Adventurers"/selected-players list section in `@if (boardType != BoardType.Campaign) { ... }`. Do NOT alter the Session Recap add/edit UI or its badge — it must continue to render for closed campaign quests (D-08). If Details displays the completed/finalized date, apply the same `(quest.FinalizedDate ?? quest.ClosedDate)` fallback.

    Apply the mirror of each change to `QuestLog/Index.Mobile.cshtml` and `QuestLog/Details.Mobile.cshtml`.

    Do not add or modify any CSS. Do not embed GSD phase/requirement IDs in comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `QuestLog/Index.cshtml` references `(BoardType)ViewBag.BoardType`; the completed-date expression uses `(quest.FinalizedDate ?? quest.ClosedDate)`.
    - The CR-badge span and the Adventurers `.meta-item` in `QuestLog/Index.cshtml` are each wrapped in `@if (boardType != BoardType.Campaign)`.
    - `QuestLog/Details.cshtml` wraps its CR badge and selected-players/Adventurers section in `@if (boardType != BoardType.Campaign)`; the Session Recap add/edit UI is NOT wrapped/removed.
    - The empty-state block in `QuestLog/Index.cshtml` is unchanged.
    - `QuestLog/Index.Mobile.cshtml` and `QuestLog/Details.Mobile.cshtml` carry the equivalent conditional changes and the ClosedDate fallback.
    - No changes to any `.css` file.
    - `QuestBoard.Service` builds with no errors.
    - No comment/string literal in edited views contains `CQUEST`, `Phase 36`, or `36-0`.
  </acceptance_criteria>
  <done>Quest Log Index + Details (desktop + mobile) drop CR/Adventurers for campaign, show the ClosedDate for campaign entries, keep Recap working, and leave one-shot rendering unchanged; Service builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify campaign Quest Log entry + recap flow</name>
  <files>QuestBoard.Service/Views/QuestLog/Index.cshtml, QuestBoard.Service/Views/QuestLog/Details.cshtml (+ .Mobile variants)</files>
  <action>Automation is complete (Task 1). Pause and hand off to the human for visual/interaction verification of the campaign Quest Log entry and recap flow on desktop and mobile. Do not modify code in this task — run the app and present the verification steps below, then wait for the resume signal.</action>
  <what-built>
    Quest Log Index and Details views (desktop + mobile) now render campaign-closed entries with no CR badge, no "Adventurers: N" line, and the completed date sourced from ClosedDate. The Session Recap add/edit flow works for closed campaign quests. One-shot Quest Log rendering is unchanged.
  </what-built>
  <how-to-verify>
    1. In a campaign group as a DM, close a campaign quest (Manage → "Close Quest").
    2. Immediately visit the Quest Log — confirm the just-closed quest appears right away (no next-day wait), its card shows NO "CR N" badge and NO "Adventurers: N" line, and the "Completed:" date shows today's date (not "Unknown Date").
    3. Click the quest to open its Quest Log Details — confirm NO CR badge and NO selected-players/Adventurers list, and that the Session Recap area is present.
    4. As the owning DM (or a group Admin), add a Session Recap, save it, and confirm it persists and the "Session Recap Available" badge appears on the Quest Log card.
    5. Reopen the quest from Manage, then confirm it disappears from the Quest Log (it is active again).
    6. Repeat steps 2-4 on a phone-width viewport (or mobile UA) to confirm the `.Mobile` variants match.
    7. Switch to a one-shot group and confirm its Quest Log cards still show the CR badge, the Adventurers count, and the FinalizedDate (no regression).
  </how-to-verify>
  <verify>
    <human-check>DM confirms all seven verification steps pass on both desktop and mobile widths, and that the one-shot Quest Log is unregressed.</human-check>
  </verify>
  <done>Human verified the campaign Quest Log card (no CR, no Adventurers line, ClosedDate shown, appears immediately), the recap add/edit flow on a closed campaign quest, and reopen removal from the log on desktop and mobile, confirmed one-shot is unregressed, and typed "approved".</done>
  <resume-signal>Type "approved" or describe any visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-rendered Razor → browser | Presentational only; BoardType from server-set ViewBag, no client input |
| Recap add/edit form → QuestLogController.UpdateRecap | Form POST crosses to an authorized action (authz + CSRF enforced server-side in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10 | Tampering / Spoofing | Recap update form (reused, unchanged) | mitigate | Existing recap form retains `@Html.AntiForgeryToken()`; `UpdateRecap` action carries `[ValidateAntiForgeryToken]` + `[Authorize(Policy="DungeonMasterOnly")]` + owner-or-admin guard (Plan 03) |
| T-36-11 | Information Disclosure | Quest Log campaign card rendering | accept | Removals are cosmetic (`@if` wraps); the ClosedDate fallback surfaces only the quest's own close date, already visible to any group member. No new data exposed. Low risk. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Razor + Bootstrap only, no new assets or CSS. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` — Razor views compile.
- `dotnet test` — full suite green (no server-side regression from view changes).
- Human-verify checkpoint approved (campaign Quest Log + recap, desktop + mobile; one-shot no regression).
</verification>

<success_criteria>
- Campaign-closed Quest Log entries show immediately with no CR/Adventurers line and a real completed date from ClosedDate.
- Session Recap works for closed campaign quests.
- One-shot Quest Log unchanged; no new CSS or views.
- Human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/36-campaign-quest-posting-closing/36-05-SUMMARY.md` when done.
</output>
