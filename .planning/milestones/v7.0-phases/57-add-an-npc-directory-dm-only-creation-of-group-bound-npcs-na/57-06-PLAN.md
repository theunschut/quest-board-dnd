---
phase: 57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na
plan: 06
type: execute
wave: 5
depends_on: ["57-05"]
files_modified:
  - QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Create.Mobile.cshtml
  - QuestBoard.Service/wwwroot/css/contacts.mobile.css
  - QuestBoard.Service/wwwroot/css/contact-detail.mobile.css
  - QuestBoard.Service/wwwroot/css/contact-form.mobile.css
autonomous: false
requirements: []
must_haves:
  truths:
    - "Four mobile views (Index/Details/Edit/Create.Mobile.cshtml) exist and render the same capabilities as their desktop counterparts"
    - "The mobile view expander serves the .Mobile.cshtml variants automatically on mobile requests"
    - "A human has verified the full Contacts feature (desktop + mobile) works end to end"
  artifacts:
    - path: "QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml"
      provides: "Mobile Contacts list view"
      min_lines: 25
    - path: "QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml"
      provides: "Mobile Contact detail incl. notes UI"
      min_lines: 50
  key_links:
    - from: "QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml"
      to: "QuestBoard.Service/ViewExpanders/MobileViewLocationExpander.cs"
      via: "the .Mobile.cshtml suffix is auto-selected on mobile requests"
      pattern: "\\.Mobile\\.cshtml"
---

<objective>
Build the four mobile Razor views for Contacts (D-18 full mobile parity) plus their per-view mobile stylesheets, then run a human verification checkpoint confirming the entire Contacts feature works desktop + mobile.

Purpose: This app has repeatedly had to fix mobile-parity gaps as separate bug-fix phases (Phase 43, Phase 54); D-18 explicitly ships mobile in this phase to avoid that. The `.Mobile.cshtml` files are auto-selected by the existing MobileViewLocationExpander — no controller changes. Mobile views carry the SAME UI-SPEC copy strings, button styles, and interaction mechanics as their desktop counterparts (57-UI-SPEC.md "Mobile views: full parity per D-18, same responsive breakpoint `@media (max-width: 768px)`") — the Show Hidden toggle two-state spec, the Hidden badge, the Reveal/Hide action, and the Notes list (add-note `btn-warning` "Add Note", inline edit-in-place, native-confirm delete) all reproduce verbatim on mobile.
Output: Four mobile views + three mobile stylesheets + a passed human verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-VALIDATION.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md
@QuestBoard.Service/Views/Characters/Index.Mobile.cshtml
@QuestBoard.Service/Views/Characters/Details.Mobile.cshtml
@QuestBoard.Service/Views/Characters/Edit.Mobile.cshtml
@QuestBoard.Service/Views/Characters/Create.Mobile.cshtml
@QuestBoard.Service/Views/Contacts/Index.cshtml
@QuestBoard.Service/Views/Contacts/Details.cshtml
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's slice)

Mobile stylesheets (mirroring the Characters mobile CSS trio, linked per-view via `@section Styles`):
- `contacts.mobile.css` — mirrors `characters.mobile.css` (glass section card, tappable list rows, mobile Show Hidden toggle + Hidden badge sizing), for `Index.Mobile.cshtml`
- `contact-detail.mobile.css` — mirrors `character-detail.mobile.css` (stacked portrait/actions/notes card mobile layout), for `Details.Mobile.cshtml`
- `contact-form.mobile.css` — mirrors `character-form.mobile.css` (mobile form field/button styling), for `Edit.Mobile.cshtml`/`Create.Mobile.cshtml`

Mobile views (in `QuestBoard.Service/Views/Contacts/`, auto-selected by MobileViewLocationExpander):
- `Index.Mobile.cshtml` — mobile flat alphabetical list + "+ Contact" `btn-warning` (DM-tier) + two-state Show Hidden toggle (DM-tier, `btn-outline-secondary`/"Show Hidden" ↔ `btn-secondary`/"Hide Hidden") + secondary Hidden badges
- `Details.Mobile.cshtml` — mobile stacked portrait/actions/description/notes cards incl. the Reveal (`btn-success`)/Hide (`btn-secondary`) action and the collaborative Notes UI (add-note `btn-warning` "Add Note", inline edit-in-place, native-confirm "Delete this note? This action cannot be undone.")
- `Edit.Mobile.cshtml` — mobile core-field form (no notes), "Edit {name}" h4 header
- `Create.Mobile.cshtml` — mobile core-field form, "Create New Contact" h4 header

No controller/expander changes — the `.Mobile.cshtml` suffix is resolved by the existing `MobileViewLocationExpander`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create the three mobile stylesheets and the four mobile Contact views</name>
  <files>QuestBoard.Service/wwwroot/css/contacts.mobile.css, QuestBoard.Service/wwwroot/css/contact-detail.mobile.css, QuestBoard.Service/wwwroot/css/contact-form.mobile.css, QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Create.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Characters/Index.Mobile.cshtml, Details.Mobile.cshtml, Edit.Mobile.cshtml, Create.Mobile.cshtml (exact mobile analogs — the `@section Styles { <link ...> }` per-view CSS wiring, stacked-card mobile layout, glass `character-section-card`/tappable `character-member-row` patterns, mobile spacing/button conventions)
    - QuestBoard.Service/wwwroot/css/characters.mobile.css, character-detail.mobile.css, character-form.mobile.css (the exact mobile CSS to mirror — glass section card, parchment headings, tappable rows, mobile form styling; copy the applicable rules renamed for Contacts)
    - QuestBoard.Service/Views/Contacts/Index.cshtml, Details.cshtml, Edit.cshtml, Create.cshtml (the desktop Contact views from Plan 05 — same ViewModels, same actions, same exact UI-SPEC toggle/badge/notes/action behavior to reproduce for mobile)
    - QuestBoard.Service/ViewExpanders/MobileViewLocationExpander.cs (confirms .Mobile.cshtml is tried first on mobile requests, falling back to the desktop view — no controller branching needed)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md ("Mobile views" note under Mirrored Patterns — full parity, same `@media (max-width: 768px)` breakpoint, single-column grid collapse; and ALL 4 New Pattern Specs + Copywriting Contract apply identically to mobile — same button styles, same copy strings, same inline-edit/native-confirm mechanics)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md (D-18 full mobile parity; D-20 layouts apply to mobile too; D-09 collaborative notes ungated)
    - CLAUDE.md (modern-card UI conventions apply to mobile views)
  </read_first>
  <action>
    Create the three mobile stylesheets by mirroring the Characters mobile CSS trio, renaming the class selectors for Contacts (`character-*` → `contact-*` where a rule is Contact-relevant): `contacts.mobile.css` from `characters.mobile.css` (glass section card, tappable list row with divider, mobile Show Hidden toggle + Hidden badge sizing); `contact-detail.mobile.css` from `character-detail.mobile.css` (stacked portrait/actions/notes card mobile layout — add a `.note-item` mobile rule if needed); `contact-form.mobile.css` from `character-form.mobile.css` (mobile form field/button styling). These files carry no `@media` queries (they are loaded exclusively by the `.Mobile.cshtml` views, exactly like their Character analogs).
    Create the four `.Mobile.cshtml` views, each opening with the same `@section Styles { <link href="~/css/{corresponding}.mobile.css" asp-append-version="true" rel="stylesheet" /> }` wiring the Character mobile views use. Reproduce EVERY desktop capability and the EXACT UI-SPEC spec on mobile:
    - `Index.Mobile.cshtml` (links `contacts.mobile.css`): flat alphabetical list (thumbnail + name + town/city per row → `Details/{id}`); when `Model.ViewerIsDmTier`, the two-state Show Hidden toggle (POST to `ToggleShowHidden`: `btn-outline-secondary`/`fa-eye-slash`/"Show Hidden"/`aria-pressed="false"` when OFF, `btn-secondary`/`fa-eye`/"Hide Hidden"/`aria-pressed="true"` when ON) and the "+ Contact" `btn-warning`/`fa-plus` button; on `IsRevealed == false` rows a secondary "Hidden" badge (`fa-eye-slash` + "Hidden").
    - `Details.Mobile.cshtml` (links `contact-detail.mobile.css`): stacked portrait card (image via `GetContactImage/{id}` or `fa-user` placeholder, name, town/city, sub-location, `badge bg-secondary` "Hidden" when hidden), Actions card gated on `Model.CanManage` (Edit `btn-warning`; Reveal `btn-success`/"Reveal Contact" or Hide `btn-secondary`/"Hide Contact" POST to `ToggleReveal`, no confirm; Delete `btn-danger` POST with the exact confirm string "Are you sure you want to delete this contact? This action cannot be undone."), Description card, Notes card (add-note `textarea` `placeholder="Add a note about this contact..."` + `btn-warning`/"Add Note", `<hr>`, newest-first `note-item` list with per-note `btn-outline-secondary` edit / `btn-outline-danger` delete icon buttons and inline edit-in-place, delete guarded by `confirm('Delete this note? This action cannot be undone.')`, "No Notes Yet" empty state) — the add-note form and per-note controls are available to ALL viewers, NOT gated on CanManage (D-09). Back to Contacts link.
    - `Edit.Mobile.cshtml` / `Create.Mobile.cshtml` (link `contact-form.mobile.css`): core-field forms (Name/Image/Description/TownCity/SubLocation), h4 headers ("Edit @Model.Name" / "Create New Contact"), `btn-warning` submit / `btn-secondary` cancel, reused image-validation script — NO notes UI.
    Keep antiforgery tokens on every POST form. Do not change any controller or the MobileViewLocationExpander. Use CRLF line endings. No phase/decision IDs in markup/comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service; dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the three mobile CSS files exist and mirror their Character analogs' rules under Contact-named selectors; each mobile view links its corresponding stylesheet via `@section Styles`.
    - Source assertion: all four `Views/Contacts/*.Mobile.cshtml` files exist and are typed to the same ViewModels as their desktop counterparts.
    - Source assertion: `Index.Mobile.cshtml` renders the two-state Show Hidden toggle (`btn-outline-secondary`/"Show Hidden" ↔ `btn-secondary`/"Hide Hidden") and the `btn-warning` "+ Contact" button, both guarded on `Model.ViewerIsDmTier`, plus a secondary "Hidden" badge on unrevealed rows.
    - Source assertion: `Details.Mobile.cshtml` contains the add-note form (`AddNote`, `placeholder="Add a note about this contact..."`, `btn-warning` "Add Note") and per-note `EditNote`/`DeleteNote` controls NOT gated on CanManage, an Actions block gated on CanManage with the `ToggleReveal` `btn-success`/`btn-secondary` action, and the exact contact + note delete-confirm strings.
    - `dotnet build QuestBoard.Service` succeeds and full `dotnet test` remains green.
  </acceptance_criteria>
  <done>The three mobile stylesheets and four mobile Contact views exist, reproduce every desktop capability with UI-SPEC fidelity in the mobile stacked-card layout, and the solution builds with the full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the full Contacts feature (desktop + mobile)</name>
  <what-built>
    The complete Contacts feature: 3 entities + migration, domain/repository/service layers, ContactsController, ViewModels, 8 views (4 desktop + 4 mobile), a "Contacts" nav link for all users on both board types, the per-group Show Hidden session toggle, the three-branch hidden-Contact visibility rule, and the collaborative notes UI on Details.
  </what-built>
  <how-to-verify>
    Run the app (`dotnet run --project QuestBoard.Service`) and, with the SQL Server running, let migrations auto-apply on startup. Then verify:
    1. Nav: the "Contacts" link appears in the top nav for a plain Player and for a DM/Admin, on BOTH a One-Shot and a Campaign group (D-19).
    2. Create (DM-tier): create a Contact with name, image (JPG/PNG), description, town/city, sub-location. Confirm it saves and defaults to Hidden (D-14). Confirm a Player has no "+ Contact" button (D-09b/D-20).
    3. Hidden visibility (D-12/D-13/D-15): as a Player, confirm the new hidden Contact does NOT appear in the Index and that visiting its Details URL directly returns Not Found (404). As the DM who created it, confirm it always shows (creator exception). As a *different* DM-tier user, confirm it is hidden until you click "Show Hidden", then appears with a muted Hidden badge; toggle off and confirm it hides again.
    4. Per-group toggle (D-15b): confirm Show Hidden ON in Group 1 does not carry over to Group 2 (each group tracks its own toggle); confirm a fresh login/session resets it to OFF.
    5. Reveal: as DM-tier, use the Reveal toggle on Details; confirm the Contact now appears for a Player.
    6. Notes (D-08/D-09/D-10/D-16): as a Player, add a note on Details; confirm it appears newest-first with your name and timestamp; edit and delete a note authored by a *different* user and confirm it succeeds (fully collaborative).
    7. Edit/Delete (DM-tier): edit core fields; confirm no notes editing on the Edit page (notes stay on Details). Delete a Contact and confirm its notes/image are removed.
    8. Mobile: repeat steps 1-7 on a mobile viewport (devtools emulation acceptable per 57-VALIDATION.md — no touch-gesture/camera concerns in this phase); confirm all four mobile views render the modern-card layout, the toggle, badges, and notes section, matching desktop behavior (D-18).
    9. Image validation (D-06): attempt to upload a non-image file or a >5MB image and confirm it is rejected with a validation message.
    10. UI-SPEC conformance (57-UI-SPEC.md): confirm the exact copy and styles — the create button reads "+ Contact" (`btn-warning`); the Show Hidden toggle reads "Show Hidden" (outline, eye-slash) when off and "Hide Hidden" (filled gray, eye) when on; the Hidden badge is muted secondary-gray (not dark/black); the Details Actions card shows "Reveal Contact" (green) when hidden and "Hide Contact" (gray) when revealed; the notes add button reads "Add Note" (`btn-warning`); per-note edit/delete are small outline icon buttons; the delete-contact confirm reads "Are you sure you want to delete this contact? This action cannot be undone." and the delete-note confirm reads "Delete this note? This action cannot be undone."; note edit is inline edit-in-place (no page navigation, no modal); empty states read "No Contacts Yet" / "No Notes Yet".
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any issues (which page, which step, what happened vs. expected) so they can be fixed before the phase closes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile view → controller | Mobile views hit the same controller/authorization as desktop; parity must not open a weaker path |
| human verification → phase closure | Manual verification confirms the visibility/session behaviors that are impractical to fully automate (session-expiry reset, mobile layout) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-17 | Information Disclosure | Mobile view leaking hidden Contacts/toggle to non-DM viewers | mitigate | Mobile views reuse the same ViewerIsDmTier/CanManage/model-filtering as desktop; human verify step 3/8 confirms parity |
| T-57-18 | Elevation of Privilege | Mobile Edit/Delete/Reveal affordances shown to Players | mitigate | Actions gated on CanManage in mobile views; controller policy enforces (Plan 04); verified in step 8 |
| T-57-19 | Session Management | Per-group toggle / session-expiry reset behaving incorrectly | mitigate | Human verify steps 4 covers per-group isolation + fresh-login reset (D-15b, manual-only per 57-VALIDATION.md) |
| T-57-SC | Tampering | npm installs | accept | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- All four mobile views + three mobile stylesheets build; full `dotnet test` green.
- MobileViewLocationExpander serves the `.Mobile.cshtml` variants (no controller change); each mobile view links its corresponding mobile stylesheet via `@section Styles`.
- Mobile views carry the same UI-SPEC copy strings, button styles, and interaction mechanics as desktop (4 new patterns).
- Human verification checkpoint passes (all 10 steps, desktop + mobile, including UI-SPEC conformance).
</verification>

<success_criteria>
- Four mobile Contact views + three mobile stylesheets exist with full desktop parity and UI-SPEC fidelity (D-18).
- The human verifier confirms nav visibility, create/default-hidden, three-branch visibility, per-group toggle + session reset, reveal, collaborative notes, edit/delete, mobile parity, image validation, and UI-SPEC copy/style conformance.
</success_criteria>

<output>
Create `.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-06-SUMMARY.md` when done
</output>
