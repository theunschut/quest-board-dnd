---
phase: 57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Repository/Entities/ContactEntity.cs
  - QuestBoard.Repository/Entities/ContactImageEntity.cs
  - QuestBoard.Repository/Entities/ContactNoteEntity.cs
  - QuestBoard.Repository/Entities/QuestBoardContext.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Three new EF Core entities exist: ContactEntity (name/image/description/town/sub-location/IsRevealed/CreatedByUserId/Notes), ContactImageEntity (1:1 FK-as-PK), ContactNoteEntity (author + timestamp + text)"
    - "All three entities have a fail-closed group-scoped HasQueryFilter with no SuperAdmin cross-group bypass"
    - "An EF Core migration AddContactsFeature exists creating Contacts, ContactImages, ContactNotes tables"
  artifacts:
    - path: "QuestBoard.Repository/Entities/ContactEntity.cs"
      provides: "Contact EF entity"
      contains: "class ContactEntity"
    - path: "QuestBoard.Repository/Entities/ContactImageEntity.cs"
      provides: "1:1 image table"
      contains: "class ContactImageEntity"
    - path: "QuestBoard.Repository/Entities/ContactNoteEntity.cs"
      provides: "authored+timestamped note child table"
      contains: "class ContactNoteEntity"
  key_links:
    - from: "QuestBoard.Repository/Entities/QuestBoardContext.cs"
      to: "QuestBoard.Repository/Entities/ContactEntity.cs"
      via: "DbSet<ContactEntity> + fail-closed HasQueryFilter on GroupId"
      pattern: "HasQueryFilter"
    - from: "QuestBoard.Repository/Entities/ContactNoteEntity.cs"
      to: "QuestBoard.Repository/Entities/ContactEntity.cs"
      via: "ContactId FK, cascade delete; AuthorUserId FK to UserEntity, NoAction"
      pattern: "HasForeignKey"
---

<objective>
Build the data layer for Contacts: three EF Core entities mirroring the Character entity family, their query-filter registrations in `QuestBoardContext`, and one migration creating all three tables.

Purpose: This is the foundation every later plan depends on. All type names follow the locked "Contact"/"Contacts" feature name (D-01) and this app's `[Feature]Entity` convention (D-02) — `ContactEntity`/`ContactImageEntity`/`ContactNoteEntity`, never "NPC". The fail-closed group-scoped query filter is the multi-tenancy security boundary (Phase 55 precedent); the Notes child-collection is the only novel entity shape in the phase.
Output: `ContactEntity`, `ContactImageEntity`, `ContactNoteEntity`, updated `QuestBoardContext`, and the `AddContactsFeature` migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md
@QuestBoard.Repository/Entities/CharacterEntity.cs
@QuestBoard.Repository/Entities/CharacterImageEntity.cs
@QuestBoard.Repository/Entities/CharacterClassEntity.cs
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's slice)

Entities:
- `ContactEntity` (table `Contacts`) — Id, Name (`StringLength(100)`, required), Description (`StringLength(2000)`, optional), TownCity (`StringLength(200)`, optional), SubLocation (`StringLength(200)`, optional), IsRevealed (bool, default false), CreatedByUserId (int FK to UserEntity) + CreatedByUser nav, CreatedAt, GroupId (int FK to GroupEntity) + Group nav, ProfileImage (nav to ContactImageEntity), Notes (`ICollection<ContactNoteEntity>`)
- `ContactImageEntity` (table `ContactImages`) — Id (PK = FK to Contact), ImageData (`byte[]`), Contact nav — 1:1 FK-as-PK
- `ContactNoteEntity` (table `ContactNotes`) — Id, ContactId (FK) + Contact nav, Text (`StringLength(2000)`, required), AuthorUserId (FK to UserEntity) + Author nav, CreatedAt, UpdatedAt (nullable)

Context registrations (in `QuestBoardContext.OnModelCreating`):
- Fail-closed `HasQueryFilter` on ContactEntity (GroupId), ContactImageEntity (via Contact.GroupId), ContactNoteEntity (via Contact.GroupId) — NO SuperAdmin bypass
- `HasMany(c => c.Notes).WithOne(n => n.Contact).HasForeignKey(n => n.ContactId).OnDelete(Cascade)`
- `HasOne(n => n.Author).WithMany().HasForeignKey(n => n.AuthorUserId).OnDelete(NoAction)`

Migration:
- `AddContactsFeature` (creates Contacts, ContactImages, ContactNotes tables)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactEntity, ContactImageEntity, ContactNoteEntity</name>
  <files>QuestBoard.Repository/Entities/ContactEntity.cs, QuestBoard.Repository/Entities/ContactImageEntity.cs, QuestBoard.Repository/Entities/ContactNoteEntity.cs</files>
  <read_first>
    - QuestBoard.Repository/Entities/CharacterEntity.cs (exact analog for ContactEntity — mirror StringLength/FK/nav conventions; IEntity interface)
    - QuestBoard.Repository/Entities/CharacterImageEntity.cs (exact analog for ContactImageEntity — 1:1 FK-as-PK shape, copy verbatim renamed)
    - QuestBoard.Repository/Entities/CharacterClassEntity.cs (child-FK collection shape informing ContactNoteEntity)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md (Pattern Assignments: ContactEntity/ContactImageEntity/ContactNoteEntity full code blocks)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md (Pattern 3: ContactNoteEntity composite design)
  </read_first>
  <action>
    Create `ContactEntity` (`[Table("Contacts")]`, implements `IEntity`) mirroring `CharacterEntity`: Id (identity PK); Name (`[Required]`, `[StringLength(100)]`); Description (`[StringLength(2000)]`, nullable, per D-05); TownCity (`[StringLength(200)]`, nullable, no `[Required]`, per D-04); SubLocation (`[StringLength(200)]`, nullable, per D-03); `IsRevealed` (bool, default `false`, per D-14); `CreatedByUserId` (int) + `[ForeignKey]` `CreatedByUser` nav to `UserEntity` (per D-07 — name it CreatedByUserId, NOT OwnerId, to avoid implying an ownership/edit guard); CreatedAt (`DateTime`, default `DateTime.UtcNow`); `ProfileImage` (nullable nav to `ContactImageEntity`); GroupId (int) + `[ForeignKey]` `Group` nav to `GroupEntity`; `Notes` (`virtual ICollection<ContactNoteEntity>` initialized `= [];`). Do NOT copy Character's Level/SheetLink/Backstory/Status/Role/Classes fields.
    Create `ContactImageEntity` (`[Table("ContactImages")]`, implements `IEntity`) verbatim-renamed from `CharacterImageEntity`: Id as `[Key]` + `[ForeignKey(nameof(Contact))]` (1:1 FK-as-PK), ImageData (`byte[]`, `[Required]`, `= [];`), Contact nav.
    Create `ContactNoteEntity` (`[Table("ContactNotes")]`, implements `IEntity`) per RESEARCH.md Pattern 3: Id (identity PK); ContactId (`[Required]`) + `[ForeignKey]` Contact nav; Text (`[Required]`, `[StringLength(2000)]`, per D-11); AuthorUserId (`[Required]`) + `[ForeignKey]` Author nav to `UserEntity`; CreatedAt (`DateTime`, default `DateTime.UtcNow`); UpdatedAt (`DateTime?` nullable).
    Use CRLF line endings and match the file header/using/namespace style of the Character entity files exactly. Do NOT add any GSD phase/decision IDs in comments (CLAUDE.md rule).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ContactEntity` declares `IsRevealed` (bool), `CreatedByUserId` (int, NOT named OwnerId), `TownCity` and `SubLocation` (both nullable `[StringLength(200)]`), and `Notes` collection; it does NOT declare Level/SheetLink/Backstory/Status/Role.
    - Source assertion: `ContactImageEntity.Id` carries both `[Key]` and `[ForeignKey(nameof(Contact))]` (1:1 FK-as-PK).
    - Source assertion: `ContactNoteEntity` declares `Text` (`[StringLength(2000)]`), `AuthorUserId` FK, `CreatedAt`, and `UpdatedAt` (nullable).
    - `dotnet build QuestBoard.Repository` succeeds.
  </acceptance_criteria>
  <done>All three entity classes exist, implement IEntity, follow Character entity conventions, and the Repository project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Register entities + fail-closed query filters + relationships in QuestBoardContext</name>
  <files>QuestBoard.Repository/Entities/QuestBoardContext.cs</files>
  <read_first>
    - QuestBoard.Repository/Entities/QuestBoardContext.cs (existing CharacterEntity/CharacterImageEntity/CharacterClassEntity filter block ~lines 297-318; the ActiveGroupId local-capture warning comment ~lines 251-252; the QuestEntity.DungeonMasterId NoAction FK config)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md (QuestBoardContext.cs section: exact filter block + insertion point + NoAction warning)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md (Pattern 1 fail-closed filter; Pattern 3 relationship registration; Anti-Patterns: no local-variable capture, no SuperAdmin bypass)
  </read_first>
  <action>
    Add `DbSet<ContactEntity> Contacts`, `DbSet<ContactImageEntity> ContactImages`, `DbSet<ContactNoteEntity> ContactNotes` to `QuestBoardContext`.
    In `OnModelCreating`, immediately after the existing `CharacterImageEntity` filter block, add fail-closed `HasQueryFilter` registrations for all three entities: `ContactEntity` filtered on `activeGroupContext.ActiveGroupId != null && e.GroupId == activeGroupContext.ActiveGroupId`; `ContactImageEntity` filtered via `ci.Contact.GroupId`; `ContactNoteEntity` filtered via `cn.Contact.GroupId`. Reference the `activeGroupContext` service instance INSIDE each lambda — do NOT capture `ActiveGroupId` into a local variable (existing documented pitfall). Do NOT add a SuperAdmin cross-group bypass — Contacts is a per-group roster shape like Characters, not a cross-group platform-admin shape.
    Register relationships: `HasMany(c => c.Notes).WithOne(n => n.Contact).HasForeignKey(n => n.ContactId).OnDelete(DeleteBehavior.Cascade)` (deleting a Contact deletes its notes); `HasOne(n => n.Author).WithMany().HasForeignKey(n => n.AuthorUserId).OnDelete(DeleteBehavior.NoAction)` (avoid cascade cycle through UserEntity, same as QuestEntity.DungeonMaster).
    Add a plain-language comment (no phase/decision IDs) explaining the deliberate omission of a SuperAdmin bypass, mirroring the existing CharacterEntity comment's intent.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `QuestBoardContext` exposes `Contacts`, `ContactImages`, `ContactNotes` DbSets.
    - Source assertion: three `HasQueryFilter` calls exist for the Contact entities, each referencing `activeGroupContext.ActiveGroupId` inside the lambda (verify no `var groupId = activeGroupContext.ActiveGroupId;` local capture precedes them within OnModelCreating).
    - Source assertion: the Notes relationship uses `OnDelete(DeleteBehavior.Cascade)` and the Author relationship uses `OnDelete(DeleteBehavior.NoAction)`.
    - Source assertion: no SuperAdmin/cross-group escape clause appears in any Contact query filter.
    - `dotnet build QuestBoard.Repository` succeeds.
  </acceptance_criteria>
  <done>QuestBoardContext registers all three Contact DbSets, fail-closed group filters, and the Notes/Author relationships; Repository compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the AddContactsFeature migration</name>
  <files>QuestBoard.Repository/Migrations/ (new AddContactsFeature migration files)</files>
  <read_first>
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md (Code Examples: EF Core migration invocation — run from QuestBoard.Service/, NOT the stale create-migration.sh)
    - CLAUDE.md (EF migration workflow, migrations auto-applied on startup via context.Database.Migrate())
    - The most recent existing migration file under QuestBoard.Repository/Migrations/ (e.g. AddGroupIdToCharacters) to confirm the generated migration matches expected shape
  </read_first>
  <action>
    From the `QuestBoard.Service/` directory run: `dotnet ef migrations add AddContactsFeature --project ../QuestBoard.Repository --context QuestBoardContext`.
    Do NOT use the stale `create-migration.sh` (references a WSL path). Do NOT run `dotnet ef database update` — migrations auto-apply on startup via `context.Database.Migrate()`.
    Inspect the generated `Up()`: confirm it creates three tables — `Contacts` (with GroupId FK to Groups, CreatedByUserId FK to Users/AspNetUsers-equivalent, IsRevealed, Name, Description, TownCity, SubLocation, CreatedAt), `ContactImages` (Id as both PK and FK to Contacts, ImageData), and `ContactNotes` (ContactId FK cascade, AuthorUserId FK NoAction, Text, CreatedAt, UpdatedAt). Confirm the ContactNotes → Users FK uses `ReferentialAction.NoAction` / `.Restrict` (no cascade cycle) and ContactImages/ContactNotes → Contacts use cascade.
    If the migration includes unexpected unrelated changes (drift), stop and report rather than editing by hand.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `dotnet ef migrations add AddContactsFeature` completes without error; a new timestamped `*_AddContactsFeature.cs` migration and the updated model snapshot exist under QuestBoard.Repository/Migrations/.
    - Source assertion: the migration `Up()` calls `CreateTable` for `Contacts`, `ContactImages`, and `ContactNotes`.
    - Source assertion: the `ContactNotes` → users FK uses a non-cascade delete behavior (NoAction/Restrict); `ContactImages`/`ContactNotes` → `Contacts` use cascade.
    - `dotnet build QuestBoard.Repository` succeeds.
  </acceptance_criteria>
  <done>The AddContactsFeature migration exists, creates all three tables with correct FK cascade behaviors, matches the current model snapshot, and the project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active group context → EF query | The GroupId query filter is the multi-tenancy boundary; a null/mismatched group must return zero rows |
| Contact ↔ Group | Every Contact/ContactImage/ContactNote is scoped to exactly one group via the filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04 | Information Disclosure | Cross-tenant Contact data leak | mitigate | Fail-closed `HasQueryFilter` on GroupId for all three entities, no SuperAdmin bypass, no local-variable capture (per RESEARCH.md Pattern 1 / Phase 55 precedent) |
| T-57-05 | Tampering | Cascade-cycle / orphaned rows on delete | mitigate | Notes cascade-delete with Contact; Author FK uses NoAction to avoid cascade cycle through UserEntity (RESEARCH.md Pattern 3) |
| T-57-SC | Tampering | NuGet installs | accept | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build QuestBoard.Repository` succeeds after all three tasks.
- Query filters reference the live `activeGroupContext` service instance inside each lambda (no stale-null local capture).
- No SuperAdmin cross-group bypass on any Contact filter.
- Migration creates exactly three new tables with correct cascade/NoAction FK behaviors.
</verification>

<success_criteria>
- Three Contact entities exist and mirror the Character entity family conventions.
- QuestBoardContext registers all three DbSets, fail-closed group filters, and Notes/Author relationships.
- AddContactsFeature migration exists and creates Contacts/ContactImages/ContactNotes.
- Repository project compiles.
</success_criteria>

<output>
Create `.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-02-SUMMARY.md` when done
</output>
