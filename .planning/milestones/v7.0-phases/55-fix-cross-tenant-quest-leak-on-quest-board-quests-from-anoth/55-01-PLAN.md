---
phase: 55-fix-cross-tenant-quest-leak-on-quest-board-quests-from-anoth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Repository/Entities/QuestBoardContext.cs
  - QuestBoard.UnitTests/Repository/QuestBoardContextFilterTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A query for any of the 7 group-scoped entities returns zero rows (not every group's rows) when ActiveGroupId is null (D-03)"
    - "A query for those entities with a concrete ActiveGroupId returns only that group's rows (D-03)"
    - "The DailyReminderJob cross-group sweep still returns all groups' quests (it uses IgnoreQueryFilters, unaffected by the filter predicate change) (D-03)"
  artifacts:
    - path: "QuestBoard.Repository/Entities/QuestBoardContext.cs"
      provides: "Fail-closed HasQueryFilter for all 7 group-scoped entities"
      contains: "ActiveGroupId != null &&"
    - path: "QuestBoard.UnitTests/Repository/QuestBoardContextFilterTests.cs"
      provides: "Regression tests proving each of the 7 filters returns zero rows for a null ActiveGroupId"
  key_links:
    - from: "QuestBoardContext.cs QuestEntity filter"
      to: "activeGroupContext.ActiveGroupId"
      via: "HasQueryFilter predicate"
      pattern: "ActiveGroupId != null &&"
---

<objective>
Harden the EF Core global query filters for all 7 group-scoped entities in `QuestBoardContext.cs` to drop the `ActiveGroupId == null ||` fail-open escape hatch (D-03), matching `CharacterEntity`'s existing fail-closed shape from Phase 49. This is the defense-in-depth layer: even if the middleware group-gate (Plan 02) is ever bypassed, a null `ActiveGroupId` must yield zero rows, not every group's rows.

Purpose: Close the data-layer half of the cross-tenant leak. RESEARCH.md confirmed 7 entities share the fail-open shape — the 5 named in CONTEXT.md (`QuestEntity`, `ShopItemEntity`, `ProposedDateEntity`, `PlayerDateVoteEntity`, `PlayerSignupEntity`) plus 2 discovered by grep (`ReminderLogEntity`, `UserTransactionEntity`). All 7 must be hardened for the fix to be exhaustive.
Output: 7 hardened filter predicates, corrected explanatory comments, and a new regression test file proving fail-closed behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-fix-cross-tenant-quest-leak-on-quest-board-quests-from-anoth/55-CONTEXT.md
@.planning/phases/55-fix-cross-tenant-quest-leak-on-quest-board-quests-from-anoth/55-RESEARCH.md
@.planning/phases/55-fix-cross-tenant-quest-leak-on-quest-board-quests-from-anoth/55-PATTERNS.md

<interfaces>
<!-- The fail-closed template already in QuestBoardContext.cs (lines 299-316, Phase 49) — replicate its shape. -->
CharacterEntity filter (the target shape to copy):
```
.HasQueryFilter(e =>
    activeGroupContext.ActiveGroupId != null &&
    e.GroupId == activeGroupContext.ActiveGroupId);
```

The 7 entities to harden and their group-scoping path (keep each navigation chain, only flip the predicate):
- QuestEntity          — e.GroupId (direct FK)
- ShopItemEntity       — e.GroupId (direct FK)
- ProposedDateEntity   — pd.Quest.GroupId
- PlayerDateVoteEntity — pdv.ProposedDate.Quest.GroupId
- PlayerSignupEntity   — ps.Quest.GroupId
- ReminderLogEntity    — r.Quest.GroupId
- UserTransactionEntity — t.ShopItem.GroupId

Unit-test analog (already in PlayerSignupRepositoryTests.cs, lines 13-29):
```
private static QuestBoardContext CreateContext(string databaseName, IActiveGroupContext activeGroupContext)
private sealed class TestActiveGroupContext : IActiveGroupContext  // ActiveGroupId => null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing fail-closed filter regression tests (Wave 0)</name>
  <files>QuestBoard.UnitTests/Repository/QuestBoardContextFilterTests.cs</files>
  <read_first>
    - QuestBoard.UnitTests/Repository/PlayerSignupRepositoryTests.cs (lines 1-35 — the `CreateContext(databaseName, IActiveGroupContext)` overload and the `TestActiveGroupContext`/`MutableTestGroupContext` pattern to mirror; read the seeding helpers for GroupEntity/QuestEntity/ShopItemEntity to reuse the same in-memory seeding style)
    - QuestBoard.Repository/Entities/QuestBoardContext.cs (lines 244-327 — the 7 current fail-open filters and the 3 CharacterEntity fail-closed filters, so tests assert against the real predicate shape)
    - QuestBoard.Repository/Entities (browse for QuestEntity, ShopItemEntity, ProposedDateEntity, PlayerDateVoteEntity, PlayerSignupEntity, ReminderLogEntity, UserTransactionEntity to get required FK/navigation properties for valid seeding)
  </read_first>
  <behavior>
    - Test per entity (7 total, or a parameterized theory): seed two distinct groups' worth of rows for the entity, build a context with an IActiveGroupContext whose ActiveGroupId is null, query the entity's DbSet, and assert the result count is ZERO (fail-closed).
    - Companion positive test (at least one entity, e.g. QuestEntity): seed two groups, build a context with ActiveGroupId = group 1's id, assert only group 1's rows are returned.
    - Use the InMemory provider via a fresh `Guid.NewGuid().ToString()` database name per test to avoid cross-test state bleed.
  </behavior>
  <action>
    Create `QuestBoardContextFilterTests.cs` in `QuestBoard.UnitTests/Repository/`. Mirror `PlayerSignupRepositoryTests.cs`'s `CreateContext(string databaseName, IActiveGroupContext activeGroupContext)` overload and its private `TestActiveGroupContext` (returns null ActiveGroupId) / `MutableTestGroupContext` (settable ActiveGroupId) inner classes — do not add these to the existing file, declare them locally in the new test class. For each of the 7 group-scoped entities (QuestEntity, ShopItemEntity, ProposedDateEntity, PlayerDateVoteEntity, PlayerSignupEntity, ReminderLogEntity, UserTransactionEntity), seed rows belonging to two different GroupId values (creating the minimal required parent chain — e.g. a QuestEntity for the Quest-navigation entities, a ShopItemEntity for UserTransactionEntity — using SaveChanges through a context built WITH a concrete ActiveGroupId so the seed writes succeed), then re-query through a context built with a null-ActiveGroupId context and assert `.Count()` is 0. These tests MUST FAIL against the current fail-open filters (which return all rows for null ActiveGroupId). Do NOT modify QuestBoardContext.cs in this task — the RED state proves the tests catch the bug. Do not embed phase/decision IDs (D-03, Phase 55) in any comment or string per CLAUDE.md's Code Comments rule.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.UnitTests/QuestBoard.UnitTests.csproj --filter FullyQualifiedName~QuestBoardContextFilterTests</automated>
  </verify>
  <acceptance_criteria>
    - File `QuestBoard.UnitTests/Repository/QuestBoardContextFilterTests.cs` exists and compiles.
    - Test run shows the null-ActiveGroupId tests FAILING (they assert zero rows but the current fail-open filter returns all rows) — this RED state is the expected, correct outcome of this task.
    - There is at least one null-ActiveGroupId zero-rows assertion for each of the 7 entity types (grep the file: 7 distinct entity DbSet queries under a null-context).
    - No `D-03`, `D-0`, `Phase 55`, `55-01`, or similar planning/tracking token appears in the file (grep -v '^#' filtered): `grep -niE 'D-0[0-9]|Phase 55|55-0' QuestBoard.UnitTests/Repository/QuestBoardContextFilterTests.cs` returns nothing.
  </acceptance_criteria>
  <done>New test file exists; the 7 null-ActiveGroupId assertions fail against the current fail-open filters (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Harden all 7 group-scoped filters to fail-closed + correct comments</name>
  <files>QuestBoard.Repository/Entities/QuestBoardContext.cs</files>
  <read_first>
    - QuestBoard.Repository/Entities/QuestBoardContext.cs (lines 244-327 — all 7 fail-open filters to change AND the 3 CharacterEntity fail-closed filters (lines 295-316) whose comment style is the target to match)
    - QuestBoard.UnitTests/Repository/QuestBoardContextFilterTests.cs (the tests from Task 1 that must go GREEN after this change)
    - .planning/phases/55-fix-cross-tenant-quest-leak-on-quest-board-quests-from-anoth/55-PATTERNS.md (the exact before/after transformation for each of the 7 entities)
  </read_first>
  <action>
    In `QuestBoardContext.cs`, change the `HasQueryFilter` predicate for all 7 group-scoped entities from `activeGroupContext.ActiveGroupId == null || <path>.GroupId == activeGroupContext.ActiveGroupId` to `activeGroupContext.ActiveGroupId != null && <path>.GroupId == activeGroupContext.ActiveGroupId`, keeping each entity's existing navigation path unchanged: QuestEntity (`e.GroupId`, ~line 251-254), ShopItemEntity (`e.GroupId`, ~256-259), ProposedDateEntity (`pd.Quest.GroupId`, ~266-269), PlayerDateVoteEntity (`pdv.ProposedDate.Quest.GroupId`, ~274-277), PlayerSignupEntity (`ps.Quest.GroupId`, ~283-286), ReminderLogEntity (`r.Quest.GroupId`, ~290-293), UserTransactionEntity (`t.ShopItem.GroupId`, ~323-326). Rewrite the explanatory comment block at ~lines 244-250 (currently states "Null = see all (SuperAdmin/seeding contexts intentionally bypass group scoping)") and each per-entity inline comment that references the old "SuperAdmin see all" / "escape hatch" framing to describe the new fail-closed intent, matching the tone of the existing CharacterEntity comment at ~lines 295-298 ("deliberately does NOT offer a SuperAdmin cross-group view... an empty roster is the intended behavior"). Keep the "CRITICAL: Do NOT capture activeGroupContext.ActiveGroupId into a local var" note — it stays true and important. Do NOT touch the 3 already-fail-closed CharacterEntity/CharacterClassEntity/CharacterImageEntity filters (295-316) or the UserEntity no-filter comment (318). Write comments in plain "why" language, no phase/decision IDs, per CLAUDE.md.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.UnitTests/QuestBoard.UnitTests.csproj --filter FullyQualifiedName~QuestBoardContextFilterTests</automated>
  </verify>
  <acceptance_criteria>
    - All 7 filters now use `ActiveGroupId != null &&`: `grep -c 'ActiveGroupId != null &&' QuestBoard.Repository/Entities/QuestBoardContext.cs` returns 10 (7 hardened + 3 pre-existing CharacterEntity family).
    - Zero fail-open predicates remain: `grep -c 'ActiveGroupId == null ||' QuestBoard.Repository/Entities/QuestBoardContext.cs` returns 0.
    - The Task 1 regression tests now PASS (GREEN): `dotnet test --filter FullyQualifiedName~QuestBoardContextFilterTests` reports all tests passed.
    - The comment at ~line 247 no longer contains the string "Null = see all"; grep confirms it is absent: `grep -c 'Null = see all' QuestBoard.Repository/Entities/QuestBoardContext.cs` returns 0.
    - No planning/tracking token in changed comments: `grep -niE 'D-0[0-9]|Phase 55|55-0[0-9]' QuestBoard.Repository/Entities/QuestBoardContext.cs` returns nothing.
  </acceptance_criteria>
  <done>All 7 filters fail-closed; regression tests GREEN; stale "see all" comment corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller/repository → EF Core query filter | Group-scoping invariant enforced at the data layer; last line of defense if a higher layer's gate is bypassed |
| Session state (ActiveGroupId) → query filter predicate | A null ActiveGroupId (fresh session, revoked group) crosses into every read; must not silently widen scope to all tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Information Disclosure | `QuestBoardContext` fail-open `HasQueryFilter` (7 entities) | mitigate | Change predicate from `ActiveGroupId == null \|\| ...` to `ActiveGroupId != null && ...` so a null ActiveGroupId returns zero rows instead of every group's rows — the confirmed root cause of the reported cross-tenant leak |
| T-55-02 | Elevation of Privilege | Defense-in-depth gap — data layer relying on the middleware gate alone | mitigate | This plan makes the filter independently fail-closed, so a bypass of the Plan-02 middleware gate cannot leak cross-tenant data (Phase 49's documented "single-layer enforcement has repeatedly proven fragile" lesson) |
| T-55-03 | Tampering | Regression drift — a future edit reintroducing the fail-open shape on one entity | mitigate | `QuestBoardContextFilterTests` asserts zero-rows-on-null for all 7 entities; CI fails if any entity is reverted to fail-open |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — all APIs ship in the existing Microsoft.EntityFrameworkCore 10.0.9 reference; RESEARCH.md Package Legitimacy Audit records zero packages evaluated |
</threat_model>

<verification>
- `dotnet build` succeeds (no compile errors from the predicate/comment edits).
- `dotnet test --filter FullyQualifiedName~QuestBoardContextFilterTests` — all fail-closed regression tests GREEN.
- `dotnet test QuestBoard.UnitTests/QuestBoard.UnitTests.csproj` — full unit suite green (confirms no existing test relied on the fail-open behavior).
- Manual grep: exactly 10 `ActiveGroupId != null &&` occurrences, zero `ActiveGroupId == null ||` occurrences in QuestBoardContext.cs.
</verification>

<success_criteria>
- All 7 group-scoped entity filters are fail-closed (zero rows for null ActiveGroupId).
- No fail-open (`== null ||`) predicate remains in QuestBoardContext.cs.
- New regression test file proves the fail-closed behavior for all 7 entities and is GREEN.
- Stale "Null = see all" comment rationale corrected to describe the fail-closed intent.
</success_criteria>

<output>
Create `.planning/phases/55-fix-cross-tenant-quest-leak-on-quest-board-quests-from-anoth/55-01-SUMMARY.md` when done.
</output>
