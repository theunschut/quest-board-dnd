---
phase: 61-allow-dms-to-edit-finalized-quest-details-excluding-proposed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.IntegrationTests/Controllers/QuestFinalizedEditTests.cs
autonomous: true
requirements: [61]
must_haves:
  truths:
    - "A failing integration test asserts the finalized-quest Edit GET returns 200 (not 400 BadRequest)"
    - "A failing integration test asserts the finalized-quest Edit page hides the Proposed Dates block but still shows Challenge Rating / Total Player Count / DM-Session"
    - "A failing integration test asserts a non-finalized OneShot quest still shows Proposed Dates (regression guard)"
    - "A failing integration test asserts lowering Total Player Count below the selected-player count re-renders the form (200) without persisting the change (D-01)"
    - "A failing integration test asserts a valid finalized-quest edit persists Title/Description without wiping the selected roster or FinalizedDate"
    - "A failing integration test asserts the Manage page (desktop + mobile) renders an Edit Quest link for a finalized OneShot quest"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/QuestFinalizedEditTests.cs"
      provides: "Wave-0 failing test coverage for the finalized-quest edit feature"
      contains: "class QuestFinalizedEditTests"
  key_links:
    - from: "QuestFinalizedEditTests.cs"
      to: "/Quest/Edit/{id} and /Quest/Manage/{id}"
      via: "HttpClient GET/POST against the running app"
      pattern: "/Quest/(Edit|Manage)/"
---

<objective>
Author the Wave-0 failing integration tests that pin every behavior Phase 61 must deliver: relaxing the finalized-quest Edit block, hiding Proposed Dates on finalized quests (desktop + mobile), the Total Player Count floor guard (D-01), the no-roster-wipe guarantee, and the new Edit Quest entry point on Manage (desktop + mobile).

Purpose: Lock the target behavior in executable form before any production code changes, following the exact TDD structure Phase 50 used for this same Edit/Manage view area (QuestCampaignUiParityTests). These tests are expected to FAIL against the current tree (which returns 400 BadRequest for finalized-quest Edit) and go GREEN in plan 61-02.
Output: A new QuestFinalizedEditTests.cs integration test file, committed RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-allow-dms-to-edit-finalized-quest-details-excluding-proposed/61-CONTEXT.md
@.planning/phases/61-allow-dms-to-edit-finalized-quest-details-excluding-proposed/61-PATTERNS.md

<interfaces>
<!-- Test helper signatures the executor uses directly — no exploration needed. -->

From QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs:
- CreateTestQuestAsync(IServiceProvider services, int dungeonMasterId, string title = "Test Quest", string description = "Test Description", int challengeRating = 5, bool isFinalized = false, bool dungeonMasterSession = false, DateTime? finalizedDate = null, bool isClosed = false, DateTime? closedDate = null, int groupId = 1) — NOTE: sets TotalPlayerCount = 4 internally (not a parameter).
- CreatePlayerSignupAsync(IServiceProvider services, int questId, int playerId, int signupRole = 0, bool isSelected = false) — signupRole 0 = Player.
- CreateProposedDateAsync(IServiceProvider services, int questId, DateTime date).
- ClearDatabaseAsync(IServiceProvider services).

From QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs:
- CreateAuthenticatedClientWithUserAsync(factory, "username", "email@example.com", roles: ["DungeonMaster"]) returns (HttpClient client, User user); seeds group-1 membership automatically.
- CreateTestUserAsync(factory.Services, "username", "email@example.com") returns a User (for seeding seated players / the quest's DM).

From QuestBoard.IntegrationTests/Helpers/AntiForgeryHelper.cs:
- ExtractAntiForgeryTokenAsync(HttpResponseMessage) returns (string Token, string CookieValue).
- CreateFormContentWithAntiForgeryToken(Dictionary<string,string> formData, string antiForgeryToken) returns FormUrlEncodedContent.

From WebApplicationFactoryBase:
- factory.TestGroupContext.ActiveGroupId — settable int for board-scope control (group 1 = OneShot board, default).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write finalized-quest Edit + Manage failing integration tests</name>
  <files>QuestBoard.IntegrationTests/Controllers/QuestFinalizedEditTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs (the closest analog — copy its structure: IClassFixture<WebApplicationFactoryBase>, MobileUserAgent const, ClearDatabaseAsync + CreateAuthenticatedClientWithUserAsync setup, GET-then-assert-content and POST-with-antiforgery flows, TestContext.Current.CancellationToken usage)
    - QuestBoard.IntegrationTests/Controllers/QuestJoinFinalizedQuestTests.cs (the analog for building a FINALIZED quest with selected players: CreateTestQuestAsync with isFinalized: true + finalizedDate, CreateProposedDateAsync, loop of CreatePlayerSignupAsync with isSelected: true, and the scope/context re-read assertion pattern)
    - QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs (confirm CreateTestQuestAsync/CreatePlayerSignupAsync/CreateProposedDateAsync signatures and that TotalPlayerCount defaults to 4)
    - QuestBoard.IntegrationTests/Helpers/AntiForgeryHelper.cs (POST form-content + token extraction pattern)
    - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs (Edit GET lines 138-185, Edit POST lines 187-267 — the current 400-BadRequest behavior these tests invert)
  </read_first>
  <action>
    Create QuestBoard.IntegrationTests/Controllers/QuestFinalizedEditTests.cs in namespace QuestBoard.IntegrationTests.Controllers, class QuestFinalizedEditTests(WebApplicationFactoryBase factory) : IClassFixture<WebApplicationFactoryBase>. Copy the MobileUserAgent iPhone constant and the mobile-request-building block (HttpRequestMessage + User-Agent header + copy Authorization) verbatim from QuestCampaignUiParityTests. All tests target group 1 (the default OneShot board) so no campaign seeding is needed; author every test on the DungeonMaster role via CreateAuthenticatedClientWithUserAsync with roles: ["DungeonMaster"].

    Write these tests (each on its own finalized quest; all use CreateTestQuestAsync(..., isFinalized: true, finalizedDate: DateTime.UtcNow.AddDays(7)) followed by CreateProposedDateAsync(..., quest.FinalizedDate!.Value)):

    1. FinalizedEdit_Get_Desktop_Returns200_NotBadRequest — GET /Quest/Edit/{id} on a finalized quest owned by the calling DM. Assert StatusCode == HttpStatusCode.OK (this currently returns 400 BadRequest — the core RED assertion).

    2. FinalizedEdit_Get_Desktop_HidesProposedDates_ShowsOneShotFields — GET the finalized-quest Edit page. Assert content Should().NotContain("Proposed Dates") AND content Should().Contain("Challenge Rating") AND content Should().Contain("Total Player Count") AND content Should().Contain("Dungeon Master Session Only"). (Proves only the dates sub-block is hidden, not the whole OneShot block.)

    3. FinalizedEdit_Get_Mobile_HidesProposedDates_ShowsOneShotFields — same as #2 but send the MobileUserAgent request; same four assertions.

    4. NonFinalizedEdit_Get_Desktop_StillShowsProposedDates — regression guard: create a quest with isFinalized: false, GET its Edit page, assert content Should().Contain("Proposed Dates") (proves the new !IsFinalized condition does not leak into the un-finalized path).

    5. FinalizedEdit_Post_LoweringPlayerCountBelowSelected_ReRendersAndDoesNotPersist — create a finalized quest (TotalPlayerCount = 4), seed 3 selected Player signups (loop CreatePlayerSignupAsync with isSelected: true, signupRole: 0, each on a distinct CreateTestUserAsync user). GET the Edit form, extract the antiforgery token/cookie (mirror QuestCampaignUiParityTests.CampaignEdit_InvalidModelState flow), POST form data with Id, Quest.Id, Quest.Title (non-empty), Quest.Description (non-empty), Quest.ChallengeRating=5, Quest.DungeonMasterSession=false, and Quest.TotalPlayerCount=2 (below the 3 selected). Assert StatusCode == HttpStatusCode.OK (form re-renders with validation error, no redirect). Then open a fresh scope + QuestBoardContext, re-read the quest, assert quest.TotalPlayerCount Should().Be(4) (unchanged — the invalid edit was rejected).

    6. FinalizedEdit_Post_ValidTitleEdit_PersistsWithoutWipingRoster — create a finalized quest, seed 2 selected Player signups. GET + token, POST with a changed Quest.Title (e.g. "Renamed Finalized Quest"), Quest.Description non-empty, Quest.TotalPlayerCount=4, Quest.ChallengeRating=5, Quest.DungeonMasterSession=false. Assert the response is a redirect (BeOneOf HttpStatusCode.Redirect, HttpStatusCode.Found) or OK. Then re-read via a fresh QuestBoardContext scope: assert quest.Title Should().Be("Renamed Finalized Quest"), quest.IsFinalized Should().BeTrue(), quest.FinalizedDate Should().NotBeNull(), and context.PlayerSignups.Count(s => s.QuestId == quest.Id && s.IsSelected) Should().Be(2) (roster survived untouched — the core D-04 guarantee).

    7. FinalizedManage_Desktop_RendersEditQuestLink — create a finalized quest, GET /Quest/Manage/{id}, assert StatusCode OK, content Should().Contain("Edit Quest") AND content Should().Contain($"/Quest/Edit/{quest.Id}").

    8. FinalizedManage_Mobile_RendersEditQuestLink — same as #7 but with the MobileUserAgent request.

    Do NOT modify any production code in this plan — tests only. Use TestContext.Current.CancellationToken on every await that accepts a token, matching the existing test files.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - File QuestBoard.IntegrationTests/Controllers/QuestFinalizedEditTests.cs exists and contains `class QuestFinalizedEditTests`.
    - The project compiles: `dotnet build QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj` succeeds with 0 errors.
    - The file defines exactly 8 [Fact] test methods with the names listed in the action.
    - `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~QuestFinalizedEditTests"` runs and shows failing tests (RED) — specifically FinalizedEdit_Get_Desktop_Returns200_NotBadRequest fails because the current controller returns 400. (A RED result here is the expected, correct outcome for this plan.)
    - No file under QuestBoard.Service/ is modified by this plan.
  </acceptance_criteria>
  <done>QuestFinalizedEditTests.cs compiles, contains 8 finalized-quest edit/manage facts, and the suite is RED (feature not yet implemented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Tests exercise real HTTP endpoints; no new trust boundary introduced (test-only plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | QuestFinalizedEditTests.cs | accept | Test-only code, runs against an isolated test database (ClearDatabaseAsync per test); no production surface, no packages installed. |
</threat_model>

<verification>
- `dotnet build QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj` succeeds.
- `dotnet test --filter "FullyQualifiedName~QuestFinalizedEditTests"` executes 8 tests, all currently failing (RED) — the intended state until 61-02 lands.
</verification>

<success_criteria>
- 8 finalized-quest edit/manage integration tests authored and compiling.
- Suite is RED, proving the tests actually exercise the not-yet-built behavior.
- Zero production-code changes.
</success_criteria>

<output>
Create `.planning/phases/61-allow-dms-to-edit-finalized-quest-details-excluding-proposed/61-01-SUMMARY.md` when done
</output>
