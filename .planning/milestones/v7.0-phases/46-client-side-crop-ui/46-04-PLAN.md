---
phase: 46-client-side-crop-ui
plan: 04
type: execute
wave: 3
depends_on: [46-03]
files_modified:
  - QuestBoard.Service/Views/Characters/Index.cshtml
  - QuestBoard.Service/Views/Characters/Index.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Index.cshtml
  - QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Details.cshtml
  - QuestBoard.Service/Views/Quest/Manage.cshtml
  - QuestBoard.Service/Views/Quest/_QuestCard.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml
autonomous: true
requirements: [IMAGE-04]
must_haves:
  truths:
    - "Every read-only avatar/thumbnail outside Character Details and Contact Details serves the cropped image endpoint"
    - "Character Details and Contact Details views are left untouched and still serve the original image (per D-03)"
  artifacts:
    - path: "QuestBoard.Service/Views/Characters/Index.cshtml"
      provides: "Cropped thumbnails on the Characters list"
      contains: "GetCroppedPicture"
    - path: "QuestBoard.Service/Views/Contacts/Index.cshtml"
      provides: "Cropped thumbnails on the Contacts list"
      contains: "GetCroppedContactImage"
    - path: "QuestBoard.Service/Views/Quest/Details.cshtml"
      provides: "Cropped participant avatars (selected + waitlist)"
      contains: "GetCroppedPicture"
  key_links:
    - from: "read-only avatar <img> src"
      to: "GetCroppedPicture / GetCroppedContactImage actions (Plan 03)"
      via: "Url.Action name change only"
      pattern: "GetCroppedPicture|GetCroppedContactImage"
---

<objective>
Repoint every read-only avatar/thumbnail `<img src>` outside Character Details and Contact Details from the original-read action to the new cropped-read action (`GetProfilePicture` → `GetCroppedPicture` for Characters, `GetContactImage` → `GetCroppedContactImage` for Contacts), per D-03. Character Details and Contact Details views are deliberately left unchanged (they are the only two pages that show the original). The DM Profile/EditProfile views need no markup change — their `GetDMProfilePicture` action was repointed in place in Plan 03.

Purpose: Make the app show the cropped image everywhere the display rule (D-03) requires.
Output: 9 modified read-only views (11 `src` occurrences), no Details view touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-client-side-crop-ui/46-CONTEXT.md
@.planning/phases/46-client-side-crop-ui/46-PATTERNS.md
@.planning/phases/46-client-side-crop-ui/46-03-SUMMARY.md

<interfaces>
<!-- Exact occurrence inventory (verified by grep). Repoint the action name ONLY — no other attribute changes. -->

Characters (GetProfilePicture → GetCroppedPicture, same-controller Url.Action):
  Views/Characters/Index.cshtml            lines 38, 113
  Views/Characters/Index.Mobile.cshtml     lines 24, 73

Contacts (GetContactImage → GetCroppedContactImage, same-controller Url.Action):
  Views/Contacts/Index.cshtml              line 50
  Views/Contacts/Index.Mobile.cshtml       line 42

Cross-controller Characters avatars (Url.Action("GetProfilePicture", "Characters", ...) → "GetCroppedPicture"):
  Views/Quest/Details.cshtml               lines 119 (selected), 235 (waitlist)
  Views/Quest/Manage.cshtml                line 410
  Views/Quest/_QuestCard.cshtml            line 61
  Views/QuestLog/Details.cshtml            line 84
  Views/QuestLog/Details.Mobile.cshtml     line 69

DO NOT TOUCH (serve original per D-03):
  Views/Characters/Details.cshtml (line 18), Details.Mobile.cshtml (line 20)
  Views/Contacts/Details.cshtml (line 15), Details.Mobile.cshtml (line 17)

NOT in this plan (upload-view preview repoints belong to Plan 06):
  Views/Characters/Create.cshtml, Edit.cshtml, Edit.Mobile.cshtml
  Views/Contacts/Edit.cshtml, Edit.Mobile.cshtml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint Characters/Contacts index thumbnails to the cropped-read actions</name>
  <files>QuestBoard.Service/Views/Characters/Index.cshtml, QuestBoard.Service/Views/Characters/Index.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Index.cshtml, QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Characters/Index.cshtml (the two `Url.Action("GetProfilePicture", new { id = character.Id })` calls at lines 38 and 113)
    - QuestBoard.Service/Views/Characters/Index.Mobile.cshtml (lines 24 and 73)
    - QuestBoard.Service/Views/Contacts/Index.cshtml (line 50 `Url.Action("GetContactImage", ...)`)
    - QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml (line 42)
    - .planning/phases/46-client-side-crop-ui/46-PATTERNS.md (section "Views/Quest/Details.cshtml" — the single-attribute repoint rule; Index note at the end)
  </read_first>
  <action>In `Characters/Index.cshtml` (both occurrences, lines 38 and 113) and `Characters/Index.Mobile.cshtml` (both occurrences, lines 24 and 73), change the action name inside `@Url.Action("GetProfilePicture", new { id = character.Id })` from `"GetProfilePicture"` to `"GetCroppedPicture"` — change the action-name string ONLY; leave the anonymous route object, `alt`, `class`, `style`, and any `onerror` attributes exactly as-is. In `Contacts/Index.cshtml` (line 50) and `Contacts/Index.Mobile.cshtml` (line 42), change `"GetContactImage"` to `"GetCroppedContactImage"`, same single-string change. Do not touch any Details view.</action>
  <verify>
    <automated>grep -rn "GetProfilePicture" QuestBoard.Service/Views/Characters/Index.cshtml QuestBoard.Service/Views/Characters/Index.Mobile.cshtml; grep -rn "GetContactImage" QuestBoard.Service/Views/Contacts/Index.cshtml QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `Characters/Index.cshtml` and `Characters/Index.Mobile.cshtml` contain no remaining `GetProfilePicture` reference (both occurrences repointed to `GetCroppedPicture`).
    - `Contacts/Index.cshtml` and `Contacts/Index.Mobile.cshtml` contain no remaining `GetContactImage` reference (repointed to `GetCroppedContactImage`).
    - The verify grep for `GetProfilePicture` in the two Character Index files and `GetContactImage` in the two Contact Index files returns no matches.
    - No route-object, class, alt, or onerror attribute was altered (only the action-name string changed).
  </acceptance_criteria>
  <done>All four index views serve cropped thumbnails.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint Quest and QuestLog participant avatars to GetCroppedPicture</name>
  <files>QuestBoard.Service/Views/Quest/Details.cshtml, QuestBoard.Service/Views/Quest/Manage.cshtml, QuestBoard.Service/Views/Quest/_QuestCard.cshtml, QuestBoard.Service/Views/QuestLog/Details.cshtml, QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Details.cshtml (the two cross-controller avatars at lines 119 and 235: `Url.Action("GetProfilePicture", "Characters", new { id = ... })`)
    - QuestBoard.Service/Views/Quest/Manage.cshtml (line 410)
    - QuestBoard.Service/Views/Quest/_QuestCard.cshtml (line 61)
    - QuestBoard.Service/Views/QuestLog/Details.cshtml (line 84)
    - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml (line 69)
    - .planning/phases/46-client-side-crop-ui/46-PATTERNS.md (section "Views/Quest/Details.cshtml" — repoint pattern, both occurrences called out)
  </read_first>
  <action>In each of the listed files, change the action-name string inside `@Url.Action("GetProfilePicture", "Characters", new { id = ... })` from `"GetProfilePicture"` to `"GetCroppedPicture"` — keep the `"Characters"` controller argument and the route object intact. In `Quest/Details.cshtml` change BOTH occurrences (line 119 selected-participant avatar and line 235 waitlist avatar). In `Quest/Manage.cshtml` (line 410), `_QuestCard.cshtml` (line 61), `QuestLog/Details.cshtml` (line 84), and `QuestLog/Details.Mobile.cshtml` (line 69), change the single occurrence each. Change only the action-name string; leave every other attribute (alt, class, style, onerror) unchanged.</action>
  <verify>
    <automated>grep -rn "GetProfilePicture" QuestBoard.Service/Views/Quest/ QuestBoard.Service/Views/QuestLog/</automated>
  </verify>
  <acceptance_criteria>
    - `Quest/Details.cshtml` has both avatar occurrences (selected + waitlist) repointed to `GetCroppedPicture`.
    - The verify grep returns no `GetProfilePicture` matches anywhere under `Views/Quest/` or `Views/QuestLog/`.
    - Each repointed `Url.Action` still passes `"Characters"` as the controller argument.
  </acceptance_criteria>
  <done>All Quest and QuestLog participant avatars serve the cropped image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-markup src repointing only; the authorization boundary lives in the controller actions (Plan 03), not the view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04-01 | Information Disclosure | Repointed cropped-read src on shared Quest/QuestLog pages | accept | The cropped-read actions carry the same authorization as their original-read siblings (established and tested in Plan 03: Characters bare-authorize matching GetProfilePicture, Contacts IsVisibleTo parity). Repointing a src does not change who can call the endpoint. |
| T-46-04-02 | Tampering | Accidental repoint of a Details view (D-03 violation) | mitigate | Task actions explicitly exclude the four Details views; the verify greps for remaining GetProfilePicture/GetContactImage confirm only the intended read-only views changed, and a Details view retaining the original action is the expected state. |
| T-46-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install in this plan. N/A. |
</threat_model>

<verification>
- `grep` confirms no `GetProfilePicture` remains in the four Character/Quest/QuestLog target files and no `GetContactImage` in the two Contact index files.
- The four Details views still reference the original actions (unchanged).
- Full `dotnet build QuestBoard.Service` succeeds (Razor compiles).
</verification>

<success_criteria>
- Cropped image displays on all list thumbnails, quest participant avatars, and recap avatars.
- Character Details and Contact Details still display the original image.
</success_criteria>

<output>
Create `.planning/phases/46-client-side-crop-ui/46-04-SUMMARY.md` when done.
</output>
