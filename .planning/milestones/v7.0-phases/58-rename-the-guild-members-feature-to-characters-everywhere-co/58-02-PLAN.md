---
phase: 58-rename-the-guild-members-feature-to-characters-everywhere-co
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/wwwroot/css/characters.css
  - QuestBoard.Service/wwwroot/css/characters.mobile.css
  - QuestBoard.Service/wwwroot/css/guild-members.css
  - QuestBoard.Service/wwwroot/css/guild-members.mobile.css
  - QuestBoard.Service/wwwroot/css/character-detail.mobile.css
  - QuestBoard.Service/wwwroot/css/character-form.mobile.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "characters.css and characters.mobile.css exist in wwwroot/css/ with all classes renamed to character-* equivalents"
    - "guild-members.css and guild-members.mobile.css no longer exist"
    - "No CSS file under wwwroot/css/ contains a selector or comment referencing 'guild'"
  artifacts:
    - path: "QuestBoard.Service/wwwroot/css/characters.css"
      provides: "Desktop character-card styles (renamed from guild-members.css); .guild-members-page renamed to .characters-page"
      contains: ".characters-page"
    - path: "QuestBoard.Service/wwwroot/css/characters.mobile.css"
      provides: "Mobile character-list-row styles (renamed from guild-members.mobile.css); all .guild-member-* / .guild-section-* / .guild-empty-state classes renamed to character-*"
      contains: ".character-member-row"
  key_links: []
---

<objective>
Rename the two character-roster CSS files and every `guild-*` class inside them to `character-*` equivalents (D-01). This is the leaf of the dependency graph: no view references the NEW filenames/class names yet (the `<link>` tags and class usages are updated in Plan 03/Plan 04), so this plan is a safe, isolated change that can run in Wave 1 in parallel with Plan 01 (disjoint files).

CRITICAL COEXISTENCE NOTE: this plan renames the CSS FILES (`guild-members.css` → `characters.css`). Until Plan 03 (mobile `<link>` in Index.Mobile.cshtml) and Plan 04 (desktop `<link>` in _Layout.cshtml) update the `<link href>` paths, the app's existing pages still point at the old `guild-members.css` path. That is acceptable because this plan and Plans 03/04 land as an ordered wave sequence (Wave 1 → Wave 2 → Wave 3), and the intermediate state is never verified/shipped independently — the phase-end grep+test gate (Plan 06) is the only completeness checkpoint. Do NOT leave both old and new CSS files present; delete the old files as part of the rename.

Purpose: The Domain/Repository layers are already "Character"-named; this brings the static presentation assets into alignment.
Output: `characters.css` + `characters.mobile.css` with `character-*` classes; old files deleted; two sibling CSS files' stale header comments corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-CONTEXT.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename guild-members.css to characters.css and rename the .guild-members-page scoping class</name>
  <files>QuestBoard.Service/wwwroot/css/characters.css, QuestBoard.Service/wwwroot/css/guild-members.css</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/guild-members.css (307 lines — full read; RESEARCH.md section "3. CSS files" confirms only ONE class needs renaming here)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "3. CSS files", the "guild-members.css (307 lines)" rename table
  </read_first>
  <action>
    Rename the file `QuestBoard.Service/wwwroot/css/guild-members.css` to `QuestBoard.Service/wwwroot/css/characters.css` (git mv / create-new-delete-old). Inside it, rename the single top-level scoping class `.guild-members-page` to `.characters-page` at every occurrence — RESEARCH.md verified this is used 27× as a scoping-prefix selector (e.g. `.guild-members-page .character-card` → `.characters-page .character-card`). Do a whole-file replace of `guild-members-page` → `characters-page` (there are no other `guild` class tokens in this file — every other selector like `.character-card`, `.character-image`, `.character-placeholder`, `.retired-badge`, `.dead-badge`, `.main-badge`, `.character-info`, `.character-name`, `.character-class`, `.character-level`, `.character-owner`, and the `character-card-ripple` keyframe is already `character-*`-named). Also update the header comment block (RESEARCH.md notes it reads like `/* GUILD MEMBERS - CHARACTER CARDS */`) to a guild-free equivalent such as `/* CHARACTERS - CHARACTER CARDS */` so no stale "guild" text survives in the renamed file. Delete the old `guild-members.css`.
  </action>
  <verify>
    <automated>test -f QuestBoard.Service/wwwroot/css/characters.css && ! test -f QuestBoard.Service/wwwroot/css/guild-members.css && grep -c "guild" QuestBoard.Service/wwwroot/css/characters.css</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoard.Service/wwwroot/css/characters.css` exists; `QuestBoard.Service/wwwroot/css/guild-members.css` does not
    - `grep -i "guild" QuestBoard.Service/wwwroot/css/characters.css` returns 0 hits (class token AND header comment cleaned)
    - `grep -c "\.characters-page" QuestBoard.Service/wwwroot/css/characters.css` returns >= 1 (renamed scoping class present)
    - No `.character-card`, `.retired-badge`, `.dead-badge`, `.main-badge` etc. selector was altered (only the `.guild-members-page` prefix changed)
  </acceptance_criteria>
  <done>characters.css exists with .characters-page scoping and zero "guild" text; old file gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rename guild-members.mobile.css to characters.mobile.css and rename all guild-* mobile classes</name>
  <files>QuestBoard.Service/wwwroot/css/characters.mobile.css, QuestBoard.Service/wwwroot/css/guild-members.mobile.css</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/guild-members.mobile.css (115 lines — full read; RESEARCH.md section "3. CSS files" has the complete 11-row class rename table)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "3. CSS files", the "guild-members.mobile.css (115 lines)" rename table (definitive old→new mapping)
  </read_first>
  <action>
    Rename the file `QuestBoard.Service/wwwroot/css/guild-members.mobile.css` to `QuestBoard.Service/wwwroot/css/characters.mobile.css`. Apply the exact class renames from RESEARCH.md's table (character-roster mobile scope). Old → New:
    `.guild-section-card` → `.character-section-card`;
    `.guild-section-heading` → `.character-section-heading`;
    `.guild-member-row` → `.character-member-row` (includes its `:last-child`, `:active`, `.retired`, `.dead` modifier rules);
    `.guild-member-thumbnail` → `.character-member-thumbnail`;
    `.guild-member-placeholder` → `.character-member-placeholder`;
    `.guild-member-name` → `.character-member-name`;
    `.guild-member-class` → `.character-member-class`;
    `.guild-member-owner` → `.character-member-owner`;
    `.guild-empty-state` → `.character-empty-state` (includes its `i`, `h5`, `p` child selectors);
    and the two compound selectors `.guild-section-card .text-muted` → `.character-section-card .text-muted` and `.guild-section-card .badge` → `.character-section-card .badge`.
    The simplest correct approach: whole-file replace of the substring `guild-section` → `character-section`, `guild-member` → `character-member`, and `guild-empty-state` → `character-empty-state` (these three substring replacements cover every class token above without over-matching). Update the file's header comment (RESEARCH.md notes it reads like `/* guild-members.mobile.css — loaded exclusively by GuildMembers/Index.Mobile.cshtml ... */`) to reference the new filename and path: `/* characters.mobile.css — loaded exclusively by Characters/Index.Mobile.cshtml ... */`. Delete the old `guild-members.mobile.css`. NOTE: the matching class-name updates in `Index.Mobile.cshtml` happen in Plan 03 — do not edit any view here; this task is CSS-only. Use `.character-member-row` (not the terser `.character-row`) per RESEARCH.md's recommendation to keep the two CSS files' scoped classes grep-distinct.
  </action>
  <verify>
    <automated>test -f QuestBoard.Service/wwwroot/css/characters.mobile.css && ! test -f QuestBoard.Service/wwwroot/css/guild-members.mobile.css && grep -c "guild" QuestBoard.Service/wwwroot/css/characters.mobile.css</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoard.Service/wwwroot/css/characters.mobile.css` exists; `guild-members.mobile.css` does not
    - `grep -i "guild" QuestBoard.Service/wwwroot/css/characters.mobile.css` returns 0 hits (classes AND header comment cleaned)
    - `grep -c "\.character-member-row" QuestBoard.Service/wwwroot/css/characters.mobile.css` returns >= 1
    - `grep -c "\.character-section-card" QuestBoard.Service/wwwroot/css/characters.mobile.css` returns >= 1
    - `grep -c "\.character-empty-state" QuestBoard.Service/wwwroot/css/characters.mobile.css` returns >= 1
  </acceptance_criteria>
  <done>characters.mobile.css exists with all guild-* mobile classes renamed to character-* and zero "guild" text; old file gone.</done>
</task>

<task type="auto">
  <name>Task 3: Correct stale "GuildMembers/..." header-comment paths in two sibling CSS files</name>
  <files>QuestBoard.Service/wwwroot/css/character-detail.mobile.css, QuestBoard.Service/wwwroot/css/character-form.mobile.css</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/character-detail.mobile.css (line 1 header comment only)
    - QuestBoard.Service/wwwroot/css/character-form.mobile.css (line 1 header comment only)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "3. CSS files", the "CSS files that reference 'guild' only in a header comment" table
  </read_first>
  <action>
    These two files are already correctly named and have NO class renames — only their line-1 header comment references the old `GuildMembers/...` view path and must be corrected for accuracy:
    (a) `character-detail.mobile.css` line 1 — change `loaded exclusively by GuildMembers/Details.Mobile.cshtml` to `loaded exclusively by Characters/Details.Mobile.cshtml`.
    (b) `character-form.mobile.css` line 1 — change `loaded exclusively by GuildMembers/Create.Mobile.cshtml and Edit.Mobile.cshtml` to `loaded exclusively by Characters/Create.Mobile.cshtml and Edit.Mobile.cshtml`.
    Change nothing else in either file. These are the only two edits.
  </action>
  <verify>
    <automated>grep -l "GuildMembers" QuestBoard.Service/wwwroot/css/character-detail.mobile.css QuestBoard.Service/wwwroot/css/character-form.mobile.css; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GuildMembers" QuestBoard.Service/wwwroot/css/character-detail.mobile.css` returns 0
    - `grep -c "GuildMembers" QuestBoard.Service/wwwroot/css/character-form.mobile.css` returns 0
    - Both files' header comments now reference `Characters/...` paths; no CSS rule/selector in either file was changed
  </acceptance_criteria>
  <done>Both sibling CSS files' header comments reference the new Characters/ view path; no other changes.</done>
</task>

</tasks>

<verification>
- `ls QuestBoard.Service/wwwroot/css/guild-members*` returns no matches (both old files gone).
- `grep -rin "guild" QuestBoard.Service/wwwroot/css/` returns 0 hits across the entire css folder.
- `characters.css` and `characters.mobile.css` both exist with `character-*` selectors.
</verification>

<success_criteria>
- Both `guild-members*.css` files renamed to `characters*.css` with every `guild-*` class token converted to a `character-*` equivalent.
- Zero "guild" occurrences (class or comment) remain anywhere under `wwwroot/css/`.
- No unrelated selector or rule body was altered — this is a pure rename.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

New/renamed static assets and class tokens created by this plan:
- `characters.css` (file) — renamed from `guild-members.css`.
- `characters.mobile.css` (file) — renamed from `guild-members.mobile.css`.
- `.characters-page` (CSS class) — renamed from `.guild-members-page`.
- `.character-section-card`, `.character-section-heading`, `.character-member-row`, `.character-member-thumbnail`, `.character-member-placeholder`, `.character-member-name`, `.character-member-class`, `.character-member-owner`, `.character-empty-state` (CSS classes) — renamed from their `.guild-*` equivalents.

Removed assets: `guild-members.css`, `guild-members.mobile.css`, and all `.guild-*` class tokens.
</artifacts>

<output>
Create `.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-02-SUMMARY.md` when done.
</output>
