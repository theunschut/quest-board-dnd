---
phase: 58-rename-the-guild-members-feature-to-characters-everywhere-co
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs
  - QuestBoard.Service/ViewModels/GuildMembersViewModels/GuildMembersIndexViewModel.cs
  - QuestBoard.Service/Controllers/QuestBoard/PlayersController.cs
  - QuestBoard.Service/Views/Players/Index.cshtml
  - QuestBoard.Service/Views/Players/Index.Mobile.cshtml
  - QuestBoard.Service/Views/_ViewImports.cshtml
autonomous: true
requirements: []
must_haves:
  truths:
    - "No class named GuildMembersIndexViewModel exists anywhere in the Service project"
    - "The Players nav page (/Players) still renders its DungeonMasters + Players list identically to before"
    - "The Players page empty-state copy no longer contains the phrase 'guild registry'"
  artifacts:
    - path: "QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs"
      provides: "Renamed view model for PlayersController.Index (DungeonMasters + Players lists)"
      contains: "class PlayersIndexViewModel"
    - path: "QuestBoard.Service/Controllers/QuestBoard/PlayersController.cs"
      provides: "Players controller constructing PlayersIndexViewModel"
      contains: "new PlayersIndexViewModel"
  key_links:
    - from: "QuestBoard.Service/Controllers/QuestBoard/PlayersController.cs"
      to: "QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs"
      via: "constructs and returns PlayersIndexViewModel"
      pattern: "PlayersIndexViewModel"
    - from: "QuestBoard.Service/Views/Players/Index.cshtml"
      to: "QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs"
      via: "@model PlayersIndexViewModel"
      pattern: "@model PlayersIndexViewModel"
---

<objective>
Fix the Players/DM-roster naming collision (D-02): the class `GuildMembersIndexViewModel` is NOT part of the character-roster feature — it is the view model for the unrelated `PlayersController.Index` page (lists `DungeonMasters` + `Players`). Rename it to `PlayersIndexViewModel`, relocate it into a new `ViewModels/PlayersViewModels/` folder matching the codebase's `ViewModels/[Feature]ViewModels/` convention, update all three consumers, and reword its "guild registry" empty-state copy.

This plan is fully independent of the character-roster rename (Plans 02–05) — it touches a disjoint set of files (PlayersController + Players views + the ViewModels folder split) and shares nothing with the `GuildMembersController`/`Views/GuildMembers` rename. It runs in Wave 1.

Purpose: Leaving a `GuildMembersIndexViewModel` in the codebase after this phase would defeat the rename's goal ("grep for GuildMembers returns zero hits") and confuse the next reader.
Output: `PlayersIndexViewModel` in `ViewModels/PlayersViewModels/`; empty `GuildMembersViewModels/` folder deleted; three consumers updated; reworded Players empty-state copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-CONTEXT.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename and relocate GuildMembersIndexViewModel to PlayersViewModels/PlayersIndexViewModel</name>
  <files>QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs, QuestBoard.Service/ViewModels/GuildMembersViewModels/GuildMembersIndexViewModel.cs</files>
  <read_first>
    - QuestBoard.Service/ViewModels/GuildMembersViewModels/GuildMembersIndexViewModel.cs (the 8-line file being renamed — full current content is in RESEARCH.md section "4. ViewModels")
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "4. ViewModels" (has the exact old content, the consumer list, and the naming-precedent rationale for PlayersViewModels)
    - .planning/codebase/CONVENTIONS.md — confirm the ViewModels/[Feature]ViewModels/ folder-per-feature convention
  </read_first>
  <action>
    Create `QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs` with namespace `QuestBoard.Service.ViewModels.PlayersViewModels` and class `PlayersIndexViewModel`, carrying over the two existing properties verbatim: `IEnumerable<User> DungeonMasters` (default `= []`) and `IEnumerable<User> Players` (default `= []`). Preserve the `User` type reference (add a `using` for the namespace that holds `User` if the new namespace does not already resolve it — check the original file's resolution; the original relied on a global using). Delete the old file `QuestBoard.Service/ViewModels/GuildMembersViewModels/GuildMembersIndexViewModel.cs`. After the delete, the `GuildMembersViewModels/` folder contains exactly one file (confirmed via ls in RESEARCH.md), so delete the now-empty `GuildMembersViewModels/` folder entirely. Do NOT touch `ViewModels/CharacterViewModels/` — `CharacterViewModel.cs` and `CharactersIndexViewModel.cs` are already correctly named and are the character-roster controller's actual view models.
  </action>
  <verify>
    <automated>test -f QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs && ! test -e QuestBoard.Service/ViewModels/GuildMembersViewModels && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs` exists and declares `public class PlayersIndexViewModel` in namespace `QuestBoard.Service.ViewModels.PlayersViewModels`
    - The class has both `DungeonMasters` and `Players` properties, each typed `IEnumerable<User>` and defaulted to `[]`
    - `QuestBoard.Service/ViewModels/GuildMembersViewModels/` no longer exists as a directory
    - `grep -rn "GuildMembersIndexViewModel" QuestBoard.Service/ViewModels/` returns 0 hits
  </acceptance_criteria>
  <done>The renamed view model exists in its convention-matching folder and the old folder is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update all three consumers of the renamed view model and reword Players empty-state copy</name>
  <files>QuestBoard.Service/Controllers/QuestBoard/PlayersController.cs, QuestBoard.Service/Views/Players/Index.cshtml, QuestBoard.Service/Views/Players/Index.Mobile.cshtml, QuestBoard.Service/Views/_ViewImports.cshtml</files>
  <read_first>
    - QuestBoard.Service/Controllers/QuestBoard/PlayersController.cs (line 2 `using`, line 14 `new GuildMembersIndexViewModel`)
    - QuestBoard.Service/Views/Players/Index.cshtml (line 2 `@using`, line 3 `@model`, line 45 "guild registry" copy)
    - QuestBoard.Service/Views/Players/Index.Mobile.cshtml (line 2 `@using`, line 3 `@model`, line 35 "guild registry" copy)
    - QuestBoard.Service/Views/_ViewImports.cshtml (line 9 global `@using ...GuildMembersViewModels`)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "4. ViewModels" (consumer list) and "Open Questions" #1 (recommendation: update the global using's namespace rather than remove it)
  </read_first>
  <action>
    Update the four consumers so nothing references the old type or namespace:
    (a) `PlayersController.cs` — change the `using QuestBoard.Service.ViewModels.GuildMembersViewModels;` at line 2 to `using QuestBoard.Service.ViewModels.PlayersViewModels;`, and change `new GuildMembersIndexViewModel` at line 14 to `new PlayersIndexViewModel` (property initializers `DungeonMasters = ...`, `Players = ...` stay unchanged).
    (b) `Views/Players/Index.cshtml` — change line 2 `@using QuestBoard.Service.ViewModels.GuildMembersViewModels` to `@using QuestBoard.Service.ViewModels.PlayersViewModels`, and line 3 `@model GuildMembersIndexViewModel` to `@model PlayersIndexViewModel`. Reword the line-45 empty-state copy — currently "The guild registry is currently empty. Brave souls may register as new quest leaders." — to drop the "guild registry" framing while keeping the same tone (e.g. "No quest leaders are registered yet. Brave souls may register as new quest leaders." — exact wording is Claude's discretion per D-02, just remove "guild").
    (c) `Views/Players/Index.Mobile.cshtml` — same three edits at lines 2, 3, and 35 (line 35 has the identical "guild registry" copy — reword it identically to the desktop view for consistency).
    (d) `Views/_ViewImports.cshtml` — update the global `@using QuestBoard.Service.ViewModels.GuildMembersViewModels` at line 9 to `@using QuestBoard.Service.ViewModels.PlayersViewModels` (per RESEARCH.md Open Question #1 recommendation: update the namespace rather than remove the line — lowest-risk, preserves current behavior).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service 2>&1 | grep -Ei "error|Build succeeded" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "GuildMembers" QuestBoard.Service/Controllers/QuestBoard/PlayersController.cs` returns 0 hits
    - `grep -rn "GuildMembers" QuestBoard.Service/Views/Players/` returns 0 hits
    - `grep -n "GuildMembers" QuestBoard.Service/Views/_ViewImports.cshtml` returns 0 hits
    - `grep -rin "guild registry" QuestBoard.Service/Views/Players/` returns 0 hits (empty-state copy reworded in both desktop and mobile)
    - `dotnet build QuestBoard.Service` succeeds with 0 errors
  </acceptance_criteria>
  <done>All references to the old GuildMembersIndexViewModel type/namespace are gone, the Service project compiles, and the "guild registry" copy is reworded on both Players views.</done>
</task>

</tasks>

<verification>
- `grep -rn "GuildMembersIndexViewModel" QuestBoard.Service/` returns 0 hits (no consumer, no definition left).
- `grep -rn "GuildMembersViewModels" QuestBoard.Service/` returns 0 hits (namespace fully gone).
- `dotnet build QuestBoard.Service` succeeds.
- Optional local smoke: run the app, visit `/Players`, confirm the DungeonMasters + Players list still renders and (when a section is empty) the reworded copy shows without the word "guild".
</verification>

<success_criteria>
- `PlayersIndexViewModel` lives in `ViewModels/PlayersViewModels/` and is the only view model type for `PlayersController.Index`.
- The `GuildMembersViewModels/` folder no longer exists.
- The Players page renders identically (zero behavior change) with reworded, guild-free empty-state copy.
- Service project builds clean.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

New/renamed identifiers created by this plan:
- `PlayersIndexViewModel` (class) — `QuestBoard.Service/ViewModels/PlayersViewModels/PlayersIndexViewModel.cs`, namespace `QuestBoard.Service.ViewModels.PlayersViewModels`. Replaces the deleted `GuildMembersIndexViewModel`.

Removed identifiers:
- `GuildMembersIndexViewModel` (class) and the `QuestBoard.Service.ViewModels.GuildMembersViewModels` namespace/folder — deleted.
</artifacts>

<output>
Create `.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-01-SUMMARY.md` when done.
</output>
