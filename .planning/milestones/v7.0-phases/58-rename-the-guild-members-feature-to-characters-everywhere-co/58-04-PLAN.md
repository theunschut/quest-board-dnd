---
phase: 58-rename-the-guild-members-feature-to-characters-everywhere-co
plan: 04
type: execute
wave: 3
depends_on:
  - 58-03
files_modified:
  - QuestBoard.Service/Views/QuestLog/Details.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Details.cshtml
  - QuestBoard.Service/Views/Quest/Manage.cshtml
  - QuestBoard.Service/Views/Quest/_QuestCard.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every @Url.Action(\"GetProfilePicture\", ...) cross-reference targets the \"Characters\" controller"
    - "Character profile-picture thumbnails render (do not 404) on the Quest board, Quest details, Quest manage, and Quest log pages"
    - "The desktop and mobile nav 'Characters' link points at the Characters controller and reads 'Characters', not 'Guild Members'"
    - "The desktop layout loads ~/css/characters.css (not guild-members.css)"
  artifacts:
    - path: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      provides: "Desktop layout: nav link to Characters controller + characters.css link"
      contains: "asp-controller=\"Characters\""
    - path: "QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml"
      provides: "Mobile layout: nav link to Characters controller"
      contains: "asp-controller=\"Characters\""
  key_links:
    - from: "QuestBoard.Service/Views/Quest/_QuestCard.cshtml"
      to: "QuestBoard.Service/Controllers/Characters/CharactersController.cs"
      via: "@Url.Action(\"GetProfilePicture\", \"Characters\", ...) resolves the profile-picture route"
      pattern: "Url.Action\\(\"GetProfilePicture\", \"Characters\""
    - from: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      to: "QuestBoard.Service/Controllers/Characters/CharactersController.cs"
      via: "nav asp-controller=\"Characters\" asp-action=\"Index\""
      pattern: "asp-controller=\"Characters\""
---

<objective>
Update every cross-controller reference and the nav links now that the controller is `CharactersController` (Plan 03). Two groups of edits:
(1) The six `@Url.Action("GetProfilePicture", "GuildMembers", ...)` call sites across four Quest/QuestLog views (including the easily-missed shared partial `_QuestCard.cshtml` — RESEARCH.md Pitfall 3) → change the controller-name string to `"Characters"`, else character thumbnails silently 404 wherever those views render.
(2) The desktop + mobile nav links (`asp-controller="GuildMembers"` + "Guild Members" text) → point at `Characters` with the text "Characters", and the desktop layout's `<link href="~/css/guild-members.css">` → `~/css/characters.css` (Plan 02's renamed file).

Depends on Plan 03 (Wave 2): the `"Characters"` controller must exist for these `Url.Action`/`asp-controller` references to resolve. Runs in Wave 3, parallel with Plan 05 (integration tests) — disjoint files.

Purpose: Keep profile-picture images working everywhere and finish the user-facing nav rename.
Output: 6 cross-refs pointing at "Characters"; nav links + desktop CSS link updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-CONTEXT.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint all 6 Url.Action("GetProfilePicture", "GuildMembers", ...) cross-references to "Characters"</name>
  <files>QuestBoard.Service/Views/QuestLog/Details.cshtml, QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml, QuestBoard.Service/Views/Quest/Details.cshtml, QuestBoard.Service/Views/Quest/Manage.cshtml, QuestBoard.Service/Views/Quest/_QuestCard.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/QuestLog/Details.cshtml (line 73)
    - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml (line 69)
    - QuestBoard.Service/Views/Quest/Details.cshtml (lines 111 and 227 — two occurrences)
    - QuestBoard.Service/Views/Quest/Manage.cshtml (line 410)
    - QuestBoard.Service/Views/Quest/_QuestCard.cshtml (line 61 — the 6th occurrence, a shared partial; RESEARCH.md Pitfall 3)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "6. Cross-references" (the confirmed 6-occurrence-across-4-files table) and "Common Pitfalls" Pitfall 3
  </read_first>
  <action>
    In each of the following, change only the second positional argument of `@Url.Action("GetProfilePicture", "GuildMembers", new { id = ... })` from `"GuildMembers"` to `"Characters"` — leave the action name `"GetProfilePicture"`, the anonymous `id` route value, and all surrounding markup unchanged:
    - `Views/QuestLog/Details.cshtml:73` (id = player.Character.Id)
    - `Views/QuestLog/Details.Mobile.cshtml:69` (id = player.Character.Id)
    - `Views/Quest/Details.cshtml:111` (id = participant.Character.Id)
    - `Views/Quest/Details.cshtml:227` (id = player.Character.Id)
    - `Views/Quest/Manage.cshtml:410` (id = participant.Character.Id)
    - `Views/Quest/_QuestCard.cshtml:61` (id = player.Character.Id)
    A whole-repo-scoped find/replace of the exact string `"GetProfilePicture", "GuildMembers"` → `"GetProfilePicture", "Characters"` across these 5 files is the safe way to hit all 6 occurrences without touching anything else. RESEARCH.md confirmed via grep these are the ONLY `Url.Action(..., "GuildMembers", ...)` sites in the repo — after this task, `grep -rn 'Url.Action.*"GuildMembers"' QuestBoard.Service/` must return 0.
  </action>
  <verify>
    <automated>grep -rn 'Url.Action.*"GuildMembers"' QuestBoard.Service/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'Url.Action.*"GuildMembers"' QuestBoard.Service/` returns 0 hits
    - `grep -rc 'Url.Action("GetProfilePicture", "Characters"' QuestBoard.Service/Views/Quest/_QuestCard.cshtml` returns >= 1 (the partial's 6th occurrence updated — Pitfall 3 covered)
    - The action name `"GetProfilePicture"` and each `new { id = ... }` route value are unchanged in all six sites
  </acceptance_criteria>
  <done>All 6 profile-picture cross-references target the "Characters" controller; zero "GuildMembers" Url.Action strings remain in the Service project.</done>
</task>

<task type="auto">
  <name>Task 2: Update desktop + mobile nav links and the desktop CSS link in the layouts</name>
  <files>QuestBoard.Service/Views/Shared/_Layout.cshtml, QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (line 19 CSS link, lines 131-132 nav asp-controller + "Guild Members" text)
    - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml (lines 113-114 nav asp-controller + "Guild Members" text)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "7. Nav links and global using" (the exact old→new table for lines 19/131/132/113/114)
  </read_first>
  <action>
    Edit `Views/Shared/_Layout.cshtml`:
    - Line 19: `<link rel="stylesheet" href="~/css/guild-members.css" asp-append-version="true" />` → change the href to `~/css/characters.css` (Plan 02's renamed file). RESEARCH.md confirmed line 19 is the ONLY guild-members CSS `<link>` in this file.
    - Line 131: `asp-controller="GuildMembers"` → `asp-controller="Characters"` (asp-action="Index" stays).
    - Line 132: nav link visible text `Guild Members` → `Characters` (keep the `<i class="fas fa-people-group me-1"></i>` icon).
    Edit `Views/Shared/_Layout.Mobile.cshtml`:
    - Line 113: `asp-controller="GuildMembers"` → `asp-controller="Characters"`.
    - Line 114: nav link visible text `Guild Members` → `Characters` (keep the `<i class="fas fa-people-group me-2"></i>` icon).
    Change nothing else in either layout — the Players nav item and every other link stay exactly as they are.
  </action>
  <verify>
    <automated>grep -ci "guild" QuestBoard.Service/Views/Shared/_Layout.cshtml QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "guild" QuestBoard.Service/Views/Shared/_Layout.cshtml` returns 0 hits
    - `grep -i "guild" QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml` returns 0 hits
    - `grep -c 'href="~/css/characters.css"' QuestBoard.Service/Views/Shared/_Layout.cshtml` returns 1
    - Both layouts have `asp-controller="Characters"` on the roster nav link and the visible text reads "Characters"
    - The separate "Players" nav item (different link) is untouched
  </acceptance_criteria>
  <done>Both layouts point the roster nav link at the Characters controller with "Characters" text, and the desktop layout loads characters.css; no "guild" text remains in either layout.</done>
</task>

</tasks>

<verification>
- `grep -rin "guild" QuestBoard.Service/Views/Shared/_Layout.cshtml QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml` returns 0.
- `grep -rn 'Url.Action.*"GuildMembers"' QuestBoard.Service/` returns 0.
- `dotnet build QuestBoard.Service` succeeds.
- Local smoke (recommended per RESEARCH.md Wave 0 gap): run the app, open a Quest with signed-up characters (board + details) and the Quest Log — confirm character avatar thumbnails render (no broken-image icons), proving the cross-controller Url.Action calls resolve. Confirm the nav now reads "Characters" and clicking it lands on `/Characters`.
</verification>

<success_criteria>
- All 6 `Url.Action("GetProfilePicture", "Characters", ...)` cross-references resolve — profile pictures render on Quest/QuestLog pages.
- Desktop + mobile nav links point at `Characters` and read "Characters".
- Desktop layout loads `characters.css`.
- Zero "guild" text remains in the two layouts.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

No new symbols — this plan repoints existing references. Updated wiring:
- 6 `@Url.Action("GetProfilePicture", "Characters", ...)` call sites (were `"GuildMembers"`).
- Desktop + mobile nav `asp-controller="Characters"` links with "Characters" text (were `"GuildMembers"` / "Guild Members").
- Desktop `<link href="~/css/characters.css">` (was `guild-members.css`).
</artifacts>

<output>
Create `.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-04-SUMMARY.md` when done.
</output>
