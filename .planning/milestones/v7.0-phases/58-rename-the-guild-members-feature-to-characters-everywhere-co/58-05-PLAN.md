---
phase: 58-rename-the-guild-members-feature-to-characters-everywhere-co
plan: 05
type: execute
wave: 3
depends_on:
  - 58-03
files_modified:
  - QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/GuildMembersControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs
  - QuestBoard.IntegrationTests/Controllers/LayoutNavigationTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The renamed CharactersControllerIntegrationTests exercises /Characters/* routes and all Phase 56 authorization cases pass"
    - "MobileViewsTests hits /Characters/* routes and asserts characters.mobile.css (not guild-members.mobile.css)"
    - "LayoutNavigationTests asserts the nav contains 'Characters' (not 'Guild Members')"
    - "The full test suite is green — zero behavior change confirmed"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs"
      provides: "Renamed integration test class exercising the /Characters/* route surface incl. Phase 56 owner-or-admin authorization regression tests"
      contains: "class CharactersControllerIntegrationTests"
  key_links:
    - from: "QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs"
      to: "QuestBoard.Service/Controllers/Characters/CharactersController.cs"
      via: "HTTP GET/POST to /Characters/* routes served by CharactersController"
      pattern: "\"/Characters"
---

<objective>
Update every integration test that hardcodes the old `/GuildMembers/*` route strings or asserts on old nav/CSS text, now that Plan 03 moved the route surface to `/Characters/*`. Three test files:
(1) `GuildMembersControllerIntegrationTests.cs` → rename file + class to `CharactersControllerIntegrationTests`, update all 16 route strings, 1 method name, and 1 loose `ContainAny("Guild","Members")` assertion.
(2) `MobileViewsTests.cs` — the file CONTEXT.md missed (RESEARCH.md Pitfall 2): 4 hardcoded `/GuildMembers/*` route strings, 1 method name, 1 `guild-members.mobile.css` assertion, and several doc comments.
(3) `LayoutNavigationTests.cs` — 1 method name, 1 `Contain("Guild Members")` assertion, 1 comment.

Depends on Plan 03 (Wave 2): the `/Characters/*` routes and the renamed nav/CSS must be live for these tests to pass. Runs in Wave 3, parallel with Plan 04 — disjoint files (this plan is entirely in `QuestBoard.IntegrationTests/`).

CRITICAL: this plan verifies the phase's "zero behavior change" guarantee. The Phase 56 owner-or-admin authorization tests (Admin/SuperAdmin/Player/cross-group Edit/Delete/ToggleRetirement) live in the renamed test file — they must all still pass after the route rename. They are renamed, NOT rewritten: same assertions, new route paths.

Purpose: Keep the test suite green and prove the rename introduced no behavior change.
Output: renamed test class + updated route strings across 3 test files; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-CONTEXT.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the controller integration test class + update all 16 route strings and the loose assertion</name>
  <files>QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/GuildMembersControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/GuildMembersControllerIntegrationTests.cs (full read — 504 lines, 17 test methods; RESEARCH.md section "5. Integration tests" has the complete line-by-line route-string inventory)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "5. Integration tests" (route-string table for lines 12,18,49,77,103,125,145,165,190,209,251,311,346,373,408,435,471,499 + the line-23 ContainAny note + the method-name note) and "Security Domain" (the Phase 56 authorization tests must keep passing)
  </read_first>
  <action>
    Rename the file `QuestBoard.IntegrationTests/Controllers/GuildMembersControllerIntegrationTests.cs` to `CharactersControllerIntegrationTests.cs` and rename the class `GuildMembersControllerIntegrationTests` to `CharactersControllerIntegrationTests`. Update every hardcoded route string from `/GuildMembers...` to `/Characters...`: a whole-file substring replace of `"/GuildMembers` with `"/Characters` cleanly covers `"/GuildMembers"`, `"/GuildMembers/Edit/{...}"`, `"/GuildMembers/Details/{...}"`, `"/GuildMembers/Delete"`, and `"/GuildMembers/ToggleRetirement"` at all 16 locations (lines 12, 18, 49, 77, 103, 125, 145, 165, 190, 209, 251, 311, 346, 373, 408, 435, 471, 499). Additional edits:
    - Rename the test method `Index_ShouldReturnGuildMembersPage` to `Index_ShouldReturnCharactersPage`.
    - Change the line-23 assertion `content.Should().ContainAny("Guild", "Members")` to `content.Should().ContainAny("Character", "Characters")` — the renamed page contains neither "Guild" nor "Members", so the old loose assertion would fail.
    - Reword any remaining test-method name or comment that contains "GuildMembers" to its Characters equivalent. The Phase 56 authorization test method names (e.g. `Edit_AdminEditingAnotherPlayersCharacter_ShouldSucceed`) are already Character-named and need no rename — only the route strings inside their bodies change.
    Do NOT change any assertion logic, HTTP verb, expected status code, or test-data setup — this is a route-string plus class-name rename only. The Phase 56 owner-or-admin authorization tests keep their exact assertions.
  </action>
  <verify>
    <automated>test -f QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs && ! test -f QuestBoard.IntegrationTests/Controllers/GuildMembersControllerIntegrationTests.cs && grep -c "GuildMembers" QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs` exists with `public class CharactersControllerIntegrationTests`; the old file does not exist
    - `grep -c "GuildMembers" QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs` returns 0
    - `grep -c '"/Characters' QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs` returns >= 16
    - `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~CharactersControllerIntegrationTests"` exits 0 (all Phase 56 authorization cases still pass under the new routes)
  </acceptance_criteria>
  <done>The controller integration test file/class is renamed, all 16 routes point at /Characters, the loose assertion is corrected, and the scoped test run is green.</done>
</task>

<task type="auto">
  <name>Task 2: Update MobileViewsTests.cs route strings, method name, CSS assertion, and doc comments (Pitfall 2)</name>
  <files>QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs (the "Guild" section around lines 737-829; RESEARCH.md section "5. Integration tests" has the exact 12-row edit table for this file)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "5. Integration tests" (the MobileViewsTests.cs table) and "Common Pitfalls" Pitfall 2
  </read_first>
  <action>
    This file was NOT in CONTEXT.md's list — RESEARCH.md found it via full-file read (Pitfall 2). Apply these edits:
    - Line ~748 method name `MobileGuildMembers_MobileUserAgent_RendersListRows` → rename to `MobileCharacters_MobileUserAgent_RendersListRows`.
    - Line ~753 route string `"/GuildMembers"` → `"/Characters"`.
    - Line ~760 assertion `html.Should().Contain("guild-members.mobile.css")` → `html.Should().Contain("characters.mobile.css")` (matches Plan 02's renamed CSS file and Plan 03's updated `<link>` in Index.Mobile.cshtml).
    - Line ~778 route string `"/GuildMembers/Details/{character.Id}"` → `"/Characters/Details/{character.Id}"`.
    - Line ~803 route string `"/GuildMembers/Create"` → `"/Characters/Create"`.
    - Line ~829 route string `"/GuildMembers/Edit/{character.Id}"` → `"/Characters/Edit/{character.Id}"`.
    - Update the doc/inline comments at lines ~737, 741, 744, 745, 768-769, 794-795, 819-820 that reference `/GuildMembers`, `guild-members.mobile.css`, `guild-member-row`, or `GuildMembers/Index.Mobile.cshtml` — reword each to its Characters/character equivalent (e.g. `Characters/Index.Mobile.cshtml`, `characters.mobile.css`, `character-member-row`). The line-745 stale "Test starts RED — GuildMembers/Index.Mobile.cshtml does not exist yet" historical comment should be updated for consistency (or removed if it no longer describes reality).
    Change no assertion logic beyond the CSS filename string — routes and CSS filename are the only functional edits; the rest are comment corrections.
  </action>
  <verify>
    <automated>grep -c "GuildMembers\|guild-member" QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "guild" QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs` returns 0
    - `grep -c '"/Characters' QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs` returns >= 4 (all four route strings updated)
    - `grep -c "characters.mobile.css" QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs` returns >= 1 (CSS assertion updated)
    - The method is named `MobileCharacters_MobileUserAgent_RendersListRows`
  </acceptance_criteria>
  <done>MobileViewsTests hits /Characters routes, asserts characters.mobile.css, and contains zero "guild" text.</done>
</task>

<task type="auto">
  <name>Task 3: Update LayoutNavigationTests.cs nav assertion, method name, and comment; run full suite</name>
  <files>QuestBoard.IntegrationTests/Controllers/LayoutNavigationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/LayoutNavigationTests.cs (lines 92 comment, 98 method name, 107 assertion; RESEARCH.md section "5. Integration tests" LayoutNavigationTests table)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — section "5. Integration tests" (LayoutNavigationTests.cs table) and "Validation Architecture" (full-suite command)
  </read_first>
  <action>
    Edit `QuestBoard.IntegrationTests/Controllers/LayoutNavigationTests.cs`:
    - Line ~92 comment referencing "Guild Members link PRESENT (regression guard)" → reword to "Characters link PRESENT (regression guard)" (drop any planning-ID prefix per CLAUDE.md if present in the comment).
    - Line ~98 method name `Nav_CampaignAuthenticated_GuildMembersLinkPresent` → rename to `Nav_CampaignAuthenticated_CharactersLinkPresent`.
    - Line ~107 assertion `html.Should().Contain("Guild Members")` → `html.Should().Contain("Characters")` (matches Plan 04's renamed nav link text).
    Then run the full test suite from the repo root to confirm zero behavior change across all three test projects. If any test outside this phase's file list fails referencing "GuildMembers" or "guild-members", capture it in the SUMMARY as a missed reference for the Plan 06 sweep to catch — but RESEARCH.md's exhaustive grep indicates none remain.
  </action>
  <verify>
    <automated>dotnet test 2>&1 | grep -Ei "Passed!|Failed!|error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "guild" QuestBoard.IntegrationTests/Controllers/LayoutNavigationTests.cs` returns 0
    - The method is named `Nav_CampaignAuthenticated_CharactersLinkPresent` and asserts `Contain("Characters")`
    - `dotnet test` reports 0 failed tests across all three test projects (full suite green — zero behavior change confirmed)
  </acceptance_criteria>
  <done>LayoutNavigationTests asserts the "Characters" nav link, and the entire test suite passes green.</done>
</task>

</tasks>

<verification>
- `grep -rin "guild" QuestBoard.IntegrationTests/` returns 0 hits across the whole test project.
- `dotnet test` — all three projects green, zero failures.
- The Phase 56 owner-or-admin authorization tests (now in CharactersControllerIntegrationTests) pass under the new /Characters/* routes.
</verification>

<success_criteria>
- Integration test file/class renamed to `CharactersControllerIntegrationTests`; all 16 route strings updated.
- `MobileViewsTests.cs` (Pitfall 2) and `LayoutNavigationTests.cs` updated — routes, CSS assertion, nav assertion, method names, comments.
- Full test suite green — the phase's zero-behavior-change guarantee is verified.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

New/renamed identifiers created by this plan:
- `CharactersControllerIntegrationTests` (test class) — `QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs`. Replaces the deleted `GuildMembersControllerIntegrationTests`.
- `Index_ShouldReturnCharactersPage`, `MobileCharacters_MobileUserAgent_RendersListRows`, `Nav_CampaignAuthenticated_CharactersLinkPresent` (test methods) — renamed from their GuildMembers-named predecessors.

Removed identifiers: `GuildMembersControllerIntegrationTests` (class) and the three GuildMembers-named test methods.
</artifacts>

<output>
Create `.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-05-SUMMARY.md` when done.
</output>
