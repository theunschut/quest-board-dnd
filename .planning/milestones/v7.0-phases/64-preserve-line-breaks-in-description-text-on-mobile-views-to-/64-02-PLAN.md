---
phase: 64-preserve-line-breaks-in-description-text-on-mobile-views-to-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/wwwroot/css/character-detail.mobile.css
  - QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml
  - QuestBoard.Service/Views/Shop/Details.Mobile.cshtml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Character Description and Backstory preserve typed line breaks on the mobile Characters/Details view"
    - "Shop item Description preserves typed line breaks on both desktop and mobile shop views"
  artifacts:
    - path: "QuestBoard.Service/wwwroot/css/character-detail.mobile.css"
      provides: "white-space: pre-wrap on .character-info-value"
      contains: "white-space: pre-wrap"
    - path: "QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml"
      provides: "inline white-space: pre-wrap on the desktop shop Description paragraph"
      contains: "white-space: pre-wrap"
    - path: "QuestBoard.Service/Views/Shop/Details.Mobile.cshtml"
      provides: "inline white-space: pre-wrap on the mobile shop Description paragraph"
      contains: "white-space: pre-wrap"
  key_links:
    - from: "QuestBoard.Service/Views/Characters/Details.Mobile.cshtml"
      to: "QuestBoard.Service/wwwroot/css/character-detail.mobile.css"
      via: ".character-info-value class on the Description and Backstory paragraphs"
      pattern: "character-info-value"
    - from: "QuestBoard.Service/Views/Shop/Details.cshtml"
      to: "QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml"
      via: "partial include rendering @Model.Description"
      pattern: "Model.Description"
---

<objective>
Close the two remaining instances of the missing-line-break bug: the mobile Characters/Details Description + Backstory fields (D-01) and the Shop item Description on both desktop and mobile (D-03). D-01 is fixed at the CSS-class level (`.character-info-value` in `character-detail.mobile.css`), matching the mobile convention of putting pre-wrap on a dedicated class. D-03 is fixed with an inline `style="white-space: pre-wrap;"` on each of the two shop Description paragraphs, matching the local convention in those files (the shop Description paragraphs use no dedicated per-field class, and the mobile one shares the generic `.parchment-text-muted` utility that must not be globally altered).

Purpose: These free-text fields collapse typed newlines into a single wrapped paragraph, unlike the ~90% of free-text fields in the app that already render pre-wrap.
Output: One CSS-class edit (`character-detail.mobile.css`) and two inline-style edits (`_ShopItemDetailsContent.cshtml`, `Shop/Details.Mobile.cshtml`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-wrap to .character-info-value (D-01 — mobile Characters/Details)</name>
  <files>QuestBoard.Service/wwwroot/css/character-detail.mobile.css</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/character-detail.mobile.css (see the grouped rule at lines 54-60 that includes `.character-info-value` — the mobile class that renders Description and Backstory but has no `white-space: pre-wrap`)
    - QuestBoard.Service/Views/Characters/Details.Mobile.cshtml (see Description at lines 80-86 and Backstory at lines 87-93, both using `<p class="character-info-value mb-0">`; note the same class is also used on single-line values like "Owned by" at line 27 and "Level" at line 51 — pre-wrap is harmless there)
    - QuestBoard.Service/Views/Characters/Details.cshtml (the desktop counterpart that already applies pre-wrap via inline `style="white-space: pre-wrap;"` on its Description/Backstory — the behavior to match)
    - .planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-CONTEXT.md (D-01 decision; Claude's Discretion note: mobile files follow the "dedicated CSS class" convention)
  </read_first>
  <action>
    In `character-detail.mobile.css`, add `white-space: pre-wrap;` so that `.character-info-value` receives it. The class currently appears in a grouped selector (lines 54-60: `.character-detail-card .text-muted, .character-detail-card small, .character-info-value { color: ...; text-shadow: ...; }`). Because that grouped rule also styles `.text-muted` and `small` (which include single-line labels), do NOT add `white-space: pre-wrap;` to the shared grouped block — that would over-apply. Instead add a NEW dedicated rule targeting only `.character-info-value`:

    Create a new rule with selector `.character-info-value` whose single declaration is `white-space: pre-wrap;`. Place it immediately after the existing grouped rule (after line 60) so the cascade is clear. Follow the file's 4-space indentation and the existing formatting style (opening brace on the selector line, one declaration per line, trailing semicolon). Do not add `!important` (the existing pre-wrap conventions in the codebase, e.g. `.recap-display-box`, omit it on `white-space`).

    This targets `.character-info-value` only; single-line values sharing the class are unaffected because they contain no embedded newlines.
  </action>
  <verify>
    <automated>cd QuestBoard.Service && grep -c 'white-space: pre-wrap' wwwroot/css/character-detail.mobile.css</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `QuestBoard.Service/wwwroot/css/character-detail.mobile.css` contains a rule whose selector is exactly `.character-info-value` (standalone, not grouped) with declaration `white-space: pre-wrap;`.
    - Behavior: `grep -c 'white-space: pre-wrap' QuestBoard.Service/wwwroot/css/character-detail.mobile.css` outputs `1`.
    - Source assertion: the pre-existing grouped rule at lines 54-60 (`.character-detail-card .text-muted, .character-detail-card small, .character-info-value`) still exists and did NOT gain a `white-space` declaration.
    - Behavior: no `.cshtml` file was modified for this task.
  </acceptance_criteria>
  <done>The mobile Characters/Details Description and Backstory render typed newlines as line breaks, matching the desktop view; a standalone `.character-info-value { white-space: pre-wrap; }` rule exists in the mobile CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline pre-wrap to Shop item Description on desktop and mobile (D-03)</name>
  <files>QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml, QuestBoard.Service/Views/Shop/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml (see line 136 `<p class="mb-0">@Model.Description</p>` — the desktop shop Description; this partial is included by both the modal and full desktop `Shop/Details.cshtml`, so this one edit covers all desktop shop rendering. The paragraph uses no dedicated per-field class, so inline style matches the local pattern)
    - QuestBoard.Service/Views/Shop/Details.Mobile.cshtml (see line 21 `<p class="mt-2 parchment-text-muted">@Model.Description</p>` — the mobile shop Description)
    - QuestBoard.Service/wwwroot/css/shop-details.mobile.css (see `.parchment-text-muted` at line 22 — a GROUPED/shared utility selector used for multiple text elements; do NOT add pre-wrap to this class or other muted text would change. Inline style on the specific `<p>` is the surgical fix)
    - QuestBoard.Service/Views/Quest/Details.cshtml (line 31 — reference example of the correct inline `style="white-space: pre-wrap;"` pattern to replicate)
    - .planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-CONTEXT.md (D-03 decision — broken on both platforms, no divergence)
  </read_first>
  <action>
    Apply an inline `style="white-space: pre-wrap;"` to each of the two shop Description paragraphs, without altering any CSS class:

    1. In `_ShopItemDetailsContent.cshtml` line 136, change the desktop Description paragraph so it carries `style="white-space: pre-wrap;"` in addition to its existing `class="mb-0"`. Keep the `class="mb-0"` and the `@Model.Description` content unchanged.

    2. In `Shop/Details.Mobile.cshtml` line 21, add `style="white-space: pre-wrap;"` to the mobile Description paragraph while keeping its existing `class="mt-2 parchment-text-muted"` and `@Model.Description` content unchanged.

    Do NOT edit `shop-details.mobile.css` or any CSS file — the `.parchment-text-muted` class is shared and must not gain a global `white-space` declaration. Use inline styles only, matching the pattern already used in `Quest/Details.cshtml`.
  </action>
  <verify>
    <automated>cd QuestBoard.Service && test $(grep -c 'white-space: pre-wrap' Views/Shared/_ShopItemDetailsContent.cshtml) -eq 1 && test $(grep -c 'white-space: pre-wrap' Views/Shop/Details.Mobile.cshtml) -eq 1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Views/Shared/_ShopItemDetailsContent.cshtml` has the Description `<p>` carrying both `class="mb-0"` and `style="white-space: pre-wrap;"`, still rendering `@Model.Description`.
    - Source assertion: `Views/Shop/Details.Mobile.cshtml` has the Description `<p>` carrying `class="mt-2 parchment-text-muted"` and `style="white-space: pre-wrap;"`, still rendering `@Model.Description`.
    - Behavior: `grep -c 'white-space: pre-wrap' Views/Shared/_ShopItemDetailsContent.cshtml` outputs `1` and `grep -c 'white-space: pre-wrap' Views/Shop/Details.Mobile.cshtml` outputs `1`.
    - Source assertion: `QuestBoard.Service/wwwroot/css/shop-details.mobile.css` is unchanged (the `.parchment-text-muted` rule gained no `white-space` declaration).
  </acceptance_criteria>
  <done>The Shop item Description renders typed newlines as line breaks on both the desktop shop details/modal (via the shared partial) and the mobile shop details view, using inline styles that leave the shared `.parchment-text-muted` class untouched.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds (Razor views must remain syntactically valid after the inline-style edits).
- `grep -c 'white-space: pre-wrap' QuestBoard.Service/wwwroot/css/character-detail.mobile.css` returns 1.
- `grep -c 'white-space: pre-wrap' QuestBoard.Service/Views/Shared/_ShopItemDetailsContent.cshtml` returns 1.
- `grep -c 'white-space: pre-wrap' QuestBoard.Service/Views/Shop/Details.Mobile.cshtml` returns 1.
- `QuestBoard.Service/wwwroot/css/shop-details.mobile.css` shows no new `white-space` declaration on `.parchment-text-muted`.
</verification>

<success_criteria>
- `.character-info-value` carries `white-space: pre-wrap;` via a dedicated (non-grouped) rule.
- Both shop Description paragraphs carry inline `style="white-space: pre-wrap;"`.
- No shared CSS class (`.parchment-text-muted`) was globally altered.
- No controller, service, ViewModel, or migration changes.
- No GSD phase/plan/requirement IDs appear in any source file or comment (per CLAUDE.md Code Comments rule).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

This plan creates NO new symbols — no decorators, classes, functions, CLI flags, struct/dataclass fields, or new files. It adds one standalone CSS rule (`.character-info-value { white-space: pre-wrap; }`) to a pre-existing stylesheet and two inline `style="white-space: pre-wrap;"` attributes on pre-existing Razor paragraphs. Pure CSS/markup consistency fix.
</artifacts_produced>

<output>
Create `.planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-02-SUMMARY.md` when done
</output>
