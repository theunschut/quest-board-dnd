---
phase: 64-preserve-line-breaks-in-description-text-on-mobile-views-to-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/wwwroot/css/quests.css
  - QuestBoard.Service/wwwroot/css/site.css
  - QuestBoard.Service/Views/QuestLog/Details.cshtml
autonomous: true
requirements: []
must_haves:
  truths:
    - "The 'Original Quest Description' box on the desktop QuestLog Details page preserves typed line breaks"
    - "The quest-board list-card description preview preserves typed line breaks on both desktop and mobile"
  artifacts:
    - path: "QuestBoard.Service/wwwroot/css/quests.css"
      provides: "white-space: pre-wrap on .quest-description-box"
      contains: "white-space: pre-wrap"
    - path: "QuestBoard.Service/wwwroot/css/site.css"
      provides: "white-space: pre-wrap on .modern-card .card-text"
      contains: "white-space: pre-wrap"
  key_links:
    - from: "QuestBoard.Service/Views/QuestLog/Details.cshtml"
      to: "QuestBoard.Service/wwwroot/css/quests.css"
      via: ".quest-description-box class on the Original Quest Description div"
      pattern: "quest-description-box"
    - from: "QuestBoard.Service/Views/Quest/_QuestCard.cshtml"
      to: "QuestBoard.Service/wwwroot/css/site.css"
      via: ".card-text class inside .modern-card"
      pattern: "modern-card .card-text"
---

<objective>
Close the two quest-domain instances of the missing-line-break bug: the desktop QuestLog "Original Quest Description" box (D-02) and the shared quest-board list-card description preview (D-04). Both are fixed by adding `white-space: pre-wrap` to the CSS class that already styles each element, matching the codebase convention where boxed callouts get pre-wrap on the class (see `.recap-display-box` in the same file).

Purpose: Free-text descriptions typed with line breaks currently collapse to a single wrapped paragraph in these two locations, unlike ~90% of other free-text fields in the app that already render pre-wrap.
Output: Two CSS-class edits (one in `quests.css`, one in `site.css`) plus one optional redundant-inline-style cleanup in `QuestLog/Details.cshtml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-wrap to .quest-description-box (D-02 — QuestLog desktop)</name>
  <files>QuestBoard.Service/wwwroot/css/quests.css, QuestBoard.Service/Views/QuestLog/Details.cshtml</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/quests.css (see `.quest-description-box` at lines 814-822 and the sibling `.recap-display-box` at lines 825-834, which already has `white-space: pre-wrap;` on line 833 — replicate that exact declaration)
    - QuestBoard.Service/Views/QuestLog/Details.cshtml (see the Original Quest Description box at lines 44-50 using `.quest-description-box`, and the Rewards box at line 59 which redundantly adds inline `style="white-space: pre-wrap;"` on the same class)
    - .planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-CONTEXT.md (D-02 decision and its note about the redundant Rewards inline override)
  </read_first>
  <action>
    In `quests.css`, add the declaration `white-space: pre-wrap;` to the `.quest-description-box` rule (currently lines 814-822), so its declaration block matches the sibling `.recap-display-box` rule which already includes it. Place the new line alongside the existing `line-height: 1.6;` line inside the same block. Do NOT create a new rule or a separate selector — extend the existing `.quest-description-box` block in place.

    Optional cleanup (nice-to-have, not required per D-02): in `QuestLog/Details.cshtml` line 59, the Rewards box `<div class="quest-description-box" style="white-space: pre-wrap;">` now has a redundant inline `style` attribute. You MAY remove the `style="white-space: pre-wrap;"` attribute from that single div since the class now provides it. If you remove it, remove only that attribute and leave the `class="quest-description-box"` intact. If you leave it, that is acceptable (harmless duplicate). Do not touch any other line in the view.

    Follow the file's existing CSS convention (4-space indent, trailing semicolon, `!important` only where the surrounding declarations already use it — `white-space` here does not need `!important`, matching `.recap-display-box`).
  </action>
  <verify>
    <automated>cd QuestBoard.Service && grep -A 10 'quest-description-box {' wwwroot/css/quests.css | grep -c 'white-space: pre-wrap'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the `.quest-description-box` rule block in `QuestBoard.Service/wwwroot/css/quests.css` contains the exact declaration `white-space: pre-wrap;`.
    - Source assertion: the `.recap-display-box` rule block is unchanged (still contains its own `white-space: pre-wrap;`).
    - Behavior: `grep -A 10 'quest-description-box {' QuestBoard.Service/wwwroot/css/quests.css | grep -c 'white-space: pre-wrap'` outputs `1`.
    - Behavior: no new CSS selector was introduced; `grep -c 'quest-description-box' QuestBoard.Service/wwwroot/css/quests.css` still outputs `1` (the single existing rule).
    - If the optional cleanup was applied: `QuestLog/Details.cshtml` line for the Rewards div no longer contains `style="white-space: pre-wrap;"` but still contains `class="quest-description-box"`.
  </acceptance_criteria>
  <done>The desktop QuestLog "Original Quest Description" box renders typed newlines as line breaks; the CSS class `.quest-description-box` now includes `white-space: pre-wrap;` in a single existing rule.</done>
</task>

<task type="auto">
  <name>Task 2: Add pre-wrap to .modern-card .card-text (D-04 — shared quest-board card preview)</name>
  <files>QuestBoard.Service/wwwroot/css/site.css</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/site.css (see `.modern-card .card-text` at lines 1313-1319 — the scrollable card-preview box with `max-height: 125px; overflow-y: auto;` that needs pre-wrap added)
    - QuestBoard.Service/Views/Quest/_QuestCard.cshtml (see line 42 `<p class="card-text">@Model.Description</p>` — the description preview rendered inside `.modern-card`; this shared partial is used by both desktop and mobile quest lists via `Quest/_QuestSection.cshtml`, so a single class edit fixes both platforms)
    - .planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-CONTEXT.md (D-04 decision)
  </read_first>
  <action>
    In `site.css`, add the declaration `white-space: pre-wrap;` to the existing `.modern-card .card-text` rule (currently lines 1313-1319, containing `max-height: 125px; overflow-y: auto; overflow-x: hidden; scrollbar-width: thin; scrollbar-color: ...`). Add the new declaration inside that same block — do NOT create a new rule or a separate `.card-text` selector.

    Note: `.card-text` also appears on single-line date/DM info paragraphs elsewhere in `_QuestCard.cshtml` (e.g. the finalized-date and dungeon-master lines). Adding `pre-wrap` to `.modern-card .card-text` is harmless for those single-line strings (no embedded newlines), so no per-element scoping is needed. Do not modify `_QuestCard.cshtml` — the fix is CSS-only.

    Match the file's existing indentation (4 spaces) and formatting for the surrounding declarations in this rule.
  </action>
  <verify>
    <automated>cd QuestBoard.Service && grep -A 8 'modern-card .card-text {' wwwroot/css/site.css | grep -c 'white-space: pre-wrap'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the `.modern-card .card-text` rule block in `QuestBoard.Service/wwwroot/css/site.css` (the one containing `max-height: 125px;`) contains the exact declaration `white-space: pre-wrap;`.
    - Behavior: `grep -A 8 'modern-card .card-text {' QuestBoard.Service/wwwroot/css/site.css | grep -c 'white-space: pre-wrap'` outputs `1`.
    - Source assertion: `QuestBoard.Service/Views/Quest/_QuestCard.cshtml` is unchanged (no view edit for this task).
    - Behavior: the `::-webkit-scrollbar` rules immediately following at lines 1321+ are unchanged.
  </acceptance_criteria>
  <done>The quest-board list-card description preview renders typed newlines as line breaks on both desktop and mobile; `.modern-card .card-text` now includes `white-space: pre-wrap;`.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds (CSS changes do not affect compilation, but confirm no view edit broke Razor syntax if the optional D-02 cleanup was applied).
- `grep -c 'white-space: pre-wrap' QuestBoard.Service/wwwroot/css/quests.css` returns 2 (one pre-existing on `.recap-display-box`, one new on `.quest-description-box`).
- `grep -A 8 'modern-card .card-text {' QuestBoard.Service/wwwroot/css/site.css` shows `white-space: pre-wrap;` inside the rule.
</verification>

<success_criteria>
- `.quest-description-box` and `.modern-card .card-text` both include `white-space: pre-wrap;`.
- No new CSS selectors created; edits extend the existing rules in place.
- No controller, service, ViewModel, or migration changes.
- No GSD phase/plan/requirement IDs appear in any source file or comment (per CLAUDE.md Code Comments rule).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

This plan creates NO new symbols — no decorators, classes, functions, CLI flags, struct/dataclass fields, or new files. It only adds one CSS declaration (`white-space: pre-wrap;`) to each of two pre-existing CSS rules (`.quest-description-box` in `quests.css`, `.modern-card .card-text` in `site.css`) and optionally removes one redundant inline `style` attribute in `QuestLog/Details.cshtml`. Pure CSS/markup consistency fix.
</artifacts_produced>

<output>
Create `.planning/phases/64-preserve-line-breaks-in-description-text-on-mobile-views-to-/64-01-SUMMARY.md` when done
</output>
