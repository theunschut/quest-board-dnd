---
phase: 53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - QuestBoard.Service/Views/QuestLog/EditRecap.cshtml
  - QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml
autonomous: false
requirements: []
must_haves:
  truths:
    - "The Details page Session Recap section renders read-only for everyone, including the DM/Admin — no inline edit textarea"
    - "D-01/D-02: A DM/Admin sees an 'Add Recap' button (empty recap) or 'Edit Recap' button (existing recap) directly under the recap content on Details, not in the Quick Actions sidebar"
    - "Clicking the button navigates to the new EditRecap page rendered in the modern-card pattern"
    - "D-03: The EditRecap page has a Save Recap (primary) and Cancel (secondary) button, Cancel returns to Details discarding changes"
    - "Both the desktop and mobile EditRecap views render and submit to the EditRecap POST action"
  artifacts:
    - path: "QuestBoard.Service/Views/QuestLog/EditRecap.cshtml"
      provides: "Desktop dedicated recap edit form (modern-card)"
      contains: "asp-action=\"EditRecap\""
    - path: "QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml"
      provides: "Mobile dedicated recap edit form (quest-edit-card-mobile)"
      contains: "asp-action=\"EditRecap\""
    - path: "QuestBoard.Service/Views/QuestLog/Details.cshtml"
      provides: "Read-only recap display + conditional entry-point button"
      contains: "EditRecap"
    - path: "QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml"
      provides: "Read-only recap display + conditional entry-point button (mobile)"
      contains: "EditRecap"
  key_links:
    - from: "Details.cshtml recap button"
      to: "QuestLogController.EditRecap (GET)"
      via: "Url.Action(\"EditRecap\", \"QuestLog\", new { id = Model.Quest.Id })"
      pattern: "Url.Action\\(\"EditRecap\""
    - from: "EditRecap.cshtml form"
      to: "QuestLogController.EditRecap (POST)"
      via: "asp-action EditRecap form submit"
      pattern: "asp-action=\"EditRecap\""
---

<objective>
Restructure the recap UI so the Details page becomes view-only for everyone and the edit form moves to a new dedicated page. Replace the inline recap edit form on both `Details.cshtml` and `Details.Mobile.cshtml` with an unconditional read-only display plus a conditional entry-point button (D-01/D-02), and create the two new `EditRecap` views (desktop + mobile) that mirror the `Quest/Edit` modern-card pattern (D-03).

Purpose: Delivers the user-observable outcome — a DM/Admin now edits the recap on its own page, and everyone (including the DM/Admin) sees a clean read-only recap on Details.

Output: Two new view files (`EditRecap.cshtml`, `EditRecap.Mobile.cshtml`) and two modified view files (`Details.cshtml`, `Details.Mobile.cshtml`). All markup is specified verbatim in 53-UI-SPEC.md — copy it directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-CONTEXT.md
@.planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-UI-SPEC.md
@.planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-PATTERNS.md

<interfaces>
<!-- Contract from Plan 01 (already implemented). Use directly. -->

Route and ViewModel the views bind against (delivered by Plan 53-01):
- GET/POST action: `EditRecap` on `QuestLogController` — form target `asp-action="EditRecap" asp-route-id="@Model.Id"`, POST redirects to `Details`
- ViewModel: `EditRecapViewModel` in namespace `QuestBoard.Service.ViewModels.QuestLogViewModels` with `int Id`, `string? Recap`, and `Quest Quest` (full domain Quest, so `Model.Quest.Title` is available for the heading)

Details views consume `ViewBag.CanEditRecap` (bool) — already set by the existing `Details` GET action (unchanged); gates whether the entry-point button renders. Both Details views already have `@model QuestLogDetailsViewModel` and `Model.Quest.Recap` / `Model.Quest.Id` in scope.

CLAUDE.md UI/UX guidelines (locked): `modern-card`/`modern-card-header`/`modern-card-body`, `<hr>` before the button row, filled colored buttons with FontAwesome + `me-2`, desktop button layout `d-flex justify-content-between` (secondary/Cancel left, primary/Save right). Mobile button row uses `d-flex gap-2` + `flex-fill` (established mobile idiom, not `justify-content-between`).

Existing CSS reused verbatim (no new stylesheet, no CSS edits): `.recap-display-box` (quests.css / quest-log-detail.mobile.css), `.quest-edit-card-mobile` + `.form-label`/`.form-text` (quest-edit.mobile.css), `.modern-card .form-control` (site.css — provides the white-background high-contrast textarea styling, making the old inline `style="background-color..."` override redundant — drop it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the desktop and mobile EditRecap views</name>
  <files>QuestBoard.Service/Views/QuestLog/EditRecap.cshtml, QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml</files>
  <read_first>
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-UI-SPEC.md ("View-by-View Contract" sections 3 and 4 — VERBATIM markup for both new views)
    - QuestBoard.Service/Views/Quest/Edit.cshtml (desktop modern-card analog)
    - QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml (mobile quest-edit-card-mobile analog + quest-edit.mobile.css link)
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-PATTERNS.md (the two EditRecap view pattern sections — icon/color/layout deviations from the analog called out explicitly)
  </read_first>
  <action>
    Create `QuestBoard.Service/Views/QuestLog/EditRecap.cshtml` using the exact markup in UI-SPEC.md "View-by-View Contract" section 3. Key locked details: `@model EditRecapViewModel` with `@using QuestBoard.Service.ViewModels.QuestLogViewModels`; `ViewData["Title"] = $"Edit Recap: {Model.Quest.Title}";`; `col-lg-8 col-md-7` single column (NO tips sidebar); `card modern-card` > `card-header modern-card-header` with `<i class="fas fa-book me-2"></i>` (fa-book, NOT fa-edit) > `card-body modern-card-body`; `asp-validation-summary="All"`; `<form asp-action="EditRecap" asp-route-id="@Model.Id" method="post">` with hidden `asp-for="Id"`; a `rows="10"` textarea `name="recap"` bound to `@Model.Recap` with placeholder "Describe what happened during this quest..." and `class="form-control"` with NO inline `style` attribute; help text `<small class="form-text text-muted">Share the story of this adventure with your players!</small>`; `<hr />` before the button row; button row `<div class="d-flex justify-content-between">` with Cancel as an `<a>` (`btn btn-secondary`, `fa-times`, links to `Url.Action("Details", "QuestLog", new { id = Model.Id })`) on the LEFT and Save Recap as a `<button type="submit" class="btn btn-primary">` (`fa-save`, NOT btn-warning) on the RIGHT. No `@section Scripts`.

    Create `QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml` using the exact markup in UI-SPEC.md section 4. Key locked details: same `@model`/`@using`/`ViewData` header; `@section Styles { <link href="~/css/quest-edit.mobile.css" asp-append-version="true" rel="stylesheet" /> }` (reuse existing stylesheet, do NOT create a recap-edit.mobile.css); `<div class="container-fluid px-2 mt-2">` > `<div class="quest-edit-card-mobile mb-3">`; heading `<h5 class="mb-0"><i class="fas fa-book me-2"></i>Edit Recap: @Model.Quest.Title</h5>` (fa-book, NO text-warning); `asp-validation-summary="All"`; same `asp-action="EditRecap"` form with hidden Id; `rows="6"` textarea (6 rows for mobile) bound to `@Model.Recap`; button row `<div class="d-flex gap-2 mt-3">` with Cancel `<a class="btn btn-secondary flex-fill">` (fa-times, links to Details) LEFT and Save Recap `<button class="btn btn-primary flex-fill">` (fa-save) RIGHT — use gap-2 + flex-fill, NOT justify-content-between, and use "Cancel"/fa-times NOT "Back to Quest"/fa-arrow-left.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - EditRecap.cshtml exists and contains `@model EditRecapViewModel`, `<i class="fas fa-book me-2"></i>`, `asp-action="EditRecap"`, `<hr />`, `d-flex justify-content-between`, `btn btn-secondary` Cancel with `fa-times`, and `btn btn-primary` Save with `fa-save`
    - EditRecap.cshtml does NOT contain `col-lg-4` (no tips sidebar), does NOT contain `btn-warning`, does NOT contain an inline `style="background-color` on the textarea
    - EditRecap.cshtml textarea has `rows="10"`
    - EditRecap.Mobile.cshtml exists and contains `quest-edit.mobile.css`, `quest-edit-card-mobile`, `asp-action="EditRecap"`, `d-flex gap-2`, `flex-fill`, `fa-times` Cancel, `btn btn-primary` Save
    - EditRecap.Mobile.cshtml textarea has `rows="6"`, and does NOT contain `justify-content-between`, `fa-arrow-left`, or "Back to Quest"
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds (Razor compiles against EditRecapViewModel)
  </acceptance_criteria>
  <done>Both EditRecap views compile, bind to EditRecapViewModel, submit to the EditRecap POST action, and follow the locked modern-card (desktop) / quest-edit-card-mobile (mobile) button-layout conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Details recap sections to read-only + entry-point button</name>
  <files>QuestBoard.Service/Views/QuestLog/Details.cshtml, QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/QuestLog/Details.cshtml (the file being modified; the current Session Recap block is lines 97-136)
    - QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml (the file being modified; the current Session Recap block is lines 91-124)
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-UI-SPEC.md ("View-by-View Contract" sections 1 and 2 — VERBATIM replacement markup)
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-CONTEXT.md (D-01 button lives inline in the recap section; D-02 dynamic Add/Edit label)
  </read_first>
  <action>
    In `Details.cshtml`, replace the entire Session Recap block (the `<div class="mb-4">` containing the `@if ((bool)ViewBag.CanEditRecap) { <form asp-action="UpdateRecap"...> } else { ...read-only... }` structure, currently lines 98-136) with the markup in UI-SPEC.md section 1: keep `<h5 class="text-white"><i class="fas fa-book me-2"></i>Session Recap</h5>`, then render the read-only display UNCONDITIONALLY — `@if (!string.IsNullOrWhiteSpace(Model.Quest.Recap)) { <div class="recap-display-box">@Model.Quest.Recap</div> } else { <p class="text-muted">No recap has been written for this quest yet.</p> }` — then append, INSIDE the same `<div class="mb-4">`, a `@if ((bool)ViewBag.CanEditRecap)` block containing an `<a href="@Url.Action("EditRecap", "QuestLog", new { id = Model.Quest.Id })" class="btn btn-primary mt-3">` whose label/icon switches on the same recap-empty condition: `fa-edit` + "Edit Recap" when the recap has content, `fa-plus` + "Add Recap" when empty (use `@:Edit Recap` / `@:Add Recap` literal syntax as shown). Remove the entire old inline `<form asp-action="UpdateRecap">` block. Touch nothing outside this Session Recap `<div class="mb-4">` — the Quick Actions sidebar, Quest Statistics, and all other sections stay exactly as they are (the sidebar's existing `Manage Quest` button under `CanEditRecap` is unrelated and must remain).

    In `Details.Mobile.cshtml`, apply the same restructuring to the Session Recap block (currently lines 92-124) using UI-SPEC.md section 2's mobile markup: outer `<div class="mb-0">`, `<h6><i class="fas fa-book me-2"></i>Session Recap</h6>`, unconditional read-only display with `<p class="mb-0 text-muted">` for the empty state, then the same conditional button but with `class="btn btn-primary w-100 mt-3"` (full-width `w-100` per the mobile convention). Remove the old inline `<form asp-action="UpdateRecap">` block. No CSS changes — `.recap-display-box` already styles correctly.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Details.cshtml no longer contains `asp-action="UpdateRecap"` (the inline edit form is removed)
    - Details.cshtml contains `Url.Action("EditRecap", "QuestLog"` inside an `@if ((bool)ViewBag.CanEditRecap)` block, with both `@:Edit Recap` (fa-edit) and `@:Add Recap` (fa-plus) branches
    - Details.cshtml renders `recap-display-box` / "No recap has been written for this quest yet." unconditionally (the read-only display is not gated behind `!CanEditRecap`)
    - Details.cshtml still contains the Quick Actions `Manage Quest` button and Quest Statistics section (unchanged)
    - Details.Mobile.cshtml no longer contains `asp-action="UpdateRecap"`, contains `Url.Action("EditRecap", "QuestLog"` with `btn btn-primary w-100`, both Add/Edit branches, and `<p class="mb-0 text-muted">` empty state
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds
  </acceptance_criteria>
  <done>Both Details views show the recap read-only for all users, with the DM/Admin entry-point button (dynamic Add/Edit label) linking to EditRecap; the inline UpdateRecap form is gone from both views.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the read-only Details + dedicated EditRecap flow</name>
  <files>QuestBoard.Service/Views/QuestLog/Details.cshtml, QuestBoard.Service/Views/QuestLog/Details.Mobile.cshtml, QuestBoard.Service/Views/QuestLog/EditRecap.cshtml, QuestBoard.Service/Views/QuestLog/EditRecap.Mobile.cshtml</files>
  <action>
    Pause for the developer to manually verify the recap read-only Details view and the new dedicated EditRecap page on both desktop and mobile, following the how-to-verify steps below. No code is written in this task — it gates the phase on human confirmation of visual/interaction correctness that the automated build and integration tests cannot fully assert.
  </action>
  <what-built>
    The recap flow is now split: `QuestLog/Details` (desktop + mobile) shows the recap read-only for everyone with an "Add Recap"/"Edit Recap" button for the DM/Admin, and clicking it opens the new dedicated `QuestLog/EditRecap` page (desktop + mobile) with Save Recap / Cancel buttons. Automated build + the Plan 01 EditRecap integration tests (403-for-Player, 200-for-Admin, redirect-on-POST) already pass; this checkpoint verifies the visual/interaction result the automation can't fully assert.
  </what-built>
  <how-to-verify>
    Run the app (`dotnet run --project QuestBoard.Service`) and, signed in as a DM or Admin, open a completed quest's log details page (`/QuestLog/Details/{id}`):
    1. Confirm the Session Recap section shows read-only text (or "No recap has been written for this quest yet.") — there is NO inline textarea on the Details page anymore.
    2. Confirm a button appears under the recap: "Add Recap" (with a + icon) when there is no recap yet, or "Edit Recap" (pencil icon) when a recap exists.
    3. Click the button → confirm it navigates to the new Edit Recap page rendered in the modern-card style, with the textarea prefilled with the current recap (if any), a "Save Recap" primary button on the right and a "Cancel" secondary button on the left.
    4. Edit the text and click Save Recap → confirm it returns to the Details page and the new text shows in the read-only box.
    5. Click Edit Recap again, change the text, click Cancel → confirm it returns to Details WITHOUT saving the change.
    6. Repeat steps 1-5 on a mobile viewport / device (the mobile Details and mobile EditRecap views): the button is full-width, the mobile edit card renders, Save/Cancel are side-by-side and full-width.
    7. (Optional) As a non-DM Player, confirm no edit button appears on Details, and visiting `/QuestLog/EditRecap/{id}` directly returns a 403 Forbidden.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms all 6 (7 optional) verification steps pass on both desktop and mobile, or reports issues to fix.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues to fix</resume-signal>
  <done>Developer has confirmed the read-only Details recap, the dynamic Add/Edit entry-point button, and the Save/Cancel navigation on the dedicated EditRecap page all work correctly on both desktop and mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → QuestLog/EditRecap view + form submit | The view exposes the edit affordance and posts recap content; authorization is enforced server-side by the Plan 01 actions, not by the view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Elevation of Privilege | Details entry-point button visibility | mitigate | Button only rendered under `@if ((bool)ViewBag.CanEditRecap)` (DM/Admin gate set server-side by the unchanged Details GET); a non-editor never sees the affordance, and even a hand-crafted GET to EditRecap is blocked 403 by Plan 01's server-side check — the view is defense-in-depth only, never the authority |
| T-53-05 | Tampering | EditRecap form POST (recap content) | mitigate | Razor `<form method="post">` emits the antiforgery token consumed by the POST action's `[ValidateAntiForgeryToken]` (Plan 01); no client-side authority is trusted |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure Razor view changes reusing existing Bootstrap/Font Awesome/project CSS; no `## Package Legitimacy Audit` required |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds (all four Razor views compile)
- `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~QuestLogControllerIntegrationTests"` — the Plan 01 EditRecap tests now fully pass including the `EditRecap_NonOwnerAdmin_ReturnsOk` content-contains "Save Recap" assertion (the view now exists)
- Human verification checkpoint confirms the read-only Details, the dynamic Add/Edit button, and the Save/Cancel navigation on both desktop and mobile
</verification>

<success_criteria>
- Details.cshtml and Details.Mobile.cshtml show the recap read-only for all users; no inline edit form remains
- The DM/Admin sees a dynamic "Add Recap"/"Edit Recap" button under the recap content linking to EditRecap
- EditRecap.cshtml and EditRecap.Mobile.cshtml render the modern-card / mobile-card edit form with Save Recap (primary) + Cancel (secondary) per D-03
- Save persists and returns to Details; Cancel discards and returns to Details
- Human verification approved on both desktop and mobile
</success_criteria>

<output>
Create `.planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-02-SUMMARY.md` when done
</output>
