---
phase: 50-fix-quest-edit-page-show-edit-button-for-campaign-quests-and
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "A failing integration test asserts the Campaign Manage page renders an Edit Quest link (D-01/D-02)"
    - "A failing integration test asserts the Campaign Manage page renders a Delete link wired to deleteQuest( (D-03)"
    - "A failing integration test asserts the Campaign Edit page hides Challenge Rating, Total Player Count, DM-Session checkbox, and Proposed Dates (D-04)"
    - "A regression test asserts the OneShot Edit page still shows all four fields (D-04 regression side)"
    - "A regression test asserts POST Edit with invalid ModelState for a Campaign quest returns 200 and does not throw (D-05 / Pitfall 3)"
    - "Mobile-user-agent variants exist for the Manage-buttons and Edit-field assertions"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs"
      provides: "Failing integration tests covering D-01 through D-05, desktop and mobile"
      contains: "SeedCampaignGroupAsync"
  key_links:
    - from: "QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs"
      to: "QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs"
      via: "SeedCampaignGroupAsync(factory.Services, groupId: 2) + CreateTestQuestAsync(groupId: 2)"
      pattern: "SeedCampaignGroupAsync"
    - from: "QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs"
      to: "QuestBoard.Service/Controllers/QuestBoard/QuestController.cs"
      via: "GET /Quest/Manage/{id} and GET/POST /Quest/Edit/{id} over HttpClient"
      pattern: "/Quest/(Manage|Edit)/"
---

<objective>
Author a new failing integration test file that pins every observable behavior this phase must deliver (D-01 through D-05), across desktop and mobile rendering, before any production markup or controller code is written. This is the Nyquist Wave-0 gate required by 50-VALIDATION.md.

Purpose: Every acceptance criterion in this phase (a button's presence, a field's absence, an exception-free POST) is source-verifiable via `HttpClient` + `content.Should().Contain(...)`/`NotContain(...)` against rendered HTML — no live browser needed. Writing these tests first guarantees Plans 02 and 03 have a red→green target and that the Pitfall-3 crash regression is caught mechanically.

Output: `QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs` — a class of `[Fact]` tests that FAIL against the current tree (Edit buttons absent, fields unconditional, POST-invalid path throws) and will PASS once Plans 02 and 03 land.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-fix-quest-edit-page-show-edit-button-for-campaign-quests-and/50-CONTEXT.md
@.planning/phases/50-fix-quest-edit-page-show-edit-button-for-campaign-quests-and/50-RESEARCH.md
@.planning/phases/50-fix-quest-edit-page-show-edit-button-for-campaign-quests-and/50-VALIDATION.md

# Existing test patterns to copy from (read these before writing)
@QuestBoard.IntegrationTests/Controllers/QuestCloseTests.cs
@QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs
@QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author Campaign/OneShot UI-parity integration tests for the Manage page (D-01, D-02, D-03)</name>
  <files>QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/QuestCloseTests.cs — the canonical pattern for Campaign-board integration tests: `ClearDatabaseAsync` + `SeedCampaignGroupAsync(factory.Services, groupId: 2)`, `CreateAuthenticatedClientWithUserAsync`, the `AddCampaignGroupMembershipAsync` helper (DM must hold a DungeonMaster GroupRole membership row in group 2), `CreateTestQuestAsync(..., groupId: 2)`, and the `factory.TestGroupContext.ActiveGroupId = 2;` set-inside-try / reset-in-finally pattern.
    - QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs — signatures of `SeedCampaignGroupAsync` (group 2 => BoardType.Campaign), `CreateTestQuestAsync` (default groupId 1 => OneShot), no OneShot-specific seed needed since group 1 is the default OneShot board.
    - QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs — the `MobileUserAgent` constant and the `request.Headers.TryAddWithoutValidation("User-Agent", MobileUserAgent)` pattern for triggering mobile view rendering, plus how the Authorization header is carried onto the manual `HttpRequestMessage`.
    - .planning/phases/50-.../50-CONTEXT.md — D-01/D-02 (Edit Quest link before Close/Reopen), D-03 (Delete link after, wired to deleteQuest(id)).
  </read_first>
  <action>
    Create the file `QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs` in namespace `QuestBoard.IntegrationTests.Controllers`, as a class `QuestCampaignUiParityTests(WebApplicationFactoryBase factory) : IClassFixture<WebApplicationFactoryBase>`, following QuestCloseTests.cs verbatim for structure (primary-constructor factory, `using QuestBoard.Domain.Enums;`, `using QuestBoard.IntegrationTests.Helpers;`).

    Copy the private `AddCampaignGroupMembershipAsync(int userId, GroupRole groupRole)` helper from QuestCloseTests.cs into this class (it adds a UserGroupEntity for groupId 2). Reuse `MobileUserAgent` — either copy the constant string from MobileViewsTests.cs or add a private `const string MobileUserAgent` field with the same value; note it must be a real mobile UA that the MobileDetectionMiddleware recognizes.

    Add these `[Fact]` tests. Each arranges a Campaign quest owned by an authenticated DM in group 2 (seed campaign group, create authenticated DM client, add group-2 DM membership, create the quest with `groupId: 2`), sets `factory.TestGroupContext.ActiveGroupId = 2;` inside a try/finally that resets to 1, GETs `/Quest/Manage/{quest.Id}`, reads `await response.Content.ReadAsStringAsync(...)` into `content`, and asserts on the raw HTML:

    - `CampaignManage_Desktop_RendersEditQuestLink` — asserts `content.Should().Contain("Edit Quest")` AND `content.Should().Contain($"/Quest/Edit/{quest.Id}")` (the Edit link target). This is D-01/D-02.
    - `CampaignManage_Desktop_RendersDeleteLinkWiredToDeleteQuest` — asserts `content.Should().Contain($"deleteQuest({quest.Id})")`. This is D-03. (The desktop Delete link label is "Delete"; asserting on the `deleteQuest(<id>)` onclick handler is the unambiguous D-03 signal since the word "Delete" already appears elsewhere in the rendered page.)
    - `CampaignManage_Mobile_RendersEditQuestAndDeleteQuestLinks` — same arrange, but issue the GET via a manual `HttpRequestMessage` with the `User-Agent: MobileUserAgent` header and the DM's Authorization header (per MobileViewsTests.cs). Assert `content.Should().Contain("Edit Quest")` AND `content.Should().Contain($"/Quest/Edit/{quest.Id}")` AND `content.Should().Contain($"deleteQuest({quest.Id})")`. This is the mobile parity for D-01/D-03.

    These tests MUST FAIL against the current tree (the Campaign Manage action row today has only Close/Reopen + Refresh Data — no Edit link, no deleteQuest call). Do not add markup to make them pass — that is Plan 02's job.
  </action>
  <acceptance_criteria>
    - behavior: The file compiles and the new `[Fact]` tests are discovered by the test runner.
    - test-command: `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestCampaignUiParity"` runs the tests and the three Manage tests FAIL (assertions unmet) — a failing run here is the CORRECT wave-0 outcome, not a defect.
    - source: The class contains `SeedCampaignGroupAsync`, `ActiveGroupId = 2`, `/Quest/Manage/`, `content.Should().Contain("Edit Quest")`, and `deleteQuest(` string references.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestCampaignUiParity" 2>&1 | grep -Ev '^#' | grep -Eq 'Failed|Passed'</automated>
  </verify>
  <done>File exists, compiles, and the three Manage-page tests are discovered and FAIL because current markup lacks the Edit/Delete affordances. Compilation succeeds (no unresolved helpers).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author Edit-page field-visibility and Edit-POST no-throw tests (D-04, D-05 / Pitfall 3)</name>
  <files>QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/QuestCloseTests.cs — specifically `Campaign_Create_WithNoProposedDates_Persists` (posts form data to a Campaign-context action) and `OneShot_Create_WithNoProposedDates_ReRendersWithValidationError` (asserts an invalid POST returns 200/`HttpStatusCode.OK` and re-renders rather than throwing) — the exact shape for the D-05 POST-invalid-ModelState regression test.
    - .planning/phases/50-.../50-RESEARCH.md — Pitfall 3 / Assumption A3 section: `Edit` POST's `ModelState.IsValid` failure path returns `View(viewModel)` and today never sets `ViewBag.BoardType`; once `Edit.cshtml` reads `(BoardType)ViewBag.BoardType` at file top (Plan 03), an invalid Edit submission would throw `InvalidCastException`. The regression test must submit an invalid Edit form (empty Title) for a Campaign quest and assert a 200 with rendered validation errors, NOT a 500/exception.
    - .planning/phases/50-.../50-VALIDATION.md — the D-04 test-map row: Campaign Edit `NotContain` for all four field labels; OneShot Edit `Contain` for all four (regression side). Group 1 is the default OneShot board, so the OneShot side needs no campaign seed.
    - QuestBoard.Service/Views/Quest/Edit.cshtml — the exact on-screen label strings to assert on: "Challenge Rating", "Total Player Count", "Dungeon Master Session Only", "Proposed Dates" (the Proposed Dates label reads "Proposed Dates & Times").
  </read_first>
  <action>
    Extend `QuestCampaignUiParityTests` with these `[Fact]` tests:

    - `CampaignEdit_Desktop_HidesFourOneShotFields` — arrange a Campaign quest (group 2) owned by the authenticated DM; GET `/Quest/Edit/{quest.Id}` with `ActiveGroupId = 2`; assert `content.Should().NotContain("Challenge Rating")`, `NotContain("Total Player Count")`, `NotContain("Dungeon Master Session Only")`, and `NotContain("Proposed Dates")`. This is D-04 (Campaign side). It MUST FAIL today because Edit.cshtml renders all four unconditionally.
    - `OneShotEdit_Desktop_ShowsFourFields` — arrange a OneShot quest (default `groupId: 1`, no campaign seed) owned by the DM; GET `/Quest/Edit/{quest.Id}` with `ActiveGroupId = 1`; assert `content.Should().Contain("Challenge Rating")`, `Contain("Total Player Count")`, `Contain("Dungeon Master Session Only")`, and `Contain("Proposed Dates")`. This is the D-04 regression side — it should PASS today and must keep passing after Plan 03. (Note: this test guards against Plan 03 over-hiding fields for OneShot quests.)
    - `CampaignEdit_InvalidModelState_Returns200_DoesNotThrow` — arrange a Campaign quest (group 2) owned by the DM; obtain the antiforgery token for the Edit form (follow the AntiForgeryHelper / existing POST-with-token pattern used elsewhere in the integration suite — inspect how other POST tests acquire the token); POST `/Quest/Edit/{quest.Id}` with form data that has an EMPTY `Quest.Title` (invalid) plus the hidden `Id` field and antiforgery token, with `ActiveGroupId = 2`; assert `response.StatusCode.Should().Be(HttpStatusCode.OK)` (form re-renders with validation errors) and NOT a 5xx. This is the D-05 / Pitfall 3 regression guard — the single most important test in the phase. It will pass today (the POST path currently works) but WILL start throwing once Plan 03 makes Edit.cshtml read ViewBag.BoardType unconditionally — UNLESS Plan 03 also sets ViewBag.BoardType on the POST failure path. Keeping this test green through Plan 03 proves the Pitfall-3 fix.
    - `CampaignEdit_Mobile_HidesFourOneShotFields` — same as the Campaign desktop hide test but with the `User-Agent: MobileUserAgent` header; assert `NotContain` for all four field labels against the mobile Edit view. This is D-04 mobile parity.

    Where you need an antiforgery token for the POST test, mirror the approach already used by other POST integration tests in this suite (see AntiForgeryHelper.cs and how QuestFinalizeTests / QuestControllerIntegrationTests_Comprehensive obtain and submit the token) rather than inventing a new mechanism. Do NOT weaken `[ValidateAntiForgeryToken]` handling.
  </action>
  <acceptance_criteria>
    - behavior: The four new Edit tests are discovered and run.
    - test-command: `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestCampaignUiParity"` — `CampaignEdit_Desktop_HidesFourOneShotFields` and `CampaignEdit_Mobile_HidesFourOneShotFields` FAIL (fields currently unconditional); `OneShotEdit_Desktop_ShowsFourFields` and `CampaignEdit_InvalidModelState_Returns200_DoesNotThrow` PASS against the current tree.
    - source: The class contains `NotContain("Challenge Rating")`, `NotContain("Proposed Dates")`, a POST to `/Quest/Edit/` with an empty Title, and `HttpStatusCode.OK` assertion on that POST.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestCampaignUiParity" 2>&1 | grep -Ev '^#' | grep -Eq 'Failed|Passed'</automated>
  </verify>
  <done>All Edit tests are discovered and compile. The two Campaign field-hide tests FAIL (fields still unconditional); the OneShot-shows-fields and Campaign-invalid-POST-returns-200 tests PASS. The failing tests form the red target for Plan 03; the passing invalid-POST test is the Pitfall-3 regression guard that Plan 03 must keep green.</done>
</task>

</tasks>

<verification>
- `dotnet build QuestBoard.IntegrationTests` succeeds (new test file compiles, all helpers resolve).
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestCampaignUiParity"` runs; the Manage-Edit-link, Manage-Delete, Campaign-Edit-hide (desktop + mobile) tests FAIL by assertion (correct wave-0 red state); the OneShot-shows-fields and Campaign-invalid-POST-returns-200 tests PASS.
- No production `.cshtml` or `.cs` files under `QuestBoard.Service/` are modified by this plan (test-only).
</verification>

<success_criteria>
- New file `QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs` exists and compiles.
- Tests exist covering every row of 50-VALIDATION.md's Per-Task Verification Map: D-01/D-02 (Edit link), D-03 (Delete/deleteQuest), D-04 (hide 4 fields Campaign, show 4 fields OneShot), D-05/Pitfall-3 (invalid POST returns 200), plus mobile-user-agent variants of the Manage and Edit-hide assertions.
- The as-yet-unimplemented behaviors fail; the regression-guard behaviors pass.
</success_criteria>

<output>
Create `.planning/phases/50-fix-quest-edit-page-show-edit-button-for-campaign-quests-and/50-01-SUMMARY.md` when done, recording the exact test method names created (Plans 02 and 03 reconcile 50-VALIDATION.md's task IDs against these names) and the pass/fail state of each at wave-0 close.
</output>

## Artifacts this phase produces

This plan creates exactly ONE new symbol/file (test-only — nothing new in the production `QuestBoard.Service`/`Domain`/`Repository` projects):

- `QuestBoard.IntegrationTests/Controllers/QuestCampaignUiParityTests.cs` — new xUnit integration test class `QuestCampaignUiParityTests` with the `[Fact]` methods listed above and a copied private `AddCampaignGroupMembershipAsync` helper.

No new production types, controllers, services, ViewModels, views, or migrations. All production changes in Plans 02/03 are edits to existing files.
