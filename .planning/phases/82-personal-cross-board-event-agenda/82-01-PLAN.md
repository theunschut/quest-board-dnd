---
phase: 82-personal-cross-board-event-agenda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/82-personal-cross-board-event-agenda/82-VALIDATION.md
autonomous: true
requirements:
  - EVTAGENDA-01
  - EVTAGENDA-02
  - EVTAGENDA-03
  - EVTAGENDA-04
  - EVTAGENDA-05
  - EVTAGENDA-06
  - EVTAGENDA-07
  - EVTAGENDA-08
  - EVTAGENDA-09
  - EVTAGENDA-10

must_haves:
  truths:
    - "Every Phase 82 behaviour has a requirement ID that can be traced from REQUIREMENTS.md to a plan and from a plan to an automated command."
    - "The requirements-coverage tooling can resolve Phase 82 to a real ID list instead of a TBD placeholder."
    - "The phase's validation contract names the exact task that proves each requirement, so a gap is visible before execution rather than after."
  artifacts:
    - ".planning/REQUIREMENTS.md contains a `### Personal Cross-Board Event Agenda` section with EVTAGENDA-01 through EVTAGENDA-10"
    - ".planning/REQUIREMENTS.md Traceability table contains ten Phase 82 rows"
    - ".planning/ROADMAP.md `## Requirements Coverage` table contains ten EVTAGENDA rows mapped to Phase 82"
    - "82-VALIDATION.md Per-Task Verification Map references real 82-NN task ids and its sign-off boxes are ticked"
  key_links:
    - "REQUIREMENTS.md ID list -> ROADMAP.md Requirements Coverage table -> each plan's `requirements` frontmatter"
    - "82-VALIDATION.md Per-Task Verification Map -> the `<automated>` command on the task it names"
---

<objective>
Mint the `EVTAGENDA-*` requirement family for Phase 82 and wire it through every tracking
surface, then complete the phase's validation contract so each requirement names the task
that proves it.

Phase 82 currently has no requirement IDs at all: `.planning/ROADMAP.md` says
`**Requirements**: TBD` and `.planning/REQUIREMENTS.md` has no Phase 82 section. Without
this plan, the requirements-coverage gate has nothing to check and `82-VALIDATION.md`'s
Per-Task Verification Map stays written against a proposal rather than against real tasks.

Purpose: make the rest of the phase traceable and auditable.
Output: ten requirement IDs defined, mapped, and bound to concrete automated commands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-personal-cross-board-event-agenda/82-VALIDATION.md
@.planning/phases/82-personal-cross-board-event-agenda/82-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan creates no source symbols. It creates the following documentation identifiers,
which later plans and reviews will reference:

- Requirement IDs `EVTAGENDA-01` … `EVTAGENDA-10` (new ID family, new prefix)
- A `### Personal Cross-Board Event Agenda` heading in `.planning/REQUIREMENTS.md`
- Ten new rows in the `.planning/REQUIREMENTS.md` `## Traceability` table
- Ten new rows in the `.planning/ROADMAP.md` `## Requirements Coverage` table
- A completed Per-Task Verification Map and Validation Sign-Off in `82-VALIDATION.md`

The full list of source symbols this *phase* produces is repeated in every plan; see
`82-02-PLAN.md` for the canonical list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mint EVTAGENDA-01..10 into REQUIREMENTS.md</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — read the whole file. The `### Calendar Events — Availability Overview` section (around line 70) is the exact format to follow for the new section, and the `## Traceability` table plus the `**Coverage:**` counter block at the end of the file both need new entries.
    - `.planning/phases/82-personal-cross-board-event-agenda/82-RESEARCH.md` § `## Phase Requirements` — the nine-ID proposal these ten are derived from, including the reasoning for a new `EVTAGENDA-*` prefix rather than extending `EVTVIEW-*`.
    - `.planning/phases/82-personal-cross-board-event-agenda/82-CONTEXT.md` § `<decisions>` — the locked decisions each requirement restates.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
Add a new section `### Personal Cross-Board Event Agenda` to `.planning/REQUIREMENTS.md`,
placed immediately after the existing `### Calendar Events — Availability Overview` section
and before `### Link Previews — Foundation and Quests`, so the events family stays
contiguous.

Use the file's existing item format exactly: `- [ ] **ID**: description`, unchecked, one
line per requirement, no trailing period. Write these ten verbatim:

- `EVTAGENDA-01`: The agenda lists every upcoming, non-cancelled event across every board the viewer is a member of, one row per event, ordered chronologically and interleaved across boards, whether or not a signup row already exists for the viewer
- `EVTAGENDA-02`: Every agenda row names the board its event belongs to
- `EVTAGENDA-03`: Every agenda row carries the viewer's own availability and the event's complete roster, using the same five-state cell vocabulary the board-scoped surfaces already use
- `EVTAGENDA-04`: A board-selection filter narrows the agenda, defaults to all of the viewer's boards, is remembered for the session, and is applied before the next-N window is taken
- `EVTAGENDA-05`: The agenda is reachable from the user menu beside Switch Group on both the desktop and the mobile layout, for every authenticated user, with no board-type gate and no board-count gate
- `EVTAGENDA-06`: Acting on a row whose event is on a board other than the viewer's active board prompts to switch before continuing to that event's details; a row already on the active board goes straight through
- `EVTAGENDA-07`: A board the viewer has left never appears on their agenda on the very next request, because membership is read fresh on every request rather than from session or claims
- `EVTAGENDA-08`: A SuperAdmin's agenda is scoped by their own board memberships exactly like any other user's, with no wider cross-board read
- `EVTAGENDA-09`: The agenda never shows another board's events or members, and the board filter can only narrow the viewer's own memberships, never widen them
- `EVTAGENDA-10`: The agenda page loads for an authenticated user who has no active board selected, rather than diverting them to the board picker

`EVTAGENDA-01` … `EVTAGENDA-09` are the research proposal, reworded for consistency with the
existing entries. `EVTAGENDA-10` is an addition made during planning: `GroupSessionMiddleware`
redirects any authenticated GET with no active board to the board picker, which would make the
agenda unreachable in exactly the situation the phase boundary says it must cover. Nothing in
the source artifacts covers that behaviour, so it gets its own ID.

Then append ten rows to the `## Traceability` table, after the last `LINKCARD-06` row, in the
existing `| ID | Phase | Status |` format with `Phase 82` and `Not started`.

Finally update the `**Coverage:**` block at the end of the file: the two counts currently
reading 50 become 60, and `Mapped to phases: 50/50` becomes `Mapped to phases: 60/60`.

Write the file with CRLF line endings — this repository uses CRLF and REQUIREMENTS.md is
already CRLF.
  </action>

  <acceptance_criteria>
    - `grep -c 'EVTAGENDA-' .planning/REQUIREMENTS.md` outputs `20` (ten definitions plus ten traceability rows)
    - `.planning/REQUIREMENTS.md` contains the literal heading `### Personal Cross-Board Event Agenda`
    - `grep -c '^| EVTAGENDA-0\?[0-9]* | Phase 82 | Not started |' .planning/REQUIREMENTS.md` outputs `10`
    - `.planning/REQUIREMENTS.md` contains `- [ ] **EVTAGENDA-10**:` and `- [ ] **EVTAGENDA-01**:`
    - `.planning/REQUIREMENTS.md` contains `- v1 requirements: 60 total` and `- Mapped to phases: 60/60`
    - `file .planning/REQUIREMENTS.md` still reports `CRLF line terminators`
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Repos/quest-board" &amp;&amp; test "$(grep -c 'EVTAGENDA-' .planning/REQUIREMENTS.md)" = "20" &amp;&amp; grep -q '### Personal Cross-Board Event Agenda' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Mapped to phases: 60/60' .planning/REQUIREMENTS.md &amp;&amp; echo OK</automated>
  </verify>

  <done>
    Ten EVTAGENDA requirements are defined in their own section, listed in the traceability
    table against Phase 82, and the coverage counters reconcile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Map EVTAGENDA-01..10 into the ROADMAP Requirements Coverage table</name>

  <read_first>
    - `.planning/ROADMAP.md` — the `## Requirements Coverage` table near the end of the file (it currently ends with the `LINKCARD-*` rows), and the `### Phase 82: Personal Cross-Board Event Agenda` entry, whose `**Requirements**:` line is set by the planner rather than by this task.
    - `.planning/REQUIREMENTS.md` — as written by Task 1, so the IDs match exactly.
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
Append ten rows to the `## Requirements Coverage` table in `.planning/ROADMAP.md`, after the
last existing row, in the file's exact `| Requirement | Phase |` two-column format:

`| EVTAGENDA-01 | Phase 82 |` through `| EVTAGENDA-10 | Phase 82 |`, one per line, in
ascending ID order.

Touch nothing else in ROADMAP.md. In particular, do **not** edit the
`### Phase 82: Personal Cross-Board Event Agenda` entry — its `**Requirements**:` and
`**Plans**:` lines are owned by the planner and are already filled in. This task owns the
coverage table section only, so the two edits cannot collide.

`.planning/ROADMAP.md` uses LF line endings, unlike `.planning/REQUIREMENTS.md` and the source
tree, which are CRLF. Match the file you are editing rather than applying one rule to both.
  </action>

  <acceptance_criteria>
    - `grep -c '^| EVTAGENDA-0\?[0-9]* | Phase 82 |$' .planning/ROADMAP.md` outputs `10`
    - `.planning/ROADMAP.md` contains `| EVTAGENDA-01 | Phase 82 |` and `| EVTAGENDA-10 | Phase 82 |`
    - The Phase 82 entry no longer carries the requirements placeholder: `awk '/^### Phase 82:/,/^### Phase 83:|^## Phase Ordering Rationale/' .planning/ROADMAP.md | grep -c 'Requirements\*\*: TBD'` outputs `0`. Two later phases legitimately still carry that placeholder, so the check is scoped to the Phase 82 entry rather than to the whole file
    - Every ID present in the ROADMAP coverage table is also present in `.planning/REQUIREMENTS.md`: `comm -13 <(grep -o 'EVTAGENDA-[0-9]*' .planning/REQUIREMENTS.md | sort -u) <(grep -o 'EVTAGENDA-[0-9]*' .planning/ROADMAP.md | sort -u)` produces no output
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Repos/quest-board" &amp;&amp; test "$(grep -c '| EVTAGENDA-' .planning/ROADMAP.md)" -ge "10" &amp;&amp; test -z "$(comm -13 <(grep -o 'EVTAGENDA-[0-9]*' .planning/REQUIREMENTS.md | sort -u) <(grep -o 'EVTAGENDA-[0-9]*' .planning/ROADMAP.md | sort -u))" &amp;&amp; echo OK</automated>
  </verify>

  <done>
    The roadmap's coverage table maps all ten EVTAGENDA requirements to Phase 82, and no ID
    exists in the roadmap that is missing from REQUIREMENTS.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Complete the phase validation contract in 82-VALIDATION.md</name>

  <read_first>
    - `.planning/phases/82-personal-cross-board-event-agenda/82-VALIDATION.md` — read the whole file. The Per-Task Verification Map, the Wave 0 Requirements checklist, and the Validation Sign-Off block are all edited by this task; the Sampling Rate, Manual-Only Verifications, and phase-gate static audit sections are already correct and must be left as they are.
    - `.planning/phases/82-personal-cross-board-event-agenda/82-02-PLAN.md`, `82-03-PLAN.md`, `82-04-PLAN.md`, `82-05-PLAN.md`, `82-06-PLAN.md` — the task names and `<automated>` commands referenced in the map below must match what those plans actually carry.
  </read_first>

  <files>.planning/phases/82-personal-cross-board-event-agenda/82-VALIDATION.md</files>

  <action>
Rewrite the `## Per-Task Verification Map` table so it names real task ids and adds the
Status column the file's own note asks for. Replace the whole table (keep the explanatory
blockquote above it, but drop its now-stale sentence about the planner still having to mint
the IDs) with a six-column table
`| Requirement | Behavior | Task(s) | Test Type | Automated Command | Status |` carrying one
row per requirement:

- `EVTAGENDA-01` — cross-board rows start from events, not signups; an event with no signup row for the viewer still appears — tasks `82-02 T2`, `82-02 T3`, `82-03 T3` — integration — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~AgendaControllerIntegrationTests"` — ⬜
- `EVTAGENDA-02` — every row names its board, resolved from the membership read rather than an event-to-group include — tasks `82-03 T2`, `82-03 T3`, `82-04 T1` — integration — same command — ⬜
- `EVTAGENDA-03` — own availability plus the full roster on every row, rendered through the shared cell partial — tasks `82-02 T3`, `82-03 T3`, `82-04 T1` — integration — same command plus `--filter "FullyQualifiedName~AgendaMobileRenderTests"` — ⬜
- `EVTAGENDA-04` — filter applied before the take, defaults to all, remembered for the session — tasks `82-03 T2`, `82-06 T3` — integration — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~Agenda"` — ⬜
- `EVTAGENDA-05` — nav entry present for every authenticated user including when the board type is unresolved — task `82-05 T3` — integration — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~LayoutNavigationTests"` — ⬜
- `EVTAGENDA-06` — switch prompt posts to the existing board-selection action with a local return url; an active-board row skips the prompt — tasks `82-03 T3`, `82-04 T1`, `82-05 T2` — integration — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~Agenda"` — ⬜
- `EVTAGENDA-07` — a left board disappears on the next request — task `82-06 T2` — integration — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~AgendaTenantIsolationTests"` — ⬜
- `EVTAGENDA-08` — SuperAdmin scoped by their own memberships, empty state with none — tasks `82-02 T3`, `82-03 T3`, `82-06 T2` — integration + unit — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~Agenda"` and `dotnet test QuestBoard.UnitTests/QuestBoard.UnitTests.csproj --filter "FullyQualifiedName~CrossBoardAgenda"` — ⬜
- `EVTAGENDA-09` — the four-case isolation suite: non-member board absent; two joined boards both present and a third absent; left board gone; filter cannot widen — tasks `82-02 T2`, `82-02 T3`, `82-06 T1`, `82-06 T2` — integration — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~AgendaTenantIsolationTests"` — ⬜
- `EVTAGENDA-10` — the page loads with no active board instead of redirecting to the picker — tasks `82-03 T2`, `82-03 T3` — integration — `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~AgendaControllerIntegrationTests"` — ⬜

Keep the status-legend line under the table.

In `## Wave 0 Requirements`, keep the compiled-language note as written and tick each checklist
item, appending the owning task in brackets: the agenda controller/integration test class is
`82-03 T3` (extended by `82-06 T3`); the cross-board tenant isolation class is `82-06 T1`/`T2`;
the leave-a-board helper is `82-06 T2`; the LayoutNavigationTests extension is `82-05 T3`; the
membership-set intersection unit test is `82-02 T3`; the framework-install line stays ticked
with no owner.

In `## Validation Sign-Off`, tick all seven boxes and replace the approval line with
`**Approval:** signed by planner — mapped to 82-01 … 82-06 task ids`.

Set `nyquist_compliant: true` in the frontmatter. Leave `status: draft` and `wave_0_complete`
alone — `wave_0_complete` is flipped by execution, not by planning.

Write with CRLF line endings.
  </action>

  <acceptance_criteria>
    - `grep -c 'EVTAGENDA-' .planning/phases/82-personal-cross-board-event-agenda/82-VALIDATION.md` outputs at least `10`
    - The file contains `nyquist_compliant: true`
    - The file contains the task references `82-02 T2`, `82-03 T3`, `82-05 T3`, `82-06 T1`, and `82-06 T2`
    - `grep -c '^- \[ \]' .planning/phases/82-personal-cross-board-event-agenda/82-VALIDATION.md` outputs `0` — every checklist box in the file is ticked
    - The file contains `**Approval:** signed by planner`
    - The Per-Task Verification Map header row contains a `Status` column: the file contains `| Requirement | Behavior | Task(s) | Test Type | Automated Command | Status |`
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Repos/quest-board" &amp;&amp; V=.planning/phases/82-personal-cross-board-event-agenda/82-VALIDATION.md &amp;&amp; grep -q 'nyquist_compliant: true' "$V" &amp;&amp; test "$(grep -c '^- \[ \]' "$V")" = "0" &amp;&amp; grep -q '82-06 T1' "$V" &amp;&amp; grep -q 'Approval:\*\* signed by planner' "$V" &amp;&amp; echo OK</automated>
  </verify>

  <done>
    82-VALIDATION.md names a real task for every requirement, every checklist box is ticked,
    and the frontmatter declares Nyquist compliance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → executor agents | These files are the instruction source for later plans; a wrong or missing requirement here silently removes a control from the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-82-01-01 | Repudiation | Requirement traceability | medium | mitigate | Every EVTAGENDA id appears in REQUIREMENTS.md, the ROADMAP coverage table, and at least one plan's `requirements` frontmatter; Task 2's acceptance criterion diffs the two ID sets so an id can never exist in one place only |
| T-82-01-02 | Tampering | Validation contract | medium | mitigate | The Per-Task Verification Map binds each requirement to a named task and a runnable command, so a later plan cannot quietly drop the isolation suite without leaving an unmapped row |
| T-82-01-03 | Information Disclosure | — | low | accept | This plan writes no code and touches no runtime surface; it cannot itself disclose data |
| T-82-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package manager runs in this phase at all — `82-RESEARCH.md` § Package Legitimacy Audit records "not applicable, this phase installs no new dependency". No install task exists in any Phase 82 plan, so there is nothing to gate |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `82-VALIDATION.md` all agree on the same ten ids.
- No ID appears in one tracking file and not the others.
- Every plan in this phase declares a non-empty `requirements` list drawn only from these ten ids.
</verification>

<success_criteria>
- Ten EVTAGENDA requirements defined, mapped to Phase 82 in both tracking files, and bound to a runnable command each.
- `82-VALIDATION.md` carries real task ids, a ticked sign-off, and `nyquist_compliant: true`.
- No source file is touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/82-personal-cross-board-event-agenda/82-01-SUMMARY.md` when done
</output>
