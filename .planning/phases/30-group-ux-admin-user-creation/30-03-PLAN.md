---
phase: 30-group-ux-admin-user-creation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/ViewModels/AdminViewModels/CreateUserViewModel.cs
  - QuestBoard.Service/Controllers/Admin/AdminController.cs
  - QuestBoard.Service/Views/Admin/CreateUser.cshtml
  - QuestBoard.Service/Views/Admin/CreateUser.Mobile.cshtml
  - QuestBoard.Repository/UserRepository.cs
autonomous: true
requirements: [MGMT-07, MGMT-08, REG-02, REG-03]
must_haves:
  truths:
    - "A group admin can open a Create User form and submit email, display name, password, and group role"
    - "Creating a user assigns them to the admin's active group with the chosen GroupRole"
    - "Creating a user enqueues the existing email confirmation job"
    - "AdminController.Users no longer uses a hardcoded group fallback"
    - "GetAllPlayers and GetAllDungeonMasters no longer use a hardcoded group fallback"
    - "Promote/demote within a group works against the active group with no hardcoded fallback"
  artifacts:
    - path: "QuestBoard.Service/Controllers/Admin/AdminController.cs"
      provides: "CreateUser GET/POST; ?? 1 fallback removed from Users()"
      contains: "CreateUser"
    - path: "QuestBoard.Service/ViewModels/AdminViewModels/CreateUserViewModel.cs"
      provides: "Create user form binding (Email, Name, Password, GroupRole)"
      contains: "GroupRole"
    - path: "QuestBoard.Service/Views/Admin/CreateUser.cshtml"
      provides: "Create user form (desktop)"
      contains: "asp-action=\"CreateUser\""
    - path: "QuestBoard.Repository/UserRepository.cs"
      provides: "GetAllPlayers/GetAllDungeonMasters without ?? 1 fallback"
  key_links:
    - from: "QuestBoard.Service/Controllers/Admin/AdminController.cs"
      to: "QuestBoard.Domain/Interfaces/IUserService.cs"
      via: "userService.CreateAsync then SetGroupRoleAsync(userId, activeGroupId, GroupRole)"
      pattern: "SetGroupRoleAsync"
    - from: "QuestBoard.Service/Controllers/Admin/AdminController.cs"
      to: "QuestBoard.Service/Jobs/ConfirmationEmailJob.cs"
      via: "jobClient.Enqueue<ConfirmationEmailJob>(...) after account creation"
      pattern: "Enqueue<ConfirmationEmailJob>"
---

<objective>
Add admin-only user creation and remove the temporary hardcoded group fallbacks.

Purpose: REG-01 removed public sign-up (plan 30-02); MGMT-07/REG-02/REG-03 require a group admin to create accounts that land in the admin's active group, with the existing email-confirmation flow. D-17 (locked in STATE.md) requires removing the `?? 1` group fallbacks introduced in Phases 28/29 now that the picker guarantees the session group is set — without removal, group isolation silently breaks for multi-group deployments. MGMT-08 (promote/demote) is already implemented by existing AdminController actions; removing `?? 1` makes them correct per-group.

Output: `CreateUserViewModel`; `AdminController.CreateUser` GET + POST; `CreateUser.cshtml` + `.Mobile.cshtml`; `?? 1` removed from `AdminController.Users()` and from `UserRepository.GetAllDungeonMasters` / `GetAllPlayers`.

Implements D-10, D-11, D-12, D-13, D-17, D-18 — covers MGMT-07, MGMT-08, REG-02, REG-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-group-ux-admin-user-creation/30-CONTEXT.md
@.planning/phases/30-group-ux-admin-user-creation/30-PATTERNS.md
@.planning/phases/30-group-ux-admin-user-creation/30-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
**Plan 30-03 creates/modifies these symbols:**

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `QuestBoard.Service.ViewModels.AdminViewModels.CreateUserViewModel` | class | Email, Name, Password, GroupRole (default Player) with Data Annotations |
| `AdminController.CreateUser(int)` GET | action | returns empty `CreateUserViewModel` view |
| `AdminController.CreateUser(CreateUserViewModel)` POST | action | CreateAsync → SetGroupRoleAsync → Enqueue ConfirmationEmailJob → TempData → RedirectToAction(Users) |
| `AdminController.Users()` | action (modified) | `?? 1` removed; null-guard redirect added |
| `Views/Admin/CreateUser.cshtml` | view | create form (desktop) |
| `Views/Admin/CreateUser.Mobile.cshtml` | view | create form (mobile) |
| `UserRepository.GetAllDungeonMasters` / `GetAllPlayers` | method (modified) | `?? 1` removed |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CreateUserViewModel and Create User views (desktop + mobile)</name>
  <files>QuestBoard.Service/ViewModels/AdminViewModels/CreateUserViewModel.cs, QuestBoard.Service/Views/Admin/CreateUser.cshtml, QuestBoard.Service/Views/Admin/CreateUser.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/ViewModels/AccountViewModels/RegisterViewModel.cs (analog ViewModel — Data Annotation patterns for Email/Name/Password)
    - QuestBoard.Service/Views/Admin/EditUser.cshtml (exact analog for the modern-card form layout + button row + Back to Users)
    - QuestBoard.Service/Views/Account/Login.Mobile.cshtml (account-card-mobile wrapper for the mobile form)
    - QuestBoard.Domain/Enums/GroupRole.cs (enum values Player / DungeonMaster / Admin for the dropdown)
    - .planning/phases/30-group-ux-admin-user-creation/30-UI-SPEC.md (§View Inventory #3 and #4 — exact field list, button copy, icons, colors)
    - .planning/phases/30-group-ux-admin-user-creation/30-PATTERNS.md (§ViewModels/AdminViewModels/CreateUserViewModel.cs and §Views/Admin/CreateUser.cshtml)
  </read_first>
  <action>
    Create `CreateUserViewModel` in namespace `QuestBoard.Service.ViewModels.AdminViewModels` with usings `QuestBoard.Domain.Enums` and `System.ComponentModel.DataAnnotations`. Properties: `Email` (`[Required][EmailAddress][Display(Name = "Email")]`), `Name` (`[Required][StringLength(100)][Display(Name = "Display Name")]`), `Password` (`[Required][StringLength(100, MinimumLength = 8)][DataType(DataType.Password)][Display(Name = "Password")]`), `GroupRole` (`[Display(Name = "Group Role")]`, type `GroupRole`, default `GroupRole.Player`). All string properties initialized to `string.Empty`.

    Create `Views/Admin/CreateUser.cshtml` typed `@model CreateUserViewModel` using `_Layout.cshtml` (standard layout — admin is in normal group context). Set `ViewData["Title"] = "Create New User"`. Use the EditUser modern-card structure centered in `col-md-6`: header `<h2 class="mb-0"><i class="fas fa-user-plus text-warning me-2"></i>Create New User</h2>`. Form `asp-action="CreateUser" method="post"` with `<div asp-validation-summary="ModelOnly" class="text-danger mb-3">`. Fields: Email (`<input asp-for="Email" type="email" class="form-control">`), Name (`<input asp-for="Name" class="form-control">`), Password (`<input asp-for="Password" type="password" class="form-control">`), and a Group Role select `<select asp-for="GroupRole" asp-items="Html.GetEnumSelectList<GroupRole>()" class="form-select">`. Each field has a `form-label` label and an `asp-validation-for` span. End with `<hr>` then a `d-flex justify-content-between` button row: left `<a asp-action="Users" class="btn btn-secondary"><i class="fas fa-arrow-left me-2"></i>Back to Users</a>`, right `<button type="submit" class="btn btn-warning"><i class="fas fa-user-plus me-2"></i>Create User</button>`. Add `@using QuestBoard.Domain.Enums` if `GetEnumSelectList<GroupRole>()` needs it (note `_ViewImports.cshtml` already imports `QuestBoard.Domain.Enums`).

    Create `Views/Admin/CreateUser.Mobile.cshtml` typed `@model CreateUserViewModel` using `_Layout.Mobile.cshtml`, wrapping the same fields in an `account-card-mobile` container with a `d-grid gap-2` stacked button layout (Back to Users above, Create User below). Include `@Html.AntiForgeryToken()` inside the form (the desktop form gets it automatically from the tag helper; ensure both forms post with anti-forgery protection).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `CreateUserViewModel` has `Email`, `Name`, `Password`, and `GroupRole` (typed `GroupRole`, defaulting to `GroupRole.Player`); Email has `[EmailAddress]`, Password has `[DataType(DataType.Password)]` and `MinimumLength = 8`
    - `CreateUser.cshtml` posts to `asp-action="CreateUser"` and renders a `<select asp-for="GroupRole" asp-items="Html.GetEnumSelectList<GroupRole>()">`
    - `CreateUser.cshtml` button row uses `d-flex justify-content-between` with Back to Users (`btn-secondary`) and Create User (`btn-warning`), preceded by `<hr>` (CLAUDE.md UI rule)
    - `CreateUser.Mobile.cshtml` exists, binds `CreateUserViewModel`, and posts to `asp-action="CreateUser"`
    - `dotnet build QuestBoard.sln` succeeds
  </acceptance_criteria>
  <done>The create-user view model and both form views compile and follow the project's modern-card UI rules.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add AdminController.CreateUser GET/POST and remove ?? 1 from Users()</name>
  <files>QuestBoard.Service/Controllers/Admin/AdminController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/AdminController.cs (constructor DI already includes IUserService, IIdentityService, IBackgroundJobClient, IActiveGroupContext; Users() with `?? 1` at the GetGroupRoleByIdAsync call; SendConfirmationEmail shows the exact ConfirmationEmailJob enqueue pattern; EditUser GET/POST pattern; PromoteToAdmin null-guard pattern)
    - QuestBoard.Service/Controllers/Admin/AccountController.cs (the removed Register POST showed CreateAsync → GetIdByEmailAsync → GenerateEmailConfirmationAsync → Base64UrlEncode → Url.Action ConfirmEmail → Enqueue ConfirmationEmailJob — copy this flow)
    - QuestBoard.Domain/Interfaces/IUserService.cs (CreateAsync(email,name,password); SetGroupRoleAsync(userId,groupId,role))
    - QuestBoard.Domain/Interfaces/IIdentityService.cs (GetIdByEmailAsync, GenerateEmailConfirmationAsync)
    - QuestBoard.Service/ViewModels/AdminViewModels/CreateUserViewModel.cs (created in Task 1)
    - .planning/phases/30-group-ux-admin-user-creation/30-PATTERNS.md (§Controllers/Admin/AdminController.cs — CreateUser GET/POST + ?? 1 removal + null-guard redirect)
  </read_first>
  <action>
    Add a `CreateUser` GET action (`[HttpGet]`) that returns `View(new CreateUserViewModel())`.

    Add a `CreateUser` POST action (`[HttpPost]` + `[ValidateAntiForgeryToken]`, parameter `CreateUserViewModel model`):
    - If `!ModelState.IsValid`, return `View(model)`.
    - Read `var groupId = activeGroupContext.ActiveGroupId;`. If `groupId == null`, return `RedirectToAction("Index", "GroupPicker")` (no active group — send the admin to pick one; RESEARCH.md Pitfall 7).
    - Call `var result = await userService.CreateAsync(model.Email, model.Name, model.Password);`.
    - On `result.Succeeded`: get `var userId = await identityService.GetIdByEmailAsync(model.Email);`. If `userId.HasValue`: call `await userService.SetGroupRoleAsync(userId.Value, groupId.Value, model.GroupRole);` (REG-02 — assign to the admin's active group, NOT a value from the form, so a tampered form cannot place the user in another group). Then generate the confirmation token exactly as `SendConfirmationEmail` does: `GenerateEmailConfirmationAsync(userId.Value)` → `WebEncoders.Base64UrlEncode(Encoding.UTF8.GetBytes(rawToken))` → `Url.Action("ConfirmEmail", "Account", new { userId = userId.Value, token = encodedToken }, Request.Scheme)` → `jobClient.Enqueue<ConfirmationEmailJob>(j => j.ExecuteAsync(model.Email, model.Name, callbackUrl!, CancellationToken.None))` (REG-03), guarding for a null token. Set `TempData["Success"] = $"Account created for {model.Name}. A confirmation email has been sent.";` and return `RedirectToAction(nameof(Users))` (D-13).
    - On failure: `foreach (var error in result.Errors) ModelState.AddModelError(string.Empty, error.Description);` then return `View(model)`.

    Remove the `?? 1` fallback in `Users()` (D-17): replace the line `GroupRole? groupRole = await userService.GetGroupRoleByIdAsync(user.Id, groupId ?? 1);` so it uses the non-null active group id. At the top of `Users()`, after reading `var groupId = activeGroupContext.ActiveGroupId;`, add a guard `if (groupId == null) return RedirectToAction("Index", "GroupPicker");` and then inside the loop call `await userService.GetGroupRoleByIdAsync(user.Id, groupId.Value);`. Also delete the stale `// ?? 1: Phase 30 ...` comment on the preceding line so no `?? 1` token or its TODO note remains in the file.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `AdminController` has a `CreateUser` GET returning `View(new CreateUserViewModel())` and a `CreateUser` POST decorated `[HttpPost]` + `[ValidateAntiForgeryToken]`
    - CreateUser POST calls `SetGroupRoleAsync(userId.Value, groupId.Value, model.GroupRole)` where `groupId` comes from `activeGroupContext.ActiveGroupId` (server-side session, never from the form) — REG-02
    - CreateUser POST calls `jobClient.Enqueue<ConfirmationEmailJob>(...)` after successful creation — REG-03
    - CreateUser POST sets `TempData["Success"]` and returns `RedirectToAction(nameof(Users))` on success
    - `Users()` no longer contains the token `?? 1` (verify: `grep -v '^[[:space:]]*//' QuestBoard.Service/Controllers/Admin/AdminController.cs | grep -c '?? 1'` returns 0) and the stale `?? 1` TODO comment is gone
    - `dotnet build QuestBoard.sln` succeeds
  </acceptance_criteria>
  <done>Admins can create users that land in their active group and receive a confirmation email; the hardcoded group fallback is gone from AdminController.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove ?? 1 fallback from UserRepository.GetAllPlayers and GetAllDungeonMasters</name>
  <files>QuestBoard.Repository/UserRepository.cs</files>
  <read_first>
    - QuestBoard.Repository/UserRepository.cs (GetAllDungeonMasters lines using `activeGroupContext.ActiveGroupId ?? 1`; GetAllPlayers same; IActiveGroupContext injected via primary constructor)
    - QuestBoard.Domain/Interfaces/IActiveGroupContext.cs (ActiveGroupId is `int?`)
    - .planning/phases/30-group-ux-admin-user-creation/30-CONTEXT.md (D-17 — locked removal of these two fallbacks)
  </read_first>
  <action>
    In `GetAllDungeonMasters` and `GetAllPlayers`, remove the `?? 1` fallback (D-17). Replace `var groupId = activeGroupContext.ActiveGroupId ?? 1;` in each method. Since these methods return `IList<User>` (no controller to redirect), use a null-safe approach: if `activeGroupContext.ActiveGroupId` is null, return an empty list (`return [];` or `return Mapper.Map<IList<User>>(...)` over an empty set) rather than defaulting to group 1 — after Phase 30 the picker guarantees the session is set, and integration tests use `MutableGroupContext` defaulting to GroupId = 1, so the null path is a safety net only. Use the non-null `groupId.Value` in the existing `Where` predicate. Delete the stale `// ?? 1: Phase 30 ...` comment lines so no `?? 1` token or its TODO note remains in the file.

    Do NOT alter `GetGroupRoleAsync`, `SetGroupRoleAsync`, or `ExistsAsync` — only the two list methods carry the fallback.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `UserRepository.cs` contains no `?? 1` token (verify: `grep -v '^[[:space:]]*//' QuestBoard.Repository/UserRepository.cs | grep -c '?? 1'` returns 0)
    - Both `GetAllDungeonMasters` and `GetAllPlayers` read `activeGroupContext.ActiveGroupId` and handle the null case without defaulting to a hardcoded group id
    - The stale `?? 1` TODO comments are removed
    - `dotnet build QuestBoard.sln` succeeds
  </acceptance_criteria>
  <done>The repository's player/DM queries scope to the real active group with no hardcoded fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → AdminController.CreateUser | AdminOnly-authorized admin submits new-user fields; the target group is NOT taken from the form |
| session → repository group scoping | UserRepository now reads the real ActiveGroupId; no fallback masking missing context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Tampering / IDOR (create user in another group) | CreateUser POST | mitigate | Target group is `activeGroupContext.ActiveGroupId` from server-side session; the form has no group field. A malicious admin cannot place a user in a group they are not active in (REG-02). |
| T-30-10 | Elevation of Privilege (create Admin-role user) | CreateUser POST GroupRole | accept | A group admin may legitimately assign Admin GroupRole within THEIR OWN active group (MGMT-07 allows Player/DM/Admin). Scope is limited to the admin's group by T-30-09; cross-group escalation is not possible. Acceptable per phase scope. |
| T-30-11 | Spoofing (CSRF on CreateUser) | CreateUser POST | mitigate | `[ValidateAntiForgeryToken]` + anti-forgery token in both desktop and mobile forms |
| T-30-12 | Information Disclosure (wrong-group user list after fallback removal) | Users() / GetAllPlayers / GetAllDungeonMasters | mitigate | Removing `?? 1` ensures lists scope to the real active group instead of always group 1; prevents leaking group 1 membership to a different group's admin |
| T-30-13 | Denial of Service (null active group loops) | Users() null guard | mitigate | Null active group redirects once to GroupPicker (not back to Users) — no redirect loop (RESEARCH.md Pitfall 7) |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan |
</threat_model>

<verification>
- `dotnet build QuestBoard.sln -c Debug` succeeds after each task
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~AdminController"` passes (MutableGroupContext defaults ActiveGroupId = 1, so the `?? 1` removal does not regress existing admin tests — RESEARCH.md Pitfall 3). New CreateUser tests are added in plan 30-05.
- Negative grep gate: no un-commented `?? 1` remains in AdminController.cs or UserRepository.cs.
</verification>

<success_criteria>
- A group admin can create a user assigned to their active group with a chosen GroupRole (MGMT-07, REG-02) and a confirmation email is queued (REG-03)
- Promote/demote actions work against the active group with no hardcoded fallback (MGMT-08)
- No `?? 1` fallback remains in AdminController.Users() or UserRepository list methods (D-17)
- Solution builds; existing admin tests still pass
</success_criteria>

<output>
Create `.planning/phases/30-group-ux-admin-user-creation/30-03-SUMMARY.md` when done
</output>
