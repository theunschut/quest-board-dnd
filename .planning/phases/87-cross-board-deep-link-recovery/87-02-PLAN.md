---
phase: 87-cross-board-deep-link-recovery
plan: 02
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - QuestBoard.UnitTests/Architecture/CrossBoardIgnoreQueryFiltersSeamTests.cs
  - QuestBoard.Domain/Enums/CrossBoardLookupKind.cs
  - QuestBoard.Domain/Interfaces/ICrossBoardLinkResolver.cs
  - QuestBoard.Domain/Services/CrossBoardLinkResolverService.cs
  - QuestBoard.Repository/CrossBoardLinkRepository.cs
  - QuestBoard.Service/Helpers/CrossBoardLinkRegistry.cs
  - QuestBoard.UnitTests/Helpers/CrossBoardLinkRegistryTests.cs
  - QuestBoard.IntegrationTests/Middleware/CrossBoardRouteCoverageTests.cs
autonomous: true
# No REQ-IDs exist for Phase 87 (.planning/REQUIREMENTS.md has no Phase 87 rows).
# Traceability anchors are 87-CONTEXT.md's own locked decision IDs.
requirements: [D-09, D-10, D-11, D-12, D-16, D-17, D-18]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - All 18 board-scoped top-level GET routes that take an id resolve across boards — the eight read routes and the ten edit/manage routes named in the phase decisions — so a Dungeon Master sharing an edit link with a co-DM on the other board reaches the page rather than the 404 (D-16).
    - The six image subresource routes and the shop item's modal variant are not in the registry and do not need to be: they arrive as a non-document fetch destination and fail the navigation gate, and once the page itself has switched the board they resolve normally (D-16).
    - A profile link resolves only when the answer is unambiguous — the target user shares exactly one board with the viewer and it is not the board already active; sharing several boards, or already sharing the active one, leaves today's behaviour standing (D-17).
    - A profile edit link with no id at all is a self-edit and never resolves (D-17).
    - Every cross-board id-to-board lookup in the application lives in one repository class, and a new filter-bypassing call site anywhere else in production code fails the build until it is added to an explicit allowlist (D-11).
    - Each lookup returns an integer board id and nothing else, with the viewer's membership set pinned inside the query predicate rather than checked afterwards (D-10).
    - A route absent from the registry never resolves, so no action added to the application later silently gains the ability to repoint a session (D-09).
    - The resolver still takes a user id and nothing else, so widening the route set adds no place for a role-based escape hatch (D-12).
    - A resolved link that the page then refuses to serve still resolves — the viewer is switched and the page answers for itself; visibility rules and authorization policy stay out of the lookup (D-18).
  prohibitions:
    - statement: Widening the route set must not widen the tenancy bypass. The filter-bypassing call must not appear in any production file outside the one dedicated repository class, and no lookup may return anything other than a board id.
      status: flagged-unverified
      verification: CrossBoardIgnoreQueryFiltersSeamTests scans every .cs file under QuestBoard.Repository, QuestBoard.Domain and QuestBoard.Service with comments stripped, and fails naming any occurrence in a file outside the allowlist. Return-shape is asserted by source criteria on the projections. Not dismissed by this plan on its own evidence.
    - statement: A route must not gain cross-board resolution by accident. Adding a controller action, or renaming one, must never silently extend what the escape hatch covers.
      status: flagged-unverified
      verification: The registry is a closed table keyed by nameof-derived controller and action names, and CrossBoardLinkRegistryTests asserts its exact membership count and contents, so both an addition and a rename surface as a failing test rather than as silent behaviour.
  artifacts:
    - QuestBoard.UnitTests/Architecture/CrossBoardIgnoreQueryFiltersSeamTests.cs
    - QuestBoard.IntegrationTests/Middleware/CrossBoardRouteCoverageTests.cs
  key_links:
    - The registry maps a (controller, action) pair to a CrossBoardLookupKind; the repository switch maps that kind to one projection. Adding a route is one registry line when the kind already exists, and a kind plus a projection when it does not.
    - QuestLog/Details and QuestLog/EditRecap map to the quest kind, because the quest log has no entity of its own — QuestLogController resolves through the quest service.
    - The profile routes map to a kind backed by UserGroups rather than an entity table, which is why the repository exposes a separate shared-boards method returning a set: the ambiguity rule needs the count, not the first hit.
    - The six image routes are excluded for free by the navigation gate rather than by an exclusion list, so there is no second list to keep in sync with the registry.
---

<objective>
Widen the proven slice sideways: every one of the 18 board-scoped GET routes that takes an id, backed by seven more projections in the one repository class, with the profile routes carrying the ambiguity rule — and close the containment question with an allowlist architecture test before any of that widening happens.

Purpose: 87-01 proved one route end to end. This plan is the horizontal expansion, and expansion is exactly when a tenancy bypass spreads by copy-paste. The allowlist test lands first so that every projection added afterwards is added inside a boundary the build enforces.

Output: the closed registry at full size, the remaining id-to-board projections, the profile ambiguity rule, a table-driven test over all 18 routes, and the architecture test that keeps the escape hatch in one file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-cross-board-deep-link-recovery/87-CONTEXT.md
@.planning/phases/87-cross-board-deep-link-recovery/87-RESEARCH.md
@.planning/phases/87-cross-board-deep-link-recovery/87-PATTERNS.md
@.planning/phases/87-cross-board-deep-link-recovery/87-01-SUMMARY.md
@CLAUDE.md
</context>

<recorded_choices>
1. **Planner correction to 87-CONTEXT.md D-11's call-site count.** D-11 says "7 call sites today" and then lists six: `EventSignupRepository.cs:91`, `EventRepository.cs:172`, `GroupRepository.cs:135` and `:147`, `QuestRepository.cs:270` and `:293`. A repo-wide scan confirms **six real call sites across four files**; the seventh occurrence, at `QuestRepository.cs:269`, is a comment mentioning the method name, which the comment-stripping pass removes before scanning. The allowlist is therefore four existing files plus the new repository class, and the test asserts file membership rather than an occurrence count.
2. **Planner scoping decision — the allowlist test scans production source only.** `IgnoreQueryFilters()` appears in roughly 30 places across `QuestBoard.UnitTests` and `QuestBoard.IntegrationTests`, where it is the ordinary way to seed and assert against rows on a board the test client is not currently on. Including the test projects would make the allowlist a list of test files that grows with every new test, which is noise rather than a guardrail. The scan covers `QuestBoard.Repository`, `QuestBoard.Domain` and `QuestBoard.Service`, and the test says so in a comment.
3. **Planner discretion — the allowlist asserts file membership, not per-file counts.** `CrossBoardLinkRepository.cs` gains a projection per entity kind across this plan and could gain more later; pinning a count there would fail for the right reason at the wrong time. What must not change without deliberation is *which files* may contain the call at all.
4. **`QuestLog/Details` and `QuestLog/EditRecap` map to the quest kind.** `QuestLogController` resolves everything through the quest service and there is no quest-log entity in the context, so both routes reuse the quest projection rather than gaining one of their own.
</recorded_choices>

<artifacts_this_phase_produces>
Every symbol Phase 87 creates, across all four plans. The plan-review-convergence source-grounding pass uses this list to exclude newly created symbols from drift verification. The owning plan is named in brackets.

**Production types and members**
- `QuestBoard.Domain/Enums/CrossBoardLookupKind.cs` — `CrossBoardLookupKind`; member `Quest` [87-01]; members `Event`, `Character`, `Contact`, `ShopItem`, `EventSeries`, `ContactCategory`, `BoardMember` [87-02]
- `QuestBoard.Domain/Models/CrossBoardTarget.cs` — `CrossBoardTarget` record with `GroupId`, `GroupName` [87-01]
- `QuestBoard.Domain/Interfaces/ICrossBoardLinkRepository.cs` — `ICrossBoardLinkRepository`; `ResolveBoardIdAsync`; `ResolveSharedBoardIdsForUserAsync` [87-01]
- `QuestBoard.Domain/Interfaces/ICrossBoardLinkResolver.cs` — `ICrossBoardLinkResolver`; `ResolveAsync` [87-01]
- `QuestBoard.Domain/Services/CrossBoardLinkResolverService.cs` — `CrossBoardLinkResolverService` [87-01]
- `QuestBoard.Repository/CrossBoardLinkRepository.cs` — `CrossBoardLinkRepository`; private projections `ResolveQuestBoardIdAsync` [87-01], `ResolveEventBoardIdAsync`, `ResolveCharacterBoardIdAsync`, `ResolveContactBoardIdAsync`, `ResolveShopItemBoardIdAsync`, `ResolveEventSeriesBoardIdAsync`, `ResolveContactCategoryBoardIdAsync` [87-02]
- `QuestBoard.Service/Constants/TempDataKeys.cs` — `TempDataKeys`; `BoardSwitchTargetName`, `BoardSwitchPreviousGroupId`, `BoardSwitchPreviousGroupName` [87-01]
- `QuestBoard.Service/Helpers/CrossBoardLinkRegistry.cs` — `CrossBoardLinkRegistry`; `TryGetLookupKind`; `RegisteredRoutes` [87-01, widened to all 18 routes in 87-02]
- `QuestBoard.Service/Helpers/CrossBoardRouteTarget.cs` — `CrossBoardRouteTarget` record with `Kind`, `Id`; `TryFromRouteValues` [87-01]; `TryFromLocalUrl` [87-03]
- `QuestBoard.Service/Services/IActiveBoardSwitcher.cs` — `IActiveBoardSwitcher`; `SwitchAsync` [87-01]
- `QuestBoard.Service/Services/ActiveBoardSwitcherService.cs` — `ActiveBoardSwitcherService` [87-01]
- `QuestBoard.Service/Middleware/CrossBoardDeepLinkMiddleware.cs` — `CrossBoardDeepLinkMiddleware`; private `IsRealTopLevelNavigation` [87-01]

**Markup / view vocabulary**
- `QuestBoard.Service/Views/Shared/_Toasts.cshtml` — new board-switch toast block; marker class `board-switch-toast` [87-01]. No `.Mobile.cshtml` twin is created or needed: the partial is already rendered by `_Layout.cshtml`, `_Layout.Mobile.cshtml` and `_Layout.GroupPicker.cshtml`.

**New test files**
- `QuestBoard.IntegrationTests/Helpers/CrossBoardWebApplicationFactory.cs` [87-01]
- `QuestBoard.IntegrationTests/Middleware/CrossBoardTestHarnessTests.cs` [87-01]
- `QuestBoard.IntegrationTests/Middleware/CrossBoardDeepLinkMiddlewareTests.cs` [87-01]
- `QuestBoard.UnitTests/Helpers/CrossBoardLinkRegistryTests.cs` [87-01]
- `QuestBoard.UnitTests/Architecture/CrossBoardIgnoreQueryFiltersSeamTests.cs` [87-02]
- `QuestBoard.IntegrationTests/Middleware/CrossBoardRouteCoverageTests.cs` [87-02]
- `QuestBoard.IntegrationTests/Controllers/CrossBoardPickerSkipTests.cs` [87-03]
- `QuestBoard.IntegrationTests/Security/CrossBoardOracleParityTests.cs` [87-04]
- `QuestBoard.IntegrationTests/Security/CrossBoardAuthorizationBoundaryTests.cs` [87-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Close the filter-bypass seam with an allowlist architecture test, before widening anything</name>
  <precondition>No Visual Studio debugger is holding QuestBoard.Service build outputs — `dotnet build` on the untouched tree exits 0. If it fails with a locked-file error, stop and ask the developer to stop the debugger (Shift+F5) rather than working around it.</precondition>
  <files>QuestBoard.UnitTests/Architecture/CrossBoardIgnoreQueryFiltersSeamTests.cs</files>
  <read_first>
    - `QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs` (whole file, 306 lines) — the direct structural mirror. In particular `ResolveRepoRelativePath` (the upward walk from `AppContext.BaseDirectory`), `StripComments` (which removes `//` line comments, `/* */` blocks and `@* *@` Razor blocks before any scanning), and the closed-list plus `[Theory]`/`MemberData` shape.
    - `QuestBoard.Repository/QuestRepository.cs` lines 260-300 — the two real call sites and the comment at line 269 that mentions the method name without calling it. This is exactly the case the comment-stripping pass exists for.
    - `QuestBoard.Repository/GroupRepository.cs` lines 120-150, `QuestBoard.Repository/EventRepository.cs` lines 160-180, `QuestBoard.Repository/EventSignupRepository.cs` lines 70-101 — the other existing call sites, each with its own why-comment.
    - `QuestBoard.Repository/CrossBoardLinkRepository.cs` — produced by 87-01; the fifth allowlisted file.
  </read_first>
  <action>
Create `QuestBoard.UnitTests/Architecture/CrossBoardIgnoreQueryFiltersSeamTests.cs`, mirroring `AmbientClockSeamTests` in structure and tone but inverting its direction. `AmbientClockSeamTests` asks "do these listed files still avoid the banned shape". This test asks the stronger question: "is the banned shape confined to these listed files, anywhere in production source".

Copy `ResolveRepoRelativePath` and `StripComments` from `AmbientClockSeamTests` (adapting `ResolveRepoRelativePath` only as far as needed to resolve a project *directory* rather than a single file — the upward walk for a directory containing `QuestBoard.Service` is the same idea and the same failure message style).

Declare a `private static readonly string[] AllowedCallSites` holding exactly five repo-relative paths: `QuestBoard.Repository/EventSignupRepository.cs`, `QuestBoard.Repository/EventRepository.cs`, `QuestBoard.Repository/GroupRepository.cs`, `QuestBoard.Repository/QuestRepository.cs`, `QuestBoard.Repository/CrossBoardLinkRepository.cs`. Comment the list in the house style: this is the complete set of places in production code allowed to read past the board filters; adding a sixth is a deliberate act that has to be argued for here, not a line someone copy-pastes into a repository because it made a query work.

Declare the scanned roots as `QuestBoard.Repository`, `QuestBoard.Domain` and `QuestBoard.Service`, and comment why the two test projects are excluded: seeding and asserting against rows on a board the test client is not currently on is the ordinary, correct way to write a test here, so including them would turn the allowlist into a list of test files that grows with every new test and stops being a guardrail.

Write these facts:
  - **The confinement fact.** Enumerate every `*.cs` file under the three scanned roots, skipping any path segment named `obj` or `bin`, read each file, run it through `StripComments`, and collect every file that still contains `IgnoreQueryFilters`. Assert the resulting set equals the allowlist — failing with a message that names each offending file path, so the failure tells the next person exactly what to look at and that they must justify the addition rather than just extend the list.
  - **The allowlist-is-live fact.** Assert every path in `AllowedCallSites` exists on disk and, after comment stripping, actually contains the call. A stale entry for a file that no longer needs the bypass is itself a defect, because it silently re-permits the call there.
  - **The comment-immunity fact.** Assert that `StripComments` applied to a small inline string containing the method name inside a `//` line comment, a `/* */` block and a `@* *@` block leaves none of them — so a file that merely *discusses* the bypass in prose can never fail the confinement fact, and a real call can never hide inside a comment form.

Name the class `CrossBoardIgnoreQueryFiltersSeamTests` so its fully-qualified name matches both the `CrossBoard` and the `CrossBoardIgnoreQueryFilters` filters. Do not write a phase number, decision id, plan number or planning document name into the file.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CrossBoardIgnoreQueryFilters</automated>
    <fails_when>non-zero exit, or the run summary reports "Passed: 0" / "total: 0" (a filter that matched no tests), or the confinement fact's failure message names a file outside the allowlist</fails_when>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CrossBoardIgnoreQueryFilters` exits 0 and reports at least 3 tests passed.
    - Behaviour: temporarily adding the filter-bypassing call to any production file outside the allowlist makes the confinement fact fail with a message naming that file. (Verify by trying it in the working tree and then reverting; do not commit the temporary edit.)
    - Behaviour: adding a `//` comment mentioning the method name to a production file outside the allowlist leaves the suite green.
    - `QuestBoard.UnitTests/Architecture/CrossBoardIgnoreQueryFiltersSeamTests.cs` contains the literal `QuestBoard.Repository/CrossBoardLinkRepository.cs` and the literal `StripComments`.
    - `git status --porcelain QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs` returns nothing — the existing seam test is mirrored, never edited or weakened.
    - `dotnet test QuestBoard.UnitTests` exits 0.
  </acceptance_criteria>
  <done>A new filter-bypassing call site anywhere in production code fails the build until someone adds it to an explicit five-entry list, and a comment discussing the bypass cannot trigger a false failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add the remaining id-to-board projections and the profile ambiguity rule</name>
  <files>QuestBoard.Domain/Enums/CrossBoardLookupKind.cs, QuestBoard.Repository/CrossBoardLinkRepository.cs, QuestBoard.Domain/Services/CrossBoardLinkResolverService.cs, QuestBoard.Domain/Interfaces/ICrossBoardLinkResolver.cs</files>
  <read_first>
    - `QuestBoard.Repository/CrossBoardLinkRepository.cs` — produced by 87-01; the switch, the quest projection and the class-level comment convention every new projection must follow.
    - `QuestBoard.Domain/Services/CrossBoardLinkResolverService.cs` — produced by 87-01; the fresh membership read and the two properties its signature encodes.
    - `QuestBoard.Domain/Interfaces/ICrossBoardLinkRepository.cs` — produced by 87-01; `ResolveSharedBoardIdsForUserAsync` already exists for the profile case.
    - `QuestBoard.Repository/Entities/QuestBoardContext.cs` lines 385-575 — the `DbSet` names and the `HasQueryFilter` registration for each entity being projected, so each new projection bypasses a filter that genuinely exists.
    - `QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs` lines 18-60 and 200-210 — `Profile`'s `IsTargetInActiveGroupAsync` gate (which is what produces today's 404 for a co-DM on another board), `EditProfile`'s `int? id` signature, and the fact that bio, name and picture are board-independent while the quest list is board-scoped.
    - `87-CONTEXT.md` D-16 and D-17 — the route list and the exact ambiguity rule.
    - `.claude/architecture.md` — EF packages live only in `QuestBoard.Repository`; the resolver in Domain must not gain one.
  </read_first>
  <action>
Add seven members to `CrossBoardLookupKind`: `Event`, `Character`, `Contact`, `ShopItem`, `EventSeries`, `ContactCategory`, `BoardMember`. Keep one member per kind of thing an id can point at, not one per route — six routes share the quest kind, two share the event kind, and so on.

Add one private projection per new entity-backed kind to `CrossBoardLinkRepository`, each shaped exactly like the quest projection produced by 87-01: `IgnoreQueryFilters()`, a `Where` pinning both the id and `memberGroupIds.Contains(x.GroupId)`, a `Select` of `(int?)x.GroupId` and nothing else, and `FirstOrDefaultAsync`. The `DbSet` and the entity for each: `Events`/`EventEntity`, `Characters`/`CharacterEntity`, `Contacts`/`ContactEntity`, `ShopItems`/`ShopItemEntity`, `EventSeries`/`EventSeriesEntity`, `ContactCategories`/`ContactCategoryEntity` — all six carry a `public int GroupId`. Wire each into the existing `switch` in `ResolveBoardIdAsync`. The `BoardMember` kind does **not** get an arm in that switch: it has a different answer shape and is served by the already-existing `ResolveSharedBoardIdsForUserAsync`. Make that explicit rather than implicit — give `BoardMember` its own switch arm that throws `InvalidOperationException` with a message saying this kind is resolved through the shared-boards method because the ambiguity rule needs the whole set, so a future caller that reaches for the wrong method fails immediately instead of getting a plausible-looking wrong answer. Keep the `default` arm throwing `ArgumentOutOfRangeException`.

Do not repeat the class-level why-comment on every projection; the class carries it once. Do add a short comment where a projection is not self-evident: that the quest kind serves the quest log's routes as well, because the quest log has no table of its own.

In `CrossBoardLinkResolverService.ResolveAsync`, branch once at the top on the kind. For `BoardMember`: call `repository.ResolveSharedBoardIdsForUserAsync(id, memberGroupIds, token)` — the route's id is the *target user's* id here, not an entity id — and apply the ambiguity rule, returning a target only when the returned set contains exactly one board. When it contains none or more than one, return null and today's behaviour stands. Comment the rule in plain language: a profile link is only worth following automatically when there is exactly one board it can mean; when the two people share several boards there is no right answer to guess, and guessing would move the viewer's board for a page that would have rendered anyway. Note also that an absent id never reaches here at all, because the route reader requires an id it can parse, which is what makes the self-edit form of the profile edit route a no-op by construction.

For every other kind, the existing path is unchanged: pin the membership set, call `ResolveBoardIdAsync`, map the resolved id back to the membership row for the board name.

Nothing in this task may consult the entity's visibility rules or the viewer's role. A cancelled event, an archived shop item, or a page the viewer is only a Player on still resolves; the lookup answers which board owns the id and the page answers everything else. Add a comment on the resolver saying so, in plain language, because it is the property that keeps this class auditable.

Do not write a phase number, decision id, plan number or planning document name into any source file, comment or string literal.
  </action>
  <verify>
    <automated>dotnet build && dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CrossBoardIgnoreQueryFilters</automated>
    <fails_when>non-zero exit, or the run summary reports "Passed: 0" / "total: 0", or the confinement fact names a file outside the allowlist (a projection was added in the wrong place)</fails_when>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 with no new warnings.
    - `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CrossBoardIgnoreQueryFilters` exits 0 — the widened repository is still the only production file outside the four pre-existing ones that reads past the filters.
    - `QuestBoard.Domain/Enums/CrossBoardLookupKind.cs` declares exactly 8 enum members.
    - `grep -c 'Select(' QuestBoard.Repository/CrossBoardLinkRepository.cs` is at least 8, and `grep -cE '\.Select\(\s*[a-z]+ => \(int\?\)[a-z]+\.GroupId\)' QuestBoard.Repository/CrossBoardLinkRepository.cs` is 7 — one scalar board-id projection per entity-backed kind.
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Repository/CrossBoardLinkRepository.cs | grep -cE 'Include\(|\.ToListAsync\(\)\s*;\s*$|Select\([a-z]+ => [a-z]+\)' ` is 0 — no projection returns an entity or a row.
    - `grep -rn "IgnoreQueryFilters" QuestBoard.Domain QuestBoard.Service --include='*.cs' | grep -v '/obj/' | grep -v '/bin/' | grep -vE ':\s*(//|/\*|\*)'` returns nothing — comment lines are filtered out, so prose about the bypass cannot fail the gate.
    - `grep -rn "Microsoft.EntityFrameworkCore" QuestBoard.Domain --include='*.csproj'` returns nothing — the resolver gained no EF dependency.
    - `dotnet test` exits 0 (full suite).
  </acceptance_criteria>
  <reversibility rating="costly">Each projection is a new method in one class, trivially removable — but the `int?`-only return shape is now asserted by seven call sites plus the allowlist test. Widening any of them to return an entity later means re-auditing the leak-nothing property the whole audit story rests on, which is why the shape is pinned by acceptance criteria rather than left to review.</reversibility>
  <done>Every entity kind a registered route can point at has a membership-pinned board-id projection in the one allowlisted repository class, and the profile kind resolves only when the answer is unambiguous.</done>
</task>

<task type="auto">
  <name>Task 3: Open the registry to all 18 routes and prove each one end to end</name>
  <files>QuestBoard.Service/Helpers/CrossBoardLinkRegistry.cs, QuestBoard.UnitTests/Helpers/CrossBoardLinkRegistryTests.cs, QuestBoard.IntegrationTests/Middleware/CrossBoardRouteCoverageTests.cs</files>
  <read_first>
    - `QuestBoard.Service/Helpers/CrossBoardLinkRegistry.cs` — produced by 87-01; the dictionary, the `ControllerNameOf<T>()` helper and the one existing entry.
    - `QuestBoard.UnitTests/Helpers/CrossBoardLinkRegistryTests.cs` — produced by 87-01; the membership assertions being extended.
    - `QuestBoard.IntegrationTests/Middleware/CrossBoardDeepLinkMiddlewareTests.cs` — produced by 87-01; the fixture, the two-board seeding and the navigation-header helper to reuse.
    - `QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs` (whole file) — the existing seeders: `CreateTestQuestAsync`, `CreateShopItemAsync`, `CreateTestCharacterAsync`, `CreateTestContactAsync`, `CreateTestContactCategoryAsync`, and what each one needs. Series and events have no helper; seed those through `factory.Database.CreateContext()` the way `AgendaTenantIsolationTests` does.
    - The 10 controllers whose actions are being registered, to confirm each action name and that it takes an `int id`: `QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` (`Details`, `Edit`, `Manage`, `CreateFollowUp`), `Controllers/Events/EventsController.cs` (`Details`, `Edit`), `Controllers/Characters/CharactersController.cs` (`Details`, `Edit`), `Controllers/Contacts/ContactsController.cs` (`Details`, `Edit`), `Controllers/QuestBoard/QuestLogController.cs` (`Details`, `EditRecap`), `Controllers/Shop/ShopController.cs` (`Details`), `Controllers/Events/SeriesController.cs` (`Details`), `Controllers/Shop/ShopManagementController.cs` (`Edit`), `Controllers/Contacts/ContactCategoryManagementController.cs` (`Edit`), `Controllers/DungeonMaster/DungeonMasterController.cs` (`Profile`, `EditProfile`).
    - `QuestBoard.Service/Program.cs` lines 355-365 — the conventional `{controller=Home}/{action=Index}/{id?}` route every one of these 18 URLs is served by.
    - `87-CONTEXT.md` D-16 — the authoritative route list and the excluded subresources.
  </read_first>
  <action>
Fill the registry to its full size: 18 entries mapping a `(controller, action)` pair to a `CrossBoardLookupKind`. Build every key from `ControllerNameOf<TController>()` and `nameof(TController.ActionName)` — never a bare string literal — so renaming a controller or an action surfaces here as a compile error or a failing test rather than as a route that quietly stops resolving. The mapping, exactly:

Quest kind — quest details, quest edit, quest manage, quest create-follow-up, quest-log details, quest-log edit-recap. Event kind — events details, events edit. Character kind — characters details, characters edit. Contact kind — contacts details, contacts edit. ShopItem kind — shop details, shop-management edit. EventSeries kind — series details. ContactCategory kind — contact-category-management edit. BoardMember kind — dungeon-master profile, dungeon-master edit-profile.

Add nothing else. In particular do not add the six image routes (`GetProfilePicture`, `GetCroppedPicture`, `GetContactImage`, `GetCroppedContactImage`, `GetDMProfilePicture`, `GetOriginalDMProfilePicture`): they are excluded by the navigation gate rather than by a list, because they arrive with a non-document fetch destination, and once the page itself has switched the board they resolve normally. Record that reasoning as a comment on the registry so the absence reads as deliberate rather than as an oversight — and say plainly that the right way to exclude a route is to leave it out, never to add an exclusion list beside the registry that would then need keeping in sync.

Extend `CrossBoardLinkRegistryTests` to assert the table's exact contents: `RegisteredRoutes` has exactly 18 entries; each of the 18 pairs resolves to the named kind; each of the six image action names, and a handful of ordinary non-participating routes (a list action, an index action, a delete action), resolve to nothing.

Create `QuestBoard.IntegrationTests/Middleware/CrossBoardRouteCoverageTests.cs` using `IClassFixture<CrossBoardWebApplicationFactory>`, with the viewer a member of two boards and one row of every seeded kind on the second board. Write:
  - A `[Theory]` over the 16 entity-backed routes (the 18 minus the two profile routes, which have their own rule): with Board A active, a navigation-shaped GET of the Board B URL returns a non-404 response and a following request shows Board B active. Assert "not 404" rather than "200" for the edit and manage routes: whether the page then renders or refuses is the page's business and depends on the seeded viewer's role, which is exactly the separation this phase depends on. For the read routes, assert 200 and that the response body carries the seeded row's distinguishing text.
  - A `[Theory]` over the six image routes: with Board A active, a GET of the Board B URL carrying image-shaped fetch metadata leaves the board unchanged and produces the same response as an id that does not exist.
  - A fact for the shop item's modal variant: the same id requested with empty-destination fetch metadata leaves the board unchanged, while the full-page variant of the same URL switches it.
  - Three facts for the profile rule: a target user sharing exactly one board with the viewer, where that board is not the active one, switches and renders; a target sharing two boards with the viewer leaves the board unchanged; the profile edit route with no id in the URL leaves the board unchanged and behaves as the self-edit it is.

Name every test method so its fully-qualified name contains `CrossBoardRouteCoverage`.
  </action>
  <verify>
    <automated>dotnet build && dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CrossBoardLinkRegistry</automated>
    <fails_when>non-zero exit, or the run summary reports "Passed: 0" / "total: 0", or the membership assertion reports a count other than 18</fails_when>
    <automated>dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~CrossBoardRouteCoverage</automated>
    <fails_when>non-zero exit, or the run summary reports "Passed: 0" / "total: 0", or any theory row reports a 404 for a route that should have resolved, or reports the board changed for one of the image rows</fails_when>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 with no new warnings.
    - `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CrossBoardLinkRegistry` exits 0 and the registry-size assertion passes at exactly 18.
    - `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~CrossBoardRouteCoverage` exits 0 and reports at least 26 tests passed (16 entity-route rows, 6 image rows, 1 modal fact, 3 profile facts).
    - Behaviour: for each of the 16 entity-backed routes, a navigation-shaped GET of the other board's URL no longer returns 404 and leaves that board active.
    - Behaviour: for each of the 6 image routes, the same GET with image-shaped fetch metadata leaves the active board unchanged.
    - Behaviour: a profile link to a user who shares two boards with the viewer leaves the active board unchanged.
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Service/Helpers/CrossBoardLinkRegistry.cs | grep -cE 'GetProfilePicture|GetCroppedPicture|GetContactImage|GetCroppedContactImage|GetDMProfilePicture|GetOriginalDMProfilePicture'` is 0 — no image route is registered, and a comment explaining their absence cannot satisfy the gate.
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Service/Helpers/CrossBoardLinkRegistry.cs | grep -c 'nameof('` is at least 18 — every action name is derived, not typed.
    - `dotnet test` exits 0 (full suite).
  </acceptance_criteria>
  <done>All 18 routes named in the phase decisions resolve across boards, each proven by a real HTTP round trip; the image subresources and the modal variant provably do not, and the registry contains no exclusion list to keep in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → application | 18 URLs now carry an entity id that the application will resolve past the board filters |
| application → tenancy boundary | `CrossBoardLinkRepository` gains seven more projections; it remains the only production code permitted to read past the 18 `HasQueryFilter` predicates |
| future contributor → the seam | Anyone adding a repository query or a controller action can widen what the escape hatch covers, months after this phase closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-03 | Tampering | The filter-bypassing call spreading by copy-paste | high | mitigate | `CrossBoardIgnoreQueryFiltersSeamTests` scans every production `.cs` file under the three source projects with comments stripped and fails naming any occurrence outside a five-entry allowlist. Landed in Task 1, before the seven new projections in Task 2, so the expansion happens inside an enforced boundary. This is the mechanism the roadmap's top risk asks for, in place of an intention. |
| T-87-08 | Information Disclosure | A new projection returning more than a board id | high | mitigate | Every projection is `Select(x => (int?)x.GroupId)` with the membership set pinned in the same predicate. Pinned by source acceptance criteria asserting seven scalar projections and zero entity-returning or `Include`-bearing queries in the file. |
| T-87-09 | Elevation of Privilege | A route gaining cross-board reach by accident — a new action, or a renamed one | medium | mitigate | The registry is closed and keyed by `nameof`-derived names, so a rename breaks the build and an addition requires an explicit line. `CrossBoardLinkRegistryTests` asserts the exact membership count, so adding a route without deliberation fails a test. |
| T-87-10 | Information Disclosure | Profile routes resolving ambiguously and revealing a shared board | medium | mitigate | The profile kind resolves only when the viewer and the target share exactly one board, and the shared-boards query is itself membership-pinned, so the set can only ever contain boards the viewer already belongs to. No count, name or identity of any other board is returned or implied. |
| T-87-11 | Tampering | Image subresources repointing the board while a page loads | medium | mitigate | The six image routes are absent from the registry *and* arrive with a non-document fetch destination, so two independent conditions each stop them. Proven by a theory asserting the board is unchanged, not merely that the response looks ordinary. |
| T-87-SC | Tampering | npm/pip/cargo/NuGet installs | high | mitigate | Not applicable in practice: this plan installs zero packages, and `87-RESEARCH.md` § Package Legitimacy Audit records N/A for the whole phase. No install task exists. If an executor finds itself about to run `dotnet add package`, stop and escalate. |
</threat_model>

<verification>
- `dotnet build` exits 0.
- `dotnet test` exits 0 (full suite).
- `dotnet test --filter "FullyQualifiedName~CrossBoard"` exits 0 with a non-zero passed count.
- The filter-bypassing call appears in exactly five production files, all under `QuestBoard.Repository`.
- No source file, XML doc comment or string literal contains a phase number, a `D-NN` decision id, a plan number or a planning document name.
</verification>

<success_criteria>
- All 18 board-scoped GET routes that take an id resolve across boards, each proven by an HTTP round trip rather than by a registry entry alone.
- The six image routes and the modal variant provably do not move the board, and there is no exclusion list beside the registry to keep in sync.
- The profile routes resolve only when unambiguous, and a missing id is a self-edit by construction.
- Adding a filter-bypassing call anywhere else in production code fails the build.
- The lookup still answers with a board id and nothing else, for every one of the eight kinds.
</success_criteria>

<output>
Create `.planning/phases/87-cross-board-deep-link-recovery/87-02-SUMMARY.md` when done.
</output>
