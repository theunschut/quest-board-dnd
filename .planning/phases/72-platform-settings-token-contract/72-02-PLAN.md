---
phase: 72-platform-settings-token-contract
plan: 02
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - QuestBoard.Repository/IntegrationSettingRepository.cs
  - QuestBoard.Domain/Services/IntegrationSettingService.cs
  - QuestBoard.Repository/Extensions/ServiceExtensions.cs
  - QuestBoard.Domain/Extensions/ServiceExtensions.cs
  - QuestBoard.UnitTests/Services/IntegrationSettingServiceTests.cs
autonomous: true
requirements: [SETT-04, SETT-05, SETT-06]

must_haves:
  truths:
    - "GetAsync returns the current settings row, creating a default disabled/empty row on first access"
    - "SaveAsync with a non-blank secret overwrites OmphalosSharedSecret"
    - "SaveAsync with a null/whitespace secret preserves the previously stored secret (never overwrites with empty)"
    - "IsEnabled and OmphalosUrl round-trip through SaveAsync/GetAsync unchanged"
  artifacts:
    - path: "QuestBoard.Domain/Services/IntegrationSettingService.cs"
      provides: "GetAsync bootstrap + SaveAsync blank-preserve guard"
      min_lines: 25
    - path: "QuestBoard.Repository/IntegrationSettingRepository.cs"
      provides: "BaseRepository-derived repository for the singleton"
    - path: "QuestBoard.UnitTests/Services/IntegrationSettingServiceTests.cs"
      provides: "Behavior tests for blank-preserve, enabled-flag, bootstrap"
  key_links:
    - from: "QuestBoard.Domain/Extensions/ServiceExtensions.cs"
      to: "IntegrationSettingService"
      via: "AddScoped<IIntegrationSettingService, IntegrationSettingService>()"
      pattern: "IIntegrationSettingService"
    - from: "QuestBoard.Repository/Extensions/ServiceExtensions.cs"
      to: "IntegrationSettingRepository"
      via: "AddScoped<IIntegrationSettingRepository, IntegrationSettingRepository>()"
      pattern: "IIntegrationSettingRepository"
---

<objective>
Implement the repository and Domain service behind the contracts from plan 72-01, wire both into DI, and prove the two security-relevant behaviors with unit tests: the blank-preserves-existing-secret guard (SETT-04) and the enabled-flag persistence (SETT-05).

This is a test-first (RED→GREEN) plan: Task 1 writes the failing behavior tests that pin the contract, Task 2 implements the service/repository/DI to make them green.

Purpose: SETT-04 (blank secret on save preserves the existing value), SETT-05 (IsEnabled persists), SETT-06 (single-row persistence round-trips).

Output: New repository + service implementations, DI registrations in both `ServiceExtensions.cs` files, and a passing unit-test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-platform-settings-token-contract/72-RESEARCH.md
@.planning/phases/72-platform-settings-token-contract/72-PATTERNS.md
@.planning/phases/72-platform-settings-token-contract/72-01-SUMMARY.md

<interfaces>
<!-- Contracts from plan 72-01 that this plan implements. -->
IIntegrationSettingService:
  Task<IntegrationSetting> GetAsync(CancellationToken token = default)
  Task SaveAsync(string omphalosUrl, string? newSecret, bool isEnabled, CancellationToken token = default)

IIntegrationSettingRepository : IBaseRepository<IntegrationSetting>
  (inherits GetByIdAsync(int id), AddAsync(model), UpdateAsync(model))

IntegrationSetting (IModel): int Id, string OmphalosUrl, string OmphalosSharedSecret, bool IsEnabled

BaseRepository<TModel,TEntity>.GetByIdAsync uses DbSet.FindAsync([id]) — returns null when no row exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing unit tests for IntegrationSettingService (RED)</name>
  <files>QuestBoard.UnitTests/Services/IntegrationSettingServiceTests.cs</files>
  <read_first>
    - QuestBoard.UnitTests/Services/GroupServiceTests.cs (NSubstitute + xUnit v3 + FluentAssertions pattern, TestContext.Current.CancellationToken usage)
    - QuestBoard.Domain/Interfaces/IIntegrationSettingService.cs (from 72-01)
    - QuestBoard.Domain/Interfaces/IIntegrationSettingRepository.cs (from 72-01)
    - QuestBoard.Domain/Models/IntegrationSetting.cs (from 72-01)
    - .planning/phases/72-platform-settings-token-contract/72-PATTERNS.md (IntegrationSettingServiceTests analog section)
  </read_first>
  <action>
    Create `IntegrationSettingServiceTests` in QuestBoard.UnitTests/Services/ modeled on `GroupServiceTests`: constructor substitutes `IIntegrationSettingRepository` (and `IMapper` only if the service constructor requires it — match the actual constructor once Task 2's shape is decided; the service delegates persistence to the repository, so the repository substitute is the primary collaborator). Use NSubstitute (`Substitute.For<>`, `.Returns(...)`, `.Received(1)`), FluentAssertions `.Should()`, and `TestContext.Current.CancellationToken`.

    Cover these behaviors (the service contract this plan pins):
    1. `GetAsync_WhenRowExists_ReturnsIt`: repository `GetByIdAsync(1)` returns an existing `IntegrationSetting`; assert `GetAsync` returns that same instance/values.
    2. `GetAsync_WhenNoRow_BootstrapsDefaultDisabledRow`: repository `GetByIdAsync(1)` returns null; assert `GetAsync` calls `AddAsync` once with a default row (`Id == 1`, `IsEnabled == false`, empty `OmphalosSharedSecret`) and returns a non-null default.
    3. `SaveAsync_WithNonBlankSecret_OverwritesStoredSecret`: existing row has secret "old"; call `SaveAsync(url, "new-secret", true)`; assert the value passed to the repository's `UpdateAsync` has `OmphalosSharedSecret == "new-secret"`.
    4. `SaveAsync_WithNullSecret_PreservesExistingSecret`: existing row has secret "old"; call `SaveAsync(url, null, true)`; assert the value passed to `UpdateAsync` has `OmphalosSharedSecret == "old"` — never empty string. Add a second case using a whitespace string ("   ") that must behave identically to null (belt-and-suspenders; the controller passes null, but the service must not trust that).
    5. `SaveAsync_PersistsIsEnabledAndUrl`: assert `IsEnabled` and `OmphalosUrl` from the call reach the persisted entity/model unchanged.

    Use `Arg.Is<IntegrationSetting>(s => ...)` matchers to assert the exact persisted values. These tests will not compile until Task 2 exists — that is the expected RED state.
  </action>
  <verify>
    <automated>MISSING — this test file is the Wave 0 RED scaffold; it will compile-fail until Task 2 implements IntegrationSettingService. After Task 2, `dotnet test QuestBoard.UnitTests --filter "FullyQualifiedName~IntegrationSettingService"` must compile and the blank-preserve/bootstrap/round-trip facts must pass.</automated>
  </verify>
  <acceptance_criteria>
    - `IntegrationSettingServiceTests.cs` exists with the five behaviors above as distinct `[Fact]` methods (the whitespace-secret case may be a `[Theory]` row or a sixth `[Fact]`).
    - Each blank-preserve assertion checks the persisted secret equals the prior stored value, and explicitly asserts it is NOT `string.Empty`.
    - Tests reference `TestContext.Current.CancellationToken` (xUnit v3), not `CancellationToken.None`, matching the codebase convention.
    - The file references `IntegrationSettingService` (concrete) so it compiles only after Task 2.
  </acceptance_criteria>
  <done>The behavior test file exists and encodes the blank-preserve, bootstrap, and enabled/url round-trip contract; it is RED (uncompilable) pending Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Implement repository + service + DI registration (GREEN)</name>
  <files>QuestBoard.Repository/IntegrationSettingRepository.cs, QuestBoard.Domain/Services/IntegrationSettingService.cs, QuestBoard.Repository/Extensions/ServiceExtensions.cs, QuestBoard.Domain/Extensions/ServiceExtensions.cs</files>
  <read_first>
    - QuestBoard.Repository/GroupRepository.cs (internal class + BaseRepository derivation shape)
    - QuestBoard.Repository/BaseRepository.cs (GetByIdAsync/AddAsync/UpdateAsync mechanics — reuse, do not reimplement)
    - QuestBoard.Domain/Services/GroupService.cs (delegation style, constructor-injected repository + IMapper)
    - QuestBoard.Repository/Extensions/ServiceExtensions.cs (repository AddScoped registrations, ~lines 18-31)
    - QuestBoard.Domain/Extensions/ServiceExtensions.cs (service AddScoped registrations, ~lines 15-24)
    - QuestBoard.UnitTests/Services/IntegrationSettingServiceTests.cs (from Task 1 — the contract to satisfy)
    - .planning/phases/72-platform-settings-token-contract/72-PATTERNS.md (repository/service target shapes, Pattern 1/3)
  </read_first>
  <action>
    Create `IntegrationSettingRepository` in QuestBoard.Repository/ (flat file, matching `GroupRepository.cs` location) as `internal class IntegrationSettingRepository(QuestBoardContext dbContext, IMapper mapper) : BaseRepository<IntegrationSetting, IntegrationSettingEntity>(dbContext, mapper), IIntegrationSettingRepository` — no bespoke methods; it inherits `GetByIdAsync`/`AddAsync`/`UpdateAsync`.

    Create `IntegrationSettingService` in QuestBoard.Domain/Services/ implementing `IIntegrationSettingService` directly (NOT `BaseService<T>` — the interface deviates per RESEARCH.md Pattern 1). Constructor injects `IIntegrationSettingRepository repository` (match whatever the Task 1 tests substitute; add `IMapper` only if actually needed — the repository already maps, so the service likely does not need IMapper). Implement:
    - `GetAsync`: call `repository.GetByIdAsync(1)`; if null, construct a default `IntegrationSetting { Id = 1, OmphalosUrl = "", OmphalosSharedSecret = "", IsEnabled = false }`, `AddAsync` it, and return it (first-run bootstrap — no raw SQL, per RESEARCH.md Don't Hand-Roll).
    - `SaveAsync(omphalosUrl, newSecret, isEnabled, token)`: load the current row via `GetAsync` (guarantees a row exists), set `OmphalosUrl = omphalosUrl`, set `IsEnabled = isEnabled`, and update `OmphalosSharedSecret` ONLY when `!string.IsNullOrWhiteSpace(newSecret)` — otherwise leave the existing secret untouched (the blank-preserve guard, SETT-04). Persist via `repository.UpdateAsync(current)`. Add a plain-language comment on the secret guard (no requirement-ID tokens per CLAUDE.md).

    Register DI: in QuestBoard.Repository/Extensions/ServiceExtensions.cs add `services.AddScoped<IIntegrationSettingRepository, IntegrationSettingRepository>();` alongside the existing repository registrations. In QuestBoard.Domain/Extensions/ServiceExtensions.cs add `services.AddScoped<IIntegrationSettingService, IntegrationSettingService>();` alongside the existing service registrations.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.UnitTests --filter "FullyQualifiedName~IntegrationSettingService"</automated>
  </verify>
  <acceptance_criteria>
    - All `IntegrationSettingServiceTests` from Task 1 pass (GREEN).
    - `IntegrationSettingService.cs` contains a `string.IsNullOrWhiteSpace` guard around the secret assignment and does NOT assign `OmphalosSharedSecret` in the blank branch.
    - `IntegrationSettingService` does NOT inherit `BaseService<` (grep confirms narrow implementation).
    - Both `ServiceExtensions.cs` files contain the two new `AddScoped` lines (`IIntegrationSettingRepository` and `IIntegrationSettingService`).
    - `dotnet build` for the full solution succeeds.
  </acceptance_criteria>
  <done>Repository and service are implemented and DI-registered; the blank-preserve and bootstrap behaviors are proven green by the unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → service | The service is the last guard before the secret is written; it must never persist an empty secret over a real one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05 | Tampering / Information Disclosure | `SaveAsync` accidentally wiping the stored secret with an empty value | mitigate | `string.IsNullOrWhiteSpace(newSecret)` guard in the service (SETT-04); unit-tested for both null and whitespace inputs so a blank field can never destroy a working secret. |
| T-72-04 | Information Disclosure | Secret plaintext at rest | accept | Same accepted posture recorded in plan 72-01; no new exposure introduced here. |
| T-72-SC | Tampering | package installs | accept (N/A) | Zero new packages (RESEARCH.md Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `dotnet test QuestBoard.UnitTests --filter "FullyQualifiedName~IntegrationSettingService"` is green.
- `dotnet build` (full solution) succeeds with the new DI registrations.
- Blank/whitespace secret never overwrites the stored value; enabled flag and URL round-trip.
</verification>

<success_criteria>
- SETT-04: blank secret on save preserves the existing value (unit-proven for null and whitespace).
- SETT-05: `IsEnabled` persists through SaveAsync/GetAsync.
- SETT-06: the singleton row round-trips (bootstrap-on-first-access, update thereafter).
</success_criteria>

<output>
Create `.planning/phases/72-platform-settings-token-contract/72-02-SUMMARY.md` when done.
</output>
