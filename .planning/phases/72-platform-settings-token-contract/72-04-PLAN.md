---
phase: 72-platform-settings-token-contract
plan: 04
type: execute
wave: 3
depends_on: ["72-03"]
files_modified:
  - QuestBoard.Service/Areas/Platform/Controllers/IntegrationsController.cs
  - QuestBoard.Service/ViewModels/PlatformViewModels/IntegrationSettingsViewModel.cs
  - QuestBoard.Service/Areas/Platform/Views/Integrations/Index.cshtml
  - QuestBoard.Service/Areas/Platform/Views/Integrations/Index.Mobile.cshtml
  - QuestBoard.Service/wwwroot/css/platform-integrations.mobile.css
  - QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml
  - QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml
autonomous: true
requirements: [SETT-01, SETT-02, SETT-03, SETT-04, SETT-05, SETT-07]
must_haves:
  truths:
    - "A SuperAdmin reaches an Integrations page from the /platform area and saves an Omphalos URL, a masked shared secret, and an enabled toggle as the instance-wide default"
    - "The GET view never renders the real stored secret; it shows a configured/not-configured indicator instead"
    - "Submitting with the secret field blank preserves the previously-saved secret"
    - "A 'Generate Secret' action creates a CSPRNG secret, persists it immediately, and shows it once"
  artifacts:
    - "QuestBoard.Service/Areas/Platform/Controllers/IntegrationsController.cs"
    - "QuestBoard.Service/ViewModels/PlatformViewModels/IntegrationSettingsViewModel.cs"
    - "QuestBoard.Service/Areas/Platform/Views/Integrations/Index.cshtml (+ .Mobile)"
  key_links:
    - "IntegrationsController -> IPlatformSettingService (GetForScope(null)/Save(null)/GenerateAndSaveSecret(null))"
    - "Group/Index header button -> Integrations/Index (desktop + mobile)"
    - "[Authorize(Policy = SuperAdminOnly)] on IntegrationsController"
---

<objective>
Build the SuperAdmin instance-wide Integrations settings page under `/platform` — the surface for the `GroupId == null` default rows. A lean `[Area("Platform")] [Authorize(Policy = "SuperAdminOnly")]` controller (modeled on `UsersController`), its ViewModel, desktop + mobile modern-card views, mobile CSS, and the header-button nav entry on the existing Group Management page.

Purpose: This is the SuperAdmin's runtime-editable control for the platform-wide Omphalos URL/secret/enabled default. It resolves and persists through the service from 72-03 with scope `null`.
Output: A working, authorization-gated, mobile-paired settings page reachable from `/platform`.

Implements: SETT-01 (navigate from /platform), SETT-02 (URL + secret fields), SETT-03 (secret masked `type="password"`), SETT-04 (blank preserves secret — controller maps blank→null into the service guard), SETT-05 (Integration Enabled toggle persisted), SETT-07 (SuperAdminOnly). Realizes D-03 (Generate Secret button, persists immediately), D-05 ("Integrations" generic naming under /platform).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-platform-settings-token-contract/72-CONTEXT.md
@.planning/phases/72-platform-settings-token-contract/72-PATTERNS.md
@.planning/phases/72-platform-settings-token-contract/72-RESEARCH.md
@.planning/phases/72-platform-settings-token-contract/72-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

- `QuestBoard.Service.Areas.Platform.Controllers.IntegrationsController` — `[Area("Platform")] [Authorize(Policy = "SuperAdminOnly")]`, actions `Index` (GET), `Index` (POST save), `GenerateSecret` (POST)
- `QuestBoard.Service.ViewModels.PlatformViewModels.IntegrationSettingsViewModel` — `OmphalosUrl`, `SharedSecret`, `IsEnabled`, `HasSecretConfigured` (never the raw secret)
- `Areas/Platform/Views/Integrations/Index.cshtml` + `Index.Mobile.cshtml` — modern-card settings form
- `wwwroot/css/platform-integrations.mobile.css`
- Third header button on `Group/Index.cshtml` + `Index.Mobile.cshtml`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IntegrationsController + IntegrationSettingsViewModel</name>
  <files>QuestBoard.Service/Areas/Platform/Controllers/IntegrationsController.cs, QuestBoard.Service/ViewModels/PlatformViewModels/IntegrationSettingsViewModel.cs</files>
  <behavior>
    - GET Index: renders the form pre-filled with the instance-default URL and IsEnabled, HasSecretConfigured reflecting whether a default secret exists, and SharedSecret always blank.
    - POST Index (valid): saves URL + IsEnabled, preserves the secret when SharedSecret is blank, sets TempData Success, redirects to Index.
    - POST Index (invalid URL): re-renders the form with the validation error, does not save.
    - POST GenerateSecret: generates+persists a new default secret immediately, carries the plaintext once via TempData, redirects to Index.
  </behavior>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Controllers/UsersController.cs (whole file — [Area]/[Authorize] class attributes, GET Index building a ViewModel, [HttpPost][ValidateAntiForgeryToken] mutating actions with TempData Success/Error + RedirectToAction)
    - QuestBoard.Service/ViewModels/PlatformViewModels/ (existing PlatformUserViewModel shape)
    - QuestBoard.Domain/Interfaces/IPlatformSettingService.cs and QuestBoard.Domain/Models/OmphalosSettings.cs (from 72-01/72-03)
    - QuestBoard.Service/ViewModels/ShopViewModels/EditShopItemViewModel.cs ([Url] DataAnnotation precedent)
  </read_first>
  <action>
    Create `IntegrationSettingsViewModel` in `QuestBoard.Service.ViewModels.PlatformViewModels`: `[Url]`-annotated `string OmphalosUrl` (with a validation message matching the UI-SPEC copy: "Enter a valid URL, e.g. https://your-omphalos-instance.example.com."), `[StringLength(200)] string? SharedSecret`, `bool IsEnabled`, and `bool HasSecretConfigured`. Never add a property that carries the raw stored secret to the view.

    Create `IntegrationsController(IPlatformSettingService settingService) : Controller` in `QuestBoard.Service.Areas.Platform.Controllers` with `[Area("Platform")]` and `[Authorize(Policy = "SuperAdminOnly")]` at the class level (SETT-07 — controller-level, fails closed before the action body). Route is the default `{controller}/{action}` = `/platform/Integrations`.

    Actions (scope is always `null` on this page — instance-wide default):
      - `[HttpGet] Index()`: `var s = await settingService.GetForScopeAsync(null, ...)`; build the ViewModel with `OmphalosUrl = s.Url`, `IsEnabled = s.IsEnabled`, `HasSecretConfigured = s.HasSecret`, and `SharedSecret = null` (NEVER pre-fill the real secret — RESEARCH.md Pattern 3). If TempData carries a freshly generated secret (from GenerateSecret), surface it for the one-time display per the UI-SPEC. Return the view.
      - `[HttpPost][ValidateAntiForgeryToken] Index(IntegrationSettingsViewModel model)`: if `!ModelState.IsValid`, re-populate `HasSecretConfigured` from the service and return the view. Otherwise map blank→null: `var newSecret = string.IsNullOrWhiteSpace(model.SharedSecret) ? null : model.SharedSecret;` (add a plain-language comment: blank means keep the current secret — no planning identifiers). Call `settingService.SaveAsync(null, model.OmphalosUrl, newSecret, model.IsEnabled, ...)`, set `TempData["Success"] = "Integration settings saved."`, redirect to `Index`.
      - `[HttpPost][ValidateAntiForgeryToken] GenerateSecret()`: `var secret = await settingService.GenerateAndSaveSecretAsync(null, ...)` (persists immediately — RESEARCH.md Pitfall 3), put the plaintext in TempData for one render, set `TempData["Success"] = "New shared secret generated. Copy it now and update Omphalos's configuration — it won't be shown again."`, redirect to `Index`.
    Do not reference any planning/requirement identifier in source comments (root CLAUDE.md).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service` succeeds.
    - `IntegrationsController` carries `[Area("Platform")]` and `[Authorize(Policy = "SuperAdminOnly")]` at the class level.
    - Both mutating actions carry `[ValidateAntiForgeryToken]`.
    - The GET action never assigns the real secret to `IntegrationSettingsViewModel.SharedSecret`.
    - The POST save maps a blank/whitespace `SharedSecret` to null before calling the service.
    - `GenerateSecret` calls `GenerateAndSaveSecretAsync(null, ...)` and redirects (does not defer persistence).
  </acceptance_criteria>
  <done>The SuperAdmin controller resolves/persists the instance-wide default via scope null, gated by SuperAdminOnly, with the blank-preserve and generate-immediately semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Desktop + mobile Integrations views and mobile CSS</name>
  <files>QuestBoard.Service/Areas/Platform/Views/Integrations/Index.cshtml, QuestBoard.Service/Areas/Platform/Views/Integrations/Index.Mobile.cshtml, QuestBoard.Service/wwwroot/css/platform-integrations.mobile.css</files>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml lines 1-21 (modern-card shell, card-header, header-button row)
    - QuestBoard.Service/Views/Admin/EditUser.cshtml (masked input + plain form-check checkbox markup, asp-validation, hr-before-buttons, d-flex justify-content-between button row)
    - QuestBoard.Service/wwwroot/css/platform-users.mobile.css (mobile CSS naming + wrapper-class convention)
    - .planning/phases/72-platform-settings-token-contract/72-UI-SPEC.md (Copywriting Contract, Color, Typography, Spacing for the instance-wide page)
  </read_first>
  <action>
    Create `Areas/Platform/Views/Integrations/Index.cshtml` typed to `IntegrationSettingsViewModel`, using the modern-card shell (`card modern-card` / `card-header modern-card-header` / `card-body modern-card-body`) exactly as `Group/Index.cshtml`. Card header: `<h2 class="mb-0"><i class="fas fa-plug text-danger me-2"></i>Integrations</h2>`. Intro line (text-muted paragraph) per UI-SPEC: "Configure the connection Quest Board uses to hand DMs and players off into Omphalos session notes."

    Form (single form, POST to `Index`, include the anti-forgery token via `asp-antiforgery`/the default tag helper):
      - Omphalos URL: `asp-for="OmphalosUrl"` text input, `form-label` "Omphalos URL", placeholder `https://your-omphalos-instance.example.com`, `form-text` helper "Base URL of your Omphalos instance. Used to build the signed redirect link.", `asp-validation-for="OmphalosUrl"`.
      - Shared Secret: `asp-for="SharedSecret"` input with `type="password"` (SETT-03 — masked). Below it render the secret-status caption from the ViewModel's `HasSecretConfigured`: configured → text-muted "•••• Configured — leave blank to keep the current secret." with a `bg-success` "Configured" badge; not configured → text-muted italic "Not configured yet — click Generate Secret or paste a value below." with a `bg-secondary` "Not configured" badge. When TempData carries a freshly generated secret, show it once here.
      - Integration Enabled: plain `form-check` checkbox `asp-for="IsEnabled"` (NEVER `form-switch` — RESEARCH.md Pattern 5), label with `fa-toggle-on text-success me-2` "Integration Enabled".
      - `asp-validation-summary="ModelOnly"` for the generic save-failure message.
      - `<hr>` before the button row (mandatory per CLAUDE.md). Button row `d-flex justify-content-between`: left = "Back to Group Management" (`btn-secondary`, `fa-arrow-left`) linking to `Group/Index` in the Platform area; right cluster = "Generate Secret" (`btn-danger`, `fa-key`, wired to POST the `GenerateSecret` action, guarded by a JS `confirm()` with the UI-SPEC copy "Generate a new shared secret? The current secret stops working immediately. Update Omphalos's configured secret to match, or SSO will fail until you do.") and "Save Settings" (`btn-warning`, `fa-save`, submit).

    Create `Index.Mobile.cshtml` with the same fields/labels/validation and the same actions, adapted to the mobile layout convention used by the other Platform `.Mobile.cshtml` views, wrapped in the `.platform-integrations-card-mobile` class. Create `wwwroot/css/platform-integrations.mobile.css` following `platform-users.mobile.css` verbatim in structure (wrapper class `.platform-integrations-card-mobile`), referenced by the mobile view the same way the sibling mobile views reference their CSS. Desktop and mobile views ship together in this task (mobile parity is mandatory — RESEARCH.md Pattern 4).

    Add zero new component classes to `site.css` — compose only existing modern-card classes (UI-SPEC).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service && grep -q 'type="password"' QuestBoard.Service/Areas/Platform/Views/Integrations/Index.cshtml && grep -q 'type="password"' QuestBoard.Service/Areas/Platform/Views/Integrations/Index.Mobile.cshtml && grep -q "modern-card" QuestBoard.Service/Areas/Platform/Views/Integrations/Index.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Service` succeeds.
    - Both `Index.cshtml` and `Index.Mobile.cshtml` render the shared-secret input with `type="password"`.
    - Both views use the modern-card shell and a plain `form-check` checkbox (no `form-switch`).
    - Both views include an `<hr>` before a `d-flex justify-content-between` button row, with Save on the right (`btn-warning`) and Back on the left.
    - `Generate Secret` is a `btn-danger` POST guarded by a JS `confirm()`.
    - `wwwroot/css/platform-integrations.mobile.css` exists using the `.platform-integrations-card-mobile` wrapper convention.
    - No new component classes are added to `site.css`.
  </acceptance_criteria>
  <done>Desktop and mobile Integrations views render the masked-secret form with the correct copy, colors, and actions; mobile CSS follows the sibling convention.</done>
</task>

<task type="auto">
  <name>Task 3: Nav entry — Integrations header button on Group/Index (desktop + mobile)</name>
  <files>QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml, QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml lines 6-20 (header button row with Create Group + Manage Users)
    - QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml (the mobile header/button region)
  </read_first>
  <action>
    In `Group/Index.cshtml`, add a third header button in the existing `d-flex gap-2` row (after "Manage Users"): `<a asp-controller="Integrations" asp-action="Index" asp-area="Platform" class="btn btn-secondary"><i class="fas fa-plug me-2"></i>Integrations</a>` (SETT-01 nav entry; the Platform area has no shared nav list — the header button is the established cross-link mechanism, RESEARCH.md Pattern 4).

    Make the equivalent addition in `Group/Index.Mobile.cshtml` so the mobile Group Management page exposes the same Integrations entry point. Both edits ship in this one task (mobile parity mandatory).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service && grep -q 'asp-controller="Integrations"' QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml && grep -q 'asp-controller="Integrations"' QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Both `Group/Index.cshtml` and `Group/Index.Mobile.cshtml` contain a link to the Platform-area `Integrations`/`Index` action.
    - `dotnet build QuestBoard.Service` succeeds.
  </acceptance_criteria>
  <done>A SuperAdmin can reach the Integrations page from Group Management on both desktop and mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (SuperAdmin) → Platform controller | Untrusted form input (URL, secret, enabled) and mutating POSTs cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-04-01 | Elevation of Privilege | Non-SuperAdmin reaching the instance-wide settings page | high | mitigate | `[Authorize(Policy = "SuperAdminOnly")]` at the controller level; ASP.NET Core authorization fails closed before the action body. Verified by the Wave 4 authorization matrix (72-06). |
| T-72-04-02 | Information Disclosure | Secret leaked via GET-populated masked field | high | mitigate | GET never assigns the stored secret to the ViewModel; a configured/not-configured indicator is shown instead (RESEARCH.md Pattern 3). |
| T-72-04-03 | Tampering | CSRF on save / generate-secret POST | medium | mitigate | `[ValidateAntiForgeryToken]` on both mutating actions + the anti-forgery token in the form. |
| T-72-04-04 | Input Validation | Malformed URL | low | mitigate | `[Url]` DataAnnotation on `OmphalosUrl` + server-side `ModelState` re-render on invalid. |
| T-72-04-SC | Tampering | NuGet package installs | low | accept | Zero new packages; server-side secret generation uses BCL `RandomNumberGenerator` (from 72-03). |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` succeeds.
- The masked-secret and modern-card grep gates pass on both desktop and mobile views.
- The authorization gate and end-to-end rendering are exercised by 72-06 (Wave 4).
</verification>

<success_criteria>
- A SuperAdmin navigates to Integrations from /platform, saves URL + masked secret + enabled toggle as the instance-wide default, and the values persist.
- Saving with the secret blank preserves the prior secret.
- Generate Secret produces and persists a new secret shown once.
- The GET view never exposes the stored secret.
</success_criteria>

<output>
Create `.planning/phases/72-platform-settings-token-contract/72-04-SUMMARY.md` when done.
</output>
