---
phase: 72-platform-settings-token-contract
plan: 04
type: execute
wave: 3
depends_on: [72-02, 72-03]
files_modified:
  - QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml
  - QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml
  - QuestBoard.IntegrationTests/Controllers/IntegrationsAreaIntegrationTests.cs
autonomous: true
requirements: [SETT-01, SETT-07]

must_haves:
  truths:
    - "A SuperAdmin can reach the Integrations page from the Group Management page via a header button (desktop and mobile)"
    - "A SuperAdmin GET of /platform/Integrations/Index returns 200 (full stack renders end to end)"
    - "Admin, Player, and unauthenticated requests to the Integrations page are denied/redirected"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/IntegrationsAreaIntegrationTests.cs"
      provides: "4-case authorization matrix + 200-render check"
      contains: "IntegrationsIndex"
  key_links:
    - from: "QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml"
      to: "IntegrationsController.Index"
      via: "asp-controller=Integrations asp-action=Index asp-area=Platform header button"
      pattern: "asp-controller=\"Integrations\""
    - from: "QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml"
      to: "IntegrationsController.Index"
      via: "asp-controller=Integrations header button (btn-sm)"
      pattern: "asp-controller=\"Integrations\""
---

<objective>
Wire the navigation entry point to the new Integrations page and prove the authorization contract end to end. Adds the "Integrations" header button to the Group Management page (desktop + mobile, in the SAME task) and creates the integration-test authorization matrix.

There is no shared Platform-area nav list — cross-linking is done via a header button on the source page (RESEARCH.md Pattern 4). This is the third button alongside "Create Group" and "Manage Users".

Purpose: completes SETT-01 (navigate to the page from /platform) and verifies SETT-07 (SuperAdminOnly) with an automated authorization matrix modeled on the existing Platform-area tests.

Output: modified Group/Index desktop + mobile views, and `IntegrationsAreaIntegrationTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-platform-settings-token-contract/72-RESEARCH.md
@.planning/phases/72-platform-settings-token-contract/72-PATTERNS.md
@.planning/phases/72-platform-settings-token-contract/72-02-SUMMARY.md
@.planning/phases/72-platform-settings-token-contract/72-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Integrations" header button to Group/Index (desktop + mobile)</name>
  <files>QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml, QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Views/Group/Index.cshtml (existing header button row, lines 12-19 — "Create Group" + "Manage Users")
    - QuestBoard.Service/Areas/Platform/Views/Group/Index.Mobile.cshtml (existing header button row, lines 15-22 — same two buttons with btn-sm)
    - .planning/phases/72-platform-settings-token-contract/72-UI-SPEC.md (nav button copy/icon: "Integrations", btn-secondary, fa-plug)
  </read_first>
  <action>
    In `Group/Index.cshtml`, inside the existing `<div class="d-flex gap-2">` header button group (after the "Manage Users" anchor), add a third anchor: `<a asp-controller="Integrations" asp-action="Index" asp-area="Platform" class="btn btn-secondary"><i class="fas fa-plug me-2"></i>Integrations</a>`.

    In `Group/Index.Mobile.cshtml`, inside the existing `<div class="d-flex flex-wrap gap-2">` header button group (after the "Manage Users" anchor), add the same anchor with `btn btn-secondary btn-sm` sizing to match the mobile row: `<a asp-controller="Integrations" asp-action="Index" asp-area="Platform" class="btn btn-secondary btn-sm"><i class="fas fa-plug me-2"></i>Integrations</a>`.

    Do NOT add a shared-layout nav item — the header-button mechanism is the only cross-linking pattern in the Platform area (RESEARCH.md Pattern 4). Both edits happen in this one task (desktop+mobile parity mandate).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - `Group/Index.cshtml` contains an anchor with `asp-controller="Integrations"`, `asp-action="Index"`, `asp-area="Platform"`, class `btn btn-secondary`, and icon `fa-plug`.
    - `Group/Index.Mobile.cshtml` contains the same anchor with class `btn btn-secondary btn-sm`.
    - Neither `_Layout.Platform.cshtml` nor `_Layout.Platform.Mobile.cshtml` is modified (no shared nav item added).
    - `dotnet build QuestBoard.Service` succeeds.
  </acceptance_criteria>
  <done>The Integrations page is reachable from Group Management on both desktop and mobile via a header button; no shared nav item introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test — authorization matrix + 200 render</name>
  <files>QuestBoard.IntegrationTests/Controllers/IntegrationsAreaIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/PlatformAreaIntegrationTests.cs (the 4-test authorization matrix to copy verbatim and retarget)
    - QuestBoard.IntegrationTests/Helpers/ (AuthenticationHelper, TestDataHelper, WebApplicationFactoryBase — same helpers the analog uses)
    - .planning/phases/72-platform-settings-token-contract/72-PATTERNS.md (integration-test analog section)
  </read_first>
  <action>
    Create `IntegrationsAreaIntegrationTests : IClassFixture<WebApplicationFactoryBase>` in QuestBoard.IntegrationTests/Controllers/, copying the four-case shape from `PlatformAreaIntegrationTests` and retargeting the URL from `/platform/Group/Index` to `/platform/Integrations/Index`:
    - `IntegrationsIndex_WhenSuperAdmin_ShouldReturn200`: clears DB, authenticates a SuperAdmin (`AuthenticationHelper.CreateAuthenticatedSuperAdminClientAsync`), GETs `/platform/Integrations/Index`, asserts `HttpStatusCode.OK`. This exercises the full stack (controller + view from 72-03 + DI-registered service from 72-02 + entity/migration from 72-01) — a 500 here means the singleton bootstrap or view is broken.
    - `IntegrationsIndex_WhenNotSuperAdmin_ShouldDeny`: Player role → `BeOneOf(Forbidden, Redirect)`.
    - `IntegrationsIndex_WhenNotAuthenticated_ShouldRedirect`: non-redirecting client → `BeOneOf(Redirect, Found, Unauthorized)`.
    - `IntegrationsIndex_WhenAdmin_ShouldDeny`: Admin role → `BeOneOf(Forbidden, Redirect)`.
    Use the exact same `AuthenticationHelper`/`TestDataHelper` calls and `TestContext.Current.CancellationToken` as the analog. Rename the class/methods to Integrations.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Integrations"</automated>
  </verify>
  <acceptance_criteria>
    - `IntegrationsAreaIntegrationTests.cs` exists with the four `[Fact]` methods above, all targeting `/platform/Integrations/Index`.
    - `IntegrationsIndex_WhenSuperAdmin_ShouldReturn200` asserts `HttpStatusCode.OK` (proving end-to-end render, SETT-01).
    - The three non-SuperAdmin cases assert denial/redirect (proving SETT-07).
    - `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Integrations"` is green (all four pass).
  </acceptance_criteria>
  <done>The authorization matrix passes: SuperAdmin renders 200 end to end; Admin/Player/unauthenticated are denied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-SuperAdmin → /platform/Integrations | Must be denied before rendering — verified by automated tests here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02 | Elevation of Privilege | Non-SuperAdmin reaching the settings page | mitigate | Verified by the 4-case authorization matrix (Admin/Player/unauthenticated denied; SuperAdmin 200). The policy itself is implemented in plan 72-03; this plan proves it. |
| T-72-nav | Information Disclosure | Nav button rendered to non-SuperAdmin | mitigate | The button lives only on `/platform/Group/Index`, itself SuperAdminOnly — no non-SuperAdmin ever sees the link, and the target enforces the policy independently regardless of link visibility. |
| T-72-SC | Tampering | package installs | accept (N/A) | Zero new packages. |
</threat_model>

<verification>
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Integrations"` green.
- Header button present on desktop + mobile Group/Index; no shared nav item.
- `dotnet build QuestBoard.Service` succeeds.
</verification>

<success_criteria>
- SETT-01: the Integrations page is reachable from /platform (header button) and returns 200 for a SuperAdmin.
- SETT-07: Admin, Player, and unauthenticated users are denied — automated.
</success_criteria>

<output>
Create `.planning/phases/72-platform-settings-token-contract/72-04-SUMMARY.md` when done.
</output>
