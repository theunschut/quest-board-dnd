---
phase: 72-platform-settings-token-contract
plan: 06
type: execute
wave: 4
depends_on: ["72-04", "72-05"]
files_modified:
  - QuestBoard.IntegrationTests/Controllers/IntegrationsAreaIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/AdminIntegrationsAuthorizationTests.cs
autonomous: true
requirements: [SETT-01, SETT-07, SETT-09, SETT-10]
must_haves:
  truths:
    - "A SuperAdmin can reach the instance-wide Integrations page; an Admin, a Player, and an unauthenticated user cannot"
    - "A group Admin can reach the group-override page; a DungeonMaster and a Player cannot; an unauthenticated user is redirected"
    - "The new PlatformSettings migration applies cleanly against the integration-test database"
  artifacts:
    - "QuestBoard.IntegrationTests/Controllers/IntegrationsAreaIntegrationTests.cs"
    - "QuestBoard.IntegrationTests/Controllers/AdminIntegrationsAuthorizationTests.cs"
  key_links:
    - "Test host runs the AddPlatformSettings migration (SETT-08 proven transitively)"
    - "SuperAdminOnly matrix on /platform/Integrations (SETT-07)"
    - "AdminOnly (GroupRole.Admin, DM excluded) matrix on /AdminIntegrations (SETT-09/SETT-10)"
---

<objective>
Prove the authorization boundaries of both settings surfaces with integration tests that exercise the real ASP.NET Core authorization pipeline against a live test database (which also transitively proves the `AddPlatformSettings` migration applies cleanly).

Purpose: The security guarantees of this phase (SuperAdminOnly on the instance-wide page; group-scoped Admin-only, DM-excluded on the override page) are the highest-severity threats in the plan set. They are asserted end-to-end here, mirroring the existing `PlatformAreaIntegrationTests` and `AdminHandlerIntegrationTests` shapes exactly.
Output: Two green integration-test classes covering the full authorization matrix for both pages.

Implements verification for SETT-01 (SuperAdmin reaches the page), SETT-07 (instance-wide page SuperAdminOnly), SETT-09 (group Admin reaches the override page), SETT-10 (DungeonMaster denied).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-platform-settings-token-contract/72-CONTEXT.md
@.planning/phases/72-platform-settings-token-contract/72-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- `QuestBoard.IntegrationTests.Controllers.IntegrationsAreaIntegrationTests` — SuperAdmin/Admin/Player/unauthenticated matrix for `/platform/Integrations`
- `QuestBoard.IntegrationTests.Controllers.AdminIntegrationsAuthorizationTests` — Group Admin/DM/Player/unauthenticated matrix for `/AdminIntegrations/Index` (+ SuperAdmin bypass)

<tasks>

<task type="auto">
  <name>Task 1: Instance-wide Integrations authorization matrix (SETT-01, SETT-07)</name>
  <files>QuestBoard.IntegrationTests/Controllers/IntegrationsAreaIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/PlatformAreaIntegrationTests.cs (whole file — the exact 4-test shape: SuperAdmin 200, non-SuperAdmin Player deny, unauthenticated redirect, Admin deny; the AuthenticationHelper + TestDataHelper + WebApplicationFactoryBase usage)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs (CreateAuthenticatedSuperAdminClientAsync, CreateAuthenticatedClientWithUserAsync roles arg, CreateNonRedirectingClient)
  </read_first>
  <action>
    Create `IntegrationsAreaIntegrationTests` (`IClassFixture<WebApplicationFactoryBase>`) modeled verbatim on `PlatformAreaIntegrationTests`, retargeting the URL to the new instance-wide page `GET /platform/Integrations/Index`:
      - `Integrations_WhenSuperAdmin_ShouldReturn200`: authenticated SuperAdmin client → `HttpStatusCode.OK` (SETT-01 + SETT-07 positive).
      - `Integrations_WhenNotSuperAdmin_ShouldDeny`: a Player client → `Forbidden` or `Redirect`.
      - `Integrations_WhenAdmin_ShouldDeny`: an "Admin" (group-role Admin, not SuperAdmin) client → `Forbidden` or `Redirect` (proves the instance-wide page is NOT reachable by a mere group Admin — SETT-07).
      - `Integrations_WhenNotAuthenticated_ShouldRedirect`: `CreateNonRedirectingClient()` → `Redirect`/`Found`/`Unauthorized`.
    Use `TestDataHelper.ClearDatabaseAsync(_factory.Services)` at the start of each authenticated test and `TestContext.Current.CancellationToken` on requests, exactly as the model class does.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~IntegrationsAreaIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - The test class contains the four matrix tests above and all pass.
    - The SuperAdmin test asserts 200 against `/platform/Integrations/Index`; the Admin and Player tests assert deny; the unauthenticated test asserts redirect.
    - The tests run against the real test host, which applies the `AddPlatformSettings` migration (SETT-08 proven transitively — no separate migration-smoke test needed).
  </acceptance_criteria>
  <done>The instance-wide page's SuperAdminOnly boundary is proven: SuperAdmin in, Admin/Player/anonymous out.</done>
</task>

<task type="auto">
  <name>Task 2: Group-override authorization matrix (SETT-09, SETT-10)</name>
  <files>QuestBoard.IntegrationTests/Controllers/AdminIntegrationsAuthorizationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AdminHandlerIntegrationTests.cs (whole file — the AdminOnly matrix shape: GroupRole.Admin 200, Player deny, SuperAdmin bypass 200, unauthenticated redirect; retarget the URL)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs lines 110-142 (how roles: ["Admin"]/["DungeonMaster"]/["Player"] seed the UserGroups GroupRole for group 1, and how the factory defaults ActiveGroupId=1)
  </read_first>
  <action>
    Create `AdminIntegrationsAuthorizationTests` (`IClassFixture<WebApplicationFactoryBase>`) modeled on `AdminHandlerIntegrationTests`, targeting `GET /AdminIntegrations/Index`:
      - `GroupIntegrations_WhenGroupAdmin_ShouldReturn200`: `CreateAuthenticatedClientWithUserAsync(..., roles: ["Admin"])` (seeds `GroupRole.Admin` on group 1) → `HttpStatusCode.OK` (SETT-09).
      - `GroupIntegrations_WhenDungeonMaster_ShouldDeny`: `roles: ["DungeonMaster"]` → `Forbidden` or `Redirect` (SETT-10 — DM is explicitly excluded).
      - `GroupIntegrations_WhenPlayer_ShouldDeny`: `roles: ["Player"]` → `Forbidden` or `Redirect`.
      - `GroupIntegrations_WhenSuperAdmin_ShouldReturn200`: `CreateAuthenticatedSuperAdminClientAsync` → `HttpStatusCode.OK` (SuperAdmin bypass, consistent with AdminHandler).
      - `GroupIntegrations_WhenNotAuthenticated_ShouldRedirect`: `CreateNonRedirectingClient()` → `Redirect`/`Found`/`Unauthorized`.
    Use `TestDataHelper.ClearDatabaseAsync` + `TestContext.Current.CancellationToken` as the model class does.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~AdminIntegrationsAuthorizationTests"</automated>
  </verify>
  <acceptance_criteria>
    - The test class contains the five matrix tests above and all pass.
    - The Group Admin test asserts 200; the DungeonMaster and Player tests assert deny (proving SETT-10's DM exclusion); the SuperAdmin test asserts 200 (bypass); the unauthenticated test asserts redirect.
  </acceptance_criteria>
  <done>The group-override page's AdminOnly (GroupRole.Admin, DM-excluded) boundary is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → both settings controllers | This plan asserts the authorization boundary itself holds under real requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-06-01 | Elevation of Privilege | Regression that opens either settings page to the wrong role | high | mitigate | Full authorization matrix tests (SuperAdmin/Admin/DM/Player/anonymous) that fail the build if a boundary regresses — the standing guard against a future change silently weakening access control. |
| T-72-06-SC | Tampering | NuGet package installs | low | accept | Zero new packages; test infrastructure (xUnit v3, NSubstitute, WebApplicationFactoryBase) already present. |
</threat_model>

<verification>
- `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Integrations"` is green (both classes).
- Full suite `dotnet test` is green before phase verification.
</verification>

<success_criteria>
- Both authorization matrices pass, proving SETT-01/SETT-07 (instance-wide) and SETT-09/SETT-10 (group-override, DM excluded).
- The `AddPlatformSettings` migration applies cleanly in the integration-test host.
</success_criteria>

<output>
Create `.planning/phases/72-platform-settings-token-contract/72-06-SUMMARY.md` when done.
</output>
