---
phase: 86-viewer-local-times-and-correct-job-scheduling
plan: 05
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - QuestBoard.Service/Views/Quest/Manage.cshtml
  - QuestBoard.Service/Views/Quest/Details.cshtml
  - QuestBoard.Service/Views/Quest/_QuestCard.cshtml
  - QuestBoard.Service/Views/Shop/Index.cshtml
  - QuestBoard.Service/Views/ShopManagement/Index.cshtml
  - QuestBoard.Service/Views/Admin/EmailStats.cshtml
autonomous: true
# No REQ-IDs exist for Phase 86. Traceability anchors are CONTEXT.md decision IDs.
requirements: [D-01, D-02, D-05, D-06]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The seven SignupTime render sites on Quest/Manage.cshtml and Quest/Details.cshtml are wrapped in <time class="local-time"> with the design contract's assigned style, and their mobile twins are left alone because those twins use SignupTime only as an OrderBy key and never display it.
    - Model.Quest.CreatedAt on Quest/Manage.cshtml, Quest/Details.cshtml and the shared Quest/_QuestCard.cshtml partial renders through Html.LocalTime with the date style.
    - Shop/Index.cshtml's TransactionDate renders through Html.LocalTime while the DateTime.UtcNow minus TransactionDate arithmetic that drives the 24-hour return window stays entirely in UTC and is not fed a converted value.
    - Admin/EmailStats.cshtml's AsOf and ShopManagement/Index.cshtml's DeniedAt render through Html.LocalTime, with their existing null guards preserved as explicit conditionals that render exactly what they render today when the value is absent.
    - No mobile view gains a brand-new rendered field it did not have before — the six carve-out rows in the design contract are content gaps that predate this phase and stay as they are.
    - Every quest FinalizedDate and every ProposedDate on these views is left untouched, including the four datetime-local form round-trip values.
  artifacts:
    - QuestBoard.Service/Views/Quest/Manage.cshtml
    - QuestBoard.Service/Views/Quest/Details.cshtml
    - QuestBoard.Service/Views/Quest/_QuestCard.cshtml
    - QuestBoard.Service/Views/Shop/Index.cshtml
  key_links:
    - Quest/_QuestCard.cshtml is a shared partial rendered by both layouts through Quest/_QuestSection.cshtml:26, so one edit covers desktop and mobile with no twin to match.
    - Html.LocalTime is in scope through the @using QuestBoard.Service.Extensions line added to Views/_ViewImports.cshtml in 86-01.
---

<objective>
Convert the remaining real-instant render sites: quest signup times, quest creation dates, shop transaction dates, shop-management denial dates and the admin email-stats timestamp.

Purpose: this batch carries the three corrections that `86-RESEARCH.md` made against `86-CONTEXT.md`'s classification narrative, and the two landmines that a blanket sweep would trip — the UTC subtraction driving the shop's return window, and the six mobile twins that never rendered these values at all.

Output: six converted view files, with every carve-out and every wall-clock value provably untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-CONTEXT.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-UI-SPEC.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-01-SUMMARY.md
@CLAUDE.md
</context>

<recorded_correction>
Two corrections adopted from `86-RESEARCH.md` over `86-CONTEXT.md`, recorded here so the override is visible rather than silent. `86-UI-SPEC.md` §3 already folded both into its call-site table and marked them ✱.

**A. `SignupTime` is rendered, and is in scope (correction 1).** `86-CONTEXT.md` states: *"`SignupTime` needs nothing. It appears in the views only as an `OrderBy` key … never rendered."* `86-RESEARCH.md`'s direct grep found seven desktop render sites — `Quest/Manage.cshtml:283,326,362,515` rendering `"MMM dd, h:mm tt"` and `Quest/Details.cshtml:886,907,928` rendering `"MMM dd"`. A line-by-line code read outranks a prose summary, and `SignupTime` is already on the real-instant side of CONTEXT.md's own classification table. All seven convert. Their mobile twins genuinely do use `SignupTime` only as an `OrderBy` key, so the parity rule is satisfied by leaving them alone.

**B. `Admin/EmailStats.cshtml:63` `Model.AsOf` is in scope (correction 2).** Not named in CONTEXT.md's classification table. `AsOf` is assigned `DateTime.UtcNow` at `QuestBoard.Service/Controllers/Admin/AdminController.cs:491` and rendered raw through `.ToString("g")`. The locked rule is that conversion is a property of the value, not of the format string and not of the page's audience, so a low-traffic admin surface converts like any other.

*(The third correction — `Areas/Platform/Views/Group/Index.cshtml` — is recorded and planned in `86-04`.)*
</recorded_correction>

<carve_out_contract>
`86-UI-SPEC.md` §5's non-regression carve-out is binding on this plan. **Do NOT "fix" these by adding a new render site** — converting a value where it already renders is this phase's job; adding a brand-new rendered field to a mobile view to match its desktop twin is scope creep.

| Desktop site in this plan | Property | Mobile twin | Why it is not touched |
|---|---|---|---|
| `Quest/Manage.cshtml:754` | `CreatedAt` | `Manage.Mobile.cshtml` | never rendered `CreatedAt` — pre-existing content gap |
| `Quest/Details.cshtml:844` | `CreatedAt` | `Details.Mobile.cshtml` | same |
| `Quest/Manage.cshtml:283,326,362,515` | `SignupTime` | `Manage.Mobile.cshtml` | uses `SignupTime` only as an `OrderBy` key |
| `Quest/Details.cshtml:886,907,928` | `SignupTime` | `Details.Mobile.cshtml` | same |
| `Shop/Index.cshtml:105,151` | `TransactionDate` | `Shop/Index.Mobile.cshtml` | never rendered `TransactionDate` |
| `ShopManagement/Index.cshtml:148` | `DeniedAt` | `ShopManagement/Index.Mobile.cshtml` | never rendered `DeniedAt` |
| `Admin/EmailStats.cshtml:63` | `AsOf` | — | no mobile twin file exists at all |

`Quest/_QuestCard.cshtml` is the one file in this plan with no parity question: it is a single shared partial rendered by both layouts via `Quest/_QuestSection.cshtml:26`.
</carve_out_contract>

<artifacts_this_phase_produces>
See `86-01-PLAN.md` § `<artifacts_this_phase_produces>` for the complete phase-wide list. This plan produces **no new symbols** — it only replaces `.ToString(...)` expressions with calls to `Html.LocalTime`, which 86-01 created.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Quest Manage — four signup times and the creation date</name>
  <files>QuestBoard.Service/Views/Quest/Manage.cshtml</files>
  <read_first>
    - `QuestBoard.Service/Views/Quest/Manage.cshtml` lines 278-288, 320-330, 357-366 and 510-520 — the four `SignupTime` render sites: three inside `(Signed up: …)` parentheses for player, assistant and spectator, and one bare inside a `<td><small class="text-muted">` in the participant table.
    - `QuestBoard.Service/Views/Quest/Manage.cshtml` lines 748-758 — `<p><strong>Created:</strong> @Model.CreatedAt.ToString("MMM dd, yyyy")</p>` at line 754.
    - `QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml` — confirm by grep that every `SignupTime` occurrence there is an `OrderBy` argument and that `CreatedAt` does not appear at all. Do not edit this file.
    - `QuestBoard.Service/Views/Quest/Manage.cshtml` lines 95-105 and 175-185 and 785-795 — the `FinalizedDate` and `ProposedDate` renders that must stay untouched, so the executor can tell them apart from the sites being changed.
    - `86-UI-SPEC.md` §3 rows 5 and 13-16 — `Model.CreatedAt` takes `date`; the four `SignupTime` sites take `date-time-compact`.
    - `86-UI-SPEC.md` §5 carve-out table — why `Manage.Mobile.cshtml` gains nothing here.
  </read_first>
  <action>
Convert exactly five expressions in `Quest/Manage.cshtml` and nothing else in the file.

At lines 283, 326, 362 and 515 replace `.ToString("MMM dd, h:mm tt")` on the four signup-time values with `Html.LocalTime(<the same value>, "date-time-compact")`. The four values are `player.SignupTime`, `assistant.SignupTime`, `spectator.SignupTime` and `participant.SignupTime`. Preserve each site's surrounding markup exactly: the `(Signed up: …)` parentheses and their enclosing elements on the first three, and the `<td><small class="text-muted">` wrapper on the fourth.

At line 754 replace `@Model.CreatedAt.ToString("MMM dd, yyyy")` with `@Html.LocalTime(Model.CreatedAt, "date")`, keeping the `<p><strong>Created:</strong> ` prefix.

Leave every `SignupTime` occurrence that appears as an `OrderBy` argument exactly as it is — ordering is zone-invariant and those expressions are not render sites. There are many of them in this file; only the four sites that call `.ToString(...)` on a signup time change.

Do not touch `Quest/Manage.Mobile.cshtml` in this task or in any other. Its `SignupTime` occurrences are all `OrderBy` keys and it has never rendered `CreatedAt`; adding either would be new content, not parity.

Leave every `FinalizedDate` and `ProposedDate` render on this page untouched — those are the naive wall-clock game nights the Dungeon Master typed, and one of them is the value the calendar feed emits.

Add no zone label, no tooltip widget, no highlight and no new CSS class.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet build && dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestManage|FullyQualifiedName~QuestFinalize"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 and `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestManage|FullyQualifiedName~QuestFinalize"` exits 0.
    - `grep -c 'Html.LocalTime' QuestBoard.Service/Views/Quest/Manage.cshtml` is 5.
    - `grep -c '"date-time-compact"' QuestBoard.Service/Views/Quest/Manage.cshtml` is 4.
    - `grep -c 'Html.LocalTime(Model.CreatedAt, "date")' QuestBoard.Service/Views/Quest/Manage.cshtml` is 1.
    - `grep -vE "^\s*(//|@\*|\*)" QuestBoard.Service/Views/Quest/Manage.cshtml | grep -c 'SignupTime.ToString'` is 0.
    - `grep -c 'OrderBy' QuestBoard.Service/Views/Quest/Manage.cshtml` is unchanged from before the task — no ordering expression was altered.
    - `git status --porcelain QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml` returns nothing.
    - `grep -cE 'Html\.LocalTime\((finalizedDate|Model\.FinalizedDate|date\.Date)' QuestBoard.Service/Views/Quest/Manage.cshtml` is 0 — no wall-clock value reaches the helper.
    - `grep -vE "^\s*(@\*|\*|//)" QuestBoard.Service/Views/Quest/Manage.cshtml | grep -ciE "(UTC|CET|CEST|Amsterdam|timezone)"` is 0 — comment lines stripped first, because the rule is that no zone label reaches *visible text*.
  </acceptance_criteria>
  <reversibility rating="reversible">View-only edits.</reversibility>
  <done>A Dungeon Master managing a quest sees every signup time in their own browser's zone, the quest's creation date likewise, and every proposed and finalized game night exactly where it was.</done>
</task>

<task type="auto">
  <name>Task 2: Quest Details and the shared quest card</name>
  <files>QuestBoard.Service/Views/Quest/Details.cshtml, QuestBoard.Service/Views/Quest/_QuestCard.cshtml</files>
  <read_first>
    - `QuestBoard.Service/Views/Quest/Details.cshtml` lines 840-848 — `<p><strong>Created:</strong> @Model.Quest?.CreatedAt.ToString("MMM dd, yyyy")</p>` at line 844. Note the null-conditional on `Model.Quest`: when the quest is null the whole expression is null and renders as empty text.
    - `QuestBoard.Service/Views/Quest/Details.cshtml` lines 880-932 — the three `<small class="text-muted">@signup.SignupTime.ToString("MMM dd")</small>` sites at 886, 907 and 928, in the Players, AssistantDM and Spectator groups.
    - `QuestBoard.Service/Views/Quest/_QuestCard.cshtml` lines 83-91 — `<strong class="text-warning">Created:</strong> @Model.CreatedAt.ToString("MMM dd, yyyy")` at line 87, and `Model.FinalizedDate` at line 48 which must stay untouched.
    - `QuestBoard.Service/Views/Quest/_QuestSection.cshtml` line 26 — `@await Html.PartialAsync("_QuestCard", quest)`, confirming the partial is shared by both layouts.
    - `QuestBoard.Service/Views/Quest/Details.Mobile.cshtml` — confirm by grep that every `SignupTime` occurrence is an `OrderBy` argument and that `CreatedAt` does not appear. Do not edit this file.
    - `86-UI-SPEC.md` §3 rows 6, 7 and 17-19 — `_QuestCard` `CreatedAt` and `Details` `CreatedAt` take `date`; the three `SignupTime` sites take `date-compact`.
    - `86-UI-SPEC.md` `### Implementation seam notes` § null contract — `Html.LocalTime` takes a non-nullable `DateTime`; every existing call-site null guard is preserved, with no null overload and no placeholder copy added.
  </read_first>
  <action>
`Quest/Details.cshtml` line 844: `Html.LocalTime` takes a non-nullable `DateTime`, so the `Model.Quest?.` null-conditional cannot be carried through the call. Replace the expression with an explicit Razor conditional that preserves today's exact behaviour: when `Model.Quest` is not null, render `Html.LocalTime(Model.Quest.CreatedAt, "date")`; when it is null, render nothing at all. Do not introduce a placeholder, an em dash, or any empty-state copy — today's null path renders empty text and must continue to. Keep the `<p><strong>Created:</strong> ` prefix in place in both branches or outside the conditional, whichever preserves the current output exactly.

`Quest/Details.cshtml` lines 886, 907 and 928: replace `@signup.SignupTime.ToString("MMM dd")` with `@Html.LocalTime(signup.SignupTime, "date-compact")` at each, keeping the `<small class="text-muted">` wrapper. Leave every `SignupTime` occurrence that appears as an `OrderBy` argument exactly as it is.

`Quest/_QuestCard.cshtml` line 87: replace `@Model.CreatedAt.ToString("MMM dd, yyyy")` with `@Html.LocalTime(Model.CreatedAt, "date")`, keeping the `<strong class="text-warning">Created:</strong> ` prefix. This partial is rendered by both layouts through `_QuestSection.cshtml`, so one edit covers both — there is no twin file to match. Leave `Model.FinalizedDate` at line 48 untouched.

Do not touch `Quest/Details.Mobile.cshtml`. Add no zone label, no tooltip widget, no highlight and no new CSS class.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet build && dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestDetails|FullyQualifiedName~QuestController"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 and `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestDetails|FullyQualifiedName~QuestController"` exits 0.
    - `grep -c 'Html.LocalTime' QuestBoard.Service/Views/Quest/Details.cshtml` is 4 and `grep -c '"date-compact"' QuestBoard.Service/Views/Quest/Details.cshtml` is 3.
    - `grep -vE "^\s*(//|@\*|\*)" QuestBoard.Service/Views/Quest/Details.cshtml | grep -c 'SignupTime.ToString'` is 0.
    - `grep -vE "^\s*(//|@\*|\*)" QuestBoard.Service/Views/Quest/Details.cshtml | grep -c 'CreatedAt.ToString'` is 0.
    - `grep -c 'Html.LocalTime(Model.CreatedAt, "date")' QuestBoard.Service/Views/Quest/_QuestCard.cshtml` is 1 and `grep -c 'Html.LocalTime' QuestBoard.Service/Views/Quest/_QuestCard.cshtml` is 1.
    - `grep -c 'Model.FinalizedDate' QuestBoard.Service/Views/Quest/_QuestCard.cshtml` is at least 1 and `grep -c 'Html.LocalTime(Model.FinalizedDate' QuestBoard.Service/Views/Quest/_QuestCard.cshtml` is 0.
    - `git status --porcelain QuestBoard.Service/Views/Quest/Details.Mobile.cshtml` returns nothing.
    - `cat QuestBoard.Service/Views/Quest/Details.cshtml QuestBoard.Service/Views/Quest/_QuestCard.cshtml | grep -vE "^\s*(@\*|\*|//)" | grep -ciE "(UTC|CET|CEST|Amsterdam|timezone)"` is 0.
  </acceptance_criteria>
  <reversibility rating="reversible">View-only edits.</reversibility>
  <done>The quest detail page and the quest card on the board show signup times and creation dates in the reader's own zone, the null-quest path still renders nothing, and every game-night value is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Shop, shop management and the admin email-stats timestamp</name>
  <files>QuestBoard.Service/Views/Shop/Index.cshtml, QuestBoard.Service/Views/ShopManagement/Index.cshtml, QuestBoard.Service/Views/Admin/EmailStats.cshtml</files>
  <read_first>
    - `QuestBoard.Service/Views/Shop/Index.cshtml` lines 72-80 — `var timeSincePurchase = DateTime.UtcNow - purchase.TransactionDate;` at line 76 and `var canReturn = timeSincePurchase.TotalHours <= 24;` at line 77. This arithmetic is correct today precisely because both sides are UTC.
    - `QuestBoard.Service/Views/Shop/Index.cshtml` lines 101-109 and 147-155 — the two `@purchase.TransactionDate.ToString("MMM dd")` render sites, each inside `<small class="text-muted d-block">` after a `<i class="fas fa-clock me-1"></i>` icon, in two different tabs of the same view.
    - `QuestBoard.Service/Views/ShopManagement/Index.cshtml` lines 144-152 — `<span class="text-muted">Denied on @item.DeniedAt.Value.ToString("MMM dd, yyyy")</span>` at line 148, guarded by `@if (item.DeniedAt.HasValue)` immediately above it.
    - `QuestBoard.Service/Views/Admin/EmailStats.cshtml` lines 59-67 — `Stats as of @Model.AsOf?.ToString("g") — cached for 5 minutes` at line 63. `AsOf` is a `DateTime?` on `EmailStatsViewModel`, so the null-conditional renders empty text when it is absent.
    - `86-RESEARCH.md` Pitfall 2 — why the shop's subtraction must keep the raw UTC value and must never be handed a converted one.
    - `86-UI-SPEC.md` §3 rows 8-10 and 12 — `TransactionDate` takes `date-compact`; `DeniedAt` takes `date`; `AsOf` takes `date-time`.
    - `86-UI-SPEC.md` `### Implementation seam notes` § null contract.
  </read_first>
  <action>
`Shop/Index.cshtml`: at lines 105 and 151 replace `@purchase.TransactionDate.ToString("MMM dd")` with `@Html.LocalTime(purchase.TransactionDate, "date-compact")`, keeping each site's `<small class="text-muted d-block">` wrapper and clock icon. Leave line 76's subtraction and line 77's 24-hour comparison exactly as they are: the span between now and the purchase instant is a duration, not a display value, and both operands must stay in UTC. Do not introduce a converted local variable anywhere in this file, and do not assign a converted value back over `purchase.TransactionDate` — the conversion happens only inside the helper, at the render position.

`ShopManagement/Index.cshtml` line 148: replace `@item.DeniedAt.Value.ToString("MMM dd, yyyy")` with `@Html.LocalTime(item.DeniedAt.Value, "date")`, keeping the `Denied on ` prefix and the `<span class="text-muted">` wrapper. Leave the enclosing `@if (item.DeniedAt.HasValue)` guard and its `<br>` exactly as they are — the guard is what makes `.Value` safe and it is preserved verbatim.

`Admin/EmailStats.cshtml` line 63: `Html.LocalTime` takes a non-nullable `DateTime`, so the `?.` cannot be carried through. Replace the expression with an explicit conditional that preserves today's behaviour exactly: when `Model.AsOf` has a value, render `Html.LocalTime(Model.AsOf.Value, "date-time")`; when it does not, render nothing. Keep the surrounding `Stats as of ` prefix, the ` — cached for 5 minutes` suffix, the `<i class="fas fa-clock me-1"></i>` icon and the `<p class="small text-muted mt-2 mb-0">` wrapper. Add no placeholder, no em dash, and no empty-state copy.

None of these three views has a mobile twin that renders the value being changed, and `EmailStats` has no mobile twin file at all. Do not create or edit any `.Mobile.cshtml` file in this task.

Add no zone label, no tooltip widget, no highlight and no new CSS class.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet build && dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Shop|FullyQualifiedName~Admin"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 and `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Shop|FullyQualifiedName~Admin"` exits 0.
    - `grep -c 'Html.LocalTime(purchase.TransactionDate, "date-compact")' QuestBoard.Service/Views/Shop/Index.cshtml` is 2.
    - `grep -c 'DateTime.UtcNow - purchase.TransactionDate' QuestBoard.Service/Views/Shop/Index.cshtml` is 1 — the UTC subtraction is byte-identical.
    - `grep -c 'timeSincePurchase.TotalHours <= 24' QuestBoard.Service/Views/Shop/Index.cshtml` is 1.
    - `grep -vE "^\s*(//|@\*|\*)" QuestBoard.Service/Views/Shop/Index.cshtml | grep -c 'TransactionDate.ToString'` is 0.
    - `grep -c 'Html.LocalTime(item.DeniedAt.Value, "date")' QuestBoard.Service/Views/ShopManagement/Index.cshtml` is 1 and `grep -c 'item.DeniedAt.HasValue' QuestBoard.Service/Views/ShopManagement/Index.cshtml` is 1 — the guard survived.
    - `grep -vE "^\s*(//|@\*|\*)" QuestBoard.Service/Views/Admin/EmailStats.cshtml | grep -c 'AsOf?.ToString'` is 0, and `grep -c 'Html.LocalTime(Model.AsOf.Value, "date-time")' QuestBoard.Service/Views/Admin/EmailStats.cshtml` is 1.
    - `grep -c 'cached for 5 minutes' QuestBoard.Service/Views/Admin/EmailStats.cshtml` is 1 — the suffix copy is unchanged.
    - `git status --porcelain QuestBoard.Service/Views/Shop/Index.Mobile.cshtml QuestBoard.Service/Views/ShopManagement/Index.Mobile.cshtml` returns nothing.
    - `cat QuestBoard.Service/Views/Shop/Index.cshtml QuestBoard.Service/Views/ShopManagement/Index.cshtml QuestBoard.Service/Views/Admin/EmailStats.cshtml | grep -vE "^\s*(@\*|\*|//)" | grep -ciE "(CET|CEST|Amsterdam|timezone)"` is 0. `UTC` is deliberately excluded from this pattern because `Shop/Index.cshtml` legitimately keeps its `DateTime.UtcNow` subtraction.
  </acceptance_criteria>
  <reversibility rating="reversible">View-only edits.</reversibility>
  <done>Purchase, denial and stats timestamps render in the reader's own zone; the 24-hour return window still computes on two UTC operands; no mobile view gained a field it never had.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered HTML → browser DOM | converted timestamps become `<time>` elements on pages that also render user-authored quest and shop-item text |
| rendered value → business rule | `Shop/Index.cshtml`'s 24-hour return window is computed in the view from the same property a render site now converts |
| admin-only surface → `/health`-adjacent operational data | `Admin/EmailStats.cshtml` renders a server-generated `AsOf` instant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-01 | Tampering | `<time>` attribute construction on these six views | low | mitigate | Views never build the element; every site calls `Html.LocalTime`, which builds it with `TagBuilder` and encodes inner text. Pinned by the criterion that no view file gains raw `<time` markup. |
| T-86-12 | Tampering | the shop's 24-hour return window comparison | medium | mitigate | Handing the comparison a board-zone-converted value would silently widen or narrow the return window by the board's offset, letting a purchase be returned outside (or blocked inside) its intended hours. Mitigated by leaving the subtraction byte-identical and never binding a converted value to a local — pinned by two exact-match grep criteria on lines 76 and 77. |
| T-86-13 | Information Disclosure | `title` attribute on admin and shop-management surfaces | low | accept | The `title` discloses only the instant already visible in the element's text, at a different precision, on pages behind the `AdminOnly` and `DungeonMasterOnly` policies. The locked decision names `title` as the disclosure mechanism. |
| T-86-14 | Tampering | quest `FinalizedDate` and `ProposedDate` renders on `Quest/Manage.cshtml` and `_QuestCard.cshtml` | high | mitigate | These are the wall-clock game nights; converting one would move a session on the board and, for `FinalizedDate`, in every calendar subscriber's client. Mitigated by leaving them untouched and by the negative-grep criteria asserting no wall-clock value reaches the helper, backed by the floating-time guard test in 86-06. |
| T-86-SC | Tampering | package-manager installs | high | mitigate | No install task exists in this plan; the phase installs zero packages. If an install becomes necessary, stop and escalate. |
</threat_model>

<verification>
- `dotnet build` exits 0 and `dotnet test` exits 0.
- `git status --porcelain` shows no change to any `.Mobile.cshtml` file — every mobile twin of this plan's sites is on the design contract's carve-out list.
- `git status --porcelain QuestBoard.Domain/Services/CalendarFeedWriter.cs QuestBoard.Domain/Services/CalendarSubscriptionService.cs` returns nothing.
- No file in this plan's diff contains a raw `<time` literal — the element only ever comes from `Html.LocalTime`.
</verification>

<success_criteria>
- Fourteen real-instant render sites across six views are converted to the design contract's assigned canonical styles.
- The shop's UTC subtraction and 24-hour comparison are byte-identical.
- Both null-guarded sites (`Model.Quest?.CreatedAt`, `Model.AsOf?`) preserve their current empty-output behaviour through explicit conditionals, with no placeholder copy introduced.
- Zero `.Mobile.cshtml` files are modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-05-SUMMARY.md` when done.
</output>
