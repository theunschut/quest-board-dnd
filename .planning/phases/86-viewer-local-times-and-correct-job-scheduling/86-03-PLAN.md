---
phase: 86-viewer-local-times-and-correct-job-scheduling
plan: 03
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - QuestBoard.Domain/Services/EventSeriesService.cs
  - QuestBoard.Repository/GroupRepository.cs
  - QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs
  - QuestBoard.Service/Controllers/Events/EventsController.cs
  - QuestBoard.Service/Controllers/Events/SeriesController.cs
  - QuestBoard.Service/ViewModels/SeriesViewModels/SeriesDetailsViewModel.cs
  - QuestBoard.Service/Views/Series/Details.cshtml
  - QuestBoard.UnitTests/Services/EventSeriesServiceTests.cs
  - QuestBoard.UnitTests/Repository/GroupRepositoryTests.cs
autonomous: true
# No REQ-IDs exist for Phase 86. Traceability anchors are CONTEXT.md decision IDs.
requirements: [D-07]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Every ambient clock read compared against a board-local date in EventSeriesService, GroupRepository, CalendarController, EventsController, SeriesController and Series/Details.cshtml goes through IBoardClock, so between midnight and 02:00 board time these no longer operate on yesterday (D-07).
    - EventSeriesService produces the same series, runway and top-up results under a fixed board clock as it did under the host clock for the same date, proven by a test file that did not exist before this phase.
    - The campaign auto-signup sweep in GroupRepository selects future events against the board-local today rather than the container's UTC today.
    - Series/Details.cshtml reads its "Today" divider position and its end-series confirmation label from the view model rather than calling the ambient clock directly in Razor, so the view and SeriesController agree on which day is today.
    - EmailPreviewController's five DateTime.Today uses are left untouched — they generate cosmetic sample data for the admin preview page and need no clock.
  artifacts:
    - QuestBoard.UnitTests/Services/EventSeriesServiceTests.cs
  key_links:
    - IBoardClock is constructor-injected into each consumer exactly as CalendarSubscriptionService takes TimeProvider — no new DI mechanism, just a new parameter replacing a static call.
    - SeriesController.Details fills SeriesDetailsViewModel.Today from boardClock.Today; Series/Details.cshtml consumes Model.Today and Model.TodayLabel instead of DateTime.Today.
    - Adding a constructor parameter to GroupRepository breaks ten construction sites in GroupRepositoryTests; they are updated in the same task so the build never sits red across a wave boundary.
---

<objective>
Move every ambient clock read that is compared against a board-local date onto the one board clock, including the seventh site CONTEXT.md's file list missed.

Purpose: these reads currently take UTC and compare it against `DateOnly` values that are board-local wall-clock, so between midnight and 02:00 board time `GetActiveSeriesAsync(today)`, `GetSeriesBelowRunwayAsync(today)` and the campaign auto-signup sweep all operate on yesterday.

Output: seven `EventSeriesService` call sites, four one-line controller/repository sites, the `Series/Details.cshtml` view-level read routed through the view model, and the regression net (`EventSeriesServiceTests`) that did not exist before.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-CONTEXT.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-PATTERNS.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-01-SUMMARY.md
@CLAUDE.md
</context>

<recorded_correction>
**`Series/Details.cshtml:12,17` is a seventh D-07 site, recorded rather than silently added.** CONTEXT.md D-07 names six files (`EventSeriesService`, `CalendarController`, `EventsController`, `SeriesController`, `GroupRepository`, `DailyReminderJob`). `86-RESEARCH.md` Pitfall 4 and `86-UI-SPEC.md` §5 both found two further ambient reads inside the Razor view itself — `var todayLabel = DateTime.Today.ToString("MMMM d, yyyy");` and `var today = DateOnly.FromDateTime(DateTime.Today);`. D-07's file list was written from the controller and service layer, so a view calling the clock directly is a call site the same audit would not catch. It is treated as in scope here: once `SeriesController` is corrected but the view is not, the "Today" divider sits one row away from where the controller's own corrected `today` would place it. `DailyReminderJob` — the sixth named file — is handled in 86-02 alongside the cron registration it must not drift from.
</recorded_correction>

<artifacts_this_phase_produces>
See `86-01-PLAN.md` § `<artifacts_this_phase_produces>` for the complete phase-wide list. Produced by **this** plan:

- `QuestBoard.Service/ViewModels/SeriesViewModels/SeriesDetailsViewModel.cs` — new members `Today` (a `DateOnly`) and `TodayLabel` (a computed `string`)
- `QuestBoard.UnitTests/Services/EventSeriesServiceTests.cs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move EventSeriesService's seven clock reads onto the board clock, with the regression net it never had</name>
  <files>QuestBoard.Domain/Services/EventSeriesService.cs, QuestBoard.UnitTests/Services/EventSeriesServiceTests.cs</files>
  <read_first>
    - `QuestBoard.Domain/Services/EventSeriesService.cs` — the primary constructor `internal class EventSeriesService(IEventSeriesRepository repository, IEventRepository eventRepository, IUserRepository userRepository, IBoardTypeResolver boardTypeResolver, IActiveGroupContext activeGroupContext, IOptions<EventSeriesOptions> options) : IEventSeriesService`, and all seven `var today = DateOnly.FromDateTime(DateTime.Today);` lines at 21, 58, 90, 172, 185, 192 and 229.
    - `QuestBoard.Domain/Interfaces/IEventSeriesService.cs` — the public surface: `PreviewAsync`, `CreateWithFirstPassAsync`, `TopUpAsync`, `GetActiveSeriesForActiveGroupAsync`, `GetSeriesBelowRunwayAsync`, `GetRemovalImpactAsync`, `ApplyTemplateToFutureAsync`.
    - `QuestBoard.UnitTests/Services/EventsOverviewAggregationTests.cs` — the structure to model the new test file on: fixed-clock fields, `NSubstitute`-backed repository mocks, `FluentAssertions` assertions, hand-rolled fixtures rather than new packages.
    - `QuestBoard.UnitTests/Helpers/FakeBoardClock.cs` — the shared fake from 86-01.
    - `QuestBoard.UnitTests/Services/EventSeriesDateGeneratorTests.cs` — existing coverage of the pure date generator, so the new file covers the service's clock-dependent branching rather than re-testing slot generation.
    - `86-PATTERNS.md` § "`EventSeriesServiceTests` — NO ANALOG, confirmed absent" — this is new-file creation, not test-file extension.
  </read_first>
  <behavior>
    - With a fake board clock fixed at 2026-09-21 and a series anchored earlier, `PreviewAsync` reports `AnchorFullyInPast` on exactly the same boundary it reported under a host clock set to the same date.
    - `GetActiveSeriesForActiveGroupAsync` includes a series whose end date is 2026-09-21 and excludes one whose end date is 2026-09-20, when the board clock says today is 2026-09-21.
    - `GetSeriesBelowRunwayAsync` counts occurrences from the board clock's today forward, not from the host's.
    - `TopUpAsync` generates from the board clock's today, so a run at 00:30 board time (22:30 the previous day in UTC) tops up from today rather than from yesterday.
    - `ApplyTemplateToFutureAsync` treats "future" as strictly after the board clock's today.
    - The service reads the clock only through `IBoardClock`: no `DateTime.Today`, `DateTime.Now` or `DateTime.UtcNow` remains in the file outside comments.
  </behavior>
  <action>
Add `IBoardClock boardClock` to `EventSeriesService`'s primary constructor parameter list, following the same constructor-injection style every other dependency there already uses. Replace all seven occurrences of `var today = DateOnly.FromDateTime(DateTime.Today);` — at lines 21, 58, 90, 172, 185, 192 and 229 — with `var today = boardClock.Today;`. The expression is character-identical at all seven sites, so this is a mechanical substitution; do not change any surrounding comparison, filter or ordering.

Create `QuestBoard.UnitTests/Services/EventSeriesServiceTests.cs`, modelled on `EventsOverviewAggregationTests`'s structure. `EventSeriesService` is `internal` and `QuestBoard.Domain` already grants `InternalsVisibleTo("QuestBoard.UnitTests")`, so it can be constructed directly. Use `NSubstitute` for `IEventSeriesRepository`, `IEventRepository`, `IUserRepository`, `IBoardTypeResolver` and `IActiveGroupContext`, a real `IOptions<EventSeriesOptions>` via `Options.Create(new EventSeriesOptions())`, and `FakeBoardClock` with `Today` fixed at `new DateOnly(2026, 9, 21)`.

Cover the six behaviours listed above, one test method each at minimum. Each test must set the fake clock to a date that is deliberately different from the host's `DateTime.Today`, so a regression that reintroduces the ambient read fails the test rather than passing by coincidence. Name the class so its fully-qualified name contains `EventSeriesService` to match the validation filter.

Keep comments in plain language. Do not write a phase number, a decision id, or a plan number into any source or test file.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~EventSeriesService</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~EventSeriesService` exits 0 and reports at least 6 tests run.
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Domain/Services/EventSeriesService.cs | grep -cE 'DateTime\.(Today|Now|UtcNow)'` is 0.
    - `grep -c 'boardClock.Today' QuestBoard.Domain/Services/EventSeriesService.cs` is 7.
    - `grep -c 'IBoardClock boardClock' QuestBoard.Domain/Services/EventSeriesService.cs` is 1.
    - `QuestBoard.UnitTests/Services/EventSeriesServiceTests.cs` contains `new DateOnly(2026, 9, 21)` and `FakeBoardClock`.
    - `dotnet test QuestBoard.UnitTests` exits 0 — no existing test regressed.
  </acceptance_criteria>
  <reversibility rating="costly">One of six files whose constructor changes to take the clock by injection; unwinding means reverting the injection across all of them together. Recorded as costly in CONTEXT.md D-07, not one-way — no stored data changes.</reversibility>
  <done>All seven clock reads resolve through `IBoardClock`, and six clock-dependent branches are pinned by a test file that did not exist at the start of this phase.</done>
</task>

<task type="auto">
  <name>Task 2: Move the repository sweep and the two controller defaults onto the board clock</name>
  <files>QuestBoard.Repository/GroupRepository.cs, QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs, QuestBoard.Service/Controllers/Events/EventsController.cs, QuestBoard.UnitTests/Repository/GroupRepositoryTests.cs</files>
  <read_first>
    - `QuestBoard.Repository/GroupRepository.cs` lines 1-20 and 60-95 — the primary constructor `internal class GroupRepository(QuestBoardContext dbContext, IMapper mapper) : BaseRepository<Group, GroupEntity>(dbContext, mapper), IGroupRepository`, and the campaign auto-signup sweep's `var today = DateOnly.FromDateTime(DateTime.Today);` at line 74 feeding `GetFutureEventIdsForGroupIgnoringActiveBoardAsync`.
    - `QuestBoard.UnitTests/Repository/GroupRepositoryTests.cs` — ten `new GroupRepository(context, CreateMapper())` construction sites at lines 101, 134, 166, 198, 205, 244, 286, 326, 363 and 397. Every one needs the new argument.
    - `QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs` lines 1-35 — the primary constructor parameter list and `var currentDate = DateTime.Now;` at line 24, which feeds the default year/month of the calendar grid.
    - `QuestBoard.Service/Controllers/Events/EventsController.cs` lines 1-30 and 90-100 — the primary constructor parameter list and `new EventViewModel { Date = DateOnly.FromDateTime(DateTime.Today) }` at line 95, the Create form's default date.
    - `QuestBoard.UnitTests/Helpers/FakeBoardClock.cs`.
    - `CLAUDE.md` § Entity Framework — EF packages stay in `QuestBoard.Repository`; `IBoardClock` lives in `QuestBoard.Domain`, which `QuestBoard.Repository` already references, so no package reference changes.
  </read_first>
  <action>
`GroupRepository`: add `IBoardClock boardClock` as a third primary-constructor parameter, keeping the existing `: BaseRepository<Group, GroupEntity>(dbContext, mapper)` base call unchanged (the base takes only the two arguments it takes today). Replace `var today = DateOnly.FromDateTime(DateTime.Today);` at line 74 with `var today = boardClock.Today;`. This is the campaign auto-signup sweep, which decides which events count as "today or later" — on the container's UTC clock it silently skips a board-local today for the first two hours after midnight.

`QuestBoard.UnitTests/Repository/GroupRepositoryTests.cs`: update all ten `new GroupRepository(context, CreateMapper())` calls to pass a `FakeBoardClock`. Introduce a single private helper in the test class (for example `CreateBoardClock()` returning a `FakeBoardClock` with `Today` fixed at `new DateOnly(2026, 9, 21)`) rather than repeating the construction ten times. Add one new test asserting the sweep uses the board clock's today: with the fake clock set to a date that is not the host's `DateTime.Today`, an event dated on that board-local date is included in the auto-signup set and one dated the previous day is not.

`CalendarController`: add `IBoardClock boardClock` to the primary constructor parameter list and replace `var currentDate = DateTime.Now;` with `var currentDate = boardClock.Now;`. Leave the downstream `year ?? currentDate.Year` / `month ?? currentDate.Month` expressions and both validation guards untouched.

`EventsController`: add `IBoardClock boardClock` to the primary constructor parameter list and replace `DateOnly.FromDateTime(DateTime.Today)` in the `Create()` GET action with `boardClock.Today`.

Where a replaced line carried a comment, keep the comment's meaning and reword only if it asserted something about the server or container clock. Write no phase number, decision id, or plan number into any file.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet build && dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~GroupRepository</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~GroupRepository` exits 0 with at least one more test than before this task.
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Repository/GroupRepository.cs | grep -cE 'DateTime\.(Today|Now|UtcNow)'` is 0.
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs | grep -cE 'DateTime\.(Today|Now|UtcNow)'` is 0.
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Service/Controllers/Events/EventsController.cs | grep -cE 'DateTime\.(Today|Now|UtcNow)'` is 0.
    - `grep -c 'boardClock.Now' QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs` is 1.
    - `grep -c 'new GroupRepository(context, CreateMapper())' QuestBoard.UnitTests/Repository/GroupRepositoryTests.cs` is 0 — every construction site was updated.
    - `dotnet test` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">Same injection wave as Task 1 — four more constructors take the clock. No stored data changes.</reversibility>
  <done>The campaign auto-signup sweep, the calendar month default and the event Create form default all read the board clock, and the repository's ten test construction sites compile against the new signature.</done>
</task>

<task type="auto">
  <name>Task 3: Route Series' "today" through the view model instead of the Razor view</name>
  <files>QuestBoard.Service/Controllers/Events/SeriesController.cs, QuestBoard.Service/ViewModels/SeriesViewModels/SeriesDetailsViewModel.cs, QuestBoard.Service/Views/Series/Details.cshtml</files>
  <read_first>
    - `QuestBoard.Service/Controllers/Events/SeriesController.cs` lines 1-50 and 55-80 — the primary constructor parameter list, the `Details` action that builds `SeriesDetailsViewModel` and fills `CyclePositions`/`Occurrences`/`CanManage`/the removal-impact counts, and `DateOnly.FromDateTime(DateTime.Today)` inside `EndAsync` at line 68.
    - `QuestBoard.Service/ViewModels/SeriesViewModels/SeriesDetailsViewModel.cs` (whole file) — the existing convention of computed display properties (`CadenceLabel`, `TimeLabel`, `TotalCount`) with a comment explaining why each lives on the view model rather than in the view.
    - `QuestBoard.Service/Views/Series/Details.cshtml` lines 1-25 — `var todayLabel = DateTime.Today.ToString("MMMM d, yyyy");` at line 12, the two confirmation-message strings that interpolate it, `var today = DateOnly.FromDateTime(DateTime.Today);` at line 17, and `var todayDividerRendered = false;` at line 18.
    - `QuestBoard.Service/Views/Series/Details.cshtml` — every later use of the local `today` variable (the "Today" divider placement in the occurrence list) so the substitution reaches all of them.
    - `86-UI-SPEC.md` §5, final paragraph — this is a view-level ripple, not a `<time>` render site: the board-local date gets no `<time>` wrapper and no zone label, because it is a wall-clock date, not a real instant.
  </read_first>
  <action>
`SeriesDetailsViewModel`: add `public DateOnly Today { get; set; }` and a computed `public string TodayLabel => Today.ToString("MMMM d, yyyy");`, each with a short comment in the file's existing style explaining that the board-local today is filled by the controller so the view and the controller cannot disagree about which day it is. Follow the file's existing computed-property convention (`CadenceLabel`, `TimeLabel`).

`SeriesController`: add `IBoardClock boardClock` to the primary constructor parameter list. In the `Details` action, after `var viewModel = mapper.Map<SeriesDetailsViewModel>(series);`, set `viewModel.Today = boardClock.Today;` alongside the other controller-filled properties. In `EndAsync`, replace `DateOnly.FromDateTime(DateTime.Today)` with `boardClock.Today`, leaving the `removeFutureOccurrences: true` argument and the surrounding comment's meaning intact.

`Views/Series/Details.cshtml`: delete the two ambient reads in the `@{ }` block. Replace `var todayLabel = DateTime.Today.ToString("MMMM d, yyyy");` with `var todayLabel = Model.TodayLabel;` (or interpolate `Model.TodayLabel` directly into the two confirmation-message strings and drop the local). Replace `var today = DateOnly.FromDateTime(DateTime.Today);` with `var today = Model.Today;`. Leave `var todayDividerRendered = false;` and every downstream use of `today` and `todayLabel` exactly as they are — this is a source change, not a behaviour change: the divider still sits on the same date, computed on a clock that is now correct through the hours either side of midnight.

Do not wrap the label in a `<time>` element and do not add a zone marker: this is a board-local wall-clock date, which the locked classification puts on the never-converted side.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet build && dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~Series</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~Series` exits 0.
    - `grep -vE "^\s*(//|@\*|\*)" QuestBoard.Service/Views/Series/Details.cshtml | grep -cE 'DateTime\.(Today|Now|UtcNow)'` is 0.
    - `grep -c 'DateTime.Today' QuestBoard.Service/Views/Series/Details.cshtml` is 0 — deliberately unstripped, because a Razor comment recording the removed call would be dead noise in a view. <!-- planner-discipline-allow: DateTime.Today -->
    - `grep -vE "^\s*(//|/\*|\*)" QuestBoard.Service/Controllers/Events/SeriesController.cs | grep -cE 'DateTime\.(Today|Now|UtcNow)'` is 0.
    - `QuestBoard.Service/ViewModels/SeriesViewModels/SeriesDetailsViewModel.cs` contains `public DateOnly Today { get; set; }` and `public string TodayLabel =>`.
    - `grep -c 'viewModel.Today = boardClock.Today' QuestBoard.Service/Controllers/Events/SeriesController.cs` is 1.
    - `grep -c '<time' QuestBoard.Service/Views/Series/Details.cshtml` is 0 — the board-local date stays plain text.
    - `dotnet test` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Two new view-model members and one substituted local in a view; reverting restores the ambient read with no data implication.</reversibility>
  <done>The Series detail page's "Today" divider and its end-series confirmation label both come from the controller's board-local today; the Razor view no longer calls the ambient clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → `SeriesController.EndAsync` | a destructive write whose cut-off date now comes from the board clock instead of the host clock |
| background sweep → `GroupRepository` campaign auto-signup | writes `EventSignup` rows for every member of a campaign board, scoped by a date this plan changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-07 | Tampering | `SeriesController.EndAsync` cut-off date | low | mitigate | The clock source changes but the authorization boundary does not: the action keeps `[Authorize(Policy = "DungeonMasterOnly")]`, `[ValidateAntiForgeryToken]` and the existing `SeriesIsOnActiveBoardAsync` guard. No parameter becomes caller-supplied — `boardClock.Today` is server-resolved exactly as `DateTime.Today` was. |
| T-86-08 | Elevation of Privilege | `GroupRepository` campaign auto-signup row creation | low | accept | The change is the date the sweep filters on; the tenant scoping (`groupId` from the caller's route, `GroupEntity` carrying no query filter, and the explicit comment recording why the board-type resolver is deliberately not used here) is unchanged. No new row reaches a member of a different board. Accepted because the blast radius is identical to today's. |
| T-86-09 | Information Disclosure | `SeriesDetailsViewModel.Today` / `TodayLabel` | low | accept | A board-local calendar date is not sensitive and is already visible on the page as a divider label. No new field carries user or tenant data. |
| T-86-SC | Tampering | package-manager installs | high | mitigate | No install task exists in this plan; the phase installs zero packages (`86-RESEARCH.md` § Package Legitimacy Audit records N/A). If an install becomes necessary, stop and escalate. |
</threat_model>

<verification>
- `dotnet build` exits 0 and `dotnet test` exits 0.
- For each of `QuestBoard.Domain/Services/EventSeriesService.cs`, `QuestBoard.Repository/GroupRepository.cs`, `QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs`, `QuestBoard.Service/Controllers/Events/EventsController.cs`, `QuestBoard.Service/Controllers/Events/SeriesController.cs` and `QuestBoard.Service/Views/Series/Details.cshtml`: the comment-stripped file contains no `DateTime.Today`, `DateTime.Now` or `DateTime.UtcNow`.
- `grep -cE 'DateTime\.Today' QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs` is still 5 — the cosmetic sample-data uses are deliberately untouched.
- `git status --porcelain QuestBoard.Domain/Services/CalendarFeedWriter.cs QuestBoard.Domain/Services/CalendarSubscriptionService.cs` returns nothing.
</verification>

<success_criteria>
- All seven `EventSeriesService` reads, the `GroupRepository` sweep, both controller defaults, `SeriesController.EndAsync` and the two `Series/Details.cshtml` reads resolve through `IBoardClock`.
- `EventSeriesServiceTests` exists and pins six clock-dependent branches against a fake clock set to a date the host clock does not share.
- `EmailPreviewController` is untouched and still holds its five cosmetic `DateTime.Today` uses.
- No board-local wall-clock date gained a `<time>` wrapper or a zone label.
</success_criteria>

<output>
Create `.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-03-SUMMARY.md` when done.
</output>
