---
phase: 86-viewer-local-times-and-correct-job-scheduling
plan: 06
type: execute
wave: 3
depends_on: ["86-01", "86-02", "86-03", "86-04", "86-05"]
files_modified:
  - QuestBoard.IntegrationTests/Controllers/WallClockUnmovedTests.cs
  - QuestBoard.UnitTests/Services/CalendarFeedFloatingTimeGuardTests.cs
  - QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs
  - .planning/PROJECT.md
  - .planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md
autonomous: false
# No REQ-IDs exist for Phase 86. Traceability anchors are CONTEXT.md decision IDs.
requirements: [D-01, D-03, D-04, D-05, D-06, D-07]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # Item 3 of the 11 liftable truths in 86-UI-SPEC.md "## UI Considerations".
    # The other 10 (9 explicit + 1 backstop) are carried in 86-01's must_haves. None dropped.
    - Every one of the 19 real-instant render sites maps to exactly one of the four canonical styles; no site retains a bespoke format string.
    # --- Phase-goal truths (goal-backward) ---
    - A wall-clock value renders byte-for-byte identically when the configured board zone is changed to a zone that is not the default, proving no game night moved.
    - The calendar feed's DTSTART digits for a finalized quest equal the stored FinalizedDate's own digits with no zone designator, under any configured board zone.
    - QuestBoard.Domain/Services/CalendarFeedWriter.cs and QuestBoard.Domain/Services/CalendarSubscriptionService.cs do not appear in this phase's diff at all.
    - No production code path in the six migrated files and the one migrated view reads DateTime.Today, DateTime.Now or DateTime.UtcNow ambiently; every clock read goes through the board-clock seam. EmailPreviewController's five cosmetic uses and test fixtures are the only exemptions.
    - PROJECT.md's standing tech-debt entry asserting FinalizedDate "is correct for LXC host timezone" is corrected, because the shipped container sets no TZ, mounts no /etc/localtime and installs no tzdata override.
  artifacts:
    - QuestBoard.IntegrationTests/Controllers/WallClockUnmovedTests.cs
    - QuestBoard.UnitTests/Services/CalendarFeedFloatingTimeGuardTests.cs
    - QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs
  key_links:
    - WallClockUnmovedTests drives a real rendered page through WebApplicationFactoryBase with the board zone overridden, so it exercises the same Html.LocalTime call sites the executor edited rather than the helper in isolation.
    - AmbientClockSeamTests reads the production source files from disk with the same upward-walk resolver MobileCssTests uses, so it enforces the seam even for call sites no behavioural test reaches.
    - The container tzdata assumption is unprovable from CI and is carried into the human-verification checkpoint rather than asserted silently.
---

<objective>
Close the phase with the guards the ROADMAP's own risk list demands: prove no wall-clock value moved, prove the calendar feed is untouched, prove the ambient clock cannot creep back, correct the false tech-debt entry, and put the three unprovable-in-CI behaviours in front of a human.

Purpose: three of this phase's four named risks — shifting the calendar feed, converting a wall-clock date, and fixing one clock but not the other — are only closed by a guard that outlives the phase. Intentions do not survive the next developer.

Output: three test files, one corrected tech-debt entry, a filled-in validation map, and a blocking human-verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-CONTEXT.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-01-SUMMARY.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-03-SUMMARY.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-04-SUMMARY.md
@.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-05-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
See `86-01-PLAN.md` § `<artifacts_this_phase_produces>` for the complete phase-wide list. Produced by **this** plan:

- `QuestBoard.IntegrationTests/Controllers/WallClockUnmovedTests.cs`
- `QuestBoard.UnitTests/Services/CalendarFeedFloatingTimeGuardTests.cs`
- `QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs` (new folder `QuestBoard.UnitTests/Architecture/`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Prove no game night moved and the calendar feed is untouched</name>
  <files>QuestBoard.IntegrationTests/Controllers/WallClockUnmovedTests.cs, QuestBoard.UnitTests/Services/CalendarFeedFloatingTimeGuardTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/WebApplicationFactoryBase.cs` — `builder.UseEnvironment("Testing")`, `ConfigureServices`, `ConfigureTestServices` and the `TestDatabase`/`MutableGroupContext` wiring; this is where the board zone gets overridden for a variant factory.
    - `QuestBoard.IntegrationTests/Controllers/BoardTimeZoneHealthCheckTests.cs` as left by 86-02 — the configuration-override shape to reuse for pointing `TimeZone:BoardTimeZoneId` at a non-default zone.
    - `QuestBoard.IntegrationTests/Tests/CalendarSubscriptionQuestFeedTests.cs` — the existing end-to-end `.ics` test: how a finalized quest is seeded, how the anonymous feed URL is fetched, and how the body is asserted. This is the shape the floating-time guard mirrors.
    - `QuestBoard.UnitTests/Services/CalendarFeedWriterTests.cs` lines 1-30 — the exact-byte assertion style (plain string and regex assertions, no mocking, no snapshot framework) and the `MakeEntry` helper.
    - `QuestBoard.Domain/Services/CalendarFeedWriter.cs` lines 74-100 and 145-156 — `DTSTART:` + `FormatBasicDateTime(start)` for a timed entry, `DTSTART;VALUE=DATE:` for an all-day one, and the standing comment recording that both are floating local time with no zone designator.
    - `QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs` — how a finalized quest with a known `FinalizedDate` is seeded.
    - `86-UI-SPEC.md` §1 — the rule that distinguishes a converted instant from an untouched wall-clock value in the DOM is the presence or absence of the `<time class="local-time" datetime="...">` wrapper.
  </read_first>
  <action>
Create `QuestBoard.IntegrationTests/Controllers/WallClockUnmovedTests.cs`. Build a factory variant whose configuration sets `TimeZone:BoardTimeZoneId` to a zone that is deliberately not the code default and not UTC, using the same `ConfigureAppConfiguration` in-memory-collection shape 86-02 established. Seed a finalized quest with a `FinalizedDate` whose date and time are both unambiguous (for example `2026-09-20 19:00`), plus a proposed date and an event with a known `Date`/`StartTime`.

Assert, on the real rendered HTML:
  - the quest's finalized game night appears with its exact expected digits (the day number and the hour as stored) and is NOT inside a `<time class="local-time">` element — the wrapper's presence or absence is the structural marker that separates a converted instant from an untouched wall-clock value;
  - the same assertion holds when the board zone configuration is switched to a second, different zone — the rendered wall-clock text is identical across both runs, which is the actual proof that nothing moved;
  - a real instant on the same page IS inside a `<time class="local-time">` element, so the test would fail if the conversion had simply been switched off wholesale rather than applied selectively.

Cover at least the finalized-quest render and one proposed-date or event render. Name the class so its fully-qualified name contains `WallClockUnmoved` to match the validation filter.

Create `QuestBoard.UnitTests/Services/CalendarFeedFloatingTimeGuardTests.cs` in the exact-byte style of `CalendarFeedWriterTests`. Assert that for a feed entry built from a finalized quest dated `2026-09-20` at `19:00`, the writer emits a `DTSTART:` line whose value is exactly `20260920T190000` — with no trailing `Z`, no `TZID=` parameter, and no `VTIMEZONE` block anywhere in the document. Add a second case asserting that constructing a `TimeZoneInfo` for a non-default zone and holding it in scope changes nothing about the emitted bytes, which is what makes the writer structurally immune: it takes `DateOnly`/`TimeOnly`, so there is no instant for a zone to act on.

Add a third case at the service boundary, modelled on the existing quest-feed test's seeding: a quest whose `FinalizedDate` is `2026-09-20 19:00` produces a feed entry whose `Date` is `2026-09-20` and whose `StartTime` is `19:00`, unchanged when the registered board clock carries a non-default zone. This is the assertion that would fail if anyone ever routed `FinalizedDate` through a conversion upstream of the writer.

Do not modify `CalendarFeedWriter.cs` or `CalendarSubscriptionService.cs` — if either needs a change to make these tests pass, stop and escalate, because a change there is the exact regression this task exists to detect.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CalendarFeedFloatingTimeGuard && dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~WallClockUnmoved</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~CalendarFeedFloatingTimeGuard` exits 0 and reports at least 3 tests run.
    - `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~WallClockUnmoved` exits 0 and reports at least 2 tests run.
    - `git diff --name-only main...HEAD -- QuestBoard.Domain/Services/CalendarFeedWriter.cs QuestBoard.Domain/Services/CalendarSubscriptionService.cs` returns nothing.
    - `QuestBoard.UnitTests/Services/CalendarFeedFloatingTimeGuardTests.cs` asserts on the literal `20260920T190000` and asserts the emitted document contains no `TZID` and no `VTIMEZONE`.
    - `QuestBoard.IntegrationTests/Controllers/WallClockUnmovedTests.cs` sets `TimeZone:BoardTimeZoneId` to two different non-default values and asserts the rendered wall-clock text is identical in both.
    - `dotnet test` exits 0.
  </acceptance_criteria>
  <done>Changing the configured board zone provably does not move a rendered game night, and the `.ics` feed's floating-local-time contract is pinned by a test rather than by a comment.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the promoted clock in with an invariant test</name>
  <files>QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Mobile/MobileCssTests.cs` lines 35-75 — the upward-walk path resolver (`new DirectoryInfo(AppContext.BaseDirectory)`, walking to `dir.Parent` until a `QuestBoard.Service/...` candidate resolves) and the descriptive `FileNotFoundException` that names the attempted path. Reuse this shape; do not invent a new resolver.
    - `86-03-SUMMARY.md` and `86-02-SUMMARY.md` — confirm which files were migrated and that none was missed.
    - `QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs` — the five `DateTime.Today.AddDays(N)` uses at lines 70, 88, 89, 157 and 175 that are explicitly exempt because they generate cosmetic sample data for the admin preview page.
    - `86-01-PLAN.md` § `<assumption_delta_decision>` — the promoted noun and the rationale this test encodes.
  </read_first>
  <action>
Create the folder `QuestBoard.UnitTests/Architecture/` and the file `AmbientClockSeamTests.cs`.

The test reads production source files from disk and asserts on their text, using the upward-walk resolver pattern from `MobileCssTests`. Define a closed, explicit list of exactly seven guarded paths — this scoping is what makes the test enforceable rather than aspirational:
  - `QuestBoard.Domain/Services/EventSeriesService.cs`
  - `QuestBoard.Repository/GroupRepository.cs`
  - `QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs`
  - `QuestBoard.Service/Controllers/Events/EventsController.cs`
  - `QuestBoard.Service/Controllers/Events/SeriesController.cs`
  - `QuestBoard.Service/Jobs/DailyReminderJob.cs`
  - `QuestBoard.Service/Views/Series/Details.cshtml`

For each guarded path, assert that after stripping comment lines the file contains none of `DateTime.Today`, `DateTime.Now` or `DateTime.UtcNow`. Strip C#-style line comments (`//`), C#-style block-comment continuation lines (leading `*`) and Razor comment markers (`@*`) before matching, so a plain-language comment can never fail the gate and a real call can never hide in one. Write the failure message so it names the offending file, the offending line number and the offending token, and states that every clock read in that file must go through the board-clock seam — a developer who hits this in five phases' time should learn what to do from the message alone, with no planning document to consult.

Add two positive assertions so the test cannot pass vacuously: each guarded `.cs` file mentions `IBoardClock`, and `Views/Series/Details.cshtml` mentions `Model.Today`.

Add one exemption test that documents the boundary rather than leaving it implicit: assert that `QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs` still contains five `DateTime.Today` uses, with a comment on the test explaining in plain language that those generate sample data for an admin preview page, are never compared against a stored board-local date, and are deliberately outside the seam. Test fixtures are outside the guarded list entirely and need no exemption.

Name the class so its fully-qualified name contains `AmbientClockSeam`. Write no phase number, decision id, or plan number into the file.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~AmbientClockSeam</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~AmbientClockSeam` exits 0 and reports at least 8 tests run (seven guarded paths plus the exemption).
    - `QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs` names all seven guarded paths as literal strings.
    - `QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs` contains `EmailPreviewController` and asserts a count of 5.
    - Temporarily reinserting `var x = DateTime.Today;` into `QuestBoard.Service/Controllers/Events/EventsController.cs` makes `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~AmbientClockSeam` exit non-zero; reverting makes it exit 0 again. Confirm this by running it, then revert.
    - `grep -rnE "(Phase 86|D-0[1-7]|86-0[1-9])" QuestBoard.UnitTests/Architecture/AmbientClockSeamTests.cs` returns nothing.
    - `dotnet test` exits 0.
  </acceptance_criteria>
  <done>A future phase that reintroduces an ambient clock read in any of the seven migrated paths fails a test whose message tells the developer exactly what to do instead.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the false tech-debt entry and fill in the validation map</name>
  <files>.planning/PROJECT.md, .planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md</files>
  <read_first>
    - `.planning/PROJECT.md` — the `**Known issues / tech debt:**` list, and specifically the bullet reading `` `FinalizedDate` stored as server local time (CET/CEST) — reminder job uses `DateTime.Today.AddDays(1)` which is correct for LXC host timezone but should be reviewed if deployment timezone changes ``.
    - `Dockerfile` and `docker-compose.yml` — confirm the base image tag carries no `-alpine` suffix, that no `TZ` environment variable is set, that no `/etc/localtime` is mounted, and that no tzdata package is installed. These are the facts that make the existing bullet false.
    - `.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md` — the `## Per-Task Verification Map` table with `TBD` in the Task ID / Plan / Wave columns, the `## Wave 0 Requirements` checklist, and the `## Manual-Only Verifications` table.
    - The five `-SUMMARY.md` files from plans 86-01 through 86-05 — for the actual task numbering and test file names to fill in.
  </read_first>
  <action>
`.planning/PROJECT.md`: replace the `FinalizedDate` tech-debt bullet. It asserts the value is "correct for LXC host timezone", which is false for the shipped deployment — the container sets no `TZ`, mounts no `/etc/localtime`, and installs no tzdata over its base image, so its clock is UTC. Replace it with a bullet recording the resolved state instead: `FinalizedDate` is a naive wall-clock value with no zone attached, deliberately so, because the calendar feed emits it as floating local time; the reminder job now computes "tomorrow" on a configured board clock rather than on the container's clock; and the remaining debt — that the wall-clock-versus-instant distinction lives in a written classification and in tests rather than in the type system — is the actual open item, with a pointer to the deferred-ideas entry that parks it. Keep the surrounding bullets untouched and keep the file's existing bullet style, including its milestone-attribution suffix convention.

`.planning/phases/.../86-VALIDATION.md`: fill in the `Task ID`, `Plan` and `Wave` columns of the `## Per-Task Verification Map` from the five completed summaries, and update each row's `File Exists` and `Status` columns to reflect reality. Tick every item in `## Wave 0 Requirements` that is now satisfied, and annotate the wrapped-timezone-resolver item with the reason it was not built: an unresolvable id passed straight to the real resolver exercises the identical fallback path with no extra interface.

Correct one stale row in `## Manual-Only Verifications`: the "No flash of UTC on first paint" row's test instruction says to *"confirm the pre-hydration value carries an explicit zone label rather than a bare wrong-looking time"*. That describes the ROADMAP's original framing, which the discuss pass explicitly rejected. Replace the instruction with the locked behaviour: confirm the pre-hydration value is the board-zone formatted string with no zone label at all, and that after hydration it becomes the viewer's zone. Leave the other two rows' substance as they are.

Set `nyquist_compliant: true` and `wave_0_complete: true` in the validation file's frontmatter only if every sign-off box under `## Validation Sign-Off` genuinely holds; otherwise leave them false and record which box does not hold.

These are planning documents, so decision ids and phase numbers are fine here — the prohibition applies to source files only.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && ! grep -q 'correct for LXC host timezone' .planning/PROJECT.md && ! grep -q 'carries an explicit zone label' .planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md && ! grep -q 'TBD | TBD | TBD' .planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'correct for LXC host timezone' .planning/PROJECT.md` is 0 — deliberately unstripped: this phrase is the false claim being corrected, so it must not survive anywhere in the file. <!-- planner-discipline-allow: correct for LXC host timezone -->
    - `grep -c 'FinalizedDate' .planning/PROJECT.md` is at least 1 — the bullet was rewritten, not deleted.
    - `grep -c 'carries an explicit zone label' .planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md` is 0 — the stale manual-verification instruction is replaced with the locked no-label behaviour. <!-- planner-discipline-allow: carries an explicit zone label -->
    - `grep -c 'TBD | TBD | TBD' .planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md` is 0 — every verification-map row names a real task, plan and wave.
    - `grep -c '❌ W0' .planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-VALIDATION.md` is 0 — every Wave 0 gap is closed or explicitly annotated with why it was not built.
    - `git diff --name-only -- QuestBoard.Domain QuestBoard.Service QuestBoard.Repository QuestBoard.UnitTests QuestBoard.IntegrationTests` returns nothing for this task — it touches planning documents only.
  </acceptance_criteria>
  <done>The standing tech-debt entry describes the shipped reality instead of an assumption about a host that is not there, and the validation map names real tasks instead of placeholders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — container zone, first-paint behaviour, and no moved game nights</name>
  <read_first>
    - `86-RESEARCH.md` § "Pitfall 7" and § "Assumptions Log" row A1 — the container tzdata claim is `[CITED]` from third-party sources, never verified against the built image. This checkpoint is where it stops being an assumption.
    - `86-VALIDATION.md` § `## Manual-Only Verifications` — the three behaviours that cannot be asserted from CI, as corrected in Task 3.
    - `86-UI-SPEC.md` §2 — the no-flash contract, so the verifier knows the server-rendered fallback is the board-zone string with no zone label, not UTC with one.
    - `86-04-PLAN.md` § `<recorded_correction>` item B — the deliberate QuestLog format asymmetry the operator is being asked to accept or reject.
    - `docker-compose.yml` — the service name and published port for the `docker compose` commands below.
  </read_first>
  <what-built>
Phase 86 in full: a configurable board-clock seam feeding both the three Hangfire sweeps and every ambient clock read; an `Html.LocalTime` helper emitting `<time class="local-time" data-style="..." datetime="...Z" title="... UTC">`; a `site.js` hydration pass that rewrites those elements into the viewer's own zone; ~22 converted render sites across desktop views and their mobile twins; a `board-timezone` health check reporting `Degraded` when the zone cannot be resolved; and the regression guards proving no game night moved and the calendar feed is untouched.

Three behaviours cannot be proven from CI and need a human. The first is the container's tzdata, which `86-RESEARCH.md` records as **Assumption A1** — `[CITED]` from third-party sources, never verified against the actual built image. It is surfaced here as an explicit assumption rather than silently assumed to hold.
  </what-built>
  <how-to-verify>
**1. Container tzdata (RESEARCH.md Assumption A1 — unproven until now).**
Build the image and check the zone file exists, then confirm the health check agrees:
  - `docker compose build questboard`
  - `docker run --rm <built-image-tag> /bin/sh -c 'ls /usr/share/zoneinfo/Europe/Amsterdam'` — expect the path to print, not an error.
  - `docker compose up -d`, wait for the container to report healthy, then `curl -s http://localhost:8080/health` — expect the body `Healthy`, not `Degraded`.
If it prints `Degraded`, the assumption was wrong: the application is still correct and still booting (that is the whole point of the fallback), but the sweeps are running on UTC and the Dockerfile needs a tzdata install. Report which you saw.

**2. No flash of a wrong-looking time, for a viewer outside the board zone.**
In a browser, set the device or browser timezone to something clearly different — `America/New_York` is easiest to read — and load `/Account/Profile` on a user with a calendar subscription.
  - Expect: the "Last fetched" value appears immediately as Amsterdam time and then becomes New York time. What you must NOT see is a UTC value, a zone label such as "UTC" or "CEST" in the visible text, an empty gap, or a spinner.
  - Hover the value: the tooltip shows the raw UTC instant, for example `2026-09-20 09:45 UTC`. That is the only place `UTC` should ever appear.
  - Reload with JavaScript disabled: the value stays Amsterdam time permanently. That is intended, not a fallback failure.
  - Check the same page on a narrow mobile viewport (or the mobile layout), and again with the browser language set to German or Russian, confirming the longer month name does not clip or wrap destructively in the subscription row. This is the one item the design contract marked as a backstop with no automated coverage.

**3. No game night moved, on the pages that matter most.**
  - Open the board (`/Quest`), a quest's Details page, a quest's Manage page, the calendar, and the events list. Every proposed date, every finalized session time and every event start time must read exactly as it did before this phase — same day, same hour. Compare against a screenshot or a second browser pointed at the deployed old version if one is available.
  - Open `/QuestLog`. Note the deliberate cosmetic asymmetry this phase introduces: a quest that was *finalized* shows its Completed date in the old wording, while a quest that was only *closed* shows the new canonical short-date form. That difference is expected and was accepted during planning; confirm you are content with it rather than treating it as a defect.
  - If you subscribe to the calendar feed on a real device, confirm a finalized session still sits at the same hour in your calendar app after the feed refreshes. Nothing should move.
  </how-to-verify>
  <acceptance_criteria>
    - The verifier reports the result of `docker run --rm <built-image-tag> /bin/sh -c 'ls /usr/share/zoneinfo/Europe/Amsterdam'` — present or absent — and the `/health` body observed on the running container (`Healthy` or `Degraded`). Record the answer in the summary either way; an absent zone file is a valid outcome that turns Assumption A1 into a known fact plus a follow-up, not a phase failure.
    - The verifier confirms that on `/Account/Profile` with the browser zone set outside the board zone, the pre-hydration text carries no zone label and no `UTC` string, and the post-hydration text is the viewer's zone.
    - The verifier confirms that with JavaScript disabled the value stays board time and no value is blank.
    - The verifier confirms that no proposed date, finalized session time or event start time changed on `/Quest`, a quest Details page, a quest Manage page, the calendar and the events list.
    - The verifier states explicitly whether the QuestLog finalized-versus-closed format asymmetry is accepted or is to be reworked.
    - The resume signal is received; on anything other than approval, the reported issues are recorded in the summary and a gap-closure pass is proposed rather than the phase being marked complete.
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe what you saw — especially if `/health` reported `Degraded`, if any game night moved, or if the QuestLog asymmetry is not acceptable.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar-feed subscriber → `.ics` document | an anonymous, bearer-token URL whose `DTSTART` values this phase must not move |
| CI / future developer → the board-clock seam | source-level enforcement is the only thing preventing a later phase from reintroducing an ambient clock read |
| deployment image → `TimeZoneInfo` resolution | the container's tzdata determines whether the board zone resolves or falls back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-10 | Tampering | `FinalizedDate` reaching the calendar feed | high | mitigate | Two independent guards, not one intention: `CalendarFeedFloatingTimeGuardTests` pins the emitted `DTSTART` digits and the absence of `TZID`/`VTIMEZONE`, and a `git diff --name-only` check proves `CalendarFeedWriter.cs` and `CalendarSubscriptionService.cs` never appear in the phase's diff. The human checkpoint adds a third, real-device confirmation. |
| T-86-15 | Tampering | silent reintroduction of an ambient clock read in a future phase | medium | mitigate | `AmbientClockSeamTests` asserts on the source text of seven named paths with a comment-stripping match, a closed exemption list, and a failure message that teaches the fix. Its own effectiveness is proven by the reinsert-then-revert acceptance criterion. |
| T-86-16 | Denial of Service | container missing tzdata, degrading all three sweeps to UTC on every boot | medium | transfer | Transferred to the operator via the blocking human checkpoint plus the `board-timezone` health check. The application remains correct and available in this state by design; what the checkpoint buys is that the operator learns about it on first deploy rather than months later. Assumption A1 is surfaced explicitly rather than assumed. |
| T-86-17 | Information Disclosure | `title` attribute disclosing raw UTC instants across ~22 render sites | low | accept | Accepted as the locked disclosure mechanism. The attribute reveals only the instant already rendered in the element's visible text, at a different precision, on pages the reader is already authorized to see. |
| T-86-SC | Tampering | package-manager installs | high | mitigate | No install task exists in any plan of this phase; `86-RESEARCH.md` § Package Legitimacy Audit records N/A with the reason. If an install becomes necessary, stop and escalate rather than proceeding. |
</threat_model>

<verification>
- `dotnet build` exits 0 and `dotnet test` exits 0 across the full suite.
- `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~WallClockUnmoved` — see the integration filter; the unit-side guard is `CalendarFeedFloatingTimeGuard`.
- `git diff --name-only main...HEAD -- QuestBoard.Domain/Services/CalendarFeedWriter.cs QuestBoard.Domain/Services/CalendarSubscriptionService.cs` returns nothing.
- `grep -rn 'server local time' QuestBoard.Service/` returns nothing.
- `grep -c 'correct for LXC host timezone' .planning/PROJECT.md` is 0.
- The human-verification checkpoint is approved, with the container tzdata result recorded either way.
</verification>

<success_criteria>
- A wall-clock value is proven unmoved under two different non-default board zones, on a real rendered page.
- The `.ics` feed's floating-local-time contract is pinned by a test, and the two feed source files are absent from the phase diff.
- The ambient clock cannot creep back into any of the seven migrated paths without failing a test that names the file, the line and the fix.
- `PROJECT.md`'s tech-debt list describes the shipped reality, and `86-VALIDATION.md` names real tasks with no `TBD` placeholders and no open Wave 0 gap.
- The operator has confirmed the container resolves its zone, that no flash of a wrong-looking time occurs for a viewer abroad, that no game night moved, and that the QuestLog format asymmetry is acceptable.
</success_criteria>

<output>
Create `.planning/phases/86-viewer-local-times-and-correct-job-scheduling/86-06-SUMMARY.md` when done.
</output>