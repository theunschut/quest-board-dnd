---
phase: 85-one-shot-quests-in-the-calendar-feed
plan: 06
type: execute
wave: 5
depends_on:
  - 85-05
files_modified:
  - QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md
autonomous: true
requirements:
  - QUESTFEED-08
  - QUESTFEED-09

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The shipped copy guard forbids any application copy claiming the board knows how long a session runs, in the same shape it already forbids the equivalent claim about an event (QUESTFEED-08)"
    - "All eighteen QUESTFEED requirements read as complete in REQUIREMENTS.md's body and as Complete in its Traceability table"
    - "ROADMAP.md's Phase 85 block reads 6/6 plans complete with every plan bullet checked, and records the decisions this phase reached"
    - "85-VALIDATION.md's per-task map reads green for every automated row, its Wave 0 checklist is complete, and its frontmatter reads nyquist_compliant: true"
    - "85-VALIDATION.md still carries both manual-only rows as open, with the relational-translation row naming the compensating manual check and the real-device row naming the phase that deferred it"
  artifacts:
    - QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md
  key_links:
    - "Every requirement flipped to complete here is covered by at least one green automated fact named in 85-VALIDATION.md's per-task map"
    - "The copy guard's new forbidden phrases sit in the same data-driven theory as the existing ones, so they are enforced by the same walk rather than by a parallel mechanism"
  prohibitions:
    - "No requirement is marked complete without a green automated fact or a recorded manual verification behind it"
    - "The real-device subscription check is NOT marked closed, claimed, or removed — it was deferred in the prior phase and this phase does not observe it"
    - "The relational SQL-translation gap is NOT marked closed — it is deferred a third time with its reason recorded"
    - "No CALFEED id and no sibling phase entry is altered"
    - "No planning or tracking identifier appears in any source comment, doc comment or string literal"
---

<objective>
Close the phase: add the one shipped guard this phase's locked decisions genuinely require, then bring all three ledgers into agreement with what was actually built and actually proven — and leave the two open gaps visibly open rather than quietly closed.

Purpose: a phase that ships behaviour but leaves its requirement ledger reading Pending produces a milestone audit finding, and a phase that flips a ledger to Complete without a green fact behind each line produces something worse. This plan does both halves deliberately: each requirement is flipped only against a named fact, and the two things this phase did not prove are re-stated as open in the same pass.

The guard in Task 1 is not bookkeeping. The locked decision behind the four-hour session block states plainly that the figure is invented and **must not be presented anywhere in the application UI as though the board knows how long a session runs**. This phase adds no UI, so there is nothing to fix today — which is exactly why the guard belongs here. It costs two lines in an existing data-driven theory now, and it is the only thing that will stop a future phase from writing that claim into a view.

Output: an extended copy guard, three agreeing ledgers, and two gaps still visibly open.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-CONTEXT.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-02-SUMMARY.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-03-SUMMARY.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-04-SUMMARY.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-05-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The phase-wide symbol inventory lives in `85-02-PLAN.md`'s `<artifacts_this_phase_produces>` section. This plan adds no new production symbol and no new file.

New in this plan:

- Additional cases in the existing data-driven forbidden-claim theory in `QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs`, plus an extension of that class's comment explaining why each new case is forbidden
- Ledger state changes in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Forbid any copy claiming the board knows how long a session runs</name>
  <files>QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs` — the whole file, and in particular the forbidden-claim theory around line 127 with its data cases and the comment above them explaining why each existing case is forbidden, plus the view-reading helpers and the planning-reference guard at the end of the file
    - `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-CONTEXT.md` — decision D-05, and specifically the sentence stating the invented duration must not be presented in the application UI as though the board knows it
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-CONTEXT.md` — decision D-02, the equivalent statement about the invented event duration, which is what the existing forbidden case already defends
    - `QuestBoard.Service/Views/Account/Profile.cshtml` and `QuestBoard.Service/Views/Account/Profile.Mobile.cshtml` — the two layouts the theory walks, to confirm neither already contains a phrase the new cases would match
  </read_first>
  <action>
Extend the existing forbidden-claim theory. Do not add a second theory, do not add a second walk, and do not change any existing case.

Add cases covering the shapes a claim about session length would take in copy: a stated number of hours presented as the session's length, and any phrasing asserting a session runs for, lasts, or ends after a given time. Write them as the same kind of case-insensitive pattern data the existing cases use, matched against the same two layouts. Pick patterns tight enough not to match unrelated copy — a bare number of hours appearing anywhere in a Profile page is not the target; a number of hours adjacent to session, quest or game wording is.

Extend the comment above the case list to say why, in the same voice as the existing entries and with no planning or tracking identifier in it: nothing in the schema records when a session ends, the block the feed emits is invented purely so the entry has a length on a phone, and copy that presents it as known would be telling a member something the board does not know. Note that it is forbidden for the same reason the equivalent claim about an event already is.

This phase ships no view, so both layouts pass these cases today. Say that in the comment too — the case exists to stop a future surface from introducing the claim, not to fix one.

Leave the existing planning-reference guard exactly as it is. It already walks the shipped source tree for phase, requirement and review identifiers, and it is the standing enforcement behind every no-tracking-identifier acceptance criterion in this phase's plans — nothing needs adding to it for this phase, and touching it risks its self-exclusion logic.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionStaticGuardTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionStaticGuardTests` exits 0
    - The forbidden-claim theory's case count is higher than before this task, and every pre-existing case is unchanged
    - Temporarily adding a phrase claiming a session runs for a given number of hours into one Profile layout makes the theory fail; removing it makes it pass. Run this mutation check once and revert it — a forbidden-phrase list nobody has seen trip is a list that may match nothing.
    - The planning-reference guard test still passes and its body is unchanged
    - `dotnet test` exits 0 for the whole solution
  </acceptance_criteria>
  <done>The copy guard forbids any application copy presenting the invented session length as something the board knows, enforced by the same walk that already forbids the equivalent claim about an event, and proven to trip by a mutation check.</done>
  <reversibility rating="reversible">Test-only. Removing a case from a data-driven theory is a one-line edit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Bring REQUIREMENTS.md and ROADMAP.md into agreement with what shipped</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the `### Calendar Feed — One-Shot Quest Sessions` section and the eighteen QUESTFEED rows in the `## Traceability` table, both as plan 85-01 wrote them; and the Phase 84 CALFEED section directly above, for the completed shape to match
    - `.planning/ROADMAP.md` section `### Phase 85: One-Shot Quests in the Calendar Feed` — its `**Plans**` line, its wave-grouped plan list, its `**Decisions this phase must still reach before planning:**` block and its `**Risks this phase must actively avoid:**` block; and the Phase 84 section directly above, for the shape a completed phase entry takes including its `**Decisions reached**` block
    - `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` — the per-task verification map, so each requirement is flipped against a named fact rather than against a memory of having written one
    - The five summary files `85-02-SUMMARY.md` through `85-05-SUMMARY.md` and `85-01-SUMMARY.md` — what was actually built, which may differ from what was planned
  </read_first>
  <action>
Two files, every edit scoped. Do not rewrite either file.

**In `.planning/REQUIREMENTS.md`:** flip each of the eighteen QUESTFEED body bullets from unchecked to checked, and each of the eighteen Traceability rows from Pending to Complete.

Flip each one **only** against evidence. Before flipping a given id, find the row in `85-VALIDATION.md`'s per-task map that names it and confirm that row's automated command is green. If any id has no green row behind it, do not flip it — leave it unchecked, leave its traceability row Pending, and state plainly in this plan's summary which id and why. A ledger that reads complete ahead of the work is worse than one that reads behind it, because the next audit trusts it.

If the shipped behaviour diverged from a requirement's wording — a bound that moved, a default that changed, a case that turned out unreachable — reword that requirement to match what shipped rather than flipping a sentence that is no longer true, and note the rewording in the summary. Phase 84 did exactly this when one of its own requirements had to be reworded mid-phase.

**In `.planning/ROADMAP.md`, inside the Phase 85 block only:** check every plan bullet in the wave-grouped list, and change the `**Plans**` line to read six of six complete in the shape Phase 84's line uses.

Then replace the `**Decisions this phase must still reach before planning:**` block. Every question in it has been answered: whether a session the reader runs counts, whether a waitlisted signup counts, what happens when a finalized date moves or a quest is closed, and whether the vote-marker convention carries over. Replace the block with a `**Decisions reached**` block in the shape Phase 84's section uses — a short summary of each decision with a pointer to `85-CONTEXT.md` as authoritative over the summaries. Cover all ten locked decisions plus the query-shape and ordering calls the plans made, each in one or two sentences. Record explicitly that two of the roadmap's original open questions became moot rather than being answered: there is nothing left to mark once waitlisted signups are excluded, and closing a quest is rejected on a one-shot board so it is unreachable for any quest this phase can emit.

Then revise the `**Risks this phase must actively avoid:**` block in place, marking each of the three named risks with how it was addressed rather than deleting them — the cross-board read carries the pinned set, the re-check and the logged drop; membership and board type are two predicates applied inside one condition and each is proven against a second board; and identifiers are namespaced by source with a colliding-id fact behind it.

Finally, append to the Phase 85 block a short paragraph recording the two gaps this phase did **not** close, in the same plain voice Phase 84 used for its own deferred check: the integration suite runs on an in-memory provider and cannot prove the new predicate's relational translation, deferred for the third time with the manual compensating check recorded in `85-VALIDATION.md`; and the real-device subscription check inherited from Phase 84 remains unobserved, so no claim about refresh latency or client rendering is made anywhere in this phase's output.

Before editing the roadmap, record its current count of lines beginning `### Phase `; after editing, confirm it is unchanged.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*QUESTFEED-' /mnt/Data/repos/quest-board-dnd/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*QUESTFEED-' .planning/REQUIREMENTS.md` outputs `18`, or fewer with each shortfall named and justified in the plan summary
    - `grep -c '^| QUESTFEED-[0-9][0-9] | Phase 85 | Complete |$' .planning/REQUIREMENTS.md` matches the checked-bullet count exactly
    - `grep -c '^- \[x\] \*\*CALFEED-' .planning/REQUIREMENTS.md` still outputs `17`
    - `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged from its pre-edit value
    - The Phase 85 block contains no unchecked plan bullet and no `**Decisions this phase must still reach before planning:**` heading
    - The Phase 85 block contains a decisions-reached summary naming all ten locked decisions plus the query-shape and ordering calls, and pointing at `85-CONTEXT.md` as authoritative
    - The Phase 85 block names both open gaps explicitly, and neither is described as closed, resolved or verified
    - `git diff .planning/ROADMAP.md` shows changes confined to the Phase 85 block
  </acceptance_criteria>
  <done>Both requirement ledgers read complete for every QUESTFEED id that has a green fact behind it, the roadmap's Phase 85 block reads six of six with its open questions replaced by the decisions actually reached and its risks marked as addressed, and both unclosed gaps are named in place.</done>
  <reversibility rating="reversible">Documentation ledger state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Sign off the validation contract, and leave the two open gaps open</name>
  <files>.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` — the frontmatter, the per-task verification map as plan 85-01 keyed it, the Wave 0 Requirements checklist, the Manual-Only Verifications table and the Validation Sign-Off checklist
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md` — the shape a signed-off contract takes in this project
    - The five summary files from this phase, for the actual test counts and commands
  </read_first>
  <action>
Run the full suite first: the sign-off is a record of an observation, not a formality. If anything is red, stop and report rather than signing.

Then update `85-VALIDATION.md`:

Set each row's Status column in the per-task verification map to green, and each row's File Exists column to reflect that the file now exists. Where a row's automated command has changed from what plan 85-01 predicted — a different filter, a different project — correct it to the command that actually runs, and say so in the summary.

Tick every item in the Wave 0 Requirements checklist.

Tick the Validation Sign-Off checklist items that are genuinely true, and set `nyquist_compliant: true` in the frontmatter. Leave `status` at `draft`; promoting it to `validated` belongs to `/gsd-validate-phase`, not here. Set `wave_0_complete: true` only if every Wave 0 item is ticked.

Add a short line beneath the sign-off recording the total automated fact count this phase added and the commands that run them, so a later audit can re-run the evidence rather than trust the ticks.

**Leave the Manual-Only Verifications table open, and strengthen it rather than closing it.** Both rows stay. For the relational-translation row, confirm its test instructions are still runnable as written against this phase's shipped code and correct them if the shipped method name or route differs from what was seeded. Add one sentence recording that this is the third consecutive phase to defer this gap and that closing it means standing up relational test infrastructure, which is larger than this phase.

For the real-device row, confirm it still reads as inherited and unclosed. Do not add a claim about refresh latency, client rendering, or how a client behaves when an entry disappears between polls — none of that was observed by this phase, and the silent-disappearance decision depends on exactly that unobserved behaviour, which is worth stating in the row rather than leaving implicit.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` exits 0 for the whole solution before any tick is written
    - `grep -c '⬜ pending' .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` outputs `0`
    - The frontmatter reads `nyquist_compliant: true` and `status: draft`
    - Every unticked box in the Wave 0 Requirements checklist is gone
    - The Manual-Only Verifications table still carries exactly two rows, and neither is described as closed, resolved, verified or observed
    - The relational-translation row's instructions name a command and a path that exist in the shipped code
    - A line beneath the sign-off names the total fact count this phase added and the commands that run them, and those commands exit 0 when run
  </acceptance_criteria>
  <done>The validation contract is signed off against an observed green suite, with every automated row green, the Wave 0 checklist complete, and both manual-only gaps left explicitly open and still runnable.</done>
  <reversibility rating="reversible">Documentation ledger state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application copy to member | What the interface asserts as fact. The invented session length is not something the board knows, and presenting it as known is a false statement to a member — the boundary Task 1's guard defends. |
| ledger to future audit | A requirement marked complete is read by a later milestone audit as evidence. A ledger flipped ahead of the work is a false statement to that audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-14 | Repudiation | a requirement marked complete with nothing behind it | medium | mitigate | Task 2 forbids flipping any id without a green row naming it in the per-task verification map, and requires any shortfall to be named in the summary rather than papered over. Task 3 runs the full suite before writing a single tick. |
| T-85-15 | Spoofing | application copy presenting the invented session length as a fact the board knows | low | mitigate | Task 1 adds the claim to the shipped forbidden-copy theory with a mutation check proving the new cases actually trip. No UI ships in this phase, so the guard is preventive: it is what stops a future surface from introducing the claim. |
| T-85-16 | Repudiation | a deferred gap silently disappearing from the record | medium | mitigate | Tasks 2 and 3 both require the two open gaps to be restated in place, and both carry an acceptance criterion that neither may be described as closed, resolved, verified or observed. The real-device gap in particular was deferred by operator decision in the prior phase and belongs to the operator to close, not to this phase to quietly drop. |
| T-85-07 | Tampering | whole-file rewrite of a ledger | medium | mitigate | Task 2 mandates scoped replacements and asserts the roadmap's phase-heading count is unchanged, and requires the roadmap diff to be confined to the Phase 85 block. |
| T-85-SC | Tampering | package-manager installs | n/a | accept | No install task exists in this phase; the legitimacy gate is not applicable. |

**ASVS level 1, block on high.** This plan touches no high-severity threat. The phase's high-severity register entries are stated in `85-02-PLAN.md` and verified in plans 85-03 and 85-05.
</threat_model>

<verification>
- `dotnet test` exits 0 for the whole solution
- `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionStaticGuardTests` exits 0, with a case count higher than before this plan
- `grep -c '^- \[x\] \*\*QUESTFEED-' .planning/REQUIREMENTS.md` matches `grep -c '^| QUESTFEED-[0-9][0-9] | Phase 85 | Complete |$' .planning/REQUIREMENTS.md`
- `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged
- `grep -c '⬜ pending' .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` returns `0`
- `git status --porcelain` names exactly the four files in `files_modified`
- The Manual-Only Verifications table carries two rows and both still read as open

## What this phase does not claim

Stated once more, at the close, because this is where such statements get quietly dropped:

- **Relational translation of the new quest predicate is unproven.** Every integration fact runs on the EF Core in-memory provider. This is the third consecutive phase to carry that gap; it is recorded with a runnable manual compensating check, and it is not closed here.
- **No calendar client's behaviour has been observed.** The real-device subscription check was deferred by operator decision in the prior phase because Outlook and Google Calendar fetch server-side and cannot reach a local address. Nothing in this phase observed refresh latency, in-place update, or what a client does when an entry disappears between polls — and the silent-disappearance decision rests on precisely that last behaviour. No output of this phase promises any of it.
</verification>

<success_criteria>
- The shipped copy guard forbids presenting the invented session length as known, proven to trip by a mutation check
- Every QUESTFEED requirement that has a green fact behind it reads complete in both ledgers, and any that does not is named and left open
- ROADMAP.md's Phase 85 block reads six of six, with its pre-planning open questions replaced by the decisions actually reached and its three named risks marked as addressed
- 85-VALIDATION.md is signed off against an observed green full-solution run, with nyquist_compliant true
- Both open gaps remain open, named in both the roadmap and the validation contract, and neither is described as closed
- No sibling phase entry and no CALFEED id changed
</success_criteria>

<output>
Create `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-06-SUMMARY.md` when done
</output>
