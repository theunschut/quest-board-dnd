---
phase: 85-one-shot-quests-in-the-calendar-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md
autonomous: true
requirements:
  - QUESTFEED-01
  - QUESTFEED-02
  - QUESTFEED-03
  - QUESTFEED-04
  - QUESTFEED-05
  - QUESTFEED-06
  - QUESTFEED-07
  - QUESTFEED-08
  - QUESTFEED-09
  - QUESTFEED-10
  - QUESTFEED-11
  - QUESTFEED-12
  - QUESTFEED-13
  - QUESTFEED-14
  - QUESTFEED-15
  - QUESTFEED-16
  - QUESTFEED-17
  - QUESTFEED-18

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md carries a Calendar Feed — One-Shot Quest Sessions section holding QUESTFEED-01 through QUESTFEED-18, each an unchecked one-sentence observable outcome"
    - "REQUIREMENTS.md's Traceability table carries one row per QUESTFEED id, each mapped to Phase 85 with status Pending"
    - "REQUIREMENTS.md's Coverage block reads 134 total and 134/134 mapped"
    - "ROADMAP.md's Phase 85 section names all eighteen QUESTFEED ids instead of TBD, and its Plans line and plan list name the six plans of this phase grouped by wave"
    - "ROADMAP.md's Requirements Coverage table carries one QUESTFEED row per id mapped to Phase 85, and its coverage footer names no phase still awaiting requirements"
    - "85-VALIDATION.md's Per-Task Verification Map names real task ids from this phase's plans instead of TBD, and its Manual-Only Verifications table still carries the InMemory-provider row unchanged"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md
  key_links:
    - "Every QUESTFEED id minted here is claimed by at least one of plans 85-02 through 85-06 in its requirements frontmatter"
    - "Every task id written into 85-VALIDATION.md exists as a named task in the plan it cites"
  prohibitions:
    - "No requirement is minted for a Deferred Idea from 85-CONTEXT.md — nothing about waitlist markers, a Dungeon Master suffix, a separate quest window, opaque transparency, or a cross-board quest page"
    - "No CALFEED id is renumbered, reworded, unchecked or re-mapped — the Phase 84 family is closed and shipped"
    - "No sibling phase entry in ROADMAP.md is rewritten; only the Phase 85 block and the coverage table gain content"
---

<objective>
Mint the `QUESTFEED-*` requirement family for Phase 85, register it in all three ledgers that must agree (REQUIREMENTS.md body, REQUIREMENTS.md Traceability table, ROADMAP.md Phase 85 block plus its Requirements Coverage table), and finish the validation contract by replacing its placeholder task ids with this phase's real ones.

Purpose: `.planning/ROADMAP.md` currently reads `**Requirements**: TBD` for Phase 85. Until the family exists, no other plan in this phase can carry a non-empty `requirements` field and nothing downstream can check coverage. This plan is deliberately documentation-only so it runs in wave 1 in parallel with the tracer plan and touches no source file.

Output: eighteen registered requirement ids, a wave-grouped plan list on the roadmap, and a validation map keyed by real task ids.

**Prefix rationale — a decision this plan makes, recorded deliberately:** the new family is `QUESTFEED-*`, not a continuation of `CALFEED-*`. `CALFEED-01` through `CALFEED-17` are all complete and all mapped to Phase 84 in both traceability tables; extending that family would place Pending and Complete rows for two different phases under one prefix, which no other family in this file does. Every topically adjacent but distinct phase in this project's history minted its own prefix even when directly extending a shipped surface (`EVENT` to `EVTAVAIL` to `EVTRECUR` to `EVTVIEW` to `EVTAGENDA` to `EVTNAME`). 85-RESEARCH.md Open Question 1 recommends the same and leaves the call to the planner; this is that call.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-CONTEXT.md
@.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md
</context>

<recorded_skips>
## Spec-less probe fallback — recorded skip (visible)

The spec-less probe fallback did not run for this phase. Phase 85's ROADMAP entry reads `**Requirements**: TBD`, so there were no requirement ids to probe — they are being minted in this very plan. No probe-derived edge predicates and no probe-derived prohibitions were authored this run.

No `85-SPEC.md` exists either, so there is no `## Edge Coverage`, `## Prohibitions` or `## UI Considerations` section to lift. Every `must_haves.truths` and `must_haves.prohibitions` entry across plans 85-01 through 85-06 was therefore derived directly from the ROADMAP goal, the ten locked decisions D-01 through D-10 in `85-CONTEXT.md`, and the three named ROADMAP risks — not from a resolved spec edge.

The requirement-minting this plan performs resolves the underlying cause for any future run against this phase: once `QUESTFEED-01` through `QUESTFEED-18` are registered, a later probe pass has ids to work from.

## API coverage — recorded skip

No `COVERAGE.md` is required for this phase and none is fabricated. This phase adds no new HTTP surface — 85-CONTEXT.md's Phase Boundary rules out a new endpoint.

## Schema-push gate — recorded skip

No Payload, Prisma, Drizzle, Supabase or TypeORM file patterns exist. This is an EF Core project whose migrations auto-apply on startup through `context.Database.Migrate()`, and **this phase adds no migration at all** — 85-CONTEXT.md's Phase Boundary states "This phase stores nothing new." No blocking schema-push task exists.

## Package Legitimacy Gate — not applicable

No package-manager install task exists anywhere in this phase. 85-RESEARCH.md `## Package Legitimacy Audit` reads "Not applicable — this phase adds no external package." No assumed-or-suspicious package checkpoint is required, and `T-85-SC` in each threat model records the gate as inapplicable rather than passed.
</recorded_skips>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan produces no source symbols. The phase-wide source symbol inventory — every enum member, property, interface method, repository method, options key and test class Phase 85 creates — is listed in `85-02-PLAN.md`'s `<artifacts_this_phase_produces>` section, with per-plan detail in 85-03 through 85-06.

Documentation artifacts produced here:

- `.planning/REQUIREMENTS.md` — new `### Calendar Feed — One-Shot Quest Sessions` section holding `QUESTFEED-01` through `QUESTFEED-18`, plus eighteen new `## Traceability` rows and an updated `**Coverage:**` block
- `.planning/ROADMAP.md` — Phase 85's `**Requirements**` line, `**Plans**` line and `Plans:` list, plus eighteen new `## Requirements Coverage` rows and an updated coverage footer
- `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` — `## Per-Task Verification Map` rows keyed by real task ids, and a `## Wave 0 Requirements` checklist naming the plan that satisfies each item
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mint the eighteen QUESTFEED requirements into REQUIREMENTS.md and its Traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the `### Calendar Feed — Foundation and Event Subscription` section (around line 100) for the exact bullet shape and heading style, the `### Link Previews — Foundation and Quests` heading that follows it, the `## Traceability` table's final `| CALFEED-17 | Phase 84 | Complete |` row, and the `**Coverage:**` block at the end of the file
    - `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-CONTEXT.md` — the `<decisions>` block, so each requirement traces to a real locked decision rather than invented scope
    - `.planning/ROADMAP.md` section `### Phase 85: One-Shot Quests in the Calendar Feed` — the goal, the scope notes, the operator-locked constraints and the three named risks
  </read_first>
  <action>
Insert a new section headed `### Calendar Feed — One-Shot Quest Sessions` into `.planning/REQUIREMENTS.md` under `## v1 Requirements`, immediately after the existing `### Calendar Feed — Foundation and Event Subscription` section and immediately before `### Link Previews — Foundation and Quests`. Use the same bullet shape the neighbouring sections use but unchecked, because these are new: a `- [ ] **QUESTFEED-NN**: ` prefix followed by the sentence.

Write these eighteen bullets, in this order, with these exact sentences:

QUESTFEED-01 — The calendar address a member already holds starts carrying their one-shot quest sessions alongside events, with no second address, no second endpoint, no new page and no new Profile control

QUESTFEED-02 — A finalized quest on a one-shot board the member belongs to, where that member holds a confirmed seat, appears in their feed as a calendar entry

QUESTFEED-03 — A finalized quest the member is running as Dungeon Master appears in their feed even though a Dungeon Master holds no signup row on their own quest

QUESTFEED-04 — A quest where the member is both the Dungeon Master and the holder of a confirmed seat appears exactly once, as a single calendar entry carrying a single identifier

QUESTFEED-05 — A waitlisted signup never reaches the feed, only a confirmed seat does, and a promotion off the waitlist reaches the phone at the next fetch like any other change

QUESTFEED-06 — A Player seat, a Spectator seat and an Assistant Dungeon Master seat all reach the feed identically, with no seat kind singled out

QUESTFEED-07 — A quest flagged as a Dungeon Master session still reaches the feed for anyone holding a confirmed seat on it

QUESTFEED-08 — A quest becomes a timed entry starting at its finalized date and time and running for a configured number of hours, four by default, and is never emitted as an all-day entry

QUESTFEED-09 — The quest session length is changeable through configuration with no code change, and the application refuses to start when it is configured below one hour

QUESTFEED-10 — Every quest entry is marked transparent, exactly like an event, so a subscriber never reads as busy

QUESTFEED-11 — A quest entry's title is the board name in square brackets followed by the quest title, with no marker identifying the entry as a quest and no marker identifying a session the member runs rather than plays

QUESTFEED-12 — A quest entry's title never gains a parenthesised availability answer, for any availability value the entry could hold

QUESTFEED-13 — A quest that stops qualifying, whether by being un-finalized, deleted, moved out of the window, or losing the member's seat, simply disappears from the feed at the next fetch with no cancellation marker emitted

QUESTFEED-14 — Quests use the same rolling window as events, governed by the existing configuration bounds and no second pair of knobs

QUESTFEED-15 — A finalized quest the member holds a confirmed seat on never reaches the feed when its board is a campaign board, while that same board's events keep appearing

QUESTFEED-16 — A quest from a board the member does not belong to never reaches the feed, and a quest row that survives the feed query's predicate but falls outside the member's one-shot board set is dropped before the response and recorded as an error in the application log

QUESTFEED-17 — A quest and an event that share the same numeric identifier produce two distinct calendar identifiers, so neither can overwrite the other in a subscriber's calendar

QUESTFEED-18 — The combined document orders every entry by date and then start time regardless of which source it came from, and a fetch that turns up no qualifying quest produces the same document the event-only feed produced before this phase

Then append eighteen rows to the `## Traceability` table immediately after the existing `| CALFEED-17 | Phase 84 | Complete |` row, each shaped `| QUESTFEED-NN | Phase 85 | Pending |` for NN from 01 to 18. Do not alter any existing row.

Finally update the `**Coverage:**` block: `- v1 requirements: 116 total` becomes `- v1 requirements: 134 total`, and `- Mapped to phases: 116/116 ✓` becomes `- Mapped to phases: 134/134 ✓`. Leave `- Unmapped: 0` unchanged.

Make every edit a scoped replacement. Do not rewrite the file wholesale — it carries 116 shipped requirements across fifteen families and a whole-file rewrite is the one way to lose them.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*QUESTFEED-' /mnt/Data/repos/quest-board-dnd/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*QUESTFEED-' .planning/REQUIREMENTS.md` outputs exactly `18`
    - `grep -c '^| QUESTFEED-[0-9][0-9] | Phase 85 | Pending |$' .planning/REQUIREMENTS.md` outputs exactly `18`
    - `grep -c '^- \[x\] \*\*CALFEED-' .planning/REQUIREMENTS.md` outputs exactly `17`, proving the shipped Phase 84 family is untouched
    - `grep -c '^### Calendar Feed — One-Shot Quest Sessions$' .planning/REQUIREMENTS.md` outputs exactly `1`, and its line number is greater than that of `### Calendar Feed — Foundation and Event Subscription` and smaller than that of `### Link Previews — Foundation and Quests`
    - `.planning/REQUIREMENTS.md` contains the literal `- v1 requirements: 134 total` and the literal `- Mapped to phases: 134/134 ✓`
    - For each of `01 02 03 04 05 06 07 08 09 10 11 12 13 14 15 16 17 18`, `grep -c "QUESTFEED-$n" .planning/REQUIREMENTS.md` is at least `2` — one body bullet plus one traceability row
    - Every one of the eighteen bullets names a behaviour a person or a calendar client can observe; none names a class, method, property or file path
  </acceptance_criteria>
  <done>REQUIREMENTS.md holds eighteen unchecked QUESTFEED bullets in their own section, eighteen Pending traceability rows mapped to Phase 85, a coverage count of 134, and a byte-identical CALFEED family.</done>
  <reversibility rating="reversible">A requirement id family is a documentation ledger entry; renumbering or renaming it is a scoped text edit with no code, data or migration consequence.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Replace ROADMAP.md's Phase 85 TBD requirements, plan count and plan list, and extend its coverage table</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` section `### Phase 85: One-Shot Quests in the Calendar Feed` — the `**Requirements**: TBD` line, the `**Plans**: 0 plans` line, and the `Plans:` list ending in the single placeholder bullet that points at `/gsd-plan-phase 85`
    - `.planning/ROADMAP.md` section `### Phase 84: Calendar Feed Foundation and Event Subscription` — its `Plans:` block, for the exact wave-grouped list shape: a `**Wave N**` heading, with `*(blocked on Wave N-1 completion)*` appended for every wave after the first, followed by checkbox bullets ending in `(wave N)`
    - `.planning/ROADMAP.md` section `## Requirements Coverage` — its two-column table shape, its final `| CALFEED-17 | Phase 84 |` row, and the `**Coverage:**` footer line directly beneath the table
    - `.planning/REQUIREMENTS.md` — the QUESTFEED section written by Task 1, so the ids referenced here match it exactly
  </read_first>
  <action>
Make four scoped replacements inside `.planning/ROADMAP.md`. Every one is a targeted find-and-replace. Do not rewrite the file, and do not touch any `### Phase ` heading other than Phase 85's own block.

First, in the Phase 85 block, replace `**Requirements**: TBD` with a comma-separated list naming `QUESTFEED-01` through `QUESTFEED-18` in ascending order, formatted exactly like Phase 84's own `**Requirements**:` line.

Second, confirm the `**Plans**` line reads `**Plans**: 6 plans`. The planner already registered the plan count and the wave-grouped plan list when the plans were written, so this is a reconciliation rather than a first write — if the line or the list disagrees with the plan files that actually exist on disk in this phase directory, correct it to match the files and note the discrepancy in the summary.

Third, confirm the `Plans:` block carries the wave-grouped list below, in Phase 84's shape, and write it if it is missing:

Wave 1 carries two bullets. The first reads `85-01-PLAN.md — Mint the QUESTFEED requirement family into REQUIREMENTS.md and ROADMAP.md, and key the phase validation contract to real task ids (wave 1)`. The second reads `85-02-PLAN.md — End-to-end tracer: the quest source wired through repository, service, writer and the live feed address, plus the configurable session length and the source-aware writer suite (wave 1)`.

Wave 2, headed `**Wave 2** *(blocked on Wave 1 completion)*`, carries one bullet reading `85-03-PLAN.md — Seat and Dungeon Master predicate suite: both branches that put a quest in the feed, the single-entry guarantee when a reader is both, the confirmed-seat rule, all three seat kinds, and the Dungeon Master session flag (wave 2)`.

Wave 3, headed `**Wave 3** *(blocked on Wave 2 completion)*`, carries one bullet reading `85-04-PLAN.md — Disappearance and window suite: every way a quest stops qualifying, and both bounds of the shared rolling window (wave 3)`.

Wave 4, headed `**Wave 4** *(blocked on Wave 3 completion)*`, carries one bullet reading `85-05-PLAN.md — Board-type narrowing, cross-board isolation with its logged drop, the events-keep-their-reach guard, and merged-document ordering (wave 4)`.

Wave 5, headed `**Wave 5** *(blocked on Wave 4 completion)*`, carries one bullet reading `85-06-PLAN.md — Requirement and roadmap ledger close-out, validation sign-off, and the phase static guard (wave 5)`.

Every bullet is an unchecked checkbox, matching Phase 84's shape before that phase completed. Plan 85-06 checks them at phase close.

**The Requirements line and the coverage table are the genuinely new work in this task** — the planner did not write those, because the ids did not exist until Task 1 of this plan minted them.

Fourth, append eighteen rows to the `## Requirements Coverage` table immediately after the existing `| CALFEED-17 | Phase 84 |` row, each shaped `| QUESTFEED-NN | Phase 85 |` for NN from 01 to 18. Then rewrite the `**Coverage:**` footer line beneath that table so its count reads `134/134` instead of `116/116` and so its trailing clause no longer names a phase awaiting requirements — after this task Phase 85 is no longer awaiting them, and the count of phases awaiting requirements is zero.

Before editing, record the file's current count of lines beginning `### Phase `; after editing, confirm that count is unchanged. A clobbered sibling phase entry is the exact failure mode this guard exists for.
  </action>
  <verify>
    <automated>grep -c '^| QUESTFEED-[0-9][0-9] | Phase 85 |$' /mnt/Data/repos/quest-board-dnd/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| QUESTFEED-[0-9][0-9] | Phase 85 |$' .planning/ROADMAP.md` outputs exactly `18`
    - `grep -c '^### Phase ' .planning/ROADMAP.md` outputs the same number after the edit as before it
    - `grep -c 'QUESTFEED-01' .planning/ROADMAP.md` is at least `2` — one in the Phase 85 requirements line, one in the coverage table
    - `grep -o '85-0[1-6]-PLAN.md' .planning/ROADMAP.md | sort -u | wc -l` outputs exactly `6`
    - The Phase 85 block contains no occurrence of the literal `TBD`
    - `grep -c '134/134 requirements mapped' .planning/ROADMAP.md` outputs `1`
    - The Phase 85 `Plans:` block contains five wave headings and six unchecked plan bullets, and every bullet ends with its own `(wave N)` marker matching the heading it sits under
  </acceptance_criteria>
  <done>ROADMAP.md's Phase 85 block names eighteen requirement ids, six plans and a five-wave plan list; its coverage table carries eighteen new Phase 85 rows; and no sibling phase entry changed.</done>
  <reversibility rating="reversible">Roadmap ledger text. Reverting is a scoped text edit with no code or data consequence.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Key 85-VALIDATION.md's per-task map to this phase's real task ids</name>
  <files>.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` — the `## Per-Task Verification Map` table, the `## Wave 0 Requirements` checklist, the `## Manual-Only Verifications` table and the `## Validation Sign-Off` checklist
    - `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-02-PLAN.md`, `85-03-PLAN.md`, `85-04-PLAN.md`, `85-05-PLAN.md` and `85-06-PLAN.md` — the `<tasks>` blocks, to read each task's position within its plan so the task ids below are correct
  </read_first>
  <action>
Rewrite the `## Per-Task Verification Map` table rows in `85-VALIDATION.md`, replacing every `TBD` in the Task ID, Plan and Wave columns with real values. Keep every existing column, keep the `Threat Ref` column populated where this plan set defines a threat id, keep the `Secure Behavior`, `Test Type`, `Automated Command` and `File Exists` columns as written, and keep the status vocabulary line beneath the table. Task ids follow the form `85-<plan>-T<n>` where `<n>` is the task's ordinal position in its plan's `<tasks>` block.

Assign each existing row as follows. The D-01-both-branches row and the D-02/D-03 row and the D-04 row all belong to plan `85-03`, wave 2 — map them to `85-03-T1`, `85-03-T2` and `85-03-T2` respectively, and add requirement ids `QUESTFEED-03`/`QUESTFEED-04`, `QUESTFEED-05`/`QUESTFEED-06` and `QUESTFEED-07`. The D-05 row and the D-06/D-07/D-08 row belong to plan `85-02`, wave 1, task `85-02-T3`, with requirement ids `QUESTFEED-08`/`QUESTFEED-09` and `QUESTFEED-10`/`QUESTFEED-11`/`QUESTFEED-12`. The D-09 row and the D-10 row belong to plan `85-04`, wave 3, tasks `85-04-T1` and `85-04-T2`, with requirement ids `QUESTFEED-13` and `QUESTFEED-14`. The board-type-narrowing row and the tenant-isolation row belong to plan `85-05`, wave 4, both to task `85-05-T1`, with requirement ids `QUESTFEED-15` and `QUESTFEED-16` and threat refs `T-85-02` and `T-85-01`. The UID-collision row belongs to plan `85-02`, wave 1, task `85-02-T3`, with requirement id `QUESTFEED-17` and threat ref `T-85-04`.

Add four rows the seeded table does not yet carry, in the same shape: the tracer end-to-end row (`85-02-T1`, plan `85-02`, wave 1, requirements `QUESTFEED-01`/`QUESTFEED-02`, threat ref `T-85-03`, automated command `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionQuestFeedTests`); the configuration-refuses-to-start row (`85-02-T2`, plan `85-02`, wave 1, requirement `QUESTFEED-09`, automated command `dotnet test QuestBoard.UnitTests --filter CalendarFeedOptionsValidationTests`); the logged-drop row (`85-05-T2`, plan `85-05`, wave 4, requirement `QUESTFEED-16`, threat ref `T-85-01`, automated command `dotnet test QuestBoard.UnitTests --filter CalendarSubscriptionQuestRecheckTests`); and the merged-ordering-and-events-unchanged row (`85-05-T3`, plan `85-05`, wave 4, requirement `QUESTFEED-18`, automated command `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionQuestFeedTests`).

Update the `## Wave 0 Requirements` checklist so each item names the plan that creates or extends it: the new integration test file is created by `85-02` and extended by `85-03`, `85-04` and `85-05`; the writer unit suite extension is made by `85-02`; the options-validation suite extension is made by `85-02`; the second-layer re-check unit suite is a new file created by `85-05`; the no-framework-install item stays as written.

Leave the `## Manual-Only Verifications` table exactly as written. Both of its rows stay open by deliberate decision: the relational SQL-translation row is an inherited gap this phase does not close (see each plan's `<verification>` block for the stated reason), and the real-device row was deferred by the operator in Phase 84 and this phase does not claim to close it.

Do not set `nyquist_compliant: true` or `wave_0_complete: true` in the frontmatter here. Those are set by the closing plan `85-06` once every suite is green, and by `/gsd-validate-phase` respectively.
  </action>
  <verify>
    <automated>grep -c 'TBD' /mnt/Data/repos/quest-board-dnd/.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD' .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` outputs `0`
    - `grep -cE '^\| 85-0[2-5]-T[0-9]+ \|' .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` outputs `14`
    - Every task id in the Task ID column matches the pattern `85-0N-TM` and names a task that exists at position M in plan `85-0N`'s `<tasks>` block
    - Every row's Requirement column names at least one id from `QUESTFEED-01` through `QUESTFEED-18`
    - The `## Manual-Only Verifications` table still carries exactly two rows and both still read as open
    - The frontmatter still reads `status: draft`, `nyquist_compliant: false` and `wave_0_complete: false`
  </acceptance_criteria>
  <done>85-VALIDATION.md's per-task map is keyed by fourteen real task-id references across four plans, every row names a requirement id, and both manual-only rows remain open and unchanged.</done>
  <reversibility rating="reversible">A validation ledger table. Re-keying it is a scoped text edit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner to ledger files | This plan writes only to `.planning/` documentation. No trust boundary in the running application is crossed, created or moved by it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-07 | Tampering | `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` whole-file rewrite | medium | mitigate | Tasks 1 and 2 mandate scoped replacements only, and Task 2 asserts the pre-edit `### Phase ` heading count is unchanged, so a clobbered sibling phase entry or a lost requirement family fails the task |
| T-85-08 | Repudiation | A minted requirement that no plan claims | low | mitigate | Each of Tasks 1 through 3 lists ids, and `must_haves.key_links` requires every minted id to appear in at least one of plans 85-02 through 85-06 `requirements` frontmatter; the downstream requirement-coverage gate reads the same fields |
| T-85-SC | Tampering | package-manager installs | n/a | accept | No install task exists in this phase. 85-RESEARCH.md `## Package Legitimacy Audit` records the gate as not applicable because no external package is added. Nothing to verify and no human checkpoint required |

No application-facing threat is introduced by this plan. The security-relevant threat register for the running application — cross-tenant leakage, board-type leakage, the Dungeon Master session decision and UID collision — lives in `85-02-PLAN.md` and is reproduced in each plan that implements or verifies one of its mitigations.
</threat_model>

<verification>
Run after every task, from the repository root:

- `grep -c '^- \[ \] \*\*QUESTFEED-' .planning/REQUIREMENTS.md` returns `18`
- `grep -c '^| QUESTFEED-[0-9][0-9] | Phase 85 | Pending |$' .planning/REQUIREMENTS.md` returns `18`
- `grep -c '^| QUESTFEED-[0-9][0-9] | Phase 85 |$' .planning/ROADMAP.md` returns `18`
- `grep -c 'TBD' .planning/phases/85-one-shot-quests-in-the-calendar-feed/85-VALIDATION.md` returns `0`
- `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged from its pre-plan value
- `git diff --stat` names exactly the three files in `files_modified` and no source file at all

This plan compiles nothing and runs no test — it touches no code. `dotnet build` is not part of its verification and running it proves nothing about this plan's work.
</verification>

<success_criteria>
- Eighteen QUESTFEED requirements exist as unchecked v1 bullets in their own REQUIREMENTS.md section, each an observable outcome
- Eighteen Pending traceability rows map those ids to Phase 85 in REQUIREMENTS.md, and eighteen coverage rows map them in ROADMAP.md
- Both coverage counters read 134
- ROADMAP.md's Phase 85 block names its eighteen requirement ids, six plans and a five-wave plan list, with no remaining `TBD`
- 85-VALIDATION.md's per-task map carries fourteen real task-id references and no placeholder, and both manual-only rows stay open
- No source file changed
</success_criteria>

<output>
Create `.planning/phases/85-one-shot-quests-in-the-calendar-feed/85-01-SUMMARY.md` when done
</output>
