---
phase: 74-event-schema-crud-and-calendar-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Repository/Entities/EventSeriesEntity.cs
  - QuestBoard.Repository/Entities/EventEntity.cs
  - QuestBoard.Repository/Entities/EventSignupEntity.cs
  - QuestBoard.Repository/Entities/QuestBoardContext.cs
  - QuestBoard.Repository/Migrations/
autonomous: true
requirements: [EVENT-01, EVENT-02]

must_haves:
  truths:
    - "Three entity types exist — EventSeriesEntity, EventEntity, EventSignupEntity — each implementing IEntity, mapped to tables EventSeries, Events, EventSignups (D-02)."
    - "EventEntity.Date is DateOnly and EventEntity.StartTime is TimeOnly?, mapping to SQL Server date and time with no third-party package (D-01)."
    - "EventEntity carries a nullable SeriesId FK so a one-off event and a future materialized occurrence are the same entity (D-03)."
    - "EventEntity and EventSeriesEntity each carry their own GroupId; EventSignupEntity is scoped through its required Event navigation (D-04)."
    - "All three query filters are fail-closed: a null ActiveGroupId returns zero rows for all three entities, including EventSignupEntity which no code reads yet (D-04, RESEARCH Pitfall 3)."
    - "One purely additive migration creates all three tables in dependency order with no backfill, so startup migration cannot break boot (D-02, D-24)."
    - "EventEntity has no category discriminator and no link to a quest, so an event is informational by definition and its meaning comes from the board type (D-07)."
  artifacts:
    - QuestBoard.Repository/Entities/EventEntity.cs
    - QuestBoard.Repository/Entities/EventSeriesEntity.cs
    - QuestBoard.Repository/Entities/EventSignupEntity.cs
    - QuestBoard.Repository/Migrations/*_AddCalendarEventsFeature.cs
  key_links:
    - "QuestBoardContext.OnModelCreating gains three HasQueryFilter entries in the existing filter block, referencing activeGroupContext directly rather than a captured local."
    - "The generated migration's Date column reads type 'date' and StartTime reads type 'time'."
  prohibitions:
    - statement: "EventEntity carries no kind or category discriminator column and no relationship to a quest — an event's meaning comes from the board's own immutable board type, and events are informational by definition (D-07)."
      status: required
      verification: "EventEntity declares exactly the ten members listed in Task 1 and no others; the file has no navigation property or foreign key pointing at a quest, and no enum-backed category column."
    - statement: "No third-party DateOnly/TimeOnly EF package (for example ErikEJ.EntityFrameworkCore.SqlServer.DateOnlyTimeOnly) may be added to any csproj — EF Core 10 maps these natively and the package would conflict."
      status: required
      verification: "`git diff` shows no PackageReference added to any .csproj in this plan."
    - statement: "No EF Core package may be added to QuestBoard.Service."
      status: required
      verification: "`grep -c EntityFrameworkCore QuestBoard.Service/QuestBoard.Service.csproj` is unchanged from its pre-plan value."
    - statement: "The migration must contain no data backfill, no UPDATE, no DELETE, and no alteration of any pre-existing table."
      status: required
      verification: "The generated Up() method contains only CreateTable and CreateIndex calls; Down() contains only DropTable calls."
    - statement: "ActiveGroupId must never be captured into a local variable before a HasQueryFilter lambda."
      status: required
      verification: "All three new filter lambdas dereference activeGroupContext.ActiveGroupId inline, matching the existing QuestEntity filter."
---

<objective>
Create the storage layer and tenant scoping that the whole Calendar Events feature (Phases 74-77) will build on: three entities, three fail-closed query filters, and one purely additive migration.

Purpose: this phase owns the storage convention. Getting `DateOnly`, the `GroupId` columns, and the fail-closed filters right now is far cheaper than correcting them after occurrence data and votes exist. Two of the three tables ship with no code reading them until Phases 75/76 — that is the deliberate trade recorded in D-02.
Output: three entity classes, three `DbSet`s, three query filters, explicit FK delete behaviour, indexes, and one migration file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-CONTEXT.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-RESEARCH.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
</context>

## Artifacts this phase produces

New symbols created by Phase 74 — none exist before this phase, so exclude them from drift / source-grounding checks:

- **Entities:** `EventEntity` (`Events`), `EventSeriesEntity` (`EventSeries`), `EventSignupEntity` (`EventSignups`); DbSets `QuestBoardContext.Events`, `.EventSeries`, `.EventSignups`; migration class `AddCalendarEventsFeature`
- **Repository:** `EventRepository` with `GetEventsForCalendarAsync`, `GetEventWithDetailsAsync`, `GetSeriesGroupIdAsync`; `EntityProfile` maps `CreateMap<EventEntity, Event>()` / `CreateMap<Event, EventEntity>()`
- **Domain:** `Event`, `IEventRepository`, `IEventService`, `EventService`
- **Service:** `EventViewModel`, `EventsController` (`Details`, `Create` GET/POST, `Edit` GET/POST, `Delete`, private `IsDmTierAsync`), routes `/Events/Details/{id}`, `/Events/Create`, `/Events/Edit/{id}`, `/Events/Delete`; `EventOnDay`; `CalendarViewModel.Events`, `CalendarViewModel.GetEventsForDate(DateTime)`, `CalendarDay.EventsOnDay`; views `Views/Events/Create.cshtml`, `Edit.cshtml`, `Details.cshtml`; `ViewModelProfile` maps `CreateMap<Event, EventViewModel>()` / `CreateMap<EventViewModel, Event>()`
- **CSS:** `.calendar-events`, `.calendar-event`, `.calendar-event-title`, `.calendar-event-time`, `.legend-item.event`, `.agenda-event-entry`, `.agenda-event-title`
- **Tests:** `EventsControllerIntegrationTests`, `EventCalendarPartialTests`, `EventTenantIsolationTests`

<tasks>

<task type="auto">
  <name>Task 1: Add the three event entities and their DbSets</name>
  <files>QuestBoard.Repository/Entities/EventSeriesEntity.cs, QuestBoard.Repository/Entities/EventEntity.cs, QuestBoard.Repository/Entities/EventSignupEntity.cs, QuestBoard.Repository/Entities/QuestBoardContext.cs</files>
  <read_first>
    - `QuestBoard.Repository/Entities/ContactEntity.cs` — full file. This is the exact shape to clone: `[Table]`, `[Key] [DatabaseGenerated(DatabaseGeneratedOption.Identity)] int Id`, `[Required] [StringLength]` on the title-like field, `DateTime CreatedAt = DateTime.UtcNow`, then `int GroupId` with `[ForeignKey(nameof(GroupId))] public virtual GroupEntity Group { get; set; } = null!;`.
    - `QuestBoard.Repository/Entities/PlayerSignupEntity.cs` — the through-navigation shape for a signup-style child entity and its `[ForeignKey(nameof(...))]` nav pairs.
    - `QuestBoard.Repository/Entities/UserGroupEntity.cs` lines 15-25 — the established convention for storing an enum value: an `int` property with a `[Range(...)]` attribute and a comment naming the enum in plain language, NOT a Domain enum type on the entity.
    - `QuestBoard.Repository/Entities/QuestBoardContext.cs` lines 1-50 — the DbSet declaration block and its XML doc-comment style.
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-CONTEXT.md` — D-01, D-02, D-03, D-04, D-05, D-06.
    - `.planning/ROADMAP.md` Phase 75 and Phase 76 entries — their scope notes constrain the column set created here (Phase 75: availability reuses the No/Maybe/Yes vote values, no signup role, no character, no waitlist ordering; Phase 76: base cadence of every N weeks on a weekday, an anchor date, a comma-delimited cycle mask stored as nvarchar(200), and idempotency keyed on series plus slot index).
  </read_first>
  <action>
Create three entity files in `QuestBoard.Repository/Entities/`, namespace `QuestBoard.Repository.Entities`, each implementing `IEntity`.

`EventSeriesEntity.cs`, `[Table("EventSeries")]`:
- `Id` — `[Key] [DatabaseGenerated(DatabaseGeneratedOption.Identity)] int`
- `AnchorDate` — `DateOnly`
- `IntervalWeeks` — `int`
- `WeekDay` — `int`, with `[Range(0, 6)]` and a plain-language comment saying it stores a day-of-week ordinal where 0 is Sunday, matching `System.DayOfWeek`
- `CycleMask` — `string`, `[StringLength(200)]`, defaulted to `string.Empty`
- `CreatedAt` — `DateTime`, defaulted to `DateTime.UtcNow`
- `GroupId` — `int`, plus `[ForeignKey(nameof(GroupId))] public virtual GroupEntity Group { get; set; } = null!;`
- Do NOT add an inverse `ICollection<EventEntity>` navigation — the occurrence collection belongs to the phase that generates occurrences.
- Head the file with a plain-language comment: this table stores the repeating-schedule definition; no code reads or writes it yet, and it exists now so the storage convention and tenant scoping are settled before any occurrence data is created.

`EventEntity.cs`, `[Table("Events")]`:
- `Id` — `[Key] [DatabaseGenerated(DatabaseGeneratedOption.Identity)] int`
- `Title` — `[Required] [StringLength(200)] string`, defaulted to `string.Empty` (200 matches `QuestEntity.Title`)
- `Description` — `string?` with NO `[StringLength]` at all. It is unbounded Markdown, matching `QuestEntity.Description` rather than `ContactEntity.Description`. Add a one-line comment saying the description is Markdown rendered through the shared markdown service and is deliberately unbounded like a quest description.
- `Date` — `DateOnly`
- `StartTime` — `TimeOnly?`, with a comment saying a null start time means the event runs all day
- `SeriesId` — `int?`, plus `[ForeignKey(nameof(SeriesId))] public virtual EventSeriesEntity? Series { get; set; }`
- `SeriesSlotIndex` — `int?`, with a comment saying it identifies which slot of a repeating schedule produced this occurrence and stays null for a one-off event
- `CreatedAt` — `DateTime`, defaulted to `DateTime.UtcNow`
- `GroupId` — `int`, plus `[ForeignKey(nameof(GroupId))] public virtual GroupEntity Group { get; set; } = null!;`
- There is NO author column: no `CreatedByUserId`, no `CreatedByUser`, no `DungeonMasterId`. Add a comment stating in plain language that an event is board-level information rather than one person's item, so any Dungeon Master on the board may edit or delete it, and that the absence of an author column is deliberate.
- Per D-07 the entity carries no category or kind discriminator and no relationship of any sort to a quest. The ten members listed above are the complete set. Add a comment saying an event's meaning comes from the board it lives on rather than from a category field, and that events are informational and are deliberately not linked to quests.

`EventSignupEntity.cs`, `[Table("EventSignups")]`:
- `Id` — `[Key] [DatabaseGenerated(DatabaseGeneratedOption.Identity)] int`
- `EventId` — `[Required] int`, plus `[ForeignKey(nameof(EventId))] public virtual EventEntity Event { get; set; } = null!;`
- `UserId` — `[Required] int`, plus `[ForeignKey(nameof(UserId))] public virtual UserEntity User { get; set; } = null!;`
- `Availability` — `int` with `[Range(0, 2)]` and a comment saying it stores the same three availability values used for quest date votes, where 0 is No, 1 is Maybe and 2 is Yes
- `CreatedAt` — `DateTime`, defaulted to `DateTime.UtcNow`
- `UpdatedAt` — `DateTime?`, with a comment saying a null value means the answer has never been changed since it was created
- Head the file with a plain-language comment: this table carries no GroupId of its own and is tenant-scoped through its required Event navigation; no code reads or writes it yet.

Then edit `QuestBoard.Repository/Entities/QuestBoardContext.cs`: add three `DbSet` properties alongside the existing block (which ends with `Contacts`, `ContactImages`, `ContactNotes`), following the same one-line-XML-doc style already used there:
- `public DbSet<EventEntity> Events { get; set; }`
- `public DbSet<EventSeriesEntity> EventSeries { get; set; }`
- `public DbSet<EventSignupEntity> EventSignups { get; set; }`

Do not add any query filter or relationship configuration in this task — Task 2 owns `OnModelCreating`.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj` exits 0.
    - `grep -c "public DateOnly Date" QuestBoard.Repository/Entities/EventEntity.cs` is 1.
    - `grep -c "public TimeOnly? StartTime" QuestBoard.Repository/Entities/EventEntity.cs` is 1.
    - `grep -c "public int? SeriesId" QuestBoard.Repository/Entities/EventEntity.cs` is 1.
    - `grep -vE '^\s*(//|/\*|\*)' QuestBoard.Repository/Entities/EventEntity.cs | grep -cE "CreatedByUserId|DungeonMasterId"` is 0 — no author column.
    - `grep -vE '^\s*(//|/\*|\*)' QuestBoard.Repository/Entities/EventEntity.cs | grep -c "StringLength" ` is 1 — only `Title` is length-bounded; `Description` is not.
    - `EventEntity.cs` declares exactly 10 `public ` members (per D-07 there is no category discriminator and no quest relationship), and `grep -cE 'QuestId|QuestEntity|EventType' QuestBoard.Repository/Entities/EventEntity.cs` is 0. Note the namespace itself begins with the product name, so a case-insensitive substring search is not a valid check here.
    - `grep -c "DbSet<EventEntity> Events" QuestBoard.Repository/Entities/QuestBoardContext.cs` is 1, and the same for `DbSet<EventSeriesEntity> EventSeries` and `DbSet<EventSignupEntity> EventSignups`.
    - `git diff -- '*.csproj'` is empty — no package was added.
    - No comment or string literal in the three new files contains a requirement identifier, phase number, or plan number (CLAUDE.md rule).
  </acceptance_criteria>
  <done>Three entities compile, `Date`/`StartTime` use the native EF Core date and time CLR types, `EventEntity` has a nullable series FK and no author column, and three DbSets are registered.</done>
</task>

<task type="auto">
  <name>Task 2: Add fail-closed query filters, FK delete behaviour, and indexes</name>
  <files>QuestBoard.Repository/Entities/QuestBoardContext.cs</files>
  <read_first>
    - `QuestBoard.Repository/Entities/QuestBoardContext.cs` lines 48-200 — the relationship configuration block. Note the file-level comment "Configure all foreign key relationships to use NO ACTION (Restrict) to avoid cascade cycles", the `HasOne(...).WithMany().HasForeignKey(...).OnDelete(DeleteBehavior.NoAction)` idiom, the `Cascade` usage for owned children such as `ProposedDates` and `DateVotes`, and the unique composite index `HasIndex(pdv => new { pdv.PlayerSignupId, pdv.ProposedDateId }).IsUnique()`.
    - `QuestBoard.Repository/Entities/QuestBoardContext.cs` lines 268-350 — the global query filter block. Read the whole comment block, especially the warning against capturing `ActiveGroupId` into a local, and the two filter shapes: own-GroupId (`QuestEntity`, `ShopItemEntity`, `CharacterEntity`, `ContactEntity`) and through-navigation (`ProposedDateEntity`, `PlayerSignupEntity`, `PlayerDateVoteEntity`, `ReminderLogEntity`).
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-RESEARCH.md` § Common Pitfalls, Pitfall 3 — why `EventSignupEntity`'s filter must land now even though nothing reads the table.
  </read_first>
  <action>
Edit `QuestBoard.Repository/Entities/QuestBoardContext.cs` — `OnModelCreating` only. Three additions.

(a) Relationship configuration, placed with the other relationship blocks (before the query-filter block):
- `EventEntity` → `Group`: `HasOne(e => e.Group).WithMany().HasForeignKey(e => e.GroupId).OnDelete(DeleteBehavior.NoAction)`
- `EventSeriesEntity` → `Group`: same shape, `DeleteBehavior.NoAction`
- `EventEntity` → `Series`: `HasOne(e => e.Series).WithMany().HasForeignKey(e => e.SeriesId).OnDelete(DeleteBehavior.NoAction).IsRequired(false)`
- `EventSignupEntity` → `Event`: `HasOne(es => es.Event).WithMany().HasForeignKey(es => es.EventId).OnDelete(DeleteBehavior.Cascade)` — a signup is an owned child of its event, so a hard-deleted event takes its signups with it. Add a comment saying so in plain language.
- `EventSignupEntity` → `User`: `HasOne(es => es.User).WithMany().HasForeignKey(es => es.UserId).OnDelete(DeleteBehavior.NoAction)` — a comment should note that NoAction here is what keeps SQL Server from rejecting multiple cascade paths into the signup table.

(b) Indexes, placed alongside the existing `HasIndex` calls:
- `modelBuilder.Entity<EventEntity>().HasIndex(e => new { e.GroupId, e.Date })` — the monthly calendar read is always group-then-date, so this composite is the covering shape. Add a plain-language comment saying that.
- `modelBuilder.Entity<EventSignupEntity>().HasIndex(es => new { es.EventId, es.UserId }).IsUnique()` — one answer per person per event, mirroring the existing unique vote index.

(c) Query filters, appended to the existing global query filter block AFTER the `ContactEntity` filter, using the fail-closed shape verbatim:
- `EventEntity`: `HasQueryFilter(e => activeGroupContext.ActiveGroupId != null && e.GroupId == activeGroupContext.ActiveGroupId)`
- `EventSeriesEntity`: identical shape on its own `GroupId`. Add a comment saying a series cannot be scoped through an event, because the foreign key points from event to series and is nullable, so a series must carry its own group.
- `EventSignupEntity`: through-navigation shape, `HasQueryFilter(es => activeGroupContext.ActiveGroupId != null && es.Event.GroupId == activeGroupContext.ActiveGroupId)`. Add a comment stating that this filter is added now, before any code reads the table, so the scoping convention is settled rather than retrofitted later.

Add a short shared comment above the three new filters noting that these follow the same fail-closed rule as everything above them: with no group selected, every event query returns nothing rather than every board's events merged together, and that `activeGroupContext` must be dereferenced inside the lambda rather than read into a local first.

Do not touch any existing filter, relationship, or index.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 with no EF model-validation warnings about a required relationship to a filtered entity.
    - `grep -c "HasQueryFilter" QuestBoard.Repository/Entities/QuestBoardContext.cs` increased by exactly 3.
    - The three new filter lambdas each contain the substring `activeGroupContext.ActiveGroupId != null &&`.
    - `grep -nE "var .*= *activeGroupContext\.ActiveGroupId" QuestBoard.Repository/Entities/QuestBoardContext.cs` returns no matches — the value is never captured into a local.
    - The `EventSignupEntity` filter reaches group through `es.Event.GroupId`, not through a `GroupId` column on the signup itself.
    - `grep -c "HasIndex(e => new { e.GroupId, e.Date })" QuestBoard.Repository/Entities/QuestBoardContext.cs` is 1.
    - No comment or string literal added contains a requirement identifier, phase number, or plan number.
  </acceptance_criteria>
  <done>All three event tables are fail-closed on tenant scope for reads, delete behaviour is explicit and cycle-free, and the group-plus-date index the calendar read needs exists.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and verify the additive AddCalendarEventsFeature migration</name>
  <files>QuestBoard.Repository/Migrations/</files>
  <read_first>
    - `QuestBoard.Repository/Migrations/20260706193921_AddContactsFeature.cs` — full file. This is the named precedent: ordered `CreateTable` calls with parents before FK-dependent children, foreign keys declared inline in the `constraints` lambda, `CreateIndex` calls last, and a `Down()` that only drops tables in reverse order.
    - `QuestBoard.Repository/Extensions/ServiceExtensions.cs` — `ConfigureDatabase` calls `context.Database.Migrate()` on startup, which is why a bad migration breaks boot.
    - `CLAUDE.md` § Development Commands — the `dotnet ef migrations add` invocation is run from `QuestBoard.Service/` with `--project ../QuestBoard.Repository`, and there is NO separate `database update` step.
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-RESEARCH.md` § Common Pitfalls, Pitfall 4 — the generated `Date` and `StartTime` column types must be inspected, not assumed.
  </read_first>
  <action>
From the `QuestBoard.Service` directory run:

`dotnet ef migrations add AddCalendarEventsFeature --project ../QuestBoard.Repository`

Then open the generated `QuestBoard.Repository/Migrations/<timestamp>_AddCalendarEventsFeature.cs` and verify, correcting only if EF ordered something wrongly:

- `Up()` creates tables in this order: `EventSeries` first (it depends on nothing new), then `Events` (nullable FK to `EventSeries`), then `EventSignups` (required FK to `Events`). If EF emitted a different order, reorder the `CreateTable` calls so a table is never referenced before it exists.
- The `Events.Date` column reads `type: "date"` and the `Events.StartTime` column reads `type: "time"`. The `EventSeries.AnchorDate` column reads `type: "date"`. If any of these read `datetime2`, stop and report — it means the native mapping did not engage and the entity property types are wrong.
- `Events.Title` reads `nvarchar(200)`, `nullable: false`; `Events.Description` reads `nvarchar(max)`, `nullable: true`.
- `EventSeries.CycleMask` reads `nvarchar(200)`.
- `Up()` contains only `CreateTable` and `CreateIndex` calls — no `AddColumn`, `AlterColumn`, `Sql`, `UpdateData`, `InsertData` or `DropColumn` against any pre-existing table.
- `Down()` contains only `DropTable` calls, in reverse dependency order.
- A unique index on `EventSignups (EventId, UserId)` and a non-unique index on `Events (GroupId, Date)` are present.

Do NOT run any `database update` or `dotnet ef database update` command — migrations apply automatically on startup.

Confirm no `.csproj` gained a package reference as a side effect of running the EF tool.

If `dotnet build` or `dotnet ef` fails because output files are locked, the app is running under the Visual Studio debugger — ask the user to stop it (Shift+F5) and retry rather than working around it.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build && dotnet test --filter "FullyQualifiedName~TenantIsolationTests"</automated>
  </verify>
  <acceptance_criteria>
    - A file matching `QuestBoard.Repository/Migrations/*_AddCalendarEventsFeature.cs` exists, together with its `.Designer.cs`, and `QuestBoardContextModelSnapshot.cs` is updated.
    - `grep -c 'CreateTable' <migration file>` is 3 and `grep -c 'DropTable' <migration file>` is 3.
    - `grep -cE 'AddColumn|AlterColumn|UpdateData|InsertData|DropColumn|migrationBuilder\.Sql' <migration file>` is 0 — the migration is purely additive.
    - `grep -c 'type: "date"' <migration file>` is at least 2 (`Events.Date` and `EventSeries.AnchorDate`).
    - `grep -c 'type: "time"' <migration file>` is at least 1 (`Events.StartTime`).
    - In the file text, the line index of `name: "EventSeries"` is lower than that of `name: "Events"`, which is lower than that of `name: "EventSignups"`.
    - `dotnet build` exits 0.
    - `dotnet test --filter "FullyQualifiedName~TenantIsolationTests"` exits 0 — the existing tenant isolation suite still passes, proving the new query filters did not disturb the shared model.
    - `git diff -- '*.csproj'` is empty.
  </acceptance_criteria>
  <done>One additive migration creates all three tables in dependency order with correct SQL Server date and time column types, and the existing suite still passes against the changed EF model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request session → EF model | `IActiveGroupContext.ActiveGroupId` is the only thing standing between one board's rows and another's on every read. |
| application startup → database | `context.Database.Migrate()` runs unattended on boot; a malformed migration takes the whole app down. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-03 | Information Disclosure | `EventEntity` / `EventSeriesEntity` / `EventSignupEntity` reads | high | mitigate | Three fail-closed `HasQueryFilter` entries added in Task 2, each requiring `activeGroupContext.ActiveGroupId != null` before the group comparison, so a session with no selected board reads zero rows rather than every board's rows. Verified by the acceptance criterion that all three lambdas contain the null guard. |
| T-74-10 | Information Disclosure | `HasQueryFilter` closure over `ActiveGroupId` | high | mitigate | Capturing `ActiveGroupId` into a local at model-build time bakes in `null` permanently and would silently disable scoping. Acceptance criterion greps for any local assignment of `activeGroupContext.ActiveGroupId` in the context file and requires zero matches. |
| T-74-11 | Information Disclosure | `EventSignupEntity` with no filter | high | mitigate | The table ships with no reader until a later phase; without a filter now it would be inherited unscoped. Task 2 adds the through-navigation filter immediately, and the acceptance criteria assert it reaches group via `es.Event.GroupId`. |
| T-74-12 | Denial of Service | startup migration | high | mitigate | Migrations auto-apply on boot. Task 3 asserts the migration is purely additive (zero `AddColumn`/`AlterColumn`/`Sql`/`UpdateData` calls) and correctly ordered, so it cannot fail against an existing pre-phase database. |
| T-74-13 | Tampering | EF `DateOnly`/`TimeOnly` mapping | medium | mitigate | Older guidance suggests a third-party mapping package that would conflict with the native EF Core 10 mapping. Prohibition plus a `git diff -- '*.csproj'` empty check blocks it; column-type greps prove native mapping engaged. |
| T-74-SC | Tampering | package installs | low | accept | Zero new NuGet packages this phase; `74-RESEARCH.md` § Package Legitimacy Audit records this explicitly, so no install checkpoint applies. |
</threat_model>

<verification>
1. `dotnet build` exits 0 across the solution.
2. `dotnet test --filter "FullyQualifiedName~TenantIsolationTests"` exits 0.
3. The migration file passes every grep assertion in Task 3.
4. `git diff -- '*.csproj'` is empty.
</verification>

<success_criteria>
- Three entities, three DbSets, three fail-closed query filters, explicit delete behaviour, two new indexes.
- One additive migration, correctly ordered, with SQL Server `date` and `time` columns.
- Existing tests unaffected.
</success_criteria>

<output>
Create `.planning/phases/74-event-schema-crud-and-calendar-display/74-02-SUMMARY.md` when done
</output>
