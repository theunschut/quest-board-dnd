---
phase: 74-event-schema-crud-and-calendar-display
plan: 06
type: execute
wave: 4
depends_on: ["74-04"]
files_modified:
  - QuestBoard.Service/ViewModels/CalendarViewModels/EventOnDay.cs
  - QuestBoard.Service/ViewModels/CalendarViewModels/CalendarDay.cs
  - QuestBoard.Service/ViewModels/CalendarViewModels/CalendarViewModel.cs
  - QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs
  - QuestBoard.Service/Views/Shared/_Calendar.cshtml
  - QuestBoard.Service/wwwroot/css/calendar.css
  - QuestBoard.Service/Views/Calendar/Index.cshtml
autonomous: false
requirements: [EVENT-03]

must_haves:
  truths:
    - "An event on a board appears in that day's cell on the desktop calendar page (EVENT-03)."
    - "Events render in their own block immediately above the quest list, never interleaved into it, so position alone distinguishes them before colour does (D-08)."
    - "The event chip carries a third, unclaimed accent colour and its own icon, so an event is distinguishable from a quest without relying on colour perception (EVENT-03)."
    - "The whole event chip is a link to the event details view (D-10)."
    - "The calendar grid row can grow, so a day holding one event and three quests renders without clipping (D-08 accepted cost, RESEARCH Pitfall 1)."
    - "The Legend card gains an Event swatch row and its hint sentence covers events as well as quests (D-12)."
    - "The DateOnly-to-DateTime comparison happens in exactly one named place in the view model (D-01)."
  artifacts:
    - QuestBoard.Service/ViewModels/CalendarViewModels/EventOnDay.cs
  key_links:
    - "CalendarViewModel.Events defaults to an empty list, so the five quest-detail call sites of the shared partial render nothing with no flag and no branch (D-09)."
    - "CalendarController.Index fetches events alongside quests from IEventService."
  prohibitions:
    - statement: "The five _Calendar.cshtml call sites in Views/Quest/Details.cshtml and Views/Quest/Details.Mobile.cshtml must render zero event markup, and neither file may be edited."
      status: required
      verification: "`git status --porcelain QuestBoard.Service/Views/Quest/` shows no modification, and `dotnet test --filter \"FullyQualifiedName~EventCalendarPartialTests\"` exits 0."
    - statement: "No duplicate calendar partial may be created — the one shared partial is extended in place."
      status: required
      verification: "`ls QuestBoard.Service/Views/Shared/_Calendar*.cshtml` lists exactly one file."
    - statement: "The events block must not be gated on ViewBag.IsDetailsPage or any other flag."
      status: required
      verification: "The new block's only condition is whether the day's event collection has any items."
    - statement: "No Dungeon Master control may be added to the event chip."
      status: required
      verification: "The chip contains one anchor to the details view and no form, no button and no write action."
---

<objective>
Render events on the desktop calendar page: a per-day events block above the quest list, its CSS, a grid row that can grow, and a Legend row that explains the new chip.

Purpose: `_Calendar.cshtml` has six call sites and only one is in scope. The protection is structural rather than flag-based — the events collection defaults to empty, so a call site that never populates it renders nothing, and a future seventh call site inherits that safe default automatically. That structural claim is verified by a test, not by code review.
Output: one new view model, three edited view-model files, one edited controller, the shared partial, the stylesheet, and the Legend card.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-CONTEXT.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-UI-SPEC.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-PATTERNS.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-04-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 74 — none exist before this phase, so exclude them from drift / source-grounding checks:

- **Entities:** `EventEntity` (`Events`), `EventSeriesEntity` (`EventSeries`), `EventSignupEntity` (`EventSignups`); DbSets `QuestBoardContext.Events`, `.EventSeries`, `.EventSignups`; migration `AddCalendarEventsFeature`
- **Repository:** `EventRepository` with `GetEventsForCalendarAsync`, `GetEventWithDetailsAsync`, `GetSeriesGroupIdAsync`; `EntityProfile` maps `CreateMap<EventEntity, Event>()` / `CreateMap<Event, EventEntity>()`
- **Domain:** `Event`, `IEventRepository`, `IEventService`, `EventService`
- **Service:** `EventViewModel`, `EventsController` (`Details`, `Create` GET/POST, `Edit` GET/POST, `Delete`), routes `/Events/Details/{id}`, `/Events/Create`, `/Events/Edit/{id}`, `/Events/Delete`; `EventOnDay` (`Event`, `IsAllDay`, `TimeLabel`); `CalendarViewModel.Events`, `CalendarViewModel.GetEventsForDate(DateTime)`, `CalendarDay.EventsOnDay`; views `Views/Events/Create.cshtml`, `Edit.cshtml`, `Details.cshtml`; `ViewModelProfile` maps `CreateMap<Event, EventViewModel>()` / `CreateMap<EventViewModel, Event>()`
- **CSS:** `.calendar-events`, `.calendar-event`, `.calendar-event-title`, `.calendar-event-time`, `.legend-item.event`, `.agenda-event-entry`, `.agenda-event-title`
- **Tests:** `EventsControllerIntegrationTests`, `EventCalendarPartialTests`, `EventTenantIsolationTests`

<tasks>

<task type="auto">
  <name>Task 1: Add EventOnDay, wire the events collection through the calendar view model and controller</name>
  <files>QuestBoard.Service/ViewModels/CalendarViewModels/EventOnDay.cs, QuestBoard.Service/ViewModels/CalendarViewModels/CalendarDay.cs, QuestBoard.Service/ViewModels/CalendarViewModels/CalendarViewModel.cs, QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs</files>
  <read_first>
    - `QuestBoard.Service/ViewModels/CalendarViewModels/QuestOnDay.cs` — the per-day wrapper template.
    - `QuestBoard.Service/ViewModels/CalendarViewModels/CalendarDay.cs` — the four existing members and the `List<QuestOnDay> QuestsOnDay { get; set; } = [];` default-empty idiom.
    - `QuestBoard.Service/ViewModels/CalendarViewModels/CalendarViewModel.cs` — full file. Note `Quests` defaults to an empty list, `GetCalendarDays()` builds a single Monday-first day list padded with `IsEmpty` days at both ends, and `GetQuestsForDate(DateTime date)` is the private per-day resolver whose shape the event resolver mirrors.
    - `QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs` — full file (42 lines); the single-service constructor and the `Quests = [.. allQuests]` assignment.
    - `QuestBoard.Domain/Interfaces/IEventService.cs` (from plan 74-03) — `GetEventsForCalendarAsync`.
    - `QuestBoard.Service/ViewModels/EventViewModels/EventViewModel.cs` (from plan 74-04) — how the all-day wording is already expressed, so the same wording is not re-invented here with different text.
  </read_first>
  <action>
Create `QuestBoard.Service/ViewModels/CalendarViewModels/EventOnDay.cs`, namespace `QuestBoard.Service.ViewModels.CalendarViewModels`, `public class EventOnDay` with:
- `public Event Event { get; set; } = null!;`
- `public bool IsAllDay => Event.StartTime == null;`
- `public string TimeLabel => Event.StartTime.HasValue ? Event.StartTime.Value.ToString("HH:mm") : "All day";` with a comment saying an event with no start time is worded rather than left blank, because an empty time slot is indistinguishable from a rendering failure, and that both the desktop chip and the mobile agenda entry read this one property so the two platforms cannot drift.
There is no finalized-or-proposed distinction for an event, so the class carries no status flag.

Edit `CalendarDay.cs` — add `public List<EventOnDay> EventsOnDay { get; set; } = [];` alongside `QuestsOnDay`, with a comment saying the default-empty list is what keeps every caller that does not populate events rendering nothing at all.

Edit `CalendarViewModel.cs`:
- Add `public List<Event> Events { get; set; } = new();` directly under `Quests`, with a comment in plain language: this defaults to empty on purpose. The shared calendar partial is rendered from several places that build their own instance of this model and never set this collection; those places therefore render no events with no flag to set and nothing to remember, and any future caller inherits the same safe default.
- Inside `GetCalendarDays()`, in the real-days loop, compute `var eventsOnDay = GetEventsForDate(date);` and assign `EventsOnDay = eventsOnDay` on the constructed `CalendarDay`. Do not touch the padding logic or the quest resolution.
- Add `private List<EventOnDay> GetEventsForDate(DateTime date)` which converts once — `var dateOnly = DateOnly.FromDateTime(date);` — then filters `Events` on `e.Date == dateOnly`, orders by `e.StartTime ?? TimeOnly.MinValue` so timed events precede all-day ones consistently, and projects to `EventOnDay`. Put a comment directly above the conversion line stating that this is the single place the stored date type meets the calendar grid's date-and-time type, and that no other file may convert between them.

Edit `CalendarController.cs`:
- Add `IEventService eventService` as a second primary-constructor parameter.
- After the existing quest fetch, add `var allEvents = await eventService.GetEventsForCalendarAsync(token);` and set `Events = [.. allEvents]` on the constructed `CalendarViewModel`, matching the existing `Quests = [.. allQuests]` style.
- Leave the year and month validation, the defaults, and the returned view untouched.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build && dotnet test --filter "FullyQualifiedName~CalendarControllerIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - `dotnet test --filter "FullyQualifiedName~CalendarControllerIntegrationTests"` exits 0 — the existing calendar suite still passes with the second data source wired in.
    - `grep -c "DateOnly.FromDateTime" QuestBoard.Service/ViewModels/CalendarViewModels/CalendarViewModel.cs` is 1, and `grep -rn "DateOnly.FromDateTime\|ToDateTime(" QuestBoard.Service/Views/ QuestBoard.Service/Controllers/` returns no matches — the conversion lives in exactly one place.
    - `grep -c "public List<Event> Events" QuestBoard.Service/ViewModels/CalendarViewModels/CalendarViewModel.cs` is 1 and the declaration ends with an empty-list initializer.
    - `grep -c "public List<EventOnDay> EventsOnDay" QuestBoard.Service/ViewModels/CalendarViewModels/CalendarDay.cs` is 1 and the declaration ends with `= [];`.
    - `grep -c "GetEventsForCalendarAsync" QuestBoard.Service/Controllers/QuestBoard/CalendarController.cs` is 1.
    - `grep -c "All day" QuestBoard.Service/ViewModels/CalendarViewModels/EventOnDay.cs` is 1.
    - No comment or string literal contains a requirement identifier, phase number, or plan number.
  </acceptance_criteria>
  <done>The calendar page fetches events alongside quests, each day carries its own event list, every other caller of the model gets an empty list by default, and the stored date type meets the grid's date type in exactly one named place.</done>
</task>

<task type="auto">
  <name>Task 2: Render the events block in the shared partial and add its styles</name>
  <files>QuestBoard.Service/Views/Shared/_Calendar.cshtml, QuestBoard.Service/wwwroot/css/calendar.css</files>
  <read_first>
    - `QuestBoard.Service/Views/Shared/_Calendar.cshtml` lines 1-80 — the day cell: `<div class="day-number">`, then `@if (day.QuestsOnDay.Any())` wrapping `<div class="quest-events">` with `.Take(3)`, each chip an `<a ... class="quest-link">` containing a title div, a DM div and a time div, followed by status icons and vote controls outside the link.
    - `QuestBoard.Service/wwwroot/css/calendar.css` lines 34-185 — `.calendar-body` (`grid-auto-rows: 120px`), `.calendar-day` (`overflow: hidden`, flex column, `padding: 8px`), `.quest-events` (`gap: 1px`), `.quest-event` (`padding: 2px 4px`, `border-left: 3px solid`, `min-height: 24px`), `.quest-link`, `.calendar-quest-title` (10px/600), `.quest-time` (8px, `display: none`) and the `:hover` rules that reveal the time and grow the chip to 48px.
    - `QuestBoard.Service/wwwroot/css/calendar.css` lines 400-420 — the details-page overrides already use a growable row height; the base rule must get the same treatment, not only the override.
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-UI-SPEC.md` § Spacing Scale (the day-cell exception — 2px 4px padding and 1px gap, NOT the 8-point scale), § Typography System B (10px title, 8px secondary), § Color (the event accent and why colour alone is insufficient), and § Component Contract "Desktop calendar day-cell".
  </read_first>
  <action>
Edit `QuestBoard.Service/Views/Shared/_Calendar.cshtml`. Inside the non-empty day cell, immediately after the `<div class="day-number">@day.Day</div>` line and BEFORE the existing `@if (day.QuestsOnDay.Any())` block, insert a sibling block:

- Condition: `@if (day.EventsOnDay.Any())` — nothing else. No `isDetailsPage` check, no `currentQuestId` check, no other flag.
- Container: `<div class="calendar-events">`.
- Loop `day.EventsOnDay.Take(3)` — the same per-day cap the quest list already uses, so cell height stays predictable.
- Each chip: `<div class="calendar-event" title="@eventOnDay.Event.Title - @eventOnDay.TimeLabel">` containing a single `<a href="@Url.Action("Details", "Events", new { id = eventOnDay.Event.Id })" class="quest-link">` wrapping `<div class="calendar-event-title"><i class="fas fa-calendar-day me-1"></i>@eventOnDay.Event.Title</div>` and `<div class="calendar-event-time">@eventOnDay.TimeLabel</div>`.
- The whole chip is the click target via the anchor; add no button, no form and no Dungeon Master control inside it.
- The tooltip carries only the title and the time label. Do not put the description in a tooltip — a tooltip is plain text and the description is Markdown.

Add a comment above the new block, in plain language: this block renders only when the day actually has events, and the collection is empty by default, so the callers of this partial that build their own model and never populate events render nothing here without any flag to set.

Edit `QuestBoard.Service/wwwroot/css/calendar.css`:

(a) Change the base `.calendar-body` rule's `grid-auto-rows` from the fixed `120px` to `minmax(120px, auto)`, with a comment saying the row must be able to grow because a day cell can now stack an events block above the quest list, and the cell clips its overflow. Do not change the two existing details-page overrides, which already use the growable form.

(b) Add a new block after the existing quest chip rules, headed by a comment naming it as the board-event chip system that mirrors the quest chip:
- `.calendar-events` — `display: flex; flex-direction: column; gap: 1px; overflow: hidden; margin-bottom: 1px;`
- `.calendar-event` — mirror `.quest-event` exactly except: `border-left: 3px solid #6f42c1;` declared inline in the rule rather than through a status modifier class, and no `padding-right: 30px` reservation (an event chip has no status icon overlay). Keep `background-color: rgba(255, 255, 255, 0.9); border-radius: 3px; padding: 2px 4px; font-size: 9px; line-height: 1.2; min-height: 24px;` plus the same `display: flex; align-items: flex-start; flex-shrink: 0; width: 100%; box-sizing: border-box; cursor: pointer; transition: all 0.2s ease;`.
- `.calendar-event:hover` — mirror `.quest-event:hover`: brighter background, the same box shadow, `z-index: 10`, and `min-height: 48px`.
- `.calendar-event-title` — mirror `.calendar-quest-title` exactly: `font-weight: 600; font-size: 10px;` with the same nowrap/ellipsis/width rules.
- `.calendar-event-time` — mirror `.quest-time`: `font-size: 8px; color: #666; display: none; flex-shrink: 0;` plus `font-style: italic;`.
- `.calendar-event:hover .calendar-event-time { display: block !important; }` — mirroring the existing hover rule that reveals the quest time.
- `.legend-item.event { border-left-color: #6f42c1; }` placed with the other `.legend-item` modifiers.

Use the accent value `#6f42c1` and no other new colour. Do not introduce any font size other than 10px and 8px inside the day cell, and do not apply 8px or 16px spacing inside it — the cell is roughly 112px tall and larger values will not fit.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build && dotnet test --filter "FullyQualifiedName~EventCalendarPartialTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - `dotnet test --filter "FullyQualifiedName~EventCalendarPartialTests"` exits 0 — a quest detail page with a same-day event on the same board still renders no event markup, and quest creation is still unaffected.
    - `grep -c "grid-auto-rows: minmax(120px, auto)" QuestBoard.Service/wwwroot/css/calendar.css` is 3 (the base rule plus the two pre-existing details-page overrides), and `grep -c "grid-auto-rows: 120px" QuestBoard.Service/wwwroot/css/calendar.css` is 0.
    - `grep -c "^\.calendar-events {" QuestBoard.Service/wwwroot/css/calendar.css` is 1 and `grep -c "^\.calendar-event {" QuestBoard.Service/wwwroot/css/calendar.css` is 1.
    - `grep -c "#6f42c1" QuestBoard.Service/wwwroot/css/calendar.css` is 2 (the chip border and the legend swatch).
    - `grep -c "calendar-events" QuestBoard.Service/Views/Shared/_Calendar.cshtml` is 1 and `grep -c "EventsOnDay" QuestBoard.Service/Views/Shared/_Calendar.cshtml` is 2 (the condition and the loop).
    - In `_Calendar.cshtml`, the line number of `class="calendar-events"` is greater than the line number of `class="day-number"` and less than the line number of `class="quest-events"` — events render above the quest list.
    - `grep -nE 'IsDetailsPage|currentQuestId' QuestBoard.Service/Views/Shared/_Calendar.cshtml` shows no occurrence between the `calendar-events` container line and the `quest-events` container line — the new block is gated only on whether the day has events.
    - `ls QuestBoard.Service/Views/Shared/_Calendar*.cshtml` lists exactly one file.
    - `git status --porcelain QuestBoard.Service/Views/Quest/` shows no modification.
    - No comment or string literal contains a requirement identifier, phase number, or plan number.
  </acceptance_criteria>
  <done>Events render as their own chip block above the quest list, in an unclaimed accent colour with their own icon, each chip linking to the event details view, and the grid row can grow so nothing is clipped.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Legend event row and update its hint</name>
  <files>QuestBoard.Service/Views/Calendar/Index.cshtml</files>
  <read_first>
    - `QuestBoard.Service/Views/Calendar/Index.cshtml` — full file. The Legend card hardcodes three swatch/label rows (Proposed, Finalized, No building key) and a hint line at the bottom inside `<small class="text-muted">` prefixed with a lightbulb icon.
    - `QuestBoard.Service/wwwroot/css/calendar.css` § Legend — `.legend-item` is a 20x12 box with a 3px left border and the modifier classes carry the colour.
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-UI-SPEC.md` § Component Contract "Legend card" and § Copywriting Contract — the row label and the exact replacement hint sentence.
  </read_first>
  <action>
Edit `QuestBoard.Service/Views/Calendar/Index.cshtml`, Legend card only:

(a) Insert a fourth swatch row between the Finalized row and the No building key row, matching the existing rows' markup exactly:
```
<div class="d-flex align-items-center mb-2">
    <div class="legend-item event me-2"></div>
    <small>Event</small>
</div>
```

(b) Replace the hint sentence at the bottom of the Legend card. The current wording mentions only quests; change it so it names both quests and events, since event chips are now clickable too. The replacement text is exactly: `Click quests or events for details`. Leave the surrounding `<div class="mt-2">`, `<small class="text-muted">` and lightbulb icon untouched.

Change nothing else in this file. In particular, do not alter the partial call, the navigation, or the card shell.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build && dotnet test --filter "FullyQualifiedName~CalendarControllerIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 and `dotnet test --filter "FullyQualifiedName~CalendarControllerIntegrationTests"` exits 0.
    - `grep -c "legend-item event" QuestBoard.Service/Views/Calendar/Index.cshtml` is 1.
    - `grep -c "Click quests or events for details" QuestBoard.Service/Views/Calendar/Index.cshtml` is 1.
    - The file contains exactly four `legend-item`-or-icon legend rows inside the Legend card body, in the order Proposed, Finalized, Event, No building key.
    - `git diff --stat QuestBoard.Service/Views/Calendar/Index.cshtml` shows at most 5 changed lines.
    - No comment or string literal contains a requirement identifier, phase number, or plan number.
  </acceptance_criteria>
  <done>The Legend explains the event chip's colour and its hint no longer claims only quests are clickable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human check — desktop day cell does not clip and the event chip is distinguishable</name>
  <files>QuestBoard.Service/wwwroot/css/calendar.css, QuestBoard.Service/Views/Shared/_Calendar.cshtml, QuestBoard.Service/Views/Calendar/Index.cshtml</files>
  <read_first>
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-VALIDATION.md` § Manual-Only Verifications — both rows in that table are exactly what this checkpoint covers: perceptual distinctness and silent CSS clipping, neither of which any DOM assertion can catch.
  </read_first>
  <action>Pause and hand the running application to the developer for the visual checks below. Do not modify any file during this task; if the developer reports clipping, mis-ordering, or indistinguishable chips, capture the report and fix it by revisiting Task 2's stylesheet changes before resuming.</action>
  <acceptance_criteria>
    - The developer confirms a day cell holding one event and three quests renders uncropped at both 1280px and 1920px window widths.
    - The developer confirms the event chip sits above the quest chips and is distinguishable from them without reading the text.
    - The developer confirms clicking the chip opens the event details view, the Legend shows four rows with the updated hint, and a no-start-time event's chip shows the all-day wording on hover rather than a blank line.
  </acceptance_criteria>
  <verify>
    <human-check>Developer confirms, in a real browser at 1280px and 1920px, that a day cell holding one event and three quests is uncropped, the event chip sits above the quest chips and is distinguishable without reading the text, and the chip opens the event details view.</human-check>
  </verify>
  <done>The developer has typed "approved", or the reported visual defects have been fixed and re-approved.</done>
  <what-built>Events now render on the desktop calendar page as a purple-accented chip block above the quest list in each day cell, the grid row grows instead of clipping, each chip links to the event details view, and the Legend has an Event row with an updated hint.</what-built>
  <how-to-verify>
    Two things here cannot be proved by a DOM assertion — perceptual distinctness and silent CSS clipping — so both need eyes on a real browser.

    1. Run the app (`dotnet run --project QuestBoard.Service`) and sign in as a Dungeon Master.
    2. Create a quest with a proposed date on some day this month, and create a second quest and a third on the SAME day, so that day holds three quest chips.
    3. From the Dungeon Master navbar, choose Create Event and create an event on that same day, with a start time.
    4. Open /Calendar for that month at a 1280px-wide browser window.
    5. Confirm: the event chip sits ABOVE the three quest chips in the day cell, and nothing in the cell is cut off at the bottom — the row grew to fit. Repeat at 1920px.
    6. Confirm the event is distinguishable from the quests without reading the text: it has a different left-border colour and a calendar icon the quest chips do not have.
    7. Click the event chip and confirm it opens the event details view.
    8. Check the Legend card: it now has four rows and the hint reads "Click quests or events for details".
    9. Create a second event on the same day with NO start time and confirm hovering its chip shows the wording "All day" rather than a blank line.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is clipped, mis-ordered, or indistinguishable</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller-populated model → shared partial | The partial is rendered from six call sites; five of them build their own model instance. Whatever the partial reads must be safe when unset. |
| stored event title → rendered HTML and tooltip | Titles are author-supplied and are rendered into a day cell and into a `title` attribute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-03 | Information Disclosure | `CalendarController.Index` events fetch | high | mitigate | `GetEventsForCalendarAsync` reads through the fail-closed entity query filter, so the calendar page can only ever show the active board's events. Proven by the two-group test in plan 74-08, which asserts both absence for the other board and presence for its own. |
| T-74-18 | Information Disclosure | events leaking onto quest detail pages | high | mitigate | The events collection defaults to empty and the render block's only condition is whether the day has events. The five quest-detail call sites never populate it, so they render nothing. This is verified by `EventCalendarPartialTests` on both the desktop and the mobile quest detail page, not by code review. |
| T-74-04 | Tampering | event title in the chip and its `title` attribute | medium | mitigate | Razor HTML-encodes `@eventOnDay.Event.Title` in both the element body and the attribute; no `Html.Raw` is used and the Markdown description is deliberately kept out of the tooltip, so no unsanitised author text reaches the day cell. |
| T-74-17 | Elevation of Privilege | write controls inside the shared partial | high | mitigate | The chip contains one anchor and nothing else — no form, no button, no Dungeon Master action. Edit and Delete remain confined to the event details view. |
| T-74-SC | Tampering | package installs | low | accept | Zero new NuGet packages and zero new front-end dependencies; the chip uses Bootstrap and Font Awesome already loaded by both layouts. |
</threat_model>

<verification>
1. `dotnet build` exits 0.
2. `dotnet test --filter "FullyQualifiedName~EventCalendarPartialTests"` exits 0.
3. `dotnet test --filter "FullyQualifiedName~CalendarControllerIntegrationTests"` exits 0.
4. `git status --porcelain QuestBoard.Service/Views/Quest/` shows no modification.
5. Human checkpoint approved: no clipping at 1280px and 1920px, event chip above quests, visually distinguishable, chip click reaches the details view.
</verification>

<success_criteria>
- Events appear in the correct day cell on the desktop calendar, above the quest list, in their own accent colour with their own icon.
- The grid row grows; a day with one event and three quests renders uncropped.
- The Legend has an Event row and an updated hint.
- Quest detail pages render zero event markup, proven by test.
</success_criteria>

<output>
Create `.planning/phases/74-event-schema-crud-and-calendar-display/74-06-SUMMARY.md` when done
</output>
