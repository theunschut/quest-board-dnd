---
phase: 74-event-schema-crud-and-calendar-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.IntegrationTests/Controllers/EventsControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/EventCalendarPartialTests.cs
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-05]

must_haves:
  truths:
    - "A route-based integration test file exists that exercises /Events/Create, /Events/Edit/{id}, /Events/Details/{id} and /Events/Delete and fails RED until the controller lands (EVENT-01, EVENT-02)."
    - "A test asserts that a Quest Details page rendered on a board that has a same-day event contains zero event chip markup (D-09)."
    - "A test asserts quest creation succeeds and persists unchanged when an event already exists on the chosen date (EVENT-05, D-23)."
    - "The whole solution still compiles: `dotnet build` exits 0 even though the new tests fail at runtime."
  artifacts:
    - QuestBoard.IntegrationTests/Controllers/EventsControllerIntegrationTests.cs
    - QuestBoard.IntegrationTests/Controllers/EventCalendarPartialTests.cs
  key_links:
    - "Both new files reference Events routes as string literals only — never as C# symbols — so the test project compiles before EventsController exists."
    - "Both files use WebApplicationFactoryBase + AuthenticationHelper + TestDataHelper, the established harness."
  prohibitions:
    - statement: "Neither test file may reference EventEntity, Event, EventViewModel, EventsController or any other Phase 74 C# symbol."
      status: required
      verification: "`dotnet build` exits 0 with this plan's files present and no Event* production type yet created."
    - statement: "Neither test file may edit, or depend on edits to, Views/Quest/Details.cshtml or Views/Quest/Details.Mobile.cshtml."
      status: required
      verification: "`git diff --name-only` for this plan lists only the two new test files."
---

<objective>
Create the Wave 0 RED test scaffold for Phase 74 so every later plan has a sub-60-second automated feedback signal, and so the two hardest-to-retrofit acceptance criteria — D-09 (Quest Details renders zero event markup) and EVENT-05 (quest creation provably unaffected) — exist as first-class tests before any implementation makes them convenient to skip.

Purpose: `74-VALIDATION.md` lists four Wave 0 gaps. This plan closes the three that can be written with route literals only, so the test project keeps compiling while `Event*` production types do not yet exist. The two-group tenant isolation test (D-22) needs `EventEntity` as a compile-time symbol and therefore lands in plan 74-08.
Output: Two new integration test files, both RED, both compiling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-CONTEXT.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-VALIDATION.md
@.planning/phases/74-event-schema-crud-and-calendar-display/74-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

## Artifacts this phase produces

New symbols created by Phase 74 — none of these exist before this phase, so exclude them from drift / source-grounding checks:

**Repository layer**
- `QuestBoard.Repository/Entities/EventEntity.cs` — class `EventEntity` (table `Events`): `Id`, `Title`, `Description`, `Date`, `StartTime`, `SeriesId`, `Series`, `SeriesSlotIndex`, `CreatedAt`, `GroupId`, `Group`
- `QuestBoard.Repository/Entities/EventSeriesEntity.cs` — class `EventSeriesEntity` (table `EventSeries`): `Id`, `AnchorDate`, `IntervalWeeks`, `WeekDay`, `CycleMask`, `CreatedAt`, `GroupId`, `Group`
- `QuestBoard.Repository/Entities/EventSignupEntity.cs` — class `EventSignupEntity` (table `EventSignups`): `Id`, `EventId`, `Event`, `UserId`, `User`, `Availability`, `CreatedAt`, `UpdatedAt`
- `QuestBoardContext.Events`, `QuestBoardContext.EventSeries`, `QuestBoardContext.EventSignups` (DbSet properties)
- `QuestBoard.Repository/Migrations/<timestamp>_AddCalendarEventsFeature.cs` — migration class `AddCalendarEventsFeature`
- `QuestBoard.Repository/EventRepository.cs` — class `EventRepository`: `GetEventsForCalendarAsync`, `GetEventWithDetailsAsync`, `GetSeriesGroupIdAsync`
- `EntityProfile` entries `CreateMap<EventEntity, Event>()`, `CreateMap<Event, EventEntity>()`

**Domain layer**
- `QuestBoard.Domain/Models/Event.cs` — class `Event`
- `QuestBoard.Domain/Interfaces/IEventRepository.cs` — interface `IEventRepository`
- `QuestBoard.Domain/Interfaces/IEventService.cs` — interface `IEventService`
- `QuestBoard.Domain/Services/EventService.cs` — class `EventService`

**Service layer**
- `QuestBoard.Service/ViewModels/EventViewModels/EventViewModel.cs` — class `EventViewModel`: `Id`, `Title`, `Description`, `Date`, `StartTime`, `CanManage`, `TimeLabel`
- `QuestBoard.Service/Controllers/Events/EventsController.cs` — class `EventsController`: `Details(int)`, `Create()`, `Create(EventViewModel)`, `Edit(int)`, `Edit(int, EventViewModel)`, `Delete(int)`, private `IsDmTierAsync()`
- Routes `/Events/Details/{id}`, `/Events/Create`, `/Events/Edit/{id}`, `/Events/Delete`
- `QuestBoard.Service/ViewModels/CalendarViewModels/EventOnDay.cs` — class `EventOnDay`: `Event`, `IsAllDay`, `TimeLabel`
- `CalendarViewModel.Events`, `CalendarViewModel.GetEventsForDate(DateTime)`, `CalendarDay.EventsOnDay`
- `QuestBoard.Service/Views/Events/Create.cshtml`, `Edit.cshtml`, `Details.cshtml`
- `ViewModelProfile` entries `CreateMap<Event, EventViewModel>()`, `CreateMap<EventViewModel, Event>()`

**CSS classes**
- `calendar.css`: `.calendar-events`, `.calendar-event`, `.calendar-event-title`, `.calendar-event-time`, `.legend-item.event`
- `calendar.mobile.css`: `.agenda-event-entry`, `.agenda-event-title`

**Tests**
- `QuestBoard.IntegrationTests/Controllers/EventsControllerIntegrationTests.cs`
- `QuestBoard.IntegrationTests/Controllers/EventCalendarPartialTests.cs`
- `QuestBoard.IntegrationTests/Tests/EventTenantIsolationTests.cs`

<tasks>

<task type="auto">
  <name>Task 1: Create the Events CRUD route-based RED scaffold</name>
  <files>QuestBoard.IntegrationTests/Controllers/EventsControllerIntegrationTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs` — lines 1-200. This is the exact precedent for a Wave 0 RED scaffold that targets route literals rather than C# symbols. Copy its file-header comment style, its `IClassFixture<WebApplicationFactoryBase>` primary-constructor shape, its `TestDataHelper.ClearDatabaseAsync` arrange step, and its `AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(..., roles: ["DungeonMaster"])` / `roles: ["Player"]` usage.
    - `QuestBoard.IntegrationTests/GlobalUsings.cs` — `FluentAssertions`, `Microsoft.EntityFrameworkCore`, `Microsoft.Extensions.DependencyInjection` and `QuestBoard.Repository.Entities` are already global; do not re-import them.
    - `QuestBoard.IntegrationTests/WebApplicationFactoryBase.cs` — note `TestAntiforgeryDecorator`: POSTs in this suite do NOT need a `__RequestVerificationToken` field.
    - `QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs` — signature of `CreateAuthenticatedClientWithUserAsync(factory, userName, email, password, name, roles)`.
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-CONTEXT.md` — D-11 (any DM on the board may edit/delete), D-19 (past dates allowed), D-20 (redirect to the calendar at the event's month).
  </read_first>
  <action>
Create `QuestBoard.IntegrationTests/Controllers/EventsControllerIntegrationTests.cs` in namespace `QuestBoard.IntegrationTests.Controllers`, class `EventsControllerIntegrationTests(WebApplicationFactoryBase factory) : IClassFixture<WebApplicationFactoryBase>`.

Open the file with a header comment, in plain language, stating that this is an intentionally-failing scaffold that targets the Events routes as string literals so the project compiles before the controller exists, and that every fact here is expected to return 404 until the controller lands. Do not name any planning identifier in that comment.

Reference the endpoints only as string literals: `"/Events/Create"`, `"/Events/Edit/{id}"` built with interpolation, `"/Events/Details/{id}"`, `"/Events/Delete"`. Do not `using` or reference `EventsController`, `EventViewModel`, `Event` or `EventEntity` anywhere — the project must still compile.

Write these facts:

1. `Create_Get_PlayerAccess_ShouldBeBlocked` — a client with `roles: ["Player"]` GETs `/Events/Create`; assert status is one of `Forbidden`, `Redirect`, `Unauthorized` (the cookie scheme redirects to `/Account/AccessDenied`, which is why the assertion is a set).
2. `Create_Get_DungeonMasterAccess_ShouldSucceed` — a client with `roles: ["DungeonMaster"]` GETs `/Events/Create`; assert `HttpStatusCode.OK`.
3. `Create_Post_ValidEvent_PersistsAndRedirectsToEventMonth` — a DM POSTs `FormUrlEncodedContent` with `Title = "Wave Zero Feast"`, `Date = "2026-01-17"`, `StartTime = "19:30"`, `Description = "**Bring dice**"` to `/Events/Create`. Assert the response status is `Redirect` and the `Location` header contains both `year=2026` and `month=1` (D-20 — the event's month, not the current month).
4. `Create_Post_WithoutTitle_ReturnsFormWithValidationError` — a DM POSTs `Date = "2026-01-17"` with no `Title`; assert `HttpStatusCode.OK` (the form is redisplayed, not a redirect).
5. `Create_Post_PastDate_IsAccepted` — a DM POSTs `Title = "Backfilled Session"`, `Date` set to a date 45 days in the past via `DateTime.Today.AddDays(-45).ToString("yyyy-MM-dd")`; assert `Redirect`, encoding D-19's "an event is a record, not a booking".
6. `Create_Post_WithoutStartTime_IsAccepted` — a DM POSTs `Title = "All Day Moot"` and a `Date` with no `StartTime`; assert `Redirect`.
7. `Details_Get_BoardMember_CanRead` — a DM creates an event via `/Events/Create`, then a second authenticated client with `roles: ["Player"]` GETs `/Events/Details/1`; assert `HttpStatusCode.OK` and that the body contains the event title. (Every board member can read an event — D-10.)
8. `Edit_Post_ByAnyDmOnBoard_Succeeds` — DM A creates an event; a *different* DM account (`roles: ["DungeonMaster"]`) POSTs to `/Events/Edit/1` with a changed `Title = "Renamed By Other Dm"`; assert `Redirect`. This encodes D-11/D-05 — there is no author column, so any DM on the board may edit any event.
9. `Delete_Post_PlayerAccess_ShouldBeBlocked` — a Player POSTs `id=1` to `/Events/Delete`; assert one of `Forbidden`, `Redirect`, `Unauthorized`.
10. `Delete_Post_ByDm_RedirectsToEventMonth` — a DM creates an event dated `2026-03-09`, then POSTs `id=1` to `/Events/Delete`; assert `Redirect` and that the `Location` header contains `year=2026` and `month=3`.

Every fact begins with `await TestDataHelper.ClearDatabaseAsync(factory.Services);` and uses a unique user name / email per fact so accounts never collide across facts. Use `TestContext.Current.CancellationToken` on every awaited HTTP call, matching the suite convention.

Do not add a `[ValidateAntiForgeryToken]` workaround — the factory decorates `IAntiforgery` so plain form posts already validate.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0 (the file compiles despite no `EventsController` existing).
    - `EventsControllerIntegrationTests.cs` contains exactly 10 `[Fact]` attributes.
    - `grep -c "Events/" QuestBoard.IntegrationTests/Controllers/EventsControllerIntegrationTests.cs` is at least 10 (routes are string literals).
    - `grep -nE "EventEntity|EventViewModel|EventsController|QuestBoard\.Domain\.Models\.Event" QuestBoard.IntegrationTests/Controllers/EventsControllerIntegrationTests.cs` returns no matches — the scaffold has no compile-time dependency on Phase 74 production types.
    - `dotnet test --filter "FullyQualifiedName~EventsControllerIntegrationTests"` exits non-zero and reports failures, not "no tests found" — this is the intended Wave 0 RED state.
    - No comment or string literal in the file contains a requirement identifier, phase number, or plan number (CLAUDE.md rule).
  </acceptance_criteria>
  <done>Ten RED facts covering EVENT-01 (create with title/description/date/optional start time) and EVENT-02 (DM edit/delete on own board) exist, the solution still builds, and the facts fail with 404 rather than a compile error.</done>
</task>

<task type="auto">
  <name>Task 2: Create the structural-protection and quest-creation-unaffected tests</name>
  <files>QuestBoard.IntegrationTests/Controllers/EventCalendarPartialTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Controllers/CalendarControllerIntegrationTests.cs` — full file. Copy its arrange pattern for creating a DM, a viewer client, a quest with a proposed date, and setting `FinalizedDate` through a scoped `QuestBoardContext`.
    - `QuestBoard.IntegrationTests/Controllers/QuestControllerIntegrationTests_Comprehensive.cs` — the established shape for a quest-creation POST and its persistence assertion.
    - `QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs` — signatures for `CreateTestQuestAsync`, `CreateProposedDateAsync`, `ClearDatabaseAsync`.
    - `QuestBoard.Service/Views/Quest/Details.cshtml` lines 595-710 and `QuestBoard.Service/Views/Quest/Details.Mobile.cshtml` lines 150-200 — the five `@await Html.PartialAsync("_Calendar", calendarMonth)` call sites. Read them to understand what those pages render; do NOT edit them.
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-CONTEXT.md` — D-09 (structural protection is an acceptance criterion, not a code-review note) and D-23 (quest creation must be provably unaffected).
    - `.planning/phases/74-event-schema-crud-and-calendar-display/74-UI-SPEC.md` § Component Contract — the event chip's markup identity is the CSS class `calendar-event`; that is the string the D-09 test greps the Quest Details HTML for.
  </read_first>
  <action>
Create `QuestBoard.IntegrationTests/Controllers/EventCalendarPartialTests.cs` in namespace `QuestBoard.IntegrationTests.Controllers`, class `EventCalendarPartialTests(WebApplicationFactoryBase factory) : IClassFixture<WebApplicationFactoryBase>`.

Header comment, plain language: this file proves two negatives that are easy to lose silently — that the quest detail pages never render event markup even when an event exists on the same board and same day, and that quest creation is untouched by the presence of events.

Reference Events only through the route literal `"/Events/Create"`. Do not reference any Phase 74 C# type.

Fact 1 — `QuestDetails_WithSameDayEventOnSameBoard_RendersNoEventMarkup`:
  - Clear the database. Create a DM user and an authenticated DM client.
  - Create a quest with `TestDataHelper.CreateTestQuestAsync(..., isFinalized: true)` and a proposed date on `DateTime.Today.AddDays(10)`, then set `FinalizedDate` on that quest through a scoped `QuestBoardContext`, exactly as `CalendarControllerIntegrationTests.Index_WithFinalizedQuests_ShouldDisplayQuestsOnCalendar` does.
  - POST an event to `/Events/Create` on the *same* date, `Title = "Same Day Council Meeting"`.
  - GET `/Quest/Details/{questId}` with the DM client. Assert `HttpStatusCode.OK`.
  - Assert the body does NOT contain `"Same Day Council Meeting"`.
  - Assert the body does NOT contain the event chip container class name nor the event chip class name. Reference both as `const string` fields at the top of the class named `EventBlockClass` and `EventChipClass`, whose values are the two calendar event CSS class names defined in the UI spec, so the assertion has a single source of truth.
  - Assert the body DOES contain the quest title, proving the page rendered its calendar at all and the test is not vacuously passing on an error page.

Fact 2 — `QuestDetailsMobile_WithSameDayEventOnSameBoard_RendersNoEventMarkup`:
  - Same arrangement, but issue the GET with the iPhone User-Agent so the `.Mobile` view is selected. Copy the `MobileUserAgent` constant and the `GetWithUserAgentAsync(url, userAgent, authorization)` helper shape from `QuestBoard.IntegrationTests/Mobile/MobileViewsTests.cs` lines 16-56, including passing the authenticated client's `DefaultRequestHeaders.Authorization` through, since `/Quest/Details` requires auth.
  - Same three assertions as Fact 1.

Fact 3 — `QuestCreate_WithExistingEventOnChosenDate_SucceedsUnchanged`:
  - Clear the database. Create a DM client.
  - POST an event to `/Events/Create` dated `DateTime.Today.AddDays(21)`.
  - POST a quest creation form to `/Quest/Create` whose proposed date is that same day. Mirror the form field names used by `QuestControllerIntegrationTests_Comprehensive`'s create test exactly.
  - Assert the quest POST returns `Redirect` — not `OK` (which would mean a redisplayed form with a validation error) and not `BadRequest`.
  - Assert through a scoped `QuestBoardContext` that the quest row exists with the expected title.
  - Assert the response body is empty or contains no warning text: specifically assert the response has no `ModelState` error surface by checking the status is `Redirect`. Add a comment in plain language explaining that this fact proves an absence — nothing in the quest creation path may inspect events — and that it must keep passing for the life of the feature.

Every fact clears the database first and uses unique user names.
  </action>
  <verify>
    <automated>cd C:\Repos\quest-board && dotnet build QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` exits 0.
    - `EventCalendarPartialTests.cs` contains exactly 3 `[Fact]` attributes.
    - The file declares `const string EventBlockClass` and `const string EventChipClass` and both D-09 facts assert `NotContain` against those constants.
    - `grep -nE "EventEntity|EventViewModel|EventsController" QuestBoard.IntegrationTests/Controllers/EventCalendarPartialTests.cs` returns no matches.
    - `git status --porcelain QuestBoard.Service/Views/Quest/` shows no modifications — the five out-of-scope call sites are untouched.
    - `dotnet test --filter "FullyQualifiedName~EventCalendarPartialTests"` exits non-zero (RED) because `/Events/Create` returns 404.
    - No comment or string literal in the file contains a requirement identifier, phase number, or plan number.
  </acceptance_criteria>
  <done>The D-09 structural-protection assertion and the EVENT-05 negative assertion exist as automated tests before any implementation, and `Views/Quest/Details.cshtml` / `Details.Mobile.cshtml` are provably unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app under test | The integration harness replaces `IActiveGroupContext` with a mutable singleton and decorates `IAntiforgery`; a test written against that harness can pass while the production path is broken. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-08 | Spoofing | `EventCalendarPartialTests` D-09 facts | medium | mitigate | A `NotContain` assertion passes vacuously if the page 500s or redirects. Each D-09 fact also asserts `HttpStatusCode.OK` and asserts the quest title IS present, so a broken page fails the test instead of silently satisfying it. |
| T-74-09 | Repudiation | Wave 0 RED scaffold | low | accept | The scaffold intentionally fails until later plans land; a reader could mistake a red suite for a regression. Mitigated by the plain-language file header stating the intended RED state. |
| T-74-SC | Tampering | package installs | low | accept | Zero new NuGet packages this phase — xUnit v3, FluentAssertions, NSubstitute and the EF InMemory provider are all already referenced. `74-RESEARCH.md` § Package Legitimacy Audit records this explicitly, so no install checkpoint applies. |
</threat_model>

<verification>
1. `dotnet build` exits 0 for the whole solution.
2. `dotnet test --filter "FullyQualifiedName~Event"` runs and reports failures (RED) rather than "no tests found".
3. `git status --porcelain` shows only the two new test files as added.
</verification>

<success_criteria>
- Two new test files exist and compile.
- 13 new facts total, all RED, none of them a compile error.
- No production source file was touched.
</success_criteria>

<output>
Create `.planning/phases/74-event-schema-crud-and-calendar-display/74-01-SUMMARY.md` when done
</output>
