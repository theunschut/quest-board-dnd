---
phase: 32-first-login-password-flow
plan: 05
type: execute
wave: 3
depends_on: ["32-03", "32-04"]
files_modified:
  - QuestBoard.Service/Controllers/Admin/AccountController.cs
  - QuestBoard.Domain/Interfaces/IIdentityService.cs
  - QuestBoard.Repository/IdentityService.cs
  - QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs
autonomous: false
requirements: [PWFLOW-01, PWFLOW-02, PWFLOW-03, PWFLOW-04, PWFLOW-05]
must_haves:
  truths:
    - "D-05: The retired confirm-email-only flow leaves no dead code: AccountController.ConfirmEmail and the orphaned IIdentityService/IdentityService confirm-email methods (ConfirmEmailAsync + GenerateEmailConfirmationAsync) are deleted, with zero callers remaining solution-wide"
    - "D-11: Integration tests prove ForgotPassword POST is enumeration-safe (same response for known and unknown emails)"
    - "D-12: Integration tests prove the 4th rapid ForgotPassword POST from one client returns HTTP 429"
    - "Integration tests prove a passwordless account cannot sign in (Login returns failure, no exception)"
    - "Integration tests prove SetPassword sets the password and confirms the email"
    - "The full test suite is green before the phase is verified"
  artifacts:
    - path: "QuestBoard.Domain/Interfaces/IIdentityService.cs"
      provides: "IIdentityService with the orphaned confirm-email members removed; the separate email-address-change flow intact"
      absent: "GenerateEmailConfirmationAsync"
    - path: "QuestBoard.Service/Controllers/Admin/AccountController.cs"
      provides: "AccountController without the orphaned ConfirmEmail GET action; ConfirmEmailChange (email-change flow) intact"
      absent: "> ConfirmEmail("
    - path: "QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs"
      provides: "ForgotPassword + SetPassword + passwordless-login integration coverage"
      contains: "ForgotPassword"
    - path: "QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs"
      provides: "Adapted Welcome-resend coverage"
      contains: "SendConfirmationEmail"
  key_links:
    - from: "QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs"
      to: "/Account/ForgotPassword and /Account/SetPassword"
      via: "HTTP client GET/POST"
      pattern: "/Account/(ForgotPassword|SetPassword)"
---

<objective>
Complete the phase: first finish retiring the confirm-email-only flow by deleting its now-orphaned code (Task 1), then prove the phase behaviors with integration tests, run the full suite green, and gate on a human visual verification of the two new emails and the end-to-end flow. This plan runs in Wave 3, after Plan 03 (user-facing ForgotPassword/SetPassword) and Plan 04 (admin passwordless flow) have both fully merged — so it can delete the orphaned confirm-email code with a single unconditional solution-wide grep confirming zero callers remain, with NO cross-plan "did the other plan run yet?" branching. Covers the Wave 0 test gaps from VALIDATION.md: ForgotPassword GET/POST (enumeration-safety + rate-limit 429), SetPassword GET/POST (password set + EmailConfirmed), passwordless-login-fails, and the adapted Admin Welcome-resend path. The job unit tests (`WelcomeEmailJobTests`, `ForgotPasswordEmailJobTests`) and the deletion of `ConfirmationEmailJobTests` already landed in Plan 02.

Purpose: This is the cleanup + verification wave. It (1) completes the D-05 retirement that Plans 03/04 set up by removing AdminController's and AccountController's callers, and (2) confirms PWFLOW-01..05 hold end-to-end and blocks phase completion on both a green suite and a human check of the (non-automatable) email rendering and full click-through.
Output: Orphaned confirm-email code deleted; new/adapted integration tests; full `dotnet test` green; a passed human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-first-login-password-flow/32-CONTEXT.md
@.planning/phases/32-first-login-password-flow/32-RESEARCH.md
@.planning/phases/32-first-login-password-flow/32-PATTERNS.md
@.planning/phases/32-first-login-password-flow/32-VALIDATION.md
@.planning/phases/32-first-login-password-flow/32-03-SUMMARY.md
@.planning/phases/32-first-login-password-flow/32-04-SUMMARY.md

<interfaces>
<!-- Dead-code members to delete (Task 1) and test analogs/helpers (Tasks 2-3) — extracted from codebase. -->

Dead-code members to delete in Task 1 (all callers removed by Plans 03/04 in Wave 2 — verify with an unconditional grep before deleting):
  AccountController.ConfirmEmail(int userId, string token)   // AccountController.cs GET ~lines 23-53 — the ONLY caller of IIdentityService.ConfirmEmailAsync; Plan 03 left it in place for this plan to delete
  IIdentityService.GenerateEmailConfirmationAsync(int userId)   // IIdentityService.cs:24 — its only callers were AdminController lines 122 & 277, both removed by Plan 04 Task 2
  IIdentityService.ConfirmEmailAsync(int userId, string token)  // IIdentityService.cs:25 — its only caller is AccountController.ConfirmEmail (deleted in this same task)
  IdentityService.GenerateEmailConfirmationAsync                // QuestBoard.Repository/IdentityService.cs:118-123 implementation
  IdentityService.ConfirmEmailAsync                             // QuestBoard.Repository/IdentityService.cs:125-131 implementation
  NOTE: these two methods have NO IUserService/UserService wrappers (grep confirms) — do not look for any.
  DO NOT TOUCH: AccountController.ConfirmEmailChange, IIdentityService.GenerateChangeEmailTokenAsync, IIdentityService.ChangeEmailAsync (IIdentityService.cs:26-27), IdentityService.GenerateChangeEmailTokenAsync (line 133+) — these are the SEPARATE, still-active email-address-change flow (D-05 explicitly keeps this).

AccountControllerIntegrationTests analogs (QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs):
  Login_Get_ShouldReturnSuccessStatusCode (lines 11-21): _client.GetAsync + StatusCode OK + content Contains
  Logout_Post_ShouldRedirectToHome (lines 84-109): AuthenticationHelper.CreateAuthenticatedClientWithUserAsync; AntiForgeryHelper.ExtractAntiForgeryTokenAsync; AntiForgeryHelper.CreateFormContentWithAntiForgeryToken; PostAsync
  Register_Post_WithValidData_ShouldReturnNotFound (lines 35-54): FormUrlEncodedContent POST-body construction primitive
  TestContext.Current.CancellationToken used on every async call

Helpers available: AuthenticationHelper (CreateAuthenticatedClientWithUserAsync), AntiForgeryHelper (ExtractAntiForgeryTokenAsync, CreateFormContentWithAntiForgeryToken), WebApplicationFactoryBase (CreateNonRedirectingClient), TestDataHelper.ClearDatabaseAsync (seeds roles + Group 1 FK). Framework: xUnit v3 + FluentAssertions.

Passwordless-login test approach: seed a user via the passwordless creation path (IUserService.CreateAsync(email, name) — Plan 01) resolved from the factory's service scope, then POST valid-looking credentials to /Account/Login and assert the response does NOT indicate a successful sign-in (no redirect to an authenticated area / SignInResult path fails) and no 500.

Rate-limit test approach (PWFLOW-04): issue 4 rapid POSTs to /Account/ForgotPassword from the same client (same partition IP) with valid anti-forgery tokens; assert the 4th returns HttpStatusCode.TooManyRequests (429). Note the policy window is 15 min — the test must run 4 requests inside one window from one client; the in-memory test host shares the RemoteIpAddress partition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the orphaned confirm-email-only dead code</name>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/AccountController.cs (ConfirmEmail GET at ~lines 23-53 — DELETE this action; ConfirmEmailChange further down is the SEPARATE email-change flow — KEEP it)
    - QuestBoard.Domain/Interfaces/IIdentityService.cs (lines 24-25 — the two declarations to delete; lines 26-27 GenerateChangeEmailTokenAsync/ChangeEmailAsync are the email-change flow — KEEP)
    - QuestBoard.Repository/IdentityService.cs (GenerateEmailConfirmationAsync lines 118-123 and ConfirmEmailAsync lines 125-131 — the two implementations to delete; GenerateChangeEmailTokenAsync starting line 133 — KEEP)
    - .planning/phases/32-first-login-password-flow/32-CONTEXT.md (D-05 — the confirm-email-only flow is retired; the email-address-change flow is explicitly NOT part of the retirement and must stay)
    - .planning/phases/32-first-login-password-flow/32-03-SUMMARY.md (confirms Plan 03 left AccountController.ConfirmEmail in place for this plan to delete)
    - .planning/phases/32-first-login-password-flow/32-04-SUMMARY.md (confirms Plan 04 removed AdminController's two GenerateEmailConfirmationAsync call sites)
  </read_first>
  <action>
    This plan runs in Wave 3, AFTER both Plan 03 and Plan 04 have merged, so all callers of the confirm-email-only members are already gone. This task completes the D-05 retirement by deleting the orphaned code with NO conditional logic — a single unconditional pre-flight grep proves zero callers, then the deletions proceed cleanly.

    PRE-FLIGHT VERIFICATION (must pass before deleting — if any call site remains, STOP: it means Plan 03 or 04 did not merge as expected):
      - grep the whole solution (source only) for "identityService.ConfirmEmailAsync(" — filter out comment lines and expect ZERO matches (AccountController.ConfirmEmail, its only caller, was left by Plan 03 for THIS task to remove; but as a live call site it must be gone once we delete the action — so verify no OTHER caller exists first).
      - grep the whole solution (source only) for "identityService.GenerateEmailConfirmationAsync(" — filter out comment lines and expect ZERO matches (Plan 04 removed AdminController's two sites).
      Use a grep that excludes comment lines to avoid self-invalidating header prose, e.g. grep across *.cs, then `grep -v '^\s*//'`, and confirm the count is 0 for GenerateEmailConfirmationAsync and that the only ConfirmEmailAsync source reference is the definition/impl about to be deleted (plus the internal `userManager.ConfirmEmailAsync` line inside the impl being removed).

    Deletions:
      1. QuestBoard.Service/Controllers/Admin/AccountController.cs — delete the entire ConfirmEmail(int userId, string token) GET action (~lines 23-53, including its [HttpGet] attribute). Leave ConfirmEmailChange and every other action intact.
      2. QuestBoard.Domain/Interfaces/IIdentityService.cs — delete the GenerateEmailConfirmationAsync(int userId) declaration (line 24) and the ConfirmEmailAsync(int userId, string token) declaration (line 25). Do NOT touch GenerateChangeEmailTokenAsync / ChangeEmailAsync (lines 26-27).
      3. QuestBoard.Repository/IdentityService.cs — delete the GenerateEmailConfirmationAsync implementation (lines 118-123) and the ConfirmEmailAsync implementation (lines 125-131). Leave GenerateChangeEmailTokenAsync (line 133+) and everything else intact.

    If removing ConfirmEmail leaves an unused `using` in AccountController.cs, the build warns but does not fail — leave usings unless the compiler errors.
  </action>
  <verify>
    <automated>cd /c/Repos/quest-board && dotnet build QuestBoard.Service && dotnet test QuestBoard.UnitTests</automated>
  </verify>
  <acceptance_criteria>
    - QuestBoard.Service/Controllers/Admin/AccountController.cs no longer contains a ConfirmEmail(int userId, string token) action (grep for "> ConfirmEmail(" returns zero matches; ConfirmEmailChange is still present)
    - QuestBoard.Domain/Interfaces/IIdentityService.cs no longer declares GenerateEmailConfirmationAsync OR ConfirmEmailAsync (grep for each returns zero matches), and STILL declares GenerateChangeEmailTokenAsync + ChangeEmailAsync
    - QuestBoard.Repository/IdentityService.cs no longer implements GenerateEmailConfirmationAsync OR ConfirmEmailAsync (grep for each declaration returns zero matches), and STILL implements GenerateChangeEmailTokenAsync
    - Solution-wide grep (source, comments excluded) for "GenerateEmailConfirmationAsync" returns zero matches
    - Solution-wide grep (source, comments excluded) for "identityService.ConfirmEmailAsync(" returns zero matches
    - dotnet build QuestBoard.Service succeeds and dotnet test QuestBoard.UnitTests is green
  </acceptance_criteria>
  <done>The retired confirm-email-only flow leaves no dead code: the orphaned controller action and BOTH ConfirmEmailAsync + GenerateEmailConfirmationAsync members are removed, with zero callers solution-wide; the separate email-address-change flow is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: AccountController integration tests — ForgotPassword, SetPassword, passwordless login</name>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs (all three analog tests named in the interfaces block; the anti-forgery POST pattern; TestContext.Current.CancellationToken usage)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs and AntiForgeryHelper.cs (helper signatures)
    - .planning/phases/32-first-login-password-flow/32-PATTERNS.md (AccountControllerIntegrationTests section — GET pattern, anti-forgery POST pattern, rate-limit 4-POST approach)
    - .planning/phases/32-first-login-password-flow/32-VALIDATION.md (Per-Task Verification Map rows PWFLOW-02/03/04)
    - .planning/phases/32-first-login-password-flow/32-03-SUMMARY.md (the enumeration-safe message text + SetPassword route shape to assert against)
  </read_first>
  <action>
    Add integration tests to AccountControllerIntegrationTests.cs:

    (1) ForgotPassword_Get_ShouldReturnSuccessStatusCode — GET /Account/ForgotPassword returns 200 and content contains the form (mirror Login_Get).

    (2) ForgotPassword_Post_WithKnownEmail_ShouldReturnGenericMessage and ForgotPassword_Post_WithUnknownEmail_ShouldReturnSameGenericMessage — seed a known user (via ClearDatabaseAsync + a created user), POST each email with a valid anti-forgery token, and assert BOTH produce the same outcome: the redirect/response surfaces the identical "If that email is registered, a reset link has been sent." TempData message and neither reveals account existence (enumeration-safety, PWFLOW-04).

    (3) ForgotPassword_Post_ExceedingRateLimit_ShouldReturn429 — issue 4 rapid POSTs to /Account/ForgotPassword from the same client with valid anti-forgery tokens; assert the 4th response is HttpStatusCode.TooManyRequests (PWFLOW-04). If the shared test-host partition makes a per-test 429 flaky, isolate this test's client and document the partition assumption in a comment.

    (4) SetPassword_Get_WithUserIdAndToken_ShouldReturnForm — GET /Account/SetPassword?userId=1&token=abc returns 200 with the password form (hidden UserId/Token present in the HTML).

    (5) SetPassword_Post_WithValidToken_ShouldSetPasswordAndConfirmEmail — seed a passwordless user, generate a real reset token via the factory service scope, Base64Url-encode it, POST userId + token + matching NewPassword/ConfirmPassword with anti-forgery, assert redirect to Login and that the user can subsequently sign in AND EmailConfirmed is true (PWFLOW-02). SetPassword_Post_WithInvalidToken_ShouldFailGracefully — POST a garbage token, assert a non-500 response and the password is unchanged.

    (6) Login_Post_PasswordlessAccount_ShouldNotSignIn — seed a user via the passwordless CreateAsync path, POST plausible credentials to /Account/Login, assert no successful sign-in and no 500 (PWFLOW-03).

    Use TestContext.Current.CancellationToken on every async call, FluentAssertions for assertions, and the existing AntiForgeryHelper primitives for POST bodies. Reset state with TestDataHelper.ClearDatabaseAsync where seeding is required.
  </action>
  <verify>
    <automated>cd /c/Repos/quest-board && dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~AccountControllerIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - AccountControllerIntegrationTests.cs contains tests covering: ForgotPassword GET, ForgotPassword POST known-vs-unknown-email same response, ForgotPassword POST 4th-request 429, SetPassword GET form, SetPassword POST valid token (password set + EmailConfirmed true), SetPassword POST invalid token graceful failure, and passwordless-account login failure
    - The known-email and unknown-email ForgotPassword tests assert the SAME response outcome (enumeration-safety)
    - The rate-limit test asserts HttpStatusCode.TooManyRequests on the 4th request
    - dotnet test QuestBoard.IntegrationTests --filter for AccountControllerIntegrationTests reports all tests passed
  </acceptance_criteria>
  <done>ForgotPassword/SetPassword/passwordless-login behaviors are proven by green integration tests.</done>
</task>

<task type="auto">
  <name>Task 3: Adapt AdminController tests + full-suite green gate</name>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs (existing SendConfirmationEmail-related tests to adapt; existing CreateUser tests)
    - .planning/phases/32-first-login-password-flow/32-PATTERNS.md (AdminController analogs)
    - .planning/phases/32-first-login-password-flow/32-04-SUMMARY.md (confirm SendConfirmationEmail was NOT renamed, and the passwordless CreateUser behavior)
    - .planning/phases/32-first-login-password-flow/32-VALIDATION.md (PWFLOW-05 row; full-suite gate)
  </read_first>
  <action>
    Adapt the existing SendConfirmationEmail / CreateUser tests in AdminControllerIntegrationTests.cs to the new behavior: CreateUser POST no longer submits a Password field (remove any Password form value from the test's POST body) and the account is created passwordless; the SendConfirmationEmail action (name unchanged) still requires EmailConfirmed == false and now drives the Welcome path. Assert the existing behaviors that remain observable through the HTTP layer: CreateUser POST with valid Email/Name/GroupRole succeeds and redirects to Users; SendConfirmationEmail on an unconfirmed user redirects to Users with a success TempData (PWFLOW-05). Do not assert on enqueued Hangfire job internals (not observable via HTTP) — the job wiring is covered by the Plan 02 unit tests. Remove or update any assertion that depended on an admin-set password.

    Then run the full suite as the phase green gate: dotnet test (all projects). All pre-existing tests plus the new/adapted ones must pass. If any pre-existing test broke due to the signature/flow changes (e.g. a CreateUser test still sending a Password field), fix the test to match the new passwordless contract — do not weaken assertions to hide a regression.
  </action>
  <verify>
    <automated>cd /c/Repos/quest-board && dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - AdminControllerIntegrationTests CreateUser POST test no longer sends a Password form field
    - AdminControllerIntegrationTests still covers SendConfirmationEmail on an EmailConfirmed == false user redirecting to Users with a success outcome
    - No test asserts on an admin-set password anywhere
    - dotnet test (full suite) reports all tests passed with zero failures
  </acceptance_criteria>
  <done>Admin tests reflect the passwordless/Welcome contract; entire suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Passwordless admin user creation + combined Welcome (set-password + confirm-email) flow, self-service Forgot Password flow (enumeration-safe + rate-limited), shared SetPassword landing page, retired confirm-email-only flow (orphaned code now fully deleted), 7-day token lifespan, and two new email templates. Full automated suite is green.
  </what-built>
  <how-to-verify>
    1. Run the app: `dotnet run --project QuestBoard.Service` (ensure the debugger is not holding the build; Shift+F5 in Visual Studio if a build lock appears).
    2. Email visuals: visit `/EmailPreview` and open the new **Welcome** and **ForgotPassword** previews. Confirm both render in the Cinzel/wax-seal style, the CTA buttons read "Set My Password" / "Reset My Password", and the ForgotPassword email has the "if you did not request a password reset" disclaimer. Confirm there is NO ConfirmEmail preview link anymore.
    3. Admin create-user: as an Admin, go to Create User. Confirm there is NO password field. Create a user; confirm the success message mentions a welcome/set-password email.
    4. Welcome link: retrieve the enqueued email link (from the mail provider dashboard or dev inbox) — it should point at `/Account/SetPassword?userId=...&token=...`. Open it, set a password, and confirm you are redirected to Login with a success message and can then log in.
    5. Forgot password: from the Login page, click "Forgot password?", submit a known email — confirm the generic "If that email is registered, a reset link has been sent." message. Submit an unknown email — confirm the SAME message (no difference). Use the reset link to set a new password and log in.
    6. Rate limit: submit the Forgot Password form 4 times rapidly with the same email — confirm the 4th is rejected (HTTP 429 / "Too many requests").
    7. Passwordless sign-in: attempt to log in as the just-created user BEFORE setting a password — confirm login fails (no crash).
    8. Resend welcome: on the Users admin page, for an unconfirmed user, confirm the row button now reads "Resend Welcome Email" and sends the welcome email when clicked.
    9. Reverse-proxy IP note (deploy-env only): confirm `ForwardedHeaders` behavior in the deployed environment so the rate-limit partition key reflects the real client IP, not the proxy (documented manual item; not blocking local verification).
  </how-to-verify>
  <resume-signal>Type "approved" once the emails render correctly and the full flow (create -> welcome -> set password -> login, plus forgot-password + rate limit + passwordless-login-fails + resend button) works, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → anonymous endpoints | Integration tests exercise ForgotPassword/SetPassword/Login as untrusted callers |
| human verifier → deployed reverse proxy | Manual confirmation of forwarded-header IP behavior for the rate-limit partition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-17 | Information Disclosure | Enumeration-safety regression | mitigate | Explicit known-vs-unknown-email test asserts identical responses; a regression fails the suite |
| T-32-18 | Denial of Service | Rate-limit regression | mitigate | 4th-request-429 test asserts the limiter is active; partition-key correctness behind a proxy is the T-32-04 manual item, re-checked at step 9 |
| T-32-19 | Elevation of Privilege | Passwordless sign-in regression | mitigate | Login_Post_PasswordlessAccount_ShouldNotSignIn asserts a passwordless account cannot authenticate (PWFLOW-03) |
| T-32-20 | Attack Surface | Removal of the anonymous ConfirmEmail GET endpoint | mitigate | Deleting the orphaned confirm-email GET action (Task 1) removes an unauthenticated, now-unreachable token-consuming endpoint — net reduction in attack surface, consistent with D-05 retirement; the separate email-change flow is untouched |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this plan; legitimacy checkpoint not applicable (no installs across the phase) |
</threat_model>

<verification>
- Task 1 deletions: solution-wide grep (source, comments excluded) for GenerateEmailConfirmationAsync and "identityService.ConfirmEmailAsync(" both return zero matches; ConfirmEmailChange + GenerateChangeEmailTokenAsync (email-change flow) still present.
- dotnet test (full suite) is green — the phase gate.
- New AccountController tests cover enumeration-safety, rate-limit 429, SetPassword success + EmailConfirmed, and passwordless-login failure.
- Admin tests reflect the passwordless CreateUser + Welcome-resend contract.
- Human checkpoint passed: both emails render correctly and the end-to-end flow works.
</verification>

<success_criteria>
- The retired confirm-email-only flow leaves no dead code: ConfirmEmail action + ConfirmEmailAsync + GenerateEmailConfirmationAsync deleted with zero callers; the email-address-change flow is untouched.
- All Wave 0 integration gaps closed; PWFLOW-01..05 proven by green tests.
- Full suite green with zero failures.
- Human verifier approves email rendering and the complete create->welcome->set->login + forgot-password + rate-limit + passwordless-fail + resend flow.
</success_criteria>

<output>
Create `.planning/phases/32-first-login-password-flow/32-05-SUMMARY.md` when done, recording which dead-code members were deleted in Task 1 (confirming zero callers solution-wide), the final test count, any partition-key/rate-limit test caveats, and the outcome of the reverse-proxy ForwardedHeaders manual check (step 9).
</output>
</output>
