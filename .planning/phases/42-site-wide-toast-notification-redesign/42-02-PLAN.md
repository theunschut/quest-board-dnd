---
phase: 42-site-wide-toast-notification-redesign
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - QuestBoard.Service/Views/Shop/Index.cshtml
  - QuestBoard.Service/Views/Shop/Index.Mobile.cshtml
  - QuestBoard.Service/Views/Shop/Details.cshtml
  - QuestBoard.Service/Views/Shop/Details.Mobile.cshtml
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Shop Index and Details views (desktop and mobile) no longer render their own local toast container — they rely on the shared _Toasts.cshtml partial from the layout"
    - "Shop's GoldReceived '+X gp' celebratory toast still appears after a sale, rendered by the shared partial, alongside (not replacing) the generic Success toast"
    - "The 4 duplicated inline toast-init scripts are removed from Shop's @section Scripts blocks; init now runs once from site.js"
    - "Shop's 'Mystical Merchant' novelty toast (showMerchantDialog/showMerchantToast) is completely untouched and still works with its yellow styling"
  artifacts:
    - path: "QuestBoard.Service/Views/Shop/Index.cshtml"
      provides: "Shop index with merchant novelty toast preserved, generic toast container + init removed"
      contains: "showMerchantToast"
  key_links:
    - from: "QuestBoard.Service/Views/Shop/Details.cshtml"
      to: "QuestBoard.Service/Views/Shared/_Toasts.cshtml"
      via: "GoldReceived rendering delegated to the shared partial via TempData[\"GoldReceived\"]"
      pattern: "GoldReceived"
---

<objective>
Migrate Shop's 4 toast-bearing views (Index/Details, desktop + mobile) off their local Bootstrap toast markup and init scripts onto the shared `_Toasts.cshtml` partial created in Plan 01 (D-07). Remove each view's local `.toast-container` block and its inline toast-init `@section Scripts` snippet. The GoldReceived toast is now handled by the shared partial (it was ported there in Plan 01). Leave Shop's "Mystical Merchant" novelty toast entirely untouched (D-06).

Purpose: Full site-wide unification — Shop's toasts become indistinguishable from the rest of the site except for the bespoke GoldReceived block, and the last 4 duplicate init scripts are eliminated (RESEARCH Pitfall 3).

Output: 4 Shop views with local toast markup + init removed, merchant toast preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-CONTEXT.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md
@.planning/phases/42-site-wide-toast-notification-redesign/42-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan CREATES no new symbols — it only DELETES local markup/scripts from 4 Shop views. It DEPENDS on `_Toasts.cshtml` (created in Plan 01), which now owns the Success/Error/GoldReceived rendering and the site.js init. Do not re-create any toast markup here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove local toast container + init from Shop Index (desktop + mobile), preserving the merchant toast</name>
  <files>QuestBoard.Service/Views/Shop/Index.cshtml, QuestBoard.Service/Views/Shop/Index.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shop/Index.cshtml (lines 450-489 = local Success/Error `.toast-container` block + the generic toast-init part of `@section Scripts`; lines 514-572 = the Mystical Merchant `showMerchantDialog`/`showMerchantToast` functions that MUST stay)
    - QuestBoard.Service/Views/Shop/Index.Mobile.cshtml (lines 259-298 = local `.toast-container` block + `@section Scripts` toast-init; check whether this file also has merchant functions)
    - .planning/phases/42-site-wide-toast-notification-redesign/42-RESEARCH.md (Pitfall 3 caveat — only the generic bulk-init is removed; showMerchantToast's own `new bootstrap.Toast(newToast)` stays)
  </read_first>
  <action>
    In `Views/Shop/Index.cshtml`: delete the entire local `<div class="toast-container position-fixed top-0 end-0 p-3" ...>...</div>` block (the `@if (TempData["Success"])` + `@if (TempData["Error"])` container, approx lines 450-478) — the shared partial now renders these. Inside `@section Scripts`, delete ONLY the generic toast bulk-init snippet (the `document.addEventListener('DOMContentLoaded', ...)` that does `document.querySelectorAll('.toast')` → `new bootstrap.Toast` → `.show()`, approx lines 481-489). Keep every other script in that block: the modal-loading `show.bs.modal` handler, the purchase-button `stopPropagation` handler, and the ENTIRE `showMerchantDialog()`/`showMerchantToast()` pair (approx lines 514-572) — the merchant toast self-initializes its own dynamic toast instance and is explicitly out of scope (D-06). If removing the generic init leaves `@section Scripts` still containing other scripts, keep the section; only if the section would become empty should it be removed entirely.

    In `Views/Shop/Index.Mobile.cshtml`: delete the local `.toast-container` block (approx lines 260-287) and the generic toast-init snippet inside `@section Scripts` (approx lines 289-298). Preserve any non-toast scripts (e.g. the modal-loading handler) and any merchant functions present in this mobile file — same rule as desktop.

    Do not otherwise alter page content, styling, or the `merchant-toast` CSS class usage.
  </action>
  <verify>
    <automated>bash -c "! grep -q 'toast-container position-fixed top-0 end-0' QuestBoard.Service/Views/Shop/Index.cshtml && ! grep -q 'toast-container position-fixed top-0 end-0' QuestBoard.Service/Views/Shop/Index.Mobile.cshtml && grep -q 'showMerchantToast' QuestBoard.Service/Views/Shop/Index.cshtml && ! grep -q \"querySelectorAll('.toast')\" QuestBoard.Service/Views/Shop/Index.cshtml && echo SHOP_INDEX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Neither `Index.cshtml` nor `Index.Mobile.cshtml` contains the string `toast-container position-fixed top-0 end-0`.
    - Neither file contains a `document.querySelectorAll('.toast')` generic bulk-init call.
    - `Index.cshtml` still contains `showMerchantDialog` and `showMerchantToast` and their `new bootstrap.Toast(newToast)` call (merchant toast preserved).
    - The modal-loading `show.bs.modal` handler and purchase-button `stopPropagation` handler are still present in `Index.cshtml`.
  </acceptance_criteria>
  <done>Shop Index (desktop + mobile) delegates generic toasts to the shared partial + site.js init; the Mystical Merchant novelty toast is fully preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove local toast container + init from Shop Details (desktop + mobile), including the local GoldReceived block</name>
  <files>QuestBoard.Service/Views/Shop/Details.cshtml, QuestBoard.Service/Views/Shop/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Shop/Details.cshtml (lines 51-112 = local `.toast-container` with Success/Error/GoldReceived blocks + `@section Scripts` toast-init)
    - QuestBoard.Service/Views/Shop/Details.Mobile.cshtml (lines 9-57 = local `.toast-container` with Success/Error/GoldReceived; lines ~106-112 = `@section Scripts` toast-init)
    - QuestBoard.Service/Views/Shared/_Toasts.cshtml (created in Plan 01 — confirm the GoldReceived block is present there so removal here is safe)
  </read_first>
  <action>
    In `Views/Shop/Details.cshtml`: delete the entire local `<div class="toast-container position-fixed top-0 end-0 p-3" ...>...</div>` block including its `@if (TempData["Success"])`, `@if (TempData["Error"])`, and `@if (TempData["GoldReceived"])` sub-blocks (approx lines 51-99) — the shared partial now owns all three, including the bespoke GoldReceived "+X gp" toast that was ported into `_Toasts.cshtml` in Plan 01. Then delete the `@section Scripts` block's generic toast-init snippet (approx lines 101-112); if that leaves `@section Scripts` empty, remove the empty section entirely.

    In `Views/Shop/Details.Mobile.cshtml`: delete the local `.toast-container` block (approx lines 9-57, including its GoldReceived sub-block) and the generic toast-init `@section Scripts` snippet (approx lines 106-112, remove the section if it becomes empty).

    After this, `GoldReceived` and `+X gp gp`-style markup should exist ONLY in `_Toasts.cshtml`, not in the Details views. Do not alter the shop-details card content, the purchase form, or any styling.
  </action>
  <verify>
    <automated>bash -c "! grep -q 'toast-container position-fixed top-0 end-0' QuestBoard.Service/Views/Shop/Details.cshtml && ! grep -q 'toast-container position-fixed top-0 end-0' QuestBoard.Service/Views/Shop/Details.Mobile.cshtml && ! grep -q 'GoldReceived' QuestBoard.Service/Views/Shop/Details.cshtml && ! grep -q 'GoldReceived' QuestBoard.Service/Views/Shop/Details.Mobile.cshtml && echo SHOP_DETAILS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Neither `Details.cshtml` nor `Details.Mobile.cshtml` contains `toast-container position-fixed top-0 end-0`.
    - Neither file contains the string `GoldReceived` (the block now lives only in `_Toasts.cshtml`).
    - Neither file contains a `document.querySelectorAll('.toast')` generic bulk-init call.
    - The shop-details card / purchase form content is otherwise unchanged.
  </acceptance_criteria>
  <done>Shop Details (desktop + mobile) delegates Success/Error/GoldReceived to the shared partial; no local toast markup or init remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Razor view (TempData) | Shop flash strings (Success/Error/GoldReceived) cross into rendered HTML — now via the shared partial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Information Disclosure | Removed local toast markup replaced by shared partial | mitigate | Shared partial (Plan 01) already uses plain `@TempData[...]` (no `@Html.Raw`); this plan only deletes redundant local copies, introducing no new rendering path. |
| T-42-02-02 | Denial of Service (double-init) | Leftover inline init running alongside site.js init | mitigate | Task 1/2 acceptance criteria grep for absence of `querySelectorAll('.toast')` in the migrated views, guaranteeing exactly one init path (site.js) plus the untouched merchant self-init. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — zero package installs in this phase. |
</threat_model>

<verification>
- Build: `dotnet build` succeeds (Razor-only edits). If build fails on locked files, the VS debugger is running — ask the user to stop it (Shift+F5).
- Grep gates for Tasks 1 and 2 pass (no local toast-container, no generic bulk-init, merchant toast preserved, no GoldReceived outside the shared partial).
- Post-phase grep for `new bootstrap.Toast` across the repo should return exactly two sites: the consolidated `site.js` init and Shop's `showMerchantToast()` (RESEARCH Pitfall 3).
- Manual (per 42-VALIDATION.md): complete a sale in Shop Details and confirm both the GoldReceived "+X gp" badge toast AND the generic Success toast appear together; trigger the merchant dialog in Shop Index and confirm its yellow/`bg-warning` styling and behavior are unchanged.
</verification>

<success_criteria>
- 4 Shop views no longer render local toast containers or generic init scripts.
- GoldReceived toast still appears after a sale (via the shared partial).
- Mystical Merchant novelty toast is unchanged and functional.
- `dotnet build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/42-site-wide-toast-notification-redesign/42-02-SUMMARY.md` when done.
</output>
