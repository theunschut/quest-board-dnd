---
phase: 58-rename-the-guild-members-feature-to-characters-everywhere-co
plan: 06
type: execute
wave: 4
depends_on:
  - 58-01
  - 58-04
  - 58-05
files_modified:
  - QuestBoard.Repository/Entities/QuestBoardContext.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A case-insensitive grep for 'guild' across the Service and IntegrationTests projects returns zero hits"
    - "The full solution builds and the entire test suite passes"
    - "No 'GuildMembers' symbol, route string, CSS class, or nav label survives anywhere in the two in-scope projects"
  artifacts: []
  key_links: []
---

<objective>
Final completeness gate for the rename. This phase's only real risk is completeness, not correctness (RESEARCH.md "Don't Hand-Roll"): run the definitive `grep -ri "guild"` sweep scoped to `QuestBoard.Service/` and `QuestBoard.IntegrationTests/`, which must return zero hits, and confirm the full solution builds and all tests pass. Optionally apply the one low-priority `QuestBoardContext.cs` comment touch-up (RESEARCH.md Open Question #2) since it fits naturally as a one-line edit.

Depends on Plan 01 (ViewModels split), Plan 04 (cross-refs + nav — completes the Service-project rename), and Plan 05 (tests). It runs last, in Wave 4, as the phase gate. Explicitly excluded from the sweep: `.planning/` (immutable historical docs) and `README.md` (generic "guild system" flavor prose) — both are Deferred Ideas / out of scope per CONTEXT.md and D-01's boundary.

Purpose: Prove the rename is complete and shipped nothing broken.
Output: verified zero-guild-hit sweep + green build/test; optional QuestBoardContext comment touch-up.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-CONTEXT.md
@.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optional QuestBoardContext.cs "guild roster" comment touch-up</name>
  <files>QuestBoard.Repository/Entities/QuestBoardContext.cs</files>
  <read_first>
    - QuestBoard.Repository/Entities/QuestBoardContext.cs (around line 300 — the "an empty guild roster is the intended behavior here" comment; RESEARCH.md Open Question #2 has the exact wording and recommendation)
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — "Open Questions" #2 (low-priority, optional; reword "guild roster" to "character roster" for terminology consistency)
  </read_first>
  <action>
    Locate the code comment near line 300 in `QuestBoard.Repository/Entities/QuestBoardContext.cs` that currently reads like "an empty guild roster is the intended behavior here, not an oversight" (it explains why an empty-group character query intentionally returns nothing). Reword "guild roster" to "character roster" so the phrase matches this phase's tenant-generic terminology: e.g. "an empty character roster is the intended behavior here, not an oversight". This is a single-line comment edit with zero behavior impact and no code change. Per CLAUDE.md's comment-hygiene rule, use plain descriptive language (no planning-ID references). Do NOT alter any query, filter, or logic in this file — comment text only. If, on reading, the comment turns out to already be reworded or absent (e.g. changed by a prior plan), skip this task and note it in the SUMMARY — it is explicitly optional and outside the Service-project boundary.
  </action>
  <verify>
    <automated>grep -ci "guild" QuestBoard.Repository/Entities/QuestBoardContext.cs</automated>
  </verify>
  <acceptance_criteria>
    - The line-~300 comment no longer contains the word "guild" (reads "character roster" or equivalent); OR the SUMMARY notes the comment was already absent/reworded and the task was skipped
    - No query, filter, entity config, or other code in `QuestBoardContext.cs` was changed — comment text only
  </acceptance_criteria>
  <done>The QuestBoardContext "guild roster" comment is reworded to "character roster" (or skipped as already-clean), with no code change.</done>
</task>

<task type="auto">
  <name>Task 2: Run the definitive zero-guild sweep + full build + full test suite (phase gate)</name>
  <files>QuestBoard.Service/, QuestBoard.IntegrationTests/</files>
  <read_first>
    - .planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-RESEARCH.md — "Don't Hand-Roll" (the closing grep sweep command + scope/exclusions) and "Runtime State Inventory" (confirms the only acceptable remaining "guild" strings are in .planning/ and README.md, both out of scope)
    - all prior plan SUMMARYs in this phase directory (58-01 through 58-05) — confirm each reported its acceptance criteria met
  </read_first>
  <action>
    Run the completeness gate:
    (a) Zero-guild sweep — run `grep -ri "guild" --include="*.cs" --include="*.cshtml" --include="*.css" QuestBoard.Service/ QuestBoard.IntegrationTests/`. This MUST return zero hits. The scope is deliberately limited to those two projects and those three file extensions. Exclusions (NOT failures): `.planning/` historical docs and `README.md`'s generic "guild system" prose are out of scope per D-01's boundary and CONTEXT.md's Deferred Ideas — do not scrub them. If the sweep finds ANY hit inside `QuestBoard.Service/` or `QuestBoard.IntegrationTests/`, that is a missed rename: fix it in the owning file (matching the pattern this phase established) and re-run the sweep until it is clean. Also run `grep -ri "guild" --include="*.cs" QuestBoard.Domain/` and expect zero (RESEARCH.md confirmed Domain has none) as a defensive cross-check.
    (b) Full build — run `dotnet build` from the repo root; expect 0 errors (the stale `bin/obj` staticwebassets manifests referencing the old CSS filenames self-heal on rebuild per RESEARCH.md Runtime State Inventory; a `dotnet clean` first is optional if a stale-manifest 404 appears locally).
    (c) Full test suite — run `dotnet test` from the repo root; expect all three test projects green, 0 failures. This is the final confirmation of the phase's zero-behavior-change guarantee.
    Record the sweep result (0 hits) and the test summary (X passed / 0 failed) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -ri "guild" --include="*.cs" --include="*.cshtml" --include="*.css" QuestBoard.Service/ QuestBoard.IntegrationTests/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "guild" --include="*.cs" --include="*.cshtml" --include="*.css" QuestBoard.Service/ QuestBoard.IntegrationTests/` returns exactly 0 hits
    - `grep -ri "guild" --include="*.cs" QuestBoard.Domain/` returns 0 hits (defensive cross-check)
    - `dotnet build` (repo root) succeeds with 0 errors
    - `dotnet test` (repo root) reports 0 failed tests across all three projects
    - The SUMMARY records the sweep result and the passed/failed test counts
  </acceptance_criteria>
  <done>The zero-guild sweep is clean across both in-scope projects, the solution builds, and the full test suite is green — the rename is verifiably complete with zero behavior change.</done>
</task>

</tasks>

<verification>
- `grep -ri "guild" --include="*.cs" --include="*.cshtml" --include="*.css" QuestBoard.Service/ QuestBoard.IntegrationTests/` returns 0.
- `dotnet build` and `dotnet test` both succeed from the repo root.
- Only remaining "guild" strings repo-wide are in `.planning/` and `README.md` (both intentionally out of scope).
</verification>

<success_criteria>
- Zero "guild" references remain in `QuestBoard.Service/` and `QuestBoard.IntegrationTests/`.
- Full solution builds; full test suite green.
- The one optional `QuestBoardContext.cs` comment is reworded (or documented as already-clean).
- The phase's completeness-over-correctness verification strategy (RESEARCH.md) is satisfied.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

No new symbols — this plan is the completeness/verification gate. Optional wiring change:
- `QuestBoardContext.cs` line-~300 comment reworded from "guild roster" to "character roster" (comment-only, zero behavior change).
</artifacts>

<output>
Create `.planning/phases/58-rename-the-guild-members-feature-to-characters-everywhere-co/58-06-SUMMARY.md` when done.
</output>
