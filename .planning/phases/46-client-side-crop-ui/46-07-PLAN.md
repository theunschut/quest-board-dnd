---
phase: 46-client-side-crop-ui
plan: 07
type: execute
wave: 4
depends_on: [46-02, 46-04, 46-06]
files_modified: []
autonomous: false
requirements: [IMAGE-01, IMAGE-04, IMAGE-05]
must_haves:
  truths:
    - "Real-device access is explicitly confirmed with the user before any device verification is attempted"
    - "On a real iOS Safari device, a real phone-camera portrait photo crops right-side-up (EXIF orientation correct)"
    - "On a real iOS Safari device, a full-resolution 12MP+ camera photo does not crash or blank the crop canvas"
    - "On a real touchscreen, the crop frame responds precisely to drag and pinch gestures"
    - "The end-to-end dual-file submission stores both original and cropped, and the display rule (D-03) shows the right version on every surface"
  artifacts: []
  key_links:
    - from: "device-access confirmation gate"
      to: "the four real-device manual checks"
      via: "blocking decision checkpoint precedes any device test"
      pattern: "device access"
---

<objective>
Perform the phase's real-device verification: EXIF orientation on a real phone-camera photo, iOS Safari canvas-memory ceiling with a full-resolution 12MP+ photo, touch-drag/pinch precision on a real touchscreen, and the end-to-end desktop flow (interactive crop + dual-file submission + D-03 display rule across all surfaces). These are the checks that cannot be automated and that devtools emulation cannot reproduce.

Per 46-CONTEXT.md's Pre-Execution Blocker, real-device access is NOT YET CONFIRMED. This plan opens with a blocking decision checkpoint that halts until the user confirms device access — do NOT attempt the device checks without it. All non-device work (Plans 01-06) is already complete and independently reviewable regardless of this plan's timing.

Purpose: Satisfy phase Success Criterion #4 (real-device checks) and confirm the full user-facing crop loop works.
Output: A human-verified pass (or a recorded issue list) for the crop feature; no source files changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-client-side-crop-ui/46-CONTEXT.md
@.planning/phases/46-client-side-crop-ui/46-VALIDATION.md
@.planning/phases/46-client-side-crop-ui/46-RESEARCH.md
@.planning/phases/46-client-side-crop-ui/46-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm real-device access before any device verification</name>
  <decision>Is real-device access available now to run the touch/EXIF/canvas-memory checks?</decision>
  <context>46-CONTEXT.md's Pre-Execution Blocker states real-device access is NOT YET CONFIRMED. Phase 43 verified its iOS fix on a physical iPhone over LAN (no device cloud). This phase needs the same iPhone for EXIF/canvas-memory/touch checks (and optionally an Android device for a second touch-gesture platform). Devtools "mobile" emulation does NOT reproduce EXIF orientation, the iOS 16.7M-pixel canvas ceiling, or real touch-gesture behavior — so it cannot substitute. Also confirm the iOS version is 16+ (createImageBitmap imageOrientation support; RESEARCH.md Assumption A2 — on iOS 15 or older, EXIF correction would silently fail and a fallback parser would be needed).</context>
  <options>
    <option id="device-available">
      <name>Device access is available now</name>
      <pros>Can complete all four real-device checks and close the phase's Success Criterion #4</pros>
      <cons>None — this is the intended path</cons>
    </option>
    <option id="device-deferred">
      <name>Device access is not available yet — defer the device checks</name>
      <pros>Unblocks shipping the code-complete work (Plans 01-06 are reviewable/mergeable independently); records the device checks as an outstanding verification item</pros>
      <cons>Success Criterion #4 stays open until device access is arranged; the phase is code-complete but not fully verified</cons>
    </option>
  </options>
  <action>Ask the user directly whether real-device access (the Phase-43 iPhone over LAN, iOS 16+, and optionally an Android device) is available right now, and confirm the iOS version is 16 or newer. Do NOT proceed to Task 3's device checks until the user selects device-available. If the user selects device-deferred, skip Task 3 and record the outstanding real-device verification plus the iOS-version question in the SUMMARY so it is not lost.</action>
  <verify>
    <human-check>User has explicitly answered device-available or device-deferred, and (if available) confirmed iOS 16+.</human-check>
  </verify>
  <done>Device-access decision is recorded; execution either proceeds to the device checks or defers them with the outstanding item logged.</done>
  <resume-signal>Select: device-available (proceed to Task 2) or device-deferred (record the outstanding device-verification item in the SUMMARY, confirm the iOS version question for later, and stop — do not attempt device checks)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Desktop end-to-end crop + dual-file + display-rule verification</name>
  <what-built>The crop modal, EXIF/downscale pipeline, dual-file submission, cropped-read endpoints, the square list-card layout, and the D-03 display-rule repoints across all surfaces (Plans 01-06).</what-built>
  <action>Run the app on desktop (`dotnet run --project QuestBoard.Service`; ask the user to stop any Visual Studio debugger first if the build is locked) and walk the user through the how-to-verify steps below, capturing a pass or a specific issue for each surface. This task changes no source — it is verification only.</action>
  <how-to-verify>
    On a desktop browser:
    1. Character Edit: select a photo → confirm the crop modal opens with a draggable/resizable square (1:1) frame → drag and resize it → click "Use This Crop" → submit the form. Then confirm the Characters index thumbnail shows the CROPPED image and the Character Details page shows the ORIGINAL (uncropped) image.
    2. Repeat step 1 for Contact Edit (index thumbnail cropped, Contact Details original) and DM EditProfile (DM Profile page shows the cropped image, per D-03 — DM has no original page).
    3. Character/Contact Create: select a photo, submit WITHOUT touching the crop frame → confirm a centered default 1:1 crop was saved (the thumbnail is square-cropped, not the raw landscape), proving D-04's auto-default.
    4. Confirm the Characters index and Contacts index list cards are SQUARE boxes (not landscape), at both a wide and a narrow window width.
    5. Quest Details (selected + waitlist), Quest Manage, a quest-board card (_QuestCard), and QuestLog Details recap: confirm participant avatars show the cropped image.
    6. "Discard Photo" in the modal: select a photo, open modal, click Discard → confirm the file selection is cleared (no upload happens on submit).
  </how-to-verify>
  <verify>
    <human-check>User confirms all six desktop scenarios behave as described (or reports specific failures).</human-check>
  </verify>
  <done>The desktop crop flow, dual-file submission, square list-card, and D-03 display rule are confirmed working (or issues are recorded).</done>
  <resume-signal>Type "approved" or describe issues (which surface, expected vs actual)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-device iOS Safari + touchscreen verification (only if Task 1 = device-available)</name>
  <what-built>The EXIF-orientation correction (createImageBitmap imageOrientation), the ≤2400px downscale-before-canvas step (iOS canvas-memory ceiling mitigation), and Cropper.js v2's touch/pointer crop-frame interaction with the touch-action:none stage fix.</what-built>
  <action>Only if Task 1 resolved to device-available: guide the user through the real-device checks below on the Phase-43 iPhone over LAN (iOS 16+). If Task 1 resolved to device-deferred, SKIP this task entirely and ensure the outstanding real-device verification is recorded in the SUMMARY. This task changes no source — it is verification only.</action>
  <how-to-verify>
    On a real iOS device (the Phase-43 iPhone) over LAN, iOS Safari, iOS 16+ (verified in Task 1). If Task 1 selected "device-deferred", SKIP this task and record it as outstanding.
    1. EXIF orientation: take a NEW portrait photo directly with the phone camera (do not use a pre-rotated/screenshot image), upload it via the mobile Character Edit crop flow, save, and confirm it displays right-side-up everywhere — NOT sideways or upside-down.
    2. Canvas-memory ceiling: upload a full-resolution 12MP+ photo straight from the iPhone camera roll (not a downscaled copy); confirm the crop modal renders the image and does not crash, blank, or freeze the crop canvas.
    3. Touch-drag/pinch precision: on the real touchscreen, drag the crop frame and pinch/resize it; confirm smooth, precise tracking with no missed or laggy gestures, and that dragging the frame does not accidentally scroll the modal/page.
    4. Optional (if an Android device is available): repeat step 3 on Android Chrome for a second touch-gesture platform.
  </how-to-verify>
  <verify>
    <human-check>User confirms EXIF orientation, canvas-memory stability, and touch precision on a real iOS device (or reports failures / records the check as deferred).</human-check>
  </verify>
  <done>The three real-device-only checks pass on a real iOS device, or are explicitly recorded as outstanding pending device access.</done>
  <resume-signal>Type "approved" or describe issues (which check, device/iOS version, expected vs actual). If EXIF correction fails on iOS 15 or older, note it — the RESEARCH.md fallback parser (Assumption A2) becomes a follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification-only) | This plan runs verifications; it modifies no source and introduces no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-07-01 | Denial of Service (client) | iOS Safari canvas-memory ceiling under a real 12MP photo | mitigate | Verified directly in Task 3 against a real full-resolution camera photo — the mitigation (≤2400px downscale + explicit memory release, Plan 05) is confirmed on the actual constrained runtime, not emulation. |
| T-46-07-02 | Tampering | EXIF orientation silently wrong on old iOS | mitigate | Task 1 confirms iOS 16+; Task 3 verifies a real phone-camera photo crops right-side-up. If iOS < 16 is the only device, the RESEARCH.md fallback becomes a recorded follow-up rather than a silent failure. |
| T-46-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install in this plan. N/A. |
</threat_model>

<verification>
- Task 1 device-access gate resolved (available or deferred, explicitly).
- Desktop end-to-end flow approved (Task 2).
- Real-device checks approved or recorded as outstanding (Task 3).
- Full `dotnet test` suite green before this phase is considered done.
</verification>

<success_criteria>
- The interactive crop, dual-file submission, and D-03 display rule are confirmed working end-to-end on desktop.
- The three real-device-only checks (EXIF, canvas memory, touch precision) pass on a real iOS device — or are explicitly recorded as outstanding pending device access.
</success_criteria>

<output>
Create `.planning/phases/46-client-side-crop-ui/46-07-SUMMARY.md` when done. If Task 1 = device-deferred, the SUMMARY must record the outstanding real-device verification item and the iOS-version question so it is not lost.
</output>
