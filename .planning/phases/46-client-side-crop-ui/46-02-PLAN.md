---
phase: 46-client-side-crop-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/wwwroot/css/characters.css
  - QuestBoard.Service/wwwroot/css/contacts.css
autonomous: true
requirements: [IMAGE-04]
must_haves:
  truths:
    - "The Characters index and Contacts index list-card image containers render as a 1:1 square box at any grid column width, not a fixed-height landscape box"
    - "The mobile responsive override no longer forces a fixed landscape height that would re-crop the square"
  artifacts:
    - path: "QuestBoard.Service/wwwroot/css/characters.css"
      provides: ".character-image square aspect-ratio box"
      contains: "aspect-ratio: 1 / 1"
    - path: "QuestBoard.Service/wwwroot/css/contacts.css"
      provides: ".contact-image square aspect-ratio box"
      contains: "aspect-ratio: 1 / 1"
  key_links:
    - from: ".character-image"
      to: "square crop display"
      via: "aspect-ratio 1/1 replacing height 200px"
      pattern: "aspect-ratio:\\s*1\\s*/\\s*1"
---

<objective>
Change the Characters and Contacts list-card image containers from a fixed-height landscape box (`width: 100%; height: 200px`) to a 1:1 square aspect-ratio box, so a square-cropped photo displays without `object-fit: cover` re-cropping it top/bottom to fill a wider box (per D-02). Remove the mobile `@media` overrides that hardcode a fixed landscape height, since the `aspect-ratio` rule already sizes the box responsively at any column width.

Purpose: Make the list-card display shape honor the user's square framing (D-02) — a pure CSS layout change independent of all backend and view work.
Output: Two modified CSS files with square-box containers and removed mobile height overrides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-client-side-crop-ui/46-CONTEXT.md
@.planning/phases/46-client-side-crop-ui/46-UI-SPEC.md
@.planning/phases/46-client-side-crop-ui/46-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert .character-image and .contact-image to a 1:1 square box and remove the mobile height overrides</name>
  <files>QuestBoard.Service/wwwroot/css/characters.css, QuestBoard.Service/wwwroot/css/contacts.css</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/characters.css (the `.characters-page .character-image` rule at lines 112-125, and the `@media (max-width: 768px)` override at lines 278-285 that sets `height: 180px`)
    - QuestBoard.Service/wwwroot/css/contacts.css (the `.contact-image` rule at lines 91-121, and its `@media (max-width: 768px)` override at lines 218-225 with the same `height: 180px`)
    - .planning/phases/46-client-side-crop-ui/46-UI-SPEC.md (section "4. List-Card Square Box (D-01/D-02)" — exact before/after and the explicit mobile-override removal instruction)
    - .planning/phases/46-client-side-crop-ui/46-PATTERNS.md (section "characters.css" — the verbatim square-box change and the "delete, not change" instruction for the mobile override)
  </read_first>
  <action>In `characters.css`, in the `.characters-page .character-image` rule, replace the `height: 200px;` declaration with `aspect-ratio: 1 / 1;` followed by `height: auto;`. Keep every other property in the rule unchanged (`position: relative`, `width: 100%`, `margin-bottom`, `border-radius: 8px`, `overflow: hidden`, the `linear-gradient` background, `display: flex`, `align-items`, `justify-content`, `box-shadow`, `transition`, and any hover-state `transform: scale(...)`). Then in the `@media (max-width: 768px)` block (around lines 278-285), DELETE the `height: 180px;` line entirely (remove the line, do not change its value) — the aspect-ratio rule handles responsive square sizing. If that mobile block contains only the `height: 180px` declaration for `.character-image` and becomes empty after removal, remove the now-empty selector too. Apply the identical two changes to `contacts.css`'s `.contact-image` rule (square box) and its `@media (max-width: 768px)` override (delete the `height: 180px` line). Do not touch `dm-profile.css` / `dm-profile.mobile.css` — DM's circle box is already square. Do NOT embed any GSD/phase-ID references in CSS comments per CLAUDE.md.</action>
  <verify>
    <automated>grep -c "aspect-ratio: 1 / 1" QuestBoard.Service/wwwroot/css/characters.css && grep -c "aspect-ratio: 1 / 1" QuestBoard.Service/wwwroot/css/contacts.css</automated>
  </verify>
  <acceptance_criteria>
    - `characters.css` `.characters-page .character-image` rule contains `aspect-ratio: 1 / 1;` and `height: auto;` and no longer contains `height: 200px;`.
    - `contacts.css` `.contact-image` rule contains `aspect-ratio: 1 / 1;` and no longer contains `height: 200px;`.
    - Neither file contains a `height: 180px;` declaration inside a `@media (max-width: 768px)` block for the image container.
    - Running `grep "height: 200px" QuestBoard.Service/wwwroot/css/characters.css QuestBoard.Service/wwwroot/css/contacts.css` returns no line for the `.character-image`/`.contact-image` containers.
    - All other properties (border-radius, overflow, background gradient, box-shadow, transition, hover transform) remain present on both rules.
  </acceptance_criteria>
  <done>Both list-card image containers are 1:1 square boxes with the fixed mobile-height override removed; DM CSS untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static CSS change only — no untrusted input, no data flow, no new dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | Tampering | Static CSS assets | accept | No trust boundary is crossed by a layout-only CSS change; no input is processed and no new asset is fetched. |
| T-46-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install in this plan. N/A. |
</threat_model>

<verification>
- `grep` confirms `aspect-ratio: 1 / 1` present in both files and `height: 200px` / mobile `height: 180px` removed from the image containers.
- Visual confirmation of the square list-card is part of the human-verify checkpoint in Plan 07 (not gated here).
</verification>

<success_criteria>
- Characters and Contacts index list-card image boxes are square (1:1) at all widths.
- No fixed-height landscape override remains that would re-crop the square.
</success_criteria>

<output>
Create `.planning/phases/46-client-side-crop-ui/46-02-SUMMARY.md` when done.
</output>
