---
phase: 78-link-preview-foundation-and-quest-cards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/server-setup.md
autonomous: false
requirements: [LINKPREV-01]
user_setup:
  - service: app-container-environment-file
    why: "Forwarded-header trust is set per-environment in /etc/questboard/env on the App CT and cannot be read or written from the repository. On this runtime an empty known-proxy list makes the app drop every forwarded header, which would make the scheme/host fix a no-op and means per-client rate limiting is already collapsed into one shared bucket."
    env_vars:
      - name: ReverseProxy__KnownProxies__0
        source: "/etc/questboard/env on the App CT — value is the Traefik CT's IP address"
      - name: EmailSettings__AppUrl
        source: "/etc/questboard/env on the App CT — value is the public https base URL, e.g. https://<production-host>"

must_haves:
  truths:
    - "The operator has stated, from the live App CT environment file, whether ReverseProxy__KnownProxies__0 is set and what kind of value it holds."
    - "The operator has stated, from the same file, whether EmailSettings__AppUrl is set and whether it is an https URL on the production hostname."
    - "docs/server-setup.md's environment-file template lists every environment variable the running app actually depends on, including the public base URL."
  artifacts:
    - "docs/server-setup.md updated environment-file block and reverse-proxy note"
  key_links:
    - "The reverse-proxy note in docs/server-setup.md connects an unset known-proxy value to two concrete consequences: broken per-client rate limiting and wrong absolute URLs in link-preview metadata."
  prohibitions:
    - "Do not guess, infer, or assert the production value of either variable from anything in the repository — appsettings.json and this doc are both known to be incomplete relative to the live environment file."
    - "Do not resolve an unset known-proxy value by clearing the trust list or by trusting all proxies in code."
    - "Do not add a second configuration key meaning 'public base URL'; EmailSettings:AppUrl stays the single answer."
---

<objective>
Settle, from the live server rather than from the repository, whether the two environment variables this phase's absolute URLs depend on are actually set — then record the answer in the deployment doc so the next person does not have to ask again.

This is a pre-flight, not a wrap-up. If `ReverseProxy__KnownProxies__0` is unset, the forwarded-scheme/host fix shipped in plan 78-01 is a no-op *and* the per-client rate limiting shipped in an earlier phase has already silently collapsed into a single shared bucket. That is a pre-existing production defect this phase would otherwise inherit blind, and it is cheap to settle.

Purpose: remove the last unverified assumption underneath every absolute URL this phase generates.
Output: an operator-confirmed answer for both variables, and a `docs/server-setup.md` whose environment-file template is complete.

Decisions implemented (see 78-CONTEXT.md): D-06, D-07
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-link-preview-foundation-and-quest-cards/78-CONTEXT.md
@docs/server-setup.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm forwarded-header trust and the public base URL on the App CT</name>
  <files>none — this task inspects the live server environment file, not the repository</files>
  <read_first>
    - docs/server-setup.md § "Create the environment file" — the template that names both variables
    - .planning/phases/78-link-preview-foundation-and-quest-cards/78-RESEARCH.md § Common Pitfalls → Pitfall 1 — why an empty trusted-proxy list silently drops every forwarded header on this runtime
  </read_first>
  <action>
    Pause and present the verification steps below to the operator. Do not attempt to read, guess, or infer either value from the repository — the checked-in configuration is known to be incomplete relative to the live environment file, and a guess here would defeat the entire purpose of the check. Wait for the operator's report, then record their exact answer for both variables in this plan's SUMMARY before continuing.
  </action>
  <acceptance_criteria>
    - The operator has reported, for `ReverseProxy__KnownProxies__0`: present or absent, and whether its value is a real address rather than the setup document's placeholder.
    - The operator has reported, for `EmailSettings__AppUrl`: present or absent, and whether it is an https URL on the production hostname.
    - If either was absent, the operator has stated whether they set it and restarted the service.
    - Both answers are recorded verbatim in the plan SUMMARY.
  </acceptance_criteria>
  <verify>
    <human-check>Operator reports the state of both environment variables from the live App CT.</human-check>
  </verify>
  <done>The state of both environment variables in production is a recorded fact rather than an assumption, and any gap found has been closed.</done>
  <what-built>
    Nothing is built by this task. Plan 78-01 extended the application's forwarded-header trust to cover scheme and host. That change only has an effect if the App CT's environment file names the reverse proxy as a trusted source, and the card metadata this phase generates only lands on the right hostname if the public base URL is set. Neither value lives in the repository, and neither has been confirmed since 2026-07-01.
  </what-built>
  <how-to-verify>
    On the App CT (the container/VM that runs the questboard systemd service):

    1. Run `sudo grep -E 'ReverseProxy__KnownProxies__0|EmailSettings__AppUrl' /etc/questboard/env`
    2. Report back, for each of the two variables: present or absent, and — for the known-proxy one — whether the value is a real IP address rather than the literal placeholder text from the setup doc; for the app-url one, whether it is an `https://` URL on the production hostname rather than a localhost address.
    3. If `ReverseProxy__KnownProxies__0` is absent or still a placeholder: set it to the Traefik CT's IP address, then `sudo systemctl restart questboard`, and report that you did so. This is also the fix for the already-broken per-client rate limiting, not only for this phase.
    4. If `EmailSettings__AppUrl` is absent: set it to the public `https://` base URL with no trailing slash, restart the service, and report that you did so. Production email links currently work, which is strong evidence it is already set — confirm rather than assume.

    Do not paste secrets from the file. Only the two variable names, present/absent, and the shape of each value are needed.
  </how-to-verify>
  <resume-signal>Report both variables as "set" / "absent, now set" / "absent, left unset", plus the shape of each value. Type "approved" once reported.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the confirmed environment contract in the deployment doc</name>
  <files>docs/server-setup.md</files>
  <read_first>
    - docs/server-setup.md § "Create the environment file" (the `cat > /etc/questboard/env` block) and the reverse-proxy note further down the same file
    - QuestBoard.Service/appsettings.json (the EmailSettings block — the checked-in AppUrl is a localhost development value and is overridden by the environment file)
    - .planning/phases/78-link-preview-foundation-and-quest-cards/78-CONTEXT.md § D-06 and D-07
  </read_first>
  <action>
    Update the environment-file template in `docs/server-setup.md` so it lists `EmailSettings__AppUrl` alongside the existing `EmailSettings__*` entries, with a placeholder of the form `https://<PUBLIC_HOSTNAME>` and no trailing slash. Add a short sentence directly under the block stating that the checked-in `appsettings.json` value is a development localhost URL and that the environment file is the production source of truth for the app's public base URL.

    Extend the existing reverse-proxy note so it names all three consequences of leaving the known-proxy value unset on this runtime: the app sees every request as coming from the proxy so per-client rate limiting collapses into one shared bucket; the forwarded scheme and host are dropped so any absolute URL derived from the request is wrong; and — stated plainly — the runtime drops forwarded headers entirely when the trusted list is empty, so the failure is silent rather than an error.

    Record the outcome of task 1 in this plan's SUMMARY, not in the doc: the doc describes the required contract, the SUMMARY records what the live server was found to hold.

    Write the doc in plain language with no phase numbers, plan numbers, or requirement identifiers.
  </action>
  <acceptance_criteria>
    - `grep -c 'EmailSettings__AppUrl' docs/server-setup.md` outputs at least 1.
    - `grep -c 'ReverseProxy__KnownProxies__0' docs/server-setup.md` outputs at least 2 — the template entry and the explanatory note.
    - `grep -ciE 'silent|silently' docs/server-setup.md` outputs at least 1 within the reverse-proxy note context.
    - `grep -cE 'LINKPREV-|Phase 7[0-9]|D-0[0-9]' docs/server-setup.md` outputs 0.
    - The environment-file code block still contains every variable it contained before the edit — nothing removed.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'EmailSettings__AppUrl' docs/server-setup.md && grep -c 'ReverseProxy__KnownProxies__0' docs/server-setup.md</automated>
  </verify>
  <done>The deployment doc's environment-file template is complete, and the reverse-proxy note explains the silent-failure mode in terms a future operator can act on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reverse proxy → Kestrel | Forwarded scheme/host/client-IP headers cross here; the trusted-source list is the only control |
| operator → App CT environment file | Deployment configuration that the repository cannot read, verify, or enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-05 | Spoofing | `ReverseProxy:KnownProxies` on the App CT | high | mitigate | Operator confirms the value names the real proxy address; the runtime's fail-closed behaviour means the wrong answer degrades URLs rather than trusting a spoofed host, but the same setting is what partitions rate limiting by real client IP, so an unset value is a live authorization-adjacent defect |
| T-78-10 | Denial of Service | forgot-password / set-password rate limiters | high | mitigate | Same setting. With no trusted proxy, every visitor shares one rate-limit bucket, which both lets one attacker exhaust the budget for everyone and lets distributed attempts hide inside it. Task 1 is the fix path |
| T-78-11 | Information Disclosure | environment file contents during verification | low | mitigate | The checkpoint asks only for variable names, present/absent, and value shape — never for pasted secrets |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Owned by this plan:

| Symbol / path | Kind |
|---|---|
| `docs/server-setup.md` environment-file template entry `EmailSettings__AppUrl` | documented config key |
| `docs/server-setup.md` extended reverse-proxy note for `ReverseProxy__KnownProxies__0` | documented config key |

Produced elsewhere in Phase 78 (do not create here):

| Symbol / path | Owner plan |
|---|---|
| `ForwardedHeadersOptions.ForwardedHeaders` = for \| proto \| host, `AddDataProtectionKeys` migration, `QuestBoardContext.DataProtectionKeys` | 78-01 |
| `[Authorize]` on `QuestController.Details` GET, `Details_Anonymous_RedirectsToLogin` | 78-03 |
| `IActiveGroupContext.SetGroupId(int?)`, `MutableGroupContext.SetGroupId` and 11 unit-test doubles | 78-04 |
| `ILinkSigningService`, `LinkSigningService`, `ShareLinkEntityTypes.Quest`, `ILinkCardDescriptionService`, `LinkCardDescriptionService`, `IPublicUrlBuilder`, `PublicUrlBuilder` | 78-05 |
| `wwwroot/images/link-preview-card.png`, `tools/link-preview-card/generate.py`, `BrandedImageServingTests` | 78-06 |
| `QuestPreviewController`, route `/s/quest/{token}`, `QuestCardViewModel`, `Views/LinkPreview/QuestCard.cshtml`, `GroupSessionMiddleware` `/s` exemption, `QuestPreviewControllerTests` | 78-07 |
| `Views/Shared/_ShareLinkButton.cshtml`, `ViewBag.ShareLinkUrl`, shared copy handler in `site.js`, `QuestDetailsCopyLinkTests` | 78-08 |
| `docs/link-previews.md` and the deployed-host acceptance pass | 78-09 |
</artifacts_produced>

<verification>
- The operator has reported the state of both environment variables from the live App CT.
- `docs/server-setup.md` lists both variables and explains the silent-failure mode.
- No planning identifiers appear in the doc.
</verification>

<success_criteria>
Whether forwarded-header trust is actually configured in production is a known fact rather than an assumption, any gap found has been closed by the operator, and the deployment doc now describes the full environment contract.
</success_criteria>

<output>
Create `.planning/phases/78-link-preview-foundation-and-quest-cards/78-02-SUMMARY.md` when done. Record the operator's answer for both variables verbatim in the SUMMARY — this is the artifact that closes a verification item outstanding since 2026-07-01.
</output>
