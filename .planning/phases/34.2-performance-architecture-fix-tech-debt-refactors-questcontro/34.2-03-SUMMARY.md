---
phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro
plan: 03
subsystem: database
tags: [ef-core, migrations, hangfire, sql-server, query-optimization]

# Dependency graph
requires:
  - phase: 34.2-01
    provides: rollback-safety fix in QuestService.CreateFollowUpQuestWithDetailsAsync
  - phase: 34.2-02
    provides: ControllerExtensions MVC boilerplate helpers
provides:
  - Non-unique composite index on Quests(IsFinalized, FinalizedDate) via EF migration
  - Shop list query (GetAllAsync) no longer eagerly loads the Transactions collection
  - Global Hangfire AutomaticRetryAttribute (5 attempts, 1/2/4/8/16s backoff)
  - Documented per-entity EF Core query-filter rationale in OnModelCreating
affects: [34.2-04, 34.2-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Global Hangfire retry policy registered via GlobalJobFilters.Filters.Add inside the non-Testing guard"
    - "List-view repository methods scoped to omit collections only the detail view needs"

key-files:
  created:
    - QuestBoard.Repository/Migrations/20260702081517_AddQuestFinalizedDateIndex.cs
    - QuestBoard.Repository/Migrations/20260702081517_AddQuestFinalizedDateIndex.Designer.cs
  modified:
    - QuestBoard.Repository/Entities/QuestBoardContext.cs
    - QuestBoard.Repository/Migrations/QuestBoardContextModelSnapshot.cs
    - QuestBoard.Repository/ShopRepository.cs
    - QuestBoard.Service/Program.cs

key-decisions:
  - "ShopItemEntity has no image BLOB column of its own (verified against the live entity) — the actionable list-view over-fetch was the eager .Include(si => si.Transactions) in GetAllAsync, not an image load; removed it there instead of chasing a non-existent BLOB navigation"
  - "Verified ShopManagementController.Index (GetAllAsync's only caller) renders Name/Status/Price grouping and never reads ShopItem.Transactions, so the include could be safely dropped without a projected/count-only fallback"
  - "Used new[] { 1, 2, 4, 8, 16 } for AutomaticRetryAttribute.DelaysInSeconds to match the plan's exact API shape"

patterns-established:
  - "Query-filter documentation block in OnModelCreating explains which entities are group-scoped, which are intentionally excluded (UserEntity/Identity), and which are transitively scoped through a filtered parent (Character/PlayerSignup)"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-07-02
---

# Phase 34.2 Plan 03: Repository/Config Performance & Reliability Fixes Summary

**Composite Quests index, leaner shop list query, global Hangfire exponential-backoff retry, and query-filter documentation — all four Repository/config-tier CONCERNS.md items closed in one disjoint plan.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-02T08:06:00Z
- **Completed:** 2026-07-02T08:18:35Z
- **Tasks:** 4
- **Files modified:** 6 (2 new migration files, 4 modified)

## Accomplishments
- Added a non-unique composite index on `Quests(IsFinalized, FinalizedDate)` via a clean, reversible EF Core migration (D-07 — scale-readiness lever, implemented unconditionally)
- Removed the eager `.Include(si => si.Transactions)` from `ShopRepository.GetAllAsync`, the one list-returning query that was over-fetching a related collection only the detail view needs (D-08)
- Registered a global Hangfire `AutomaticRetryAttribute` (5 attempts, 1/2/4/8/16s exponential backoff) inside the existing non-Testing guard, using the confirmed real API instead of the nonexistent `UseAutoRetry`
- Documented the per-entity EF Core query-filter rationale in `OnModelCreating` — why Quest/ShopItem are filtered, why UserEntity is not, and why Character/PlayerSignup are transitively scoped through their filtered parents — with no behavior change and no ID tags

## Task Commits

Each task was committed atomically:

1. **Task 1: Add composite index on Quests(IsFinalized, FinalizedDate) + generate migration** - `69379a6` (feat)
2. **Task 2: Project shop list queries to avoid unnecessary BLOB loading (D-08)** - `95085a0` (perf)
3. **Task 3: Register global Hangfire AutomaticRetryAttribute retry policy in Program.cs** - `bcb1b1d` (feat)
4. **Task 4: Document the per-entity query-filter decisions in OnModelCreating** - `aed08be` (docs)

**Plan metadata:** pending (docs: complete plan, committed after this summary)

## Files Created/Modified
- `QuestBoard.Repository/Entities/QuestBoardContext.cs` - New `HasIndex` on `QuestEntity(IsFinalized, FinalizedDate)`; extended query-filter documentation block
- `QuestBoard.Repository/Migrations/20260702081517_AddQuestFinalizedDateIndex.cs` - New migration, `Up()` creates the index, `Down()` drops it
- `QuestBoard.Repository/Migrations/20260702081517_AddQuestFinalizedDateIndex.Designer.cs` - EF-generated migration metadata
- `QuestBoard.Repository/Migrations/QuestBoardContextModelSnapshot.cs` - Regenerated by `dotnet ef migrations add` tooling
- `QuestBoard.Repository/ShopRepository.cs` - `GetAllAsync` drops `.Include(si => si.Transactions)`; `GetItemWithDetailsAsync` unchanged
- `QuestBoard.Service/Program.cs` - `GlobalJobFilters.Filters.Add(new AutomaticRetryAttribute { ... })` inside the non-Testing Hangfire block

## Decisions Made
- Scoped the D-08 fix to the confirmed real over-fetch (`Transactions` include in `GetAllAsync`) rather than a hypothetical image-BLOB load, since `ShopItemEntity` has no image column — verified directly against the entity file before editing, per the plan's read-first instruction.
- Confirmed via `FindReferences`-equivalent grep that `ShopManagementController.Index` is `GetAllAsync`'s only caller and does not read `Transactions`, so the include could be removed outright rather than replaced with a projected/count-only load.

## Deviations from Plan

None - plan executed exactly as written. The plan's own "read_first" guidance anticipated the ShopItemEntity BLOB-column verification and the AutomaticRetryAttribute API correction; both were confirmed true during execution, not discovered as deviations.

## Issues Encountered

None. Build was not locked (no Visual Studio debugger running), so no CLAUDE.md build-lock stop was needed.

## User Setup Required

None - no external service configuration required. The new migration auto-applies via the existing `context.Database.Migrate()` call on next app startup; no manual `database update` step needed per CLAUDE.md.

## Next Phase Readiness

- All four artifacts (`AddQuestFinalizedDateIndex` migration, Hangfire retry filter, shop list projection, query-filter docs) are in place and build/test-verified.
- Full solution build (6 projects) and full test suite (62 unit + 201 integration tests) both green after this plan — no regressions in reminder/finalize or shop flows.
- Plan 05 (per 34.2-CONTEXT.md D-06/Claude's Discretion) still needs to add the runtime `ActiveGroupId` null-guard at read-sites and the Hangfire-retry-behavior + group-query-filter-enforcement tests — this plan intentionally left those out (documented in the plan's `<artifacts_produced>` as landing in Plan 05).

---
*Phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro*
*Completed: 2026-07-02*

## Self-Check: PASSED

All 5 created/modified source files and the SUMMARY.md itself verified present on disk. All 4 task commit hashes (`69379a6`, `95085a0`, `bcb1b1d`, `aed08be`) verified present in git log.
