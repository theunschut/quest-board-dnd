---
phase: 34.2-performance-architecture-fix-tech-debt-refactors-questcontro
verified: 2026-07-02T08:49:41Z
resolved: 2026-07-02T10:57:00Z
status: passed
score: 18/18 must-haves verified
overrides_applied: 0
gaps: []
human_verification: []
---

## Post-Verification Resolution

The `RequireActiveGroupId()` adoption gap flagged below (must-have #14 / anti-pattern row) was routed to human verification. The user chose "Fix now": `QuestController.SendReminder` (line 713) now calls `activeGroupContext.RequireActiveGroupId()` instead of `activeGroupContext.ActiveGroupId ?? 1`, committed at `81f57e9`. Full build clean (0 errors/warnings), full test suite green (107 unit + 203 integration, 0 regressions). Status upgraded from `human_needed` to `passed`; see `34.2-HUMAN-UAT.md` for the resolution record.

# Phase 34.2: Performance & Architecture Verification Report

**Phase Goal:** Fix the Tech Debt, Performance Bottlenecks, Fragile Areas, Scaling Limits, and Dependencies at Risk items catalogued in `.planning/codebase/CONCERNS.md`, plus remaining Test Coverage Gaps: `QuestController`/`AdminController` cleanup via selective service-layer extraction + MVC-boilerplate helpers (no physical controller split, per 34.2-CONTEXT.md D-01/D-02), `DateTime.Now` → `UtcNow` fix in `ShopSeedService`, follow-up quest two-phase-update consolidation into a service method, composite index on `Quests(IsFinalized, FinalizedDate)`, shop-item query projection, Hangfire job scope-management helper + `AutomaticRetryAttribute` retry policy, EF Core Global Query Filter documentation, AutoMapper enum-cast validation test, `ActiveGroupId` null-guard, and dependency migration-plan documentation. Cross-controller `Forbid()` defense-in-depth checks and Hangfire job-queue batching are documentation-only in this phase (deferred code implementation, per D-06/D-09).

**Verified:** 2026-07-02T08:49:41Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Follow-up quest create+rollback consolidated into a single `QuestService` method | VERIFIED | `QuestService.CreateFollowUpQuestWithDetailsAsync` (QuestService.cs:243-277) composes `CreateFollowUpQuestAsync` + `UpdateQuestPropertiesWithNotificationsAsync`, rolls back via `RemoveAsync` in a nested try/catch that never masks the primary exception. `QuestController.CreateFollowUp` (QuestController.cs:802-876) is thinned to validate→delegate→redirect/re-render; no `RemoveAsync`/`UpdateQuestPropertiesWithNotificationsAsync` remains in the controller. |
| 2 | `ShopSeedService` seeds using UTC time, not machine-local time | VERIFIED | `ShopSeedService.cs:224-225` uses `DateTime.UtcNow.AddDays(...)`; zero `DateTime.Now` occurrences remain in the file. |
| 3 | Net-new `ControllerExtensions` MVC-boilerplate helper exists and is applied to `AdminController` | VERIFIED | `QuestBoard.Service/Extensions/ControllerExtensions.cs` defines `RedirectWithMessage`/`RedirectWithSuccess`/`RedirectWithError`/`TryReturnInvalidModel`; `AdminController.cs` calls these at 8 sites (`CreateUser`, `EditUser`, `ResetPassword`, `SendConfirmationEmail`). |
| 4 | `AdminController`'s Resend/EmailStats logic is untouched by the refactor | VERIFIED | `EmailStats`/`GetResendStatsAsync` region unchanged; git log shows only `89dca20` (Task 2 of Plan 02) touching the file after Phase 34.1's Resend extraction (`23f2f1e`), and the diff is confined to the TempData/ModelState call sites. |
| 5 | Composite index on `Quests(IsFinalized, FinalizedDate)` exists via a clean EF migration | VERIFIED | `QuestBoardContext.cs:195` has the `HasIndex` call; migration `20260702081517_AddQuestFinalizedDateIndex.cs` creates/drops `IX_Quests_IsFinalized_FinalizedDate`; `QuestBoardContextModelSnapshot.cs:445` reflects it. |
| 6 | Shop list-view queries no longer eagerly load data only detail views need | VERIFIED | `ShopRepository.GetAllAsync` (ShopRepository.cs:14-21) no longer includes `.Include(si => si.Transactions)`; `GetItemWithDetailsAsync` (line 62-70) still includes `Transactions.ThenInclude(User)` for the detail view. |
| 7 | Hangfire jobs retry transient failures via a global `AutomaticRetryAttribute` (not `UseAutoRetry`) | VERIFIED | `Program.cs:241` — `GlobalJobFilters.Filters.Add(new AutomaticRetryAttribute { Attempts = 5, DelaysInSeconds = new[] { 1, 2, 4, 8, 16 } });` inside the non-Testing Hangfire guard. Zero `UseAutoRetry` occurrences anywhere. |
| 8 | `OnModelCreating` documents why Quest/ShopItem are filtered and User is not | VERIFIED | `QuestBoardContext.cs:237-259` — descriptive prose comments (no `TENANT-`/`D-`/`Phase` tags), CRITICAL landmine comment preserved, UserEntity exclusion rationale extended to cover Character/PlayerSignup transitive scoping. |
| 9 | `HangfireJobHelper` formalizes the scope+group-context pattern; all 3 jobs use it | VERIFIED | `HangfireJobHelper.RunInScopeAsync` (HangfireJobHelper.cs:18-33) conditionally calls `SetGroupId` only when `groupId` is non-null; `SessionReminderJob`, `DailyReminderJob` (groupId: null), and `QuestFinalizedEmailJob` all call it — confirmed by grep, no inline `CreateAsyncScope()` prologue remains in any of the three. |
| 10 | Documentation note: `Forbid()` defense-in-depth is deferred; EF query filter is current safety net (D-06) | VERIFIED | `ARCHITECTURE.md:260-266` "Deferred Hardening / Scaling Notes → Tenant-isolation defense-in-depth (deferred)" states this exactly; `CONCERNS.md` Scaling Limits item has an appended "Status (Phase 34.2): documented as deferred" line. No `Forbid()` code was added anywhere (grep confirms). |
| 11 | Documentation note: Hangfire fan-out is the correct pattern, batching is monitored future work (D-09) | VERIFIED | `ARCHITECTURE.md:268-275` "Hangfire fan-out is intentional" states this exactly; `CONCERNS.md` batching item has the matching status line. No `SessionReminderBatchJob` exists anywhere. |
| 12 | Dependency migration-plan documentation (Identity email routing, Resend SPOF) exists | VERIFIED | `ARCHITECTURE.md:277-281` "Dependencies at risk" documents both items with the retry-based mitigation. |
| 13 | Enum append-only/no-reorder convention documented; validated by a round-trip test | VERIFIED | `CONVENTIONS.md:143-147` "Enum Handling" section; `EntityProfileEnumCastTests.cs` covers all 10 EntityProfile-cast enums (42 values) via `[Theory]`/`[MemberData]`, asserting `(T)(int)value == value` and `Enum.IsDefined`. |
| 14 | `RequireActiveGroupId()` guard exists, throws on unexpected-null, doesn't alter the null-see-all contract | VERIFIED (as a seam) | `ActiveGroupContextExtensions.cs:14-16` implements the guard exactly as specified; `QuestBoardContext.HasQueryFilter`'s null-see-all predicate is byte-for-byte unchanged (confirmed at lines 244-252); unit test (`ActiveGroupContextExtensionsTests.cs`) and integration companion test (`TenantIsolationTests.RequireActiveGroupId_NullContext_Throws`) both pass. **However:** the guard has zero production call sites — see Gaps/Human Verification below. |
| 15 | Group Query Filter enforcement test proves non-existent group → empty; null → see-all preserved | VERIFIED | `TenantIsolationTests.GroupFilter_NonExistentGroup_ReturnsEmpty` (new) sets `ActiveGroupId = 999` and asserts the seeded quest is absent; existing `GroupFilter_NullGroupIdShowsAllGroups` unchanged and still passes. |
| 16 | Hangfire retry policy test proves 5 attempts / 1,2,4,8,16s backoff using the real `AutomaticRetryAttribute` | VERIFIED | `HangfireRetryPolicyTests.cs` asserts `Attempts == 5` and `DelaysInSeconds` equals `[1,2,4,8,16]` against the real `Hangfire.AutomaticRetryAttribute` type. |
| 17 | No `(D-xx)`/phase/requirement-ID comment introduced in any phase-touched file | VERIFIED | Scanned all 18 source/test files touched by this phase's 5 plans for `(D-[0-9]`, `Phase [0-9]`, `TENANT-`, `WR-[0-9]`, `GROUP-[0-9]`, `SESSION-[0-9]`, `EMAIL-[0-9]`, `REMIND-[0-9]` — zero matches. Also scanned for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — zero matches. |
| 18 | Full build and test suite are green (no regressions) | VERIFIED | `dotnet build`: 6 projects, 0 errors, 0 warnings. `dotnet test`: 107/107 unit tests, 203/203 integration tests passed (0 failed, 0 skipped). |

**Score:** 17/18 truths fully verified without qualification; 1 truth (#14) verified at the artifact/test level but with a live-code gap noted below.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `QuestBoard.Domain/Services/QuestService.cs` | `CreateFollowUpQuestWithDetailsAsync` | VERIFIED | Present, substantive, wired from controller |
| `QuestBoard.Domain/Interfaces/IQuestService.cs` | Matching interface contract, ID-tag-free doc | VERIFIED | Present, plain `<summary>`, no tags |
| `QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` | Thinned `CreateFollowUp` | VERIFIED | Delegates to service, catches `InvalidOperationException` |
| `QuestBoard.Domain/Services/ShopSeedService.cs` | `DateTime.UtcNow` | VERIFIED | Both occurrences fixed |
| `QuestBoard.Service/Extensions/ControllerExtensions.cs` | MVC boilerplate helpers | VERIFIED | 4 extension methods, no hard-coded action names |
| `QuestBoard.Service/Controllers/Admin/AdminController.cs` | Uses helper; Resend untouched | VERIFIED | 8 call sites converted; EmailStats/GetResendStatsAsync unchanged |
| `QuestBoard.Repository/Entities/QuestBoardContext.cs` | Composite index + filter docs | VERIFIED | `HasIndex` + descriptive prose comments |
| `QuestBoard.Repository/Migrations/20260702081517_AddQuestFinalizedDateIndex.cs` | Index migration | VERIFIED | `Up()`/`Down()` correct, snapshot updated |
| `QuestBoard.Repository/ShopRepository.cs` | Lean list-view projection | VERIFIED | `GetAllAsync` drops `Transactions` include |
| `QuestBoard.Service/Program.cs` | Global retry filter | VERIFIED | `AutomaticRetryAttribute` inside non-Testing guard |
| `QuestBoard.Service/Jobs/HangfireJobHelper.cs` | Scope/group-context wrapper | VERIFIED | `RunInScopeAsync`, used by all 3 jobs |
| `.planning/codebase/ARCHITECTURE.md` | Deferred-hardening + dependency notes | VERIFIED | "Deferred Hardening / Scaling Notes" section present |
| `.planning/codebase/CONCERNS.md` | Status annotations on deferred items | VERIFIED | Both Forbid() and batching items annotated |
| `.planning/codebase/CONVENTIONS.md` | Enum handling note | VERIFIED | "Enum Handling" section present |
| `QuestBoard.Domain/Extensions/ActiveGroupContextExtensions.cs` | `RequireActiveGroupId()` guard | VERIFIED (unwired) | Exists, tested; zero production call sites |
| `QuestBoard.UnitTests/Services/EntityProfileEnumCastTests.cs` | Enum round-trip validation | VERIFIED | 10 enums, 42 values |
| `QuestBoard.IntegrationTests/Tests/TenantIsolationTests.cs` | Non-existent-group + null-guard tests | VERIFIED | 2 new tests added, 3 existing unchanged |
| `QuestBoard.UnitTests/Services/HangfireRetryPolicyTests.cs` | Retry policy config test | VERIFIED | Asserts 5 attempts / 1,2,4,8,16s |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `QuestController.CreateFollowUp` | `QuestService.CreateFollowUpQuestWithDetailsAsync` | direct call | WIRED | QuestController.cs:855 |
| `AdminController` (8 call sites) | `ControllerExtensions` | extension method calls | WIRED | Confirmed via grep, all 8 call sites present |
| `QuestBoardContext` HasIndex | `AddQuestFinalizedDateIndex` migration | `dotnet ef migrations add` | WIRED | Migration + snapshot consistent |
| `Program.cs` | Hangfire `GlobalJobFilters` | `GlobalJobFilters.Filters.Add(...)` | WIRED | Inside non-Testing guard |
| `SessionReminderJob`/`DailyReminderJob`/`QuestFinalizedEmailJob` | `HangfireJobHelper.RunInScopeAsync` | direct call | WIRED | All 3 jobs confirmed |
| `ActiveGroupContextExtensionsTests` / `TenantIsolationTests` | `ActiveGroupContextExtensions.RequireActiveGroupId` | test call | WIRED (test-only) | No production controller/job calls this guard — see Gaps |
| `QuestController.SendReminder` | `RequireActiveGroupId()` | *(not wired — expected per new guard's stated purpose)* | NOT WIRED | `SendReminder` (QuestController.cs:713) still does `activeGroupContext.ActiveGroupId ?? 1`, the exact unsafe pattern the guard exists to replace |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full solution builds | `dotnet build` | 6 projects, 0 errors, 0 warnings | PASS |
| Unit test suite green | `dotnet test QuestBoard.UnitTests --no-build` | 107/107 passed | PASS |
| TenantIsolationTests green (existing + new) | `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~TenantIsolationTests"` | 5/5 passed | PASS |
| Full suite green (regression check) | `dotnet test --no-build` | 107 unit + 203 integration, all passed | PASS |

### Requirements Coverage

No requirement IDs are mapped to this phase — confirmed via `grep "requirements:" 34.2-*-PLAN.md` (all five plans declare `requirements: []`) and via `grep -i "34.2" .planning/REQUIREMENTS.md` (zero matches). This matches the phase's own declaration ("None mapped — cleanup/hardening phase; tracked by CONCERNS.md item names + CONTEXT.md decisions D-01..D-11"). No orphaned requirements exist for this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` | 713 | `activeGroupContext.ActiveGroupId ?? 1` — silent fallback to Group 1 instead of failing fast | WARNING | Pre-existing code; the exact "silent null-scope" risk this phase's D-scope item explicitly targeted; the new `RequireActiveGroupId()` guard was built to close this and was not adopted here. Confirmed via code review (REVIEW.md WR-01/IN-01) and independently confirmed by this verification's direct code read. |
| `QuestBoard.Service/Controllers/Admin/AdminController.cs` | 205 | `callbackUrl!` null-forgiving dereference, inconsistent with two sibling guarded call sites in the same file | INFO | Pre-existing defect (REVIEW.md WR-02), not introduced by this phase, not in this phase's declared scope (Plan 02 only converted TempData+redirect boilerplate, not this branch's nullability handling) |
| `QuestBoard.Domain/Services/ShopSeedService.cs` | 23-25 | `SeedBasicEquipmentAsync` never sets `GroupId` on seeded shop items — orphaned at `GroupId = 0`, invisible under the group query filter | INFO | Pre-existing defect (REVIEW.md WR-03), confirmed pre-existing via git blame (only the `DateTime.UtcNow` lines were touched by this phase's Task 3, commit `ace6282`); not a CONCERNS.md-catalogued item for this phase, out of declared scope |

None of these three findings are BLOCKER-severity: none are newly introduced by this phase, and none contradict an artifact/truth this phase explicitly promised to deliver, except WR-01 which relates directly to must-have #14's stated intent (see Human Verification below).

## Human Verification Required

### 1. RequireActiveGroupId() guard adoption gap at SendReminder

**Test:** Review `QuestController.SendReminder` (QuestController.cs:713) and decide whether `activeGroupContext.ActiveGroupId ?? 1` should be replaced with `activeGroupContext.RequireActiveGroupId()` before this phase (and the v5.0 Multi-Tenancy milestone it closes) is considered fully closed.
**Expected:** A decision: (a) accept as a documented, tracked residual risk and defer the one-line fix to a follow-up phase, or (b) require the fix now as a small closure item before merging.
**Why human:** This is a risk-acceptance judgment call, not a mechanical pass/fail. The phase's own must-have for `RequireActiveGroupId()` (Plan 05, `34.2-05-PLAN.md`) explicitly scoped it as "introduce the seam + prove it works... wiring it into request-scoped controllers is left for a future phase" — so the plan's literal must-have is satisfied. However, the phase's own code review (REVIEW.md WR-01/IN-01, 2026-07-02) flagged this as the single most notable finding: the new fail-fast guard exists specifically to prevent silently dispatching a tenant-scoped Hangfire job (a session reminder email) under the wrong group's context if `ActiveGroupId` is ever unexpectedly null, and that exact scenario remains live in production code. Since this phase's goal explicitly includes "ActiveGroupId null-guard" as a Scaling-Limits deliverable, a human should decide whether "guard exists but isn't used where the bug lives" satisfies that goal or leaves it half-done.

## Gaps Summary

No BLOCKER-severity gaps. All 5 plans' artifacts exist, are substantive (not stubs), are correctly wired where the plan's own must-haves required wiring, and the full build/test suite is green with zero regressions (107 unit + 203 integration tests passing). All documentation-only deliverables (Forbid() deferral, Hangfire batching deferral, dependency notes, enum convention) are present and prose-only, matching D-06/D-09's mandatory exclusions — no `Forbid()` code or `SessionReminderBatchJob` was introduced anywhere in the codebase.

One item is routed to human verification rather than marked as a gap: the `RequireActiveGroupId()` null-guard (must-have #14) is fully implemented and tested exactly as the plan specified, but the plan's own scope decision (guard as an available seam, not force-wired into controllers) means the phase's broader stated goal — closing the "silent null-scope" Scaling Limit risk — is not actually exercised on any live request path. The phase's own code review already surfaced this as its top finding (WR-01/IN-01). This needs a human risk-acceptance decision, not a mechanical fix-or-fail verdict, since the plan technically delivered what it promised.

Two additional pre-existing, out-of-scope defects were independently confirmed during verification (AdminController.EditUser's unguarded `callbackUrl!`, ShopSeedService's missing `GroupId` on seeded items) — both already tracked in REVIEW.md as WR-02/WR-03 and confirmed via git history to predate this phase's touches to those files. They are informational only; neither was a declared must-have of this phase.

---

_Verified: 2026-07-02T08:49:41Z_
_Verifier: Claude (gsd-verifier)_
