---
phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che
plan: 02
type: execute
wave: 2
depends_on: ["34.3-01"]
files_modified:
  - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "An Admin who is not the quest's DM sees the Manage Quest card on quest Details"
    - "An Admin who is not the quest's DM can Edit/Delete/Finalize/Open/SendReminder/CreateFollowUp without a Forbid()"
    - "The Manage page's ViewBag.IsAdmin is true for a real Admin who is not the quest's DM"
    - "Anonymous visitors to Index and Details never trigger RequireActiveGroupId() (no 500)"
  artifacts:
    - path: "QuestBoard.Service/Controllers/QuestBoard/QuestController.cs"
      provides: "All 12 inline role checks migrated to GetEffectiveGroupRoleAsync"
      contains: "GetEffectiveGroupRoleAsync"
  key_links:
    - from: "QuestBoard.Service/Controllers/QuestBoard/QuestController.cs"
      to: "IUserService.GetEffectiveGroupRoleAsync"
      via: "activeGroupContext.RequireActiveGroupId() for authenticated sites, guarded by currentUser != null for anonymous-reachable sites"
      pattern: "GetEffectiveGroupRoleAsync\\(User,"
---

<objective>
Migrate all 12 inline `IsInRoleAsync(User, "Admin"|"DungeonMaster")` call sites in `QuestController` to the D-02 helper `GetEffectiveGroupRoleAsync`, preserving each site's existing ownership comparison and anonymous-reachability guard exactly.

Purpose: `QuestController.Details`'s `ViewBag.CanManage` is the primary user-visible symptom named in the phase goal ("Manage Quest card disappears for Admins who aren't the quest's own DM"). This plan fixes it and the 11 sibling sites in the same file.
Output: A QuestController where every Admin/DM decision reads `UserGroups.GroupRole` (via the helper) instead of the empty `AspNetUserRoles`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-CONTEXT.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-PATTERNS.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-RESEARCH.md

<interfaces>
<!-- The helper produced by Plan 01 (Wave 1) — call this, do not re-derive. -->
From QuestBoard.Domain/Interfaces/IUserService.cs:
```csharp
// SuperAdmin bypass baked in; returns GroupRole.Admin for SuperAdmin, else the user's group role, else null.
Task<GroupRole?> GetEffectiveGroupRoleAsync(ClaimsPrincipal user, int groupId);
```

GroupRole enum: Player = 0, DungeonMaster = 1, Admin = 2
Role enum (QuestBoard.Domain/Enums/Role.cs) is numerically identical: Player = 0, DungeonMaster = 1, Admin = 2

Already present in QuestController.cs (no DI/import change needed):
- Constructor already injects `IActiveGroupContext activeGroupContext`
- `using QuestBoard.Domain.Extensions;` is already imported → `RequireActiveGroupId()` is usable
- `using QuestBoard.Domain.Enums;` is already imported → `GroupRole`/`Role` resolve
- Line ~714 in SendReminder already calls `activeGroupContext.RequireActiveGroupId()` (proves DI is wired)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the anonymous-reachable and Index sites (Index line ~42, Details CanManage line ~282, Manage IsAdmin line ~739)</name>
  <files>QuestBoard.Service/Controllers/QuestBoard/QuestController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs (read the Index action's `if (userEntity != null)` block ~line 35-48, the Details action's `ViewBag.CanManage` block ~line 280-283, and the Manage action's `ViewBag.IsAuthorized`/`ViewBag.IsAdmin` block ~line 737-741 — confirm current line numbers, which may have drifted)
    - QuestBoard.Service/Views/Quest/Manage.cshtml (confirm how `ViewBag.IsAdmin` is consumed — needed so the fix keeps the standalone IsAdmin flag correct, not just IsAuthorized, per Pitfall 5)
    - QuestBoard.Service/Authorization/AdminHandler.cs (the canonical SuperAdmin→group→role shape being replicated)
  </read_first>
  <behavior>
    - Index: an authenticated Admin or DungeonMaster gets `userRole` mapped to Role.Admin / Role.DungeonMaster (feeding isAdminOrDm), derived from ONE GetEffectiveGroupRoleAsync call inside the existing `if (userEntity != null)` guard; an anonymous visitor never calls the helper (userRole stays Role.Player).
    - Details: `ViewBag.CanManage` is true for a real Admin who is not the quest's DM, and false for an anonymous visitor — with no exception thrown for the anonymous case.
    - Manage: for a real Admin who is not the quest's DM, BOTH `ViewBag.IsAuthorized` and `ViewBag.IsAdmin` are true (one lookup feeds both).
  </behavior>
  <action>
    Index (~line 42-48): inside the existing `if (userEntity != null)` block, replace the two calls (`isAdmin = await userService.IsInRoleAsync(User, "Admin")` and `isDungeonMaster = await userService.IsInRoleAsync(User, "DungeonMaster")`) with a single `var role = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId());`, then map: `if (role == GroupRole.Admin) userRole = Role.Admin; else if (role == GroupRole.DungeonMaster) userRole = Role.DungeonMaster;`. Keep the mapping inside the existing `if (userEntity != null)` guard — do NOT hoist the helper/RequireActiveGroupId call above that guard (anonymous users must not reach it, per Pitfall 2).

    Details `ViewBag.CanManage` (~line 282): preserve the `currentUser != null &&` short-circuit. Replace `currentUser != null && await userService.IsInRoleAsync(User, "Admin")` with a form that only calls the helper when `currentUser != null`, e.g. compute `var isAdmin = currentUser != null && await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId()) == GroupRole.Admin;`. The `&&` short-circuit guarantees the helper/RequireActiveGroupId is never evaluated for anonymous visitors. Keep `ViewBag.CanManage = isQuestDm || isAdmin;` unchanged.

    Manage `ViewBag.IsAdmin` (~line 739): this action is authenticated (currentUser already validated non-null above). Replace `var isAdmin = await userService.IsInRoleAsync(User, "Admin");` with `var isAdmin = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId()) == GroupRole.Admin;`. Keep BOTH `ViewBag.IsAuthorized = isQuestDm || isAdmin;` and `ViewBag.IsAdmin = isAdmin;` (Pitfall 5 — the standalone IsAdmin must stay correct, not just IsAuthorized).

    Do not fold any `isQuestDm`/ownership comparison into the helper (Pitfall 1). Do not add comments referencing a phase/decision ID.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - The Index role mapping calls `GetEffectiveGroupRoleAsync` exactly once inside the `if (userEntity != null)` block and no longer calls `IsInRoleAsync(User, "DungeonMaster")`
    - The Details `ViewBag.CanManage` assignment retains a `currentUser != null` short-circuit before any `GetEffectiveGroupRoleAsync`/`RequireActiveGroupId` call
    - The Manage action still assigns BOTH `ViewBag.IsAuthorized` and `ViewBag.IsAdmin`, both derived from the single helper result
    - `dotnet build QuestBoard.Service` exits 0
    - No `RequireActiveGroupId()` call appears outside a `currentUser != null` / `userEntity != null` guard in Index or Details
  </acceptance_criteria>
  <done>Index role filtering, Details CanManage, and Manage IsAdmin all read GroupRole via the helper; anonymous-reachable sites stay guarded; Service builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the 8 ownership-OR-admin Forbid() sites (Edit GET/POST, Delete, Finalize, Open, SendReminder, CreateFollowUp GET/POST)</name>
  <files>QuestBoard.Service/Controllers/QuestBoard/QuestController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/QuestBoard/QuestController.cs (read each guard: Edit GET ~127, Edit POST ~179, Delete ~237, Finalize ~611, Open ~648, SendReminder ~682, CreateFollowUp GET ~760, CreateFollowUp POST ~815 — line numbers may have drifted; find each `!User.IsInRole("Admin")` or `!isAdmin` guard. Note the two shape variants: multi-line `if (!currentUser.Equals(quest.DungeonMaster) && !User.IsInRole("Admin")) { return Forbid(); }` vs. the two-step `var isAdmin = await userService.IsInRoleAsync(User, "Admin"); if (!isQuestDm && !isAdmin) return Forbid();`)
    - QuestBoard.Service/Authorization/AdminHandler.cs (canonical role-check shape)
  </read_first>
  <behavior>
    - For each of the 8 actions: a real Admin who is NOT the quest's DM is NOT returned Forbid() (this incorrect Forbid IS the regression).
    - The quest's own DM (non-Admin) continues to pass the ownership branch as before.
    - A Player who is neither owner nor Admin still gets Forbid().
  </behavior>
  <action>
    All 8 sites are behind action-level `[Authorize(Policy = "DungeonMasterOnly")]` (and/or already return `Challenge()` when `currentUser == null` earlier in the action), so `activeGroupContext.RequireActiveGroupId()` fail-hard is safe here (D-03).

    For the multi-line `if (!currentUser.Equals(quest.DungeonMaster) && !User.IsInRole("Admin"))` sites (Edit GET/POST, Delete, Open, SendReminder): replace ONLY the trailing `!User.IsInRole("Admin")` with a role-derived condition. Compute `var role = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId());` on the line before the `if`, then change the guard to `if (!currentUser.Equals(quest.DungeonMaster) && role != GroupRole.Admin)`. Leave the `currentUser.Equals(...)` ownership comparison untouched (Pitfall 1).

    For the single-line Finalize guard (`if (!currentUser.Equals(quest.DungeonMaster) && !User.IsInRole("Admin")) return Forbid();`): same treatment — hoist a `var role = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId());` above it and change to `role != GroupRole.Admin`.

    For the two-step CreateFollowUp GET/POST sites (`var isAdmin = await userService.IsInRoleAsync(User, "Admin"); if (!isQuestDm && !isAdmin) return Forbid();`): replace with `var isAdmin = await userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId()) == GroupRole.Admin;` and keep `if (!isQuestDm && !isAdmin) return Forbid();` unchanged.

    Do NOT change `RemovePlayerSignup` — it already uses `[Authorize(Policy = "AdminOnly")]` and is not an inline site. Do not add phase/decision-ID comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service -c Debug && grep -c 'IsInRoleAsync(User, "Admin")\|User.IsInRole("Admin")' QuestBoard.Service/Controllers/QuestBoard/QuestController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'IsInRole("Admin")' QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` returns 0
    - `grep -c 'IsInRoleAsync(User, "Admin")' QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` returns 0
    - `grep -c 'IsInRoleAsync(User, "DungeonMaster")' QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` returns 0
    - Each of the 8 ownership guards retains its `currentUser.Equals(...)` / `isQuestDm` ownership term and only the admin term now derives from `GetEffectiveGroupRoleAsync`
    - `RemovePlayerSignup` is unchanged (still `[Authorize(Policy = "AdminOnly")]`, no inline helper call)
    - `dotnet build QuestBoard.Service` exits 0
  </acceptance_criteria>
  <done>All 8 Forbid()-guard sites derive the admin override from GroupRole via the helper; ownership comparisons are preserved inline; zero IsInRole("Admin")/IsInRoleAsync(...,"Admin"|"DungeonMaster") occurrences remain in QuestController.cs; Service builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous/authenticated request → QuestController action | Index/Details are anonymous-reachable; Edit/Delete/Manage/Finalize/Open/SendReminder/CreateFollowUp are DungeonMasterOnly |
| ownership (is-the-quest's-DM) vs. role (is-Admin) | Two orthogonal grants combined per site; must not be collapsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-07 | Denial of Service | RequireActiveGroupId() on anonymous-reachable Index/Details | mitigate | Preserve the existing `if (userEntity != null)` / `currentUser != null &&` guards; acceptance criterion forbids RequireActiveGroupId outside those guards. Prevents a 500 for anonymous visitors. |
| T-34.3-08 | Elevation of Privilege | ownership-vs-role collapse into the helper | mitigate | Only the `!User.IsInRole("Admin")` term is replaced; `currentUser.Equals(quest.DungeonMaster)`/`isQuestDm` stays inline (Pitfall 1). Acceptance criterion requires each guard keeps its ownership term. |
| T-34.3-09 | Elevation of Privilege | Manage ViewBag.IsAdmin left stale while IsAuthorized fixed | mitigate | Both flags derive from the one helper result; acceptance criterion requires both assignments present. Plan 06 asserts Manage page body renders admin UI for a real Admin. |
| T-34.3-10 | Denial of Service (regression stays broken) | any missed site still evaluating IsInRole false | mitigate | grep gate asserts zero `IsInRole("Admin")`/`IsInRoleAsync(...,"Admin"|"DungeonMaster")` remain in the file. Plan 06 integration tests cover each action. |
| T-34.3-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` exits 0.
- `grep -c 'IsInRole("Admin")\|IsInRoleAsync(User, "Admin")\|IsInRoleAsync(User, "DungeonMaster")' QuestBoard.Service/Controllers/QuestBoard/QuestController.cs` returns 0.
- `GetEffectiveGroupRoleAsync(User,` appears at each migrated site; every anonymous-reachable call is inside a null-guard.
- Behavioral proof deferred to Plan 06 (integration tests).
</verification>

<success_criteria>
- All 12 QuestController sites read GroupRole via the helper; the Manage Quest card and Manage IsAdmin flag work for real Admins who are not the quest's DM.
- Anonymous visitors to Index/Details incur no exception.
- Ownership comparisons remain inline and unchanged.
</success_criteria>

<output>
Create `.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-02-SUMMARY.md` when done.
</output>
