---
phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Domain/Interfaces/IUserService.cs
  - QuestBoard.Domain/Services/UserService.cs
  - QuestBoard.UnitTests/Services/UserServiceTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A SuperAdmin resolves to GroupRole.Admin from GetEffectiveGroupRoleAsync without any UserGroups membership"
    - "A non-SuperAdmin Admin member of the active group resolves to GroupRole.Admin"
    - "A non-SuperAdmin DungeonMaster member resolves to GroupRole.DungeonMaster"
    - "A user with no membership in the group resolves to null"
  artifacts:
    - path: "QuestBoard.Domain/Interfaces/IUserService.cs"
      provides: "GetEffectiveGroupRoleAsync interface declaration"
      contains: "Task<GroupRole?> GetEffectiveGroupRoleAsync"
    - path: "QuestBoard.Domain/Services/UserService.cs"
      provides: "GetEffectiveGroupRoleAsync implementation with SuperAdmin bypass"
      contains: "GetEffectiveGroupRoleAsync"
    - path: "QuestBoard.UnitTests/Services/UserServiceTests.cs"
      provides: "Unit tests for the SuperAdmin-bypass + group-role-lookup helper"
      contains: "GetEffectiveGroupRoleAsync"
  key_links:
    - from: "QuestBoard.Domain/Services/UserService.cs"
      to: "GetGroupRoleAsync"
      via: "delegation after SuperAdmin bypass"
      pattern: "GetGroupRoleAsync\\(user, groupId\\)"
---

<objective>
Add a single reusable Domain-layer helper `GetEffectiveGroupRoleAsync(ClaimsPrincipal, int)` on `IUserService`/`UserService` that encapsulates the SuperAdmin-bypass + `GetGroupRoleAsync` lookup, so all downstream controller sites derive Admin/DungeonMaster booleans from one lookup instead of duplicating the pattern (D-02).

Purpose: This is the foundation of the whole phase. Plans 02, 03, and 04 (the controller migrations) call this helper. The Hangfire/IdentityService dead-clause deletions in Plan 05 do NOT use it.
Output: One new interface method, one new implementation, and unit tests locking in the SuperAdmin bypass and null-membership behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-CONTEXT.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-07-02. Use directly — no exploration needed. -->

Existing method family in QuestBoard.Domain/Interfaces/IUserService.cs (mirror this shape):
```csharp
Task<GroupRole?> GetGroupRoleAsync(ClaimsPrincipal user, int groupId);
Task<GroupRole?> GetGroupRoleByIdAsync(int userId, int groupId);
```

Existing implementation in QuestBoard.Domain/Services/UserService.cs:
```csharp
public async Task<GroupRole?> GetGroupRoleAsync(ClaimsPrincipal user, int groupId)
{
    var userId = await identityService.GetUserIdAsync(user);
    if (userId == null) return null;
    return await repository.GetGroupRoleAsync(userId.Value, groupId);
}
```

Canonical SuperAdmin-bypass logic to port (QuestBoard.Service/Authorization/AdminHandler.cs):
```csharp
if (context.User.IsInRole("SuperAdmin")) { context.Succeed(requirement); return; }
```

GroupRole enum (QuestBoard.Domain/Enums/GroupRole.cs): Player = 0, DungeonMaster = 1, Admin = 2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare and implement GetEffectiveGroupRoleAsync on IUserService / UserService</name>
  <files>QuestBoard.Domain/Interfaces/IUserService.cs, QuestBoard.Domain/Services/UserService.cs</files>
  <read_first>
    - QuestBoard.Domain/Interfaces/IUserService.cs (the interface being extended — see the existing GetGroupRoleAsync/GetGroupRoleByIdAsync XML-doc + signature convention to match)
    - QuestBoard.Domain/Services/UserService.cs (the implementation being extended — see the existing GetGroupRoleAsync body to delegate to)
    - QuestBoard.Service/Authorization/AdminHandler.cs (source of the exact `context.User.IsInRole("SuperAdmin")` bypass string to copy verbatim)
    - QuestBoard.Domain/Enums/GroupRole.cs (enum values Player=0, DungeonMaster=1, Admin=2)
  </read_first>
  <behavior>
    - Test 1: GetEffectiveGroupRoleAsync returns GroupRole.Admin for a ClaimsPrincipal that IsInRole("SuperAdmin"), without any repository/membership call being required.
    - Test 2: For a non-SuperAdmin principal, GetEffectiveGroupRoleAsync returns whatever GetGroupRoleAsync/repository.GetGroupRoleAsync returns — GroupRole.Admin when the underlying lookup yields Admin.
    - Test 3: For a non-SuperAdmin principal whose underlying group-role lookup yields null (not a member), GetEffectiveGroupRoleAsync returns null.
    - Test 4: For a non-SuperAdmin principal whose lookup yields GroupRole.DungeonMaster, GetEffectiveGroupRoleAsync returns GroupRole.DungeonMaster.
  </behavior>
  <action>
    Add a new interface method `Task<GroupRole?> GetEffectiveGroupRoleAsync(ClaimsPrincipal user, int groupId);` to `IUserService`, placed alphabetically/grouped next to the existing `GetGroupRoleAsync`/`GetGroupRoleByIdAsync` methods, with a `/// <summary>` XML doc stating it returns the effective GroupRole treating SuperAdmin as an automatic Admin-equivalent bypass that requires no group membership. Do NOT reference any phase/plan/requirement ID in the doc comment (CLAUDE.md rule).

    Implement it in `UserService` with a `/// <inheritdoc/>` tag. Body: if `user.IsInRole("SuperAdmin")` return `GroupRole.Admin`; otherwise `return await GetGroupRoleAsync(user, groupId);` (reuse the existing instance method — do NOT re-query the repository directly, do NOT re-resolve the userId a second time). Copy the SuperAdmin role-name string `"SuperAdmin"` verbatim from `AdminHandler` — do not vary casing or claim type. The helper covers ONLY SuperAdmin-bypass + group-role lookup; it takes NO owner/currentUser parameter (ownership checks stay inline at each call site in later plans).

    `System.Security.Claims` is already imported in both files; `QuestBoard.Domain.Enums` is already imported in UserService.cs. Confirm `GroupRole` resolves without a new using in IUserService.cs (it uses `GroupRole?` already at line 49/54, so the using is present).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Domain -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - QuestBoard.Domain/Interfaces/IUserService.cs contains `Task<GroupRole?> GetEffectiveGroupRoleAsync(ClaimsPrincipal user, int groupId);`
    - QuestBoard.Domain/Services/UserService.cs contains a `GetEffectiveGroupRoleAsync` method whose body contains `user.IsInRole("SuperAdmin")` and `return GroupRole.Admin;`
    - The non-SuperAdmin path delegates to the existing `GetGroupRoleAsync(user, groupId)` (grep `GetGroupRoleAsync\(user, groupId\)` in the new method body)
    - No new owner/currentUser parameter appears on the method signature
    - No `D-0`, `TENANT-`, `Phase `, or `34.3` string appears in the added XML doc comments
    - `dotnet build QuestBoard.Domain` exits 0
  </acceptance_criteria>
  <done>GetEffectiveGroupRoleAsync is declared on IUserService and implemented in UserService with the verbatim SuperAdmin bypass, delegating to the existing GetGroupRoleAsync for non-SuperAdmins; Domain project builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test the SuperAdmin bypass and null-membership behavior</name>
  <files>QuestBoard.UnitTests/Services/UserServiceTests.cs</files>
  <read_first>
    - QuestBoard.UnitTests/Services/UserServiceTests.cs (existing UserService unit-test fixture — match its mocking style for IIdentityService/IUserRepository/IMapper; if this file does not exist, discover the actual UserService test file name via `ls QuestBoard.UnitTests/Services/` and mirror the closest existing service-test fixture in that folder)
    - QuestBoard.Domain/Services/UserService.cs (the SUT — confirm which collaborators the SuperAdmin path must NOT call)
  </read_first>
  <behavior>
    - Test: SuperAdmin principal → returns GroupRole.Admin AND the underlying IUserRepository.GetGroupRoleAsync is never invoked (Verify Times.Never) — proves the bypass short-circuits before any DB lookup.
    - Test: non-SuperAdmin principal whose repository lookup returns GroupRole.Admin → returns GroupRole.Admin.
    - Test: non-SuperAdmin principal whose repository lookup returns GroupRole.DungeonMaster → returns GroupRole.DungeonMaster.
    - Test: non-SuperAdmin principal whose IIdentityService.GetUserIdAsync returns null (or repository returns null) → returns null.
  </behavior>
  <action>
    Add unit tests to the UserService test fixture covering the four behaviors above. Construct a SuperAdmin `ClaimsPrincipal` with a role claim of `"SuperAdmin"` (use `new ClaimsPrincipal(new ClaimsIdentity(new[] { new Claim(ClaimTypes.Role, "SuperAdmin") }, "test"))` so `IsInRole("SuperAdmin")` returns true). For the non-SuperAdmin cases, build a principal WITHOUT that role claim and mock `IIdentityService.GetUserIdAsync` to return a user id, then mock `IUserRepository.GetGroupRoleAsync(userId, groupId)` to return the target GroupRole (or null). Assert the returned value with FluentAssertions (`result.Should().Be(GroupRole.Admin)` etc.). For the SuperAdmin test, additionally assert the repository mock's `GetGroupRoleAsync` was never called. Use whatever mocking framework the existing fixture uses (Moq per the codebase convention). Do not embed any phase/requirement ID in test names or comments.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.UnitTests --filter "FullyQualifiedName~UserService"</automated>
  </verify>
  <acceptance_criteria>
    - The UserService unit-test file contains at least 4 new test methods referencing `GetEffectiveGroupRoleAsync`
    - One test asserts the SuperAdmin case returns `GroupRole.Admin` and verifies `IUserRepository.GetGroupRoleAsync` was never called (`Times.Never`)
    - One test asserts a null return when the underlying lookup yields null
    - `dotnet test QuestBoard.UnitTests --filter "FullyQualifiedName~UserService"` exits 0 with all new tests passing
    - No `D-0`, `Phase `, or `34.3` string appears in the new test method names or comments
  </acceptance_criteria>
  <done>Four unit tests lock in the SuperAdmin bypass (no DB call), the Admin/DungeonMaster pass-through, and the null-membership case; the UserService unit-test filter passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated request → controller action (downstream consumers) | This helper is the single authorization-role source for those actions; a bug here propagates to all ~20 call sites |
| ClaimsPrincipal role claims → GroupRole decision | SuperAdmin claim short-circuits the group-membership lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-01 | Elevation of Privilege | GetEffectiveGroupRoleAsync SuperAdmin bypass | mitigate | Copy the `IsInRole("SuperAdmin")` string verbatim from AdminHandler (RESEARCH "Don't Hand-Roll"); a mistyped/different claim check would either over-grant or silently re-break. Unit test asserts SuperAdmin → Admin and never queries membership. |
| T-34.3-02 | Elevation of Privilege | ownership-vs-role collapse | mitigate | Helper takes NO owner parameter; it computes role only. Ownership comparison stays at each call site (enforced by acceptance criterion: no owner/currentUser param on signature). |
| T-34.3-03 | Denial of Service (regression stays broken) | non-SuperAdmin delegation | mitigate | Non-SuperAdmin path delegates to the already-correct GetGroupRoleAsync (single source of truth); unit test covers Admin/DM/null returns. |
| T-34.3-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH: pure refactor, no new dependencies). |
</threat_model>

<verification>
- `dotnet build QuestBoard.Domain` exits 0.
- `dotnet test QuestBoard.UnitTests --filter "FullyQualifiedName~UserService"` exits 0.
- Grep confirms `GetEffectiveGroupRoleAsync` exists in both IUserService.cs and UserService.cs.
</verification>

<success_criteria>
- `GetEffectiveGroupRoleAsync(ClaimsPrincipal, int)` is available on IUserService for injection by all downstream controllers.
- SuperAdmin resolves to GroupRole.Admin with no membership lookup; non-members resolve to null; the helper carries no ownership concern.
- New unit tests pass and no other tests regress in the UnitTests project.
</success_criteria>

<output>
Create `.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-01-SUMMARY.md` when done.
</output>
