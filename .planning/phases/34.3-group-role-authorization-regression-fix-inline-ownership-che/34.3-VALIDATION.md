---
phase: 34.3
slug: group-role-authorization-regression-fix-inline-ownership-che
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-02
---

# Phase 34.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit v3 (3.2.2) + `Microsoft.AspNetCore.Mvc.Testing` 10.0.9 + `Microsoft.EntityFrameworkCore.InMemory` 10.0.9 + FluentAssertions 8.10.0 |
| **Config file** | `QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj`, `xunit.runner.json` |
| **Quick run command** | `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~AdminHandlerIntegrationTests\|FullyQualifiedName~<TouchedControllerName>"` |
| **Full suite command** | `dotnet test` (repo root — no `.sln`; per-project convention from Phase 34-01) |
| **Estimated runtime** | ~60-120 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~<TouchedControllerName>"`
- **After every plan wave:** Run `dotnet test` (full suite)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 34.3-06 (QuestController group) | 06 | 3 | D-02/D-03 | Elevation-of-privilege regression | Admin sees "Manage Quest" for a quest they don't own; Edit/Delete/Finalize/Open/SendReminder/CreateFollowUp not `Forbid()`-den for Admin | integration | `dotnet test --filter FullyQualifiedName~QuestControllerAuthorizationRegressionTests` | ❌ W0 (new file, created by 06) | ⬜ pending |
| 34.3-06 (QuestLogController group) | 06 | 3 | D-02/D-03 | Elevation-of-privilege regression | Admin sees recap-edit link and can update recap for non-owned completed quest | integration | `dotnet test --filter FullyQualifiedName~QuestLogControllerIntegrationTests` (extended) | Existing file, extended by 06 | ⬜ pending |
| 34.3-06 (DungeonMasterController group) | 06 | 3 | D-02/D-03 | Elevation-of-privilege regression | Admin sees edit link and is not `Forbid()`-den editing non-owned DM profile | integration | `dotnet test --filter FullyQualifiedName~DungeonMasterControllerIntegrationTests` (extended) | Existing file, extended by 06 | ⬜ pending |
| 34.3-06 (AccountController group) | 06 | 3 | D-02/D-04 | Elevation-of-privilege regression + fail-soft null-group | Admin/DM see correct role badges; null-ActiveGroupId shows no badges (fail-soft, not throw) | integration | `dotnet test --filter FullyQualifiedName~AccountControllerIntegrationTests` (extended) | Existing file, extended by 06 | ⬜ pending |
| 34.3-06 (Hangfire group) | 06 | 3 | D-01 | Elevation-of-privilege via dead Admin clause | Group Admin (non-SuperAdmin) denied `/hangfire`; SuperAdmin access unchanged | integration | new or extended platform/dashboard test (created by 06) | ❌ W0 (created by 06) | ⬜ pending |
| 34.3-06 (IdentityService group) | 06 | 3 | 24th site (user-approved in-scope) | Dead-code cleanup, no behavior change | Admin can still successfully reset another user's password end-to-end (policy gate already enforces authz) | integration | `dotnet test --filter FullyQualifiedName~AdminControllerIntegrationTests` (extended) | Existing file, extended by 06 | ⬜ pending |

Fix implementation is spread across plans 01 (shared helper, Wave 1), 02/03/04 (call-site migrations, Wave 2), and 05 (Hangfire + IdentityService dead-clause deletion, Wave 1) — plan 06 (Wave 3, depends on 02/03/04/05) provides the regression-test coverage rows above. See `34.3-01-PLAN.md` through `34.3-06-PLAN.md` for full task-level detail; verified by gsd-plan-checker (`## VERIFICATION PASSED`, all 24 call sites covered, no gaps).

---

## Wave 0 Requirements

- [ ] New integration test file for `QuestController` authorization regression coverage (`QuestControllerAuthorizationRegressionTests.cs` or similar).
- [ ] Extend `QuestLogControllerIntegrationTests.cs` (already exists) — do not create a duplicate file.
- [ ] Extend `DungeonMasterControllerIntegrationTests.cs` (already exists) — do not create a duplicate file.
- [ ] Extend `AccountControllerIntegrationTests.cs` (already exists) with Profile/Edit role-badge tests, including the D-04 fail-soft null-`ActiveGroupId` case (set `MutableGroupContext.ActiveGroupId = null` for that test only; confirm test isolation vs. the GroupId=1 default used elsewhere in the fixture).
- [ ] New or extended Hangfire-access test confirming D-01's SuperAdmin-only behavior post-fix (no existing coverage found).
- [ ] Extend `AdminControllerIntegrationTests.cs` with a test covering `IdentityService.AdminResetPasswordAsync` post-cleanup (24th site, user-approved in scope).

Framework install: none — all needed test infrastructure already exists and is proven working via `AdminHandlerIntegrationTests.cs`.

---

## Manual-Only Verifications

*None — all phase behaviors have automated integration-test coverage per the Wave 0 requirements above.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-02 (gsd-plan-checker VERIFICATION PASSED)
