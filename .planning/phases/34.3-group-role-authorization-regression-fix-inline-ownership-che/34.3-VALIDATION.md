---
phase: 34.3
slug: group-role-authorization-regression-fix-inline-ownership-che
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-02
---

# Phase 34.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit v3 (3.2.2) + `Microsoft.AspNetCore.Mvc.Testing` 10.0.9 + `Microsoft.EntityFrameworkCore.InMemory` 10.0.9 + FluentAssertions 8.10.0 |
| **Config file** | `QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj`, `xunit.runner.json` |
| **Quick run command** | `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~AdminHandlerIntegrationTests\|FullyQualifiedName~<TouchedControllerName>"` |
| **Full suite command** | `dotnet test` (repo root — no `.sln`; per-project convention from Phase 34-01) |
| **Estimated runtime** | ~60-120 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~<TouchedControllerName>"`
- **After every plan wave:** Run `dotnet test` (full suite)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD-QuestController | TBD | TBD | D-02/D-03 | Elevation-of-privilege regression | Admin sees "Manage Quest" for a quest they don't own; Edit/Delete/Finalize/Open/SendReminder/CreateFollowUp not `Forbid()`-den for Admin | integration | `dotnet test --filter FullyQualifiedName~QuestControllerAuthorizationRegressionTests` | ❌ W0 (new file) | ⬜ pending |
| TBD-QuestLogController | TBD | TBD | D-02/D-03 | Elevation-of-privilege regression | Admin sees recap-edit link and can update recap for non-owned completed quest | integration | `dotnet test --filter FullyQualifiedName~QuestLogControllerAuthorizationRegressionTests` (or extend existing file) | ❌ W0 (extend existing) | ⬜ pending |
| TBD-DungeonMasterController | TBD | TBD | D-02/D-03 | Elevation-of-privilege regression | Admin sees edit link and is not `Forbid()`-den editing non-owned DM profile | integration | `dotnet test --filter FullyQualifiedName~DungeonMasterControllerAuthorizationRegressionTests` (or extend `DungeonMasterControllerIntegrationTests.cs`) | ❌ W0 (extend existing) | ⬜ pending |
| TBD-AccountController | TBD | TBD | D-02/D-04 | Elevation-of-privilege regression + fail-soft null-group | Admin/DM see correct role badges; null-ActiveGroupId shows no badges (fail-soft, not throw) | integration | extend `AccountControllerIntegrationTests.cs` | Existing file, new test methods | ⬜ pending |
| TBD-Hangfire | TBD | TBD | D-01 | Elevation-of-privilege via dead Admin clause | Group Admin (non-SuperAdmin) denied `/hangfire`; SuperAdmin access unchanged | integration | new or extend platform/dashboard test file | ❌ W0 — verify no existing Hangfire-access test first | ⬜ pending |
| TBD-IdentityService | TBD | TBD | 24th site (user-approved in-scope) | Dead-code cleanup, no behavior change | Admin can still successfully reset another user's password end-to-end (policy gate already enforces authz) | integration | extend `AdminControllerIntegrationTests.cs` | Existing file, new test method | ⬜ pending |

*Task IDs/plan/wave columns are TBD until the planner assigns them — this table enumerates the required coverage per site group; the planner must map each row to concrete task IDs.*

---

## Wave 0 Requirements

- [ ] New integration test file for `QuestController` authorization regression coverage (`QuestControllerAuthorizationRegressionTests.cs` or similar).
- [ ] Extend `QuestLogControllerIntegrationTests.cs` (already exists) — do not create a duplicate file.
- [ ] Extend `DungeonMasterControllerIntegrationTests.cs` (already exists) — do not create a duplicate file.
- [ ] Extend `AccountControllerIntegrationTests.cs` (already exists) with Profile/Edit role-badge tests, including the D-04 fail-soft null-`ActiveGroupId` case (set `MutableGroupContext.ActiveGroupId = null` for that test only; confirm test isolation vs. the GroupId=1 default used elsewhere in the fixture).
- [ ] New or extended Hangfire-access test confirming D-01's SuperAdmin-only behavior post-fix (no existing coverage found).
- [ ] Extend `AdminControllerIntegrationTests.cs` with a test covering `IdentityService.AdminResetPasswordAsync` post-cleanup (24th site, user-approved in scope).

Framework install: none — all needed test infrastructure already exists and is proven working via `AdminHandlerIntegrationTests.cs`.

---

## Manual-Only Verifications

*None — all phase behaviors have automated integration-test coverage per the Wave 0 requirements above.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
