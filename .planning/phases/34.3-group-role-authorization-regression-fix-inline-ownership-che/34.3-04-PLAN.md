---
phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che
plan: 04
type: execute
wave: 2
depends_on: ["34.3-01"]
files_modified:
  - QuestBoard.Service/Controllers/Admin/AccountController.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A real Admin sees the Admin role badge on their own Profile page"
    - "A real DungeonMaster sees the DungeonMaster badge; an Admin also satisfies the combined DM flag on Edit"
    - "An authenticated user with a null ActiveGroupId sees no role badges and gets no exception (fail-soft, D-04)"
  artifacts:
    - path: "QuestBoard.Service/Controllers/Admin/AccountController.cs"
      provides: "IActiveGroupContext injected + Profile/Edit badges migrated with fail-soft null-group handling"
      contains: "GetEffectiveGroupRoleAsync"
  key_links:
    - from: "QuestBoard.Service/Controllers/Admin/AccountController.cs"
      to: "IUserService.GetEffectiveGroupRoleAsync"
      via: "graceful ActiveGroupId is { } groupId guard (never RequireActiveGroupId)"
      pattern: "activeGroupContext.ActiveGroupId is \\{ \\} groupId"
---

<objective>
Add `IActiveGroupContext` to `AccountController`'s constructor and migrate its 2 role-badge sites (Profile GET, Edit GET — 4 `IsInRoleAsync` calls total) to the D-02 helper using the FAIL-SOFT null-group pattern (D-04), NOT the fail-hard `RequireActiveGroupId()` used elsewhere in this phase.

Purpose: `/Account` is exempt from `GroupSessionMiddleware` (to avoid a login redirect loop), so these two actions are genuinely reachable by an authenticated user whose session-scoped `ActiveGroupId` is null/expired. Using `RequireActiveGroupId()` here would throw a 500 on a user's own profile page (Pitfall 3). D-04 requires "no role, show no badges" instead.
Output: AccountController role badges read `UserGroups.GroupRole` and degrade gracefully to no-badges on a null active group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-CONTEXT.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-PATTERNS.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-RESEARCH.md

<interfaces>
<!-- The helper produced by Plan 01 (Wave 1) — call this, do not re-derive. -->
From QuestBoard.Domain/Interfaces/IUserService.cs:
```csharp
Task<GroupRole?> GetEffectiveGroupRoleAsync(ClaimsPrincipal user, int groupId);
```

GroupRole enum: Player = 0, DungeonMaster = 1, Admin = 2

Fail-soft null-group shape to use here (mirrors AdminHandler's graceful guard, NOT RequireActiveGroupId):
```csharp
GroupRole? role = null;
if (activeGroupContext.ActiveGroupId is { } groupId)
{
    role = await userService.GetEffectiveGroupRoleAsync(User, groupId);
}
// role stays null when no active group → no badges
```

Constraint (RESEARCH.md Constraint 1): the current sites call `userService.IsInRoleAsync(user, ...)` passing the already-fetched DOMAIN `User` model. The helper takes `ClaimsPrincipal`. Pass the controller's inherited `User` property (the ClaimsPrincipal), NOT the domain `user` variable — behavior-neutral, both resolve to the same identity.

IActiveGroupContext is in `QuestBoard.Domain.Interfaces`; add `using QuestBoard.Domain.Interfaces;` if not already present. Do NOT add `using QuestBoard.Domain.Extensions;` for RequireActiveGroupId — it must never be used in this file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject IActiveGroupContext and migrate Profile + Edit badges with fail-soft null-group handling</name>
  <files>QuestBoard.Service/Controllers/Admin/AccountController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/AccountController.cs (read the full constructor `(IUserService userService, IIdentityService identityService, IBackgroundJobClient jobClient, ILogger<AccountController> logger)` ~line 15; the Profile GET block ~line 151-161 that sets `ViewData["IsDungeonMaster"]`/`ViewData["IsAdmin"]`; the Edit GET block ~line 170-179 that sets `EditProfileViewModel.IsDungeonMaster = isDungeonMaster || isAdmin`)
    - QuestBoard.Service/Middleware/GroupSessionMiddleware.cs (confirm `/Account` is in the exempt-path list — this is WHY fail-soft is required here, unlike the other three controllers)
    - QuestBoard.Service/Authorization/AdminHandler.cs (the graceful `ActiveGroupId is not { } groupId` null-guard shape to mirror — the fail-soft form)
    - QuestBoard.Service/Views/Account/Profile.cshtml (confirm how ViewData["IsAdmin"]/["IsDungeonMaster"] render the badges — needed for Plan 06 body assertions)
  </read_first>
  <behavior>
    - Profile (active group set): a real Admin → `ViewData["IsAdmin"] == true`; a real DungeonMaster → `ViewData["IsDungeonMaster"] == true`; an Admin → `ViewData["IsDungeonMaster"] == true` as well (Admin satisfies the DM-or-Admin badge).
    - Edit (active group set): a DungeonMaster or Admin → `EditProfileViewModel.IsDungeonMaster == true`; a Player → false.
    - Both actions with a null ActiveGroupId: no badges (IsAdmin/IsDungeonMaster false) AND no exception thrown.
  </behavior>
  <action>
    Add `IActiveGroupContext activeGroupContext` as the last parameter of the AccountController primary constructor. Add `using QuestBoard.Domain.Interfaces;` if not already present. Do NOT add a `using QuestBoard.Domain.Extensions;` and do NOT call `RequireActiveGroupId()` anywhere in this file (Pitfall 3).

    Profile GET (~line 152-153, 159-160): replace the two `IsInRoleAsync(user, ...)` calls with the fail-soft block: `GroupRole? role = null; if (activeGroupContext.ActiveGroupId is { } groupId) { role = await userService.GetEffectiveGroupRoleAsync(User, groupId); }`. Then set `ViewData["IsDungeonMaster"] = role == GroupRole.DungeonMaster || role == GroupRole.Admin;` and `ViewData["IsAdmin"] = role == GroupRole.Admin;`. Note: pass the controller's `User` (ClaimsPrincipal) to the helper, not the domain `user` model.

    Edit GET (~line 171-172, feeding `IsDungeonMaster = isDungeonMaster || isAdmin`): replace the two `IsInRoleAsync(user, ...)` calls with the same fail-soft `role` block, then set the view-model `IsDungeonMaster = role == GroupRole.DungeonMaster || role == GroupRole.Admin`. Keep the rest of the EditProfileViewModel initialization (Id, Name, Email, HasKey) unchanged.

    Do not fold ownership into anything (these sites have no ownership term). Do not add phase/decision-ID comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service -c Debug && grep -c 'IsInRoleAsync(user, "Admin")\|IsInRoleAsync(user, "DungeonMaster")\|RequireActiveGroupId' QuestBoard.Service/Controllers/Admin/AccountController.cs</automated>
  </verify>
  <acceptance_criteria>
    - AccountController constructor parameter list contains `IActiveGroupContext activeGroupContext`
    - `grep -c 'IsInRoleAsync(user, "Admin")' QuestBoard.Service/Controllers/Admin/AccountController.cs` returns 0
    - `grep -c 'IsInRoleAsync(user, "DungeonMaster")' QuestBoard.Service/Controllers/Admin/AccountController.cs` returns 0
    - `grep -c 'RequireActiveGroupId' QuestBoard.Service/Controllers/Admin/AccountController.cs` returns 0 (fail-soft only — Pitfall 3)
    - Both Profile and Edit use an `if (activeGroupContext.ActiveGroupId is { } groupId)` guard so `role` stays null on a null active group
    - The helper is called with the controller `User` (ClaimsPrincipal), not the domain `user` model
    - `dotnet build QuestBoard.Service` exits 0
  </acceptance_criteria>
  <done>AccountController has IActiveGroupContext injected; Profile and Edit badges read GroupRole via the helper using the fail-soft null-group guard; RequireActiveGroupId never appears in the file; Service builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated request (possibly null ActiveGroupId) → /Account/Profile, /Account/Edit | /Account is GroupSessionMiddleware-EXEMPT — a null ActiveGroupId is a genuine, expected state here |
| GroupRole (per-group) → profile role badges | Badges are inherently per-active-group; no group means no badge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-14 | Denial of Service | RequireActiveGroupId() throwing on the user's own profile page | mitigate | D-04 mandates fail-soft; acceptance criterion asserts `grep -c RequireActiveGroupId` returns 0 in this file. The `ActiveGroupId is { } groupId` guard prevents the 500 (Pitfall 3). |
| T-34.3-15 | Denial of Service (regression stays broken) | Admin/DM badges stay hidden | mitigate | Both sites read GroupRole via the helper; Plan 06 asserts badges render for a real Admin/DM and are absent for the null-ActiveGroupId case. |
| T-34.3-16 | Elevation of Privilege | badges over-shown on null group | accept | Fail-soft shows NO badges on null group (least privilege). These are display-only flags (ViewData/view-model), not access gates — over-hiding is safe, over-showing is prevented by null→no-badge. |
| T-34.3-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` exits 0.
- `grep -c 'IsInRoleAsync(user, "Admin")\|IsInRoleAsync(user, "DungeonMaster")\|RequireActiveGroupId' QuestBoard.Service/Controllers/Admin/AccountController.cs` returns 0.
- Both Profile and Edit guard the helper call behind `ActiveGroupId is { } groupId`.
- Behavioral proof (including the fail-soft null-group case) deferred to Plan 06 (integration tests).
</verification>

<success_criteria>
- AccountController Profile/Edit badges read GroupRole via the helper.
- A null ActiveGroupId yields no badges and no exception (fail-soft, D-04).
- RequireActiveGroupId is never used in this file.
</success_criteria>

<output>
Create `.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-04-SUMMARY.md` when done.
</output>
