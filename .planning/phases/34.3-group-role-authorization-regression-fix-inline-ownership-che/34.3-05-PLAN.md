---
phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Program.cs
  - QuestBoard.Service/Authorization/AdminDashboardAuthFilter.cs
  - QuestBoard.Repository/IdentityService.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The /hangfire dashboard is gated by SuperAdmin only — the dead Admin clause is gone from both gate sites"
    - "AdminResetPasswordAsync no longer performs a redundant IsInRoleAsync(entity, \"Admin\") check that always fails post-Phase-27"
    - "A real Admin reaching ResetPassword via the AdminOnly policy is not blocked by IdentityService's inner check"
  artifacts:
    - path: "QuestBoard.Service/Program.cs"
      provides: "SuperAdmin-only Hangfire path gate"
      contains: "IsInRole(\"SuperAdmin\")"
    - path: "QuestBoard.Service/Authorization/AdminDashboardAuthFilter.cs"
      provides: "SuperAdmin-only Hangfire dashboard auth filter"
      contains: "IsInRole(\"SuperAdmin\")"
    - path: "QuestBoard.Repository/IdentityService.cs"
      provides: "AdminResetPasswordAsync with redundant Admin check removed"
      contains: "AdminResetPasswordAsync"
  key_links:
    - from: "QuestBoard.Repository/IdentityService.cs"
      to: "AdminController.ResetPassword (via UserService.ResetPasswordAsync)"
      via: "no longer fails on the dead Admin check"
      pattern: "adminEntity == null"
---

<objective>
Delete three dead `IsInRole("Admin")`-based checks that have been permanently false since Phase 27 moved per-group roles out of `AspNetUserRoles`:
1. The Hangfire path-gate in `Program.cs` (simplify to SuperAdmin-only per D-01).
2. The twin Hangfire dashboard auth filter in `AdminDashboardAuthFilter.cs` (simplify to SuperAdmin-only per D-01).
3. The redundant defense-in-depth `IsInRoleAsync(adminEntity, "Admin")` in `IdentityService.AdminResetPasswordAsync` (the 24th site, user-approved in scope — delete the clause, not migrate).

Purpose: These three sites CANNOT use the D-02 shared helper — the two Hangfire sites must stay SuperAdmin-only (migrating would grant group Admins new access, reversing Phase 29's decision), and IdentityService is in the Repository layer which cannot reach the Service-layer `IActiveGroupContext`. All three are pure dead-clause deletions.
Output: SuperAdmin-only Hangfire gating and a working admin password-reset path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-CONTEXT.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-PATTERNS.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify both Hangfire gates to SuperAdmin-only (D-01)</name>
  <files>QuestBoard.Service/Program.cs, QuestBoard.Service/Authorization/AdminDashboardAuthFilter.cs</files>
  <read_first>
    - QuestBoard.Service/Program.cs (the inline `app.Use` Hangfire middleware around line 290-316 — confirm the current `if (!context.User.IsInRole("Admin") && !context.User.IsInRole("SuperAdmin"))` line and that the whole block is guarded by `if (!app.Environment.IsEnvironment("Testing"))` and a `/hangfire` path check)
    - QuestBoard.Service/Authorization/AdminDashboardAuthFilter.cs (full 19-line file — the twin `if (!httpContext.User.IsInRole("Admin") && !httpContext.User.IsInRole("SuperAdmin")) return false;` line)
  </read_first>
  <action>
    In `Program.cs`, change the Hangfire role gate from `if (!context.User.IsInRole("Admin") && !context.User.IsInRole("SuperAdmin"))` to `if (!context.User.IsInRole("SuperAdmin"))` — delete the `!context.User.IsInRole("Admin") &&` clause only. Leave the surrounding authenticated-check, the `/hangfire` path check, the `Testing`-environment guard, and the `Response.Redirect("/Account/Login")` body exactly as-is.

    In `AdminDashboardAuthFilter.cs`, change `if (!httpContext.User.IsInRole("Admin") && !httpContext.User.IsInRole("SuperAdmin")) return false;` to `if (!httpContext.User.IsInRole("SuperAdmin")) return false;` — delete the `!httpContext.User.IsInRole("Admin") &&` clause only. Leave the authenticated-check and `return true;` untouched.

    Do NOT migrate either site to GroupRole-aware logic — Hangfire is SuperAdmin-only by design (Phase 29). Do NOT add `IActiveGroupContext` or the D-02 helper here. Do not add any explanatory comment referencing a phase or decision ID.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'IsInRole("Admin")' QuestBoard.Service/Program.cs` returns 0 (the dead Hangfire Admin clause is gone; note QuestController etc. are in different files)
    - QuestBoard.Service/Program.cs contains `if (!context.User.IsInRole("SuperAdmin"))` on the Hangfire gate line
    - `grep -c 'IsInRole("Admin")' QuestBoard.Service/Authorization/AdminDashboardAuthFilter.cs` returns 0
    - QuestBoard.Service/Authorization/AdminDashboardAuthFilter.cs contains `if (!httpContext.User.IsInRole("SuperAdmin")) return false;`
    - Neither file gained an `IActiveGroupContext` reference or a `GetEffectiveGroupRoleAsync` call
    - `dotnet build QuestBoard.Service` exits 0
  </acceptance_criteria>
  <done>Both Hangfire gates check SuperAdmin only; the dead Admin clause is removed from both; Service project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the redundant Admin check from IdentityService.AdminResetPasswordAsync (24th site)</name>
  <files>QuestBoard.Repository/IdentityService.cs</files>
  <read_first>
    - QuestBoard.Repository/IdentityService.cs (the `AdminResetPasswordAsync` method around lines 110-122 — confirm the current `if (adminEntity == null || !await userManager.IsInRoleAsync(adminEntity, "Admin"))` guard and the rest of the method body that generates a reset token)
    - QuestBoard.Service/Controllers/Admin/AdminController.cs (confirm the class-level `[Authorize(Policy = "AdminOnly")]` at line ~20 — this is the correctly-working outer gate that makes the inner check redundant)
  </read_first>
  <action>
    In `AdminResetPasswordAsync`, change the guard `if (adminEntity == null || !await userManager.IsInRoleAsync(adminEntity, "Admin"))` to `if (adminEntity == null)` — delete the `|| !await userManager.IsInRoleAsync(adminEntity, "Admin")` clause. Keep the same failure message body (`IdentityResult.Failed(new IdentityError { Description = "Admin user not found or not authorized." })`) and the rest of the method (`FindByIdAsync`, `GeneratePasswordResetTokenAsync`, `ResetPasswordAsync`) unchanged.

    Do NOT attempt to add `IActiveGroupContext` or GroupRole logic to the Repository layer — this is a dead-clause deletion (mirrors the D-01 Hangfire precedent). The outer `[Authorize(Policy = "AdminOnly")]` on `AdminController` (which uses the already-fixed GroupRole-aware `AdminHandler`) is the real gate. Do not add a comment referencing a phase or decision ID.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'IsInRoleAsync(adminEntity, "Admin")' QuestBoard.Repository/IdentityService.cs` returns 0
    - QuestBoard.Repository/IdentityService.cs `AdminResetPasswordAsync` contains `if (adminEntity == null)` as the sole authorization guard before the FindByIdAsync lookup
    - The method still returns a reset via `GeneratePasswordResetTokenAsync` + `ResetPasswordAsync` (unchanged)
    - `dotnet build QuestBoard.Repository` exits 0
  </acceptance_criteria>
  <done>The redundant Admin check is removed from AdminResetPasswordAsync; the method now only null-checks adminEntity before proceeding; Repository project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated request → /hangfire dashboard | Only SuperAdmin may reach background-job infrastructure |
| AdminController (AdminOnly policy) → IdentityService.AdminResetPasswordAsync | Outer policy gate is the real access control; inner check was redundant defense-in-depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-04 | Elevation of Privilege | Hangfire gate migrated (not deleted) would grant group Admins access | mitigate | D-01 mandates deletion, NOT migration — simplify to `IsInRole("SuperAdmin")` only. Acceptance criterion forbids adding IActiveGroupContext/GetEffectiveGroupRoleAsync to these files. Preserves Phase 29's SuperAdmin-only intent. |
| T-34.3-05 | Elevation of Privilege | IdentityService inner check removed while outer gate absent | accept | Outer `[Authorize(Policy = "AdminOnly")]` on AdminController (verified line ~20) uses the fixed GroupRole-aware AdminHandler and is the authoritative gate; the deleted inner check was provably redundant and permanently-false dead code. Plan 06 adds an integration test proving a real Admin can complete the reset end-to-end and a Player cannot reach the action. |
| T-34.3-06 | Denial of Service (regression stays broken) | AdminResetPasswordAsync always-fails for real Admins | mitigate | Deleting the dead clause restores the intended behavior; Plan 06 locks it with an end-to-end test. |
| T-34.3-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH: pure refactor). |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` and `dotnet build QuestBoard.Repository` both exit 0.
- No `IsInRole("Admin")` remains in Program.cs, AdminDashboardAuthFilter.cs, or as `IsInRoleAsync(adminEntity, "Admin")` in IdentityService.cs.
- Neither Hangfire file references IActiveGroupContext or the D-02 helper.
</verification>

<success_criteria>
- Hangfire is gated SuperAdmin-only at both twin sites; group Admins get no new access.
- AdminResetPasswordAsync no longer blocks real Admins on a dead role check.
- All three deletions are pure clause removals with no GroupRole plumbing added to middleware or the Repository layer.
</success_criteria>

<output>
Create `.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-05-SUMMARY.md` when done.
</output>
