---
phase: 34.3-group-role-authorization-regression-fix-inline-ownership-che
plan: 06
type: execute
wave: 3
depends_on: ["34.3-02", "34.3-03", "34.3-04", "34.3-05"]
files_modified:
  - QuestBoard.IntegrationTests/Controllers/QuestControllerAuthorizationRegressionTests.cs
  - QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/DungeonMasterControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05: integration tests mirroring AdminHandlerIntegrationTests lock in every newly-fixed inline site to prevent regression"
    - "Integration tests prove an Admin who is not the quest's DM sees Manage/recap-edit/DM-edit links and is not Forbid()-den"
    - "An integration test proves the null-ActiveGroupId AccountController case shows no badges without a 500 (fail-soft)"
    - "An integration test proves a group Admin (non-SuperAdmin) is denied /hangfire while SuperAdmin is allowed"
    - "An integration test proves a real Admin can complete AdminResetPassword end-to-end"
    - "The full suite is green"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/QuestControllerAuthorizationRegressionTests.cs"
      provides: "Regression tests for all 12 QuestController sites"
      contains: "GetEffectiveGroupRole"
    - path: "QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs"
      provides: "Profile/Edit badge tests incl. fail-soft null-group case"
      contains: "ActiveGroupId"
  key_links:
    - from: "QuestBoard.IntegrationTests test methods"
      to: "AuthenticationHelper.CreateAuthenticatedClientWithUserAsync"
      via: "role-seeded clients (Admin/DungeonMaster/Player/SuperAdmin) — no manual UserGroups seeding"
      pattern: "CreateAuthenticated"
---

<objective>
Add regression integration tests (D-05) locking in every fix from Plans 02-05, mirroring the `AdminHandlerIntegrationTests.cs` pattern. This class of bug (broken inline auth) went undetected precisely because no automated test covered it; these tests prevent recurrence on the next `AspNetUserRoles`-adjacent change.

Purpose: Behavioral proof that the migration restores correct Admin/DM access. Most sites return 200 OK either way (the bug lives in a `ViewBag` flag or a hidden `Forbid()`), so tests must assert on response BODY content for flag sites and on the ABSENCE of `Forbid()` for guard sites.
Output: Six site-groups covered by tests; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-CONTEXT.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-PATTERNS.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-RESEARCH.md
@.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-VALIDATION.md

<interfaces>
<!-- Test helpers confirmed present. Use these — do NOT hand-roll UserGroups seeding (Pitfall 4). -->
From QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs (each seeds the matching UserGroups.GroupRole row for GroupId=1 automatically):
```csharp
Task<(HttpClient client, User user)> CreateAuthenticatedClientWithUserAsync(factory, userName, email, roles: ["Admin"|"DungeonMaster"|"Player"]);
Task<...> CreateAuthenticatedSuperAdminClientAsync(factory);
Task<...> CreateAuthenticatedAdminClientAsync(factory);
Task<...> CreateAuthenticatedDMClientAsync(factory);
```

From QuestBoard.IntegrationTests/Helpers/MutableGroupContext.cs (test IActiveGroupContext, default ActiveGroupId=1):
```csharp
public class MutableGroupContext : IActiveGroupContext { public int? ActiveGroupId { get; set; } = 1; }
```

Standard per-test opener: `await TestDataHelper.ClearDatabaseAsync(_factory.Services);`
Assertion style (FluentAssertions): allow → `.Should().Be(HttpStatusCode.OK)`; deny → `.Should().BeOneOf(HttpStatusCode.Forbidden, HttpStatusCode.Redirect)`.
CancellationToken: pass `TestContext.Current.CancellationToken` to GetAsync/PostAsync as the existing tests do.

Style rule: new file (QuestControllerAuthorizationRegressionTests.cs) uses primary-constructor style `(WebApplicationFactoryBase factory) : IClassFixture<WebApplicationFactoryBase>`. Extended existing files must match whatever style that file already uses.

View flag → body markers to assert on (confirm exact link text/markup by reading each view in <read_first>):
- QuestController.Details `ViewBag.CanManage` → Views/Quest/Details.cshtml (Manage Quest link/card)
- QuestController.Manage `ViewBag.IsAdmin` → Views/Quest/Manage.cshtml
- QuestLogController.Details `ViewBag.CanEditRecap` → Views/QuestLog/Details.cshtml (recap-edit link)
- DungeonMasterController.Profile `CanEdit` → Views/DungeonMaster/Profile.cshtml (edit link)
- AccountController Profile badges → Views/Account/Profile.cshtml (ViewData IsAdmin/IsDungeonMaster)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuestController regression tests (new file) + QuestLog/DungeonMaster extensions</name>
  <files>QuestBoard.IntegrationTests/Controllers/QuestControllerAuthorizationRegressionTests.cs, QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/DungeonMasterControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AdminHandlerIntegrationTests.cs (the canonical allow/deny/SuperAdmin/unauthenticated pattern to mirror, full file)
    - QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs (existing file to EXTEND — match its style; do not create a duplicate)
    - QuestBoard.IntegrationTests/Controllers/DungeonMasterControllerIntegrationTests.cs (existing file to EXTEND — match its style; do not create a duplicate)
    - QuestBoard.IntegrationTests/Controllers/QuestControllerIntegrationTests_Comprehensive.cs (existing QuestController coverage — check what Index/Manage cases it already covers to avoid duplicating; put new regression cases in the NEW file QuestControllerAuthorizationRegressionTests.cs)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs and TestDataHelper.cs (confirm exact helper signatures and how to seed a quest owned by a DIFFERENT DM than the test Admin — needed so the Admin is genuinely not-the-owner)
    - QuestBoard.Service/Views/Quest/Details.cshtml, Views/Quest/Manage.cshtml, Views/QuestLog/Details.cshtml, Views/DungeonMaster/Profile.cshtml (to pick the exact body markers to assert presence/absence of)
  </read_first>
  <action>
    Create `QuestBoard.IntegrationTests/Controllers/QuestControllerAuthorizationRegressionTests.cs` (primary-constructor style). Seed a quest whose DungeonMaster is a DIFFERENT user than the authenticated Admin under test (use TestDataHelper / direct context seeding as the comprehensive test file does), so the tests exercise the "Admin who is not the quest's DM" regression path — not the owner path.

    Cover, at minimum:
    - Details `ViewBag.CanManage`: GET the quest details page as an Admin-who-is-not-the-DM → 200 AND body CONTAINS the Manage-Quest marker; as an anonymous client → 200 with no exception and body does NOT contain the marker (and no 500).
    - Manage `ViewBag.IsAdmin`/`IsAuthorized`: GET the Manage page as the non-owner Admin → 200 (not Forbidden) AND body contains the admin-only marker driven by `ViewBag.IsAdmin`.
    - Forbid() guards (Edit GET, Delete, Finalize, Open, SendReminder, CreateFollowUp GET — pick the GET/idempotent ones that can be exercised via a request; for POST-only guards use the appropriate POST with an anti-forgery token if the existing comprehensive tests demonstrate a helper for that, otherwise assert the GET variant): a non-owner Admin does NOT receive `HttpStatusCode.Forbidden`. This incorrect Forbid IS the regression.
    - A Player who is neither owner nor Admin still receives Forbidden/Redirect on the guarded actions (proves the fix did not over-grant).
    - SuperAdmin bypass allowed on a guarded action.

    Extend `QuestLogControllerIntegrationTests.cs` (match its style): Details `ViewBag.CanEditRecap` shows the recap-edit marker for a non-owner Admin on a completed quest; UpdateRecap is not Forbid()-den for a non-owner Admin; a Player is denied.

    Extend `DungeonMasterControllerIntegrationTests.cs` (match its style): Profile `CanEdit` shows the edit marker for a non-owner Admin viewing another DM's profile; EditProfile GET is not Forbid()-den for a non-owner Admin; a Player is denied.

    Use `AuthenticationHelper.CreateAuthenticated...` helpers exclusively for auth+role seeding — do NOT hand-roll `context.UserGroups.Add(...)` (Pitfall 4). Do not embed phase/decision IDs in test names or comments.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestControllerAuthorizationRegressionTests|FullyQualifiedName~QuestLogControllerIntegrationTests|FullyQualifiedName~DungeonMasterControllerIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - QuestControllerAuthorizationRegressionTests.cs exists and contains a test asserting a non-owner Admin's Details response body contains the Manage-Quest marker
    - It contains a test asserting a non-owner Admin does NOT get `HttpStatusCode.Forbidden` on at least Edit, Delete, Finalize, Open, and CreateFollowUp guarded actions
    - It contains a Player-denied test and a SuperAdmin-allowed test on a guarded action
    - QuestLogControllerIntegrationTests.cs gained a non-owner-Admin CanEditRecap test and an UpdateRecap not-Forbidden test
    - DungeonMasterControllerIntegrationTests.cs gained a non-owner-Admin CanEdit test and an EditProfile not-Forbidden test
    - No `context.UserGroups.Add(` appears in any new/modified test method
    - `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestControllerAuthorizationRegressionTests|FullyQualifiedName~QuestLogControllerIntegrationTests|FullyQualifiedName~DungeonMasterControllerIntegrationTests"` exits 0
  </acceptance_criteria>
  <done>QuestController regression tests exist and QuestLog/DungeonMaster tests are extended; all cover the non-owner-Admin allow path, the Player deny path, and (where applicable) SuperAdmin; the scoped test filter passes.</done>
</task>

<task type="auto">
  <name>Task 2: AccountController badge tests (incl. fail-soft null-group), Hangfire deny test, AdminResetPassword test, full-suite gate</name>
  <files>QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs (existing file to EXTEND — match its primary-constructor style; note how it obtains the WebApplicationFactoryBase and whether tests run against a shared fixture)
    - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs (existing file to EXTEND — for the AdminResetPassword end-to-end test; note it currently only tests the unauthenticated-redirect case for /Admin/ResetPassword/1)
    - QuestBoard.IntegrationTests/Helpers/WebApplicationFactoryBase.cs and MutableGroupContext.cs (confirm how to set `MutableGroupContext.ActiveGroupId = null` for the fail-soft test, and how to restore it / whether the fixture is shared so isolation matters — resolve the ActiveGroupId back to 1 after the null test, or use a scoped approach that does not leak to sibling tests)
    - QuestBoard.Service/Views/Account/Profile.cshtml (confirm the badge markup driven by ViewData["IsAdmin"]/["IsDungeonMaster"] to assert on)
    - Grep QuestBoard.IntegrationTests for any existing /hangfire test to avoid duplication (RESEARCH found none)
  </read_first>
  <action>
    Extend `AccountControllerIntegrationTests.cs`:
    - Profile badges (active group = 1 default): as an Admin → response body contains the Admin badge marker; as a DungeonMaster → contains the DungeonMaster badge marker; as a Player → contains neither.
    - Edit combined flag: a DungeonMaster or Admin sees the DM-enabled UI marker; a Player does not.
    - Fail-soft null-group (D-04): set `MutableGroupContext.ActiveGroupId = null` (obtain the registered instance from `_factory.Services`), then GET /Account/Profile as an authenticated Admin → response is 200 (NOT 500) and the body contains NO role badge markers. Restore `ActiveGroupId = 1` afterward (in a finally or at test end) so sibling tests using the default are unaffected — confirm the isolation approach against how the fixture is shared.

    Add a Hangfire-access test (D-01) — new test in an appropriate existing controller/dashboard test file, or a small new file if none fits: a group Admin (non-SuperAdmin) client is DENIED /hangfire (redirect to login or non-200), and a SuperAdmin client is NOT denied. Note: the raw Hangfire middleware is guarded by `if (!app.Environment.IsEnvironment("Testing"))` — confirm whether /hangfire is reachable under the Testing environment used by the factory; if the middleware is short-circuited in Testing such that the access check cannot be exercised via HTTP, instead assert `AdminDashboardAuthFilter.Authorize` directly as a unit-style test (construct a DashboardContext or test the filter's SuperAdmin-only logic in QuestBoard.UnitTests), and document in the SUMMARY which approach was used and why.

    Extend `AdminControllerIntegrationTests.cs` (24th site): a real Admin client POSTs /Admin/ResetPassword for another user and the operation succeeds (not blocked by the removed inner Admin check) — assert a success response/redirect rather than the "Admin user not found or not authorized" failure. Reuse the existing anti-forgery-token acquisition pattern already present in that file for POST actions.

    Use `AuthenticationHelper.CreateAuthenticated...` helpers for all auth+role seeding (Pitfall 4). No phase/decision-ID strings in test names or comments.

    Finally, run the full suite as the phase gate.
  </action>
  <verify>
    <automated>dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - AccountControllerIntegrationTests.cs contains an Admin-badge test, a DungeonMaster-badge test, and a Player-no-badge test asserting on response body markers
    - It contains a null-ActiveGroupId test that sets `MutableGroupContext.ActiveGroupId = null`, asserts the Profile response is 200 (not 500) with no badge markers, and restores ActiveGroupId
    - A Hangfire test asserts a non-SuperAdmin group Admin is denied and SuperAdmin is allowed (via HTTP or, if Testing short-circuits the middleware, via a direct AdminDashboardAuthFilter/unit assertion documented in the SUMMARY)
    - AdminControllerIntegrationTests.cs contains a test where a real Admin successfully performs ResetPassword for another user (not the "not authorized" failure)
    - No `context.UserGroups.Add(` appears in any new/modified test method
    - `dotnet test` (full suite) exits 0
  </acceptance_criteria>
  <done>AccountController badge tests (including the D-04 fail-soft null-group case), the Hangfire D-01 deny/allow test, and the AdminResetPassword end-to-end test all exist and pass; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client (seeded role) → controller action | Tests exercise the real HTTP pipeline incl. middleware, DI, auth handlers, and the in-memory DB |
| MutableGroupContext.ActiveGroupId mutation | The null-group test mutates shared fixture state; must restore to avoid cross-test contamination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-17 | Denial of Service (false-negative tests) | manual UserGroups seeding with a GroupId mismatch | mitigate | Use CreateAuthenticated... helpers exclusively (they seed GroupId=1 to match MutableGroupContext's default); acceptance criterion forbids `context.UserGroups.Add(`. Prevents a confusing false failure (Pitfall 4). |
| T-34.3-18 | Tampering (test isolation) | null-ActiveGroupId test leaking into sibling tests | mitigate | Restore `MutableGroupContext.ActiveGroupId = 1` after the null test (finally/scoped); read WebApplicationFactoryBase to confirm the fixture sharing model before choosing the restore strategy. |
| T-34.3-19 | Elevation of Privilege (untested grant) | a fixed site with no regression test could silently re-break later | mitigate | This plan is the D-05 lock-in: each of the six site groups gets at least one allow-path and one deny-path assertion. |
| T-34.3-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase. |
</threat_model>

<verification>
- Scoped filter passes after Task 1: `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestControllerAuthorizationRegressionTests|FullyQualifiedName~QuestLogControllerIntegrationTests|FullyQualifiedName~DungeonMasterControllerIntegrationTests"` exits 0.
- Full suite passes after Task 2: `dotnet test` exits 0.
- No `context.UserGroups.Add(` in any new/modified test.
- The null-ActiveGroupId fail-soft test asserts 200 (not 500) and restores state.
</verification>

<success_criteria>
- All six site groups (QuestController, QuestLog, DungeonMaster, AccountController incl. fail-soft, Hangfire, IdentityService/AdminResetPassword) have regression coverage.
- Each covers a non-owner-Admin allow path and a Player/anonymous deny path.
- The full suite is green — the phase gate is met.
</success_criteria>

<output>
Create `.planning/phases/34.3-group-role-authorization-regression-fix-inline-ownership-che/34.3-06-SUMMARY.md` when done.
</output>
