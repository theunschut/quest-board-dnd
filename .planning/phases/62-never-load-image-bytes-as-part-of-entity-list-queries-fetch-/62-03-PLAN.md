---
phase: 62-never-load-image-bytes-as-part-of-entity-list-queries-fetch
plan: 03
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - QuestBoard.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs
  - QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs
  - QuestBoard.Service/ViewModels/DungeonMasterViewModels/EditDMProfileViewModel.cs
  - QuestBoard.Service/Automapper/ViewModelProfile.cs
  - QuestBoard.Service/Controllers/Characters/CharactersController.cs
  - QuestBoard.Service/Controllers/Contacts/ContactsController.cs
  - QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs
  - QuestBoard.Service/Views/Characters/Index.cshtml
  - QuestBoard.Service/Views/Characters/Index.Mobile.cshtml
  - QuestBoard.Service/Views/Characters/Details.cshtml
  - QuestBoard.Service/Views/Characters/Details.Mobile.cshtml
  - QuestBoard.Service/Views/Characters/Create.cshtml
  - QuestBoard.Service/Views/Characters/Edit.cshtml
  - QuestBoard.Service/Views/Characters/Edit.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Index.cshtml
  - QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Details.cshtml
  - QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml
  - QuestBoard.Service/Views/Contacts/Edit.cshtml
  - QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml
  - QuestBoard.Service/Views/DungeonMaster/EditProfile.cshtml
  - QuestBoard.Service/Views/DungeonMaster/EditProfile.Mobile.cshtml
  - QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/DungeonMasterControllerIntegrationTests.cs
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Character/Contact/DM list, detail, edit, and create views show a portrait when one exists using a boolean flag, not a byte[] null-check"
    - "Creating a Character/Contact with a new photo still stores the uploaded image"
    - "DM Edit Profile page shows the current-image thumbnail for a DM who has a photo"
    - "No display-path ViewModel property carries raw image bytes"
  artifacts:
    - path: "QuestBoard.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs"
      provides: "HasProfilePicture bool replacing the byte[] ProfilePicture display property"
      contains: "HasProfilePicture"
    - path: "QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs"
      provides: "HasContactImage bool replacing the byte[] ContactImage display property"
      contains: "HasContactImage"
    - path: "QuestBoard.Service/ViewModels/DungeonMasterViewModels/EditDMProfileViewModel.cs"
      provides: "HasProfilePicture bool replacing the byte[] ProfilePicture display property"
      contains: "HasProfilePicture"
    - path: "QuestBoard.Service/Automapper/ViewModelProfile.cs"
      provides: "Domain->ViewModel maps updated to the new bool members"
  key_links:
    - from: "QuestBoard.Service/Controllers/Characters/CharactersController.cs"
      to: "QuestBoard.Domain/Models/Character.cs"
      via: "Create POST sets character.ProfilePicture post-map from a local variable"
      pattern: "character.ProfilePicture ="
    - from: "QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs"
      to: "QuestBoard.Domain/Models/DungeonMasterProfile.cs"
      via: "Profile/EditProfile GET source HasProfilePicture from profile.HasProfilePicture"
      pattern: "profile\\?.HasProfilePicture"
---

<objective>
Complete the display-path conversion (CONTEXT.md D-05, RESEARCH.md Pitfalls 2 and 4): replace the raw `byte[]` display properties on `CharacterViewModel`/`ContactViewModel`/`EditDMProfileViewModel` with `HasProfilePicture`/`HasContactImage` booleans; update the two AutoMapper Domain→ViewModel maps; fix the two Create-POST controller actions that currently stage uploaded bytes onto the (now-bool) ViewModel property to instead use a local variable and set the mapped Domain model's byte[] post-map; fix the DM controller's `HasProfilePicture` source expressions (Pitfall 2, cosmetic regression); and update all affected Razor views' `!= null` / `?.Length > 0` gates to the new boolean property. Views already render `<img>` via dedicated per-entity endpoints (Phase 46), so this is a mechanical existence-gate rename, not a rendering change.

Purpose: Removes raw image bytes from the display-path ViewModel layer end-to-end and keeps the Create/Edit write paths and the DM edit-profile thumbnail working after Plan 01's Domain change.

Output: Three ViewModels with bool display flags; updated ViewModelProfile maps; two Create actions using local-variable staging; DM controller sourcing the bool from the projected Domain flag; 15 views gating on the bool; extended controller integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-never-load-image-bytes-as-part-of-entity-list-queries-fetch-/62-CONTEXT.md
@.planning/phases/62-never-load-image-bytes-as-part-of-entity-list-queries-fetch-/62-RESEARCH.md
@.planning/phases/62-never-load-image-bytes-as-part-of-entity-list-queries-fetch-/62-PATTERNS.md

# Existing correct precedent to mirror:
@QuestBoard.Service/ViewModels/DungeonMasterViewModels/DMProfileViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the three display-path ViewModels to booleans and update the two Domain->ViewModel AutoMapper maps</name>
  <files>QuestBoard.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs, QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs, QuestBoard.Service/ViewModels/DungeonMasterViewModels/EditDMProfileViewModel.cs, QuestBoard.Service/Automapper/ViewModelProfile.cs</files>
  <read_first>
    - QuestBoard.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs (ProfilePicture byte[]? at line 35; leave ProfilePictureFile/CroppedPictureFile IFormFile at 39-43 alone — those are upload-binding, unrelated)
    - QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs (ContactImage byte[]? at line 22; leave ContactImageFile/CroppedPictureFile at 26-30 alone)
    - QuestBoard.Service/ViewModels/DungeonMasterViewModels/EditDMProfileViewModel.cs (ProfilePicture byte[]? at line 13; leave ProfilePictureFile/CroppedPictureFile at 17-21 alone)
    - QuestBoard.Service/ViewModels/DungeonMasterViewModels/DMProfileViewModel.cs (line 8 HasProfilePicture bool — the target shape)
    - QuestBoard.Service/Automapper/ViewModelProfile.cs (Character->CharacterViewModel 63-66 maps ProfilePicture by convention; CharacterViewModel->Character 69-71; Contact->ContactViewModel 78-81 with explicit ContactImage ForMember at 79; ContactViewModel->Contact 84-88 with ContactImageData ForMember at 85)
  </read_first>
  <action>
    In `CharacterViewModel`, replace `public byte[]? ProfilePicture { get; set; }` with `public bool HasProfilePicture { get; set; }`. In `ContactViewModel`, replace `public byte[]? ContactImage { get; set; }` with `public bool HasContactImage { get; set; }`. In `EditDMProfileViewModel`, replace `public byte[]? ProfilePicture { get; set; }` (drop the trailing comment about raw bytes) with `public bool HasProfilePicture { get; set; }`. Do not touch any `IFormFile?` upload properties.

    In `ViewModelProfile.cs`:
    - `Character -> CharacterViewModel`: the source now exposes `Character.HasProfilePicture` (added in Plan 01) and the destination now has `CharacterViewModel.HasProfilePicture` — convention mapping matches by name, so no explicit ForMember is required for it. Remove any now-invalid reference to the old byte[] `ProfilePicture` if present; keep the existing OwnerName/IsOwner/ProfilePictureFile members.
    - `Contact -> ContactViewModel`: change the explicit `ForMember(dest => dest.ContactImage, opt => opt.MapFrom(src => src.ContactImageData))` to `ForMember(dest => dest.HasContactImage, opt => opt.MapFrom(src => src.HasContactImage))`. Keep the ContactImageFile/CanManage ignores.
    - `ContactViewModel -> Contact` (reverse): remove the `ForMember(dest => dest.ContactImageData, opt => opt.MapFrom(src => src.ContactImage))` line entirely — per Pitfall 4 the bool must NOT map back onto the Domain byte[]; the Create controller sets `ContactImageData` directly post-map (Task 2). Add `ForMember(dest => dest.ContactImageData, opt => opt.Ignore())` so AutoMapper does not attempt a convention match on the removed source. Keep the CreatedByUser/CreatedAt/Notes ignores.
    - `CharacterViewModel -> Character` (reverse): the ViewModel no longer has a byte[] to map; ensure `Character.ProfilePicture` is not convention-mapped from the bool. Add `ForMember(dest => dest.ProfilePicture, opt => opt.Ignore())` (the Create controller sets it directly post-map in Task 2). Keep the existing Owner/CreatedAt ignores.

    No fenced code in comments; plain-language only, no tracking IDs.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: none of the three ViewModels contains `byte[]? ProfilePicture` or `byte[]? ContactImage` as a display property; each contains the corresponding `bool HasProfilePicture`/`bool HasContactImage`
    - Source assertion: `ViewModelProfile.cs` maps `HasContactImage` from `src.HasContactImage`, ignores `ContactImageData` and `ProfilePicture` on the reverse (ViewModel->Domain) maps
    - Build assertion: `QuestBoard.Service` compiles (this proves no other C# code still references the removed byte[] display properties — controllers are fixed in Task 2, so this task's build may surface controller errors; that is expected and Task 2 resolves them. If controller references block the build, they are the ONLY expected failures and must be exactly the ones Task 2 addresses.)
  </acceptance_criteria>
  <done>The three ViewModels expose booleans; the AutoMapper Domain<->ViewModel maps route the bool forward and ignore the byte[] on the reverse direction.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the two Create-POST local-variable staging and the DM controller HasProfilePicture source expressions</name>
  <files>QuestBoard.Service/Controllers/Characters/CharactersController.cs, QuestBoard.Service/Controllers/Contacts/ContactsController.cs, QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Characters/CharactersController.cs (Create POST: line 155 `viewModel.ProfilePicture = memoryStream.ToArray()`, mapper.Map at 165; Edit POST 206-327 stages onto existingCharacter.ProfilePicture at 297 — the Domain model, unaffected; GetProfilePicture 397 unaffected)
    - QuestBoard.Service/Controllers/Contacts/ContactsController.cs (Create POST: line 127 `viewModel.ContactImage = memoryStream.ToArray()`, mapper.Map at 137; Edit POST stages onto existingContact.ContactImageData at 227 — unaffected)
    - QuestBoard.Service/Controllers/DungeonMaster/DungeonMasterController.cs (Profile GET line 43 `HasProfilePicture = profile?.ProfilePicture?.Length > 0`; EditProfile GET line 77 `ProfilePicture = profile?.ProfilePicture`; EditProfile POST 83-148 builds imageBytes from IFormFile only — unaffected)
  </read_first>
  <action>
    CharactersController Create POST: replace `viewModel.ProfilePicture = memoryStream.ToArray();` with a local `var uploadedOriginalImageData = memoryStream.ToArray();`. After `var character = mapper.Map<Character>(viewModel);` (line ~165), add `character.ProfilePicture = uploadedOriginalImageData;`. Declare the local at a scope visible after the mapper.Map call (e.g. `byte[]? uploadedOriginalImageData = null;` before the upload `if`, assigned inside it). Leave the Edit POST path unchanged — it already stages onto the Domain model `existingCharacter.ProfilePicture`, and its data-loss safety comes from the Plan 02 service fix.

    ContactsController Create POST: identical pattern — replace `viewModel.ContactImage = memoryStream.ToArray();` (line 127) with a local `byte[]? uploadedOriginalImageData`, and after `var contact = mapper.Map<Contact>(viewModel);` (line ~137) add `contact.ContactImageData = uploadedOriginalImageData;`. Leave Edit POST unchanged.

    DungeonMasterController: at Profile GET (line 43), change `HasProfilePicture = profile?.ProfilePicture?.Length > 0` to `HasProfilePicture = profile?.HasProfilePicture ?? false` (source the projected Domain bool from Plan 01, since `profile.ProfilePicture` is now always null on this read). At EditProfile GET (line 77), change `ProfilePicture = profile?.ProfilePicture` to `HasProfilePicture = profile?.HasProfilePicture ?? false` (the ViewModel property is now a bool). Leave EditProfile POST untouched — it reads only freshly-uploaded IFormFiles.

    Plain-language comments only; no tracking IDs.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `CharactersController.cs` Create POST no longer assigns to `viewModel.ProfilePicture`; it assigns `character.ProfilePicture` from a local after the mapper.Map call
    - Source assertion: `ContactsController.cs` Create POST no longer assigns to `viewModel.ContactImage`; it assigns `contact.ContactImageData` from a local after the mapper.Map call
    - Source assertion: `DungeonMasterController.cs` sources `HasProfilePicture` from `profile?.HasProfilePicture ?? false` at both the Profile GET and EditProfile GET call sites; no remaining reference to `profile?.ProfilePicture`
    - Build assertion: `QuestBoard.Service` compiles cleanly (Task 1's ViewModel rename + this task together resolve all controller references)
  </acceptance_criteria>
  <done>Both Create actions stage uploaded bytes on the Domain model post-map; the DM controller sources the bool from the projected Domain flag; the Service project builds with no C# errors.</done>
</task>

<task type="auto">
  <name>Task 3: Update the 15 affected Razor views to the boolean gate and extend controller integration tests</name>
  <files>QuestBoard.Service/Views/Characters/Index.cshtml, QuestBoard.Service/Views/Characters/Index.Mobile.cshtml, QuestBoard.Service/Views/Characters/Details.cshtml, QuestBoard.Service/Views/Characters/Details.Mobile.cshtml, QuestBoard.Service/Views/Characters/Create.cshtml, QuestBoard.Service/Views/Characters/Edit.cshtml, QuestBoard.Service/Views/Characters/Edit.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Index.cshtml, QuestBoard.Service/Views/Contacts/Index.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Details.cshtml, QuestBoard.Service/Views/Contacts/Details.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Edit.cshtml, QuestBoard.Service/Views/Contacts/Edit.Mobile.cshtml, QuestBoard.Service/Views/DungeonMaster/EditProfile.cshtml, QuestBoard.Service/Views/DungeonMaster/EditProfile.Mobile.cshtml, QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/DungeonMasterControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.Service/Views/Characters/Index.cshtml (lines 36, 111 `@if (character.ProfilePicture != null)`), Index.Mobile.cshtml (22, 71), Details.cshtml (16), Details.Mobile.cshtml (18), Edit.cshtml (27), Edit.Mobile.cshtml (24), Create.cshtml (30 `@if (Model.ProfilePicture != null && isEdit)`)
    - QuestBoard.Service/Views/Contacts/Index.cshtml (48 `@if (contact.ContactImage != null)`), Index.Mobile.cshtml (40), Details.cshtml (13), Details.Mobile.cshtml (15), Edit.cshtml (24), Edit.Mobile.cshtml (22)
    - QuestBoard.Service/Views/DungeonMaster/EditProfile.cshtml (26 `@if (Model.ProfilePicture?.Length > 0)`), EditProfile.Mobile.cshtml (29)
    - QuestBoard.Service/Views/DungeonMaster/Profile.cshtml (13 `@if (Model.HasProfilePicture)`) — the already-correct precedent every other view converges toward
    - QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs, ContactsControllerIntegrationTests.cs, DungeonMasterControllerIntegrationTests.cs (existing test structure; Mvc.Testing + EF InMemory)
  </read_first>
  <action>
    In each Character view, change `@if (character.ProfilePicture != null)` / `@if (Model.ProfilePicture != null)` to the boolean equivalent (`@if (character.HasProfilePicture)` / `@if (Model.HasProfilePicture)`). For `Characters/Create.cshtml:30`, change `@if (Model.ProfilePicture != null && isEdit)` to `@if (Model.HasProfilePicture && isEdit)` — on a fresh Create the bool defaults to false so the current-image thumbnail correctly stays hidden, preserving existing behavior. In each Contact view, change `@if (contact.ContactImage != null)` / `@if (Model.ContactImage != null)` to `@if (contact.HasContactImage)` / `@if (Model.HasContactImage)`. In the two DM EditProfile views, change `@if (Model.ProfilePicture?.Length > 0)` to `@if (Model.HasProfilePicture)`. Do NOT touch the `<img src="@Url.Action(...)">` lines (Phase 46 already repointed them to the dedicated endpoints) or any `asp-for="...File"` upload inputs.

    Extend the three controller integration test files so at least one test per controller renders a list/detail/edit page for an entity known to have an image and asserts the `<img>` element (or the endpoint URL) is present, and one for an entity without an image asserts it is absent — proving the bool gate drives rendering correctly end-to-end. If a Create-with-photo round-trip test is feasible in the existing harness, add one asserting the uploaded image is retrievable afterward (guards the Task 2 Create staging fix). Follow the existing integration-test conventions in each file (WebApplicationFactory, seeded groups, auth).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj && dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~CharactersControllerIntegrationTests|FullyQualifiedName~ContactsControllerIntegrationTests|FullyQualifiedName~DungeonMasterControllerIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: no view under Views/Characters, Views/Contacts, or Views/DungeonMaster contains `.ProfilePicture != null`, `.ContactImage != null`, or `.ProfilePicture?.Length > 0` (grep across those view folders returns zero matches)
    - Source assertion: the 15 listed views gate the portrait block on `HasProfilePicture` / `HasContactImage`
    - Behavior assertion: new integration tests assert the portrait `<img>`/endpoint renders for an entity with an image and does not for one without
    - Test command: the filtered integration test run passes green
  </acceptance_criteria>
  <done>All 15 views gate on the boolean; the `<img>` endpoints are untouched; the filtered controller integration tests (existing + new) pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Server (controller/view) → Browser | Rendered HTML; the portrait `<img>` fetches bytes via a separate authorized endpoint request, not inline |
| Browser (Create/Edit form) → Frontend Server (controller) | Uploaded image file crosses here; already validated by ImageValidationService (unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03-ID | Information Disclosure | Portrait image endpoints (GetProfilePicture/GetContactImage/GetCroppedContactImage/GetDMProfilePicture) | accept | Endpoints are unchanged by this plan; their existing group-scoping/visibility checks (e.g. ContactsController IsVisibleTo) remain the access-control boundary. This plan only swaps a display existence-gate from byte[] null-check to bool; it removes raw bytes from the ViewModel, marginally reducing exposure. |
| T-62-03-DL | Tampering (data integrity) | Create-POST local-variable staging | mitigate | Setting the uploaded bytes on the mapped Domain model post-map (not the removed ViewModel property) keeps new-character/new-contact photo persistence working; integration test guards it. |
| T-62-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase — not applicable. |
</threat_model>

<verification>
- `QuestBoard.Service` compiles; the filtered controller integration tests pass.
- Grep over the three view folders confirms zero remaining `ProfilePicture != null` / `ContactImage != null` / `ProfilePicture?.Length > 0` display gates.
- Manual UAT (VALIDATION.md): (1) edit a character/contact non-photo field only, confirm the photo still serves; (2) open DM Edit Profile for a DM with a photo, confirm the current-image thumbnail renders; (3) create a character/contact with a new photo, confirm it displays.
</verification>

<success_criteria>
- No display-path ViewModel property carries raw image bytes.
- Create actions persist uploaded photos via post-map Domain assignment.
- DM Edit Profile thumbnail renders for DMs with a photo (Pitfall 2 resolved).
- All 15 views gate portrait rendering on the boolean; image endpoints untouched.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

ViewModel property changes (byte[] display prop -> bool):
- `CharacterViewModel.ProfilePicture` (byte[]) -> `CharacterViewModel.HasProfilePicture` (bool)
- `ContactViewModel.ContactImage` (byte[]) -> `ContactViewModel.HasContactImage` (bool)
- `EditDMProfileViewModel.ProfilePicture` (byte[]) -> `EditDMProfileViewModel.HasProfilePicture` (bool)

AutoMapper changes (QuestBoard.Service/Automapper/ViewModelProfile.cs):
- `Contact -> ContactViewModel`: ForMember HasContactImage from src.HasContactImage (replaces ContactImage-from-ContactImageData)
- `ContactViewModel -> Contact`: ContactImageData now Ignore() (was MapFrom ContactImage)
- `CharacterViewModel -> Character`: ProfilePicture now Ignore()
- `Character -> CharacterViewModel`: HasProfilePicture by convention (no explicit ForMember)

Controller changes (no new signatures — action bodies changed):
- `CharactersController.Create` POST — local-variable staging + post-map `character.ProfilePicture` assignment
- `ContactsController.Create` POST — local-variable staging + post-map `contact.ContactImageData` assignment
- `DungeonMasterController.Profile` GET and `EditProfile` GET — HasProfilePicture sourced from `profile?.HasProfilePicture ?? false`

View changes: 15 Razor views gate portrait rendering on the new boolean.

New integration test cases in the three controller integration test files.
</artifacts_produced>

<output>
Create `.planning/phases/62-never-load-image-bytes-as-part-of-entity-list-queries-fetch-/62-03-SUMMARY.md` when done
</output>
