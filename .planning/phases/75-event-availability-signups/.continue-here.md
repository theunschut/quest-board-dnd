---
context: phase
phase: 75-event-availability-signups
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-08-27T14:16:24.791Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Tenant query filter hides assertion reads | In 75-02, unit-test assertions that read back rows under an `ActiveGroupId = null` context returned empty, because the codebase's global query filter fails closed. It looks like the write did not happen when it did. | advisory | When asserting on persisted rows in tests, add `.IgnoreQueryFilters()` to the assertion read, matching the existing `PlayerSignupRepositoryTests.cs` convention. 75-05 writes integration tests and will hit this. |
| Pre-existing EF Tools reference in the Service project | `QuestBoard.Service.csproj` already referenced `Microsoft.EntityFrameworkCore.Tools` before phase 75 began (confirmed against `HEAD~1` during 75-01). This violates the CLAUDE.md rule that EF packages belong only in `QuestBoard.Repository`, and it makes 75-01's Task 2 grep criterion fail for a reason unrelated to any phase-75 change. | advisory | Do not attribute this to phase 75 work, and do not "fix" it inside a phase-75 plan — it is out of scope. Track it separately if it should be cleaned up. |

<current_state>
Phase 75 was mid-execution under `/gsd-execute-phase 75` (wave-based parallel execution, git worktree isolation).

Waves 1 and 2 are **complete and merged** into `milestone/v9-rolling-improvements` at commit `642d021`. Wave 3 had just started: plan 75-04 was dispatched, then deliberately stopped before it made any commits (the user needed to pause). Its worktree and branch were auto-removed with nothing lost. Plan 75-05 was never dispatched.

Working tree is clean apart from one unrelated untracked file (`.planning/phases/80-contact-categories/80-DISCUSS-CHECKPOINT.json`).
</current_state>

<completed_work>

Completed plans (all merged, SUMMARY.md committed for each):
- **75-01** Event signup data tier — `EventSignup` domain model, `IEventSignupRepository`/`IEventSignupService`, narrow scalar-update `EventSignupRepository`, both AutoMapper entity maps, both DI registrations, `EventEntity.Signups` navigation, unit tests for the `UpdatedAt` stamping rule. No migration required.
- **75-02** Campaign membership availability sync — joining a campaign board backfills a Yes row on every event dated today or later (atomic with the join); leaving deletes that member's event signups on that board. Both hook the verified `GroupService` chokepoints. Platform Members remove control gained a destructive-action confirmation. Marked EVTAVAIL-02 and EVTAVAIL-04 complete in REQUIREMENTS.md.
- **75-03** Controller writes and fan-out — `EventSignupViewModel`, roster/availability state on `EventViewModel`, `SetAvailability` and `Withdraw` actions on `EventsController`, ViewModelProfile maps, and an atomic single-save campaign fan-out on event create.

Gates run and passed after both waves:
- Post-merge build: 0 errors (20 pre-existing NU1608 package-constraint warnings, unrelated).
- Post-merge tests: 805 passing (333 unit + 472 integration), 0 failures.
- Capability gates: schema-drift clear, ui.safety-gate clear. codebase-drift raised an advisory for `.claude/`, `LICENSE`, `README.md` — stale codebase map, unrelated to phase 75, no remap requested.
- Tracking commits: `e95086d` (after wave 1), `642d021` (after wave 2).
</completed_work>

<remaining_work>

**Wave 3 — both plans, neither started:**
- **75-04** Event details availability surface (3 tasks) — three answer buttons on `Views/Events/Details.cshtml` following the established vote idiom, a withdraw control on one-shot boards, a named roster visible to every board member, and a delete confirmation stating how much availability the delete destroys. Plus `EventDetailsAvailabilityRenderTests.cs`.
- **75-05** Requirement test suite and validation contract (3 tasks) — integration tests proving all five EVTAVAIL requirements including cross-board tenant isolation, and filling in `75-VALIDATION.md`.

These two have **no `files_modified` overlap** and are safe to run in parallel.

**After wave 3, the workflow still owes:**
1. Post-merge build and test gate
2. Code-review gate (`workflow.code_review` is `true` — auto-invokes `/gsd-code-review 75`)
3. Regression gate against prior phases' tests
4. `gsd-verifier` phase goal verification → `75-VERIFICATION.md`
5. Phase completion (`phase.complete` → ROADMAP/STATE/REQUIREMENTS)
</remaining_work>

<decisions_made>

- **Stopped the in-flight 75-04 executor rather than leaving it running.** It had zero commits at the time; letting it continue would have produced commits on a worktree branch with no orchestrator present to merge them. Verified afterwards that its worktree and branch were removed cleanly and no work was lost.
- **Used a 9-minute test-gate budget instead of the workflow's 5-minute default.** With ~805 tests, a 300s timeout returns exit 124, which the workflow treats as inconclusive and which blocks the tracking update. The longer budget produced a conclusive pass both times.
</decisions_made>

<blockers>
None. Execution is resumable immediately.
</blockers>

## Required Reading (in order)
1. `.planning/phases/75-event-availability-signups/75-03-SUMMARY.md` — the view-model contract 75-04 renders against (`EventSignupViewModel`, roster/availability state on `EventViewModel`, the `SetAvailability`/`Withdraw` action signatures)
2. `.planning/phases/75-event-availability-signups/75-04-PLAN.md` and `75-05-PLAN.md` — the two remaining plans
3. `CLAUDE.md` UI/UX Design Guidelines — 75-04 touches a Razor view; the modern-card pattern, FontAwesome icons with `me-2`, filled colored buttons, and the `<hr>` before button sections all apply
4. `.planning/phases/75-event-availability-signups/75-01-SUMMARY.md` and `75-02-SUMMARY.md` — what already exists in the data and membership tiers

## Infrastructure State
- Branch: `milestone/v9-rolling-improvements` at `642d021`, clean tree
- Worktrees: none outstanding — all wave 1 and 2 worktrees merged and removed
- `worktree.baseRef` is `head`, so new worktrees fork from live HEAD; no sequential degrade needed
- No running servers, watchers, or async external jobs
- Note: 75-05-PLAN.md and 75-VALIDATION.md contain placeholder text by design — filling `75-VALIDATION.md` is 75-05's job, not a false completion

<context>
The phase was progressing cleanly with no failures — three plans done, both post-merge gates green, no cross-plan conflicts. The only judgment calls worth remembering are the two in `<decisions_made>`. Wave 3 is the last wave; after it merges, the phase moves into the verification tail (code review, regression, verifier, completion).
</context>

<next_action>
Run `/gsd-execute-phase 75 --wave 3`. The wave filter is safe: no lower wave has incomplete plans, so the wave-safety check will pass. Plans 75-04 and 75-05 dispatch in parallel worktrees forked from `642d021`. Because this completes the last wave, execution will fall through to phase-level verification and completion automatically.
</next_action>
