---
phase: 73-resolve-stale-high-security-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md
  - .planning/SECURITY-TRIAGE.md
autonomous: true
requirements: [SECALERT-01, SECALERT-02]

must_haves:
  truths:
    - "Each of alerts #17, #18, #19, #20, #21 was opened individually by its own API call and its own GHSA, CVE, CVSS, vulnerable range, manifest_path and created_at are recorded separately (SECALERT-01, D-08)"
    - "The 'branch attribution' half of success criterion 1 is recorded as confirmed default-branch scope, evidenced by the absence of any ref/branch key in the alert payload, not as an extracted field value"
    - "GitHub's own server-side view of the dependency graph was re-read live at execution time via the SBOM endpoint and the dependencyGraphManifests GraphQL query (SECALERT-02, D-03)"
    - "The absence of System.Security.Cryptography.Xml from every live QuestBoard manifest is corroborated by two independent sources: fresh local dotnet list package --include-transitive AND GitHub's SBOM"
    - "The record states the corrected timeline: the 2026-04-22 bump (978d3f6) ended exposure to the alerted 8.0.0-8.0.3 range only; the same advisory family also covers 9.0.0-9.0.17 and 10.0.0-10.0.9, and the manifest's last live value was 10.0.9, so real exposure ended at the 2026-06-29 deletion (a477ab9) (D-02 as corrected by RESEARCH.md Research Question 6)"
    - ".planning/SECURITY-TRIAGE.md exists as an append-only dated log whose entry one is this incident (D-12, D-13)"
    - "The record carries a re-runnable appendix: every command verbatim, with its filtered captured output and the timestamp it was run (D-15)"
    - "The record explains the ghost/live dual dependency-set root cause (D-01) and states plainly that it survives this phase (D-06) because the only reliable eviction lever is permanently vetoed (D-05)"
  artifacts:
    - ".planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md"
    - ".planning/SECURITY-TRIAGE.md"
  key_links:
    - "Alert manifest_path -> one of the five ghost EuphoriaInn.* manifests still listed by dependencyGraphManifests: this join is what makes gate part (a) checkable at all"
    - "SBOM package entry -> its DEPENDS_ON edge from the ghost Microsoft.AspNetCore.Identity 2.3.1 node: proves the package hangs off the ghost branch, not a live manifest"
    - "73-EVIDENCE-CAPTURE.md -> SECURITY-TRIAGE.md: capture is copied in, never linked to (the phase directory is archived by /gsd-cleanup, which is exactly what D-12 exists to survive)"
  prohibitions:
    - "D-04: no force-refresh of the dependency graph, and no mutating call of any kind in this plan. Every command here is a read. ROADMAP success criterion 2 asks for a force-refresh; CONTEXT.md D-04 overrides it and satisfies SECALERT-02 with server-side reads instead."
    - "D-05: DELETE or PUT on /repos/{owner}/{repo}/vulnerability-alerts must not appear in any command run by this plan, for any reason. It is the only reliable ghost-manifest eviction lever and it is permanently vetoed for this repo because it would put the audit trail on dismissals #7, #8 and #11 at risk."
    - "No dismissal, no PATCH, no state change to any alert in this plan. Plan 73-02 owns every write, behind the D-11 operator gate."
    - "SECURITY-TRIAGE.md must not point at 73-EVIDENCE-CAPTURE.md or any other file under .planning/phases/ for its evidence. Captured output is copied in, not referenced (D-12)."
---

<objective>
Re-verify, live and individually, every fact this phase's dismissal argument rests on, and write it into a durable record a sceptical reviewer six months out can re-run command by command.

Purpose: SECALERT-01 and SECALERT-02. This is the half of the phase that structurally separates a triage from a rubber stamp. Plan 73-02 dismisses nothing that is not evidenced here.
Output: `.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` (working capture) and `.planning/SECURITY-TRIAGE.md` (durable record, entry one).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md
@.planning/phases/73-resolve-stale-high-security-alerts/73-RESEARCH.md
@.planning/phases/73-resolve-stale-high-security-alerts/73-VALIDATION.md
</context>

<phase_ground_rules>
This phase touches no application code. There is no `.csproj` edit, no NuGet change, no EF migration, no `dotnet build`, no `dotnet test`. A green build is not evidence for any requirement in this phase — do not run one as verification, and do not describe any check here as a "test". Every check is a re-runnable **state assertion** (VALIDATION.md).

`dotnet list package --include-transitive` IS used, but only as a read of the restored dependency graph — ground truth that is immune to GitHub's own cache staleness. It is not a build step.

Two repo names are in play and both are correct in their place:
- `theunschut/quest-board-dnd` — the real current name. **All GraphQL queries need this.**
- `theunschut/quest-board` — the old name, still resolving by redirect. Success criterion 5 quotes it literally, so plan 73-03 uses it verbatim there.

Windows/PowerShell: this repo develops on Windows. Any command carrying text with apostrophes or embedded quotes goes through a JSON file and `--input`, never inline `-f`. That matters in plan 73-02; in this plan it matters for the GraphQL `-f query=` argument, which contains no apostrophes and is safe as written.
</phase_ground_rules>

<!-- planner-discipline-allow: 73-EVIDENCE-CAPTURE -->

<tasks>

<task type="auto">
  <name>Task 1: Open all five alerts individually and capture per-alert evidence</name>

  <read_first>
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md` — D-01, D-02, D-08, D-16 and the alert table under `## Existing Code Insights`
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-RESEARCH.md` — Research Question 3(a) (verified field set for `GET .../alerts/{n}`, full alert #17 JSON), Research Question 4 (live alert state), Research Question 6 (full `vulnerabilities[]` correction), and Pitfall 1
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-VALIDATION.md` — the SECALERT-01 row and the Freshness rule under `## Sampling Rate`
  </read_first>

  <files>.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md</files>

  <action>
    Create the working capture file and fill its per-alert section. Record the UTC date once at the top as CHECK_DATE via `date -u +%Y-%m-%d` — plan 73-02's dismissal comments reuse this exact value, so capture it, do not retype it from memory.

    RESEARCH.md's JSON is a 2026-08-26 snapshot. Per VALIDATION.md's freshness rule, every figure below must come from a fresh call made now, not copied from RESEARCH.md. Where a fresh figure differs from RESEARCH.md's, record BOTH and flag the delta — a changed manifest_path or a changed vulnerable range is a material finding, not a formatting detail.

    Step 1 — list the open alerts and confirm the set is still exactly 17,18,19,20,21:
    `gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=open --paginate --jq '.[] | "\(.number) \(.state) \(.security_vulnerability.severity) \(.dependency.package.name) \(.dependency.manifest_path)"'`
    If the set differs from 17-21 — extra alert, missing alert, already-dismissed alert — stop and record the discrepancy prominently. Per D-19 a NEW HIGH alert that is not #17-#21 does not block this phase; it is logged and left for its own triage. A MISSING or already-dismissed alert from #17-#21 does block, because it means someone acted outside this phase.

    Step 2 — five separate calls, one per alert, n in 17 18 19 20 21. Do not loop these into one aggregated fetch: SECALERT-01 requires each alert opened individually, and the per-alert call is the act being evidenced.
    `gh api repos/theunschut/quest-board-dnd/dependabot/alerts/17 -X GET`
    From each response record: `number`, `state`, `dependency.package.ecosystem`, `dependency.package.name`, `dependency.manifest_path`, `dependency.scope`, `dependency.relationship`, `security_vulnerability.severity`, `security_vulnerability.vulnerable_version_range`, `security_vulnerability.first_patched_version.identifier`, `created_at`, `html_url`, `dismissed_at`, `dismissed_reason`.

    Step 3 — per alert, capture the FULL advisory range set, not just the single range on the alert. This is Pitfall 1 and it is the fact that corrects D-02's framing:
    `gh api repos/theunschut/quest-board-dnd/dependabot/alerts/17 --jq '{ghsa: .security_advisory.ghsa_id, cve: .security_advisory.cve_id, cvss: .security_advisory.cvss.score, ranges: [.security_advisory.vulnerabilities[] | {pkg: .package.name, range: .vulnerable_version_range, patched: .first_patched_version.identifier}]}'`
    RESEARCH.md found three affected ranges per advisory (`8.0.0-8.0.3` patched at `8.0.4`, `9.0.0-9.0.17` patched at `9.0.18`, `10.0.0-10.0.9` patched at `10.0.10`). Record what comes back now for each of the five.

    Step 4 — settle the branch-attribution question once, and record it as a scope finding rather than a field value. The Dependabot Alerts API has no branch or ref dimension at all; Dependabot scans the default branch only. Confirm the payload carries no such key:
    `gh api repos/theunschut/quest-board-dnd/dependabot/alerts/17 --jq '[paths(scalars) | map(tostring) | join(".")] | map(select(test("ref|branch";"i"))) | length'`
    Expect `0`. Record the wording as "confirmed default-branch-scoped (main); the Dependabot Alerts API exposes no per-branch dimension, unlike the Code Scanning API's instances[].ref" — NOT as "branch: main extracted from the payload". Do not write anything in this phase that claims a branch field was read.

    Structure the capture file as: a header with CHECK_DATE and the repo name; a per-alert subsection for each of #17-#21 carrying the step 2 and step 3 output; and the step 1 and step 4 output as their own subsections. Leave a placeholder section for Task 2's graph evidence.
  </action>

  <acceptance_criteria>
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` exists and contains a CHECK_DATE line in `YYYY-MM-DD` form
    - The file contains five distinct GHSA identifiers (one per alert), verified by: `grep -oE "GHSA-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}" .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md | sort -u | wc -l` returns `5`
    - The file contains five distinct CVE identifiers: `grep -oE "CVE-[0-9]{4}-[0-9]+" .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md | sort -u | wc -l` returns `5`
    - The file has one subsection per alert: `grep -cE "^#+ .*Alert #(17|18|19|20|21)" .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` returns `5`
    - Every alert's recorded `manifest_path` starts with `EuphoriaInn.` — this is gate part (a)'s input, recorded here and re-checked live in plan 73-02
    - Every alert's recorded `created_at` is a `2026-08-10` timestamp
    - The file records, per alert, the full `security_advisory.vulnerabilities[]` range list, not only `security_vulnerability.vulnerable_version_range`
    - The file states default-branch scope as a confirmed API property with the step 4 command and its `0` result as evidence, and contains no claim that a branch or ref field was read from the payload
    - `gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=open --jq '[.[] | select(.number | IN(17,18,19,20,21))] | length'` returns `5` (all five still open — nothing was dismissed by this task)
  </acceptance_criteria>

  <verify>
    <automated>grep -oE "GHSA-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}" .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md | sort -u | wc -l</automated>
    <automated>gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=open --jq '[.[] | select(.number | IN(17,18,19,20,21))] | length'</automated>
  </verify>

  <done>Five alerts opened by five separate API calls; per-alert GHSA, CVE, CVSS, full advisory range set, manifest_path, created_at and html_url captured; default-branch scope recorded as a confirmed API property with its own evidence command; all five still open.</done>
</task>

<task type="auto">
  <name>Task 2: Re-check GitHub's server-side dependency graph and corroborate with local ground truth</name>

  <read_first>
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` — Task 1's output, especially the recorded manifest_paths
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-RESEARCH.md` — Research Question 3(b) (SBOM filter + DEPENDS_ON chain + jq-only equivalent), 3(c) (GraphQL `dependencyGraphManifests`), Research Question 5 (local ground-truth sweep), Research Question 6 (git archaeology), and Pitfalls 2 and 4
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md` — D-01 (the ghost/live table), D-03, D-04, D-05
  </read_first>

  <files>.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md</files>

  <action>
    Append the graph evidence to the capture file. Two independent sources are mandatory here (Pitfall 2): GitHub's own graph is the subsystem this phase has already proven stale, so using it alone to prove absence would be circular — the exact reasoning error a sceptical reviewer catches first. Record each command with its filtered output and the timestamp it ran.

    D-04 constraint: no force-refresh is attempted. D-05 constraint: the Dependabot alerts off/on toggle is not touched. Everything below is a read.

    Step 1 — GitHub SBOM, package filter (D-03, source A):
    `gh api repos/theunschut/quest-board-dnd/dependency-graph/sbom --jq '.sbom.packages[] | select((.name | contains("Cryptography.Xml")) or (.name | contains("AspNetCore.Identity"))) | "\(.name) \(.versionInfo)"'`
    Expect the ghost/live split D-01 describes: `System.Security.Cryptography.Xml` appearing exactly once, at `8.0.3`, alongside ghost `Microsoft.AspNetCore.Identity 2.3.1` / `.EntityFrameworkCore 8.0.11` / `.UI 8.0.11` and live `.EntityFrameworkCore 10.0.9` / `.UI 10.0.9`. Filtered, not raw — the SBOM is ~438 packages and an unfiltered dump buries the three facts that matter (D-15).

    Step 2 — SBOM DEPENDS_ON chain. Re-derive the SPDXID fresh rather than hardcoding RESEARCH.md's hash suffix, in case GitHub regenerated it. First:
    `gh api repos/theunschut/quest-board-dnd/dependency-graph/sbom --jq '.sbom.packages[] | select(.name=="System.Security.Cryptography.Xml") | .SPDXID'`
    then substitute that value into:
    `gh api repos/theunschut/quest-board-dnd/dependency-graph/sbom --jq --arg t "<SPDXID>" '.sbom.relationships[] | select(.relatedSpdxElement == $t) | .spdxElementId'`
    Expect two incoming edges: the ghost `Microsoft.AspNetCore.Identity 2.3.1` node and the repo-level root node. Record the caveat RESEARCH.md flags: the SBOM flattens the graph under one repo root and carries no per-manifest attribution, so the "which manifest" question is answered by the alert's `manifest_path` plus the GraphQL query, never by the SBOM.

    Step 3 — GraphQL manifest list (D-03, the decisive ghost query). Needs the real repo name and the preview Accept header:
    `gh api graphql -H "Accept: application/vnd.github.hawkgirl-preview+json" -f query='query { repository(owner: "theunschut", name: "quest-board-dnd") { dependencyGraphManifests(first: 50) { totalCount nodes { filename blobPath parseable } } } }'`
    Record `totalCount` and the full filename list. RESEARCH.md observed 13 (5 ghost `EuphoriaInn.*` + 5 `QuestBoard.*` + 3 workflow YAMLs). Explicitly do NOT record `dependenciesCount` as evidence of anything — it returns `0` for every node including populated live manifests; it is a known preview-API artifact and citing it would put a falsifiable claim in the record.

    Step 4 — local ground truth (source B, immune to GitHub's cache). Run across all five tracked manifests: `QuestBoard.Domain/QuestBoard.Domain.csproj`, `QuestBoard.Repository/QuestBoard.Repository.csproj`, `QuestBoard.Service/QuestBoard.Service.csproj`, `QuestBoard.UnitTests/QuestBoard.UnitTests.csproj`, `QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj`. Per manifest:
    `dotnet list <csproj> package --include-transitive`
    and filter for `Cryptography.Xml` and `Identity`. This is a dependency-graph read, not a build — if it fails because output files are locked, ask the operator to stop the Visual Studio debugger rather than working around it. Record per manifest whether `System.Security.Cryptography.Xml` appears at ANY version (expected: nowhere) and whether the base `Microsoft.AspNetCore.Identity` 2.x package appears (expected: nowhere — only the unrelated `Microsoft.IdentityModel.*` and `Microsoft.Extensions.Identity.*` families and the `.EntityFrameworkCore`/`.UI` siblings at 10.0.9).

    Step 5 — git archaeology, three commands, output captured verbatim:
    `git log -S "System.Security.Cryptography.Xml" --oneline --all -- '*.csproj'`
    `git show 978d3f6 -- EuphoriaInn.Domain/EuphoriaInn.Domain.csproj`
    `git show a477ab9~1:EuphoriaInn.Domain/EuphoriaInn.Domain.csproj`
    These establish: `978d3f6` (2026-04-22) bumped the package straight from `8.0.3` to `10.0.7`, never revisiting the alerted range; the manifest's last live value before deletion was `10.0.9`; `a477ab9` (2026-06-29) deleted the manifest outright.

    Step 6 — `main` HEAD drift re-check (Pitfall 4). `main` moved from `69f2ea0` to `89a8cb6` between context-gathering and research with zero manifest changes. Re-confirm nothing has changed since:
    `git fetch origin main` then `git rev-parse origin/main` then `git diff --stat 89a8cb6 origin/main -- '*.csproj'`
    An empty diff means D-06's premise still holds (no manifest-touching commit has reached `main`, so the ghosts have had no opportunity to clear). A non-empty diff is a material finding: record it and note that the GraphQL result in step 3 may now be mid-refresh.
  </action>

  <acceptance_criteria>
    - `gh api repos/theunschut/quest-board-dnd/dependency-graph/sbom --jq '[.sbom.packages[] | select(.name=="System.Security.Cryptography.Xml") | .versionInfo] | join(",")'` returns exactly `8.0.3` — one entry, one version, no `10.0.x` entry anywhere in the live graph
    - The GraphQL `dependencyGraphManifests` result is captured with its `totalCount` and full filename list, and the count of `EuphoriaInn.*` filenames in that list is recorded explicitly
    - The capture file contains all five ghost manifest filenames: `grep -cE "EuphoriaInn\.(Domain|Repository|Service|UnitTests|IntegrationTests)/EuphoriaInn\.[A-Za-z]+\.csproj" .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` returns 5 or more
    - The capture file records a `dotnet list package --include-transitive` result for each of the five `QuestBoard.*` manifests by name: `grep -c "QuestBoard\..*\.csproj" .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` returns 5 or more
    - The capture file states, for each `QuestBoard.*` manifest, that `System.Security.Cryptography.Xml` is absent at any version
    - The capture file records the `978d3f6` diff showing `8.0.3` replaced by `10.0.7`, and the `a477ab9~1` content showing `10.0.9` as the last live value
    - `git diff --stat 89a8cb6 origin/main -- '*.csproj'` output is recorded, with its result interpreted in one line (empty = D-06 premise holds; non-empty = flagged as a material finding)
    - No command in this task mutated anything: `gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=open --jq 'length'` still returns `5`
    - `dependenciesCount` does not appear anywhere in the capture file as supporting evidence
  </acceptance_criteria>

  <verify>
    <automated>gh api repos/theunschut/quest-board-dnd/dependency-graph/sbom --jq '[.sbom.packages[] | select(.name=="System.Security.Cryptography.Xml") | .versionInfo] | join(",")'</automated>
    <automated>grep -cE "EuphoriaInn\.[A-Za-z]+/EuphoriaInn\.[A-Za-z]+\.csproj" .planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md</automated>
    <automated>gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=open --jq 'length'</automated>
  </verify>

  <done>GitHub's SBOM and GraphQL manifest list re-read live and captured filtered; the DEPENDS_ON chain traced from the ghost Identity 2.3.1 node; a fresh local dotnet sweep across all five QuestBoard manifests captured as the independent second source; git archaeology and the main HEAD drift check captured. Nothing mutated.</done>
</task>

<task type="auto">
  <name>Task 3: Write .planning/SECURITY-TRIAGE.md entry one</name>

  <read_first>
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` — everything Tasks 1 and 2 captured; this is the raw material
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md` — D-01, D-02, D-05, D-06, D-12, D-13, D-15, and `## Specific Ideas` (the three facts, in order, and who the record is written for)
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-RESEARCH.md` — the `## Summary` third finding (the D-02 correction) and Research Question 6's closing paragraph
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-VALIDATION.md` — `## Manual-Only Verifications`, the row on the record reading as evidence rather than as a summary
  </read_first>

  <files>.planning/SECURITY-TRIAGE.md</files>

  <action>
    Create `.planning/SECURITY-TRIAGE.md`. It does not exist yet. It lives at `.planning/` root, deliberately outside the phase directory, because `/gsd-cleanup` archives phase directories at milestone close and a pointer into `73-*` would break exactly when a future reviewer needs it (D-12).

    Structure it as an append-only dated log (D-13): a short preamble stating the append-only convention and that newest entries go last, then `## Entry 1 - {CHECK_DATE} - Dependabot alerts #17-#21, System.Security.Cryptography.Xml (HIGH)`. A sixth alert is expected (D-06), so the format must invite entry two without restructuring.

    Entry one carries, in this order:

    (1) **Outcome line and status.** At the time this task runs, the dismissals have not happened yet — plan 73-02 owns them. Write the outcome row as `Pending - dismissals gated on operator approval (see plan 73-02)` and leave the per-alert dismissal columns empty. Plan 73-03 fills them in with real timestamps. Do not pre-write an outcome that has not occurred; that is itself a small rubber stamp.

    (2) **Conclusion**, leading with the three facts in this order (from `## Specific Ideas`): the alerted version 8.0.3 was upgraded away on 2026-04-22 in `978d3f6`, four months before the alerts were minted on 2026-08-10; the manifest itself was deleted on 2026-06-29 in `a477ab9`; GitHub still lists all five deleted manifests in `dependencyGraphManifests` today. Then the conclusion this supports: GitHub is attributing a vulnerability to a package/version combination that exists in no live manifest, which makes the attribution factually wrong rather than merely out of date.

    (3) **A correction section, written explicitly and named as a correction.** The original framing in `73-CONTEXT.md` D-02 held that the 2026-04-22 upgrade was the strongest fact because it moved the package out of the vulnerable range. Research found the same five advisories also cover `9.0.0-9.0.17` and `10.0.0-10.0.9`, and the manifest's last live value before deletion was `10.0.9` — inside that third band, patched only at `10.0.10`. So the 2026-04-22 bump ended exposure to the *alerted* `8.0.0-8.0.3` range only; real exposure to the advisory family ended at the 2026-06-29 manifest deletion. State this plainly. It sharpens the dismissal case rather than weakening it — the package is absent from every live manifest at any version today, confirmed by two independent sources — and a reviewer who checks the GHSA ranges will find this section already there instead of finding a hole. Nothing anywhere in this phase may imply that upgrading the version eliminated the vulnerability.

    (4) **Per-alert table**, one row per alert, columns: Alert, CVE, GHSA, CVSS, Class, `manifest_path`, `created_at`, Branch scope, Gate (a), Gate (b), Dismissed at, `dismissed_reason`. Populate everything except the last two. Branch scope reads `default branch (main) - API has no per-branch dimension`, per Task 1 step 4. Gate columns stay empty until 73-02 runs them live.

    (5) **Root cause and what is not fixed.** The D-01 ghost/live dual dependency-set table (what GitHub's graph shows vs what is really on `main`). Then, stated as an accepted trade rather than an oversight: the ghost manifests survive this phase (D-06), so GitHub will keep minting alerts off them and a sixth alert is expected; the only reliable eviction lever is toggling Dependabot alerts off and on, which is permanently vetoed for this repo (D-05) because it would put the audit trail on the three existing dismissals — #7 and #8 (`AutoMapper`, 2026-06-25) and #11 (`SQLitePCLRaw.lib.e_sqlite3`, 2026-06-29), all `fix_started`, all by `cryptic96`) — at risk. Name the remaining non-destructive lever: a manifest-touching commit reaching `main`, which the `milestone/v9-rolling-improvements` merge will do naturally; re-run the GraphQL query afterwards and see whether the ghosts cleared.

    (6) **Appendix - re-runnable commands (D-15).** One subsection per command, each with: the verbatim command, the timestamp it was run, and its filtered captured output. Cover, at minimum: per-alert detail fetch; full advisory range fetch; the branch-key absence probe; the SBOM package filter; the SBOM DEPENDS_ON chain (with the note that SPDXIDs should be re-derived, not hardcoded); the GraphQL manifest query; the local `dotnet list package --include-transitive` sweep; the three `git log -S` / `git show` archaeology commands; the `main` HEAD drift diff. Filtered, not raw — the SBOM alone is 438 packages. Add a one-line caveat under the SBOM subsection that GitHub's SBOM is generated from the same cache this entry proves stale, which is why the local `dotnet` sweep is present as an independent second source.

    Copy the captured output into this file. Do not link to `73-EVIDENCE-CAPTURE.md` — that file is archived with the phase directory, and surviving that archival is the entire reason this file exists.
  </action>

  <acceptance_criteria>
    - `.planning/SECURITY-TRIAGE.md` exists at the `.planning/` root
    - `grep -c "^## Entry 1" .planning/SECURITY-TRIAGE.md` returns `1`
    - The file states the append-only, newest-last convention in its preamble
    - `grep -oE "GHSA-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}" .planning/SECURITY-TRIAGE.md | sort -u | wc -l` returns `5`
    - `grep -oE "CVE-[0-9]{4}-[0-9]+" .planning/SECURITY-TRIAGE.md | sort -u | wc -l` returns `5`
    - The per-alert table has five data rows, one per alert: `grep -cE "^\| *#?(17|18|19|20|21) *\|" .planning/SECURITY-TRIAGE.md` returns `5`
    - The file contains a section explicitly named as a correction to the original 2026-04-22 framing, and that section names the `10.0.0-10.0.9` range and the `10.0.9` last-live value
    - The file contains no sentence asserting that the 2026-04-22 upgrade eliminated the vulnerability
    - All three commit SHAs appear: `grep -c "978d3f6" .planning/SECURITY-TRIAGE.md`, `grep -c "a477ab9"`, and a reference to the current `main` HEAD each return 1 or more
    - The appendix contains at least nine distinct captured commands, each with a run timestamp and filtered output
    - The file makes no reference to any path under `.planning/phases/` as the location of evidence: `grep -c "73-EVIDENCE-CAPTURE" .planning/SECURITY-TRIAGE.md` returns `0`
    - The D-05 veto and the three protected dismissals (#7, #8, #11) are named in the root-cause section
    - Dismissal outcome cells and gate cells are empty or marked pending — no dismissal is recorded as done before plan 73-02 runs
  </acceptance_criteria>

  <verify>
    <automated>grep -oE "GHSA-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}" .planning/SECURITY-TRIAGE.md | sort -u | wc -l</automated>
    <automated>grep -cE "^\| *#?(17|18|19|20|21) *\|" .planning/SECURITY-TRIAGE.md</automated>
    <automated>grep -c "73-EVIDENCE-CAPTURE" .planning/SECURITY-TRIAGE.md</automated>
  </verify>

  <done>`.planning/SECURITY-TRIAGE.md` exists as an append-only dated log; entry one carries the conclusion (three facts in order), the named correction to the original framing, the per-alert table with dismissal columns pending, the ghost/live root-cause table with the D-05 veto and D-06 accepted consequence, and a nine-plus command re-runnable appendix with filtered output and timestamps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local executor -> GitHub REST/GraphQL API | Authenticated calls cross here. In this plan every crossing is a read; plan 73-02 owns the only writes. |
| RESEARCH.md snapshot -> execution-time reality | Stale facts cross here. The 2026-08-26 captured JSON is a snapshot; asserting against it instead of a live re-run is how a wrong conclusion enters the record. |
| Evidence capture -> permanent public audit record | A claim written here becomes the justification a public dismissal cites. An unverified claim crossing this boundary is indistinguishable from a fabricated one six months out. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Repudiation | The dismissal argument itself (all three tasks) | high | mitigate | Every fact re-fetched live at execution time rather than copied from RESEARCH.md's snapshot (Task 1 step 1, Task 2 step 6). Absence of the package proven by two independent sources — local `dotnet list package --include-transitive` and GitHub's SBOM — because GitHub's own graph is the subsystem already proven stale (Task 2 steps 1 and 4, Pitfall 2). |
| T-73-02 | Repudiation / Tampering | Existing dismissal audit trail on alerts #7, #8, #11 | high | mitigate | D-05: `DELETE`/`PUT /repos/{owner}/{repo}/vulnerability-alerts` appears in no command in this plan. Recorded as a permanent repo-level veto in the record's root-cause section, with the three protected dismissals named so a future reader knows what the veto is protecting. |
| T-73-03 | Information Disclosure | Filtered vs raw capture in a repo-tracked file | low | accept | The SBOM is public data on a public repo, so no secret exposure risk; the filtering in D-15 is for reviewability (438 packages would bury the three load-bearing facts), not confidentiality. No token, header value, or credential is written into either output file. |
| T-73-04 | Tampering | GitHub dependency-graph state | medium | mitigate | D-04: no force-refresh, no cache-bust, no mutating call of any kind in this plan. SECALERT-02 is satisfied by server-side reads (SBOM + GraphQL), which is GitHub's own data — the thing the requirement actually asks for. Verified by the post-task assertion that all five alerts remain `open`. |

No `T-73-SC` supply-chain row: this phase runs zero package-manager installs, so RESEARCH.md's Package Legitimacy Audit is not applicable by its own scope condition.

**ASVS L1 applicability (from RESEARCH.md `## Security Domain`):** V2 Authentication, V3 Session Management, V4 Access Control, V5 Input Validation and V6 Cryptography are all **not applicable** — this plan touches no application authentication, session, authorization, input-handling or cryptographic surface. The vulnerable package is itself a cryptography library, but this phase neither uses, configures nor patches it; it verifies the package is absent from the live application entirely. Padding this register with generic web-application threats would misrepresent what was analysed.
</threat_model>

<artifacts_this_phase_produces>
Phase 73 produces exactly these artifacts. Nothing else is created, and no source file is touched.

| Artifact | Kind | Produced by | Notes |
|----------|------|-------------|-------|
| `.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` | New file | Plan 73-01 (Tasks 1, 2) | Working capture; archived with the phase directory. Copied into the durable record, never linked from it. |
| `.planning/SECURITY-TRIAGE.md` | New file | Plan 73-01 (Task 3), completed by Plan 73-03 (Task 1) | Durable append-only log at `.planning/` root; entry one is this incident. Survives `/gsd-cleanup`. |
| `.planning/PROJECT.md` `## Context` -> *Known issues / tech debt* bullet | New list item | Plan 73-03 (Task 2) | Ghost-manifest root cause + re-runnable reproduction query + the D-05 veto as the reason it stays unfixed. |
| `.planning/PROJECT.md` `## Key Decisions` row | New table row | Plan 73-03 (Task 2) | The dismiss-not-patch call, and the permanent toggle veto. |
| Dependabot alert #17 state | Remote state change | Plan 73-02 (Task 3) | `state=dismissed`, `dismissed_reason=inaccurate`, per-alert `dismissed_comment`, `dismissed_by=cryptic96`. Irreversible, public. |
| Dependabot alert #18 state | Remote state change | Plan 73-02 (Task 3) | as above |
| Dependabot alert #19 state | Remote state change | Plan 73-02 (Task 3) | as above |
| Dependabot alert #20 state | Remote state change | Plan 73-02 (Task 3) | as above |
| Dependabot alert #21 state | Remote state change | Plan 73-02 (Task 3) | as above |
</artifacts_this_phase_produces>

<verification>
All checks are state assertions, not tests. No build, no test suite.

1. Five alerts each fetched individually and captured with their own GHSA/CVE/CVSS/range/manifest/timestamp — `grep -oE "GHSA-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}" .planning/SECURITY-TRIAGE.md | sort -u | wc -l` returns `5`
2. Default-branch scope recorded as a confirmed API property with its own evidence command, and no claim anywhere that a branch field was read
3. GitHub's server-side graph re-read live — `gh api repos/theunschut/quest-board-dnd/dependency-graph/sbom --jq '[.sbom.packages[] | select(.name=="System.Security.Cryptography.Xml") | .versionInfo] | join(",")'` returns `8.0.3`
4. GraphQL `dependencyGraphManifests` re-run live with its `totalCount` and ghost filename list captured
5. Local `dotnet list package --include-transitive` sweep captured for all five `QuestBoard.*` manifests as the independent second source
6. Nothing mutated — `gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=open --jq 'length'` still returns `5` at the end of this plan
7. `.planning/SECURITY-TRIAGE.md` exists with entry one, the named correction section, the per-alert table, the root-cause section and a nine-plus command appendix
</verification>

<success_criteria>
- SECALERT-01: each of #17-#21 investigated by its own API call, with manifest attribution, detection timestamp and confirmed default-branch scope recorded per alert, before any closure
- SECALERT-02: GitHub's own server-side dependency-graph view re-checked live via SBOM and GraphQL, with no force-refresh and no mutation, corroborated by an independent local ground-truth read
- `.planning/SECURITY-TRIAGE.md` reads as evidence a sceptic can re-run, not as a summary: every load-bearing claim has a command next to it
- The corrected timeline is stated explicitly, so a reviewer checking the GHSA ranges finds the correction already made rather than a hole
- All five alerts still `open` when this plan ends
</success_criteria>

<output>
Create `.planning/phases/73-resolve-stale-high-security-alerts/73-01-SUMMARY.md` when done. Record: the CHECK_DATE used, the live per-alert facts (and any delta from RESEARCH.md's snapshot), the SBOM/GraphQL results, whether the `main` HEAD drift diff was empty, and confirmation that all five alerts remain open.
</output>
