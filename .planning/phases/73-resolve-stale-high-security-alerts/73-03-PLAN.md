---
phase: 73-resolve-stale-high-security-alerts
plan: 03
type: execute
wave: 3
depends_on: ["73-02"]
files_modified:
  - .planning/SECURITY-TRIAGE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [SECALERT-04, SECALERT-05]

must_haves:
  truths:
    - "The per-alert table in .planning/SECURITY-TRIAGE.md carries the real dismissal outcome for every alert: gate (a) result, gate (b) result, dismissed_at, dismissed_by and dismissed_reason, or the recorded reason it was held (D-13, D-17)"
    - "The verbatim posted dismissal comment for each alert is preserved in the durable record, so the record and the public alert can be diffed by a future reviewer without GitHub UI access"
    - ".planning/PROJECT.md gains exactly two hooks: a Known issues / tech debt bullet under ## Context carrying the ghost-manifest root cause and a re-runnable reproduction query, and a ## Key Decisions row for the dismiss-not-patch call — both linking .planning/SECURITY-TRIAGE.md (D-14, SECALERT-04)"
    - "The Known issues bullet states, in plain language, why the ghost manifests stay unfixed: the only reliable eviction lever would put the existing dismissal audit trail at risk (D-05, D-06)"
    - "The SECALERT-05 phase gate assertion is run once, after all PATCHes, and its result is recorded together with which reading of success criterion 5 was applied (D-19)"
    - "If any HIGH alert other than #17-#21 is open at gate time, it is logged as its own dated entry in .planning/SECURITY-TRIAGE.md rather than treated as a phase failure (D-06, D-19)"
  artifacts:
    - ".planning/SECURITY-TRIAGE.md (entry one complete, outcomes filled in)"
    - ".planning/PROJECT.md (two new hooks)"
  key_links:
    - "PROJECT.md hook -> .planning/SECURITY-TRIAGE.md: the pointer must survive /gsd-cleanup archiving the phase directory, which is why it targets the .planning/ root file and nothing under .planning/phases/"
    - "SECURITY-TRIAGE.md per-alert row -> the live dismissed alert on GitHub: the record's claims must be diffable against the public state"
  prohibitions:
    - "D-05: DELETE or PUT on /repos/{owner}/{repo}/vulnerability-alerts must not appear in any command, including as a way to clear a lingering alert so the phase gate reads zero. Making the gate pass by destroying the audit trail is the exact trade this repo has permanently vetoed."
    - "D-04: no force-refresh of the dependency graph. This plan performs no mutation against GitHub at all — it reads once for the phase gate and writes only local markdown."
    - "D-18: no patching, no .csproj change, no NuGet change, under any gate outcome recorded by plan 73-02. A reachability finding is written up, not fixed here."
    - "D-19: do not edit ROADMAP.md success criterion 5 to match the reading used. The literal text stays as written; the reading is recorded in the phase artifacts and in the summary."
    - "PROJECT.md is edited with scoped Edit calls only. It is ~105KB and loaded as project context every session; a whole-file Write would destroy content outside the two insertion points."
    - "The two PROJECT.md hooks must not cite phase-local decision IDs (D-05, D-14 and similar) as their justification, because 73-CONTEXT.md is archived by /gsd-cleanup and the reference would dangle. State the reasoning in plain language and point at .planning/SECURITY-TRIAGE.md, which survives."
---

<objective>
Close the loop: finish the durable record with what actually happened, hook it into `.planning/PROJECT.md` where a future reviewer will find it, and run the phase gate with an honest reading of what it proves.

Purpose: SECALERT-04 and SECALERT-05.
Output: a complete `.planning/SECURITY-TRIAGE.md` entry one, two new hooks in `.planning/PROJECT.md`, and a recorded phase-gate result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SECURITY-TRIAGE.md
@.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md
@.planning/phases/73-resolve-stale-high-security-alerts/73-VALIDATION.md
@.planning/phases/73-resolve-stale-high-security-alerts/73-DISMISSAL-DRAFTS.md
@.planning/phases/73-resolve-stale-high-security-alerts/73-02-SUMMARY.md
</context>

<phase_ground_rules>
No code, no build, no test suite. `dotnet build` and `dotnet test` are not part of this plan's verification and prove nothing about its requirements. Every check is a state assertion.

Success criterion 5 quotes the **old** repo name literally — `gh api repos/theunschut/quest-board/dependabot/alerts` — and it still resolves via GitHub's rename redirect. Run it exactly as written for the phase gate. Everything else in this phase uses the real name `theunschut/quest-board-dnd`.
</phase_ground_rules>

<!-- planner-discipline-allow: 73-EVIDENCE-CAPTURE -->
<!-- planner-discipline-allow: D-05 -->
<!-- planner-discipline-allow: D-14 -->

<tasks>

<task type="auto">
  <name>Task 1: Complete SECURITY-TRIAGE.md entry one with the real dismissal outcomes</name>

  <read_first>
    - `.planning/SECURITY-TRIAGE.md` — entry one as plan 73-01 left it, with pending outcome cells
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-DISMISSAL-DRAFTS.md` — the approval outcome and the per-alert gate/PATCH/read-back log from plan 73-02
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-02-SUMMARY.md` — what actually happened, including anything held or failed
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md` — D-13, D-15, D-17
  </read_first>

  <files>.planning/SECURITY-TRIAGE.md</files>

  <action>
    Fill in what plan 73-01 deliberately left pending. Edit in place with scoped edits; do not rewrite the file.

    Update the entry's outcome line from pending to the real result, dated, naming the actor (`cryptic96`) and the reason code (`inaccurate`).

    Complete the per-alert table's last four columns from plan 73-02's log: gate (a) result, gate (b) result (naming both sources), `dismissed_at`, `dismissed_reason`. An alert that was held or failed a gate gets its real state in those cells — `held`, `gate (b) failed`, `open` — never a blank that reads as success. If any alert was held, add a short subsection naming it, the failing command and its output verbatim, and the fact that no patch was applied and none is in scope for this phase; that is an open finding for the operator to scope, not a defect in this record.

    Add a subsection preserving the five posted comments verbatim, one per alert, with the character count and the read-back diff result (`MATCH` or the observed difference). This is what lets a future reviewer diff the record against the live alert without needing GitHub UI access — and it is the thing that makes the claim "each alert got its own evidence-citing comment" checkable rather than asserted.

    Extend the appendix with the post-dismissal read-back command and its captured output:
    `gh api repos/theunschut/quest-board-dnd/dependabot/alerts/{n} --jq '{state, reason: .dismissed_reason, by: .dismissed_by.login, at: .dismissed_at, comment: .dismissed_comment}'`
    and the assertion that the three pre-existing dismissals are untouched:
    `gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=dismissed --jq '[.[] | select(.number | IN(7,8,11))] | map({number, dismissed_reason, by: .dismissed_by.login})'`

    Leave the append-only structure intact so entry two can be added later without restructuring — a sixth alert is expected, because the ghost manifests survive this phase by deliberate choice.
  </action>

  <acceptance_criteria>
    - `grep -c "^## Entry 1" .planning/SECURITY-TRIAGE.md` still returns `1` and the file still contains its preamble stating the append-only, newest-last convention
    - The entry's outcome line no longer contains the word `Pending` for any alert that was dismissed
    - The per-alert table has five data rows and no empty gate or dismissal cell: `grep -cE "^\| *#?(17|18|19|20|21) *\|" .planning/SECURITY-TRIAGE.md` returns `5`
    - `grep -c "inaccurate" .planning/SECURITY-TRIAGE.md` returns 1 or more, and `cryptic96` appears in the outcome line
    - The five posted comments are preserved verbatim: `grep -oE "GHSA-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}" .planning/SECURITY-TRIAGE.md | sort -u | wc -l` returns `5`
    - The appendix contains the post-dismissal read-back command and the #7/#8/#11 integrity assertion with their captured output
    - For each dismissed alert, the comment recorded in the file matches the live one: `gh api repos/theunschut/quest-board-dnd/dependabot/alerts/17 --jq '.dismissed_comment'` output appears verbatim in `.planning/SECURITY-TRIAGE.md`
    - Any held or failed alert is recorded with its real state and its failing command output, and `git status --porcelain -- '*.csproj'` is empty
    - The file still contains no reference to `73-EVIDENCE-CAPTURE`: `grep -c "73-EVIDENCE-CAPTURE" .planning/SECURITY-TRIAGE.md` returns `0`
  </acceptance_criteria>

  <verify>
    <automated>grep -cE "^\| *#?(17|18|19|20|21) *\|" .planning/SECURITY-TRIAGE.md</automated>
    <automated>grep -oE "GHSA-[a-z0-9]{4}-[a-z0-9]{4}-[a-z0-9]{4}" .planning/SECURITY-TRIAGE.md | sort -u | wc -l</automated>
    <automated>git status --porcelain -- '*.csproj'</automated>
  </verify>

  <done>Entry one is complete: real outcomes in every table cell, the five posted comments preserved verbatim with their read-back results, the post-dismissal and audit-trail-integrity commands added to the appendix, and any held alert written up as an open finding. The append-only structure is intact for entry two.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two PROJECT.md hooks</name>

  <read_first>
    - `.planning/PROJECT.md` — the `## Context` section's *Known issues / tech debt* bullet list (ends immediately before `## Constraints`) and the `## Key Decisions` table (ends immediately before `## Evolution`); read enough of each to match house style
    - `.planning/SECURITY-TRIAGE.md` — the completed entry one, so the hooks describe what the record actually says
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md` — D-05, D-06, D-12, D-14
  </read_first>

  <files>.planning/PROJECT.md</files>

  <action>
    Two scoped `Edit` calls — the two hooks D-14 specifies, and only those two. `PROJECT.md` is around 105KB and is loaded as project context every session — a whole-file `Write` would destroy everything outside the diff window. Use `Edit` with a unique anchor for each insertion.

    Do not put the full evidence table inline. That is exactly why the durable record lives in its own file. These are hooks: enough for a reader to know the issue exists and where to go.

    Neither hook may cite phase-local decision IDs as its justification. `73-CONTEXT.md` is archived by `/gsd-cleanup` at milestone close, so a "per D-05" reference in a permanently-loaded file dangles the moment the phase is archived. State the reasoning in plain language and point at `.planning/SECURITY-TRIAGE.md`, which lives at the `.planning/` root and survives archiving. Naming the phase number in the trailing attribution is fine and matches house style.

    **Hook 1 — Known issues / tech debt bullet.** Append as the last bullet of the list under `## Context`, immediately before the `## Constraints` heading. Match the existing bullets' style: one paragraph-length bullet, backticked identifiers, and a trailing ` — v9.0 (Phase 73)` attribution. Content, in this order:
    - GitHub's dependency graph for `main` still lists all five deleted `EuphoriaInn.*` `.csproj` manifests weeks after commit `a477ab9` removed them, so Dependabot keeps minting alerts against packages that exist in no live manifest — five HIGH alerts (#17-#21, `System.Security.Cryptography.Xml`) were dismissed as inaccurate on that basis.
    - The re-runnable reproduction query, inline, so a future reader can check the state in one command: `gh api graphql -H "Accept: application/vnd.github.hawkgirl-preview+json" -f query='query { repository(owner: "theunschut", name: "quest-board-dnd") { dependencyGraphManifests(first: 50) { totalCount nodes { filename } } } }'` — expect 13 manifests, five of them ghosts. Note the real repo name is required here; the old `quest-board` name only works for REST via redirect.
    - Why it stays unfixed: the only reliable eviction lever is toggling Dependabot alerts off and on for the repository, which is permanently ruled out because it would put the audit trail on three earlier dismissals (#7, #8, #11) at risk. The remaining non-destructive lever is a manifest-touching commit reaching `main`, which the `milestone/v9-rolling-improvements` merge will do naturally — re-run the query afterwards and see whether the ghosts cleared. More alerts off these ghosts are expected until then.
    - The pointer: full evidence and the re-runnable appendix in `.planning/SECURITY-TRIAGE.md`.

    **Hook 2 — Key Decisions row.** Append as the last row of the `## Key Decisions` table, immediately before the `## Evolution` heading. Three columns, matching the existing rows' voice:
    - *Decision*: five HIGH Dependabot alerts (#17-#21) dismissed as `inaccurate` rather than patched, and the repository-level Dependabot alerts off/on toggle ruled out permanently.
    - *Rationale*: the alerted package and version are absent from every live `QuestBoard.*` manifest, confirmed by two independent sources — a fresh local `dotnet list package --include-transitive` sweep and GitHub's own SBOM — so GitHub's attribution is factually wrong, not merely out of date; `not_used` would have asserted something weaker and untrue. The toggle is the only reliable ghost-manifest eviction lever but would risk the audit trail on three earlier dismissals, a bad trade in a triage whose entire value is defensible evidence.
    - *Outcome*: `✓ Good — Phase 73; evidence and re-runnable reproduction in .planning/SECURITY-TRIAGE.md`.

    Keep the table row on a single line with escaped pipes avoided — no literal `|` inside cell text.
  </action>

  <acceptance_criteria>
    - `grep -c "SECURITY-TRIAGE" .planning/PROJECT.md` returns exactly `2` — one in the Known issues bullet, one in the Key Decisions row
    - The Known issues bullet sits inside `## Context`, after the last existing bullet and before `## Constraints`: `awk '/^## Context/,/^## Constraints/' .planning/PROJECT.md | grep -c "SECURITY-TRIAGE"` returns `1`
    - The Key Decisions row sits inside the `## Key Decisions` table, before `## Evolution`: `awk '/^## Key Decisions/,/^## Evolution/' .planning/PROJECT.md | grep -c "SECURITY-TRIAGE"` returns `1`
    - The Known issues bullet contains the `dependencyGraphManifests` reproduction query and the real repo name `quest-board-dnd`
    - The Known issues bullet names the three protected dismissals (#7, #8, #11) as the reason the toggle is ruled out
    - The Key Decisions row is a well-formed 3-column table row and the table still parses: `awk '/^## Key Decisions/,/^## Evolution/' .planning/PROJECT.md | grep -c "^| "` increased by exactly 1 relative to before the edit
    - Neither hook contains a bare phase-local decision ID as justification: `awk '/^## Context/,/^## Evolution/' .planning/PROJECT.md | grep -cE "\bD-[0-9]{2}\b"` returns `0`
    - PROJECT.md was edited, not rewritten — `git diff --stat -- .planning/PROJECT.md` shows a small insertion count and no large deletion count
  </acceptance_criteria>

  <verify>
    <automated>grep -c "SECURITY-TRIAGE" .planning/PROJECT.md</automated>
    <automated>awk '/^## Context/,/^## Constraints/' .planning/PROJECT.md | grep -c "SECURITY-TRIAGE"</automated>
    <automated>git diff --stat -- .planning/PROJECT.md</automated>
  </verify>

  <done>PROJECT.md carries exactly two new hooks — a Known issues bullet with the ghost-manifest root cause, the re-runnable reproduction query and the plain-language reason it stays unfixed, and a Key Decisions row for the dismiss-not-patch call — both pointing at `.planning/SECURITY-TRIAGE.md`, both added by scoped edits, neither citing an ID that will dangle after archiving.</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase gate and record which reading of success criterion 5 applies</name>

  <read_first>
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-VALIDATION.md` — `## Phase Gate Assertion` and its D-19 qualifier
    - `.planning/phases/73-resolve-stale-high-security-alerts/73-CONTEXT.md` — D-06, D-19
    - `.planning/ROADMAP.md` — Phase 73 success criteria, read but not edited
    - `.planning/SECURITY-TRIAGE.md` — entry one as completed by Task 1
  </read_first>

  <files>.planning/SECURITY-TRIAGE.md</files>

  <action>
    Run the phase gate exactly as success criterion 5 quotes it, old repo name and all — it still resolves via GitHub's rename redirect:
    `gh api repos/theunschut/quest-board/dependabot/alerts --jq '[.[] | select(.state=="open" and .security_vulnerability.severity=="high")] | length'`

    Then run the scoped assertion that actually corresponds to what this phase was chartered to do:
    `gh api repos/theunschut/quest-board-dnd/dependabot/alerts --jq '[.[] | select(.state=="open" and (.number | IN(17,18,19,20,21)))] | .[].number'`

    Interpret honestly and record the interpretation, do not just record the number:

    - **Both return zero / empty.** The literal reading and the D-19 reading coincide. Record that, plainly.
    - **The scoped assertion is empty but the phase gate is non-zero.** A HIGH alert outside #17-#21 has appeared since the phase started. Per D-19 this does not block closure: success criterion 5 is read as "alerts #17-#21 are dismissed and no HIGH alert this phase was scoped to handle remains open." Making closure hostage to GitHub's advisory feed would be hostage to something nobody controls, and the ghost manifests surviving this phase guarantees it fires again. Append the new alert to `.planning/SECURITY-TRIAGE.md` as **entry two**, dated, with its number, package, GHSA and `manifest_path`, and an explicit note that it is untriaged and inherits this entry's reproduction queries. Record in the summary that the D-19 reading was used and why.
    - **The scoped assertion returns any of 17-21.** That alert was held or failed its gate in plan 73-02. This is not a gate to reinterpret: record it as an open finding, leave it open, and surface it to the operator. Do not patch anything and do not dismiss it to make the number read zero.

    Do not edit ROADMAP.md's success criterion 5 to match whichever reading applied. The literal text stays as written; the reading is recorded in the phase artifacts.

    Under no circumstance is the alert count reduced by toggling Dependabot alerts off and on for the repository. That lever would clear the ghosts and the number, and take the audit trail on dismissals #7, #8 and #11 with it — permanently ruled out for this repo, and making a gate pass is not a new reason.
  </action>

  <acceptance_criteria>
    - `gh api repos/theunschut/quest-board/dependabot/alerts --jq '[.[] | select(.state=="open" and .security_vulnerability.severity=="high")] | length'` was run once, after all PATCHes, and its numeric result is recorded verbatim
    - `gh api repos/theunschut/quest-board-dnd/dependabot/alerts --jq '[.[] | select(.state=="open" and (.number | IN(17,18,19,20,21)))] | length'` returns `0` for every alert that passed its gate and was approved
    - The recorded result states which reading of success criterion 5 was applied and why, in one or two sentences
    - If the phase gate returned non-zero due to a new out-of-scope HIGH alert, `.planning/SECURITY-TRIAGE.md` contains `## Entry 2` with that alert's number, package, GHSA and `manifest_path`, and `grep -c "^## Entry" .planning/SECURITY-TRIAGE.md` returns `2`
    - `.planning/ROADMAP.md` success criterion 5 text is unchanged: `git diff -- .planning/ROADMAP.md` shows no change to the Phase 73 success criteria block
    - Dependabot alerts remain enabled for the repository and the three pre-existing dismissals still read `fix_started`: `gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=dismissed --jq '[.[] | select(.number | IN(7,8,11))] | map(.dismissed_reason) | join(",")'` returns `fix_started,fix_started,fix_started`
    - `git status --porcelain -- '*.csproj'` is empty
  </acceptance_criteria>

  <verify>
    <automated>gh api repos/theunschut/quest-board/dependabot/alerts --jq '[.[] | select(.state=="open" and .security_vulnerability.severity=="high")] | length'</automated>
    <automated>gh api repos/theunschut/quest-board-dnd/dependabot/alerts --jq '[.[] | select(.state=="open" and (.number | IN(17,18,19,20,21)))] | length'</automated>
    <automated>gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=dismissed --jq '[.[] | select(.number | IN(7,8,11))] | map(.dismissed_reason) | join(",")'</automated>
  </verify>

  <done>The phase gate ran once after all PATCHes; both the literal command and the scoped assertion are recorded with their results; the reading applied is stated explicitly rather than implied by a number; any new out-of-scope HIGH alert became entry two rather than a blocker; ROADMAP text unchanged; audit trail on #7/#8/#11 intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase outcome -> permanently-loaded project context (`PROJECT.md`) | What crosses here is read by every future session. An overstated claim here outlives the phase that made it. |
| Durable record -> archived phase directory | `/gsd-cleanup` archives `.planning/phases/73-*` at milestone close. Anything the record depends on that lives on the wrong side of this boundary breaks exactly when a reviewer needs it. |
| Phase gate result -> phase closure decision | A number that can be made to read zero by a destructive action is a boundary worth naming. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Repudiation | The durable record's claims vs the real dismissal outcomes | high | mitigate | Task 1 fills outcome cells from plan 73-02's actual log, never from the plan's expectations; held or failed alerts get their real state and their failing command output. Task 1's acceptance criteria require the live `dismissed_comment` to appear verbatim in the record, so record and reality are diffable. |
| T-73-02 | Repudiation / Tampering | Existing dismissals #7, #8, #11, and the phase-gate number | high | mitigate | The toggle appears in no command. Task 3 explicitly forbids reducing the alert count by disabling and re-enabling Dependabot alerts, and asserts the three pre-existing dismissals still read `fix_started` after the gate runs. |
| T-73-03 | Tampering | `PROJECT.md` content outside the two insertion points | medium | mitigate | Scoped `Edit` calls only, never a whole-file `Write`; the file is around 105KB and permanently loaded. Verified by `git diff --stat -- .planning/PROJECT.md` showing a small insertion and no large deletion. |
| T-73-04 | Repudiation | Dangling references after `/gsd-cleanup` archives the phase directory | medium | mitigate | The durable record lives at `.planning/` root, not in the phase directory; it copies captured output rather than linking to it; and the PROJECT.md hooks cite the record and plain-language reasoning rather than phase-local decision IDs that will dangle once archived. |
| T-73-05 | Denial of Service (process-level, not technical) | Phase closure held hostage by GitHub's advisory feed | low | accept | Accepted by design: the ghost manifests survive this phase, so new alerts off them are expected. A new out-of-scope HIGH alert becomes entry two in the log rather than a blocker, and the reading applied is recorded explicitly instead of a non-zero result being silently swallowed. |

No `T-73-SC` supply-chain row: this phase runs zero package-manager installs, so RESEARCH.md's Package Legitimacy Audit is not applicable by its own scope condition.

**ASVS L1 applicability (from RESEARCH.md `## Security Domain`):** V2 Authentication, V3 Session Management, V4 Access Control, V5 Input Validation and V6 Cryptography are **not applicable** — this plan writes two markdown files and performs read-only API assertions. No application surface is touched. Padding this register with generic web-application threats would misrepresent the analysis.
</threat_model>

<artifacts_this_phase_produces>
| Artifact | Kind | Produced by | Notes |
|----------|------|-------------|-------|
| `.planning/phases/73-resolve-stale-high-security-alerts/73-EVIDENCE-CAPTURE.md` | New file | Plan 73-01 | Working capture; archived with the phase directory. |
| `.planning/SECURITY-TRIAGE.md` | New file | Plan 73-01 (created), Plan 73-03 Task 1 (completed) | Durable append-only log at `.planning/` root; entry one is this incident; entry two added by Plan 73-03 Task 3 only if a new out-of-scope HIGH alert exists at gate time. |
| `.planning/phases/73-resolve-stale-high-security-alerts/73-DISMISSAL-DRAFTS.md` | New file | Plan 73-02 | Drafts, approval outcome, per-alert gate and PATCH log. |
| `.planning/phases/73-resolve-stale-high-security-alerts/dismissals/dismiss-{17,18,19,20,21}.json` | New files | Plan 73-02 | The exact request bodies sent. |
| `.planning/PROJECT.md` `## Context` -> *Known issues / tech debt* bullet | New list item | Plan 73-03 Task 2 | Ghost-manifest root cause, reproduction query, plain-language reason it stays unfixed, pointer to the record. |
| `.planning/PROJECT.md` `## Key Decisions` row | New table row | Plan 73-03 Task 2 | Dismiss-not-patch call and the permanent toggle veto. |
| Dependabot alerts #17, #18, #19, #20, #21 | Remote state change | Plan 73-02 Task 3 | `state=dismissed`, `dismissed_reason=inaccurate`, per-alert comment, `dismissed_by=cryptic96`. Irreversible, public. |

Nothing else. No source file, no `.csproj`, no NuGet package, no migration, no test file.
</artifacts_this_phase_produces>

<verification>
All checks are state assertions, not tests. No build, no test suite.

1. `grep -c "SECURITY-TRIAGE" .planning/PROJECT.md` returns exactly `2`
2. `awk '/^## Context/,/^## Constraints/' .planning/PROJECT.md | grep -c "SECURITY-TRIAGE"` returns `1` and `awk '/^## Key Decisions/,/^## Evolution/' .planning/PROJECT.md | grep -c "SECURITY-TRIAGE"` returns `1`
3. `grep -cE "^\| *#?(17|18|19|20|21) *\|" .planning/SECURITY-TRIAGE.md` returns `5`, with no empty gate or dismissal cell
4. Each dismissed alert's live `dismissed_comment` appears verbatim in `.planning/SECURITY-TRIAGE.md`
5. `gh api repos/theunschut/quest-board-dnd/dependabot/alerts --jq '[.[] | select(.state=="open" and (.number | IN(17,18,19,20,21)))] | length'` returns `0` for the approved set
6. The phase gate command from success criterion 5 was run once and its result recorded together with the reading applied
7. `gh api repos/theunschut/quest-board-dnd/dependabot/alerts -X GET -f state=dismissed --jq '[.[] | select(.number | IN(7,8,11))] | map(.dismissed_reason) | join(",")'` returns `fix_started,fix_started,fix_started`
8. `git status --porcelain -- '*.csproj'` is empty and `git diff -- .planning/ROADMAP.md` shows no change to the Phase 73 success criteria
</verification>

<success_criteria>
- SECALERT-04: the investigation and its outcome are recorded in `.planning/PROJECT.md` via two hooks, both pointing at a durable record that survives phase-directory archiving
- SECALERT-05: no HIGH alert this phase was scoped to handle remains open, with the reading applied stated explicitly rather than a bare number reported
- The durable record's claims are diffable against live GitHub state, comment for comment
- No alert count was improved by any destructive action, and the pre-existing dismissal audit trail is intact
- No source file, `.csproj` or package was changed anywhere in the phase
</success_criteria>

<output>
Create `.planning/phases/73-resolve-stale-high-security-alerts/73-03-SUMMARY.md` when done. Record: both phase-gate command results verbatim; which reading of success criterion 5 was applied and why; whether an entry two was added; the two PROJECT.md hook locations; confirmation that ROADMAP text is unchanged, `#7`/`#8`/`#11` are intact, and no `.csproj` changed.
</output>
