---
phase: 83-availability-surface-naming-and-placement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Views/Agenda/Index.cshtml
  - QuestBoard.Service/Views/Agenda/Index.Mobile.cshtml
  - QuestBoard.Service/Views/Calendar/Index.cshtml
  - QuestBoard.Service/Views/Calendar/Index.Mobile.cshtml
  - QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs
autonomous: true
requirements: [EVTNAME-01, EVTNAME-03, EVTNAME-05, EVTNAME-06]

must_haves:
  truths:
    - "My Agenda keeps its name and gains a subtitle stating its cross-board, events-only scope on desktop and mobile (D-04)"
    - "A Dungeon Master standing on My Agenda has a header button back to Board Availability; a player does not (D-10)"
    - "The Calendar page's cross-link to the board-scoped grid reads Board Availability and renders only for a Dungeon Master, on both layouts (D-08)"
    - "The Calendar page's My Agenda button beside it is unchanged and stays visible to everyone"
    - "CalendarButtonStyleTests proves both the filled-button styling and the new visibility rule, and is green"
  artifacts:
    - "QuestBoard.Service/Views/Agenda/Index.cshtml with subtitle and DM-gated return button"
    - "QuestBoard.Service/Views/Agenda/Index.Mobile.cshtml with the same in mobile shape"
    - "QuestBoard.Service/Views/Calendar/Index.cshtml with the gated, renamed cross-link"
    - "QuestBoard.Service/Views/Calendar/Index.Mobile.cshtml with the gated, renamed cross-link"
    - "QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs with two DM-seeded cases and one player-absent case"
  key_links:
    - "The Agenda subtitle depends on the .header-subtitle rule shipped by plan 83-01 — same wave, no code dependency, but the subtitle renders unstyled until both merge"
    - "The Calendar page's My Agenda button and the .calendar-navigation month picker are flex siblings of the gated button; justify-content-between redistributes on its own, so no placeholder or fixed-width wrapper is added"
    - "The user-menu My Agenda entry in both shared layouts is what keeps a player one click from their own view once the header buttons are DM-only — it is not in this plan's files_modified and must not be touched"
---

<objective>
Complete the pair. My Agenda gains the subtitle that makes it read as the deliberate counterpart of
Board Availability (D-04) and a Dungeon-Master-only return link back to it (D-10); the Calendar
page's cross-link is renamed and gated the same way in both directions (D-08); and
`CalendarButtonStyleTests` — whose two existing cases both authenticate as a Player and assert that
link is present, so D-08 breaks both outright — is re-seeded in the same plan so the suite never
goes red.

Purpose: shipping the Board Availability subtitle without the My Agenda one leaves the pair
asymmetric and the phase half-done — the two subtitles are one decision seen from two sides.
Output: a symmetric surface pair, DM-only cross-links in both directions, and a Calendar test class
that proves both the styling and the new visibility rule.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md
@.planning/phases/83-availability-surface-naming-and-placement/83-UI-SPEC.md
@.planning/phases/83-availability-surface-naming-and-placement/83-PATTERNS.md
</context>

## Artifacts this plan produces

| Kind | Name | Location |
|------|------|----------|
| Razor local | `isDm` | `Views/Agenda/Index.cshtml`, `Views/Agenda/Index.Mobile.cshtml` |
| Rendered string | `Upcoming events across all your boards` | both Agenda Index views |
| Rendered string | `Board Availability` (button label) | both Agenda Index views, both Calendar Index views |
| Test method | `DesktopCalendar_BoardAvailabilityLink_DmSeesFilled_NotOutline` | `CalendarButtonStyleTests.cs` |
| Test method | `MobileCalendar_BoardAvailabilityLink_DmSeesFilled_NotOutline` | `CalendarButtonStyleTests.cs` |
| Test method | `Calendar_BoardAvailabilityLink_AbsentForPlayer` | `CalendarButtonStyleTests.cs` |
| Test constant | `DesktopUserAgent` | `CalendarButtonStyleTests.cs` |
| Requirement IDs exercised | EVTNAME-01, EVTNAME-03, EVTNAME-05, EVTNAME-06 | — |

<!-- planner-discipline-allow: Availability Overview -->

<tasks>

<task type="auto">
  <name>Task 1: Add the My Agenda subtitle and DM-only return button on BOTH layouts</name>
  <files>QuestBoard.Service/Views/Agenda/Index.cshtml, QuestBoard.Service/Views/Agenda/Index.Mobile.cshtml</files>
  <read_first>
    - `QuestBoard.Service/Views/Agenda/Index.cshtml` lines 1-28 — the `@{ }` block that already hoists `hasOtherBoardRow`, the two explanatory comment blocks, and the `card-header modern-card-header` that currently has no flex layout and no button.
    - `QuestBoard.Service/Views/Agenda/Index.Mobile.cshtml` lines 1-28 — the same, where the heading is a bare `<h4 class="mb-3">` directly inside `container-fluid px-2 mt-2` with no wrapper at all.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-UI-SPEC.md` — **Modification 7** and **Modification 8** (exact before/after markup) and the **Copywriting Contract** row for the My Agenda subtitle.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-PATTERNS.md` — the `Views/Agenda/Index.cshtml` and `Views/Agenda/Index.Mobile.cshtml` entries.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md` — D-04 and D-10.
  </read_first>
  <action>
Apply both edits in this one task and one commit — these are a desktop/mobile twin pair selected by user agent, and splitting them is the exact failure mode this phase is guarding against.

1. **Hoist `isDm` (D-10).** In each file, add `isDm` to the EXISTING `@{ }` block that already computes `hasOtherBoardRow` — do not open a second code block. Value: `(await AuthorizationService.AuthorizeAsync(User, "DungeonMasterOnly")).Succeeded`. `AuthorizationService` comes from `_ViewImports.cshtml`; add no `@inject`.

2. **`ViewData["Title"]` does not change.** My Agenda keeps its name (D-04). Only the markup around the heading changes.

3. **Desktop header (`Index.cshtml`).** The `card-header modern-card-header` div gains `d-flex justify-content-between align-items-start`. Wrap the existing `<h2 class="mb-0">` in a bare `<div>` with no class and add a sibling `<p class="header-subtitle mb-0">` inside that wrapper whose text is exactly `Upcoming events across all your boards` — static copy, no interpolation, no conditional. After the wrapper, add `@if (isDm) { ... }` containing an anchor with `asp-controller="Events"`, `asp-action="Index"`, `class="btn btn-secondary btn-sm"`, an `<i class="fas fa-calendar-check me-2"></i>` icon, and the label `Board Availability`. Keep the existing `fas fa-calendar-days text-purple me-2` heading icon untouched.

4. **Mobile header (`Index.Mobile.cshtml`).** Introduce a new wrapper `<div class="d-flex justify-content-between align-items-start mb-3">` immediately inside `container-fluid px-2 mt-2`, containing a bare `<div>` with the heading and the subtitle, then the same `@if (isDm)` button block, then close the wrapper before the existing `@if (Model.EmptyState == AgendaEmptyState.NoBoards)` block. Move the `mb-3` off the `<h4>` and onto that new wrapper — the `<h4>` becomes `mb-0` — so the spacing below the whole header block is preserved rather than duplicated or lost. The mobile button's icon spacing is `me-1`, matching this file's other compact buttons (the roster toggle and the filter button), not `me-2`.

5. **Do not touch** the comment block near lines 9-15 of each file explaining why this page's row action deliberately diverges from the board-scoped grid's whole-row click pattern. It is unrelated to the header and the phase context explicitly forbids "restoring consistency" there. Also do not touch the row markup, the roster rendering, the filter dropdown, the paging or the switch-board modal.

The subtitle relies on the `.header-subtitle` rule shipped by plan 83-01 in the same wave. Emit the class regardless; do not add a local style block, a page-specific CSS rule, or a Bootstrap muted-text utility class as a stand-in.

No comment added here may contain a phase number, a plan number, a decision ID or a requirement ID (CLAUDE.md). Preserve CRLF line endings.
  </action>
  <acceptance_criteria>
    - `grep -c 'Upcoming events across all your boards' QuestBoard.Service/Views/Agenda/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1`
    - `grep -c 'header-subtitle mb-0' QuestBoard.Service/Views/Agenda/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1`
    - `grep -c 'Board Availability' QuestBoard.Service/Views/Agenda/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1`
    - `grep -c 'AuthorizeAsync(User, "DungeonMasterOnly")' QuestBoard.Service/Views/Agenda/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1`
    - `grep -c 'ViewData\["Title"\] = "My Agenda"' QuestBoard.Service/Views/Agenda/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1`
    - `grep -c 'align-items-start mb-3' QuestBoard.Service/Views/Agenda/Index.Mobile.cshtml` outputs `1`
    - `grep -c '<h4 class="mb-3">' QuestBoard.Service/Views/Agenda/Index.Mobile.cshtml` outputs `0`
    - `grep -c '@inject' QuestBoard.Service/Views/Agenda/Index.cshtml` outputs `0`, and the same command against `Index.Mobile.cshtml` outputs `0`
    - `grep -cE 'Phase 83|D-1?[0-9]{1,2}\b|EVTNAME' QuestBoard.Service/Views/Agenda/Index.cshtml` outputs `0`, and the same command against `Index.Mobile.cshtml` outputs `0`
    - `dotnet build` exits 0
    - `dotnet test --filter AgendaControllerIntegrationTests` exits 0
    - `dotnet test --filter AgendaMobileRenderTests` exits 0
    - `dotnet test --filter AgendaTenantIsolationTests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build && dotnet test --filter "AgendaControllerIntegrationTests|AgendaMobileRenderTests|AgendaTenantIsolationTests"</automated>
  </verify>
  <done>Both Agenda views render the static subtitle under the heading and a `Board Availability` return button that exists in the DOM only for a Dungeon Master; the page title, row markup and existing Agenda test classes are unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 2: Rename and DM-gate the Calendar page cross-link on BOTH layouts</name>
  <files>QuestBoard.Service/Views/Calendar/Index.cshtml, QuestBoard.Service/Views/Calendar/Index.Mobile.cshtml</files>
  <read_first>
    - `QuestBoard.Service/Views/Calendar/Index.cshtml` lines 1-32 — the `card-header modern-card-header d-flex justify-content-between align-items-center` row holding the `<h2>`, the two cross-link buttons and the `.calendar-navigation` month picker as four flex children.
    - `QuestBoard.Service/Views/Calendar/Index.Mobile.cshtml` lines 25-45 — the `d-grid gap-2 mb-3` stack holding the same two buttons full-width.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-UI-SPEC.md` — **Modification 9** and **Modification 10**, including their two explicit "no layout compensation needed, and specifically why" notes.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md` — D-08, which is an operator amendment to the roadmap taken with the contradiction stated.
  </read_first>
  <action>
Apply both edits in this one task and one commit — desktop/mobile twin pair.

In each file, wrap ONLY the first of the two cross-link buttons — the one with `asp-controller="Events" asp-action="Index"` — in `@if ((await AuthorizationService.AuthorizeAsync(User, "DungeonMasterOnly")).Succeeded) { ... }`, and change its label from the retired two-word name to exactly `Board Availability`. Inline the policy check at the call site; do not introduce a partial view, a view component or a tag helper for it, and do not hoist it into a `@{ }` block in these two files (neither file computes other locals at the top, unlike the Agenda views).

Everything else about that anchor is unchanged: same `asp-controller`, same `asp-action`, same `btn btn-secondary btn-sm` (plus the desktop file's trailing `me-2`), same `fas fa-calendar-check me-2` icon.

The adjacent `My Agenda` button — `asp-controller="Agenda"` — is NOT gated and NOT relabelled in either file. Leave it exactly as it is.

Add no layout compensation in either file. On desktop the header is `d-flex justify-content-between align-items-center` and `justify-content-between` redistributes across however many children actually render, and the removed button's `me-2` disappears with it, so there is no orphaned margin; do not add a placeholder element or a fixed-width wrapper. On mobile the container is `d-grid` with full-width stacked buttons, which never had a horizontal alignment relationship to preserve.

Do not use `visually-hidden` or `display:none` — the element is absent from the DOM for a player, so it is never focusable and never announced.

No comment added here may contain a phase number, a plan number, a decision ID or a requirement ID. Preserve CRLF line endings.
  </action>
  <acceptance_criteria>
    - `grep -c 'Board Availability' QuestBoard.Service/Views/Calendar/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1`
    - `grep -c 'Availability Overview' QuestBoard.Service/Views/Calendar/Index.cshtml` outputs `0`, and the same command against `Index.Mobile.cshtml` outputs `0`
    - `grep -c 'AuthorizeAsync(User, "DungeonMasterOnly")' QuestBoard.Service/Views/Calendar/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1`
    - `grep -c 'asp-controller="Agenda"' QuestBoard.Service/Views/Calendar/Index.cshtml` outputs `1`, and the same command against `Index.Mobile.cshtml` outputs `1` — the My Agenda button still exists and is still ungated (confirm by reading: no `@if` wraps it)
    - `grep -c 'visually-hidden' QuestBoard.Service/Views/Calendar/Index.cshtml` outputs `0`, and the same command against `Index.Mobile.cshtml` outputs `0`
    - `grep -c 'd-grid gap-2 mb-3' QuestBoard.Service/Views/Calendar/Index.Mobile.cshtml` outputs `1` — the mobile container class list is unchanged
    - `dotnet build` exits 0
    - `dotnet test --filter CalendarControllerIntegrationTests` exits 0
    - `dotnet test --filter CalendarBoardTypeScopeTests` exits 0
    - `dotnet test --filter CalendarHorizonBannerTests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build && dotnet test --filter "CalendarControllerIntegrationTests|CalendarBoardTypeScopeTests|CalendarHorizonBannerTests"</automated>
  </verify>
  <done>Both Calendar views render a `Board Availability` cross-link that is present in the DOM only for a Dungeon Master, the adjacent My Agenda button is untouched and unconditional, and no layout compensation markup was added.</done>
</task>

<task type="auto">
  <name>Task 3: Re-seed CalendarButtonStyleTests as a DM and add the player-absent case</name>
  <files>QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` in full — both existing `[Fact]` cases seed `roles: ["Player"]` and assert the cross-link is present, so Task 2 above breaks both. Also read its `MobileUserAgent` constant and its private `GetWithUserAgentAsync(HttpClient, string, string)` helper.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md` — D-14, which specifies exactly this rewrite and states it is a mandatory fix, not optional cleanup.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-PATTERNS.md` — the `CalendarButtonStyleTests.cs re-seed pattern` entry under Shared Patterns.
    - `QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs` lines 78-95 and 186-195 — `CreateAuthenticatedClientWithUserAsync` (null roles defaults the group role to Player) and `CreateAuthenticatedDMClientAsync`.
    - `QuestBoard.IntegrationTests/Controllers/LayoutNavigationTests.cs` lines 14-20 — the `DesktopUserAgent` / `MobileUserAgent` constant pair to copy verbatim.
  </read_first>
  <action>
Rewrite this class so it proves the cross-link's styling AND its new visibility rule.

1. **Widen the class doc comment.** It currently describes styling only. Restate it to cover the cross-link generally: filled button classes, and who sees the link. Include one sentence recording that the role flip is what proves placement here — the link's site was role-blind before, so present-for-DM plus absent-for-player is the proof, and if the site ever becomes role-aware for another reason the flip stops proving it. Plain language, no phase number, plan number, decision ID or requirement ID (CLAUDE.md).

2. **Add a `DesktopUserAgent` constant** beside the existing `MobileUserAgent`, copying the exact Chrome-on-Windows string used in `LayoutNavigationTests`.

3. **Re-seed both existing cases as a Dungeon Master.** Rename `DesktopCalendar_AvailabilityOverviewLink_UsesFilled_NotOutline` to `DesktopCalendar_BoardAvailabilityLink_DmSeesFilled_NotOutline` and `MobileCalendar_AvailabilityOverviewLink_UsesFilled_NotOutline` to `MobileCalendar_BoardAvailabilityLink_DmSeesFilled_NotOutline`. Swap each client to `AuthenticationHelper.CreateAuthenticatedDMClientAsync` with its own distinct user name and email (keep the existing `cal_desktop_user` / `cal_mobile_user` style, e.g. `cal_desktop_dm` / `cal_mobile_dm`). Change the label assertion from the retired name to `Board Availability`. Keep every styling assertion exactly as it is: `Contain("btn btn-secondary")` and the three `NotContain` outline-variant assertions.

4. **Add one player-absent case** as a `[Theory]` with `[InlineData(DesktopUserAgent)]` and `[InlineData(MobileUserAgent)]`, named `Calendar_BoardAvailabilityLink_AbsentForPlayer(string userAgent)`. Seed with `CreateAuthenticatedClientWithUserAsync(..., roles: ["Player"])`, fetch `/Calendar` through the existing private `GetWithUserAgentAsync` helper, assert the response is `HttpStatusCode.OK`, and assert `html.Should().NotContain("Board Availability")`. Also assert `html.Should().Contain("My Agenda")` in the same case, so the test fails loudly if the wrong button was gated — that is the single most likely way this edit goes wrong and a bare absence assertion cannot see it.

Do not introduce an HTML parser, and do not split styling and visibility across two files or a third Calendar test class — this suite already has four `Calendar*` classes for one page.

Do not touch any other test class in this task.
  </action>
  <acceptance_criteria>
    - `grep -c 'Availability Overview' QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` outputs `0`
    - `grep -c 'CreateAuthenticatedDMClientAsync' QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` outputs `2`
    - `grep -c 'DesktopUserAgent' QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` is at least `2` (the constant declaration and one `InlineData`)
    - `grep -c 'Calendar_BoardAvailabilityLink_AbsentForPlayer' QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` outputs `1`
    - `grep -c 'NotContain("Board Availability")' QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` outputs `1`
    - `grep -c 'btn btn-outline-' QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` outputs `6` — the three outline-variant assertions survive in both DM cases
    - `grep -cE 'Phase 83|D-1?[0-9]{1,2}\b|EVTNAME' QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` outputs `0`
    - `dotnet test --filter CalendarButtonStyleTests` exits 0 and reports 4 executed cases (2 facts + 1 theory over 2 user agents)
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --filter CalendarButtonStyleTests</automated>
  </verify>
  <done>CalendarButtonStyleTests seeds a Dungeon Master for both styling cases, asserts the new label, and carries one theory proving a Player sees the My Agenda button but not the Board Availability button on either layout. The class is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → view-level authorization | Four view-level policy checks decide whether a navigation affordance is emitted into the DOM |
| board member → cross-board surface | My Agenda already renders every board the viewer belongs to; this plan adds a link from it, not a new read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-05 | Elevation of Privilege (inverted gate) | the `@if` wrapping the Calendar cross-link in `Calendar/Index.cshtml` + `.Mobile` | medium | mitigate | The condition uses the existing `DungeonMasterOnly` policy check verbatim — no negated expression, no hand-rolled role string. Task 3's player-absent theory asserts absence on both user agents, and its companion `Contain("My Agenda")` assertion fails loudly if the wrong one of the two adjacent buttons was wrapped. |
| T-83-06 | Denial of Service (of function) | gating the wrong button — the ungated `My Agenda` cross-link on Calendar, or the unconditional user-menu entry | high | mitigate | Task 2's acceptance criteria assert the `asp-controller="Agenda"` anchor still exists and is unwrapped in both Calendar views; Task 3 asserts `Contain("My Agenda")` for a Player on both layouts. The shared layouts are not in this plan's `files_modified` at all, so the unconditional user-menu entry cannot be reached from here. |
| T-83-07 | Information Disclosure | the new DM-only return button on My Agenda | low | accept | The button exposes a link, not data. Its destination stays open to every board member by design, and the agenda page it sits on already renders the full cross-board roster on every row. Accepted: gating the affordance is a discoverability decision, not a confidentiality control. |
| T-83-08 | Repudiation / stale coverage | `CalendarButtonStyleTests` left asserting the retired label | high | mitigate | Task 3 rewrites the class in the same plan and the same wave as the change that breaks it, so the suite never passes through a red state on this file. Acceptance criteria assert the retired label count is `0` and that the class runs 4 cases green. |
| T-83-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency — four Razor views and one test class. Nothing to audit for legitimacy. |
</threat_model>

<verification>
- `dotnet build` exits 0.
- `dotnet test --filter "AgendaControllerIntegrationTests|AgendaMobileRenderTests|AgendaTenantIsolationTests|CalendarControllerIntegrationTests|CalendarBoardTypeScopeTests|CalendarHorizonBannerTests|CalendarButtonStyleTests"` exits 0.
- `grep -rc 'Availability Overview' QuestBoard.Service/Views/Calendar/` reports `0` for both Index views.
- If `dotnet build` fails on locked output files, Visual Studio is running the app under the debugger. That needs the operator to stop it (Shift+F5) — do not retry in a loop.
</verification>

<success_criteria>
- Both Agenda views carry the static subtitle `Upcoming events across all your boards` under the heading, with the `header-subtitle` class.
- Both Agenda views carry a `Board Availability` return button rendered only for a Dungeon Master.
- Both Calendar views carry a `Board Availability` cross-link rendered only for a Dungeon Master, with the adjacent My Agenda button untouched.
- The retired page label appears in neither Calendar view nor in `CalendarButtonStyleTests`.
- `CalendarButtonStyleTests` runs 4 cases green: two DM styling cases and one player-absent theory over both user agents.
- No source comment or markup added by this plan contains a phase, plan, decision or requirement identifier.
</success_criteria>

<output>
Create `.planning/phases/83-availability-surface-naming-and-placement/83-02-SUMMARY.md` when done.
</output>
