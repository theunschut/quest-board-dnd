---
phase: 83-availability-surface-naming-and-placement
plan: 04
type: execute
wave: 2
depends_on:
  - 83-01
  - 83-02
  - 83-03
files_modified:
  - QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [EVTNAME-01, EVTNAME-07]

must_haves:
  truths:
    - "An automated case proves a caller holding only the Player role gets a 200 from GET /Events with the grid actually rendered — the one fact the navigation assertions cannot see (D-09, D-13 case 4)"
    - "A dedicated guard class proves the retired page label appears in the rendered HTML of none of Board Availability, My Agenda and the Calendar, on both user agents, as a Dungeon Master (D-15)"
    - "Every EVTNAME identifier cited by plans 83-01, 83-02 and 83-03 resolves to a written definition in .planning/REQUIREMENTS.md, with no identifier renumbered"
    - "The Phase 83 roadmap entry names its requirement IDs and its four plans with their wave structure instead of TBD, and both coverage counters reconcile at 67"
    - "The Phase 83 roadmap scope notes, including the 2026-08-30 cross-link amendment, survive unchanged"
  artifacts:
    - "QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs with one added player-reachability case and no existing case altered"
    - "QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs — a new class carrying three theories over both user agents"
    - ".planning/REQUIREMENTS.md with an Availability Surface Naming and Placement section defining EVTNAME-01 through EVTNAME-07, seven traceability rows, and a reconciled coverage block"
    - ".planning/ROADMAP.md with the Phase 83 Requirements and Plans lines resolved and seven EVTNAME rows in the Requirements Coverage table"
  key_links:
    - "This plan's guard class asserts the RETIRED label is absent, which is safe on any page in the app. Plan 83-03's absence assertions target /quests because the NEW label legitimately renders on /Events, /Agenda and /Calendar. The two rules point in opposite directions and must not be conflated."
    - "Plans 83-01, 83-02 and 83-03 remove every occurrence of the retired label; running this plan's guard class before they land fails by design, which is why this plan is wave 2 rather than wave 1"
    - "The EVTNAME numbering is already committed to by the requirements: frontmatter of the three wave-1 plans — the definitions written here must match those references exactly, because renumbering silently breaks three traceability links"
    - "EventsController.Index's bare [Authorize] attribute set is what Task 1's case observes; plan 83-01 Task 3 asserts that attribute set is unchanged, and this plan proves the behaviour it produces"
---

<objective>
Close the four gaps the three wave-1 plans leave open: the one behavioural fact the navigation
assertions structurally cannot see (a Player still gets a working page at `/Events`), the
structural guard against a partial rename leaving two names for one page, and the requirement and
roadmap ledgers that three already-written plans reference but that nothing has yet written.

Purpose: plans 83-01 through 83-03 make every discoverable link to Board Availability
Dungeon-Master-only while deliberately leaving the page itself open. That end state is defensible
but thin — it is one small edit away from the authorization gate the roadmap examined and declined,
and nothing currently fails if someone makes that edit. It is also one missed string away from the
roadmap's second named risk, a partial rename leaving two names for one page, which no single-page
test can see. This plan supplies the two controls, then reconciles the tracking documents so the
phase can actually be verified.
Output: one added integration case, one new guard test class, seven defined requirement IDs, and a
Phase 83 roadmap entry with no placeholders left in it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md
@.planning/phases/83-availability-surface-naming-and-placement/83-UI-SPEC.md
@.planning/phases/83-availability-surface-naming-and-placement/83-PATTERNS.md
</context>

## Artifacts this plan produces

| Kind | Name | Location |
|------|------|----------|
| Test method | `Index_PlayerWithoutDmRole_ReturnsOkAndRendersGrid` | `QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` |
| Test class | `StaleAvailabilityOverviewLabelGuardTests` | **NEW** `QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` |
| Test method | `BoardAvailabilityPage_DoesNotRenderRetiredLabel` | `StaleAvailabilityOverviewLabelGuardTests.cs` |
| Test method | `MyAgendaPage_DoesNotRenderRetiredLabel` | `StaleAvailabilityOverviewLabelGuardTests.cs` |
| Test method | `CalendarPage_DoesNotRenderRetiredLabel` | `StaleAvailabilityOverviewLabelGuardTests.cs` |
| Test constant | `DesktopUserAgent` | `StaleAvailabilityOverviewLabelGuardTests.cs` |
| Test constant | `MobileUserAgent` | `StaleAvailabilityOverviewLabelGuardTests.cs` |
| Test helper | `GetWithUserAgentAsync` | `StaleAvailabilityOverviewLabelGuardTests.cs` |
| Requirement ID (minted) | `EVTNAME-01` | `.planning/REQUIREMENTS.md` |
| Requirement ID (minted) | `EVTNAME-02` | `.planning/REQUIREMENTS.md` |
| Requirement ID (minted) | `EVTNAME-03` | `.planning/REQUIREMENTS.md` |
| Requirement ID (minted) | `EVTNAME-04` | `.planning/REQUIREMENTS.md` |
| Requirement ID (minted) | `EVTNAME-05` | `.planning/REQUIREMENTS.md` |
| Requirement ID (minted) | `EVTNAME-06` | `.planning/REQUIREMENTS.md` |
| Requirement ID (minted) | `EVTNAME-07` | `.planning/REQUIREMENTS.md` |
| Doc heading | `### Availability Surface Naming and Placement` | `.planning/REQUIREMENTS.md` |
| Requirement IDs exercised | EVTNAME-01, EVTNAME-07 | — |

<!-- planner-discipline-allow: Availability Overview -->
<!-- planner-discipline-allow: Board Availability -->
<!-- planner-discipline-allow: EVTNAME -->
<!-- planner-discipline-allow: Phase 83 -->

<tasks>

<task type="auto">
  <name>Task 1: Prove a Player still gets a working page at /Events</name>
  <files>QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` lines 1-30 — the class doc comment (which already claims "all-members access with no role gate"), the `MobileUserAgent` constant, and the `DisposeAsync` reset of `ActiveGroupId` and `BoardType`.
    - The same file lines 27-107 — the private seed helpers `SeedEventAsync`, `SeedEventsAsync`, `SeedMemberAsync`, `SeedSignupAsync`, and the `GetMobileAsync` helper. The new case reuses three of the seed helpers verbatim and adds none of its own.
    - The same file lines 108-140 — `Index_PlayerOnCampaignBoard_ReturnsOk`, which asserts the status code and nothing else, and `Index_Desktop_RendersOneRowPerEventAndOneColumnPerMember`, whose seed-and-assert shape the new case borrows.
    - `QuestBoard.Service/Controllers/Events/EventsController.cs` lines 1-55 — confirm the class carries a bare `[Authorize]` and that `Index` carries no policy, so the new case asserts a fact that holds today rather than one that plan 83-01 has to create.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md` — D-09 in full (including the sentence beginning "Do not 'finish the job'") and D-13 case 4.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-UI-SPEC.md` — the **Test Surface Implications** section, fourth bullet.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-PATTERNS.md` — the `EventsOverviewControllerIntegrationTests.cs (+1 case)` row in the File Classification table.
  </read_first>
  <action>
Add exactly one `[Fact]` to `EventsOverviewControllerIntegrationTests`, named
`Index_PlayerWithoutDmRole_ReturnsOkAndRendersGrid`, placed directly after the existing
`Index_PlayerOnCampaignBoard_ReturnsOk` case so the two access facts sit together. Change nothing
else in the file: no existing case is renamed, re-seeded, re-ordered or deleted, no helper is
altered, and the class doc comment stays as it is — it already describes this property, and this
case is what finally proves it.

Body, in order: call `TestDataHelper.ClearDatabaseAsync(factory.Services)`; seed one event through
`SeedEventAsync` with a distinctive title and a date one day in the future; seed one board member
through `SeedMemberAsync` with a distinctive display name; record a confirmed `VoteType.Yes`
signup for that member on that event through `SeedSignupAsync`; then build the acting client with
`AuthenticationHelper.CreateAuthenticatedClientWithUserAsync` using its own user name and email
(follow the file's `evtoverview_*` naming, for example `evtoverview_player_grid`) and an explicit
`roles: ["Player"]` argument. Pass the role explicitly rather than relying on the helper's null
default: the caller's role is the entire point of this case, so it has to be readable in the test
body instead of inferred from a defaulting branch three files away. Request `/Events` through the
client's own `GetAsync` and read the body as a string, matching the surrounding desktop cases.

Do not set `factory.TestGroupContext.BoardType` — the class resets it to `BoardType.OneShot` in
`DisposeAsync`, and the neighbouring desktop cases rely on that default rather than setting it
per test.

Four assertions, all required:
1. The response status is `HttpStatusCode.OK`. This is the primary control. A policy added to the
   action turns this into a redirect or a forbidden status and the case goes red immediately.
2. The HTML contains `avail-grid`, the desktop grid table's own class. Status alone can be satisfied
   by a shell that renders no grid at all.
3. The HTML contains the seeded event title.
4. The HTML contains the seeded member's display name — so the case also fails if the member axis
   silently stops resolving for a non-privileged caller.

Do NOT add a mobile-user-agent twin of this case. The authorization decision it guards is taken in
the MVC pipeline before a view is selected, so it cannot differ between the desktop and the mobile
render path; and the class's existing `Index_MobileUserAgent_RendersCardList` case already
exercises the mobile path with a caller whose group role is Player. A second user agent here would
add executed cases without adding a fact.

Write a comment directly above the case, in plain language, recording why it exists: the page is
deliberately open to every board member while every link that leads to it is shown only to Dungeon
Masters, that split is intentional, and this is the only case in the suite that fails if someone
later attaches an authorization policy to the action. Say that the explicit Player role and the
rendered-grid assertions are what make it a control rather than a smoke test. Per `CLAUDE.md` the
comment must not contain a phase number, a plan number, a decision ID or a requirement ID, and must
stay true after this phase closes.

Preserve CRLF line endings.
  </action>
  <acceptance_criteria>
    - `grep -c 'Index_PlayerWithoutDmRole_ReturnsOkAndRendersGrid' QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` outputs `1`
    - `grep -c 'roles: \["Player"\]' QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` outputs `2` — the pre-existing case plus the new one
    - `grep -c 'avail-grid' QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` is at least `1`
    - `grep -c 'Index_PlayerOnCampaignBoard_ReturnsOk' QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` outputs `1` — the pre-existing access case survives untouched
    - `git diff QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` contains no removed lines: every changed line in the diff is an addition
    - `grep -cE 'Phase 83|D-1?[0-9]{1,2}\b|EVTNAME' QuestBoard.IntegrationTests/Controllers/EventsOverviewControllerIntegrationTests.cs` outputs `0`
    - `dotnet build` exits 0
    - `dotnet test --filter EventsOverviewControllerIntegrationTests` exits 0, and the run output names `Index_PlayerWithoutDmRole_ReturnsOkAndRendersGrid` among the executed cases
  </acceptance_criteria>
  <verify>
    <automated>dotnet build && dotnet test --filter EventsOverviewControllerIntegrationTests</automated>
  </verify>
  <done>`EventsOverviewControllerIntegrationTests` carries one added case that seeds an event, a member and a signup, authenticates explicitly as a Player, and requires both a 200 and a rendered grid from `GET /Events`. Every pre-existing case in the file is byte-identical and the class is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the cross-surface guard class against a partial rename</name>
  <files>QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Controllers/RowNavigationAccessibilityTests.cs` lines 1-70 — the class shape the Pattern Map names as the closest structural relative: the primary-constructor `WebApplicationFactoryBase factory` parameter, `IClassFixture<WebApplicationFactoryBase>, IAsyncLifetime`, the user-agent constant with its explanatory comment, the `InitializeAsync`/`DisposeAsync` pair resetting `ActiveGroupId` and `BoardType`, and its private user-agent request helper.
    - `QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` lines 1-30 — its private `GetWithUserAgentAsync(HttpClient, string, string)` helper, which is the simpler shape this class wants: send through an already-authenticated client so the client's default authorization header still applies.
    - `QuestBoard.IntegrationTests/Controllers/LayoutNavigationTests.cs` lines 14-20 — the `DesktopUserAgent` and `MobileUserAgent` constant pair, to copy verbatim.
    - `QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs` lines 186-195 — `CreateAuthenticatedDMClientAsync` and its parameter order.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md` — D-15 in full, including its two rejected alternatives, its instruction to name the class for what it guards, and the sentence explaining why keeping the internal domain vocabulary cannot make this class fail. Also D-01, whose retained vocabulary that sentence refers to.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-PATTERNS.md` — the `## No Analog Found` table, whose single row is this class.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-UI-SPEC.md` — the **Test Surface Implications** section, first bullet.
  </read_first>
  <action>
Create the new file `QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs`
in namespace `QuestBoard.IntegrationTests.Controllers`. The class name is chosen so a reader
scanning the folder can tell what it guards without opening it.

**Class shape.** `public class StaleAvailabilityOverviewLabelGuardTests(WebApplicationFactoryBase factory)`
implementing `IClassFixture<WebApplicationFactoryBase>` and `IAsyncLifetime`, with `InitializeAsync`
returning `ValueTask.CompletedTask` and `DisposeAsync` resetting `factory.TestGroupContext.ActiveGroupId`
to `1` and `factory.TestGroupContext.BoardType` to `BoardType.OneShot` — the same shape
`RowNavigationAccessibilityTests` uses, and the same reset every board-type-sensitive class in this
folder performs.

**Constants.** `DesktopUserAgent` and `MobileUserAgent`, copied verbatim from `LayoutNavigationTests`,
each `private const string`. Carry over the one-line comment explaining that mobile views in this app
are selected from the request's user agent, so a request without the header renders the desktop view.
"Both user agents" in this class means two real user-agent strings; there is no viewport or breakpoint
mechanism to exercise instead.

**Helper.** A private `GetWithUserAgentAsync(HttpClient client, string url, string userAgent)` returning
`(HttpResponseMessage Response, string Html)`: construct an `HttpRequestMessage` for the url, call
`TryAddWithoutValidation("User-Agent", userAgent)`, send it through the supplied authenticated client
so its default authorization header still applies, and read the body as a string. Copy the shape from
`CalendarButtonStyleTests` rather than inventing a variant.

**Three `[Theory]` methods**, each carrying `[InlineData(DesktopUserAgent)]` and
`[InlineData(MobileUserAgent)]` and taking a `string userAgent` parameter — six executed cases:
1. `BoardAvailabilityPage_DoesNotRenderRetiredLabel` — fetches `/Events`
2. `MyAgendaPage_DoesNotRenderRetiredLabel` — fetches `/Agenda`
3. `CalendarPage_DoesNotRenderRetiredLabel` — fetches `/Calendar`

Every method: call `TestDataHelper.ClearDatabaseAsync(factory.Services)` first, then build the acting
client with `AuthenticationHelper.CreateAuthenticatedDMClientAsync` giving each method its own distinct
user name and email (for example `stalelabel_events`, `stalelabel_agenda`, `stalelabel_calendar`).
Authenticate as a Dungeon Master in all three: a Dungeon Master is the one role that sees every affected
surface and every cross-link in this phase, so any label this phase failed to update is inside a Dungeon
Master's rendered output. A player would not render the cross-link buttons at all and the class would
quietly stop covering half of what it exists to cover.

Seed no events, members or signups. All three pages render their heading and chrome on an empty
database, and the string this class guards lives in page chrome, not in data.

**Three assertions per method, all required:**
- The response status is `HttpStatusCode.OK`.
- A positive marker proving the intended page actually rendered rather than an error shell, an empty
  body or a redirect: `Contain("Board Availability")` for `/Events` (its own heading, which every
  viewer sees), `Contain("My Agenda")` for `/Agenda`, and `Contain("Calendar")` for `/Calendar`.
  Without this the absence assertion would pass on any blank response — the standard way a guard like
  this rots into a permanently-green no-op.
- `html.Should().NotContain("Availability Overview")`.

**Assertion direction — read this before writing the assertions; the two rules in this phase point
opposite ways.** This class asserts the absence of the RETIRED label. That is safe to assert against any
page in the app, because after this phase no page legitimately contains it. That is the opposite of the
rule plan 83-03 follows: absence assertions for the NEW label must target `/quests`, because `/Events`,
`/Agenda` and `/Calendar` all legitimately render the new label in their own page body once this phase
lands. Do not "fix" this class by moving its requests onto `/quests`, and do not add an assertion here
that the new label is absent — that would assert the opposite of what this phase ships and would fail
on all three surfaces.

**Class doc comment.** Record three things in plain language, so a future reader understands why the
class exists instead of deleting it as redundant with the navigation suite:
(a) what it guards — that one page never ends up carrying two names, which is exactly what a partial
    rename produces;
(b) why the navigation suite cannot cover it — those cases assert against the shared layout on a single
    route, while a stale label can survive in a page's own body on a route the navigation suite never
    fetches, and folding this into the single-page overview test class would let a stale label on the
    calendar or the agenda through;
(c) why keeping the internal `Overview` vocabulary in C# type names, view-model names and the domain
    service does not put this class in tension with that decision — C# identifiers never reach rendered
    HTML, so the two facts are independent.
Per `CLAUDE.md` the comment must not contain a phase number, a plan number, a decision ID or a
requirement ID.

Introduce no HTML parser and add no package reference. Write the file with CRLF line endings, matching
the rest of the test project.

This class fails on all three surfaces if run before plans 83-01, 83-02 and 83-03 have landed — those
plans are what remove the retired label from the six views. That is the dependency this plan's wave
encodes; it is not a bug in the class.
  </action>
  <acceptance_criteria>
    - `test -f QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs`
    - `grep -c 'NotContain("Availability Overview")' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `3`
    - `grep -c 'NotContain("Board Availability")' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `0` — this class never asserts the new label is absent
    - `grep -c '\[Theory\]' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `3`
    - `grep -c 'InlineData(' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `6`
    - `grep -c 'CreateAuthenticatedDMClientAsync' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `3`
    - `grep -c 'CreateAuthenticatedClientWithUserAsync' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `0` — every case authenticates as a Dungeon Master
    - `grep -c '"/quests"' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `0`
    - `grep -c 'HttpStatusCode.OK' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `3`
    - `grep -c 'AngleSharp\|HtmlDocument\|HtmlAgilityPack' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `0` — no HTML parser was introduced
    - `grep -cE 'Phase 83|D-1?[0-9]{1,2}\b|EVTNAME' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs` outputs `0`
    - The file is CRLF: `test "$(grep -c $'\r' QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs)" = "$(wc -l QuestBoard.IntegrationTests/Controllers/StaleAvailabilityOverviewLabelGuardTests.cs | cut -d' ' -f1)"` succeeds
    - `git status --short QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj` reports no change — the test project globs its sources, so no project-file edit is needed to pick the new class up
    - `dotnet build` exits 0
    - `dotnet test --filter StaleAvailabilityOverviewLabelGuardTests` exits 0 and reports 6 executed cases
  </acceptance_criteria>
  <verify>
    <automated>dotnet build && dotnet test --filter StaleAvailabilityOverviewLabelGuardTests</automated>
  </verify>
  <done>A new `StaleAvailabilityOverviewLabelGuardTests` class fetches Board Availability, My Agenda and the Calendar as a Dungeon Master on both user agents, requires a 200 and a positive page marker on each, and asserts the retired label appears in none of the six responses. Six cases execute and all pass.</done>
</task>

<task type="auto">
  <name>Task 3: Mint the EVTNAME requirement family and close out the Phase 83 roadmap entry</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full. The `### Personal Cross-Board Event Agenda` section is the exact section format to copy; the `## Traceability` table uses a three-column `| ID | Phase | Status |` layout whose last row is the `EVTAGENDA-10 | Phase 82` one; and the `**Coverage:**` block at the end currently reads `60 total` and `60/60`.
    - `.planning/ROADMAP.md` — the `### Phase 83: Availability Surface Naming and Placement` entry, which sits at the very end of the file after `## Research Flags` rather than inside the `## Phases` section. Read the whole entry: the goal line, the three placeholder lines, all six scope notes, the "Why not make the overview DM-only" paragraph, the reopen condition, and the two named risks.
    - `.planning/ROADMAP.md` — the `### Phase 82: Personal Cross-Board Event Agenda` entry, for the exact format of the `**Requirements**:` comma-separated list, the `**Plans**:` line, and the wave-grouped plan list with its `**Wave N**` headings and `*(blocked on Wave N completion)*` annotation.
    - `.planning/ROADMAP.md` — the `## Requirements Coverage` table, a two-column `| Requirement | Phase |` layout ending in the `EVTAGENDA-10` row, and the `**Coverage:**` line under it reading `60/60 requirements mapped`.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-01-PLAN.md`, `83-02-PLAN.md` and `83-03-PLAN.md` — each plan's `requirements:` frontmatter line and its `must_haves.truths` block. The seven definitions written here must be consistent with how those three plans already use the identifiers.
    - `.planning/phases/83-availability-surface-naming-and-placement/83-CONTEXT.md` — D-01 through D-15, which the seven definitions restate as outcomes a verifier can check.
  </read_first>
  <action>
Two documents, one commit. The identifier list and both coverage counters have to move together or the
two ledgers disagree with each other, which is worse than either being stale.

**Part A — `.planning/REQUIREMENTS.md`.**

Add a new section headed `### Availability Surface Naming and Placement`, placed immediately after the
existing `### Personal Cross-Board Event Agenda` section and before `### Link Previews — Foundation and
Quests`, keeping the events family contiguous — the same placement rule the agenda section followed.

Use the file's existing item format exactly: `- [ ] **ID**: description`, unchecked, one line per
requirement, no trailing period. Write these seven with these exact identifiers. **Do not renumber any
of them.** Plans 83-01, 83-02 and 83-03 are already written and already cite these numbers in their
`requirements:` frontmatter, so a renumber silently breaks three traceability links and the phase's
coverage report stops reconciling.

- `EVTNAME-01`: The board-scoped availability page is called "Board Availability" everywhere a reader meets it — browser tab title, card heading, navigation entry, and both cross-link buttons — and the retired label survives in no rendered page on either layout
- `EVTNAME-02`: Board Availability carries a subtitle under its heading naming the active board and stating that the page covers events rather than quests, with a defined fallback when no board name is in session
- `EVTNAME-03`: My Agenda keeps its name and carries a matching subtitle stating its cross-board, events-only scope, so the two surfaces read as a deliberate pair rather than as two unrelated pages
- `EVTNAME-04`: Board Availability's navigation entry sits inside the Dungeon Master menu directly after Create Event on both the desktop and the mobile layout, behind both the Dungeon Master policy and the existing resolved-board-type condition
- `EVTNAME-05`: Every discoverable route to Board Availability — the navigation entry and the Calendar page's cross-link — renders only for a Dungeon Master on both layouts; a player's remaining route is a bookmark or a shared URL
- `EVTNAME-06`: The two header cross-buttons between Board Availability and My Agenda render only for a Dungeon Master, in both directions and on both layouts, while the unconditional My Agenda entry in each layout's user menu stays visible to every authenticated user
- `EVTNAME-07`: Board Availability itself keeps no authorization gate — a player who reaches it still receives a normal 200 and the full grid, proven by an automated case rather than assumed from the absence of an attribute

Then append seven rows to the `## Traceability` table, directly after the last `EVTAGENDA-10` row, in the
file's existing three-column format, with `Phase 83` and `Not started`.

Then correct the `**Coverage:**` block at the end of the file. This is a reconciliation, not an append:
the `v1 requirements` total changes from 60 to 67, `Mapped to phases` changes from `60/60 ✓` to
`67/67 ✓`, and the `Unmapped` line stays at 0. Leave no `60` count anywhere in that block.

**Part B — `.planning/ROADMAP.md`.**

Four scoped replacements. Do not rewrite the file and do not rewrite the Phase 83 entry as a whole —
edit only the lines named below.

1. Replace `**Requirements**: TBD` with a single line listing all seven identifiers, comma-separated in
   ascending order, matching the Phase 82 entry's format.
2. Replace the `**Plans**: TBD` line, including its parenthetical instruction to run the planning
   command, with `**Plans**: 4 plans`.
3. Replace the single `- [ ] TBD` placeholder plan line with a wave-grouped list in the Phase 82 entry's
   format — a bold `**Wave 1**` heading, a blank line, its plan lines, a blank line, then a bold
   `**Wave 2**` heading carrying the `*(blocked on Wave 1 completion)*` annotation, a blank line, and its
   plan line. All four entries are unchecked. Wave 1 holds `83-01-PLAN.md` (rename and subtitle the
   Board Availability page on both layouts, gate its My Agenda header button to Dungeon Masters, and add
   the shared header-subtitle style rule), `83-02-PLAN.md` (My Agenda's matching subtitle and
   Dungeon-Master-only return link, the renamed and gated Calendar cross-link, and the re-seeded Calendar
   button test class) and `83-03-PLAN.md` (move the navigation entry into the Dungeon Master menu on both
   layouts, collapse the desktop Calendar dropdown, and replace the four role-blind navigation cases with
   the six role-flip theories). Wave 2 holds `83-04-PLAN.md` (player reachability case for the
   deliberately open page, the retired-label guard class across all three surfaces, and the requirement
   and roadmap ledger close-out). Use the file's existing em-dash separator between the filename and the
   description.
4. Append seven rows to the `## Requirements Coverage` table directly after the last `EVTAGENDA-10` row,
   in the file's two-column format, one per identifier in ascending order, each mapped to `Phase 83`.
   Then correct the `**Coverage:**` line below that table from `60/60 requirements mapped` to
   `67/67 requirements mapped`, leaving the rest of that line — the unmapped and orphaned-phase counts —
   exactly as it is.

**Do not touch the Phase 83 scope notes, the Origin paragraph, the "Why not make the overview DM-only"
paragraph, the reopen condition, or the two named risks.** The scope note stating that every discoverable
link becomes Dungeon-Master-only was amended deliberately on 2026-08-30, and it carries a parenthetical
recording that amendment. That wording is correct, it is what plans 83-02 and 83-03 implement, and
restoring the pre-amendment wording would put the roadmap back into direct conflict with the phase
context — the exact conflict the amendment was made to remove.

Both documents are CRLF from first line to last. Preserve that; do not normalise either file to LF, and
do not reflow or re-wrap any paragraph you are not explicitly replacing.
  </action>
  <acceptance_criteria>
    - `grep -c 'EVTNAME-' .planning/REQUIREMENTS.md` outputs `14` — seven definitions plus seven traceability rows
    - `grep -c '### Availability Surface Naming and Placement' .planning/REQUIREMENTS.md` outputs `1`
    - `grep -cE '^\- \[ \] \*\*EVTNAME-0[1-7]\*\*:' .planning/REQUIREMENTS.md` outputs `7`
    - `grep -cE '^\| EVTNAME-0[1-7] \| Phase 83 \| Not started \|$' .planning/REQUIREMENTS.md` outputs `7`
    - `grep -c 'v1 requirements: 67 total' .planning/REQUIREMENTS.md` outputs `1` and `grep -c 'Mapped to phases: 67/67' .planning/REQUIREMENTS.md` outputs `1`
    - `grep -cE '60 total|60/60' .planning/REQUIREMENTS.md` outputs `0` — the counters were reconciled, not appended to
    - `grep -cE '^\| EVTNAME-0[1-7] \| Phase 83 \|$' .planning/ROADMAP.md` outputs `7`
    - `grep -c '67/67 requirements mapped' .planning/ROADMAP.md` outputs `1` and `grep -c '60/60 requirements mapped' .planning/ROADMAP.md` outputs `0`
    - `awk '/^### Phase 83:/,0' .planning/ROADMAP.md | grep -c 'TBD'` outputs `0` — the Phase 83 entry sits at the end of the file, so this range covers exactly that entry
    - `grep -cE '83-0[1-4]-PLAN.md' .planning/ROADMAP.md` outputs `4`
    - `awk '/^### Phase 83:/,0' .planning/ROADMAP.md | grep -c 'Amended 2026-08-30 during the discuss pass'` outputs `1` — the deliberate scope-note amendment survived
    - `git diff .planning/ROADMAP.md` shows no removed line belonging to the `**Scope notes:**` block, the Origin paragraph, the rejected-gate paragraph, the reopen condition, or either named risk — verify by reading the diff; the only removals permitted in that entry are the three placeholder lines
    - Every identifier cited anywhere in Phase 83's plans or in the roadmap resolves to a definition: `for id in $(grep -ho 'EVTNAME-[0-9][0-9]' .planning/phases/83-availability-surface-naming-and-placement/83-0*-PLAN.md .planning/ROADMAP.md | sort -u); do grep -q "EVTNAME" .planning/REQUIREMENTS.md && grep -qE "\*\*$id\*\*" .planning/REQUIREMENTS.md || echo "MISSING $id"; done` prints nothing
    - Both files are still CRLF: for each of `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, `grep -c $'\r'` on the file equals `wc -l` on the same file
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'EVTNAME-' .planning/REQUIREMENTS.md)" = "14" && grep -q 'Mapped to phases: 67/67' .planning/REQUIREMENTS.md && grep -q '67/67 requirements mapped' .planning/ROADMAP.md && test -z "$(awk '/^### Phase 83:/,0' .planning/ROADMAP.md | grep 'TBD')" && test "$(awk '/^### Phase 83:/,0' .planning/ROADMAP.md | grep -c 'Amended 2026-08-30 during the discuss pass')" = "1" && echo OK</automated>
  </verify>
  <done>`.planning/REQUIREMENTS.md` defines EVTNAME-01 through EVTNAME-07 in their own section with seven traceability rows and a reconciled 67/67 coverage block; `.planning/ROADMAP.md`'s Phase 83 entry names those seven identifiers, lists all four plans under their waves, carries no placeholder, and has seven new coverage rows with a reconciled 67/67 line — while every scope note, the rejected-gate reasoning, the reopen condition and both risks are unchanged.</done>
</task>

</tasks>

<threat_model>
Dispositions follow OWASP ASVS Level 1. Anything rated high is mitigated inside this plan rather than
accepted. Only threats this plan's own diff can cause or fail to catch are listed; generic items with no
attack surface in a test-and-documentation change are deliberately omitted.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → controller authorization | `EventsController.Index` decides, per request, whether a board member holding no Dungeon Master role may read the aggregate availability grid. This plan adds the only automated observation of that decision. |
| rendered HTML → automated assertion | The guard class reads three pages' rendered output and decides whether the phase's rename is complete. A weak assertion here reports success it did not verify. |
| planning ledger → phase verification | The requirement ledger is what a verifier reads to decide whether the phase shipped what it promised; a wrong or missing identifier mis-reports coverage rather than mis-securing anything. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-13 | Elevation of Privilege | `EventsController.Index` attribute set | high | mitigate | The realistic threat in this diff's blast radius is not an attacker but a future maintainer attaching `[Authorize(Policy = ...)]` to this action to "finish the job" the Dungeon-Master-only links appear to start — silently implementing the design the roadmap examined and declined on the grounds that gating the grid hides strictly less than the unrestricted agenda already reveals. Task 1's case is the control: it authenticates explicitly as a Player, requests the route, and requires both a 200 and rendered grid content, so that edit turns a green suite red on the next run. No other case in the suite states that as its purpose or asserts it for an explicitly Player-role caller. |
| T-83-14 | Tampering (guard rot) | the new guard class's absence assertions | medium | mitigate | An assertion that only checks a string is absent passes on an empty body, an error page or a redirect — the standard way a guard like this decays into a permanently-green no-op. Task 2 requires a 200 status and a positive page marker alongside every absence assertion, on both user agents, so a case can only pass when the intended page actually rendered. |
| T-83-15 | Spoofing (wrong observer) | authenticating the guard class as a Dungeon Master | medium | mitigate | A player does not render either cross-link button after this phase, so a player-seeded guard would silently stop covering two of the surfaces it exists to cover while still passing. Task 2 fixes the role at Dungeon Master for all three theories and its acceptance criteria assert the player-client helper appears zero times in the file. |
| T-83-16 | Repudiation / traceability | the requirement ledger | medium | mitigate | Three plans already cite `EVTNAME-01` through `EVTNAME-07` against definitions that do not exist, so the phase's coverage cannot currently be reconciled at all and a verifier has nothing to check the plans against. Task 3 writes the definitions without renumbering, reconciles both coverage counters, and its acceptance criteria include a structural check that every identifier cited by any Phase 83 plan or by the roadmap resolves to a definition in `REQUIREMENTS.md`. |
| T-83-17 | Tampering (document regression) | the Phase 83 roadmap scope notes | medium | mitigate | The cross-link scope note was amended deliberately to record the Dungeon-Master-only decision; reverting or reflowing it would put the roadmap back in conflict with the phase context, and the researcher, planner and verifier would each read two contradictory instructions. Task 3 forbids touching the scope notes and its acceptance criteria assert the amendment marker still appears exactly once inside the Phase 83 entry and that no scope-note line appears as a removal in the diff. |
| T-83-18 | Information Disclosure | the guard class fetching three authenticated pages as a Dungeon Master | low | accept | The class runs in the test host, against a database it clears itself, and inspects HTML in-process. It grants no production access, adds no route, and asserts only on the absence of a retired label and the presence of each page's own heading. Accepted. |
| T-83-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency of any kind — one added test case, one new class inside the existing test project, and two planning documents. There is nothing to audit for legitimacy. |
</threat_model>

<verification>
- `dotnet build` exits 0.
- `dotnet test --filter "EventsOverviewControllerIntegrationTests|StaleAvailabilityOverviewLabelGuardTests"` exits 0, with the guard class reporting 6 executed cases.
- `dotnet test --filter "LayoutNavigationTests|CalendarButtonStyleTests"` exits 0 — this plan touches neither class, and both must still be green from wave 1.
- `grep -rl 'Availability Overview' QuestBoard.Service/` returns nothing: every view occurrence was removed by wave 1, and this plan adds none.
- The only file in the repository that still contains the retired label is the new guard class, which asserts its absence: `grep -rl 'Availability Overview' QuestBoard.Service/ QuestBoard.Domain/ QuestBoard.Repository/ QuestBoard.IntegrationTests/ QuestBoard.UnitTests/` names `StaleAvailabilityOverviewLabelGuardTests.cs` and nothing else.
- If `dotnet build` fails on locked output files, Visual Studio is running the app under the debugger. The operator has to stop it (Shift+F5) before the build can proceed — do not retry in a loop and do not delete `bin/` or `obj/` to work around it.
</verification>

<success_criteria>
- One automated case proves a caller holding only the Player role receives a 200 from `GET /Events` with the grid, the seeded event and the seeded member all rendered, and its comment records that it is the control against attaching an authorization policy to that action.
- `StaleAvailabilityOverviewLabelGuardTests` executes six cases — Board Availability, My Agenda and the Calendar, each on the desktop and the mobile user agent, authenticated as a Dungeon Master — and each requires a 200, a positive page marker, and the absence of the retired label.
- No source comment or test code added by this plan contains a phase number, a plan number, a decision identifier or a requirement identifier.
- `.planning/REQUIREMENTS.md` defines `EVTNAME-01` through `EVTNAME-07`, with no identifier renumbered relative to what plans 83-01, 83-02 and 83-03 already cite, and both its counters read 67.
- `.planning/ROADMAP.md`'s Phase 83 entry lists its seven requirement identifiers and all four plans under their wave headings, contains no `TBD`, and carries seven new rows plus a reconciled 67/67 line in the Requirements Coverage table.
- Every scope note, the rejected-gate reasoning, the reopen condition and both named risks in the Phase 83 roadmap entry are unchanged, including the 2026-08-30 cross-link amendment.
</success_criteria>

<output>
Create `.planning/phases/83-availability-surface-naming-and-placement/83-04-SUMMARY.md` when done.
</output>
