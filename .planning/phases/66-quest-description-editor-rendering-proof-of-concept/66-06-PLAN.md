---
phase: 66-quest-description-editor-rendering-proof-of-concept
plan: 06
type: execute
wave: 2
depends_on: [66-01, 66-04]
files_modified:
  - QuestBoard.Service/Views/Quest/Details.cshtml
  - QuestBoard.Service/Views/Quest/Details.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Manage.cshtml
  - QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Index.cshtml
autonomous: true
requirements: [QUESTMD-01, EDITOR-04]
user_setup: []

must_haves:
  truths:
    - "Quest Description renders as formatted HTML (headings/bold/lists/blockquotes) on Quest Details, desktop and mobile"
    - "Quest Description appears on Quest Manage inside a collapsible section, collapsed by default, rendered as formatted HTML"
    - "The desktop board card shows Quest Description as Markdown-stripped plain text, and that same stripped length drives poster selection"
    - "The old line-break-preserving pre-wrap styling no longer double-spaces Description, and Rewards' pre-wrap behavior is untouched"
  artifacts:
    - path: "QuestBoard.Service/Views/Quest/Details.cshtml"
      provides: "Formatted-HTML Description on desktop Details"
      contains: "Html.Markdown"
    - path: "QuestBoard.Service/Views/Quest/Manage.cshtml"
      provides: "New collapsible Description section (collapsed by default)"
      contains: "Html.Markdown"
    - path: "QuestBoard.Service/Views/Quest/Index.cshtml"
      provides: "Plain-text board-card Description feeding poster selection"
      contains: "ExtractPlainText"
  key_links:
    - from: "QuestBoard.Service/Views/Quest/Details.cshtml"
      to: "IMarkdownService (via Html.Markdown)"
      via: "read-side HtmlHelper adapter from 66-01"
      pattern: "Html.Markdown"
    - from: "QuestBoard.Service/Views/Quest/Index.cshtml"
      to: "IMarkdownService.ExtractPlainText"
      via: "injected service computes stripped text for display and poster length"
      pattern: "ExtractPlainText"
---

<objective>
Render Quest Description as formatted HTML on Quest Details (desktop + mobile) and on a new collapsible section of Quest Manage, and as Markdown-stripped plain text on the desktop board card (feeding the existing poster-length calculation), using the `Html.Markdown()` helper and `ExtractPlainText` from 66-01.

Purpose: Completes the read half of QUESTMD-01 (board card, Details, Manage) and locks EDITOR-04 (Details/Manage use the same `RenderToHtml(Web)` the preview endpoint uses, so preview matches saved display by construction). Handles two landmines: the Details.Mobile shared-with-Rewards CSS class (D-06 pre-wrap removal must not break Rewards) and the poster-length calibration (D-07: measure the stripped length, not raw Markdown).
Output: Description renders correctly everywhere it is displayed in-app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md

<interfaces>
<!-- Provided by 66-01 + 66-04. -->
- @Html.Markdown(string? markdown) — QuestBoard.Service.Extensions.HtmlHelperExtensions (internal; requires `@using QuestBoard.Service.Extensions`). Emits `<div class="markdown-content">{sanitized html}</div>`, target Web.
- IMarkdownService.ExtractPlainText(string? markdown) — Markdown-stripped plain text (word boundaries preserved). Inject via `@inject QuestBoard.Domain.Interfaces.IMarkdownService MarkdownService`.
- .markdown-content styling is provided globally by 66-04 (markdown-content.css).
Model facts (verified):
- Details.cshtml: description at line 31 = `<p class="lead" style="white-space: pre-wrap;">@Model.Quest?.Description</p>` (Description-only, safe to strip pre-wrap).
- Details.Mobile.cshtml lines ~97-110: BOTH the Description block and the Rewards block use class `quest-description-mobile`, which carries `white-space: pre-wrap` in quests.mobile.css (~lines 102-110). Rewards is NOT migrated until Phase 67 and still needs pre-wrap.
- Manage.cshtml / Manage.Mobile.cshtml: `@model Quest` (Description = `Model.Description`). Description does NOT appear on Manage today — genuinely new surface. Desktop container body is `card-body modern-card-body` at line 35.
- Index.cshtml: `@model IEnumerable<Quest>`, loop var `quest`; local function `SelectPosterByContent(Quest quest, string[] availablePosters)` at line 32 uses `var description = quest.Description ?? "";` then `title.Length + description.Length` with buckets 130/250/500; description div at line 119 = `<div class="quest-description">@quest.Description</div>`.
- _QuestSection.cshtml provides the collapse markup STRUCTURE to replicate (card modern-card + card-header modern-card-header + btn-link text-warning fw-bold collapse toggle + fas fa-chevron-down + collapse div). Do NOT include it as a partial — its model expects IEnumerable<Quest>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render Description as HTML on Details (desktop + mobile), preserving Rewards pre-wrap</name>
  <files>QuestBoard.Service/Views/Quest/Details.cshtml, QuestBoard.Service/Views/Quest/Details.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Details.cshtml (line 31 Description paragraph with pre-wrap; top-of-file usings)
    - QuestBoard.Service/Views/Quest/Details.Mobile.cshtml (lines ~97-110: Description AND Rewards both in quest-description-mobile)
    - QuestBoard.Service/wwwroot/css/quests.mobile.css (the .quest-description-mobile pre-wrap rule — DO NOT edit it)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md (Pitfall 2 — the shared-class hazard and its resolution)
  </read_first>
  <action>
    Add `@using QuestBoard.Service.Extensions` to both views. In Details.cshtml, replace the entire `<p class="lead" style="white-space: pre-wrap;">@Model.Quest?.Description</p>` with `@Html.Markdown(Model.Quest?.Description)`, leaving no `white-space: pre-wrap` on the Description element (the helper emits its own `.markdown-content` div, styled globally by 66-04).
    In Details.Mobile.cshtml, change ONLY the Description block: render `@Html.Markdown(Model.Quest.Description)` and ensure the Description container does NOT apply `.quest-description-mobile`'s pre-wrap — the simplest correct approach is to drop the `quest-description-mobile` class from the Description wrapper (or remove that wrapper) so the Html.Markdown `.markdown-content` div stands on its own. Do NOT modify quests.mobile.css and do NOT touch the Rewards block — Rewards must keep its `quest-description-mobile` wrapper and pre-wrap until Phase 67.
  </action>
  <verify>
    <automated>grep -q "Html.Markdown" QuestBoard.Service/Views/Quest/Details.cshtml && grep -q "Html.Markdown" QuestBoard.Service/Views/Quest/Details.Mobile.cshtml && ! grep -q "white-space: pre-wrap" QuestBoard.Service/Views/Quest/Details.cshtml && grep -q "quest-description-mobile" QuestBoard.Service/Views/Quest/Details.Mobile.cshtml && git diff --name-only | grep -vq "quests.mobile.css" && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Details.cshtml contains `@Html.Markdown(Model.Quest?.Description)` and NO `white-space: pre-wrap`
    - Details.Mobile.cshtml renders Description via `@Html.Markdown(Model.Quest.Description)`
    - Details.Mobile.cshtml still contains a `quest-description-mobile` occurrence (the Rewards block) and quests.mobile.css is NOT in the changed files
    - Both views declare `@using QuestBoard.Service.Extensions`
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>Description renders as formatted HTML on desktop and mobile Details; Rewards' plain-text pre-wrap display is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add a collapsible Description section to Quest Manage (desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Quest/Manage.cshtml, QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Manage.cshtml (@model Quest; the card-body modern-card-body at line 35 where the section is inserted)
    - QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml (@model Quest; its layout for placing the mobile section)
    - QuestBoard.Service/Views/Quest/_QuestSection.cshtml (the collapse markup STRUCTURE to replicate — NOT to include as a partial)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md (D-02 collapsed-by-default on both breakpoints)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-UI-SPEC.md (Manage section: title "Description", icon fas fa-scroll, chevron fas fa-chevron-down, btn-link text-warning fw-bold)
  </read_first>
  <action>
    Add `@using QuestBoard.Service.Extensions` to both views. In each, insert a NEW collapsible Description section replicating _QuestSection.cshtml's structure (card modern-card, card-header modern-card-header, an h-level heading containing a btn-link text-decoration-none text-warning fw-bold p-0 button with data-bs-toggle="collapse" and data-bs-target to a unique id such as questDescriptionCollapse, aria-expanded false, an icon `fas fa-scroll me-2` before the title text "Description", and a trailing `fas fa-chevron-down ms-2`; then a div with that id and class `collapse` — plain collapse, NOT `collapse show`, so it is collapsed by default per D-02 — wrapping a `card-body modern-card-body` that renders `@Html.Markdown(Model.Description)`). On desktop, place it as a sibling block within the existing content (near the card-body at line 35), not nested as a card-within-a-card body. On mobile, place it in the analogous content area. Do NOT include _QuestSection.cshtml as a partial (its model is incompatible). Use a distinct collapse target id per view if both could ever render together (they do not — mobile/desktop are resolved exclusively).
  </action>
  <verify>
    <automated>grep -q "Html.Markdown(Model.Description)" QuestBoard.Service/Views/Quest/Manage.cshtml && grep -q "Html.Markdown(Model.Description)" QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml && grep -q "fa-scroll" QuestBoard.Service/Views/Quest/Manage.cshtml && grep -q "data-bs-toggle=\"collapse\"" QuestBoard.Service/Views/Quest/Manage.cshtml && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Both Manage views contain a `data-bs-toggle="collapse"` toggle with a `fas fa-chevron-down` chevron and a `fas fa-scroll` header icon, titled "Description"
    - The collapse target div uses class `collapse` (NOT `collapse show`) — collapsed by default
    - Both views render Description via `@Html.Markdown(Model.Description)`
    - Neither view calls `PartialAsync("_QuestSection"...)`
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>Quest Manage shows a collapsed-by-default Description section on desktop and mobile that expands to formatted HTML.</done>
</task>

<task type="auto">
  <name>Task 3: Board card shows Markdown-stripped Description feeding poster selection (Index)</name>
  <files>QuestBoard.Service/Views/Quest/Index.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Index.cshtml (lines 32-59 SelectPosterByContent + line 119 description div; top-of-file model/usings)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md (D-06 board card plain-text; D-07 same stripped length feeds poster selection, thresholds unchanged)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md (Index.cshtml pattern assignment)
  </read_first>
  <action>
    Add `@inject QuestBoard.Domain.Interfaces.IMarkdownService MarkdownService` near the top of Index.cshtml. Inside SelectPosterByContent, change `var description = quest.Description ?? "";` to `var description = MarkdownService.ExtractPlainText(quest.Description);` so the 130/250/500 length buckets measure the SAME Markdown-stripped plain text the card displays (D-07). Do NOT change the 130/250/500 threshold values (deferred). At the description div (line 119), replace `@quest.Description` with `@MarkdownService.ExtractPlainText(quest.Description)` — Razor HTML-encodes the returned plain string, so Markdown syntax is stripped and the card shows unstyled text exactly as today (D-06). Do NOT use Html.Markdown here — the board card is intentionally plain text only, not rendered HTML.
  </action>
  <verify>
    <automated>grep -q "@inject QuestBoard.Domain.Interfaces.IMarkdownService" QuestBoard.Service/Views/Quest/Index.cshtml && grep -c "ExtractPlainText" QuestBoard.Service/Views/Quest/Index.cshtml | grep -qE "[2-9]" && ! grep -q "Html.Markdown" QuestBoard.Service/Views/Quest/Index.cshtml && grep -q "130" QuestBoard.Service/Views/Quest/Index.cshtml && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Index.cshtml injects `IMarkdownService` and calls `ExtractPlainText` in BOTH the poster-length calc and the description div (2+ occurrences)
    - Index.cshtml does NOT call `Html.Markdown` (board card stays plain text)
    - The 130 / 250 / 500 threshold literals are unchanged
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>The desktop board card renders Description as Markdown-stripped plain text, and poster selection measures that same stripped length, keeping today's calibration intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored Quest Description → page HTML | Author Markdown rendered into pages other users view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-13 | Tampering/Info Disclosure (stored XSS) | Details/Manage Html.Markdown output | mitigate | Html.Markdown routes through the Phase-65 web sanitizer; views never emit raw Description. Board card uses ExtractPlainText (also sanitized upstream) and Razor auto-encodes the resulting string |
| T-66-14 | Info Disclosure (unintended raw HTML on board card) | Index description div | mitigate | Board card renders the ExtractPlainText string via Razor `@` (auto HTML-encoded); no `MarkupString`, no Html.Markdown — no author HTML can reach the card DOM |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` succeeds
- Details desktop has no `white-space: pre-wrap`; Details.Mobile keeps `.quest-description-mobile` for Rewards and quests.mobile.css is unchanged
- Manage desktop + mobile have a collapsed-by-default Description section rendering Html.Markdown(Model.Description)
- Index injects IMarkdownService, uses ExtractPlainText for both length and display, keeps 130/250/500, and never calls Html.Markdown
- Visual confirmation deferred to 66-07
</verification>

<success_criteria>
- Quest Description renders as formatted HTML on Details (desktop + mobile) and on a collapsed-by-default Manage section
- Board card shows stripped plain text with poster calibration intact
- Rewards' pre-wrap display untouched
</success_criteria>

<output>
Create `.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-06-SUMMARY.md` when done
</output>
