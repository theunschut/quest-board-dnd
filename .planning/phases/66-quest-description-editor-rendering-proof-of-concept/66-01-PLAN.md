---
phase: 66-quest-description-editor-rendering-proof-of-concept
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Domain/QuestBoard.Domain.csproj
  - QuestBoard.Domain/Interfaces/IMarkdownService.cs
  - QuestBoard.Domain/Services/MarkdownService.cs
  - QuestBoard.UnitTests/Services/MarkdownServiceTests.cs
  - QuestBoard.Service/Extensions/HtmlHelperExtensions.cs
autonomous: true
requirements: [QUESTMD-01, EDITOR-04]
user_setup: []

must_haves:
  truths:
    - "Rendered Quest Description HTML can be reduced to plain text with word boundaries preserved across block elements (heading + paragraph + list do not smash together)"
    - "A Razor view can render sanitized Quest Description HTML via a single Html.Markdown() call without touching the sanitizer or Markdig directly"
  artifacts:
    - path: "QuestBoard.Domain/Interfaces/IMarkdownService.cs"
      provides: "ExtractPlainText(string? markdown) contract"
      contains: "string ExtractPlainText(string? markdown)"
    - path: "QuestBoard.Domain/Services/MarkdownService.cs"
      provides: "AngleSharp-backed plain-text extraction that reuses RenderToHtml"
      contains: "ExtractPlainText"
    - path: "QuestBoard.Service/Extensions/HtmlHelperExtensions.cs"
      provides: "Html.Markdown() read-side adapter resolving IMarkdownService from RequestServices"
      min_lines: 15
    - path: "QuestBoard.Domain/QuestBoard.Domain.csproj"
      provides: "Explicit AngleSharp direct PackageReference"
      contains: "AngleSharp"
  key_links:
    - from: "QuestBoard.Domain/Services/MarkdownService.cs"
      to: "RenderToHtml"
      via: "ExtractPlainText parses the output of RenderToHtml, never re-parses Markdown"
      pattern: "RenderToHtml\\("
    - from: "QuestBoard.Service/Extensions/HtmlHelperExtensions.cs"
      to: "IMarkdownService"
      via: "RequestServices.GetRequiredService<IMarkdownService>()"
      pattern: "GetRequiredService<IMarkdownService>"
---

<objective>
Extend the Phase 65 `IMarkdownService` with a plain-text extraction pass (board-card rendering per D-06/D-07) and add the read-side `Html.Markdown()` MVC adapter that every Quest Description read surface will call.

Purpose: The board card must show Markdown-stripped plain text (D-06), and Details/Manage must render full sanitized HTML through a single helper — both without any second Markdown parser or any view touching the sanitizer directly (RENDER-02 stays intact). This plan builds the server-side read primitives so the read-view plan (66-06) is pure wiring.
Output: `IMarkdownService.ExtractPlainText`, its AngleSharp implementation + unit tests, an explicit AngleSharp direct dependency, and `HtmlHelperExtensions.Markdown()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-CONTEXT.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md

<interfaces>
<!-- Shipped by Phase 65 (65-01-SUMMARY.md). Executor uses these directly — no exploration. -->
From QuestBoard.Domain/Interfaces/IMarkdownService.cs:
- public enum MarkdownRenderTarget { Web, Email }
- string RenderToHtml(string? markdown, MarkdownRenderTarget target = MarkdownRenderTarget.Web)
Registered as AddSingleton<IMarkdownService, MarkdownService>() in QuestBoard.Domain/Extensions/ServiceExtensions.cs.
MarkdownService.RenderToHtml returns string.Empty for null/whitespace, HTML-encodes on Markdig ArgumentException (nesting-depth guard), and applies WebSanitizer (keeps img) or EmailSanitizer (strips img).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ExtractPlainText contract, throwing stub, AngleSharp reference, and failing unit tests (RED)</name>
  <files>QuestBoard.Domain/Interfaces/IMarkdownService.cs, QuestBoard.Domain/Services/MarkdownService.cs, QuestBoard.Domain/QuestBoard.Domain.csproj, QuestBoard.UnitTests/Services/MarkdownServiceTests.cs</files>
  <read_first>
    - QuestBoard.Domain/Interfaces/IMarkdownService.cs (current contract + XML-doc convention to mirror)
    - QuestBoard.Domain/Services/MarkdownService.cs (current RenderToHtml implementation this method sits beside; error-handling convention)
    - QuestBoard.Domain/QuestBoard.Domain.csproj (existing PackageReference ItemGroup — Markdig/HtmlSanitizer already there)
    - QuestBoard.UnitTests/Services/MarkdownServiceTests.cs (existing test-class shape, `private static readonly IMarkdownService Service = new MarkdownService();`, FluentAssertions style, MethodUnderTest_Scenario_ExpectedBehavior naming)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md (Pattern 3 AngleSharp block-iteration approach; Package Legitimacy Audit for AngleSharp)
  </read_first>
  <action>
    Add `string ExtractPlainText(string? markdown);` to `IMarkdownService` with an XML-doc summary explaining WHY (renders the same sanitized HTML used for display, then strips it to plain text for the board card so Markdown syntax never inflates displayed length) — plain language, no phase/requirement IDs per CLAUDE.md.
    Add a temporary throwing stub `public string ExtractPlainText(string? markdown) => throw new NotImplementedException();` in `MarkdownService` so the solution compiles in the RED state.
    Add an explicit direct `<PackageReference Include="AngleSharp" Version="0.17.1" />` to `QuestBoard.Domain.csproj`'s existing package ItemGroup. AngleSharp is pre-approved in 66-RESEARCH.md's Package Legitimacy Audit (already a transitive dependency via HtmlSanitizer; this makes it a direct reference) — no install checkpoint required because it is not tagged [ASSUMED]/[SUS]/[SLOP].
    Add new `ExtractPlainText_*` test methods to `MarkdownServiceTests.cs` following the existing naming/FluentAssertions conventions. Required cases: (1) multi-block word-boundary case — input `"# Heading\n\nSome text.\n\n- item one\n- item two"` must produce plain text containing the substrings `Heading`, `Some text.`, `item one`, `item two` each separated by at least one space (assert the result contains `Heading Some text.` and `item one item two`, NOT `HeadingSome` and NOT `item oneitem two`); (2) null and whitespace input return `string.Empty`; (3) a bold/paragraph input like `"**Bold** text"` returns `Bold text` with no `*` characters and no HTML tags (assert result does not contain `<` and does not contain `*`).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Domain && dotnet test QuestBoard.UnitTests --filter MarkdownServiceTests 2>&1 | grep -Eq "Failed|NotImplementedException"</automated>
  </verify>
  <acceptance_criteria>
    - IMarkdownService.cs contains `string ExtractPlainText(string? markdown)`
    - QuestBoard.Domain.csproj contains `Include="AngleSharp"`
    - MarkdownServiceTests.cs contains at least 3 new methods whose names start with `ExtractPlainText_`
    - `dotnet build` of QuestBoard.Domain succeeds (compiles with the stub)
    - The new ExtractPlainText tests FAIL (RED) — solution compiles but the stub throws
  </acceptance_criteria>
  <done>Contract + stub + AngleSharp reference exist, solution compiles, and the new ExtractPlainText tests fail because the implementation is still a throwing stub.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ExtractPlainText via AngleSharp block iteration (GREEN)</name>
  <files>QuestBoard.Domain/Services/MarkdownService.cs</files>
  <read_first>
    - QuestBoard.Domain/Services/MarkdownService.cs (RenderToHtml implementation to call internally; existing using-directives and class shape)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-RESEARCH.md (Pattern 3 — exact AngleSharp.Html.Parser.HtmlParser usage and the top-level-children-join-with-space gotcha)
    - QuestBoard.UnitTests/Services/MarkdownServiceTests.cs (the failing ExtractPlainText cases this must satisfy)
  </read_first>
  <action>
    Replace the throwing stub with the real implementation. Call `RenderToHtml(markdown, MarkdownRenderTarget.Web)` internally (never call Markdig or a second parser — RENDER-02). If the rendered HTML is null/empty, return `string.Empty`. Parse the HTML fragment with `new AngleSharp.Html.Parser.HtmlParser()` into a document, then join the trimmed `TextContent` of each top-level child element under `document.Body` with a single space, dropping empties. This preserves word boundaries at block-element edges (the critical gotcha: `document.Body.TextContent` alone concatenates `"<h1>Foo</h1><p>Bar</p>"` as `FooBar`, not `Foo Bar`). Use a `using` on the parsed document. No new try/catch needed — RenderToHtml already fails closed on Markdig's nesting-depth guard.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.UnitTests --filter MarkdownServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - MarkdownService.cs `ExtractPlainText` body contains `RenderToHtml(` and `HtmlParser`
    - MarkdownService.cs contains no direct `Markdown.ToHtml(` call inside ExtractPlainText (single-parse architecture preserved)
    - `dotnet test QuestBoard.UnitTests --filter MarkdownServiceTests` exits 0 (all ExtractPlainText cases pass, including the multi-block word-boundary case)
    - Full `dotnet test QuestBoard.UnitTests` shows no regressions in the existing Markdown suite
  </acceptance_criteria>
  <done>ExtractPlainText returns space-separated plain text across block elements; all new and existing MarkdownServiceTests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add Html.Markdown() read-side HtmlHelper adapter</name>
  <files>QuestBoard.Service/Extensions/HtmlHelperExtensions.cs</files>
  <read_first>
    - QuestBoard.Service/Extensions/ControllerExtensions.cs (the "internal static class, one extension method per concern, XML-doc why-summary" convention to mirror exactly)
    - QuestBoard.Domain/Interfaces/IMarkdownService.cs (RenderToHtml signature + MarkdownRenderTarget)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-PATTERNS.md (HtmlHelperExtensions pattern assignment — the concrete target method)
  </read_first>
  <action>
    Create `QuestBoard.Service/Extensions/HtmlHelperExtensions.cs` as `internal static class HtmlHelperExtensions` in namespace `QuestBoard.Service.Extensions`, mirroring `ControllerExtensions.cs`. Add one method: `internal static IHtmlContent Markdown(this IHtmlHelper html, string? markdown)`. It resolves the service via `html.ViewContext.HttpContext.RequestServices.GetRequiredService<IMarkdownService>()`, calls `RenderToHtml(markdown, MarkdownRenderTarget.Web)`, and returns `new HtmlString($"<div class=\"markdown-content\">{rendered}</div>")`. Give the method an XML-doc summary explaining WHY it wraps output in `.markdown-content` (a single styling hook for all rendered Markdown, and the single call site for sanitized display so views never touch the sanitizer) — plain language, no phase/requirement IDs. Do NOT add a MarkdownPlainText helper here; the board card injects IMarkdownService directly (handled in plan 66-06) to compute plain text once for both length and display.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - HtmlHelperExtensions.cs contains `internal static class HtmlHelperExtensions`
    - File contains `GetRequiredService<IMarkdownService>()` and `MarkdownRenderTarget.Web`
    - File contains the literal `class=\"markdown-content\"` wrapper string
    - `dotnet build QuestBoard.Service` succeeds
  </acceptance_criteria>
  <done>`@Html.Markdown(...)` is available to Razor views and returns sanitized HTML wrapped in a `.markdown-content` div, resolving IMarkdownService per-request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user Markdown → server render | Quest Description text (author-supplied) is parsed and rendered to HTML that other users' browsers execute as DOM |
| NuGet supply chain | AngleSharp becomes a direct dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering/Info Disclosure | ExtractPlainText / Html.Markdown | mitigate | Both call the Phase-65 `RenderToHtml` (already sanitized via HtmlSanitizer); no code path bypasses the sanitizer or calls Markdig directly — asserted by the "no `Markdown.ToHtml(` in ExtractPlainText" acceptance criterion |
| T-66-02 | Denial of Service | ExtractPlainText on very large input | accept | Inherits Markdig's nesting-depth guard via RenderToHtml (fails closed to HTML-encoded text). Raw payload-size cap remains an open discretion item carried from Phase 65; no size cap added here (no locked requirement) |
| T-66-SC | Tampering | AngleSharp 0.17.1 NuGet direct reference | mitigate | Pre-approved in 66-RESEARCH.md Package Legitimacy Audit — already resolved and running as a transitive dependency via the already-audited HtmlSanitizer; direct reference pins the version explicitly. Not [ASSUMED]/[SUS]/[SLOP], so no blocking checkpoint |
</threat_model>

<verification>
- `dotnet build` succeeds across the solution
- `dotnet test QuestBoard.UnitTests --filter MarkdownServiceTests` exits 0
- `grep -n "Markdown.ToHtml(" QuestBoard.Domain/Services/MarkdownService.cs` still shows exactly one occurrence (inside RenderToHtml only), confirming ExtractPlainText did not introduce a second parse
</verification>

<success_criteria>
- ExtractPlainText preserves word boundaries across block elements (unit-tested)
- Html.Markdown() available to views, wraps sanitized HTML in `.markdown-content`
- AngleSharp is an explicit direct dependency of QuestBoard.Domain
- No second Markdown parser introduced anywhere
</success_criteria>

<output>
Create `.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-01-SUMMARY.md` when done
</output>
