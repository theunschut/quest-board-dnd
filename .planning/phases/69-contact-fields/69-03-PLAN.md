---
phase: 69-contact-fields
plan: 03
type: execute
wave: 3
depends_on: [69-02]
files_modified: []
autonomous: false
requirements: [CONTACTMD-01, CONTACTMD-02]
user_setup: []
must_haves:
  truths:
    - "The solution builds and ContactsControllerIntegrationTests render Create/Edit/Details end-to-end, proving both Details views resolve the new cross-folder ~/Views/Quest/_QuestFormScripts.cshtml include at runtime"
    - "An operator has visually confirmed Contact Description and Notes author + render correctly on desktop and 320px mobile"
    - "Per-note independent rendering is confirmed with two notes containing conflicting/unclosed formatting"
    - "D-03 auto-collapse with unsaved-text discard is confirmed by direct interaction"
  artifacts:
    - "Operator sign-off recorded in 69-03-SUMMARY.md"
  key_links:
    - "Razor partial resolution for the cross-folder _QuestFormScripts include is a RUNTIME check a plain build misses — ContactsControllerIntegrationTests render the views and surface any partial-not-found regression"
    - "Manual UAT is the acceptance gate for CONTACTMD-01/02, whose behaviors are view-rendering + client interaction with no automated coverage (matching the Phase 68 precedent)"
---

<objective>
Verification checkpoint for Phase 69. All code lands in plans 69-01 and 69-02; this plan first runs the full automated gate — including `ContactsControllerIntegrationTests`, which renders the Contact Create/Edit/Details views end-to-end — then pauses for a human to confirm the live write→read loop for Description and Notes across desktop and a true 320px mobile viewport.

Purpose: CONTACTMD-01/02 are behavior-shaped and view-heavy, with no automated coverage for the rendered result (per 69-RESEARCH.md's Test Map — same as the Phase 68 precedent). The build proves the wiring compiles; the integration tests prove the views still resolve at RUNTIME — critical this phase because both Contact Details views now include `~/Views/Quest/_QuestFormScripts.cshtml` for the first time (Razor partial resolution is a runtime check; a wrong path builds clean but throws when the view renders — exactly the class of bug the Phase 67-05 gate caught). The visual result (formatted output, per-note isolation, D-03 exclusivity, mobile toolbar fit, list contrast) then needs a human eye.
Output: Recorded operator approval, or a concrete defect list to route into a gap plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-contact-fields/69-RESEARCH.md
@.planning/phases/69-contact-fields/69-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
No source symbols. Produces the operator sign-off record in 69-03-SUMMARY.md. All feature code was produced by 69-01 and 69-02.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate — build the solution + render Contact views via integration tests</name>
  <files></files>
  <read_first>
    - .planning/phases/69-contact-fields/69-01-SUMMARY.md
    - .planning/phases/69-contact-fields/69-02-SUMMARY.md
    - .planning/phases/68-character-fields/68-03-PLAN.md (the analog automated-gate task; the 67-05 runtime partial-resolution lesson that makes rendering the views — not just building — the point)
  </read_first>
  <action>
    Confirm both Wave-1/Wave-2 plans landed and the solution is green before asking the operator to look at anything. Build the whole solution (`QuestBoard.slnx`), then run `ContactsControllerIntegrationTests` — this renders the Contact Create, Edit, and Details views end-to-end and will throw if either Details view's newly-added `~/Views/Quest/_QuestFormScripts.cshtml` include fails to resolve at runtime, or if the restructured Description/Notes markup breaks rendering. Also run `MarkdownServiceTests` to confirm the shared sanitizing pipeline is unregressed.

    If the build fails due to locked output files, the app is likely running under the Visual Studio debugger — ask the operator to stop it (Shift+F5) and retry, per CLAUDE.md.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx -c Debug && dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~ContactsControllerIntegrationTests" && dotnet test QuestBoard.UnitTests/QuestBoard.UnitTests.csproj --filter "FullyQualifiedName~MarkdownServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.slnx` succeeds.
    - All `ContactsControllerIntegrationTests` pass (proves Create/Edit/Details render at runtime with the new cross-folder `_QuestFormScripts` include and the restructured Description/Notes markup — no partial-not-found or Razor render exception, and AddNote/EditNote model binding + authorization unregressed).
    - `MarkdownServiceTests` passes (shared sanitizing pipeline unregressed).
  </acceptance_criteria>
  <done>Solution builds; Contact view rendering is proven by a green integration suite; markdown unit tests pass; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies the live Description + Notes loop (desktop + 320px mobile)</name>
  <files></files>
  <read_first>
    - .planning/phases/69-contact-fields/69-UI-SPEC.md (Spacing note: re-confirm the 320px one-row toolbar for Description AND a Note editor nested inside `.contact-detail-card .note-item`; Typography/Color: the mobile list `li` edits and the editor/Preview text-color fix to confirm live)
    - .planning/phases/69-contact-fields/69-RESEARCH.md (Pitfalls 1-3 verify-only findings; Open Question 2 mobile list contrast; Assumption A1 first-open full-width editor)
  </read_first>
  <action>
    Start the app, then present the numbered verification steps below to the operator and PAUSE for their confirmation. This task changes no code; it gates Phase 69 closure on a human confirming the rendered result at both desktop and a true 320px mobile viewport. If the operator reports a scoped, operator-directed CSS/markup issue, it may be fixed in-session (mirroring 66-07 / 67-05 / 68-03); anything larger is routed to a gap-closure pass.
  </action>
  <what-built>
    Plans 69-01/69-02 wired the shared Markdown editor into Contact Description (Create/Edit, desktop + mobile) and Contact Notes (Add Note + per-note inline Edit on the Details page), rendered Description and every note as sanitized formatted HTML on Contact Details (desktop + mobile), added D-03 single-editor exclusivity with unsaved-text discard and per-note lazy-init, and added two mobile CSS list edits. Index views were intentionally left unchanged (D-01).
  </what-built>
  <how-to-verify>
    Run the app (`dotnet run --project QuestBoard.Service`; if a build lock error appears, ask the operator to stop the Visual Studio debugger first). The app uses server-side User-Agent detection — use a real mobile UA or device emulation that changes the UA (not just a resized window) so the `.Mobile.cshtml` views actually load. Sign in as a group member who can view a Contact, then verify:

    Description authoring + render (desktop):
    1. Open a Contact's Edit form. Confirm Description shows the toolbar (Bold, Italic, Heading, List, Link, Blockquote) + Preview and an info tooltip near the label, with no doubled button row. (If it is still a plain grey textarea, the `_QuestFormScripts` include did not take — a hard failure.)
    2. Type mixed Markdown: a heading, a bullet list, a bold word, a blockquote, two paragraphs separated by a blank line. Toggle Preview — rendered HTML matches, and the rest of the toolbar disables while previewing. Save.
    3. On Contact Details, Description renders formatted; a single Enter does NOT start a new paragraph (only a blank line does); there is no doubled vertical spacing.

    Notes authoring + independent render (desktop):
    4. In the Add Note editor at the top of the Notes card, confirm the full toolbar + Preview shows immediately (D-02). Add a note ending with an UNCLOSED bold marker (text then `**bold` with no closing). Add a second, separate note with normal formatting.
    5. Each note renders independently — the first note's unclosed formatting does NOT bleed into the second note or the rest of the page (CONTACTMD-02).
    6. D-03: click Edit on note A (editor opens, toolbar full width immediately — no zero-width/broken editor). Type unsaved text into A. Click Edit on note B. Confirm A auto-collapses AND A's unsaved text is discarded (reopen A: it shows the last SAVED text). Only one note editor is open at once; Cancel also collapses and discards.

    Mobile (true 320px viewport):
    7. Repeat the Description toolbar check on the mobile Create/Edit form (`.contact-form-card`) and a Note's inline editor on mobile Details (`.contact-detail-card .note-item`). Each toolbar fits ONE row with no horizontal scroll at 320px. (~30-34px-wide buttons are the accepted Phase-66 deviation, not a defect.)
    8. In the mobile Description and a Note editor, type bold/heading syntax and confirm the CodeMirror editing surface AND Preview pane render black-on-white (NOT gold-tinted) — Pitfall 1 regression check.
    9. A Markdown list inside a Description and inside a Note renders with readable contrast on mobile (the two `li` CSS edits / Open Question 2). Heading text on the mobile glass detail card is legible.

    Security + D-01:
    10. Type a `<script>`-style payload into a note and save. Confirm it renders inert (escaped or stripped), NOT executed (stored-XSS sanitizer spot-check).
    11. Confirm the Contacts Index (desktop grid + mobile list) still shows only Name/Town-City/Sub-location — no Description preview was added to any card (D-01).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms steps 1-11, or reports specific failing step numbers with observed behavior.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms steps 1-11 above, OR reports specific issues.
    - Any issue the operator raises is captured for a gap-closure pass; a scoped, operator-directed "fix now" CSS/markup issue may be corrected in-session (mirroring 66-07 / 67-05 / 68-03).
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 69, or list the failing step numbers with what you observed so the issues can be routed into a gap-closure plan.</resume-signal>
  <done>Operator has signed off on the live Description + Notes loop across desktop and 320px mobile, or issues are captured for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification-only) | This plan writes no code and adds no new trust boundary; it verifies the mitigations delivered by 69-01/69-02 hold in a running app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-08 | Tampering | Stored XSS render path (verification) | high | mitigate | Step 10 exercises the `Html.Markdown()` → `IMarkdownService.RenderToHtml(..., Web)` sanitizer with a `<script>`-style payload typed into a note and confirms it renders inert, not executed — a live confirmation of the T-69-04 mitigation. |
| T-69-SC | Tampering | npm/NuGet installs | low | accept | No packages installed by this plan — verification only. |
</threat_model>

<verification>
- Automated gate (Task 1) green: solution builds, all `ContactsControllerIntegrationTests` pass (runtime view rendering incl. the cross-folder partial include + AddNote/EditNote binding), `MarkdownServiceTests` passes.
- Operator sign-off (Task 2) on the live Description + Notes loop across desktop and 320px mobile, including the XSS spot-check and D-01 confirmation.
- Any failure captured with step number + observed behavior for gap routing.
</verification>

<success_criteria>
- CONTACTMD-01 and CONTACTMD-02 observably satisfied: editor on Description write forms + Add/Edit note; formatted HTML on Contact Details desktop + mobile; per-note independent rendering; strict-paragraph spacing.
- D-03 exclusivity + unsaved-text discard confirmed; per-note editor renders full-width on first open.
- Mobile toolbar fits one row at 320px; editor/preview text not gold-tinted; list contrast readable.
- Runtime resolution of the cross-folder `_QuestFormScripts` include proven by the integration suite (the 67-05 lesson is not repeated).
- Index unchanged (D-01); stored-XSS payload renders inert. Operator records "approved" or a defect list.
</success_criteria>

<output>
Create `.planning/phases/69-contact-fields/69-03-SUMMARY.md` when done (record the operator's approval or the defect list).
</output>
