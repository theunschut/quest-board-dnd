---
phase: 26-namespace-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "EuphoriaInn.slnx -> QuestBoard.slnx"
  - "EuphoriaInn.Domain/ -> QuestBoard.Domain/ (directory + .csproj)"
  - "EuphoriaInn.Repository/ -> QuestBoard.Repository/ (directory + .csproj)"
  - "EuphoriaInn.Service/ -> QuestBoard.Service/ (directory + .csproj)"
  - "EuphoriaInn.UnitTests/ -> QuestBoard.UnitTests/ (directory + .csproj)"
  - "EuphoriaInn.IntegrationTests/ -> QuestBoard.IntegrationTests/ (directory + .csproj)"
  - "All ~201 non-migration .cs/.cshtml/.razor source files (content patch)"
  - "QuestBoard.Repository/Migrations/ — 53 migration files (.cs + .Designer.cs + ModelSnapshot)"
  - "Dockerfile, .github/workflows/binary-release.yml"
  - "CLAUDE.md, README.md, docs/server-setup.md"
autonomous: true
requirements: [RENAME-01, RENAME-02, RENAME-03, RENAME-04]

must_haves:
  truths:
    - "Five top-level project directories are named QuestBoard.Domain, QuestBoard.Repository, QuestBoard.Service, QuestBoard.UnitTests, QuestBoard.IntegrationTests"
    - "The solution file is QuestBoard.slnx and references all five renamed project paths"
    - "No tracked source file (.cs/.cshtml/.razor/.csproj/.slnx) outside .planning/ contains the token EuphoriaInn"
    - "dotnet build QuestBoard.slnx --no-incremental succeeds with zero errors"
  artifacts:
    - path: "QuestBoard.slnx"
      provides: "Solution file referencing five QuestBoard.* project paths"
      contains: "QuestBoard.Service/QuestBoard.Service.csproj"
    - path: "QuestBoard.Service/QuestBoard.Service.csproj"
      provides: "Web app project file with QuestBoard ProjectReference paths"
      contains: "QuestBoard.Domain"
    - path: "QuestBoard.Domain/Properties/AssemblyInfo.cs"
      provides: "InternalsVisibleTo attribute granting QuestBoard.UnitTests access"
      contains: "InternalsVisibleTo(\"QuestBoard.UnitTests\")"
    - path: "QuestBoard.Repository/Migrations/QuestBoardContextModelSnapshot.cs"
      provides: "EF model snapshot with QuestBoard FQN entity strings"
      contains: "QuestBoard.Repository.Entities"
  key_links:
    - from: "QuestBoard.slnx"
      to: "QuestBoard.Service/QuestBoard.Service.csproj"
      via: "Project Path element pointing to renamed directory"
      pattern: "Project Path=\"QuestBoard\\.Service/QuestBoard\\.Service\\.csproj\""
    - from: "QuestBoard.Service/QuestBoard.Service.csproj"
      to: "QuestBoard.Domain/QuestBoard.Domain.csproj"
      via: "ProjectReference Include relative path"
      pattern: "ProjectReference Include=\".*QuestBoard\\.Domain\\\\QuestBoard\\.Domain\\.csproj\""
---

<objective>
Rename the five top-level project directories, the solution file, and all `.csproj` files from `EuphoriaInn.*` to `QuestBoard.*` using `git mv` (history-preserving), then bulk-replace the `EuphoriaInn` token to `QuestBoard` across all tracked source, config, CI, and documentation files. This implements RENAME-01 (namespaces), RENAME-02 (project/solution/directory names), RENAME-03 (config/CI/scripts/docs per D-02), and RENAME-04 (migration files patched in-place per D-01). The plan ends at a green `dotnet build`; the full test gate and behavior-change verification live in plan 02.

Purpose: Establish the QuestBoard.* naming foundation for all of v5.0 Multi-Tenancy with zero behavior change. This is the first phase — every subsequent phase builds on these directory/namespace names.
Output: Five renamed directories, renamed solution + project files, all 343 tracked files patched, a clean `dotnet build`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-namespace-rename/26-CONTEXT.md
@.planning/phases/26-namespace-rename/26-RESEARCH.md
@.planning/phases/26-namespace-rename/26-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase RENAMES existing symbols — every identifier below is newly-introduced by this phase and must be excluded from any drift/source-grounding verification (the old `EuphoriaInn.*` form will no longer exist):

**Renamed directories (5):**
- `QuestBoard.Domain/` (was `EuphoriaInn.Domain/`)
- `QuestBoard.Repository/` (was `EuphoriaInn.Repository/`)
- `QuestBoard.Service/` (was `EuphoriaInn.Service/`)
- `QuestBoard.UnitTests/` (was `EuphoriaInn.UnitTests/`)
- `QuestBoard.IntegrationTests/` (was `EuphoriaInn.IntegrationTests/`)

**Renamed project/solution files (6):**
- `QuestBoard.slnx` (was `EuphoriaInn.slnx`)
- `QuestBoard.Domain/QuestBoard.Domain.csproj`
- `QuestBoard.Repository/QuestBoard.Repository.csproj`
- `QuestBoard.Service/QuestBoard.Service.csproj`
- `QuestBoard.UnitTests/QuestBoard.UnitTests.csproj`
- `QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj`

**Renamed root namespace prefix:** `QuestBoard.*` (was `EuphoriaInn.*`) — applied to all `namespace` declarations, `using` directives, `@using` Razor directives, FQN type references, EF Designer.cs entity FQN strings, the `InternalsVisibleTo` assembly attribute, and integration-test directory-path string literals.

**Renamed produced DLL/assembly name:** `QuestBoard.Service.dll` (was `EuphoriaInn.Service.dll`), and per-project assembly names `QuestBoard.Domain.dll`, `QuestBoard.Repository.dll`, etc.

**Unchanged (NOT renamed):** the literal string value `"EuphoriaInn"` as the group name in seed data / DB (none currently exists in migrations per D-04 verification); inner subfolder namespace segments (e.g. `Models.QuestBoard`, `Controllers.QuestBoard`) keep their `QuestBoard` segment; the systemd service name `questboard` on the production server; the `__EFMigrationsHistory` migration timestamp names.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rename directories, solution, and project files via git mv</name>
  <files>EuphoriaInn.Domain/, EuphoriaInn.Repository/, EuphoriaInn.Service/, EuphoriaInn.UnitTests/, EuphoriaInn.IntegrationTests/, EuphoriaInn.slnx, and the 5 .csproj files inside the renamed directories</files>
  <read_first>
    - .planning/phases/26-namespace-rename/26-PATTERNS.md (Wave 1 section — exact git mv command list)
    - .planning/phases/26-namespace-rename/26-RESEARCH.md (Rename Execution Order — steps 1-4 are non-negotiable sequence)
    - .planning/phases/26-namespace-rename/26-CONTEXT.md (D-05 — single atomic commit; do NOT commit mid-task)
  </read_first>
  <action>
    Run in Git Bash from the repo root, sequentially (the order is non-negotiable per RESEARCH execution order — directories before content patch). First rename the five top-level directories: `git mv EuphoriaInn.Domain QuestBoard.Domain`, `git mv EuphoriaInn.Repository QuestBoard.Repository`, `git mv EuphoriaInn.Service QuestBoard.Service`, `git mv EuphoriaInn.UnitTests QuestBoard.UnitTests`, `git mv EuphoriaInn.IntegrationTests QuestBoard.IntegrationTests`. Then rename the solution file: `git mv EuphoriaInn.slnx QuestBoard.slnx`. Then rename the five .csproj files inside their now-renamed directories: `git mv QuestBoard.Domain/EuphoriaInn.Domain.csproj QuestBoard.Domain/QuestBoard.Domain.csproj` and the equivalent for Repository, Service, UnitTests, IntegrationTests. Use `git mv` exclusively — PowerShell Rename-Item does NOT preserve git history. Do NOT patch any file contents in this task and do NOT commit — file-content references will be broken until Task 2 runs; that is expected and the single atomic commit (D-05) happens in plan 02 after the build gate.
  </action>
  <verify>
    <automated>test -d QuestBoard.Domain && test -d QuestBoard.Repository && test -d QuestBoard.Service && test -d QuestBoard.UnitTests && test -d QuestBoard.IntegrationTests && test -f QuestBoard.slnx && test -f QuestBoard.Service/QuestBoard.Service.csproj && test -f QuestBoard.Domain/QuestBoard.Domain.csproj && ! test -d EuphoriaInn.Service && echo RENAMED_OK</automated>
  </verify>
  <acceptance_criteria>
    - All five directories `QuestBoard.Domain`, `QuestBoard.Repository`, `QuestBoard.Service`, `QuestBoard.UnitTests`, `QuestBoard.IntegrationTests` exist; none of the `EuphoriaInn.*` directories remain
    - `QuestBoard.slnx` exists; `EuphoriaInn.slnx` does not
    - Each renamed directory contains its matching `QuestBoard.*.csproj` and no `EuphoriaInn.*.csproj`
    - `git status --short` shows the renames staged as `R` (renames), confirming history preservation, not delete+add
    - The verify command prints `RENAMED_OK`
  </acceptance_criteria>
  <done>Five directories, the solution file, and five .csproj files are renamed to QuestBoard.* via git mv with history preserved; no EuphoriaInn.* path remains on the filesystem.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk-replace EuphoriaInn token to QuestBoard across all tracked files</name>
  <files>All tracked files containing EuphoriaInn (343 files): QuestBoard.slnx, all .csproj, all .cs/.cshtml/.razor source, QuestBoard.Repository/Migrations/ (53 files incl. QuestBoardContextModelSnapshot.cs), QuestBoard.Domain/Properties/AssemblyInfo.cs, Dockerfile, .github/workflows/binary-release.yml, CLAUDE.md, README.md, docs/server-setup.md</files>
  <read_first>
    - .planning/phases/26-namespace-rename/26-PATTERNS.md (Wave 2 + Shared Patterns sections — every replace-target form: namespace decls, using directives, @using, FQN type refs, EF Designer.cs FQN strings, InternalsVisibleTo, ProjectReference paths, .slnx Project Path, Dockerfile COPY/ENTRYPOINT, CI publish path, integration-test path string literals, docs)
    - .planning/phases/26-namespace-rename/26-CONTEXT.md (D-04 — protect literal "EuphoriaInn" group-name string values; D-03 — Razor display text and .planning/ docs are OUT of scope)
    - .planning/phases/26-namespace-rename/26-RESEARCH.md (Safe Regex for Content Replace; Anti-Patterns to Avoid — do NOT line-filter, use whole-file replace; do NOT touch obj/bin; do NOT run dotnet ef migrations to regenerate)
    - QuestBoard.slnx (current content — five EuphoriaInn.* Project Path entries to replace)
    - QuestBoard.Domain/Properties/AssemblyInfo.cs (InternalsVisibleTo("EuphoriaInn.UnitTests") to replace)
  </read_first>
  <action>
    Run in Git Bash from the repo root. Replace every occurrence of the token `EuphoriaInn` with `QuestBoard` across all git-tracked files except those under `.planning/`. Use a tracked-files-only pipeline so build artifacts under `obj/`, `bin/`, `.vs/`, and `.git/` are never touched (they regenerate on build per RESEARCH anti-patterns): list tracked files with `git ls-files`, exclude `.planning` paths, narrow to files actually containing the token, and apply an in-place `sed` substitution `s/EuphoriaInn/QuestBoard/g`. Use `xargs -d '\n'` to handle any filenames with spaces correctly. This single whole-file token replacement covers all forms simultaneously: `namespace QuestBoard.X` declarations, `using QuestBoard.X` and `@using QuestBoard.X` directives, inline FQN type references in .cshtml (e.g. `List<QuestBoard.Domain.Models.Character>`), EF Designer.cs entity FQN strings (e.g. `modelBuilder.Entity("QuestBoard.Repository.Entities.QuestEntity")`), the `InternalsVisibleTo("QuestBoard.UnitTests")` assembly attribute, all `<ProjectReference>` relative paths, the `QuestBoard.slnx` Project Path entries, Dockerfile COPY/restore/build/publish paths and `ENTRYPOINT ["dotnet", "QuestBoard.Service.dll"]`, the `binary-release.yml` publish path `QuestBoard.Service/QuestBoard.Service.csproj`, the integration-test directory-path string literals in MobileCssTests.cs and QuestFinalizeTests.cs, and the docs (CLAUDE.md dev-command paths, README.md run command, docs/server-setup.md systemd ExecStart DLL name) — satisfying RENAME-01, RENAME-03, RENAME-04 in one pass and the D-02 in-scope items. Do NOT manually edit the migration timestamp attribute strings such as `[Migration("20250629200240_InitialSqlServerNoAction")]` — the token `EuphoriaInn` does not appear in them, so the replace leaves them correct. After replacing, verify D-04 safety: confirm no `InsertData`/`HasData` call ever held `"EuphoriaInn"` as a value (research A3 confirmed none exists, so no protected string was harmed). If on PowerShell instead of Git Bash, use the documented Get-ChildItem `-replace` loop from 26-PATTERNS.md Wave 2 alternative, but Git Bash `sed` is the recommended path because `git ls-files` cleanly excludes obj/bin.
  </action>
  <verify>
    <automated>test -z "$(git ls-files | grep -v '\.planning' | xargs grep -l 'EuphoriaInn' 2>/dev/null)" && grep -q 'InternalsVisibleTo("QuestBoard.UnitTests")' QuestBoard.Domain/Properties/AssemblyInfo.cs && grep -q 'QuestBoard.Repository.Entities' QuestBoard.Repository/Migrations/QuestBoardContextModelSnapshot.cs && echo REPLACE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep -v '\.planning' | xargs grep -l 'EuphoriaInn'` returns empty (zero tracked non-planning files contain the token) — satisfies RENAME-01, RENAME-03
    - `QuestBoard.Domain/Properties/AssemblyInfo.cs` contains the exact string `InternalsVisibleTo("QuestBoard.UnitTests")` and no `EuphoriaInn` — prevents the Pitfall 2 accessibility break
    - `QuestBoard.Repository/Migrations/QuestBoardContextModelSnapshot.cs` contains `QuestBoard.Repository.Entities` FQN strings and zero `EuphoriaInn` occurrences (was 65 before) — satisfies RENAME-04, prevents Pitfall 1 entity-not-found
    - `QuestBoard.slnx` Project Path entries all point to `QuestBoard.*/QuestBoard.*.csproj`
    - `.github/workflows/binary-release.yml` publish step references `QuestBoard.Service/QuestBoard.Service.csproj`; `Dockerfile` ENTRYPOINT references `QuestBoard.Service.dll`
    - `docs/server-setup.md` systemd ExecStart line references `QuestBoard.Service.dll`
    - Migration timestamp attribute strings (e.g. `20250629200240_InitialSqlServerNoAction`) are unchanged
    - No file under `obj/`, `bin/`, `.vs/`, or `.git/` was manually edited (only git-tracked files were touched)
    - The verify command prints `REPLACE_OK`
  </acceptance_criteria>
  <done>Every tracked non-planning file has EuphoriaInn replaced by QuestBoard; assembly attribute, EF snapshot FQNs, CI/Dockerfile/docs paths are all patched; migration timestamps and any protected string values are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build gate — dotnet build QuestBoard.slnx must succeed</name>
  <files>QuestBoard.slnx (build target — no edits; this task verifies the rename compiles)</files>
  <read_first>
    - .planning/phases/26-namespace-rename/26-RESEARCH.md (Verified dotnet build gate command; Don't Hand-Roll — compiler reports every broken reference)
    - .planning/phases/26-namespace-rename/26-PATTERNS.md (Post-Replace Verification Commands — build gate step 3)
  </read_first>
  <action>
    Run `dotnet build QuestBoard.slnx --no-incremental` from the repo root. The `--no-incremental` flag forces a clean rebuild so any stale `EuphoriaInn`-named references from the pre-rename build cache are discarded and cannot mask a broken reference. If the build reports errors, the compiler output names the exact file and line of each unresolved reference — fix those by completing any missed token replacement in that specific file (the most likely cause is a file that was not git-tracked at replace time, or a non-default file extension the replace pipeline skipped), then re-run the build until it exits 0. Do NOT proceed to plan 02 until the build is green. Do NOT commit yet — the single atomic commit (D-05) happens in plan 02 after the full test suite passes.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx --no-incremental</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.slnx --no-incremental` exits with code 0 — satisfies RENAME-02
    - Build output shows all five projects (QuestBoard.Domain, QuestBoard.Repository, QuestBoard.Service, QuestBoard.UnitTests, QuestBoard.IntegrationTests) built successfully
    - Zero compiler errors and zero unresolved-reference errors
  </acceptance_criteria>
  <done>dotnet build QuestBoard.slnx --no-incremental succeeds with zero errors across all five renamed projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → git repository | Bulk text replacement could corrupt source files or accidentally rewrite a protected literal string value |
| renamed source → production deployment | The produced DLL name changes (QuestBoard.Service.dll), affecting the systemd unit on the live server (handled in plan 02) |

## STRIDE Threat Register

ASVS Level 1. This is a pure mechanical rename introducing no new authentication, authorization, input handling, network surface, or cryptography. No "high" severity threats exist; the only realistic risks are tampering/integrity of the replace operation.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | Bulk sed replace across 343 files | mitigate | Restrict to `git ls-files` (tracked files only) so obj/bin/.git are never touched; D-04 verified no `"EuphoriaInn"` data-value string exists to corrupt; final grep + build gate prove integrity |
| T-26-02 | Denial of Service | EF migration Designer.cs / ModelSnapshot FQN strings | mitigate | Patch in-place (D-01), never regenerate; verify zero `EuphoriaInn` remains in Migrations/ so EF runtime entity resolution does not throw entity-not-found (Pitfall 1) |
| T-26-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH confirms zero NuGet/npm packages added; Package Legitimacy Audit not applicable |
</threat_model>

<verification>
After all three tasks:
1. `git ls-files | grep -v '\.planning' | xargs grep -l 'EuphoriaInn'` returns empty (RENAME-01, RENAME-03)
2. `grep -r "EuphoriaInn" QuestBoard.Repository/Migrations/` returns empty (RENAME-04, Pitfall 1)
3. `dotnet build QuestBoard.slnx --no-incremental` exits 0 (RENAME-02)
4. `git status --short` shows directory/file renames as `R` entries (history preserved)
5. No commit yet — plan 02 owns the single atomic commit (D-05) after the full test gate
</verification>

<success_criteria>
- All five project directories, the solution file, and all five .csproj files are renamed to QuestBoard.* with git history preserved
- Zero `EuphoriaInn` token remains in any tracked non-planning file
- EF migration files (53) including the ModelSnapshot have QuestBoard.* FQN strings and zero EuphoriaInn
- `dotnet build QuestBoard.slnx --no-incremental` succeeds
- No protected literal string value or migration timestamp was altered
</success_criteria>

<output>
Create `.planning/phases/26-namespace-rename/26-01-SUMMARY.md` when done. Record: directories renamed, file count patched, build result, and an explicit note that NO commit was made (plan 02 owns the atomic commit per D-05).
</output>
