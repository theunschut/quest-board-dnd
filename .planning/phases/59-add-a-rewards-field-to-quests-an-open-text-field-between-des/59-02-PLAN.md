---
phase: 59-add-a-rewards-field-to-quests-an-open-text-field-between-des
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - QuestBoard.Service/Views/Quest/Create.cshtml
  - QuestBoard.Service/Views/Quest/Create.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Edit.cshtml
  - QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml
  - QuestBoard.Service/Views/Quest/Details.cshtml
  - QuestBoard.Service/Views/Quest/Details.Mobile.cshtml
  - QuestBoard.Service/Views/QuestLog/Details.cshtml
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "The Create and Edit forms (desktop + mobile) show a Rewards textarea between Description and Challenge Rating"
    - "The Follow-Up Quest form shows a Rewards textarea (blank), between Description and Challenge Rating"
    - "The Quest Details page (desktop + mobile) shows a Rewards block below the Description when Rewards has a value, and hides it entirely when Rewards is empty"
    - "The completed-quest QuestLog Details page shows a read-only Rewards box near the Original Quest Description when Rewards has a value"
    - "The quest board list card (_QuestCard) shows no Rewards indicator"
    - "Rewards shows for both OneShot and Campaign board types (not gated by the Campaign conditional)"
  artifacts:
    - path: "QuestBoard.Service/Views/Quest/Create.cshtml"
      provides: "Rewards form field (asp-for=Rewards) between Description and the Campaign conditional"
      contains: "asp-for=\"Rewards\""
    - path: "QuestBoard.Service/Views/Quest/Edit.cshtml"
      provides: "Rewards form field (asp-for=Quest.Rewards)"
      contains: "asp-for=\"Quest.Rewards\""
    - path: "QuestBoard.Service/Views/Quest/Details.cshtml"
      provides: "Rewards display block (hidden when empty) below Description"
      contains: "Model.Quest?.Rewards"
    - path: "QuestBoard.Service/Views/QuestLog/Details.cshtml"
      provides: "Read-only Rewards box near Original Quest Description"
      contains: "Model.Quest.Rewards"
  key_links:
    - from: "QuestBoard.Service/Views/Quest/Details.cshtml"
      to: "QuestBoard.Domain/Models/QuestBoard/Quest.cs"
      via: "renders Model.Quest.Rewards inside a .quest-description-box, gated by IsNullOrWhiteSpace"
      pattern: "Model.Quest\\??.Rewards"
    - from: "QuestBoard.Service/Views/Quest/Create.cshtml"
      to: "QuestBoard.Service/ViewModels/QuestViewModels/QuestViewModel.cs"
      via: "asp-for=Rewards binds the textarea to QuestViewModel.Rewards"
      pattern: "asp-for=\"Rewards\""
---

<objective>
Add the Rewards field and display block to all 8 touched Razor views, exactly mirroring the existing Description field/box patterns per the UI-SPEC: a textarea on Create/Edit/Follow-Up forms (desktop + mobile) between Description and Challenge Rating, and a boxed callout (hidden when empty) below the Description on the two Details pages and near the Original Quest Description on the QuestLog Details page. The quest board list card is deliberately left untouched.

Purpose: Surface the Rewards field to DMs (input) and all users (display), completing the feature end-to-end. Depends on Plan 59-01 which added `QuestViewModel.Rewards` / `Quest.Rewards` that these views bind to and render.
Output: 8 modified view files that compile and render the Rewards field/block per the UI Design Contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-add-a-rewards-field-to-quests-an-open-text-field-between-des/59-CONTEXT.md
@.planning/phases/59-add-a-rewards-field-to-quests-an-open-text-field-between-des/59-UI-SPEC.md
@QuestBoard.Service/Views/Quest/Create.cshtml
@QuestBoard.Service/Views/Quest/Create.Mobile.cshtml
@QuestBoard.Service/Views/Quest/Edit.cshtml
@QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml
@QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml
@QuestBoard.Service/Views/Quest/Details.cshtml
@QuestBoard.Service/Views/Quest/Details.Mobile.cshtml
@QuestBoard.Service/Views/QuestLog/Details.cshtml
@QuestBoard.Service/wwwroot/css/quests.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Rewards form field to the five form views (Create/Edit/CreateFollowUp, desktop + mobile)</name>
  <files>QuestBoard.Service/Views/Quest/Create.cshtml, QuestBoard.Service/Views/Quest/Create.Mobile.cshtml, QuestBoard.Service/Views/Quest/Edit.cshtml, QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml, QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Create.cshtml — Description field block at lines 28-32; the `@if (boardType != BoardType.Campaign)` conditional starts at line 34. Insert the Rewards block between them.
    - QuestBoard.Service/Views/Quest/Create.Mobile.cshtml — Description block at lines 35-42 (note the label uses `class="form-label dm-create-label"`); Campaign conditional at line 44.
    - QuestBoard.Service/Views/Quest/Edit.cshtml — `Quest.Description` block at lines 30-34; Campaign conditional at line 36. Uses `asp-for="Quest.Rewards"` (nested Quest property).
    - QuestBoard.Service/Views/Quest/Edit.Mobile.cshtml — `Quest.Description` block at lines 44-48; Campaign conditional at line 50. Uses `asp-for="Quest.Rewards"`.
    - QuestBoard.Service/Views/Quest/CreateFollowUp.cshtml — Description block at lines 34-38; Challenge Rating block at lines 40-45 (this view has NO board-type conditional). Insert Rewards between Description and Challenge Rating. Binds to `asp-for="Rewards"` (flat FollowUpQuestViewModel).
    - .planning/phases/59-add-a-rewards-field-to-quests-an-open-text-field-between-des/59-UI-SPEC.md — Interaction Notes item 1 (exact `<div class="mb-3">` textarea markup), Copywriting Contract (label `Rewards` with NO required asterisk; placeholder `Describe the loot, gold, or other rewards for this quest...`), and the D-02 rule that Rewards sits BEFORE the Campaign conditional so it shows for both board types.
    - .planning/phases/59-add-a-rewards-field-to-quests-an-open-text-field-between-des/59-CONTEXT.md — D-01 (optional, no asterisk), D-02 (before the Campaign conditional → shows for both board types), D-04 (Follow-Up field left blank — no pre-fill; achieved because Plan 59-01 did not add Rewards to the CreateFollowUp GET pre-fill object).
  </read_first>
  <action>
    In each of the five form views, insert a `<div class="mb-3">` field block containing a `<label>` reading `Rewards` (NO `<span class="text-danger">*</span>` asterisk — the field is optional per D-01), a `<textarea class="form-control" rows="4">` with placeholder `Describe the loot, gold, or other rewards for this quest...`, and an `asp-validation-for` span. Match the surrounding Description block's exact wrapper/label/textarea structure in each file:
    - Create.cshtml, Create.Mobile.cshtml, CreateFollowUp.cshtml → bind with `asp-for="Rewards"`. On Create.Mobile.cshtml, apply the same `class="form-label dm-create-label"` on the label that its Description field uses, for visual parity.
    - Edit.cshtml, Edit.Mobile.cshtml → bind with `asp-for="Quest.Rewards"` (matching the `Quest.Description` nesting already used in those views).
    Placement: on Create/Edit (desktop + mobile) insert the block immediately after the Description field block and immediately BEFORE the `@if (boardType != BoardType.Campaign)` conditional (D-02 — this makes Rewards appear for both OneShot and Campaign boards). On CreateFollowUp.cshtml insert it immediately after the Description block and before the Challenge Rating block.
    Do NOT add any inline CSS, new CSS class, or FontAwesome icon to the form-field label (icons are only for the display-side headings, not the form input). Do NOT pre-fill the Follow-Up Rewards value — leave the textarea to bind whatever the (empty-by-default) `FollowUpQuestViewModel.Rewards` holds.
  </action>
  <verify>
    <automated>cd QuestBoard.Service && dotnet build ../QuestBoard.Service -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `Create.cshtml`, `Create.Mobile.cshtml`, `CreateFollowUp.cshtml` each contain a `<textarea asp-for="Rewards"` with the placeholder `Describe the loot, gold, or other rewards for this quest...`.
    - `Edit.cshtml` and `Edit.Mobile.cshtml` each contain `<textarea asp-for="Quest.Rewards"`.
    - In Create.cshtml and Edit.cshtml (and their mobile variants), the Rewards `<div class="mb-3">` block appears before the `@if (boardType != BoardType.Campaign)` line (verify by line order).
    - None of the five Rewards field labels contain a `<span class="text-danger">*</span>` required marker.
    - The Service project builds (Razor views compile) with zero errors.
  </acceptance_criteria>
  <done>All five form views render a Rewards textarea in the correct position, optional (no asterisk), bound to the correct property, showing for both board types.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Rewards display block to the three display views (Details desktop/mobile + QuestLog Details)</name>
  <files>QuestBoard.Service/Views/Quest/Details.cshtml, QuestBoard.Service/Views/Quest/Details.Mobile.cshtml, QuestBoard.Service/Views/QuestLog/Details.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Details.cshtml — Description rendered at line 31 (`<p class="lead" style="white-space: pre-wrap;">@Model.Quest?.Description</p>`). Insert the Rewards block immediately after it.
    - QuestBoard.Service/Views/Quest/Details.Mobile.cshtml — existing Description conditional block at lines 97-103 (`@if (!string.IsNullOrWhiteSpace(Model.Quest?.Description)) { <hr class="quest-card-divider mt-2 mb-2" /> <div class="quest-description-mobile">@Model.Quest.Description</div> }`). Mirror this exactly for Rewards, placed immediately after it, inside the same `.quest-header-card-mobile`.
    - QuestBoard.Service/Views/QuestLog/Details.cshtml — existing "Original Quest Description" block at lines 44-50 (`<hr />` then `<div class="mb-4"><h5 class="text-white"><i class="fas fa-scroll me-2"></i>Original Quest Description</h5><div class="quest-description-box">@Model.Quest.Description</div></div>`). Insert the Rewards block after it and before the following `@{ var selectedPlayers ... }` / Adventurers section.
    - QuestBoard.Service/wwwroot/css/quests.css — lines 813-834: `.quest-description-box` (no `white-space` declared) vs `.recap-display-box` (declares `white-space: pre-wrap`). Reuse `.quest-description-box` and add inline `style="white-space: pre-wrap;"` so multi-line rewards render correctly.
    - .planning/phases/59-add-a-rewards-field-to-quests-an-open-text-field-between-des/59-UI-SPEC.md — Interaction Notes items 2, 3, 4 (exact markup for each of the three views), Copywriting Contract (headings), Color section (`fa-coins text-warning` icon, gold accent), and item 6 (icon locked to `fa-coins`).
    - .planning/phases/59-add-a-rewards-field-to-quests-an-open-text-field-between-des/59-CONTEXT.md — D-06 (reuse `.quest-description-box` pattern, `fa-coins` icon, "Rewards" heading), D-07 (hide the block entirely when Rewards is null/empty/whitespace — no placeholder).
  </read_first>
  <action>
    Add a conditional Rewards display block to each of the three display views, gated by `@if (!string.IsNullOrWhiteSpace(Model.Quest?.Rewards))` (use `Model.Quest.Rewards` without the `?` where the surrounding code already dereferences `Model.Quest` non-nullably, e.g. QuestLog/Details) so the block is hidden entirely when empty (D-07 — no "no rewards" placeholder anywhere). Render the stored value with plain Razor interpolation `@Model.Quest.Rewards` (NEVER `Html.Raw` — Razor must HTML-encode the value).

    - Details.cshtml (desktop): immediately after line 31's Description `<p>`, insert a `<div class="mb-4">` containing `<h5><i class="fas fa-coins text-warning me-2"></i>Rewards</h5>` and a `<div class="quest-description-box" style="white-space: pre-wrap;">@Model.Quest.Rewards</div>`, wrapped in the `IsNullOrWhiteSpace` guard.
    - Details.Mobile.cshtml: immediately after the existing Description conditional (lines 97-103), insert an identical-shaped conditional that emits `<hr class="quest-card-divider mt-2 mb-2" />` and a `<div class="quest-description-mobile"><i class="fas fa-coins me-1"></i>@Model.Quest.Rewards</div>` — same `.quest-description-mobile` class as Description, with the `fa-coins` icon prefix as the only visual differentiator.
    - QuestLog/Details.cshtml: after the "Original Quest Description" block (lines 44-50) and before the Adventurers/`selectedPlayers` section, insert a guarded block emitting `<hr />` then `<div class="mb-4"><h5 class="text-white"><i class="fas fa-coins text-warning me-2"></i>Rewards</h5><div class="quest-description-box" style="white-space: pre-wrap;">@Model.Quest.Rewards</div></div>`.

    Use the `fa-coins` icon on every heading/label (locked by D-06 — do not substitute `fa-gem`, `fa-treasure-chest`, or any other). Do NOT introduce any new CSS class or edit quests.css — reuse `.quest-description-box` / `.quest-description-mobile` directly and rely on the inline `white-space: pre-wrap` for multi-line rendering. Do NOT touch `_QuestCard.cshtml` or `Quest/Index.cshtml` (D-05 — no board-list indicator).
  </action>
  <verify>
    <automated>cd QuestBoard.Service && dotnet build ../QuestBoard.Service -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `Details.cshtml` contains `@if (!string.IsNullOrWhiteSpace(Model.Quest?.Rewards))` and, inside it, `<i class="fas fa-coins text-warning me-2"></i>Rewards` and `@Model.Quest.Rewards` rendered in a `.quest-description-box`.
    - `Details.Mobile.cshtml` contains a `!string.IsNullOrWhiteSpace(Model.Quest?.Rewards)` guard, a `<i class="fas fa-coins me-1"></i>` icon, and `@Model.Quest.Rewards` inside a `.quest-description-mobile` div.
    - `QuestLog/Details.cshtml` contains a `!string.IsNullOrWhiteSpace(Model.Quest.Rewards)` guard, a `<h5 class="text-white"><i class="fas fa-coins text-warning me-2"></i>Rewards</h5>`, and `@Model.Quest.Rewards` in a `.quest-description-box`.
    - No display view uses `Html.Raw` on the Rewards value.
    - No display view uses an icon other than `fa-coins` for the Rewards heading.
    - `_QuestCard.cshtml` and `Quest/Index.cshtml` are NOT in the modified-files set (grep for `Rewards` in those two files returns nothing).
    - The Service project builds with zero errors.
  </acceptance_criteria>
  <done>All three display views render a boxed Rewards callout (with the fa-coins gold accent) below/near the Description, hidden when Rewards is empty, HTML-encoded; the board list card is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the Rewards feature end-to-end (desktop + mobile)</name>
  <action>Pause for human verification. Present the what-built and how-to-verify steps below to the operator, wait for the resume-signal, and do not proceed until the operator approves or reports issues.</action>
  <what-built>
    A Rewards textarea on the Quest Create, Edit, and Follow-Up forms (desktop + mobile), and a boxed Rewards callout (with a gold fa-coins heading) on the Quest Details, Quest Details mobile, and completed-quest QuestLog Details pages — shown only when Rewards has a value, hidden when empty. Backed by a nullable Rewards column persisted via a new EF migration (auto-applied on startup).
  </what-built>
  <how-to-verify>
    Run the app (`dotnet run --project QuestBoard.Service` — the AddRewardsToQuest migration auto-applies on startup) and, signed in as a DM:
    1. Create a quest, fill in the Rewards field (e.g. "500 gold, a +1 longsword"). Confirm the Rewards field sits between Description and Challenge Rating on the Create page (both desktop and a mobile/responsive session). Save.
    2. Open the quest's Details page. Confirm a boxed "Rewards" block with a gold coins icon appears below the Description showing your text (desktop + mobile).
    3. Edit the quest, clear the Rewards field, save, reopen Details — confirm the Rewards block is now hidden entirely (no "no rewards" placeholder).
    4. Create a Campaign quest (if a Campaign-type group is available): confirm the Rewards field still shows on Create (no Challenge Rating around it) and its value persists after save/reopen.
    5. Finalize a quest and open its completed-quest QuestLog Details page: confirm a read-only Rewards box appears near the Original Quest Description when Rewards is set, hidden when empty.
    6. Create a Follow-Up quest from a finalized quest: confirm the Rewards field is present and starts BLANK (not copied from the original).
    7. Confirm the quest board list card shows NO rewards indicator.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues (wrong position, block not hiding, Campaign field missing, follow-up pre-filled, board card showing rewards, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored Rewards text → rendered Razor view | DM-authored Rewards content is displayed to all group members on Details/QuestLog pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Information Disclosure / XSS | Rewards rendering on Details/Details.Mobile/QuestLog Details | mitigate | Render Rewards via plain `@Model.Quest.Rewards` interpolation only — Razor HTML-encodes by default. `Html.Raw` is explicitly forbidden on the Rewards value in all three display views (asserted in Task 2 acceptance criteria). |
| T-59-05 | Information Disclosure | Rewards visibility on hidden/DM-only quests | accept | Rewards is displayed inside the existing Details/QuestLog views whose access control is unchanged by this phase — a viewer who can already see a quest's Description can see its Rewards; no new visibility surface is introduced. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — Razor markup edits only. |
</threat_model>

<verification>
- Service project builds clean (Razor compiles): `dotnet build ../QuestBoard.Service -c Debug` from `QuestBoard.Service/`.
- Rewards field appears before the `@if (boardType != BoardType.Campaign)` block on Create/Edit (both platforms) → shows for both board types (D-02).
- Rewards display block is guarded by `IsNullOrWhiteSpace` → hidden when empty (D-07).
- All Rewards headings use `fa-coins` (D-06); no `Html.Raw` on the value (T-59-04).
- `_QuestCard.cshtml` / `Quest/Index.cshtml` untouched (D-05).
- Human verification checkpoint passes across all 7 checks.
</verification>

<success_criteria>
- Rewards textarea present on Create/Edit/CreateFollowUp forms (desktop + mobile), between Description and Challenge Rating, optional, showing for both board types.
- Rewards boxed callout present on Quest Details (desktop + mobile) and QuestLog Details, hidden when empty, using the `.quest-description-box`/`.quest-description-mobile` pattern with the gold `fa-coins` heading.
- Follow-Up Rewards starts blank; quest board list card shows no Rewards indicator.
- Service project builds; human verification approved.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New markup created by this plan (excluded from drift verification):
- Rewards form-field block in `Create.cshtml`, `Create.Mobile.cshtml`, `Edit.cshtml`, `Edit.Mobile.cshtml`, `CreateFollowUp.cshtml`
- Rewards display block in `Details.cshtml`, `Details.Mobile.cshtml`, `QuestLog/Details.cshtml`
- No new CSS classes are created — this plan reuses the existing `.quest-description-box` and `.quest-description-mobile` classes with inline `white-space: pre-wrap`.
</artifacts_produced>

<output>
Create `.planning/phases/59-add-a-rewards-field-to-quests-an-open-text-field-between-des/59-02-SUMMARY.md` when done
</output>
