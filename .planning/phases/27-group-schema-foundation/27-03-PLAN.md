---
phase: 27-group-schema-foundation
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - QuestBoard.Repository/Migrations/AddGroupSchema.cs
autonomous: false
requirements: [GROUP-04, GROUP-05, GROUP-06]
must_haves:
  truths:
    - "The AddGroupSchema migration applies cleanly on the local dev SQL Server with no FK-violation or IDENTITY_INSERT error"
    - "After apply, the Groups table contains exactly one row: Id=1, Name='EuphoriaInn'"
    - "After apply, every AspNetUsers row has a matching UserGroups row with GroupId=1 and a GroupRole matching the user's prior AspNetUserRoles role"
    - "After apply, AspNetUserRoles contains no Player/DungeonMaster/Admin rows"
    - "After apply, all Quests and ShopItems rows have GroupId=1"
    - "The deployment constraint (Phase 27 alone breaks auth on next login) is documented in the migration file and surfaced for the operator"
  artifacts:
    - path: "QuestBoard.Repository/Migrations/AddGroupSchema.cs"
      provides: "Migration with a header comment documenting the Phase 27-29 co-deployment constraint"
      contains: "deployment"
  key_links:
    - from: "QuestBoard.Repository/Migrations/AddGroupSchema.cs"
      to: "SQL Server (localhost)"
      via: "context.Database.Migrate() applies the migration on dotnet run startup"
      pattern: "AddGroupSchema"
---

<objective>
Verify the `AddGroupSchema` migration actually applies and seeds correctly against a real (local dev) SQL Server — the data-seeding correctness for GROUP-04/05/06 that the InMemory integration tests cannot exercise (RESEARCH.md Validation Architecture: "Migration data seeding has no automated test... planner should add a Wave 0 smoke test that applies the migration against a dev SQL Server and verifies row counts"). Then add a deployment-constraint comment to the migration documenting the Phase 27-29 co-deployment requirement.

This plan re-confirms GROUP-04, GROUP-05, and GROUP-06 against live SQL Server behavior (the plan 02 tests only confirm the model accepts the schema in InMemory). It also satisfies the phase's Success Criterion 5: "All migrations apply cleanly on a fresh database and on the existing production schema."

Purpose: The migration's raw SQL (IDENTITY_INSERT, highest-role CTE, AspNetUserRoles delete) never runs under the InMemory test provider, so without a real apply the seeding logic is unverified. This plan closes that gap with a human-run apply + SQL spot-checks and records the operational deployment constraint.
Output: A verified migration apply on dev SQL Server, and a documented deployment constraint in `AddGroupSchema.cs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-group-schema-foundation/27-CONTEXT.md
@.planning/phases/27-group-schema-foundation/27-RESEARCH.md
@.planning/phases/27-group-schema-foundation/27-02-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 03)

Modified symbols:
- `AddGroupSchema.cs` — a header comment block on the migration class documenting the Phase 27-29 co-deployment constraint (RESEARCH.md Security Domain + Open Question 2): deploying Phase 27 alone deletes Player/DungeonMaster/Admin from AspNetUserRoles, which breaks the existing DungeonMasterHandler/AdminHandler on the next login until Phase 29 replaces those handlers. Deploy 27-29 together, or during a maintenance window where no user logs in until Phase 29 ships.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Document the Phase 27-29 co-deployment constraint in the migration</name>
  <files>QuestBoard.Repository/Migrations/AddGroupSchema.cs</files>
  <read_first>
    - QuestBoard.Repository/Migrations/AddGroupSchema.cs (the migration authored in plan 02)
    - .planning/phases/27-group-schema-foundation/27-RESEARCH.md (Security Domain section and Open Question 2 — the auth-break-on-next-login constraint)
  </read_first>
  <action>
    Add a documentation comment block immediately above the `public partial class AddGroupSchema : Migration` declaration in the migration file. The comment must state the operational constraint from RESEARCH.md: this migration deletes the Player, DungeonMaster, and Admin rows from AspNetUserRoles (GROUP-06); the current `DungeonMasterHandler` and `AdminHandler` still read those Identity roles, so after this migration a user's DungeonMaster/Admin authorization will fail on their next login until Phase 29 replaces the handlers to read `UserGroups.GroupRole`. Therefore Phase 27 must be deployed together with Phases 28-29, OR during a maintenance window in which no user logs in until Phase 29 is also deployed. This is documentation only — do not change any migration logic.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj 2>&1 | grep -v '^#' | grep -c "error CS"</automated>
  </verify>
  <acceptance_criteria>
    - `AddGroupSchema.cs` contains a comment above the class declaration mentioning the deployment constraint and Phase 29
    - The migration's `Up()` and `Down()` logic is byte-for-byte unchanged from plan 02 (comment-only edit)
    - `dotnet build QuestBoard.Repository/QuestBoard.Repository.csproj` reports 0 `error CS` lines (count is 0)
  </acceptance_criteria>
  <done>The migration carries a clear deployment-constraint comment; build is green; no logic changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply migration on local dev SQL Server and verify seeding (GROUP-04/05/06)</name>
  <action>Pause for human verification. Apply the migration against the local dev SQL Server and run the SQL spot-checks below, then wait for the resume signal before completing the plan.</action>
  <what-built>The AddGroupSchema migration (plan 02) plus the deployment-constraint comment. This step applies the migration to the local dev SQL Server via app startup and verifies the raw-SQL data seeding that the InMemory tests cannot exercise. Note the deployment constraint: this migration removes Player/DungeonMaster/Admin from AspNetUserRoles, so existing authorization breaks on next login until Phase 29 ships — do not deploy Phase 27 alone to production.</what-built>
  <how-to-verify>
    1. Before applying, capture the baseline so seeding can be checked. Against the local dev SQL Server (connection string uses `localhost` per CLAUDE.md), run:
       `SELECT ur.UserId, r.Name FROM AspNetUserRoles ur JOIN AspNetRoles r ON r.Id = ur.RoleId;`
       and note the role per user, and `SELECT COUNT(*) FROM AspNetUsers;`, `SELECT COUNT(*) FROM Quests;`, `SELECT COUNT(*) FROM ShopItems;`.
    2. Apply the migration by starting the app (auto-applies on startup): from `QuestBoard.Service/` run `dotnet run`. Wait for startup to complete with no migration exception in the console. Stop the app once startup succeeds. (Alternatively `dotnet ef database update --project ../QuestBoard.Repository` from `QuestBoard.Service/`.)
    3. Verify GROUP-04 — exactly one group seeded:
       `SELECT Id, Name FROM Groups;` → expect a single row `1, EuphoriaInn`.
    4. Verify GROUP-04 — all shared-resource rows scoped:
       `SELECT COUNT(*) FROM Quests WHERE GroupId <> 1;` → expect 0.
       `SELECT COUNT(*) FROM ShopItems WHERE GroupId <> 1;` → expect 0.
    5. Verify GROUP-05 — every user has a membership with correct role:
       `SELECT COUNT(*) FROM AspNetUsers u LEFT JOIN UserGroups ug ON ug.UserId = u.Id WHERE ug.Id IS NULL;` → expect 0 (no user without a membership, per D-04).
       `SELECT UserId, GroupRole FROM UserGroups ORDER BY UserId;` → confirm each user's GroupRole int matches their prior role from step 1 (Admin→2, DungeonMaster→1, Player/none→0; for any multi-role user the highest wins per D-03).
    6. Verify GROUP-06 — role rows removed:
       `SELECT COUNT(*) FROM AspNetUserRoles ur JOIN AspNetRoles r ON r.Id = ur.RoleId WHERE r.Name IN ('Player','DungeonMaster','Admin');` → expect 0.
    7. Confirm no FK-violation or "Cannot insert explicit value for identity column" error appeared during apply (research Pitfalls 1 and 2).
  </how-to-verify>
  <resume-signal>Type "approved" once all six checks pass (one EuphoriaInn group, all Quests/ShopItems GroupId=1, every user has a UserGroups row with the correct role, AspNetUserRoles has no Player/DM/Admin rows, clean apply with no errors), or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → production SQL Server | Operational boundary: applying Phase 27 alone removes the Identity role rows the current auth handlers depend on. The risk is operational (auth break on next login), mitigated by the documented co-deployment constraint. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Denial of Service (availability) | AspNetUserRoles delete vs. live auth handlers | mitigate | Deployment-constraint comment in the migration + checkpoint warning: Phase 27 must ship with Phases 28-29 or during a maintenance window (RESEARCH.md Security Domain). |
| T-27-07 | Integrity | Data seeding correctness on real SQL Server | mitigate | Human-run row-count + per-user role spot-checks confirm GROUP-04/05/06 against live SQL Server — the verification the InMemory test provider cannot perform (RESEARCH.md Wave 0 gap). |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — RESEARCH.md Package Legitimacy Audit confirms zero new packages. |
</threat_model>

<verification>
- `dotnet build` exits 0; migration logic unchanged from plan 02.
- Migration applies on local dev SQL Server with no exception.
- All six SQL spot-checks return the expected values (one EuphoriaInn group; Quests/ShopItems all GroupId=1; every user has a correct-role UserGroups row; AspNetUserRoles free of Player/DM/Admin).
- Deployment constraint documented in the migration file.
</verification>

<success_criteria>
- GROUP-04/05/06 confirmed against live SQL Server, not just InMemory model state.
- Phase Success Criterion 5 met: the migration applies cleanly on the existing dev schema.
- The Phase 27-29 co-deployment constraint is documented where the operator will see it.
</success_criteria>

<output>
Create `.planning/phases/27-group-schema-foundation/27-03-SUMMARY.md` when done.
</output>
