---
phase: 53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/ViewModels/QuestLogViewModels/EditRecapViewModel.cs
  - QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs
  - QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A GET request to /QuestLog/EditRecap/{id} by the quest's DM or an Admin returns 200 and renders the recap edit form"
    - "A GET request to /QuestLog/EditRecap/{id} by a Player (non-DM, non-Admin) returns 403 Forbidden"
    - "A POST to /QuestLog/EditRecap/{id} by the quest's DM or an Admin persists the recap and redirects to Details"
    - "The new EditRecap actions are unreachable for an in-progress (not completed, not closed) quest"
  artifacts:
    - path: "QuestBoard.Service/ViewModels/QuestLogViewModels/EditRecapViewModel.cs"
      provides: "Typed model binding target for the EditRecap views (Id, Recap, Quest)"
      contains: "class EditRecapViewModel"
    - path: "QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs"
      provides: "EditRecap GET+POST action pair with two-layer DM/Admin authorization"
      contains: "EditRecap"
  key_links:
    - from: "QuestLogController.EditRecap (POST)"
      to: "IQuestService.UpdateQuestRecapAsync"
      via: "direct service call, then RedirectToAction Details"
      pattern: "UpdateQuestRecapAsync"
    - from: "QuestLogController.EditRecap (GET)"
      to: "EditRecapViewModel"
      via: "View(new EditRecapViewModel { ... })"
      pattern: "new EditRecapViewModel"
---

<objective>
Add the backend contract for the dedicated recap-edit page: a new `EditRecapViewModel` and an `EditRecap` GET+POST action pair on `QuestLogController`, replicating the exact two-layer authorization (policy + in-action DM/Admin ownership check) and completed-quest guard that the existing `UpdateRecap` POST already uses. No service, entity, or AutoMapper changes — `IQuestService.UpdateQuestRecapAsync` already exists and is called unchanged.

Purpose: Establishes the route, ViewModel shape, and authorization surface that the view layer (Plan 02) links to and binds against. Delivering this first means the view plan receives a settled contract (route name `EditRecap`, ViewModel with `Id`/`Recap`/`Quest`) rather than exploring the controller.

Output: `EditRecapViewModel.cs`, two new actions on `QuestLogController.cs`, and integration tests proving the 403-for-Player / 200-for-DM-or-Admin behavior locked by CONTEXT.md D-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-CONTEXT.md
@.planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use these directly — no exploration needed. -->

Existing UpdateRecap POST on QuestLogController.cs (lines 77-117) — the exact authorization + guard shape the new actions must replicate:
- `[HttpPost] [ValidateAntiForgeryToken] [Authorize(Policy = "DungeonMasterOnly")]`
- Loads quest via `questService.GetQuestWithDetailsAsync(id, token)`; returns `NotFound()` if null
- Completed-quest guard: `isCompletedOneShot = quest.IsFinalized && quest.FinalizedDate.HasValue && quest.FinalizedDate.Value.Date <= DateTime.UtcNow.AddDays(-1).Date && !quest.DungeonMasterSession;` then `if (!isCompletedOneShot && !quest.IsClosed) return BadRequest(...)` (POST) / the Details GET uses `return NotFound()` for the same guard
- Loads `currentUser = await userService.GetUserAsync(User)`; `if (currentUser == null) return Challenge();`
- Ownership check: `var isQuestDm = currentUser.Id == quest.DungeonMaster?.Id; var isAdmin = await GetEffectiveRoleAsync() == GroupRole.Admin; if (!isQuestDm && !isAdmin) return Forbid();`
- Then `await questService.UpdateQuestRecapAsync(id, recap, token); return RedirectToAction("Details", new { id });`

Existing private helper already on the controller (reuse as-is, do not re-declare):
- `private async Task<GroupRole?> GetEffectiveRoleAsync()` — SuperAdmin short-circuits to `GroupRole.Admin`, else `GetEffectiveGroupRoleAsync`

Existing imports already present on QuestLogController.cs (lines 1-6, no new using needed):
- `QuestBoard.Domain.Enums`, `QuestBoard.Domain.Extensions`, `QuestBoard.Domain.Interfaces`, `QuestBoard.Service.ViewModels.QuestLogViewModels`, `Microsoft.AspNetCore.Authorization`, `Microsoft.AspNetCore.Mvc`

Service method (unchanged, called from POST):
- `Task UpdateQuestRecapAsync(int questId, string recap, CancellationToken token)` on `IQuestService`

Sibling ViewModel already using the full domain Quest (the shape to copy):
- `QuestLogDetailsViewModel` = `public Quest Quest { get; set; } = new();` with `using QuestBoard.Domain.Models.QuestBoard;`

Existing integration tests establishing the 403 precedent (mirror their arrange/assert shape):
- `UpdateRecap_Player_IsForbiddenOrRedirected` — Player POST asserts `BeOneOf(Forbidden, Redirect, Unauthorized)`
- `UpdateRecap_NonOwnerAdmin_IsNotForbidden` — Admin POST asserts `NotBe(Forbidden)`
- `AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(factory, name, email, roles: [...])`, `TestDataHelper.CreateTestQuestAsync(...)`, `TestDataHelper.ClearDatabaseAsync(...)` — the helper signatures used throughout the suite
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EditRecapViewModel</name>
  <files>QuestBoard.Service/ViewModels/QuestLogViewModels/EditRecapViewModel.cs</files>
  <read_first>
    - QuestBoard.Service/ViewModels/QuestLogViewModels/QuestLogDetailsViewModel.cs (sibling ViewModel; copy its full-domain-Quest shape and namespace)
    - QuestBoard.Service/ViewModels/QuestViewModels/EditQuestViewModel.cs (the Id + editable-field analog)
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-UI-SPEC.md ("New ViewModel" section — the templates reference Model.Id, Model.Recap, Model.Quest.Title)
  </read_first>
  <action>
    Create `QuestBoard.Service/ViewModels/QuestLogViewModels/EditRecapViewModel.cs` in namespace `QuestBoard.Service.ViewModels.QuestLogViewModels`. It must expose exactly three members required by the EditRecap Razor templates: `public int Id { get; set; }`, `public string? Recap { get; set; }`, and `public Quest Quest { get; set; } = new();` using `using QuestBoard.Domain.Models.QuestBoard;` (the full domain Quest type, identical to how the sibling `QuestLogDetailsViewModel` holds it — no flattened DTO, no new AutoMapper profile entry, no nested `QuestSummaryViewModel`). The `Quest` property exists only so the page heading can read `Model.Quest.Title`; `Recap` is the textarea bind target; `Id` is the hidden form field and route id. Do not add validation attributes (recap is a free-text field with no required constraint, matching the current inline form).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - EditRecapViewModel.cs exists in QuestBoard.Service/ViewModels/QuestLogViewModels/
    - File declares `namespace QuestBoard.Service.ViewModels.QuestLogViewModels`
    - File contains `public class EditRecapViewModel`
    - File contains `public int Id { get; set; }`, `public string? Recap { get; set; }`, and `public Quest Quest { get; set; } = new();`
    - File contains `using QuestBoard.Domain.Models.QuestBoard;`
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds
  </acceptance_criteria>
  <done>EditRecapViewModel compiles with Id (int), Recap (string?), and Quest (full domain Quest) members.</done>
</task>

<task type="auto">
  <name>Task 2: Add EditRecap GET+POST actions to QuestLogController</name>
  <files>QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/QuestBoard/QuestLogController.cs (the file being modified; read the existing Details GET lines 32-75 and UpdateRecap POST lines 77-117 and the GetEffectiveRoleAsync helper lines 122-125)
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-PATTERNS.md ("Authorization" and "Completed-quest guard" Shared Patterns sections)
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-CONTEXT.md (D-04: direct-URL access by a non-editor must be 403 Forbidden, not 404)
  </read_first>
  <action>
    Add two new actions to `QuestLogController`, placed after the existing `UpdateRecap` POST and before the private helpers, no new `using` directives needed.

    GET `EditRecap(int id, CancellationToken token = default)`: annotate `[HttpGet]` and `[Authorize(Policy = "DungeonMasterOnly")]`. Load `quest = await questService.GetQuestWithDetailsAsync(id, token)`; `if (quest == null) return NotFound();`. Apply the completed-quest guard verbatim from the existing code (`isCompletedOneShot = quest.IsFinalized && quest.FinalizedDate.HasValue && quest.FinalizedDate.Value.Date <= DateTime.UtcNow.AddDays(-1).Date && !quest.DungeonMasterSession;`) and `if (!isCompletedOneShot && !quest.IsClosed) return NotFound();` (use `NotFound()` here to match the Details GET's convention for the same guard — an in-progress quest simply has no reachable recap page). Load `currentUser = await userService.GetUserAsync(User); if (currentUser == null) return Challenge();`. Apply the two-layer ownership check exactly as UpdateRecap does: `var isQuestDm = currentUser.Id == quest.DungeonMaster?.Id; var isAdmin = await GetEffectiveRoleAsync() == GroupRole.Admin; if (!isQuestDm && !isAdmin) return Forbid();` (D-04: 403, NOT the cross-tenant 404 convention — this is an intra-group ownership check). Return `View(new EditRecapViewModel { Id = quest.Id, Recap = quest.Recap, Quest = quest });`.

    POST `EditRecap(int id, string recap, CancellationToken token = default)`: annotate `[HttpPost]`, `[ValidateAntiForgeryToken]`, `[Authorize(Policy = "DungeonMasterOnly")]`. Replicate the full body of the existing `UpdateRecap` POST verbatim — same null check, same completed-quest guard (`BadRequest` on failure, matching UpdateRecap), same `currentUser`/`Challenge()` guard, same `isQuestDm || isAdmin` / `Forbid()` check — then `await questService.UpdateQuestRecapAsync(id, recap, token); return RedirectToAction("Details", new { id });`. Do not delete or alter the existing `UpdateRecap` action; leave it in place (the current inline Details form is removed in Plan 02, but keeping UpdateRecap avoids breaking its existing passing tests within this plan's wave). Reuse the existing `GetEffectiveRoleAsync()` private helper — do not redeclare it.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - QuestLogController.cs contains `public async Task<IActionResult> EditRecap(int id, CancellationToken token = default)` (GET) annotated with `[HttpGet]` and `[Authorize(Policy = "DungeonMasterOnly")]`
    - QuestLogController.cs contains `public async Task<IActionResult> EditRecap(int id, string recap, CancellationToken token = default)` (POST) annotated with `[HttpPost]`, `[ValidateAntiForgeryToken]`, `[Authorize(Policy = "DungeonMasterOnly")]`
    - The GET action contains `return View(new EditRecapViewModel` and both actions contain `if (!isQuestDm && !isAdmin)` followed by `return Forbid();`
    - The POST action contains `await questService.UpdateQuestRecapAsync(id, recap, token);` and `return RedirectToAction("Details", new { id });`
    - The existing `UpdateRecap` action is still present (not removed)
    - `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds
  </acceptance_criteria>
  <done>Both EditRecap actions compile, GET returns the ViewModel for authorized users and Forbid() for non-DM/non-Admin, POST persists via UpdateQuestRecapAsync and redirects to Details.</done>
</task>

<task type="auto">
  <name>Task 3: Add EditRecap integration tests</name>
  <files>QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs (the file being modified; copy the arrange/assert shape of UpdateRecap_Player_IsForbiddenOrRedirected, UpdateRecap_NonOwnerAdmin_IsNotForbidden, and Details_WithValidQuestId_ShouldReturnQuestDetails)
    - .planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-CONTEXT.md (D-04 403 precedent)
  </read_first>
  <action>
    Append three new `[Fact]` tests to `QuestLogControllerIntegrationTests`, following the existing arrange pattern (ClearDatabaseAsync, CreateTestUserAsync for the DM, CreateTestQuestAsync with `isFinalized: true`, then set `FinalizedDate = DateTime.UtcNow.AddDays(-2)` inside a scope so the completed-quest guard passes).

    Test `EditRecap_Player_IsForbidden`: create a Player client via `CreateAuthenticatedClientWithUserAsync(factory, ..., roles: ["Player"])`, GET `/QuestLog/EditRecap/{quest.Id}`, assert `response.StatusCode.Should().Be(HttpStatusCode.Forbidden)` (D-04 locks 403 specifically for the GET; unlike the POST test which allows a redirect, the GET has no redirect path so assert Forbidden exactly).

    Test `EditRecap_NonOwnerAdmin_ReturnsOk`: create an Admin client via `roles: ["Admin"]` (not the quest's DM), GET `/QuestLog/EditRecap/{quest.Id}`, assert `response.StatusCode.Should().Be(HttpStatusCode.OK)` and the content `Should().Contain("Save Recap")` (proves the edit form renders — note this content assertion depends on the view existing; if Plan 02's view is not yet merged this assertion's content check may fail while the 200 status still passes, so keep the status assertion as the primary gate).

    Test `EditRecap_Post_NonOwnerAdmin_RedirectsToDetails`: create an Admin client, POST `/QuestLog/EditRecap/{quest.Id}` with `FormUrlEncodedContent` `["recap"] = "Recap set via dedicated edit page."`, using a non-redirecting client (`factory.CreateNonRedirectingClient()` is available in this suite — see Index_WhenNotAuthenticated_ShouldRedirect) authenticated as the admin, or assert `NotBe(HttpStatusCode.Forbidden)` if a non-redirecting authenticated admin client is not readily constructible; prefer asserting the POST result `Should().BeOneOf(HttpStatusCode.Redirect, HttpStatusCode.Found)` to confirm the RedirectToAction("Details") path. Match whichever client-construction helper the existing UpdateRecap POST tests use for the admin.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~QuestLogControllerIntegrationTests&FullyQualifiedName~EditRecap"</automated>
  </verify>
  <acceptance_criteria>
    - QuestLogControllerIntegrationTests.cs contains `EditRecap_Player_IsForbidden`, `EditRecap_NonOwnerAdmin_ReturnsOk`, and `EditRecap_Post_NonOwnerAdmin_RedirectsToDetails` `[Fact]` methods
    - `EditRecap_Player_IsForbidden` asserts `Should().Be(HttpStatusCode.Forbidden)` on a GET to `/QuestLog/EditRecap/{id}` by a Player
    - `dotnet test ... --filter "...EditRecap"` runs and the Player-forbidden and Admin-status tests pass (the NonOwnerAdmin_ReturnsOk content-contains "Save Recap" assertion may require Plan 02's view — if it fails on content-only while status is 200, note it in the SUMMARY for Plan 02 to confirm)
  </acceptance_criteria>
  <done>Integration tests assert 403 for a Player GET, 200 for a non-owner Admin GET, and redirect for a non-owner Admin POST to EditRecap, all passing (or content-check deferred to Plan 02 with status assertions passing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → QuestLogController.EditRecap GET/POST | An authenticated group member crosses into a DM/Admin-only recap-edit surface; the route id is attacker-controllable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Elevation of Privilege | EditRecap GET (direct-URL access by a Player or non-owning DM) | mitigate | Two-layer check: `[Authorize(Policy = "DungeonMasterOnly")]` at the action + in-action `isQuestDm \|\| isAdmin` else `Forbid()` (403 per D-04); asserted by `EditRecap_Player_IsForbidden` integration test |
| T-53-02 | Elevation of Privilege | EditRecap POST (recap tampering by a non-owner) | mitigate | Same two-layer check replicated verbatim from the already-tested UpdateRecap POST; `ValidateAntiForgeryToken` prevents CSRF; asserted by `EditRecap_Post_NonOwnerAdmin_RedirectsToDetails` and covered by the Player-forbidden GET precedent |
| T-53-03 | Information Disclosure | EditRecap GET on an in-progress / DM-session quest | mitigate | Completed-quest guard (`isCompletedOneShot && !IsClosed`) returns `NotFound()`, matching the Details GET — the edit page is unreachable for quests not in the quest log, so no in-progress recap surface is exposed |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure C# controller/ViewModel/test changes against existing dependencies; no `## Package Legitimacy Audit` required |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` succeeds after both code tasks
- `dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~QuestLogControllerIntegrationTests"` — new EditRecap tests plus the existing UpdateRecap/Details tests all pass (existing UpdateRecap is untouched in this plan)
- The `EditRecap_Player_IsForbidden` test proves D-04's locked 403 behavior
</verification>

<success_criteria>
- `EditRecapViewModel` exists with `Id` (int), `Recap` (string?), `Quest` (full domain Quest)
- `QuestLogController` exposes `EditRecap` GET (returns ViewModel for DM/Admin, Forbid() otherwise) and POST (persists via `UpdateQuestRecapAsync`, redirects to Details)
- The existing `UpdateRecap` action and its tests are untouched and still pass
- New integration tests confirm 403-for-Player-GET, 200-for-Admin-GET, redirect-for-Admin-POST
</success_criteria>

<output>
Create `.planning/phases/53-add-dedicated-edit-view-for-quest-recap-so-details-page-is-v/53-01-SUMMARY.md` when done
</output>
