---
phase: 33-session-persistence-persist-activegroupid-across-app-restart
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs
requirements: [EMAIL-RATE-01, EMAIL-RATE-02, EMAIL-RATE-03, EMAIL-RATE-04, SESSION-01, SESSION-02]
autonomous: false

must_haves:
  truths:
    - "An automated integration test proves the 4th SendConfirmationEmail to the same target user within the window returns 429"
    - "An automated integration test proves two different target users have independent resend budgets"
    - "An automated integration test proves EditUser's email-change path is rate-limited and CreateUser is not"
    - "The full test suite is green after the phase's changes"
    - "A human has verified the AspNetSessionState table schema in real SQL Server and confirmed ActiveGroupId survives a real app restart"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs"
      provides: "EMAIL-RATE-01/02/03/04 integration tests modeled on ForgotPassword_Post_ExceedingRateLimit_ShouldReturn429"
      contains: "TooManyRequests"
  key_links:
    - from: "AdminControllerIntegrationTests"
      to: "AdminController.SendConfirmationEmail / EditUser"
      via: "authenticated admin client POSTs the rate-limited endpoints and asserts 429 / independent budgets"
      pattern: "SendConfirmationEmail|EditUser"
---

<objective>
Prove the admin email rate-limiting behavior with automated integration tests, gate the phase on a full-green suite, and drive the two Manual-Only Verifications (session table schema + restart-survival) through a blocking human-verify checkpoint.

Purpose: The InMemory integration test infra cannot exercise the raw-SQL session migration or a real process restart (33-VALIDATION.md Wave 0 gaps), but it CAN exercise the in-process `PartitionedRateLimiter<int>` singleton exactly like the Phase 32 `forgot-password` limiter. This plan lifts the four EMAIL-RATE behaviors into automated tests and captures the two genuinely-manual SESSION checks as an explicit human-verify step so they are not silently dropped.

Output:
- New integration tests for EMAIL-RATE-01/02/03/04 in `AdminControllerIntegrationTests.cs`
- Full `dotnet test` green
- Human-verified session table schema (SESSION-02) and restart-survival (SESSION-01)

Implements the verification side of EMAIL-RATE-01..04 and the manual verification of SESSION-01, SESSION-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-session-persistence-persist-activegroupid-across-app-restart/33-RESEARCH.md
@.planning/phases/33-session-persistence-persist-activegroupid-across-app-restart/33-PATTERNS.md
@.planning/phases/33-session-persistence-persist-activegroupid-across-app-restart/33-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan (33-03) creates the following named symbols/artifacts:
- **Test methods** in `QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs`:
  - `SendConfirmationEmail_ExceedingRateLimit_ShouldReturn429` (EMAIL-RATE-01)
  - `SendConfirmationEmail_DifferentTargetUsers_ShouldHaveIndependentBudgets` (EMAIL-RATE-02)
  - `EditUser_EmailChange_ExceedingRateLimit_ShouldReturn429` (EMAIL-RATE-03)
  - `CreateUser_RapidRequests_ShouldNotBeRateLimited` (EMAIL-RATE-04)
- No new REQ-IDs (this plan verifies REQ-IDs created in 33-01/33-02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add EMAIL-RATE integration tests</name>
  <files>QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs — existing class structure (`IClassFixture<WebApplicationFactoryBase>`, `CreateNonRedirectingClient`, admin auth via `AuthenticationHelper.CreateAuthenticatedAdminClientAsync`)
    - QuestBoard.IntegrationTests/Controllers/AccountControllerIntegrationTests.cs — the `ForgotPassword_Post_ExceedingRateLimit_ShouldReturn429` method (the 429-loop analog: GET for anti-forgery token, extract token+cookie, POST N times, assert `Contain(r => r.StatusCode == TooManyRequests)`)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs — `CreateAuthenticatedAdminClientAsync` (line 174), `CreateTestUserAsync` (line 11), `CreateAuthenticatedClientWithUserAsync` (line 88)
    - QuestBoard.IntegrationTests/Helpers/AntiForgeryHelper.cs — `ExtractAntiForgeryTokenAsync`, `CreateFormContentWithAntiForgeryToken`
    - QuestBoard.IntegrationTests/Helpers/TestDataHelper.cs — `ClearDatabaseAsync`
    - QuestBoard.Service/Views/Admin/Users.cshtml — confirm the `SendConfirmationEmail` form field name is `userId` (POST /Admin/SendConfirmationEmail)
    - QuestBoard.Service/Views/Admin/EditUser.cshtml — confirm the EditUser form posts `Id`, `Name`, `Email`, `HasKey` to POST /Admin/EditUser
  </read_first>
  <action>
    Add four integration test methods to `AdminControllerIntegrationTests.cs`, modeled on `ForgotPassword_Post_ExceedingRateLimit_ShouldReturn429` (same GET-token / extract / repeated-POST / 429-assert shape). Use an authenticated admin client via `AuthenticationHelper.CreateAuthenticatedAdminClientAsync(_factory, ...)`. Create target users to resend to via `AuthenticationHelper.CreateTestUserAsync(_factory.Services, ...)` with `EmailConfirmed = false` where the action requires an unconfirmed target (SendConfirmationEmail early-returns for already-confirmed users).

    CRITICAL isolation note: the `PartitionedRateLimiter<int>` is a process-wide SINGLETON keyed by target `userId`, and `WebApplicationFactoryBase` is an `IClassFixture` shared across all methods in this test class. Each test MUST use a DISTINCT target userId (i.e., create fresh target users per test with unique usernames/emails) so one test's exhausted budget does not bleed into another. Do NOT rely on `ClearDatabaseAsync` to reset the limiter — it resets the DB, not the in-memory limiter state; distinct userIds are the isolation mechanism. Assert on "at least one of N rapid requests returned 429" (like the analog) rather than "exactly the Nth", to stay robust.

    Tests to add:

    1. `SendConfirmationEmail_ExceedingRateLimit_ShouldReturn429` (EMAIL-RATE-01): create one unconfirmed target user; POST `/Admin/SendConfirmationEmail` with `userId=<targetId>` 4 times; assert the responses `Contain(r => r.StatusCode == HttpStatusCode.TooManyRequests)`.

    2. `SendConfirmationEmail_DifferentTargetUsers_ShouldHaveIndependentBudgets` (EMAIL-RATE-02): create two distinct unconfirmed target users A and B; POST 3 resends for A, then 3 for B; assert NONE of the first 3-for-A returned 429 AND none of the first 3-for-B returned 429 (each has its own budget). Then a 4th for A returns 429 while B is unaffected. Assert both users' first-3 sequences are 429-free (proving independent partitions).

    3. `EditUser_EmailChange_ExceedingRateLimit_ShouldReturn429` (EMAIL-RATE-03): create one target user; POST `/Admin/EditUser` 4 times with `Id=<targetId>`, an unchanged `Name`, `HasKey`, and a CHANGED `Email` each time (email must differ from the user's current email to enter the `emailChanged` branch); assert responses `Contain(r => r.StatusCode == HttpStatusCode.TooManyRequests)`. Use distinct new email values per POST so ModelState stays valid.

    4. `CreateUser_RapidRequests_ShouldNotBeRateLimited` (EMAIL-RATE-04): POST `/Admin/CreateUser` 4 times with 4 DISTINCT new usernames/emails and a valid GroupRole; assert NONE of the 4 returned 429 (`responses.Should().NotContain(r => r.StatusCode == HttpStatusCode.TooManyRequests)`). This proves the D-08 exemption. Read `CreateUserViewModel` and the CreateUser form to supply the exact required fields (Name, Email, GroupRole) — the account is passwordless post-Phase-32, so no Password field.

    Follow the existing anti-forgery handling in the analog (GET the form page, extract token + antiforgery cookie, attach cookie header, build form content with `CreateFormContentWithAntiForgeryToken`). Reuse the admin client's authenticated cookie across the loop.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Admin" 2>&1 | grep -E "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `AdminControllerIntegrationTests.cs` contains the four methods named above
    - `SendConfirmationEmail_ExceedingRateLimit_ShouldReturn429` asserts at least one 429 across 4 same-target POSTs
    - `SendConfirmationEmail_DifferentTargetUsers_ShouldHaveIndependentBudgets` asserts user A's and user B's first-3 sequences are each 429-free
    - `EditUser_EmailChange_ExceedingRateLimit_ShouldReturn429` POSTs 4 email-changing EditUser saves for the same `Id` and asserts at least one 429
    - `CreateUser_RapidRequests_ShouldNotBeRateLimited` asserts no 429 across 4 distinct CreateUser POSTs
    - Each test uses distinct target userIds so the shared singleton limiter does not cross-contaminate
    - `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~Admin"` exits 0 (all Admin tests pass)
  </acceptance_criteria>
  <done>Four EMAIL-RATE integration tests exist and pass, proving 429 on over-limit, per-target-user budgets, EditUser email-change limiting, and CreateUser exemption.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green gate</name>
  <files>QuestBoard.IntegrationTests/Controllers/AdminControllerIntegrationTests.cs</files>
  <read_first>
    - .planning/phases/33-session-persistence-persist-activegroupid-across-app-restart/33-VALIDATION.md §"Sampling Rate" — full suite must be green before verify-work
  </read_first>
  <action>
    Run the full test suite (`dotnet test` — both `QuestBoard.UnitTests` and `QuestBoard.IntegrationTests`) and confirm every test passes, including the four new EMAIL-RATE tests and all pre-existing tests (the Testing-guarded `AddDistributedMemoryCache` fallback from Plan 01 means no test touches real SQL Server for session; the singleton limiter fires in Testing exactly like the forgot-password limiter). If any pre-existing test regressed, fix the regression (do not weaken assertions to hide it) before completing this task.
  </action>
  <verify>
    <automated>dotnet test 2>&1 | grep -E "Passed!|Failed!|error|Passed:|Failed:"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` exits 0
    - The output shows 0 failed tests across `QuestBoard.UnitTests` and `QuestBoard.IntegrationTests`
    - The four new EMAIL-RATE tests are included in the passing count
  </acceptance_criteria>
  <done>The entire test suite is green with the phase's changes in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify session table schema and restart survival</name>
  <action>Pause for a blocking human-verify checkpoint: the operator inspects the real `dbo.AspNetSessionState` table schema in local SQL Server (SESSION-02) and confirms a selected group survives an actual app restart (SESSION-01). These two behaviors cannot be exercised by the InMemory integration suite. Follow the numbered steps in `<how-to-verify>` and wait for the `<resume-signal>` before completing the plan.</action>
  <what-built>
    Session persistence backed by SQL Server (`AddDistributedSqlServerCache` + `AspNetSessionState` migration) and per-target-user admin email rate limiting. Two behaviors cannot be verified by the InMemory integration suite and require manual confirmation against the real local SQL Server (per 33-VALIDATION.md Manual-Only Verifications and Wave 0 gaps).
  </what-built>
  <how-to-verify>
    1. Ensure the Visual Studio debugger is stopped, then run the app locally: `dotnet run --project QuestBoard.Service`. The `AddSessionStateTable` migration auto-applies via `context.Database.Migrate()` on startup.
    2. **SESSION-02 (table schema):** In SQL Server (localhost, `QuestBoard` database), inspect `dbo.AspNetSessionState`. Confirm the columns and constraints exactly:
       - `Id NVARCHAR(449) COLLATE SQL_Latin1_General_CP1_CS_AS NOT NULL` — clustered primary key
       - `Value VARBINARY(MAX) NOT NULL`
       - `ExpiresAtTime DATETIMEOFFSET(7) NOT NULL` — with nonclustered index `Index_ExpiresAtTime`
       - `SlidingExpirationInSeconds BIGINT NULL`
       - `AbsoluteExpiration DATETIMEOFFSET(7) NULL`
       (You can run `SELECT COLLATION_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='AspNetSessionState' AND COLUMN_NAME='Id'` and confirm it returns `SQL_Latin1_General_CP1_CS_AS`.) Also confirm no stray pre-existing `AspNetSessionState` table with a different schema existed (RESEARCH Pitfall 5) — if one did, the `IF NOT EXISTS` guard would have skipped creation.
       Also confirm a row appears in `dbo.AspNetSessionState` after you log in and pick a group (Session write is now persisted).
    3. **SESSION-01 (restart survival):** Log in, select a group (note which group is active in the nav bar). Stop the app (Ctrl+C / Shift+F5) and start it again (`dotnet run --project QuestBoard.Service`). Reload the page in the same browser (auth cookie persists). Confirm the SAME group is still active — you are NOT bounced to the group picker.
    4. **Rate-limit smoke (optional, already covered by tests):** As an admin, click "Resend Welcome Email" for one unconfirmed user 4 times in quick succession; the 4th should show the 429 "Too many requests. Please try again later." response.
  </how-to-verify>
  <resume-signal>Type "approved" if the AspNetSessionState schema is correct AND the group survives a restart, or describe what differed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → AdminController | Integration tests exercise the rate-limited endpoints as an authenticated admin |
| Developer → real local SQL Server | Manual verification inspects the real cache table and restart behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Denial of Service | Admin email resend | mitigate | EMAIL-RATE-01/03 tests assert the 429 rejection fires (behavioral proof of the Plan 02 mitigation) |
| T-33-02 | Denial of Service (cross-user budget collision) | Rate-limit partition key | mitigate | EMAIL-RATE-02 test asserts independent per-target-user budgets, proving the partition key is correct (not the RESEARCH Pitfall 1 "unknown" collapse) |
| T-33-03 | Tampering / Information Disclosure | Session cache in Testing | mitigate | Full-suite gate confirms no test regresses due to the Testing-guarded in-memory cache fallback; manual step confirms real dev DB has the correct table, not test session pollution |

No high-severity residual threats. This plan is verification-only for the Plan 01/02 mitigations.
</threat_model>

<verification>
- `dotnet test` full suite green, including the four new EMAIL-RATE tests.
- Human-verify checkpoint confirms SESSION-02 (table schema/collation/index in real SQL Server) and SESSION-01 (group survives a real app restart).
</verification>

<success_criteria>
- Four EMAIL-RATE integration tests added and passing (429 on over-limit, per-target-user budgets, EditUser email-change limited, CreateUser exempt).
- Full test suite green.
- Blocking human-verify checkpoint approved for the two Manual-Only Verifications (SESSION-01, SESSION-02).
</success_criteria>

<output>
Create `.planning/phases/33-session-persistence-persist-activegroupid-across-app-restart/33-03-SUMMARY.md` when done.
</output>
