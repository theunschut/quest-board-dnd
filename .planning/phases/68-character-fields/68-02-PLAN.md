---
phase: 68-character-fields
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Views/Characters/Details.cshtml
  - QuestBoard.Service/Views/Characters/Details.Mobile.cshtml
  - QuestBoard.Service/wwwroot/css/character-detail.mobile.css
autonomous: true
requirements: [CHARMD-01, CHARMD-02]
user_setup: []

must_haves:
  truths:
    - "Character Description and Backstory render as formatted HTML (headings, bold, lists, blockquotes, links) on Character Details — desktop and mobile."
    - "Existing multi-line Description/Backstory text shows no doubled vertical spacing after the switch to Markdown rendering."
    - "A Markdown bullet/numbered list on the mobile Details page renders its list items in full-opacity gold, matching the surrounding paragraph text (not the muted secondary color)."
    - "When Description or Backstory is blank, its whole labeled block is omitted entirely (no empty placeholder), exactly as today."
  artifacts:
    - path: "QuestBoard.Service/Views/Characters/Details.cshtml"
      provides: "Description + Backstory rendered via Html.Markdown() inside div wrappers (no illegal <p>-around-block-content)"
      contains: "Html.Markdown(Model.Description)"
    - path: "QuestBoard.Service/Views/Characters/Details.Mobile.cshtml"
      provides: "Description + Backstory rendered via Html.Markdown() inside .character-info-row"
      contains: "Html.Markdown(Model.Backstory)"
    - path: "QuestBoard.Service/wwwroot/css/character-detail.mobile.css"
      provides: "li added to the .character-detail-card catch-all so Markdown list items match full-opacity gold"
      contains: ".character-detail-card li"
  key_links:
    - from: "QuestBoard.Service/Views/Characters/Details.cshtml"
      to: "IMarkdownService (via HtmlHelperExtensions.Markdown)"
      via: "@Html.Markdown(Model.Description) / @Html.Markdown(Model.Backstory)"
      pattern: "Html\\.Markdown\\(Model\\.(Description|Backstory)\\)"
    - from: "QuestBoard.Service/wwwroot/css/character-detail.mobile.css"
      to: "Markdig-emitted <li> nodes inside .markdown-content"
      via: "catch-all color rule extended with an li selector"
      pattern: "\\.character-detail-card li"
---

<objective>
Render Character **Description** and **Backstory** as formatted, sanitized HTML on the two Character Details read views (`Details.cshtml` desktop, `Details.Mobile.cshtml` mobile) by routing both fields through the existing `Html.Markdown()` helper, replacing today's raw `@Model.X` inside a line-break-preserving `<p>`. Add one companion CSS edit so Markdown list items render at full opacity on the mobile card.

Purpose: Satisfies the read side of CHARMD-01 and CHARMD-02 — Description and Backstory display as formatted HTML wherever they appear, with no doubled spacing (the old plain-text `white-space: pre-wrap` is dropped; `.markdown-content { white-space: normal }` from Phase 67 takes over globally).
Output: Two updated read views + one CSS catch-all extension. No email surface (Character fields never appear in an email — confirmed in CONTEXT.md), no card/list preview (the roster Index shows no Description/Backstory snippet — confirmed), and no Manage page (Characters have none).

Note: `Html.Markdown()` emits block-level HTML (`<h1-6>`, `<ul>`, `<blockquote>`) that cannot legally sit inside a `<p>`. The current markup wraps both fields in `<p class="form-control-plaintext" style="white-space: pre-wrap;">` (desktop) and `<p class="character-info-value mb-0">` (mobile). Both `<p>` wrappers are replaced by a direct `Html.Markdown()` call inside the existing non-`<p>` container, matching the block-safe `<div>` precedent the Quest Details views and email templates already use.

Heading-color is already correct on both platforms and needs NO new CSS — verified in 68-UI-SPEC.md (Typography → "heading color … resolved for both platforms"): `site.css`'s `.modern-card h1-h6/p/li/span/small { color:#F4E4BC !important }` rules win by `!important` on desktop regardless of wrapper depth, and the mobile catch-all already colors `<p>`/`<a>` — the ONLY mobile gap is `<li>`, which Task 2 closes. Do NOT port Quest/Manage's `.modern-card-body > .markdown-content h1..h6 { color:#1a1a1a }` override into Character Details — it requires a direct-child relationship that does not hold here (Character Details nests `.markdown-content` one level deeper inside a `<div class="mb-3">` label wrapper) and would be dead CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-character-fields/68-CONTEXT.md
@.planning/phases/68-character-fields/68-PATTERNS.md
@.planning/phases/68-character-fields/68-UI-SPEC.md

<interfaces>
<!-- The read-side rendering helper. Reused verbatim — no changes to it this plan. -->

From QuestBoard.Service/Extensions/HtmlHelperExtensions.cs:
- `IHtmlContent Markdown(this IHtmlHelper html, string? markdown)` — resolves `IMarkdownService`, calls `RenderToHtml(markdown, MarkdownRenderTarget.Web)` (the Web sanitizer profile — correct here; NOT `.Email`), and wraps the result in `<div class="markdown-content">…</div>`. That `.markdown-content` wrapper carries its own typography (heading scale, blockquote, `white-space: normal`), so the new call site needs no extra class of its own.

Call as `@Html.Markdown(Model.Description)` / `@Html.Markdown(Model.Backstory)`. `IHtmlHelper.Markdown` is available in all Razor views via the global `_ViewImports.cshtml` (no per-view `@using` needed — the extension is on `IHtmlHelper`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render Description + Backstory as HTML on desktop Details.cshtml</name>
  <files>QuestBoard.Service/Views/Characters/Details.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Characters/Details.cshtml (current Description block lines 147-153, Backstory block lines 155-161 — both `<p class="form-control-plaintext" style="white-space: pre-wrap;">@Model.X</p>` inside a `<div class="mb-3">`/`<div>` with a `<label class="form-label fw-bold">`, all inside `.card-body.modern-card-body`)
    - QuestBoard.Service/Views/Quest/Details.cshtml (analog: `@Html.Markdown(Model.Quest?.Description)` called directly inside `.card-body.modern-card-body`, no `<p>` wrapper)
    - QuestBoard.Service/Extensions/HtmlHelperExtensions.cs (the Markdown helper being called)
    - .planning/phases/68-character-fields/68-PATTERNS.md (section "Details.cshtml" — the exact before/after restructuring)
    - .planning/phases/68-character-fields/68-UI-SPEC.md (Copywriting Contract → "Read-view restructuring" row; Typography → desktop heading-color note confirming no new CSS)
  </read_first>
  <action>
    Keep both `@if (!string.IsNullOrEmpty(Model.Description))` and `@if (!string.IsNullOrEmpty(Model.Backstory))` guards and their outer wrapper `<div>`s and `<label class="form-label fw-bold">` labels exactly as they are — the whole-block-omission behavior when a field is blank stays unchanged.

    Inside each guard, replace the single `<p class="form-control-plaintext" style="white-space: pre-wrap;">@Model.Description</p>` (and the Backstory equivalent) with a direct `@Html.Markdown(Model.Description)` / `@Html.Markdown(Model.Backstory)` call. The `<p>` element and its inline `style="white-space: pre-wrap;"` are DROPPED entirely (not moved) — `.markdown-content { white-space: normal }` (Phase 67, `markdown-content.css`) already handles paragraph spacing, and Markdig's block output cannot live inside a `<p>`.

    Add NO new CSS and do NOT introduce a `.modern-card-body > .markdown-content` heading override — desktop heading color is already correct via `site.css`'s `!important` `.modern-card` rules (see objective).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds.
    - `Details.cshtml` contains `@Html.Markdown(Model.Description)` and `@Html.Markdown(Model.Backstory)`.
    - `Details.cshtml` no longer contains `form-control-plaintext" style="white-space: pre-wrap;"` (both inline pre-wrap `<p>` wrappers for Description/Backstory are gone).
    - Both `@if (!string.IsNullOrEmpty(Model.Description))` / `@if (!string.IsNullOrEmpty(Model.Backstory))` guards and their `<label class="form-label fw-bold">` labels are still present.
    - No `.modern-card-body > .markdown-content` selector or other new CSS was added anywhere for this view.
  </acceptance_criteria>
  <done>Desktop Character Details renders Description and Backstory as formatted HTML via `Html.Markdown()`, drops the illegal pre-wrap `<p>` wrappers, keeps blank-field omission, adds no CSS, and builds green.</done>
</task>

<task type="auto">
  <name>Task 2: Render Description + Backstory as HTML on mobile Details.Mobile.cshtml + add li to the mobile catch-all</name>
  <files>QuestBoard.Service/Views/Characters/Details.Mobile.cshtml, QuestBoard.Service/wwwroot/css/character-detail.mobile.css</files>
  <read_first>
    - QuestBoard.Service/Views/Characters/Details.Mobile.cshtml (current Description block lines 80-86, Backstory block lines 87-93 — both `<p class="character-info-value mb-0">@Model.X</p>` inside a `<div class="character-info-row">` with a `<span class="form-label">` label)
    - QuestBoard.Service/wwwroot/css/character-detail.mobile.css (catch-all color rule lines 82-87; the muted `.character-info-value` rule lines 54-60; the optional pre-wrap rule lines 62-65)
    - QuestBoard.Service/Views/Quest/Details.Mobile.cshtml (analog: `@Html.Markdown(Model.Quest.Description)` mobile block)
    - .planning/phases/68-character-fields/68-PATTERNS.md (sections "Details.Mobile.cshtml" and "CSS fix 2 — mobile list-item color gap")
    - .planning/phases/68-character-fields/68-UI-SPEC.md (Typography → mobile heading-color/li note)
  </read_first>
  <action>
    In `Details.Mobile.cshtml`: keep both `@if (!string.IsNullOrEmpty(Model.X))` guards, the outer `<div class="character-info-row">` wrappers, and the `<span class="form-label">` labels. Inside each guard, replace `<p class="character-info-value mb-0">@Model.Description</p>` (and the Backstory equivalent) with a direct `@Html.Markdown(Model.Description)` / `@Html.Markdown(Model.Backstory)` call. The inner `<p class="character-info-value">` is dropped (the `.character-info-row` wrapper is NOT a `<p>`, so it stays; only the inner paragraph is replaced). No class is required on the `Html.Markdown()` call site — its `.markdown-content` div carries its own typography.

    In `character-detail.mobile.css`: add `li` to the existing catch-all parchment rule so Markdown list items get full-opacity gold instead of falling back to `.character-info-value`'s muted `rgba(244, 228, 188, 0.7)`. The rule currently lists three selectors — `.character-detail-card p`, `.character-detail-card a`, `.character-detail-card span:not(.badge)`; add a fourth selector `.character-detail-card li` to the same comma-separated list, so all four share the `color: #F4E4BC !important; text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);` declaration. Do not change the declaration or the other selectors.

    The shared `.character-info-value { white-space: pre-wrap }` rule (lines 62-65) may be left as-is — `.markdown-content { white-space: normal }` already neutralizes it for the new call sites, and its only other consumers (`Owned by`, `Level`) are single-line values where pre-wrap vs normal is visually identical. Leaving it untouched is the least-invasive choice; do not remove it unless there is a concrete reason.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds.
    - `Details.Mobile.cshtml` contains `@Html.Markdown(Model.Description)` and `@Html.Markdown(Model.Backstory)`, and no longer contains `<p class="character-info-value mb-0">@Model.Description` or `<p class="character-info-value mb-0">@Model.Backstory`.
    - Both `@if (!string.IsNullOrEmpty(...))` guards, the `<div class="character-info-row">` wrappers, and the `<span class="form-label">` labels remain.
    - `character-detail.mobile.css` contains a `.character-detail-card li` selector in the same catch-all rule as `.character-detail-card p` / `.character-detail-card a` / `.character-detail-card span:not(.badge)`, sharing the `color: #F4E4BC !important` declaration; `grep -c "\.character-detail-card li" QuestBoard.Service/wwwroot/css/character-detail.mobile.css` returns at least 1.
    - The catch-all declaration block (`color`, `text-shadow`) is unchanged from before.
  </acceptance_criteria>
  <done>Mobile Character Details renders Description and Backstory as formatted HTML, keeps blank-field omission, and its list items render full-opacity gold via the extended catch-all; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored Description/Backstory → rendered HTML on Character Details | Previously-stored user free-text is converted to HTML and injected into the page. This is the phase's actual XSS-relevant boundary: it is where untrusted stored Markdown becomes DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-I | Injection / Tampering (stored XSS) | `@Html.Markdown(Model.Description)` / `@Html.Markdown(Model.Backstory)` on Details desktop + mobile | mitigate | Both calls route through `IMarkdownService.RenderToHtml(..., MarkdownRenderTarget.Web)`, whose HtmlSanitizer (Web profile) strips `<script>`, `javascript:` URLs, and attribute-injection payloads — proven by Phase 65's unit tests (RENDER-01). `IHtmlContent` from the helper is emitted without additional Razor `@` re-encoding by design; the sanitizer is the trust gate. No raw `@Html.Raw` or unsanitized string interpolation is introduced. |
| T-68-02-R | Repudiation / integrity | Rendered output vs saved output | accept | Preview (plan 68-01) and the saved read view both call the same `MarkdownRenderTarget.Web` pipeline, so what a user previews is byte-identical to what renders here (RENDER-02). No divergence to exploit. |
| T-68-SC | Tampering (supply chain) | dependencies | accept | No package installs and no new client assets in this plan — pure server-side Razor + CSS edits reusing the Phase 65 rendering service. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` is green after both tasks.
- Both Details views call `Html.Markdown()` for Description and Backstory; neither still wraps them in a `<p>`.
- `character-detail.mobile.css` catch-all includes `li`.
- Live formatted rendering, no-doubled-spacing, and mobile list-item color are confirmed in 68-03 (human-verify checkpoint); the `CharactersControllerIntegrationTests` run in 68-03's gate exercises Details view rendering end-to-end.
</verification>

<success_criteria>
- The read side of CHARMD-01 and CHARMD-02 is complete: Description and Backstory render as formatted, sanitized HTML on Character Details desktop and mobile.
- No doubled spacing (old inline pre-wrap dropped; global `.markdown-content` override governs).
- Mobile Markdown list items render full-opacity gold.
- Blank-field omission behavior preserved on both platforms.
</success_criteria>

<output>
Create `.planning/phases/68-character-fields/68-02-SUMMARY.md` when done.
</output>
