---
phase: 40-platform-members-page-redesign
plan: 03
type: execute
wave: 3
depends_on: [40-01, 40-02]
files_modified:
  - QuestBoard.Service/Areas/Platform/Views/Group/Members.cshtml
  - QuestBoard.Service/Areas/Platform/Views/Group/Members.Mobile.cshtml
autonomous: false
requirements: [MEMBERS-01, MEMBERS-02, MEMBERS-03]
must_haves:
  truths:
    - "The desktop Members page shows current members (left) and searchable non-members with per-row Add (right)"
    - "A SuperAdmin can filter the non-member list by name or email via a GET search form"
    - "A SuperAdmin can add a non-member to the group from a per-row inline role-select + Add button"
    - "A Create New User button opens a Bootstrap modal posting to CreateMember"
    - "The mobile page stacks members, then search + non-member cards, then the Create New User trigger"
  artifacts:
    - path: "QuestBoard.Service/Areas/Platform/Views/Group/Members.cshtml"
      provides: "Two-column Members layout: members table + search form + per-row Add + Create New User modal"
      contains: "createMemberModal"
      min_lines: 120
    - path: "QuestBoard.Service/Areas/Platform/Views/Group/Members.Mobile.cshtml"
      provides: "Stacked mobile Members layout with search, per-row Add cards, and Create New User modal"
      contains: "createMemberModal"
  key_links:
    - from: "QuestBoard.Service/Areas/Platform/Views/Group/Members.cshtml"
      to: "QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs"
      via: "search GET form posts to Members, per-row form posts to AddMember, modal posts to CreateMember"
      pattern: "asp-action=\"CreateMember\""
---

<objective>
Redesign the Platform Members views into the two-column (desktop) / stacked (mobile) layout the phase requires (MEMBERS-01): left column = current members table (unchanged markup), right column = a GET search form (MEMBERS-02, D-01), a searchable non-member list with per-row inline role-select + Add controls (D-03/D-04), and a "Create New User" button that opens a Bootstrap modal posting to `CreateMember` (MEMBERS-03, D-05/D-06). The standalone single-select "Add Member" form is removed entirely (D-03). The mobile view stacks the same sections vertically with no tabs (D-08) and reuses the identical modal (D-09/Pattern 4). A blocking human-verify checkpoint confirms the redesign renders and functions correctly.

Purpose: Deliver the actual user-facing two-column redesign and the create-user entry point on top of Plans 01-02.
Output: Rewritten `Members.cshtml` and `Members.Mobile.cshtml`, plus a passed blocking human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-platform-members-page-redesign/40-CONTEXT.md
@.planning/phases/40-platform-members-page-redesign/40-RESEARCH.md
@.planning/phases/40-platform-members-page-redesign/40-PATTERNS.md
@.planning/phases/40-platform-members-page-redesign/40-UI-SPEC.md
@.planning/phases/40-platform-members-page-redesign/40-01-SUMMARY.md
@.planning/phases/40-platform-members-page-redesign/40-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols/markup this plan creates (exclude from drift/source-grounding checks):
- `createMemberModal` — new Bootstrap modal id in both `Members.cshtml` and `Members.Mobile.cshtml`
- Per-row Add `<form asp-action="AddMember">` markup with inline `Role` select (top-level `UserId`/`Role` fields, `asp-route-search`)
- GET search `<form asp-action="Members">` markup with a `search` input bound to `Model.SearchQuery`
- `TempData["Warning"]` alert block (new — the view currently renders only Success/Error)
Consumes from Plans 01/02: `GroupMembersViewModel.SearchQuery`, `GroupMembersViewModel.CreateMember`, `CreateMemberViewModel`, `GroupController.Members`/`AddMember`/`CreateMember` actions.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Members.cshtml — two-column layout, search, per-row Add, Create New User modal</name>
  <files>QuestBoard.Service/Areas/Platform/Views/Group/Members.cshtml</files>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Views/Group/Members.cshtml (the file being redesigned; lines 36-94 hold the current members table + RemoveMember per-row form to keep in the left column; lines 96-130 hold the standalone single-select Add form to REMOVE; lines 14-30 hold the Success/Error TempData alerts)
    - .planning/phases/40-platform-members-page-redesign/40-UI-SPEC.md (Layout Notes, Copywriting Contract, Color sections — the prescriptive visual contract: two-column `row g-4`, `modern-card` glass panels, `h5` column subheadings with icons, `btn-warning` gold Create New User + Search buttons, `btn-success` per-row Add, `bg-dark text-light` modal, exact placeholder/empty-state/button copy)
    - QuestBoard.Service/Views/Shop/Index.cshtml (lines 205-283 — the GET search form pattern: `<form method="get">`, `name="search"` input with `value="@Model.SearchQuery"`, submit button)
    - QuestBoard.Service/Views/ShopManagement/Index.cshtml (the `#denyModal` markup: `modal fade` → `modal-dialog` → `modal-content bg-dark text-light` → `<form method="post">` with `@Html.AntiForgeryToken()`, `modal-header/body/footer border-secondary`; and a `data-bs-toggle="modal" data-bs-target="#..."` trigger button)
    - QuestBoard.Service/Views/Admin/CreateUser.cshtml (lines 22-55 — the Email/Name/GroupRole form-field markup with `asp-for`, `asp-validation-for`, `Html.GetEnumSelectList<GroupRole>()`, and `asp-validation-summary="ModelOnly"`)
  </read_first>
  <action>
    Rewrite `Members.cshtml` as a two-column layout inside the existing `.modern-card`/`.card-body modern-card-body`. Keep the page header (`Members — @Model.Group.Name`), the Success/Error TempData alerts, and ADD a `TempData["Warning"]` alert block using `alert-warning` + `fas fa-exclamation-circle me-2` (the AlreadyMember outcome uses Warning). Keep the "Back to Groups" button.

    Use a `<div class="row g-4">` with two columns:
    - LEFT column (`col-lg-6`): `<h5><i class="fas fa-users text-danger me-2"></i>Current Members</h5>` then the existing members table markup (Name/Email/Role badges/RemoveMember per-row form) unchanged, including the empty-state (`fa-users fa-3x` + `No members in this group yet.`).
    - RIGHT column (`col-lg-6`): `<h5><i class="fas fa-user-plus text-warning me-2"></i>Available Users</h5>`, then a GET search form `<form method="get" asp-action="Members" asp-route-id="@Model.Group.Id">` containing a `<input type="text" name="search" class="form-control" value="@Model.SearchQuery" placeholder="Search by name or email..." autocomplete="off" />` and a `<button type="submit" class="btn btn-warning"><i class="fas fa-search me-1"></i>Search</button>`.

    Below the search form, render the available-users list. For each `u` in `Model.AvailableUsers`, show Name + Email (D-02 — no other columns) and a per-row Add form mirroring the RemoveMember form shape: `<form asp-action="AddMember" asp-route-id="@Model.Group.Id" asp-route-search="@Model.SearchQuery" method="post" asp-antiforgery="true" class="d-inline-flex align-items-center gap-2">` with `<input type="hidden" name="UserId" value="@u.Id" />`, a `<select name="Role" class="form-select form-select-sm">` whose options iterate `Enum.GetValues<GroupRole>()` with `Player` pre-selected, and a `<button type="submit" class="btn btn-sm btn-success"><i class="fas fa-user-plus me-1"></i>Add</button>`. Post `UserId`/`Role` as TOP-LEVEL fields (no `AddMember.` prefix) to match Plan 02's binding.

    Handle the two empty states in the right column: when `!Model.AvailableUsers.Any()` and `string.IsNullOrWhiteSpace(Model.SearchQuery)`, show `All users are already members of this group.`; when `!Model.AvailableUsers.Any()` and a search term IS present, show `No users match "@Model.SearchQuery".` plus a `Clear search` link (`fas fa-times me-1`) pointing to `asp-action="Members" asp-route-id="@Model.Group.Id"` with no search value.

    Add a `Create New User` trigger button in the right column: `<button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#createMemberModal"><i class="fas fa-user-plus me-2"></i>Create New User</button>`.

    Add the `#createMemberModal` at the end of the view mirroring `#denyModal`: `modal fade` → `modal-dialog` → `modal-content bg-dark text-light` → `<form asp-action="CreateMember" asp-route-id="@Model.Group.Id" method="post">` with `@Html.AntiForgeryToken()`. Inside: `modal-header border-secondary` (title "Create New User" + close button), `modal-body` containing `<div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>` and the three `CreateMember`-prefixed fields (`asp-for="CreateMember.Email"` type email, `asp-for="CreateMember.Name"`, `asp-for="CreateMember.GroupRole"` with `asp-items="Html.GetEnumSelectList<GroupRole>()"`, each with an `asp-validation-for` span), and a `modal-footer border-secondary` with a `btn-secondary` Cancel (`data-bs-dismiss="modal"`) left and a `btn-warning` "Create User" submit right. The modal form has a single static target — no `show.bs.modal` JS action-rewriting needed. Remove the entire old standalone single-select `Add Member` form section (D-03).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln 2>&1 | grep -qi "Build succeeded" && dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~GroupManagementIntegrationTests</automated>
  </verify>
  <acceptance_criteria>
    - `Members.cshtml` contains `row g-4` and two columns titled `Current Members` and `Available Users`
    - `Members.cshtml` contains `<form method="get"` with `name="search"` and `value="@Model.SearchQuery"`
    - `Members.cshtml` contains `asp-action="AddMember"` with `asp-route-search="@Model.SearchQuery"` and a top-level `name="UserId"` input (no `AddMember.UserId`)
    - `Members.cshtml` contains `id="createMemberModal"` and `asp-action="CreateMember"` and `asp-for="CreateMember.Email"`
    - `Members.cshtml` contains a `TempData["Warning"]` alert block with `alert-warning`
    - `Members.cshtml` does NOT contain `asp-for="AddMember.UserId"` (old standalone form removed)
    - `dotnet build QuestBoard.sln` reports Build succeeded and the integration suite passes
  </acceptance_criteria>
  <done>The desktop Members page renders two columns (members left; search + per-row Add + Create New User modal right), the old single-select form is gone, and Warning flashes render.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Members.Mobile.cshtml — stacked layout, search, per-row Add cards, reused modal</name>
  <files>QuestBoard.Service/Areas/Platform/Views/Group/Members.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Views/Group/Members.Mobile.cshtml (the file being redesigned; current stacked structure — `platform-group-card-mobile`, `group-card-mobile` member cards with RemoveMember forms at lines 41-79, and the standalone Add form at lines 89-123 to REMOVE)
    - QuestBoard.Service/Areas/Platform/Views/Group/Members.cshtml (Task 1 result — reuse the SAME `#createMemberModal` markup verbatim, the same search-form fields, and the same per-row Add form fields; only the outer layout differs)
    - .planning/phases/40-platform-members-page-redesign/40-UI-SPEC.md (Layout Notes → Mobile bullet: single vertical stack — members first, then search + Available Users `group-card-mobile` cards with inline role select + Add, then the Create New User button/modal last; D-08 no tabs; D-09 modal reused unchanged)
    - QuestBoard.Service/Views/ShopManagement/Index.Mobile.cshtml (precedent that a Bootstrap modal is reused verbatim from the desktop view on mobile with no mobile-specific changes)
  </read_first>
  <action>
    Rewrite `Members.Mobile.cshtml` as a single vertical stack inside `.platform-group-card-mobile`, keeping the `@section Styles` link to `platform-group.mobile.css`, the header, and the Success/Error alerts; ADD a `TempData["Warning"]` alert block (`alert-warning` + `fas fa-exclamation-circle me-2`) and keep "Back to Groups".

    Order per D-08: (1) current members as existing `group-card-mobile` cards (Name, Email, role badge, RemoveMember form) unchanged, with the same empty-state; (2) an `<h5>Available Users</h5>` subheading, then the SAME GET search form as desktop (`<form method="get" asp-action="Members" asp-route-id="@Model.Group.Id">` with the `search` input bound to `Model.SearchQuery` + `btn-warning` Search button); (3) for each `u` in `Model.AvailableUsers`, a `group-card-mobile` card showing Name + Email and a per-row Add form identical in fields to the desktop one (`asp-action="AddMember"`, `asp-route-search="@Model.SearchQuery"`, top-level hidden `UserId`, inline `Role` select defaulting to Player, `btn-sm btn-success` Add); with the same two empty-state messages (all-members vs. no-search-match + Clear search link); (4) a `Create New User` `btn-warning` trigger button (`data-bs-toggle="modal" data-bs-target="#createMemberModal"`) followed by the `#createMemberModal` markup copied verbatim from `Members.cshtml`. Remove the old standalone single-select Add form (D-03).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln 2>&1 | grep -qi "Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `Members.Mobile.cshtml` contains `<form method="get"` with `name="search"` and `value="@Model.SearchQuery"`
    - `Members.Mobile.cshtml` contains `asp-action="AddMember"` with `asp-route-search="@Model.SearchQuery"` and a top-level `name="UserId"` input
    - `Members.Mobile.cshtml` contains `id="createMemberModal"` and `asp-action="CreateMember"` and `asp-for="CreateMember.Email"`
    - `Members.Mobile.cshtml` contains a `TempData["Warning"]` alert block
    - `Members.Mobile.cshtml` does NOT contain `asp-for="AddMember.UserId"`
    - `dotnet build QuestBoard.sln` reports Build succeeded
  </acceptance_criteria>
  <done>The mobile Members page is a vertical stack (members → search + Available Users cards with per-row Add → Create New User modal), the old single-select form is gone, and the modal markup matches the desktop view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human-verify — Members redesign renders and scopes to the correct group</name>
  <what-built>
    The redesigned Platform Members page (desktop + mobile): two-column/stacked layout, GET search filter over non-members, per-row inline role-select + Add, a Create New User Bootstrap modal that reuses Phase 39's collision-aware creation scoped to the route group, search preserved across Add, and the four Phase-39 flash outcomes. This is a blocking human-verify because the create-user path is a silent privilege-grant surface (SuperAdmin creates/adds users to a group) and the phase's top risk is group/session confusion — a human must confirm the RIGHT group receives the member.
  </what-built>
  <action>
    Present the running app to the user for manual verification. This is a blocking pause point — do not proceed until the user approves. Automated coverage (the GroupManagementIntegrationTests search round-trip, search-preserving Add, and CreateMember create/collision/route-scoping cases) is already green; this checkpoint confirms the end-to-end user-facing rendering, the two-column/stacked layout, and the correct-group scoping of the silent privilege-grant path, which the integration tests assert at the HTTP layer but not visually.
  </action>
  <how-to-verify>
    1. Run the app (`dotnet run --project QuestBoard.Service`) and sign in as a SuperAdmin.
    2. Go to /platform/Group/Index, open Members for a specific group (note which group name).
    3. Confirm the two-column layout: current members on the left, an "Available Users" column on the right with a search box.
    4. Type part of a known non-member's name or email, click Search — confirm the list filters and the search box keeps your term.
    5. On a filtered row, pick a role and click Add — confirm a success flash, the member appears on the left, AND your search filter is still applied (URL still has ?search=...).
    6. Click "Create New User", enter a brand-new email + name, pick a role, submit — confirm the success flash ("Account created for ... welcome email ...") and that the new user is now a member OF THE GROUP YOU OPENED (not any other group).
    7. Click "Create New User" again with the email of a user already in this group — confirm the warning flash "... is already a member of this group." and no duplicate row.
    8. Repeat step 6 on a DIFFERENT group to confirm membership lands in whichever group's Members page you opened (route-scoped, not a session default).
    9. On a mobile viewport (or the mobile view), confirm the same functions work as a single vertical stack (members → search + cards → Create New User modal), no layout breakage.
  </how-to-verify>
  <verify>
    <human-check>User confirms the two-column desktop / stacked mobile layout renders, search filters and preserves the term, per-row Add works and keeps the filter, and Create New User adds the member to the group whose Members page was opened (not any other group).</human-check>
  </verify>
  <done>The redesigned Members page is verified in the running app on desktop and mobile; the Create New User path scopes membership to the correct route group with no cross-group escalation.</done>
  <resume-signal>Type "approved" or describe any layout/behavior/wrong-group issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → GroupController | Search GET, per-row Add POST, and Create New User modal POST all cross from the browser into the controller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-08 | Tampering (XSS) | `@Model.SearchQuery` reflected into the search `value` and `No users match "..."` copy | mitigate | Razor auto HTML-encodes `@Model.SearchQuery`; no `Html.Raw` used. Same protection Shop's identical `value="@Model.SearchQuery"` relies on. |
| T-40-09 | Tampering/Spoofing (CSRF) | per-row Add form + Create New User modal form | mitigate | Per-row form uses `asp-antiforgery="true"`; modal form uses `@Html.AntiForgeryToken()`; both post to `[ValidateAntiForgeryToken]` actions. GET search form needs no token. |
| T-40-10 | Elevation of Privilege | Create New User silently grants group membership to the wrong group | mitigate | The modal form targets `asp-route-id="@Model.Group.Id"` (the opened group); controller sources groupId from the route (Plan 02). A blocking human-verify checkpoint explicitly confirms the correct group receives the member (steps 6-8). |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: N/A). No install tasks present. |
</threat_model>

<verification>
- `dotnet build QuestBoard.sln` succeeds and the integration suite passes.
- Both views contain the search form, per-row Add form (top-level `UserId`/`Role`, `asp-route-search`), and the `#createMemberModal` posting to `CreateMember`.
- Neither view retains the old `asp-for="AddMember.UserId"` standalone form.
- Blocking human-verify checkpoint approved, confirming correct-group scoping.
</verification>

<success_criteria>
- MEMBERS-01: two-column desktop / stacked mobile layout — members vs. non-members.
- MEMBERS-02: searchable non-member list replacing the plain dropdown.
- MEMBERS-03: Create New User modal creates/adds a user scoped to the opened group.
- D-03/D-04/D-05/D-08/D-09 satisfied; old single-select form removed.
</success_criteria>

<output>
Create `.planning/phases/40-platform-members-page-redesign/40-03-SUMMARY.md` when done
</output>
