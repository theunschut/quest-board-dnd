---
phase: 40-platform-members-page-redesign
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs
  - QuestBoard.IntegrationTests/Controllers/GroupManagementIntegrationTests.cs
autonomous: true
requirements: [MEMBERS-02, MEMBERS-03]
must_haves:
  truths:
    - "GET /platform/Group/Members/{id}?search=foo returns only non-members matching the term and echoes the term back"
    - "POST /platform/Group/AddMember/{id} with a search value redirects back to Members preserving that search"
    - "POST /platform/Group/CreateMember/{id} creates/adds a user scoped to the route's groupId with the four Phase-39 flash outcomes"
    - "GroupController never injects or references IActiveGroupContext"
  artifacts:
    - path: "QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs"
      provides: "Members(id, search) GET, AddMember(id, ..., search) POST, new CreateMember(id, model) POST"
      contains: "CreateMember"
  key_links:
    - from: "QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs"
      to: "QuestBoard.Domain/Services/UserService.cs"
      via: "Members action calls userService.GetAvailableUsersAsync(id, search)"
      pattern: "GetAvailableUsersAsync\\(id, search"
    - from: "QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs"
      to: "QuestBoard.Domain/Services/UserService.cs"
      via: "CreateMember action calls userService.CreateOrAddToGroupAsync(email, name, id, role)"
      pattern: "CreateOrAddToGroupAsync\\(model\\.Email, model\\.Name, id"
---

<objective>
Wire the `GroupController` (Platform area) to the Plan-01 backend: rewrite `Members` GET to use the search-filtered available-users query and echo the term (MEMBERS-02); adapt `AddMember` POST to preserve the search term across its redirect (D-04); and add a new `CreateMember` POST action that reuses Phase 39's `CreateOrAddToGroupAsync` with `groupId` sourced strictly from the route `id` (MEMBERS-03, D-06, D-07). No `IActiveGroupContext` is added to this controller.

Purpose: Replace the in-memory `GetAllAsync().Where(...)` filtering with the DB-side query, keep the SuperAdmin's filter intact after adding a member, and give the Create New User modal (built in Plan 03) a route-scoped POST target with the exact Phase-39 flash-message behavior.
Output: Rewritten `Members`/`AddMember` actions, new `CreateMember` action with the four-outcome switch, expanded `GroupController` constructor (adding `IIdentityService`, `IBackgroundJobClient`, `ILogger<GroupController>` — NOT `IActiveGroupContext`), and integration tests for search round-trip, search-preserving Add, and the CreateMember outcomes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-platform-members-page-redesign/40-CONTEXT.md
@.planning/phases/40-platform-members-page-redesign/40-RESEARCH.md
@.planning/phases/40-platform-members-page-redesign/40-PATTERNS.md
@.planning/phases/40-platform-members-page-redesign/40-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates or changes (exclude from drift/source-grounding checks):
- `GroupController.CreateMember(int id, CreateMemberViewModel model)` — new POST action
- `GroupController.Members` gains a `string? search` parameter
- `GroupController.AddMember` gains a `string? search` parameter and drops `[Bind(Prefix = "AddMember")]`
- `GroupController` primary constructor gains `IIdentityService identityService, IBackgroundJobClient jobClient, ILogger<GroupController> logger`
Consumes from Plan 01: `IUserService.GetAvailableUsersAsync`, `GroupMembersViewModel.SearchQuery`, `GroupMembersViewModel.CreateMember`, `CreateMemberViewModel`.
Consumes from Phase 39 (existing): `IUserService.CreateOrAddToGroupAsync`, `CreateOrAddToGroupOutcome`, `GeneratePasswordResetTokenForUserAsync`, `HasPasswordAsync`, `WelcomeEmailJob`, `GroupMembershipAddedEmailJob`, `RedirectWithSuccess`, `RedirectWithWarning`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Members GET (search) and adapt AddMember (preserve search)</name>
  <files>QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs</files>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs (the file being modified; lines 114-152 hold the current `Members` and `AddMember` actions to replace/adapt; lines 11-13 hold the class-level `[Area("Platform")]` + `[Authorize(Policy = "SuperAdminOnly")]` + primary constructor to preserve)
    - QuestBoard.Service/ViewModels/PlatformViewModels/GroupMembersViewModel.cs (Plan 01 added `SearchQuery` and `CreateMember`; populate both when constructing the ViewModel)
    - QuestBoard.Service/ViewModels/PlatformViewModels/AddMemberViewModel.cs (repurposed as the per-row Add binding target — `UserId` + `Role` posted as TOP-LEVEL fields now, not nested under an `AddMember.` prefix)
  </read_first>
  <action>
    Change the `Members` action signature to `public async Task<IActionResult> Members(int id, string? search)`. Replace the `var allUsers = await userService.GetAllAsync(); ... availableUsers = allUsers.Where(...)` block entirely with `var availableUsers = await userService.GetAvailableUsersAsync(id, search);`. Do NOT leave any `userService.GetAllAsync()` call in `Members`. When constructing `GroupMembersViewModel`, set `AvailableUsers = availableUsers`, `SearchQuery = search`, keep `Group`, `Members`, and `AddMember = new AddMemberViewModel()`.

    Change the `AddMember` action signature to `public async Task<IActionResult> AddMember(int id, AddMemberViewModel model, string? search)` — REMOVE the `[Bind(Prefix = "AddMember")]` attribute (the per-row form posts `UserId`/`Role` as top-level fields). In EVERY `RedirectToAction(nameof(Members), ...)` inside `AddMember`, include the search term: `RedirectToAction(nameof(Members), new { id, search })` (both the invalid-model branch and the success/catch branch). This is D-04 — the filter must survive the Add.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln 2>&1 | grep -qi "Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `GroupController.cs` `Members` signature contains `Members(int id, string? search)`
    - `GroupController.cs` `Members` action contains `GetAvailableUsersAsync(id, search)`
    - `GroupController.cs` `Members` action does NOT contain `GetAllAsync()`
    - `GroupController.cs` `Members` ViewModel construction contains `SearchQuery = search`
    - `GroupController.cs` `AddMember` signature contains `string? search` and does NOT contain `[Bind(Prefix = "AddMember")]`
    - `GroupController.cs` `AddMember` contains `RedirectToAction(nameof(Members), new { id, search })`
    - `dotnet build QuestBoard.sln` reports Build succeeded
  </acceptance_criteria>
  <done>`Members` filters via the DB query and echoes `search`; `AddMember` binds top-level `UserId`/`Role` and preserves `search` on every redirect.</done>
</task>

<task type="auto">
  <name>Task 2: Add CreateMember POST action (route-scoped groupId, Phase-39 outcomes)</name>
  <files>QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs</files>
  <read_first>
    - QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs (the file being modified; extend the primary constructor at line 13 and add the new action near `AddMember`/`RemoveMember`)
    - QuestBoard.Service/Controllers/Admin/AdminController.cs (lines 113-190 hold the `CreateUser` POST whose outcome switch, email-job enqueues, token encoding, and flash strings are copied VERBATIM — except `groupId` comes from `activeGroupContext.ActiveGroupId` there and MUST come from the route `id` here; note the `using` directives at lines 4-16 needed here: `QuestBoard.Service.Extensions`, `QuestBoard.Service.Jobs`, `Hangfire`, `Microsoft.AspNetCore.WebUtilities`, `Microsoft.Extensions.Logging`, `System.Text`)
    - QuestBoard.Service/Extensions/ControllerExtensions.cs (CRITICAL: `RedirectWithSuccess`/`RedirectWithWarning` call `RedirectToAction(action)` with NO route values — they cannot carry `id`. For CreateMember, use `RedirectToAction(nameof(Members), new { id })` with a preceding `TempData["Success"]`/`TempData["Warning"]` assignment instead of the helpers, so the redirect lands on the correct group's Members page)
    - QuestBoard.Service/ViewModels/PlatformViewModels/CreateMemberViewModel.cs (Plan 01 — the action's model parameter type: Email, Name, GroupRole)
    - QuestBoard.Domain/Interfaces/IUserService.cs (lines 136-147 confirm `CreateOrAddToGroupAsync(email, name, groupId, role)` and the `CreateOrAddToGroupResult`/`CreateOrAddToGroupOutcome` shape; lines 116-129 confirm `GeneratePasswordResetTokenForUserAsync` and `HasPasswordAsync`)
  </read_first>
  <action>
    Extend the `GroupController` primary constructor to `(IGroupService groupService, IUserService userService, IIdentityService identityService, IBackgroundJobClient jobClient, ILogger<GroupController> logger)`. Do NOT add `IActiveGroupContext` — this is the phase's hard constraint (STATE.md risk flag; Phase 30 and 34.3 incidents). Add the `using` directives required by the copied body (`QuestBoard.Service.Extensions`, `QuestBoard.Service.Jobs`, `Hangfire`, `Microsoft.AspNetCore.WebUtilities`, `Microsoft.Extensions.Logging`, `System.Text`).

    Add `[HttpPost] [ValidateAntiForgeryToken] public async Task<IActionResult> CreateMember(int id, CreateMemberViewModel model)`. On invalid ModelState: re-fetch `group`/`members`/`availableUsers` (via `groupService.GetByIdAsync(id)`, `groupService.GetMembersAsync(id)`, `userService.GetAvailableUsersAsync(id, null)`), and `return View(nameof(Members), new GroupMembersViewModel { Group = group, Members = members, AvailableUsers = availableUsers, CreateMember = model })` so the modal re-renders with validation errors (guard: if `group == null` return `RedirectToAction(nameof(Index))`).

    Otherwise call `var result = await userService.CreateOrAddToGroupAsync(model.Email, model.Name, id, model.GroupRole);` and switch on `result.Outcome`, copying the `AdminController.CreateUser` bodies verbatim with these substitutions: use `id` wherever AdminController uses `groupId.Value`; resolve the group for the `AddedToGroup` email via `groupService.GetByIdAsync(id)`; and replace every `this.RedirectWithSuccess(nameof(Users), ...)` / `this.RedirectWithWarning(nameof(Users), ...)` with a `TempData["Success"] = ...;` (or `TempData["Warning"] = ...;`) assignment followed by `return RedirectToAction(nameof(Members), new { id });` — because the helpers cannot carry the `id` route value. Preserve the exact flash strings verbatim (D-07): NewAccountCreated → `$"Account created for {model.Name}. A welcome email with a set-password link has been sent."`; AddedToGroup and AddedToGroupStrandedAccount → `$"{result.Name} has been added to the group as {model.GroupRole}. A notification email has been sent."`; AlreadyMember → `$"{result.Name} is already a member of this group."` (as a Warning). On `Failed`/default: add `result.Errors` to `ModelState` and re-render the `Members` view with the `CreateMember` model (same re-render path as the invalid-ModelState branch).
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln 2>&1 | grep -qi "Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `GroupController.cs` constructor contains `IIdentityService identityService`, `IBackgroundJobClient jobClient`, and `ILogger<GroupController> logger`
    - `GroupController.cs` does NOT contain `IActiveGroupContext` or `activeGroupContext` anywhere in the file
    - `GroupController.cs` contains `public async Task<IActionResult> CreateMember(int id, CreateMemberViewModel model)`
    - `GroupController.cs` `CreateMember` contains `CreateOrAddToGroupAsync(model.Email, model.Name, id, model.GroupRole)`
    - `GroupController.cs` `CreateMember` contains the exact string `A welcome email with a set-password link has been sent.` and `is already a member of this group.`
    - `GroupController.cs` `CreateMember` uses `RedirectToAction(nameof(Members), new { id })` (not the `RedirectWithSuccess`/`RedirectWithWarning` helpers, which cannot carry `id`)
    - `dotnet build QuestBoard.sln` reports Build succeeded
  </acceptance_criteria>
  <done>`CreateMember` reuses `CreateOrAddToGroupAsync` with `groupId` from the route, fires the four Phase-39 flash outcomes verbatim, redirects back to the correct group's Members page, and the controller never references `IActiveGroupContext`.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — search round-trip, search-preserving Add, CreateMember outcomes</name>
  <files>QuestBoard.IntegrationTests/Controllers/GroupManagementIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/GroupManagementIntegrationTests.cs (the file being modified; lines 168-257 hold the existing `MembersPage`, `AddMember`, `RemoveMember` tests — reuse `TestDataHelper.ClearDatabaseAsync`, `AuthenticationHelper.CreateAuthenticatedSuperAdminClientAsync`, `CreateAuthenticatedClientWithUserAsync`, direct `QuestBoardContext` UserGroups seeding, and `TestContext.Current.CancellationToken`. NOTE: the existing `AddMember_ValidUserAndGroup` test posts `AddMember.UserId`/`AddMember.Role` with the prefix — after Plan 02 Task 1 removes `[Bind(Prefix = "AddMember")]`, update that test to post top-level `UserId`/`Role` fields)
    - QuestBoard.Service/Areas/Platform/Controllers/GroupController.cs (the actions under test after Tasks 1-2)
  </read_first>
  <action>
    Add integration tests to `GroupManagementIntegrationTests.cs`:

    (a) Search round-trip: seed a non-member user with a distinctive Name, GET `/platform/Group/Members/1?search={distinctivepart}`, assert 200 and the response body Contains that user's Name and Contains the search term echoed into the search input `value`. Also GET with a `search` that matches nothing and assert the seeded user's Name is NOT present.

    (b) Add preserves search: POST `/platform/Group/AddMember/1` with top-level form fields `UserId={id}`, `Role={Player}`, and `search={term}`; assert the response redirects (302/Found) and that the `Location` header contains `search={term}` (D-04). Assert the UserGroups row was created.

    (c) CreateMember new account: POST `/platform/Group/CreateMember/1` with a brand-new unique `Email`, `Name`, `GroupRole`; assert redirect and that a new user + a group-1 UserGroups row now exist in the DB.

    (d) CreateMember collision / already-member: create a user already in group 1, POST `/platform/Group/CreateMember/1` with that user's Email; assert redirect and that no duplicate UserGroups row was created (still exactly one membership row for that user in group 1).

    (e) Route-scoped groupId: create a second group, POST `/platform/Group/CreateMember/{group2Id}` with a new Email; assert the new user's UserGroups row is for `group2Id`, NOT group 1 — proving groupId comes from the route, not any session default.

    Update the pre-existing `AddMember_ValidUserAndGroup_ShouldAddUserGroupsRow` test to post top-level `UserId`/`Role` (drop the `AddMember.` prefix) so it still passes after the `[Bind(Prefix)]` removal.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln 2>&1 | grep -qi "Build succeeded" && dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~GroupManagementIntegrationTests</automated>
  </verify>
  <acceptance_criteria>
    - `GroupManagementIntegrationTests.cs` contains a test posting to `/platform/Group/CreateMember/` and asserting a UserGroups row for the route's group id
    - `GroupManagementIntegrationTests.cs` contains a test asserting the AddMember redirect `Location` header contains `search=`
    - `GroupManagementIntegrationTests.cs` contains a test proving CreateMember scopes membership to the route group id (not group 1) when posting to a second group
    - The pre-existing `AddMember_ValidUserAndGroup_ShouldAddUserGroupsRow` posts top-level `UserId`/`Role` (no `AddMember.` prefix)
    - `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~GroupManagementIntegrationTests` exits 0
  </acceptance_criteria>
  <done>Integration tests cover the search round-trip, the search-preserving Add redirect, and CreateMember's create/collision/route-scoping behavior, all green; the adapted AddMember test posts top-level fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → GroupController | Untrusted `id` (route), `search` (query), `UserId`/`Role`/`Email`/`Name` (form) cross into the controller |
| GroupController → UserService (Phase 39) | Controller delegates create/add to the shared collision-aware method |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Elevation of Privilege | `CreateMember` groupId sourcing | mitigate | `groupId` comes only from the route `id` parameter; `GroupController` never injects `IActiveGroupContext` (asserted by acceptance criterion + integration test (e) proving route-scoping). Directly closes the Phase 30 / 34.3 bug class. |
| T-40-05 | Tampering/Spoofing (CSRF) | `AddMember`, `CreateMember` POST actions | mitigate | Both actions carry `[ValidateAntiForgeryToken]` (existing convention on every GroupController POST); the Plan-03 forms emit antiforgery tokens. |
| T-40-06 | Elevation of Privilege | crafted POST to CreateMember/AddMember for an arbitrary group | accept | `GroupController` is `[Authorize(Policy = "SuperAdminOnly")]` class-level; SuperAdmins are trusted to manage any group by design (Platform surface). No per-group ownership check needed (RESEARCH.md Security Domain). |
| T-40-07 | Input Validation | `CreateMemberViewModel` fields | mitigate | Data annotations (`[Required][EmailAddress]` Email, `[Required][StringLength(100)]` Name) validate before the action body; invalid ModelState re-renders the Members view with errors. |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: N/A). No install tasks present. |
</threat_model>

<verification>
- `dotnet build QuestBoard.sln` succeeds.
- `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~GroupManagementIntegrationTests` passes.
- Full-file grep of `GroupController.cs` for `IActiveGroupContext`/`activeGroupContext` returns zero matches.
</verification>

<success_criteria>
- MEMBERS-02: `Members` GET filters via `GetAvailableUsersAsync(id, search)` and echoes the term; no in-memory `GetAllAsync` filtering remains.
- MEMBERS-03: `CreateMember` creates/adds a user scoped to the route group with the four Phase-39 outcomes.
- D-04: Add-to-group redirect preserves the `search` query string.
- D-06: `GroupController` sources `groupId` only from the route; no `IActiveGroupContext`.
</success_criteria>

<output>
Create `.planning/phases/40-platform-members-page-redesign/40-02-SUMMARY.md` when done
</output>
