---
phase: 40-platform-members-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Domain/Interfaces/IUserRepository.cs
  - QuestBoard.Domain/Interfaces/IUserService.cs
  - QuestBoard.Repository/UserRepository.cs
  - QuestBoard.Domain/Services/UserService.cs
  - QuestBoard.Service/ViewModels/PlatformViewModels/GroupMembersViewModel.cs
  - QuestBoard.Service/ViewModels/PlatformViewModels/CreateMemberViewModel.cs
  - QuestBoard.IntegrationTests/Controllers/GroupManagementIntegrationTests.cs
autonomous: true
requirements: [MEMBERS-02, MEMBERS-03]
must_haves:
  truths:
    - "A SuperAdmin's available-users query returns only users who are NOT members of the given group"
    - "The available-users query filters by a search term matching Name or Email (case-insensitive)"
    - "The Members page ViewModel can carry the current search term and a Create New User form model"
  artifacts:
    - path: "QuestBoard.Repository/UserRepository.cs"
      provides: "GetAvailableUsers(int groupId, string? search, CancellationToken) negated manual-join query"
      contains: "GetAvailableUsers"
    - path: "QuestBoard.Domain/Interfaces/IUserRepository.cs"
      provides: "GetAvailableUsers interface signature"
      contains: "GetAvailableUsers"
    - path: "QuestBoard.Domain/Interfaces/IUserService.cs"
      provides: "GetAvailableUsersAsync interface signature"
      contains: "GetAvailableUsersAsync"
    - path: "QuestBoard.Domain/Services/UserService.cs"
      provides: "GetAvailableUsersAsync delegate to repository"
      contains: "GetAvailableUsersAsync"
    - path: "QuestBoard.Service/ViewModels/PlatformViewModels/CreateMemberViewModel.cs"
      provides: "Create New User modal form model (Email, Name, GroupRole)"
      contains: "class CreateMemberViewModel"
    - path: "QuestBoard.Service/ViewModels/PlatformViewModels/GroupMembersViewModel.cs"
      provides: "SearchQuery + CreateMember properties on the Members page ViewModel"
      contains: "SearchQuery"
  key_links:
    - from: "QuestBoard.Domain/Services/UserService.cs"
      to: "QuestBoard.Repository/UserRepository.cs"
      via: "GetAvailableUsersAsync delegates to repository.GetAvailableUsers"
      pattern: "repository\\.GetAvailableUsers"
---

<objective>
Build the backend seam and ViewModel shapes the Platform Members redesign depends on: a DB-side "users not in this group, matching search" query (MEMBERS-02), the paired service delegate, and the reshaped `GroupMembersViewModel` plus a new `CreateMemberViewModel` (MEMBERS-03 form target). This plan introduces net-new symbols only — no controller wiring or view markup — so it can complete independently before the controller (Plan 02) and views (Plan 03) consume it.

Purpose: Push the not-in-group + search filter into a single EF query (replacing today's in-memory `GetAllAsync().Where(...)`), and give the Members view a ViewModel that can echo the search term and bind the Create New User modal.
Output: `GetAvailableUsers`/`GetAvailableUsersAsync` methods, reshaped `GroupMembersViewModel`, new `CreateMemberViewModel`, and integration coverage of the query's not-in-group + search behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-platform-members-page-redesign/40-CONTEXT.md
@.planning/phases/40-platform-members-page-redesign/40-RESEARCH.md
@.planning/phases/40-platform-members-page-redesign/40-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created across all three Phase 40 plans (exclude from drift/source-grounding checks):
- `IUserRepository.GetAvailableUsers(int groupId, string? search, CancellationToken)` — new interface method
- `UserRepository.GetAvailableUsers(...)` — new repository implementation
- `IUserService.GetAvailableUsersAsync(int groupId, string? search, CancellationToken)` — new interface method
- `UserService.GetAvailableUsersAsync(...)` — new service delegate
- `CreateMemberViewModel` — new class (`QuestBoard.Service/ViewModels/PlatformViewModels/CreateMemberViewModel.cs`) with `Email`, `Name`, `GroupRole` properties
- `GroupMembersViewModel.SearchQuery` (string?) and `GroupMembersViewModel.CreateMember` (CreateMemberViewModel) — new properties
- `GroupController.CreateMember` — new POST action (Plan 02)
- `GroupController.Members` gains a `string? search` parameter; `GroupController.AddMember` gains a `string? search` parameter (Plan 02)
- New `createMemberModal` Bootstrap modal + per-row Add form markup in `Members.cshtml`/`Members.Mobile.cshtml` (Plan 03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GetAvailableUsers query (repository + service + interfaces)</name>
  <files>QuestBoard.Domain/Interfaces/IUserRepository.cs, QuestBoard.Repository/UserRepository.cs, QuestBoard.Domain/Interfaces/IUserService.cs, QuestBoard.Domain/Services/UserService.cs, QuestBoard.IntegrationTests/Controllers/GroupManagementIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.Repository/UserRepository.cs (the file being modified; lines 50-59 hold `GetAllGroupMembers`, the exact manual-join shape to negate; note `DbSet`, `DbContext.UserGroups`, `Mapper.Map` members available on the base class)
    - QuestBoard.Domain/Interfaces/IUserRepository.cs (add the sibling signature next to `GetAllGroupMembers`; note existing XML-doc convention)
    - QuestBoard.Domain/Interfaces/IUserService.cs (add the `Async`-suffixed signature next to `GetAllGroupMembersAsync` at line 49)
    - QuestBoard.Domain/Services/UserService.cs (lines 54-58 show the `GetAllGroupMembersAsync` delegate pattern to mirror)
    - QuestBoard.IntegrationTests/Controllers/GroupManagementIntegrationTests.cs (test file; note `TestDataHelper.ClearDatabaseAsync`, `AuthenticationHelper.CreateAuthenticatedClientWithUserAsync`, direct `QuestBoardContext` UserGroups seeding via `_factory.Services.CreateScope()`, and `TestContext.Current.CancellationToken` usage)
  </read_first>
  <behavior>
    - Test 1 (not-in-group): a user with no UserGroups row for group 1 IS returned by GetAvailableUsers(1, null); a user WITH a UserGroups row for group 1 is NOT returned.
    - Test 2 (search by name): GetAvailableUsers(1, "partialname") returns a non-member whose Name contains the term and excludes non-members whose Name/Email do not match.
    - Test 3 (search by email): GetAvailableUsers(1, "partialemail") returns a non-member whose Email contains the term.
    - Test 4 (null/empty search): GetAvailableUsers(1, null) and GetAvailableUsers(1, "") return all non-members unfiltered.
  </behavior>
  <action>
    Add `Task<IList<User>> GetAvailableUsers(int groupId, string? search, CancellationToken token = default)` to `IUserRepository` (with an XML-doc summary describing "users who are NOT members of the given group, optionally filtered by a search term matching Name or Email"), and implement it in `UserRepository` adjacent to `GetAllGroupMembers` using `/// <inheritdoc/>`. The implementation NEGATES the `GetAllGroupMembers` predicate: `DbSet.Where(u => !DbContext.UserGroups.Any(ug => ug.UserId == u.Id && ug.GroupId == groupId))`, then conditionally appends `query = query.Where(u => u.Name.Contains(search) || (u.Email != null && u.Email.Contains(search)))` only when `!string.IsNullOrWhiteSpace(search)`, then `await query.ToListAsync(cancellationToken: token)` and `Mapper.Map<IList<User>>(entities)`. Take `groupId` as a plain int parameter — do NOT read from `activeGroupContext` (mirror `GetAllGroupMembers`, not `GetAllDungeonMasters`/`GetAllPlayers`). Do not add case-handling beyond the plain `Contains` — SQL Server default collation makes it case-insensitive.

    Add `Task<IList<User>> GetAvailableUsersAsync(int groupId, string? search, CancellationToken token = default)` to `IUserService` (next to `GetAllGroupMembersAsync`, with matching XML-doc) and implement in `UserService` as a one-line delegate: `return await repository.GetAvailableUsers(groupId, search, token);`.

    Add integration tests to `GroupManagementIntegrationTests.cs` that resolve `IUserService` (or `IUserRepository`) from a `_factory.Services.CreateScope()` and assert Tests 1-4 above against group 1 (EuphoriaInn, seeded by `ClearDatabaseAsync`). Seed non-member users via `CreateAuthenticatedClientWithUserAsync` (then remove any auto-seeded group-1 UserGroups row so the user is a genuine non-member) and member users by directly adding a `UserGroupEntity` for group 1. Assert on the returned users' `Id`/`Name`/`Email`.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln 2>&1 | grep -qi "Build succeeded" && dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~GroupManagementIntegrationTests</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoard.Domain/Interfaces/IUserRepository.cs` contains `GetAvailableUsers(int groupId, string? search`
    - `QuestBoard.Repository/UserRepository.cs` contains `public async Task<IList<User>> GetAvailableUsers(` and contains `!DbContext.UserGroups`
    - `QuestBoard.Repository/UserRepository.cs` `GetAvailableUsers` body does NOT contain `activeGroupContext`
    - `QuestBoard.Domain/Interfaces/IUserService.cs` contains `GetAvailableUsersAsync(int groupId, string? search`
    - `QuestBoard.Domain/Services/UserService.cs` contains `repository.GetAvailableUsers(groupId, search`
    - `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~GroupManagementIntegrationTests` exits 0 with the four new available-users tests passing
  </acceptance_criteria>
  <done>The available-users query exists at repository + service + interface level, sources groupId from its parameter (never session), filters by not-in-group and by Name/Email search, and is covered by passing integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add CreateMemberViewModel and reshape GroupMembersViewModel</name>
  <files>QuestBoard.Service/ViewModels/PlatformViewModels/CreateMemberViewModel.cs, QuestBoard.Service/ViewModels/PlatformViewModels/GroupMembersViewModel.cs</files>
  <read_first>
    - QuestBoard.Service/ViewModels/AdminViewModels/CreateUserViewModel.cs (the exact field shape to mirror: Email `[Required][EmailAddress][Display(Name="Email")]`, Name `[Required][StringLength(100)][Display(Name="Display Name")]`, GroupRole `[Display(Name="Group Role")]` defaulting to `GroupRole.Player`)
    - QuestBoard.Service/ViewModels/PlatformViewModels/GroupMembersViewModel.cs (the file being modified; current props: Group, Members, AddMember, AvailableUsers)
    - QuestBoard.Service/ViewModels/PlatformViewModels/AddMemberViewModel.cs (unchanged — kept and repurposed as the per-row Add binding target in Plan 02; note its `UserId` + `Role` shape)
  </read_first>
  <action>
    Create `QuestBoard.Service/ViewModels/PlatformViewModels/CreateMemberViewModel.cs` in namespace `QuestBoard.Service.ViewModels.PlatformViewModels`, mirroring `CreateUserViewModel` field-for-field: `Email` (`[Required][EmailAddress][Display(Name = "Email")]`, default `string.Empty`), `Name` (`[Required][StringLength(100)][Display(Name = "Display Name")]`, default `string.Empty`), `GroupRole` (`[Display(Name = "Group Role")]`, default `GroupRole.Player`). Add `using QuestBoard.Domain.Enums;` and `using System.ComponentModel.DataAnnotations;`.

    In `GroupMembersViewModel`, add `public string? SearchQuery { get; set; }` and `public CreateMemberViewModel CreateMember { get; set; } = new();`. Keep the existing `AddMember`, `Group`, `Members`, and `AvailableUsers` properties unchanged — `AddMember` is repurposed in Plan 02 as the per-row binding target, so do not remove it here.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.sln 2>&1 | grep -qi "Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `QuestBoard.Service/ViewModels/PlatformViewModels/CreateMemberViewModel.cs` contains `class CreateMemberViewModel`, `public string Email`, `public string Name`, `public GroupRole GroupRole`
    - `CreateMemberViewModel.cs` contains `[EmailAddress]` and `[StringLength(100)]`
    - `QuestBoard.Service/ViewModels/PlatformViewModels/GroupMembersViewModel.cs` contains `public string? SearchQuery` and `public CreateMemberViewModel CreateMember`
    - `GroupMembersViewModel.cs` still contains `public AddMemberViewModel AddMember`
    - `dotnet build QuestBoard.sln` reports Build succeeded
  </acceptance_criteria>
  <done>`CreateMemberViewModel` exists with validation matching `CreateUserViewModel`; `GroupMembersViewModel` carries `SearchQuery` and a nested `CreateMember` model while retaining `AddMember`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → GroupController → UserService/UserRepository | Untrusted `groupId`/`search` route+query values reach the DB query (this plan only builds the query method; the controller boundary is Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Elevation of Privilege | `GetAvailableUsers` groupId sourcing | mitigate | `groupId` is a plain method parameter; the method never reads `IActiveGroupContext` (asserted by acceptance criterion "does NOT contain activeGroupContext"). Prevents the session/route confusion bug class (Phase 30, 34.3). |
| T-40-02 | Tampering (SQL injection) | `search` predicate in `GetAvailableUsers` | accept | `search` flows through EF Core `.Where(u => u.Name.Contains(search))` — parameterized by EF, no raw SQL. No additional mitigation needed. |
| T-40-03 | Information Disclosure | available-users query returns all platform non-members | accept | By design — the Platform Members page is `SuperAdminOnly` (enforced class-level on `GroupController` in Plan 02); SuperAdmins are trusted to see and manage all platform users. |
| T-40-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: N/A, zero packages). No install tasks present. |
</threat_model>

<verification>
- `dotnet build QuestBoard.sln` succeeds.
- `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~GroupManagementIntegrationTests` passes, including the four new available-users tests.
- `GetAvailableUsers` in `UserRepository.cs` contains no reference to `activeGroupContext`.
</verification>

<success_criteria>
- MEMBERS-02 (query layer): a group-scoped, search-filtered "not in this group" query exists at the DB layer and is covered by tests.
- MEMBERS-03 (form model): `CreateMemberViewModel` exists and is nested on `GroupMembersViewModel`.
- No new package installs; no `IActiveGroupContext` in the new query.
</success_criteria>

<output>
Create `.planning/phases/40-platform-members-page-redesign/40-01-SUMMARY.md` when done
</output>
