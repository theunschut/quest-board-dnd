---
phase: 57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.UnitTests/Repository/ContactRepositoryTests.cs
  - QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A failing (RED) ContactRepositoryTests.cs exists asserting note ordering (newest-first), GroupId scoping, and image round-trip"
    - "A failing (RED) ContactsControllerIntegrationTests.cs exists asserting the D-09b/D-12/D-13/D-15/D-15b role-and-visibility matrix and D-09 note collaboration"
  artifacts:
    - path: "QuestBoard.UnitTests/Repository/ContactRepositoryTests.cs"
      provides: "Repository-level unit test scaffold for Contacts (ordering, group-scope, image round-trip)"
      contains: "class ContactRepositoryTests"
    - path: "QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs"
      provides: "Controller integration test scaffold for the visibility/auth matrix"
      contains: "class ContactsControllerIntegrationTests"
  key_links:
    - from: "QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs"
      to: "QuestBoard.IntegrationTests/WebApplicationFactoryBase"
      via: "test class inherits the existing integration harness"
      pattern: "WebApplicationFactoryBase|IClassFixture"
---

<objective>
Author the Wave 0 failing tests (Nyquist scaffolds) that specify Contact behavior before any production code exists. These two test files are the executable specification for the entire phase: `ContactRepositoryTests.cs` (unit) and `ContactsControllerIntegrationTests.cs` (integration).

Purpose: Every later plan's `<verify>` runs a subset of these tests. Writing them first locks the observable contract (note ordering D-10, group scoping, D-09b policy gating, D-12/D-13 hidden-Contact 404, D-15 three-branch visibility, D-15b session toggle, D-09 note collaboration) so executors implement to a fixed target.
Output: Two RED test files mirroring the existing Character test analogs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-VALIDATION.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (full-phase manifest — this plan authors the test scaffolds only)

Test files created by THIS plan:
- `QuestBoard.UnitTests/Repository/ContactRepositoryTests.cs`
- `QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs`

Production symbols these tests reference (created in later plans — tests compile-fail until then, which is the intended RED state):
- Entities: `ContactEntity`, `ContactImageEntity`, `ContactNoteEntity` (Plan 02)
- Domain: `Contact`, `ContactNote`, `IContactRepository`, `IContactService`, `ContactService`, `ContactRepository` (Plan 03)
- Service: `ContactsController`, `ContactViewModel`, `ContactsIndexViewModel`, `ContactNoteViewModel`, `SessionKeys.ShowHiddenContactsKey` (Plan 04)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author ContactRepositoryTests.cs (RED unit scaffold)</name>
  <files>QuestBoard.UnitTests/Repository/ContactRepositoryTests.cs</files>
  <read_first>
    - QuestBoard.UnitTests/Repository/CharacterRepositoryTests.cs (the exact analog — mirror its fixture setup, InMemory/DbContext harness, GroupId-scoping assertions, and image round-trip test verbatim, renamed)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md (Wave 0 Gaps section; Pattern 3 for ContactNoteEntity shape; D-10 note ordering via OrderByDescending(CreatedAt))
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md (D-10 newest-first, D-14 default-hidden, D-17 alphabetical index order)
  </read_first>
  <action>
    Create `ContactRepositoryTests` mirroring `CharacterRepositoryTests` structure. Reference production symbols that do not yet exist (`ContactEntity`, `ContactImageEntity`, `ContactNoteEntity`, `ContactRepository`, `Contact`, `ContactNote`) — the file WILL compile-fail; that is the intended RED state for this Wave 0 scaffold.
    Cover exactly these behaviors as separate `[Fact]` methods:
    (1) `GetAllContactsWithDetailsAsync` returns contacts ordered alphabetically by `Name` ascending (D-17) — seed out-of-order names, assert sorted.
    (2) Notes for a contact are returned newest-first via `OrderByDescending(CreatedAt)` (D-10) — seed 3 notes with distinct `CreatedAt`, assert descending order. Use `CreatedAt`, not `Id`, as the discriminator (seed so Id order and CreatedAt order deliberately disagree).
    (3) GroupId scoping: a contact in group 2 is NOT returned when the active group context resolves to group 1 (fail-closed filter) — reuse the exact active-group-context test double `CharacterRepositoryTests` uses to set `ActiveGroupId`.
    (4) Image round-trip: a contact created with image bytes returns those exact bytes via `GetContactImageAsync`/`GetContactProfilePictureAsync`-equivalent.
    (5) `AddNoteAsync` / `DeleteNoteAsync` round-trip: add a note, assert it is retrievable; delete it, assert gone (D-08/D-09 dedicated note methods, per RESEARCH.md Pitfall 4 Option (a)).
    Use the same `TestDataHelper`/seed helpers and `GroupId` conventions the Character repository tests use (per Phase 49's `TestDataHelper GroupId fix`). Do NOT invent a new fixture harness.
  </action>
  <verify>
    <automated>MISSING — this file is the Wave 0 RED scaffold; it will compile-fail until Plans 02-03 land. After Plan 03, `dotnet test QuestBoard.UnitTests --filter FullyQualifiedName~ContactRepositoryTests` must compile and the ordering/scope/image/note facts must pass.</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file declares `public class ContactRepositoryTests` with at least 5 `[Fact]` methods covering ordering (D-17), note newest-first (D-10), group scoping, image round-trip, and note add/delete.
    - Behavior assertion: the note-ordering fact seeds notes whose `Id` order and `CreatedAt` order disagree, proving the sort key is `CreatedAt` not `Id`.
    - The file references `ContactRepository`, `ContactEntity`, `ContactNoteEntity` by name (compile-fail is expected and acceptable at this wave).
  </acceptance_criteria>
  <done>ContactRepositoryTests.cs exists, mirrors CharacterRepositoryTests structure, and specifies ordering/scope/image/note behaviors as distinct facts referencing the not-yet-created Contact types.</done>
</task>

<task type="auto">
  <name>Task 2: Author ContactsControllerIntegrationTests.cs (RED integration scaffold — the visibility/auth matrix)</name>
  <files>QuestBoard.IntegrationTests/Controllers/ContactsControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/CharactersControllerIntegrationTests.cs (the exact analog — mirror its WebApplicationFactoryBase harness, authenticated-client helpers, and role-matrix `[Fact]` style, especially the `Edit_AdminEditingAnotherPlayersCharacter_ShouldSucceed`-style role tests)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md (Pitfall 1: DungeonMasterOnly policy vs GetEffectiveGroupRoleAsync; Pitfall 2: three-branch visibility as controller check not query filter; Security Domain threat table)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md (D-09b, D-12, D-13, D-14, D-15 three branches, D-15b session toggle, D-09 note collaboration)
  </read_first>
  <action>
    Create `ContactsControllerIntegrationTests` mirroring `CharactersControllerIntegrationTests`. Use the existing `WebApplicationFactoryBase` harness and its authenticated-client helpers (Player, DungeonMaster, Admin/SuperAdmin roles). Reference `ContactsController` routes (`/Contacts/Index`, `/Contacts/Details/{id}`, `/Contacts/Create`, `/Contacts/Edit/{id}`, `/Contacts/Delete/{id}`, `/Contacts/ToggleReveal/{id}`, `/Contacts/ToggleShowHidden`, `/Contacts/AddNote`, `/Contacts/EditNote`, `/Contacts/DeleteNote`) — these do not yet exist; compile-fail is the intended RED state.
    Cover exactly these facts:
    (1) D-09b: a Player GET/POST to `Create`, `Edit`, `Delete`, `ToggleReveal` receives 403/redirect (blocked); a DungeonMaster and an Admin succeed.
    (2) D-14: a Contact created via `Create` POST has `IsRevealed == false` in the response/redirect-then-Details state.
    (3) D-12/D-13: a hidden Contact is absent from a Player's Index list, and a direct `Details/{id}` GET for that hidden Contact returns `NotFound` (404) for the Player — same 404 convention as Phase 49/55.
    (4) D-15 branch 1 (creator exception): the DM-tier user who created a hidden Contact sees it on their own Index and Details regardless of toggle state.
    (5) D-15 branch 2 (DM-tier toggle): a *different* DM-tier user does NOT see the hidden Contact with toggle OFF, but DOES see it after POSTing `ToggleShowHidden` (toggle ON).
    (6) D-15 branch 3 (Player never): a plain Player never sees the hidden Contact regardless of any toggle state.
    (7) D-15b: the toggle is per-group and session-scoped — toggling ON for group 1 does not reveal hidden contacts when the active group is group 2 (assert per-group key isolation).
    (8) D-09: any group member (Player) can POST `AddNote`, `EditNote`, and `DeleteNote` on a note authored by a *different* user — no ownership guard blocks them.
    (9) Cross-tenant IDOR: a Details/{id} GET for a Contact belonging to another group returns 404.
    Mark session-expiry-timing behavior (part of D-15b) as manual-only per 57-VALIDATION.md — do NOT attempt to assert distributed-session clock expiry in this fast integration test; assert only the per-group key isolation, not the expiry reset.
  </action>
  <verify>
    <automated>MISSING — this file is the Wave 0 RED scaffold; it will compile-fail until Plan 04 lands. After Plan 04, `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~ContactsControllerIntegrationTests` must compile and the auth/visibility/note facts must pass.</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file declares `public class ContactsControllerIntegrationTests` with distinct `[Fact]` methods for each of the 9 behaviors above (D-09b block, D-14 default-hidden, D-12/D-13 404, D-15 branches 1/2/3, D-15b per-group isolation, D-09 note collaboration, cross-tenant IDOR).
    - Behavior assertion: the D-15 branch-2 fact toggles `ShowHidden` via a POST to `ToggleShowHidden` and asserts the hidden Contact appears only after the toggle.
    - The file uses `WebApplicationFactoryBase` (or the project's existing integration harness base) — no new harness is introduced.
  </acceptance_criteria>
  <done>ContactsControllerIntegrationTests.cs exists, mirrors the Character integration test harness, and specifies the full D-09b/D-12/D-13/D-15/D-15b/D-09 matrix as distinct facts referencing the not-yet-created ContactsController routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → ContactsController | Untrusted role/session input crosses here; tests assert the boundary is enforced |
| viewer identity → hidden-Contact visibility | The three-branch D-15 rule is the security-relevant decision these tests pin down |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | Hidden Contact leak via role alone | mitigate | Integration facts (D-15 branches 1/2/3) prove creator-only + toggle-gated + player-never visibility before implementation exists (per D-15) |
| T-57-02 | Elevation of Privilege | Player invoking Create/Edit/Delete/ToggleReveal | mitigate | Fact (1) asserts Player is blocked, DM-tier succeeds (per D-09b) |
| T-57-03 | Information Disclosure | Cross-tenant IDOR on Details/{id} | mitigate | Fact (9) asserts 404 for cross-group Contact id (Phase 49/55 convention) |
| T-57-SC | Tampering | npm/NuGet installs | accept | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable — zero external packages) |
</threat_model>

<verification>
- Both test files exist and follow the existing Character test analog structure.
- Files compile-fail only because the Contact production types do not yet exist (intended RED state for Wave 0). No other compile errors (typos, wrong harness usage) are acceptable.
- After Plans 02-04 land, `dotnet test` runs both suites green.
</verification>

<success_criteria>
- `ContactRepositoryTests.cs` specifies ordering (D-17), note newest-first (D-10), group scoping, image round-trip, and note add/delete as distinct facts.
- `ContactsControllerIntegrationTests.cs` specifies the full D-09b/D-12/D-13/D-14/D-15/D-15b/D-09 + cross-tenant IDOR matrix as distinct facts.
- No new test framework/harness introduced — both files reuse existing xUnit + WebApplicationFactoryBase infrastructure.
</success_criteria>

<output>
Create `.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-01-SUMMARY.md` when done
</output>
