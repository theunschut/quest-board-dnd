---
phase: 57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na
plan: 05
type: execute
wave: 4
depends_on: ["57-04"]
files_modified:
  - QuestBoard.Service/Views/Contacts/Index.cshtml
  - QuestBoard.Service/Views/Contacts/Details.cshtml
  - QuestBoard.Service/Views/Contacts/Edit.cshtml
  - QuestBoard.Service/Views/Contacts/Create.cshtml
  - QuestBoard.Service/wwwroot/css/contacts.css
  - QuestBoard.Service/Views/Shared/_Layout.cshtml
  - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Desktop Index/Details/Edit/Create views render the Contacts feature with the modern-card layout and the UI-SPEC's exact CSS classes, copy strings, and button styles"
    - "Index shows a flat alphabetical list with a '+ Contact' primary button (btn-warning, DM-tier) and a two-state 'Show Hidden'/'Hide Hidden' toggle (DM-tier), plus a muted secondary Hidden badge on hidden Contacts"
    - "Details shows portrait/actions/description/notes cards; the Notes card has an add-note form (btn-warning 'Add Note') and a newest-first list with inline edit-in-place plus native-confirm delete per note"
    - "The Actions card exposes a Reveal Contact (btn-success) or Hide Contact (btn-secondary) POST toggle to ToggleReveal between Edit and Delete, gated on CanManage"
    - "A 'Contacts' nav link appears for all users on both board types in desktop and mobile layouts"
  artifacts:
    - path: "QuestBoard.Service/Views/Contacts/Index.cshtml"
      provides: "Contacts list view (flat alphabetical, two-state Show Hidden toggle, Hidden badges)"
      min_lines: 30
    - path: "QuestBoard.Service/Views/Contacts/Details.cshtml"
      provides: "Contact detail view incl. notes UI (add-note form + inline edit/delete)"
      min_lines: 60
    - path: "QuestBoard.Service/wwwroot/css/contacts.css"
      provides: "Contacts card-grid + Hidden-badge + note-item styling mirroring characters.css under a .contacts-page scope"
      contains: ".contacts-page"
  key_links:
    - from: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      to: "QuestBoard.Service/Controllers/Contacts/ContactsController.cs"
      via: "nav <li> asp-controller=Contacts asp-action=Index in the all-board-types block"
      pattern: "asp-controller=\"Contacts\""
    - from: "QuestBoard.Service/Views/Shared/_Layout.cshtml"
      to: "QuestBoard.Service/wwwroot/css/contacts.css"
      via: "global <link> to contacts.css next to the existing characters.css link"
      pattern: "contacts\\.css"
    - from: "QuestBoard.Service/Views/Contacts/Details.cshtml"
      to: "QuestBoard.Service/Controllers/Contacts/ContactsController.cs"
      via: "add-note form posts to AddNote; per-note edit/delete post to EditNote/DeleteNote; Reveal/Hide posts to ToggleReveal"
      pattern: "asp-action=\"AddNote\"|asp-action=\"DeleteNote\"|asp-action=\"ToggleReveal\""
---

<objective>
Build the four desktop Razor views (Index/Details/Edit/Create), a dedicated `contacts.css`, and insert the "Contacts" nav link into both the desktop and mobile shared layouts.

Purpose: This is the user-facing surface that realizes the D-20 layout walkthrough with the pixel-level fidelity of 57-UI-SPEC.md. This plan is the primary consumer of the UI-SPEC's 4 new-pattern specs — the Notes list, the two-state Show Hidden toggle, the Hidden badge, and the Reveal/Hide action — and MUST use the UI-SPEC's exact CSS classes, copy strings, button styles, and interaction mechanics verbatim rather than re-deriving them from CONTEXT.md's more general D-20 prose. Every route, view folder, and nav label uses the locked "Contacts" feature name (D-01) and controller/action naming convention (D-02) — the nav label reads "Contacts", never "NPC". Mobile views ship in Plan 06.
Output: Four desktop views + `contacts.css` + two nav insertions + one global css link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md
@QuestBoard.Service/Views/Characters/Index.cshtml
@QuestBoard.Service/Views/Characters/Details.cshtml
@QuestBoard.Service/Views/Characters/Edit.cshtml
@QuestBoard.Service/Views/Characters/Create.cshtml
@QuestBoard.Service/wwwroot/css/characters.css
@QuestBoard.Service/Views/Shared/_Layout.cshtml
@QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's slice)

Stylesheet:
- `QuestBoard.Service/wwwroot/css/contacts.css` — a `.contacts-page`-scoped mirror of the reused `characters.css` rules: `.contact-grid` (`repeat(auto-fill, minmax(250px, 1fr))`, `gap: 1.5rem`), `.contact-card` (dark-gradient + amber hover-glow, copied from `.character-card`), and a `.hidden-badge` rule copied from `.retired-badge` (top-right, `rgba(108,117,125,.95)` bg, white text, `border-radius: 20px`, `0.75rem`, `backdrop-filter: blur(5px)`) plus a `.note-item` block if any styling beyond Bootstrap utilities is needed

Desktop views (in `QuestBoard.Service/Views/Contacts/`), each wrapped in `<div class="contacts-page">`:
- `Index.cshtml` — flat alphabetical `.contact-grid` list (thumbnail + name + town/city per row); header row (`d-flex justify-content-between align-items-center mb-4`) with the two-state Show Hidden toggle (`btn-outline-secondary`/`fa-eye-slash`/"Show Hidden" when OFF, `btn-secondary`/`fa-eye`/"Hide Hidden" when ON, `aria-pressed`) immediately left of the "+ Contact" `btn-warning` button — both DM-tier only; muted `.hidden-badge` ("Hidden" + `fa-eye-slash`) on unrevealed contacts; "No Contacts Yet" empty state
- `Details.cshtml` — left col portrait card (image via GetContactImage or `fa-user` placeholder, name, town/city, sub-location) + Hidden badge (`badge bg-secondary fs-6`) + Actions card gated on CanManage (Edit `btn-warning`, Reveal `btn-success`/Hide `btn-secondary` toggle to ToggleReveal, Delete `btn-danger` w/ confirm); right col Description card + Notes card (textarea + "Add Note" `btn-warning`, `<hr>`, newest-first `.note-item` list with inline edit-in-place + native-confirm delete, "No Notes Yet" empty state)
- `Edit.cshtml` — form for Name/Image/Description/TownCity/SubLocation (no notes), h4/24px card header
- `Create.cshtml` — "Create New Contact" title, same fields as Edit

Nav insertions:
- `_Layout.cshtml` — Contacts `<li>` after the Characters `<li>`, inside the all-board-types (unconditional) block; a global `<link>` to `contacts.css` next to the existing `characters.css` link (~line 19)
- `_Layout.Mobile.cshtml` — same nav `<li>` insertion, mobile spacing (me-2)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create contacts.css, insert nav links + global css link, and build the desktop Index view (grid + two-state Show Hidden toggle + Hidden badge)</name>
  <files>QuestBoard.Service/wwwroot/css/contacts.css, QuestBoard.Service/Views/Shared/_Layout.cshtml, QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml, QuestBoard.Service/Views/Contacts/Index.cshtml</files>
  <read_first>
    - QuestBoard.Service/wwwroot/css/characters.css (source rules to mirror — `.characters-page .character-grid` ~lines 11-16, `.character-card` ~lines 18-83, `.retired-badge` ~lines 160-173, `.dead-badge` ~lines 175-188; copy the grid/card/badge rules under a `.contacts-page` scope)
    - QuestBoard.Service/Views/Characters/Index.cshtml (analog — the `<div class="characters-page">` wrapper, header row `d-flex justify-content-between align-items-center mb-4`, "+ Character" `btn-warning` header button, `.character-grid`/`.character-card` markup, thumbnail-or-placeholder pattern)
    - QuestBoard.Service/Views/Shared/_Layout.cshtml (existing Characters nav `<li>` ~lines 130-134; the OneShot-only `@if (activeBoardType == BoardType.OneShot)` block boundary ~line 135; the existing `characters.css` `<link>` ~line 19)
    - QuestBoard.Service/Views/Shared/_Layout.Mobile.cshtml (existing Characters nav `<li>`, mobile spacing)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md ("New Pattern Spec 2" Show Hidden toggle — exact two-state button; "New Pattern Spec 3" Hidden badge on Index; "Mirrored Patterns" Index page layout / contact-grid; "Color" `#ffc107` accent; "Copywriting Contract" rows for "+ Contact", "Show Hidden"/"Hide Hidden", "Hidden", "No Contacts Yet" empty state)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md (D-17 flat alphabetical, D-19 nav for all users both board types, D-20 Index layout, D-15 badge/toggle visibility)
    - CLAUDE.md (UI/UX: modern-card conventions; note the two UI-SPEC-sanctioned outline-button exceptions — the toggle OFF state is one of them)
  </read_first>
  <action>
    Create `QuestBoard.Service/wwwroot/css/contacts.css`: copy the reused rules from `characters.css` under a new `.contacts-page` scope — `.contacts-page .contact-grid` (`display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 1.5rem;` per UI-SPEC Mirrored Patterns), `.contacts-page .contact-card` (the dark-gradient `linear-gradient(145deg, rgba(20,20,30,.95), rgba(30,30,40,.95))` + `2px solid rgba(255,193,7,.4)` border + amber hover-glow, copied from `.character-card`), and `.contacts-page .hidden-badge` copied verbatim from `.retired-badge` positioning/style (`position: absolute; top: .5rem; right: .5rem; background: rgba(108,117,125,.95); color: white; padding: .25rem .75rem; border-radius: 20px; font-size: .75rem; font-weight: 600; backdrop-filter: blur(5px);`) — do NOT reuse the dark `.dead-badge` `rgba(33,37,41,.95)` (UI-SPEC New Pattern Spec 2 reserves dark for Dead status). Add a `.contacts-page .note-item` rule only if Bootstrap utilities (`border rounded p-3 mb-3`) are insufficient.
    In `_Layout.cshtml`, add a global stylesheet `<link>` to `~/css/contacts.css` (with `asp-append-version="true"`) immediately next to the existing `characters.css` link (~line 19).
    Nav insertion (both layouts): insert `<li class="nav-item"><a class="nav-link" asp-controller="Contacts" asp-action="Index"><i class="fas fa-address-book me-1"></i>Contacts</a></li>` immediately after the existing Characters `<li>` in `_Layout.cshtml`, positioned BEFORE the `@if (activeBoardType == BoardType.OneShot)` block so it renders for all board types (D-19). Mirror the identical insertion in `_Layout.Mobile.cshtml` using `me-2` spacing to match that file's convention. Do NOT wrap the Contacts `<li>` in any board-type conditional.
    Create `Index.cshtml` typed to `ContactsIndexViewModel`, wrapped in `<div class="contacts-page">`. Page `<h1>` "Contacts" with `fas fa-address-book me-2 text-warning` (mirroring Characters' h1 treatment). Header row `d-flex justify-content-between align-items-center mb-4`: on the right, render (only when `Model.ViewerIsDmTier`) the two-state Show Hidden toggle immediately LEFT of the "+ Contact" button, as a POST form to `ToggleShowHidden` (antiforgery token) — when `Model.ShowHidden == false` the button is `class="btn btn-outline-secondary"` with `<i class="fas fa-eye-slash me-2"></i>Show Hidden` and `aria-pressed="false"`; when `Model.ShowHidden == true` it is `class="btn btn-secondary"` with `<i class="fas fa-eye me-2"></i>Hide Hidden` and `aria-pressed="true"` (UI-SPEC New Pattern Spec 2 exact spec). The "+ Contact" button is `class="btn btn-warning"` with `<i class="fas fa-plus me-2"></i>Contact` (UI-SPEC Copywriting Contract label is `+ Contact`), asp-action="Create", rendered only when `Model.ViewerIsDmTier`.
    Body: a flat alphabetical `.contact-grid` of `.contact-card` items (no My/Other split, D-17), each card a link to `Details/{id}` showing thumbnail via `GetContactImage/{id}` (or `fas fa-user` placeholder per UI-SPEC Mirrored Patterns), the name, and town/city (sub-location as a secondary line if present). On any card where `IsRevealed == false`, render `<span class="hidden-badge"><i class="fas fa-eye-slash me-1"></i>Hidden</span>` (UI-SPEC New Pattern Spec 3). Empty state (no contacts): centered `text-center py-5`, heading "No Contacts Yet", body "No one has added a contact yet. DMs can create the first one to start building out the world." (UI-SPEC Copywriting Contract — Player-safe wording).
    Use CRLF line endings. No phase/decision IDs in comments/markup.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `contacts.css` declares `.contacts-page .contact-grid` (`minmax(250px, 1fr)`, `gap: 1.5rem`), `.contacts-page .contact-card`, and `.contacts-page .hidden-badge` with `rgba(108, 117, 125` (secondary-gray, NOT `rgba(33, 37, 41` dark).
    - Source assertion: `_Layout.cshtml` links `contacts.css`, and both `_Layout.cshtml` and `_Layout.Mobile.cshtml` contain a Contacts nav `<li>` with `asp-controller="Contacts"` placed OUTSIDE any `@if (activeBoardType == BoardType.OneShot)` block.
    - Source assertion: `Index.cshtml` renders the Show Hidden toggle as a `btn-outline-secondary`/"Show Hidden"/`fa-eye-slash` when OFF and `btn-secondary`/"Hide Hidden"/`fa-eye` when ON, both carrying `aria-pressed`, and both the toggle and the `btn-warning` "+ Contact" button are guarded by `Model.ViewerIsDmTier`.
    - Source assertion: hidden rows render `<span class="hidden-badge">` with an `fa-eye-slash` icon and the literal text `Hidden`; the empty state uses the heading `No Contacts Yet`.
    - Source assertion: the list is a single flat `.contact-grid` (no My/Other section split).
    - `dotnet build QuestBoard.Service` succeeds (Razor + static assets compile).
  </acceptance_criteria>
  <done>contacts.css exists with the reused grid/card/hidden-badge rules; the css link and nav links are inserted; Index.cshtml renders the flat grid with the exact UI-SPEC two-state toggle and Hidden badge; the Service project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Create desktop Edit and Create form views</name>
  <files>QuestBoard.Service/Views/Contacts/Edit.cshtml, QuestBoard.Service/Views/Contacts/Create.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Characters/Edit.cshtml, Create.cshtml (analog — form fields, image upload + the client-side MaxFileSize/AllowedExtensions validation `<script>` to reuse verbatim, modern-card shell, `d-flex gap-2` Save+Cancel button row)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md ("Mirrored Patterns" Edit/Create form layout; "Typography" — every card header is h4/24px, no h3/h5; "Copywriting Contract" — Create page title `Create New Contact`, Edit page title `Edit {Contact Name}`; "Color" — Save = `btn-warning`, Cancel = `btn-secondary`)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md (D-16/D-20 — no notes UI on Edit/Create; D-03/D-04/D-05 field semantics)
    - CLAUDE.md (UI/UX: modern-card/modern-card-header/modern-card-body; `<hr>` before the button section; `d-flex justify-content-between` — Cancel left, submit right; filled colored buttons; FontAwesome + me-2)
  </read_first>
  <action>
    Create `Edit.cshtml` and `Create.cshtml` typed to `ContactViewModel`, each a single `modern-card` with a `card-header modern-card-header` whose heading is an `<h4>` (24px per UI-SPEC Typography — do NOT use h3/h5). Create's title reads "Create New Contact"; Edit's reads "Edit @Model.Name" (UI-SPEC Copywriting Contract). Form fields: Name (`asp-for="Name"`, required), Image upload (`asp-for="ContactImageFile"` — on Edit show the current image via `GetContactImage/{id}` above the file input), Description (textarea, maxlength 2000), TownCity, SubLocation. Reuse the Character Create/Edit client-side image-validation `<script>` block verbatim (renamed to the Contact file input id) so oversize/wrong-type is caught client-side too. NO notes UI on these pages (D-16/D-20).
    `<hr>` before the button section; a `d-flex justify-content-between` row with Cancel `btn btn-secondary` (asp-action="Index" or Details) on the LEFT and the submit button on the RIGHT — `btn btn-warning` labelled "Create Contact"/`fa-plus` on Create and "Save Changes"/`fa-save` (or the exact label the Character Edit uses) on Edit. Include `asp-validation-summary="All"`, per-field `asp-validation-for` spans, and the antiforgery token exactly as the Character forms do.
    Use CRLF line endings. No phase/decision IDs in comments/markup.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Create.cshtml` renders the title `Create New Contact`; `Edit.cshtml` renders `Edit @Model.Name` — both in an `<h4>` card header (no h3/h5).
    - Source assertion: both views contain form fields for Name/ContactImageFile/Description/TownCity/SubLocation and contain NO notes/add-note markup (`AddNote`/`note-item` absent).
    - Source assertion: the submit button is `btn-warning` and Cancel is `btn-secondary`, arranged in a `d-flex justify-content-between` row preceded by an `<hr>`; both views use `modern-card`/`modern-card-header`/`modern-card-body`.
    - Source assertion: both forms carry the antiforgery token and `asp-validation-summary`.
    - `dotnet build QuestBoard.Service` succeeds (Razor compiles).
  </acceptance_criteria>
  <done>Desktop Edit/Create form views exist with the exact UI-SPEC titles, h4 headers, btn-warning/btn-secondary button row, reused image validation, and no notes UI; the Service project builds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create desktop Details view (portrait/Hidden-badge/actions + Description + Notes list with inline edit-in-place)</name>
  <files>QuestBoard.Service/Views/Contacts/Details.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Characters/Details.cshtml (analog — two-column `col-lg-4`/`col-lg-8` portrait/actions/info card structure, the status-badge `<div class="mb-2">` block, the ToggleRetirement POST form shape, the Delete `onsubmit="return confirm(...)"` form, the Backstory/Description `white-space: pre-wrap` convention, the back-link)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md ("New Pattern Spec 1" Notes list — the full add-note form + note-item structure, edit-in-place mechanic, native-confirm delete, empty state; "New Pattern Spec 3" Details Hidden badge; "New Pattern Spec 4" Reveal/Hide toggle action placement and button styles; "Copywriting Contract" — "Add Note", "Reveal Contact", "Hide Contact", both delete-confirm strings, both notes empty-state strings; "Typography" h4/24px card headers; the two sanctioned outline-button exceptions for per-row note actions)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md (D-20 Details layout; D-08/D-09/D-10/D-16 notes UI; D-09b Actions DM-gated)
    - CLAUDE.md (UI/UX: modern-card conventions; the two UI-SPEC-sanctioned outline-button exceptions — per-row note Edit/Delete are the other one; note this is the ONE sanctioned departure, not a blanket outline-button license)
  </read_first>
  <action>
    Create `Details.cshtml` typed to `ContactViewModel`, wrapped in `<div class="contacts-page">`, two-column `row` (`col-lg-4` left / `col-lg-8` right) mirroring Character Details. Every `modern-card-header` heading is an `<h4>` (24px per UI-SPEC Typography).
    Left column — portrait card: image via `GetContactImage/{id}` or a `fas fa-user fa-5x` placeholder; name; town/city; sub-location. In a `<div class="mb-2">` below the name (matching where Characters renders its status badge), when `Model.IsRevealed == false` render `<span class="badge bg-secondary fs-6"><i class="fas fa-eye-slash me-1"></i>Hidden</span>` (UI-SPEC New Pattern Spec 3, Details variant).
    Left column — Actions card, gated entirely on `Model.CanManage`: three stacked full-width (`w-100`) buttons in this order (UI-SPEC New Pattern Spec 4) — (1) Edit: `btn btn-warning`, `fa-pen`/"Edit Contact", link to `Edit/{id}`, `mb-2`; (2) a POST form to `ToggleReveal/{id}` (hidden id, antiforgery) rendering, when currently hidden, `<button class="btn btn-success w-100"><i class="fas fa-eye me-2"></i>Reveal Contact</button>`, and when currently revealed, `<button class="btn btn-secondary w-100"><i class="fas fa-eye-slash me-2"></i>Hide Contact</button>` — no confirm dialog (reversible), `mb-2`; (3) Delete: a POST form to `Delete/{id}` with `onsubmit="return confirm('Are you sure you want to delete this contact? This action cannot be undone.')"` rendering `<button class="btn btn-danger w-100">` (UI-SPEC Copywriting Contract exact confirm string).
    Right column — Description card (`white-space: pre-wrap`), then a Notes card (`modern-card`, h4 header "Notes") per UI-SPEC New Pattern Spec 1, visible to ALL group members (not gated on CanManage):
    - Add-note form (POST to `AddNote`, hidden `contactId`, antiforgery): a full-width `<textarea class="form-control" rows="3" maxlength="2000" placeholder="Add a note about this contact...">` bound to note Text, then a right-aligned (`d-flex justify-content-end`) submit `<button class="btn btn-warning"><i class="fas fa-plus me-2"></i>Add Note</button>`. Follow with an `<hr>` dividing the form from the list.
    - Notes list, iterating `Model.Notes` (already newest-first from the repository, D-10): each note is `<div class="note-item border rounded p-3 mb-3">` with a header row `d-flex justify-content-between align-items-start` — left: `<small class="text-muted"><i class="fas fa-user me-1"></i>@note.AuthorName · <i class="fas fa-clock me-1"></i>@note.CreatedAt (formatted e.g. "MMM d, yyyy h:mm tt")</small>` plus ` (edited)` in the same muted small when `note.UpdatedAt != null`; right: two icon-only buttons — `btn btn-sm btn-outline-secondary` (`fa-pen`, `title="Edit note"`) and `btn btn-sm btn-outline-danger` (`fa-trash`, `title="Delete note"`) — these outline buttons are the UI-SPEC-sanctioned per-row exception. Body: `<p class="mb-0 mt-2" style="white-space: pre-wrap;">@note.Text</p>`.
    - Edit-in-place (small vanilla JS, no modal, no framework): clicking the pencil swaps the note body `<p>` for an inline `<textarea>` (same styling as add-note) with `Save` (`btn btn-sm btn-warning`) / `Cancel` (`btn btn-sm btn-secondary`) replacing the two icon buttons, then POSTs to `EditNote`. Implement as a show/hide toggle of two sibling elements (consistent with this app's existing light inline JS on the Character Classes add/remove list). Every group member sees both icon buttons (D-09 — no ownership gate).
    - Delete: the trash icon submits a per-note inline `<form>` POST to `DeleteNote` (hidden note id, antiforgery) guarded by `onsubmit="return confirm('Delete this note? This action cannot be undone.')"` (UI-SPEC exact string).
    - Empty state (no notes): `<div class="text-center py-4"><i class="fas fa-comment-slash fa-2x text-muted mb-2"></i><p class="text-muted mb-0">No Notes Yet</p><p class="text-muted small">Be the first to jot something down about this contact.</p></div>` (UI-SPEC New Pattern Spec 1 — `py-4`/`fa-2x`, smaller than a full-page empty state).
    Add a "Back to Contacts" link (asp-action="Index"). Antiforgery token on every POST form. Use CRLF line endings. No phase/decision IDs in markup/comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the Details Hidden badge is `<span class="badge bg-secondary fs-6">` with `fa-eye-slash` + literal `Hidden`, shown only when `IsRevealed == false`.
    - Source assertion: the Actions card is gated on `Model.CanManage` and contains, in order, an Edit `btn-warning`, a `ToggleReveal` POST form rendering `btn-success`/"Reveal Contact" (hidden) or `btn-secondary`/"Hide Contact" (revealed) with NO confirm, and a Delete `btn-danger` POST form whose `confirm(...)` uses the exact string "Are you sure you want to delete this contact? This action cannot be undone.".
    - Source assertion: the add-note textarea uses `placeholder="Add a note about this contact..."` and the submit button is `btn-warning` labelled `Add Note`; the note list uses `<div class="note-item border rounded p-3 mb-3">` with per-row `btn-outline-secondary` (edit) and `btn-outline-danger` (delete) icon buttons; the delete confirm string is exactly "Delete this note? This action cannot be undone.".
    - Source assertion: the add-note form and per-note edit/delete controls are NOT wrapped in a `Model.CanManage` conditional (D-09 collaborative); notes render newest-first from `Model.Notes` and show AuthorName + CreatedAt with an `(edited)` suffix when UpdatedAt is set.
    - Source assertion: the notes empty state renders the heading `No Notes Yet` with `fa-comment-slash`.
    - `dotnet build QuestBoard.Service` succeeds.
  </acceptance_criteria>
  <done>Details view renders the portrait/Hidden-badge/DM-gated Actions card with the exact Reveal/Hide button styles, plus the Description and the collaborative Notes card with add-note form, inline edit-in-place, native-confirm delete, and empty state — all per UI-SPEC; the Service project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view-rendered controls → controller actions | Buttons/forms are UX affordances only; the controller (Plan 04) enforces authorization — views must not be the security boundary |
| note controls visible to all vs. management controls DM-only | Views gate management on CanManage; note controls are intentionally ungated (D-09) but the controller still requires [Authorize] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-14 | Elevation of Privilege | Player seeing/using Edit/Delete/Reveal affordances | mitigate | Actions card gated on Model.CanManage in the view; the controller's DungeonMasterOnly policy (Plan 04) is the real enforcement (defense in depth) |
| T-57-15 | Tampering | CSRF via note/toggle/delete forms | mitigate | Antiforgery token on every POST form; controller validates it (Plan 04) |
| T-57-16 | Information Disclosure | Hidden badge/toggle rendering to non-DM viewers | mitigate | Show Hidden toggle rendered only when ViewerIsDmTier; hidden Contacts never reach a Player's Index model (Plan 03/04 filtering) |
| T-57-SC | Tampering | npm installs | accept | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service` succeeds (all four Razor views + contacts.css compile/serve).
- Views use the UI-SPEC's exact CSS classes, copy strings, and button styles for the 4 new patterns (notes list, Show Hidden toggle, Hidden badge, Reveal/Hide action) — not re-derived from CONTEXT.md's general prose.
- Note controls are ungated (D-09); Actions card is CanManage-gated (D-09b).
- Contacts nav `<li>` sits in the unconditional all-board-types block in both layouts (D-19); `contacts.css` is linked globally next to `characters.css`.
- Full `dotnet test` remains green (no regressions).
</verification>

<success_criteria>
- Four desktop views + contacts.css exist and follow modern-card + CLAUDE.md conventions with UI-SPEC fidelity.
- Index is a flat alphabetical `.contact-grid` with DM-tier "+ Contact" (`btn-warning`) + the two-state Show Hidden toggle and secondary Hidden badges.
- Details realizes the D-20 layout with the exact UI-SPEC Notes list (add-note `btn-warning`, inline edit-in-place, native-confirm delete) and Reveal/Hide action (`btn-success`/`btn-secondary`).
- The Contacts nav link is visible to all users on both board types in desktop and mobile.
</success_criteria>

<output>
Create `.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-05-SUMMARY.md` when done
</output>
