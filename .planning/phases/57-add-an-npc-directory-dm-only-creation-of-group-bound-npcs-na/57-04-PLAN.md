---
phase: 57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na
plan: 04
type: execute
wave: 3
depends_on: ["57-03"]
files_modified:
  - QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs
  - QuestBoard.Service/ViewModels/ContactViewModels/ContactsIndexViewModel.cs
  - QuestBoard.Service/Controllers/Contacts/ContactsController.cs
  - QuestBoard.Service/Automapper/ViewModelProfile.cs
  - QuestBoard.Service/Constants/SessionKeys.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ContactsController exposes Index/Details/Create/Edit/Delete/ToggleReveal/ToggleShowHidden/AddNote/EditNote/DeleteNote/GetContactImage actions"
    - "Create/Edit/Delete/ToggleReveal/ToggleShowHidden are gated by [Authorize(Policy=DungeonMasterOnly)]; note actions and Index/Details use plain [Authorize]"
    - "Details returns 404 for a hidden Contact the viewer fails all three visibility branches; the Show Hidden toggle is per-group session state"
    - "ContactsControllerIntegrationTests.cs compiles and the full auth/visibility/note matrix passes"
  artifacts:
    - path: "QuestBoard.Service/Controllers/Contacts/ContactsController.cs"
      provides: "Contact HTTP surface incl. visibility check, toggle, note actions"
      contains: "class ContactsController"
      exports: ["Index", "Details", "Create", "Edit", "Delete", "ToggleReveal", "ToggleShowHidden", "AddNote", "EditNote", "DeleteNote", "GetContactImage"]
    - path: "QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs"
      provides: "Contact form/detail ViewModel + ContactNoteViewModel + reused image validators"
      contains: "class ContactViewModel"
    - path: "QuestBoard.Service/Constants/SessionKeys.cs"
      provides: "ShowHiddenContactsKey session-key helper"
      contains: "ShowHiddenContactsKey"
  key_links:
    - from: "QuestBoard.Service/Controllers/Contacts/ContactsController.cs"
      to: "QuestBoard.Domain/Interfaces/IContactService.cs"
      via: "injected IContactService for all data operations"
      pattern: "IContactService"
    - from: "QuestBoard.Service/Controllers/Contacts/ContactsController.cs"
      to: "QuestBoard.Service/Constants/SessionKeys.cs"
      via: "HttpContext.Session.GetInt32/SetInt32 with ShowHiddenContactsKey(groupId)"
      pattern: "ShowHiddenContactsKey"
---

<objective>
Build the Service-layer HTTP surface for Contacts: the `ContactsController`, its ViewModels, the Domain↔ViewModel AutoMapper wiring, and the per-group `ShowHiddenContacts` session key. This is where the D-09b policy gating, the three-branch hidden-Contact 404 (D-13), and the session-backed Show Hidden toggle (D-15/D-15b) are enforced.

Purpose: This plan makes `ContactsControllerIntegrationTests.cs` (Plan 01) compile and go green — the highest-value security test file in the phase.
Output: `ContactsController`, `ContactViewModel`, `ContactsIndexViewModel`, `ContactNoteViewModel`, ViewModelProfile mappings, `SessionKeys.ShowHiddenContactsKey`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-CONTEXT.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md
@.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md
@QuestBoard.Service/Controllers/Characters/CharactersController.cs
@QuestBoard.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs
@QuestBoard.Service/Constants/SessionKeys.cs
@QuestBoard.Service/Services/ActiveGroupContextService.cs
@QuestBoard.Service/Authorization/DungeonMasterHandler.cs
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's slice)

ViewModels:
- `ContactViewModel` (Id, Name `[Required][StringLength(100)]`, Description `[StringLength(2000)]`, TownCity `[StringLength(200)]`, SubLocation `[StringLength(200)]`, ContactImage `byte[]?`, ContactImageFile `IFormFile?` with reused `[MaxFileSize(5MB)]`/`[AllowedExtensions(.jpg/.jpeg/.png/.gif)]`, IsRevealed, CreatedByUserId, CanManage, Notes `List<ContactNoteViewModel>`)
- `ContactNoteViewModel` (Id, Text `[Required][StringLength(2000)]`, AuthorName, CreatedAt, UpdatedAt)
- `ContactsIndexViewModel` (Contacts list, ShowHidden flag, CanManage/ViewerIsDmTier flag for toggle + "+ Contact" visibility)

Controller: `ContactsController` actions —
- `Index` (plain [Authorize]) — reads Show Hidden session toggle for DM-tier viewers, calls GetAllContactsWithDetailsAsync
- `Details/{id}` (plain [Authorize]) — three-branch visibility check → NotFound() if viewer fails all branches
- `Create` GET/POST ([Authorize(Policy="DungeonMasterOnly")]) — stamps GroupId, CreatedByUserId, IsRevealed=false
- `Edit` GET/POST ([Authorize(Policy="DungeonMasterOnly")]) — core fields only (no notes)
- `Delete` POST ([Authorize(Policy="DungeonMasterOnly")])
- `ToggleReveal` POST ([Authorize(Policy="DungeonMasterOnly")]) — flips IsRevealed
- `ToggleShowHidden` POST ([Authorize(Policy="DungeonMasterOnly")]) — flips the per-group session toggle
- `AddNote`/`EditNote`/`DeleteNote` POST (plain [Authorize], no ownership guard per D-09)
- `GetContactImage/{id}` — serves stored image via DetectImageMimeType

Session key: `SessionKeys.ShowHiddenContactsKey(int groupId) => $"ShowHiddenContacts_{groupId}"`

ViewModelProfile: `CreateMap<Contact, ContactViewModel>` + reverse; `CreateMap<ContactNote, ContactNoteViewModel>` + reverse
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactViewModel + ContactsIndexViewModel + ContactNoteViewModel, add ShowHiddenContactsKey, wire ViewModelProfile</name>
  <files>QuestBoard.Service/ViewModels/ContactViewModels/ContactViewModel.cs, QuestBoard.Service/ViewModels/ContactViewModels/ContactsIndexViewModel.cs, QuestBoard.Service/Constants/SessionKeys.cs, QuestBoard.Service/Automapper/ViewModelProfile.cs</files>
  <read_first>
    - QuestBoard.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs (analog — the MaxFileSizeAttribute/AllowedExtensionsAttribute validators at ~lines 60-106 to reuse; field/validation conventions)
    - QuestBoard.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs (analog for the index ViewModel shape)
    - QuestBoard.Service/Constants/SessionKeys.cs (existing ActiveGroupId const pattern to extend)
    - QuestBoard.Service/Automapper/ViewModelProfile.cs (existing Character mapping block ~lines 61-73)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md (ContactViewModel field block; Show Hidden session key)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md ("New Pattern Specs" 2/4 — the ViewModel flags this plan creates drive the two-state toggle copy and the Reveal/Hide action copy the views render; `ContactsIndexViewModel.ShowHidden` is the ON/OFF discriminator, `ContactViewModel.CanManage` gates the Reveal/Hide action, `ContactViewModel.IsRevealed` chooses "Reveal Contact" vs "Hide Contact")
  </read_first>
  <action>
    Create `ContactViewModel` in `QuestBoard.Service/ViewModels/ContactViewModels/` with the fields listed in artifacts_produced. Reuse `MaxFileSizeAttribute(5 * 1024 * 1024)` and `AllowedExtensionsAttribute([".jpg", ".jpeg", ".png", ".gif"])` on `ContactImageFile` (D-06) — either reference the existing attributes from `QuestBoard.Service.ViewModels.CharacterViewModels` via a using, or duplicate them into the Contact ViewModels namespace; do NOT write new validation logic. Do NOT add IsOwner/CanEdit owner-pair fields (there is no owner concept, D-07); instead add a single `CanManage` bool (DM-tier viewer flag driving Edit/Delete/Reveal button visibility). Include `List<ContactNoteViewModel> Notes`.
    Create `ContactNoteViewModel` (Id; Text `[Required][StringLength(2000)]`; AuthorName; CreatedAt; UpdatedAt) — this may live in the same file as ContactViewModel or its own file, matching the Character convention.
    Create `ContactsIndexViewModel` mirroring `CharactersIndexViewModel` but flat (no My/Other split, D-17): a `Contacts` list of ContactViewModel, a `ShowHidden` bool (current toggle state), and a `ViewerIsDmTier`/`CanManage` bool (drives whether the Show Hidden toggle and "+ Contact" button render — D-15.2/D-20).
    Add to `SessionKeys.cs`: `public static string ShowHiddenContactsKey(int groupId) => $"ShowHiddenContacts_{groupId}";` alongside the existing consts.
    In `ViewModelProfile.cs`, add `CreateMap<Contact, ContactViewModel>()` (project `ContactImageData` → `ContactImage`) + reverse, and `CreateMap<ContactNote, ContactNoteViewModel>()` + reverse, placed next to the existing Character mapping block.
    No phase/decision IDs in comments.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ContactViewModel.ContactImageFile` carries both `[MaxFileSize(...)]` and `[AllowedExtensions(...)]`; the ViewModel has `CanManage` but NO `IsOwner`/`CanEdit` pair.
    - Source assertion: `ContactsIndexViewModel` is flat (a single Contacts list + ShowHidden + ViewerIsDmTier flag) — no My/Other split.
    - Source assertion: `SessionKeys.ShowHiddenContactsKey(int)` returns `"ShowHiddenContacts_" + groupId`.
    - Source assertion: `ViewModelProfile` contains `CreateMap<Contact, ContactViewModel>` and `CreateMap<ContactNote, ContactNoteViewModel>` (both directions).
    - `dotnet build QuestBoard.Service` succeeds.
  </acceptance_criteria>
  <done>All Contact ViewModels exist with reused image validators, the session-key helper is added, ViewModelProfile is wired, and the Service project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ContactsController (policy gating, three-branch 404, session toggle, note actions, image serving)</name>
  <files>QuestBoard.Service/Controllers/Contacts/ContactsController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Characters/CharactersController.cs (analog — Index/Details/Create/Edit/Delete/ToggleRetirement/GetProfilePicture shapes; image upload validation block ~lines 102-148; DetectImageMimeType ~lines 372-375; RequireActiveGroupId usage ~line 154; GetEffectiveGroupRoleAsync usage ~line 67)
    - QuestBoard.Service/Services/ActiveGroupContextService.cs (HttpContext.Session GetInt32/SetInt32 pattern; RequireActiveGroupId)
    - QuestBoard.Service/Authorization/DungeonMasterHandler.cs (confirms DungeonMasterOnly = DM/Admin/SuperAdmin — attribute alone is the boundary)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-PATTERNS.md (ContactsController section: per-action auth, Index toggle read, Create stamp, Details three-branch, ToggleReveal/ToggleShowHidden, note actions, GetContactImage)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-RESEARCH.md (Pitfall 1 policy vs GetEffectiveGroupRoleAsync; Pitfall 2 controller-level three-branch 404; Pitfall 3 RequireActiveGroupId for toggle key; Anti-Pattern: do NOT port CanManageCharacterAsync)
    - .planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-UI-SPEC.md ("New Pattern Spec" 4 — the `ToggleReveal` action this controller exposes is consumed by the Details Actions-card POST form; confirm the action name is exactly `ToggleReveal` and `ToggleShowHidden` so the UI-SPEC's `asp-action` bindings resolve)
  </read_first>
  <action>
    Create `ContactsController` (class-level `[Authorize]`) injecting `IContactService`, `IUserService`, `IActiveGroupContext`, `IMapper` (constructor-injection style matching CharactersController). Do NOT port `CanManageCharacterAsync` — there is no owner concept (D-07/D-09b); the policy attribute is the whole boundary for core-field actions.
    Compute the viewer's DM-tier flag with `userService.GetEffectiveGroupRoleAsync(User, activeGroupContext.RequireActiveGroupId())` (Pitfall 1) wherever the view needs `CanManage`/toggle visibility — the policy attribute gates the action but does not expose the resolved role to the view.
    Actions:
    - `Index` (plain `[Authorize]`): resolve current user id + DM-tier flag; if DM-tier, read `includeHidden = HttpContext.Session.GetInt32(SessionKeys.ShowHiddenContactsKey(RequireActiveGroupId())) == 1` (else false); call `GetAllContactsWithDetailsAsync(includeHidden, currentUserId, viewerIsDmTier)`; map to `ContactsIndexViewModel` with `ShowHidden`/`ViewerIsDmTier` set. Order is alphabetical (handled in repo).
    - `Details/{id}` (plain `[Authorize]`): load the contact; if null → `NotFound()`; apply the three-branch visibility check (creator == currentUserId OR IsRevealed OR (viewerIsDmTier AND includeHidden-toggle-on)) and return `NotFound()` if the viewer fails ALL branches (D-13). Set `CanManage` = viewerIsDmTier on the ViewModel.
    - `Create` GET/POST (`[Authorize(Policy = "DungeonMasterOnly")]`, `[ValidateAntiForgeryToken]` on POST): copy Character's image-upload validation block renamed (`viewModel.ContactImageFile`); stamp `contact.GroupId = activeGroupContext.RequireActiveGroupId()`, `contact.CreatedByUserId = currentUser.Id`, `contact.IsRevealed = false` (D-14).
    - `Edit` GET/POST (`[Authorize(Policy = "DungeonMasterOnly")]`, `[ValidateAntiForgeryToken]`): core fields only (Name/Image/Description/TownCity/SubLocation) — no notes editing (D-16/D-20). Route image changes through `UpdateProfileImageAsync`.
    - `Delete` POST (`[Authorize(Policy = "DungeonMasterOnly")]`, `[ValidateAntiForgeryToken]`).
    - `ToggleReveal/{id}` POST (`[Authorize(Policy = "DungeonMasterOnly")]`, `[ValidateAntiForgeryToken]`): flip `IsRevealed`, `UpdateAsync`, redirect to Details — mirrors ToggleRetirement shape.
    - `ToggleShowHidden` POST (`[Authorize(Policy = "DungeonMasterOnly")]`, `[ValidateAntiForgeryToken]`): read current toggle, write the flipped value via `HttpContext.Session.SetInt32(SessionKeys.ShowHiddenContactsKey(RequireActiveGroupId()), newValue ? 1 : 0)` (Pitfall 3 — always RequireActiveGroupId), redirect to Index.
    - `AddNote`/`EditNote`/`DeleteNote` POST (plain `[Authorize]`, `[ValidateAntiForgeryToken]`, NO DM-tier gate per D-09): AddNote stamps `AuthorUserId = currentUser.Id` + `CreatedAt`; EditNote updates Text; DeleteNote removes; all redirect to `Details/{contactId}`. No per-note ownership check.
    - `GetContactImage/{id}`: copy Character's `GetProfilePicture` + `DetectImageMimeType` verbatim renamed (byte-signature PNG/GIF/else-JPEG).
    No phase/decision IDs in comments.
  </action>
  <verify>
    <automated>dotnet build; dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~ContactsControllerIntegrationTests</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `[Authorize(Policy = "DungeonMasterOnly")]` appears on Create/Edit/Delete/ToggleReveal/ToggleShowHidden; AddNote/EditNote/DeleteNote and Index/Details carry only plain `[Authorize]` (no DM policy, no CanManageCharacterAsync-style guard).
    - Source assertion: `Details` returns `NotFound()` when the viewer fails all three visibility branches; `Create` sets `IsRevealed = false`, `CreatedByUserId`, and `GroupId` via `RequireActiveGroupId()`.
    - Source assertion: `ToggleShowHidden` uses `SessionKeys.ShowHiddenContactsKey(activeGroupContext.RequireActiveGroupId())` (not the nullable ActiveGroupId).
    - Source assertion: every `[HttpPost]` action carries `[ValidateAntiForgeryToken]`; `CanManageCharacterAsync` is NOT present anywhere in the file.
    - Test: `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~ContactsControllerIntegrationTests` compiles and passes (Plan 01's full matrix goes GREEN).
    - `dotnet build` (full solution) succeeds.
  </acceptance_criteria>
  <done>ContactsController implements all actions with correct per-action authorization, the three-branch 404, the per-group session toggle, collaborative note actions, and image serving; the integration test matrix passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → ContactsController actions | Role and session inputs cross here; policy attributes + explicit visibility check enforce access |
| session toggle read/write ↔ per-group scope | Toggle key is scoped by RequireActiveGroupId; no null-group key collision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-09 | Elevation of Privilege | Player invoking Create/Edit/Delete/ToggleReveal | mitigate | `[Authorize(Policy = "DungeonMasterOnly")]` on those actions (D-09b); DungeonMasterHandler resolves DM/Admin/SuperAdmin |
| T-57-10 | Information Disclosure | Hidden Contact spoiler via role alone / IDOR on Details/{id} | mitigate | Explicit controller three-branch visibility check returning NotFound() for hidden Contacts and cross-group ids (D-13, Pitfall 2, Phase 49/55 404 convention) |
| T-57-11 | Tampering | CSRF on note/toggle/reveal POSTs | mitigate | `[ValidateAntiForgeryToken]` on every [HttpPost] action (matches CharactersController) |
| T-57-12 | Denial of Service / Tampering | Oversized/wrong-type image upload | mitigate | Reused MaxFileSizeAttribute (5MB) + AllowedExtensionsAttribute (.jpg/.jpeg/.png/.gif) + DetectImageMimeType byte-sniff on read (D-06) |
| T-57-13 | Session Management | Toggle key collision across "no active group" states | mitigate | `RequireActiveGroupId()` used for the toggle key (Pitfall 3), never the nullable ActiveGroupId |
| T-57-SC | Tampering | NuGet installs | accept | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build` (full solution) succeeds.
- `dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~ContactsControllerIntegrationTests` passes (all 9 matrix facts).
- Full suite `dotnet test` green (wave-merge gate per 57-VALIDATION.md).
- Policy attributes are on core-field actions only; note actions are open to all group members; no CanManageCharacterAsync appears.
</verification>

<success_criteria>
- ContactsController exposes all listed actions with correct per-action authorization.
- Hidden-Contact Details 404 (three branches) and cross-group IDOR 404 are enforced in the controller.
- Show Hidden toggle is per-group session state via RequireActiveGroupId.
- Note actions are collaborative (plain [Authorize], no ownership guard).
- ContactsControllerIntegrationTests (Plan 01) passes.
</success_criteria>

<output>
Create `.planning/phases/57-add-an-npc-directory-dm-only-creation-of-group-bound-npcs-na/57-04-SUMMARY.md` when done
</output>
