---
phase: 31-unauthenticated-landing-redirect
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03"]
files_modified:
  - QuestBoard.IntegrationTests/Controllers/HomeControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/CalendarControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs
  - QuestBoard.IntegrationTests/Controllers/QuestControllerIntegrationTests_Comprehensive.cs
  - QuestBoard.IntegrationTests/Controllers/GroupSessionMiddlewareIntegrationTests.cs
autonomous: false
requirements: [UX-01, UX-04]
must_haves:
  truths:
    - "The full integration suite is green after the Phase 31 behavior changes"
    - "Unauthenticated GET /Calendar and /QuestLog assert a 302 redirect (auth challenge)"
    - "Unauthenticated GET / asserts 200 and a Log In button; authenticated GET /quests asserts 200 with quest content"
    - "An authenticated no-group user hitting /quests is redirected to /groups/pick by the middleware; a SuperAdmin is not"
  artifacts:
    - path: "QuestBoard.IntegrationTests/Controllers/GroupSessionMiddlewareIntegrationTests.cs"
      provides: "New middleware tests for D-09/D-10/D-11"
      contains: "GroupSessionMiddleware"
    - path: "QuestBoard.IntegrationTests/Controllers/HomeControllerIntegrationTests.cs"
      provides: "Landing-page tests replacing the old quest-display tests (D-04)"
  key_links:
    - from: "GroupSessionMiddlewareIntegrationTests"
      to: "WebApplicationFactoryBase.TestGroupContext"
      via: "sets TestGroupContext.ActiveGroupId = null to simulate an expired group session"
      pattern: "TestGroupContext.ActiveGroupId"
    - from: "CalendarControllerIntegrationTests"
      to: "AuthenticationHelper.CreateAuthenticatedClientWithUserAsync"
      via: "authenticated client so [Authorize] returns 200 not 302"
      pattern: "CreateAuthenticatedClientWithUserAsync"
---

<objective>
Bring the integration suite back to green and prove the Phase 31 behavior. Three existing test classes break because of the auth lockdown and the home/quest split (Calendar + QuestLog now require auth; the home page no longer shows quests), so they must be updated. Add `/quests` route tests to the existing comprehensive QuestController test class. Create a new `GroupSessionMiddlewareIntegrationTests` class for the session-recovery behavior (D-09/D-10/D-11), driving it through the existing `MutableGroupContext` test stub. End with a full-suite green gate and a blocking human-verify checkpoint for the manual landing-page and single-group-recovery checks called out in VALIDATION.md.

Purpose: Lock in the new access-control and routing behavior with automated coverage so regressions are caught, and confirm the two manual-only visual/session flows.
Output: 4 updated/extended test files, 1 new test file, full-suite green, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-unauthenticated-landing-redirect/31-CONTEXT.md
@.planning/phases/31-unauthenticated-landing-redirect/31-RESEARCH.md
@.planning/phases/31-unauthenticated-landing-redirect/31-VALIDATION.md
@QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs
@QuestBoard.IntegrationTests/WebApplicationFactoryBase.cs
@QuestBoard.IntegrationTests/Helpers/MutableGroupContext.cs
</context>

<artifacts_this_phase_produces>
New symbols and files produced by this plan (31-04):
- `QuestBoard.IntegrationTests/Controllers/GroupSessionMiddlewareIntegrationTests.cs` — NEW test class covering: authenticated no-group user → 302 to /groups/pick; SuperAdmin no-group → not redirected; /groups/pick exempt → not looped; user with active group → reaches page
- New landing-page test methods in `HomeControllerIntegrationTests` (replacing the three quest-display tests)
- New `/quests` route test methods in `QuestControllerIntegrationTests_Comprehensive`
- Updated authenticated clients in `CalendarControllerIntegrationTests` and `QuestLogControllerIntegrationTests`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update Calendar + QuestLog tests to authenticated clients; update Home tests to landing-page assertions</name>
  <files>QuestBoard.IntegrationTests/Controllers/CalendarControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/HomeControllerIntegrationTests.cs, QuestBoard.IntegrationTests/Controllers/DungeonMasterControllerIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/CalendarControllerIntegrationTests.cs (uses `factory.CreateNonRedirectingClient()` — unauthenticated; breaks under D-01)
    - QuestBoard.IntegrationTests/Controllers/QuestLogControllerIntegrationTests.cs (same unauthenticated-client problem)
    - QuestBoard.IntegrationTests/Controllers/HomeControllerIntegrationTests.cs (three tests assert quest content on `/`; must become landing-page tests)
    - QuestBoard.IntegrationTests/Controllers/DungeonMasterControllerIntegrationTests.cs (existing tests use authenticated clients; need one new unauthenticated assertion for D-02)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs (use `CreateAuthenticatedClientWithUserAsync` — it seeds UserGroups for group 1; combined with MutableGroupContext default ActiveGroupId=1 the middleware does NOT redirect these requests)
  </read_first>
  <action>
    In `CalendarControllerIntegrationTests.cs`: for each test that asserts a 200 on `/Calendar`, replace the shared unauthenticated `_client` usage with an authenticated client obtained via `await AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(factory, ...)` (per-test, like the existing data-seeding tests do) so the `[Authorize]` added in 31-01 returns 200 instead of 302. The default `MutableGroupContext.ActiveGroupId = 1` means the new session-recovery middleware will NOT redirect these authenticated requests. Add ONE new test asserting that an UNauthenticated client (`factory.CreateNonRedirectingClient()`) GET `/Calendar` returns a redirect (`HttpStatusCode.Redirect`/`Found`/`Unauthorized`) — this proves D-01 lockdown.
    In `QuestLogControllerIntegrationTests.cs`: apply the same transformation — switch the 200-expecting tests to authenticated clients, and add ONE new test asserting unauthenticated GET `/QuestLog` returns a redirect (D-01).
    In `HomeControllerIntegrationTests.cs`: KEEP `Index_ShouldReturnSuccessStatusCode` (GET `/` still returns 200, now for the landing page) and `NonExistentRoute_ShouldReturn404`. REPLACE `Index_ShouldReturnHtmlContent` and `Index_WithQuests_ShouldDisplayQuestList` with landing-page assertions: GET `/` (unauthenticated `_client`) returns 200, content contains "Log In", and content does NOT contain quest-card markup that only the board renders (assert the page does not contain a quest title seeded into the DB — i.e. seed no quests and assert the landing copy is present). Add a test `Index_ShouldContainLoginButton` asserting the response body contains the literal `Log In` and an Account/Login link target.
    In `DungeonMasterControllerIntegrationTests.cs`: add ONE new test asserting that an unauthenticated client (`factory.CreateNonRedirectingClient()`) GET `/DungeonMaster/Profile/1` returns a redirect (`HttpStatusCode.Redirect`/`Found`/`Unauthorized`) — this proves D-02 lockdown (removal of `[AllowAnonymous]` from `Profile` GET). Seed a DM user with ID obtainable from the test DB if needed, or use a known seeded ID.
    Do NOT weaken assertions to tautologies — every updated test must assert a concrete status code and/or concrete body substring.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~CalendarController|FullyQualifiedName~QuestLogController|FullyQualifiedName~HomeController" 2>&1 | grep -E "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `CalendarControllerIntegrationTests` contains a test that uses `CreateNonRedirectingClient()` and asserts a redirect/unauthorized status for `/Calendar` (the new D-01 lockdown test)
    - `CalendarControllerIntegrationTests` 200-expecting tests now obtain a client via `CreateAuthenticatedClientWithUserAsync`
    - `QuestLogControllerIntegrationTests` contains the analogous unauthenticated-redirect test for `/QuestLog` and authenticated 200 tests
    - `HomeControllerIntegrationTests` contains a test asserting GET `/` body contains `Log In`, and no test asserts quest titles appear on `/`
    - `DungeonMasterControllerIntegrationTests` contains a test that uses `CreateNonRedirectingClient()` and asserts a redirect/unauthorized status for unauthenticated GET `/DungeonMaster/Profile/{id}` (D-02 lockdown)
    - `dotnet test --filter` over Calendar+QuestLog+Home+DungeonMaster reports `Passed!` (zero failures)
  </acceptance_criteria>
  <done>Calendar/QuestLog tests pass with authenticated clients and a new unauthenticated-redirect assertion; Home tests assert the landing page; the filtered run is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add /quests route tests and a GroupPicker fallback-redirect assertion</name>
  <files>QuestBoard.IntegrationTests/Controllers/QuestControllerIntegrationTests_Comprehensive.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/Controllers/QuestControllerIntegrationTests_Comprehensive.cs (existing comprehensive QuestController tests — add the new /quests cases here rather than a new file, to keep the route's tests with the controller)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs (CreateAuthenticatedClientWithUserAsync seeds group membership; default MutableGroupContext.ActiveGroupId = 1 so /quests is not intercepted by the middleware)
  </read_first>
  <action>
    Add two tests to `QuestControllerIntegrationTests_Comprehensive`:
    1. `Index_Quests_Authenticated_ReturnsOk` (D-05): build an authenticated client via `CreateAuthenticatedClientWithUserAsync`, GET `/quests`, assert `HttpStatusCode.OK`. Optionally seed a quest and assert its title appears in the body to prove the migrated board logic renders.
    2. `Index_Quests_Unauthenticated_Redirects` (D-05): use `factory.CreateNonRedirectingClient()`, GET `/quests`, assert the status is a redirect (`Redirect`/`Found`) or `Unauthorized` — proving the action-level `[Authorize]` added in 31-02.
    If the existing class does not already have a GroupPicker fallback assertion, do NOT add GroupPicker tests here — the existing `GroupPickerControllerIntegrationTests.SelectGroup_ShouldPersistActiveGroupInSession` already asserts SelectGroup redirects; D-07 (fallback now goes to /quests) is implicitly exercised. Only add an explicit GroupPicker assertion if it can be done without a real session round-trip (the test harness does not round-trip session cookies for header-auth clients — see the note in GroupPickerControllerIntegrationTests). If it cannot be asserted reliably, document that D-07 is covered by source assertion in 31-02 plus manual verification, and skip the brittle session-based assertion.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~QuestController" 2>&1 | grep -E "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `QuestControllerIntegrationTests_Comprehensive` contains a test that GETs `/quests` with an authenticated client and asserts `HttpStatusCode.OK`
    - `QuestControllerIntegrationTests_Comprehensive` contains a test that GETs `/quests` with an unauthenticated client and asserts a redirect/unauthorized status
    - `dotnet test --filter "FullyQualifiedName~QuestController"` reports `Passed!` (zero failures)
  </acceptance_criteria>
  <done>The /quests route has authenticated-200 and unauthenticated-redirect coverage; the QuestController filtered run is green.</done>
</task>

<task type="auto">
  <name>Task 3: Create GroupSessionMiddlewareIntegrationTests</name>
  <files>QuestBoard.IntegrationTests/Controllers/GroupSessionMiddlewareIntegrationTests.cs</files>
  <read_first>
    - QuestBoard.IntegrationTests/WebApplicationFactoryBase.cs (exposes `TestGroupContext` (MutableGroupContext) registered as the singleton IActiveGroupContext; `CreateNonRedirectingClient()` so redirects are observable)
    - QuestBoard.IntegrationTests/Helpers/MutableGroupContext.cs (settable `ActiveGroupId`; default 1 — set to null to simulate an expired group session)
    - QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs (CreateAuthenticatedClientWithUserAsync for a normal user; CreateAuthenticatedSuperAdminClientAsync for SuperAdmin)
    - QuestBoard.IntegrationTests/Controllers/GroupPickerControllerIntegrationTests.cs (pattern reference for IClassFixture<WebApplicationFactoryBase> and ClearDatabaseAsync usage)
  </read_first>
  <action>
    Create `GroupSessionMiddlewareIntegrationTests` as `IClassFixture<WebApplicationFactoryBase>`, mirroring the structure of GroupPickerControllerIntegrationTests. Because `TestGroupContext` is a shared singleton on the factory, each test must set `factory.TestGroupContext.ActiveGroupId` explicitly at the start and reset it to `1` at the end (use try/finally) to avoid cross-test bleed. Write these tests, all using `factory.CreateNonRedirectingClient()` or an authenticated client with `AllowAutoRedirect = false`:
    1. `AuthenticatedUser_NoActiveGroup_RedirectsToGroupPick` (D-09): set `TestGroupContext.ActiveGroupId = null`; authenticated non-SuperAdmin client; GET `/quests`; assert status is `Redirect`/`Found` AND `response.Headers.Location` contains `/groups/pick`.
    2. `SuperAdmin_NoActiveGroup_NotRedirectedByMiddleware` (D-09/D-11): set `TestGroupContext.ActiveGroupId = null`; SuperAdmin client; GET `/quests`; assert the response Location (if any redirect) does NOT point to `/groups/pick` — the middleware must not catch SuperAdmin. (The request may still 200 or be handled by [Authorize]; the key assertion is "not redirected to /groups/pick by the middleware".)
    3. `GroupPickPath_NoActiveGroup_NotLooped` (D-10): set `TestGroupContext.ActiveGroupId = null`; authenticated client; GET `/groups/pick`; assert the status is NOT a redirect to `/groups/pick` (i.e. the destination is exempt — it returns 200 or redirects onward to the board for a single-group user, but never back to `/groups/pick`).
    4. `AuthenticatedUser_WithActiveGroup_ReachesPage` (D-09 negative): leave `TestGroupContext.ActiveGroupId = 1`; authenticated client; GET `/quests`; assert `HttpStatusCode.OK` (no middleware redirect).
    Always restore `factory.TestGroupContext.ActiveGroupId = 1` in a finally block.
  </action>
  <verify>
    <automated>dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~GroupSession" 2>&1 | grep -E "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `QuestBoard.IntegrationTests/Controllers/GroupSessionMiddlewareIntegrationTests.cs` exists with four test methods covering D-09 redirect, SuperAdmin exemption, /groups/pick non-loop, and active-group pass-through
    - Each test sets `factory.TestGroupContext.ActiveGroupId` and restores it to `1` in a `finally`
    - The D-09 redirect test asserts `response.Headers.Location` contains `/groups/pick`
    - `dotnet test --filter "FullyQualifiedName~GroupSession"` reports `Passed!` (zero failures)
  </acceptance_criteria>
  <done>The middleware's redirect, SuperAdmin-exemption, exempt-path, and pass-through behaviors are covered by passing integration tests.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite green gate</name>
  <files>QuestBoard.IntegrationTests/Controllers/GroupSessionMiddlewareIntegrationTests.cs</files>
  <read_first>
    - .planning/phases/31-unauthenticated-landing-redirect/31-VALIDATION.md (full suite command and sampling rate)
  </read_first>
  <action>
    Run the entire test suite with `dotnet test`. Triage any failure: a remaining unauthenticated-client test elsewhere that now hits a locked-down controller (302), a test asserting quest content on `/`, or a DungeonMaster Profile test that assumed `[AllowAnonymous]`. The DungeonMaster Profile tests already use authenticated clients (confirmed in DungeonMasterControllerIntegrationTests) so they should survive D-02 — verify they pass. Fix any straggler by switching it to an authenticated client or updating its expectation, consistent with the patterns established in Tasks 1-3. Do NOT introduce `[AllowAnonymous]` or weaken `[Authorize]` to make a test pass — the lockdown is the intended behavior. The gate is: `dotnet test` reports zero failures.
  </action>
  <verify>
    <automated>dotnet test 2>&1 | grep -E "Passed!|Failed!|error"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` (full suite) reports `Passed!` with zero failed tests
    - No test was made to pass by adding `[AllowAnonymous]` or removing a `[Authorize]` added in this phase
  </acceptance_criteria>
  <done>The full integration suite is green with all Phase 31 behavior in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verify — landing page, /quests routing, and single-group session recovery</name>
  <action>Pause for human verification. Present the <what-built> summary and walk the human through the <how-to-verify> steps below, then wait for the <resume-signal> before completing the plan.</action>
  <what-built>
    The unauthenticated landing redirect feature: a public landing page at `/`, the quest board moved to `/quests` (auth-required), DM/Calendar/QuestLog locked down, and session-recovery middleware redirecting expired-group sessions to the group picker.
  </what-built>
  <how-to-verify>
    1. Stop the Visual Studio debugger if running (Shift+F5), then run `dotnet run --project QuestBoard.Service` and wait for startup.
    2. In a logged-OUT browser (or private window), visit `http://localhost:5000/` (or the configured port). CONFIRM: you see the public landing page — app name "D&D Quest Board", a tagline, and a prominent "Log In" button. CONFIRM: no quest cards are shown.
    3. From the landing page, click "Log In" and sign in as a normal single-group user. CONFIRM: after login you land on the quest board (URL `/quests`) showing the quest list — not the landing page.
    4. While logged in, click the navbar brand ("D&D Quest Board"). CONFIRM: it takes you to `/quests` (the board), not the landing page.
    5. Single-group session recovery (D-11): with the single-group user logged in, clear the session cookie (DevTools → Application → Cookies → delete the `.AspNetCore.Session` cookie) WITHOUT clearing the auth cookie, then navigate to `/quests`. CONFIRM: you are seamlessly returned to `/quests` (the group picker auto-picks your only group) without seeing a picker page.
    6. Visit `/Calendar` and `/QuestLog` while logged in. CONFIRM: both load normally. Then log out and try `/Calendar` directly. CONFIRM: you are redirected to the login page.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → locked-down controller | Tests assert the auth boundary (302 for anonymous, 200 for authenticated) |
| test stub IActiveGroupContext → middleware | MutableGroupContext drives the middleware's group-null branch under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Information Disclosure (regression) | Locked-down controllers | mitigate | Automated tests assert unauthenticated requests to /Calendar, /QuestLog, /quests redirect (do not return group data) |
| T-31-10 | DoS (redirect loop regression) | GroupSessionMiddleware | mitigate | Automated tests assert SuperAdmin and /groups/pick are exempt — a loop-causing regression fails the suite |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md confirms zero external dependencies added |
</threat_model>

<verification>
- `dotnet test` (full suite) is green.
- New GroupSession tests assert redirect, SuperAdmin exemption, exempt-path non-loop, and pass-through.
- Home tests assert the landing page; Calendar/QuestLog tests assert both authenticated-200 and unauthenticated-redirect.
- Human verify checkpoint confirms the landing page visual and single-group session recovery (manual-only per VALIDATION.md).
</verification>

<success_criteria>
- The full integration suite passes with zero failures.
- Every Phase 31 behavior (lockdown, landing page, /quests, middleware) has at least one passing automated test.
- Human sign-off recorded for the landing-page visual and single-group recovery flow.
</success_criteria>

<output>
Create `.planning/phases/31-unauthenticated-landing-redirect/31-04-SUMMARY.md` when done.
</output>
