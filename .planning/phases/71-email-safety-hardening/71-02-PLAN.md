---
phase: 71-email-safety-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 71-01
files_modified:
  - QuestBoard.Service/Components/Emails/QuestFinalized.razor
  - QuestBoard.Service/Components/Emails/SessionReminder.razor
  - QuestBoard.Service/Components/Emails/WaitlistPromoted.razor
  - QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs
autonomous: true
requirements:
  - EMAILMD-02
  - EMAILMD-03
must_haves:
  truths:
    - "All 3 quest email templates render Quest Description via MarkdownService.RenderEmailHtml(QuestDescription, QuestUrl)"
    - "The inner Description scroll-wrapper div is removed from all 3 templates; the 840px fixed-height poster card (td height:840px;overflow:hidden) is retained unchanged (D-01)"
    - "GET /EmailPreview/WaitlistPromoted renders the WaitlistPromoted template, and all 3 quest-email previews use a structured-Markdown sample so the styling + truncation are visible in the browser dev-loop"
  artifacts:
    - "QuestBoard.Service/Components/Emails/QuestFinalized.razor, SessionReminder.razor, WaitlistPromoted.razor (RenderEmailHtml call site, scroll wrapper removed)"
    - "QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs (WaitlistPromoted action + Index link + shared structured-Markdown sample)"
  key_links:
    - "QuestUrl (existing [Parameter, EditorRequired] on all 3 components) is passed as the read-more URL — no new parameter plumbing from the Hangfire jobs (D-02: reuse the existing Row 6 View Quest Details href)"
---

<objective>
Wire the 3 quest email templates to the new `RenderEmailHtml` method from Plan 01 and remove the broken Outlook scroll trick. Each template swaps its single Description call site to `RenderEmailHtml(QuestDescription, QuestUrl)` and drops the inner scroll-wrapper div (which Outlook's Word engine silently clips). The 840px fixed-height poster card stays exactly as-is per D-01. Also add the missing `WaitlistPromoted` preview action so the operator can dev-loop the new styling/truncation in a browser before the expensive live-client send in Plan 03.

Purpose: Turns the Plan 01 engine into visible behavior in the actual emails, and restores full readability of long structured Descriptions (EMAILMD-03) with correctly styled headings/lists/blockquotes (EMAILMD-02).

Output: 3 updated Razor templates + an extended `EmailPreviewController`.
</objective>

<artifacts_this_phase_produces>
Symbols this plan creates (source-grounding pass: treat as newly created, not drift):
- `EmailPreviewController.WaitlistPromoted()` — new `[HttpGet]` Admin-only preview action
- `EmailPreviewController.SampleMarkdownDescription` — new private const holding the shared structured-Markdown sample (headings + list + blockquote, long enough to exceed the 5-block/650-char budget)

Modified (not new): the 3 templates' Row 3 Description markup (call-site swap + scroll-wrapper removal), `EmailPreviewController.Index()` link list, and the existing `QuestFinalized()`/`SessionReminder()` preview sample descriptions.
No new files, no new packages, no DB/migration changes.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-email-safety-hardening/71-CONTEXT.md
@.planning/phases/71-email-safety-hardening/71-UI-SPEC.md
@.planning/phases/71-email-safety-hardening/71-PATTERNS.md
@QuestBoard.Domain/Interfaces/IMarkdownService.cs
@QuestBoard.Service/Components/Emails/QuestFinalized.razor
@QuestBoard.Service/Components/Emails/SessionReminder.razor
@QuestBoard.Service/Components/Emails/WaitlistPromoted.razor
@QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the 3 templates to RenderEmailHtml and remove the scroll wrapper</name>
  <files>QuestBoard.Service/Components/Emails/QuestFinalized.razor, QuestBoard.Service/Components/Emails/SessionReminder.razor, QuestBoard.Service/Components/Emails/WaitlistPromoted.razor</files>
  <read_first>
    - QuestBoard.Service/Components/Emails/QuestFinalized.razor — canonical reference; Row 3 Description block (lines 39-46), the outer poster cell (line 8), the existing Row 6 "View Quest Details" CTA (line 82), and the `QuestUrl` parameter (line 102)
    - QuestBoard.Service/Components/Emails/SessionReminder.razor — same structure plus an extra "The Adventure:" label `<p>` (line 43) that must be preserved
    - QuestBoard.Service/Components/Emails/WaitlistPromoted.razor — same structure as QuestFinalized
    - QuestBoard.Domain/Interfaces/IMarkdownService.cs — confirm the `RenderEmailHtml(string?, string, int, int)` signature added in Plan 01
    - .planning/phases/71-email-safety-hardening/71-UI-SPEC.md — "Truncation & Read-More Contract" item 1 (remove the inner scroll wrapper; the cell keeps vertical-align:top)
    - .planning/phases/71-email-safety-hardening/71-PATTERNS.md — the exact before/after call-site markup (lines 115-126)
  </read_first>
  <action>
    In all 3 templates (QuestFinalized.razor, SessionReminder.razor, WaitlistPromoted.razor), change the Row 3 Description call from `@((MarkupString)MarkdownService.RenderToHtml(QuestDescription, MarkdownRenderTarget.Email))` to `@((MarkupString)MarkdownService.RenderEmailHtml(QuestDescription, QuestUrl))`. `QuestUrl` is already an existing `[Parameter, EditorRequired]` on all 3 components (QuestFinalized/WaitlistPromoted line 102, SessionReminder line 103) — reuse it verbatim; add no new parameter and change no Hangfire job call.

    Remove the inner Description scroll-wrapper div described in 71-UI-SPEC.md Truncation contract item 1 and 71-PATTERNS.md (the wrapper element that gives the Description a fixed inner height and a scroll behavior Outlook cannot honor). Keep the outer poster cell `<td style="height:840px;overflow:hidden;">` (line 8) exactly as-is per D-01, keep the Row 3 cell's `vertical-align:top`, and keep the inner styled content div that carries `font-size:15px;font-family:Georgia,serif;...margin:0;` and now calls `RenderEmailHtml`. In SessionReminder.razor, preserve the "The Adventure:" label `<p>` (line 43) that sits above the Description div — only the scroll wrapper goes away, not that label.

    Update the now-stale Row 3 HTML comment (currently claims the Description scrolls when too long) to describe the truncate-with-read-more behavior instead. Do not embed phase/requirement IDs in the comment (CLAUDE.md).
  </action>
  <acceptance_criteria>
    - Each of the 3 templates calls `RenderEmailHtml(QuestDescription, QuestUrl)` exactly once and no longer calls `RenderToHtml(QuestDescription, MarkdownRenderTarget.Email)`
    - The outer poster cell still contains `height:840px` and `overflow:hidden` in all 3 templates (D-01 retained)
    - No `overflow-y` style remains in any of the 3 templates (scroll wrapper removed)
    - SessionReminder.razor still contains the "The Adventure:" label paragraph
    - `dotnet build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>dotnet build QuestBoard.Service && grep -c "RenderEmailHtml(QuestDescription, QuestUrl)" QuestBoard.Service/Components/Emails/QuestFinalized.razor QuestBoard.Service/Components/Emails/SessionReminder.razor QuestBoard.Service/Components/Emails/WaitlistPromoted.razor && ! grep -rn "overflow-y" QuestBoard.Service/Components/Emails/QuestFinalized.razor QuestBoard.Service/Components/Emails/SessionReminder.razor QuestBoard.Service/Components/Emails/WaitlistPromoted.razor</automated>
  </verify>
  <done>All 3 templates render Quest Description through RenderEmailHtml with QuestUrl as the read-more target, the Outlook-incompatible scroll wrapper is gone, the 840px poster card is untouched, and the project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add WaitlistPromoted preview action + structured-Markdown dev-loop samples</name>
  <files>QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs</files>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs — the `SessionReminder()` action (lines 128-143) is the copy analog; `QuestFinalized()` (lines 41-56) for the `nameof(...)`-keyed dictionary; `Index()` (lines 16-38) for the link list; note the class is `[Authorize(Policy = "AdminOnly")]`
    - QuestBoard.Service/Components/Emails/WaitlistPromoted.razor — its `@code` block (lines 96-105) for the exact parameter set: QuestTitle, DmName, QuestDate, QuestDescription, PlayerName, QuestUrl, ChallengeRating, AppUrl (note PlayerName, NOT ConfirmedPlayerNames)
    - .planning/phases/71-email-safety-hardening/71-RESEARCH.md — "Test-Email Trigger Mechanism" (EmailPreviewController is browser-render-only, a dev loop, not a substitute for the Plan 03 live send)
  </read_first>
  <action>
    Add a private const `SampleMarkdownDescription` to `EmailPreviewController` containing a structured-Markdown sample that exercises the new styling and truncation: at least one heading, an unordered (and ideally an ordered) list, a blockquote, and enough total content to exceed the 5-top-level-block / 650-plain-text-char budget so the truncation + "…continue reading on the quest board" link is visible in-browser. Use plain flavor text (no secrets, no real URLs).

    Add a new `[HttpGet] public async Task<IActionResult> WaitlistPromoted()` action mirroring the existing `SessionReminder()` action, rendering `Components.Emails.WaitlistPromoted` via `emailRenderService.RenderAsync<...>` with a `nameof(...)`-keyed dictionary supplying WaitlistPromoted's actual parameters: QuestTitle, DmName, QuestDate, `QuestDescription = SampleMarkdownDescription`, PlayerName, `QuestUrl = $"{appUrl}/Quest"`, ChallengeRating, AppUrl. Return `Content(html, "text/html")`.

    Point the existing `QuestFinalized()` and `SessionReminder()` actions' `QuestDescription` values at the same `SampleMarkdownDescription` const (replacing their current flat-prose sample) so all 3 quest-email previews render the new structured styling in the browser dev-loop.

    Add `<li><a href="{{appUrl}}/EmailPreview/WaitlistPromoted">Waitlist Promoted</a></li>` to the `Index()` action's `<ul>` link list (currently missing).
  </action>
  <acceptance_criteria>
    - `EmailPreviewController` has a `WaitlistPromoted()` `[HttpGet]` action returning rendered `text/html`, keyed by WaitlistPromoted's real parameters including `PlayerName`
    - `SampleMarkdownDescription` const exists and contains at least one heading, one list, and one blockquote, and is used by the QuestFinalized, SessionReminder, and WaitlistPromoted preview actions
    - `Index()` lists a "Waitlist Promoted" link to `/EmailPreview/WaitlistPromoted`
    - `dotnet build` succeeds; the controller stays `[Authorize(Policy = "AdminOnly")]`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build QuestBoard.Service && grep -c "WaitlistPromoted" QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs && grep -c "SampleMarkdownDescription" QuestBoard.Service/Controllers/Admin/EmailPreviewController.cs</automated>
  </verify>
  <done>An Admin-only WaitlistPromoted browser preview exists, all 3 quest-email previews render a structured-Markdown sample that exercises styling + truncation, the Index page links to the new preview, and the project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Razor template → RenderEmailHtml | QuestDescription (user content) continues to flow through RenderEmailHtml, which sanitizes via EmailSanitizer; no new unsanitized MarkupString path is introduced |
| Admin browser → EmailPreviewController | Admin-only, browser-render-only; renders hard-coded sample data, sends no email |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-05 | Information Disclosure | EmailPreviewController preview actions | low | accept | `[Authorize(Policy = "AdminOnly")]` gate unchanged; all preview data is hard-coded sample content, no real user data, no email dispatch. |
| T-71-06 | Tampering | template Description call-site swap | low | mitigate | QuestDescription still passes through `RenderEmailHtml` (which sanitizes via `EmailSanitizer` before styling); no raw/unsanitized `MarkupString` of user input is introduced. |
| T-71-SC | Tampering | package installs | low | accept | No new packages this phase; no `PackageReference` changes. |
</threat_model>

<verification>
- `dotnet build` succeeds after each task
- `dotnet test` full suite green after the wave (no regression to the Domain tests from Plan 01)
- Manual dev-loop (optional, recommended before Plan 03): as Admin, open `/EmailPreview/QuestFinalized`, `/EmailPreview/SessionReminder`, `/EmailPreview/WaitlistPromoted` and confirm headings/lists/blockquotes render styled and long content truncates with the read-more link — this browser preview does NOT replace the Plan 03 live Outlook/Gmail verification (it cannot surface Outlook-specific defects)
- If `dotnet build` fails with locked-file errors, ask the operator to stop the Visual Studio debugger (Shift+F5)
</verification>

<success_criteria>
The 3 quest email templates render Quest Description through `RenderEmailHtml(QuestDescription, QuestUrl)` with the Outlook-incompatible scroll wrapper removed and the 840px card intact, and an Admin-only `WaitlistPromoted` browser preview plus structured-Markdown samples exist for the dev-loop.
</success_criteria>

<output>
Create `.planning/phases/71-email-safety-hardening/71-02-SUMMARY.md` when done.
</output>
