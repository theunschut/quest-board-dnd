---
phase: 80-contact-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/80-contact-categories/80-VALIDATION.md
autonomous: true
requirements:
  - CONTACTCAT-01
  - CONTACTCAT-02
  - CONTACTCAT-03
  - CONTACTCAT-04
  - CONTACTCAT-05
  - CONTACTCAT-06
  - CONTACTCAT-07
  - CONTACTCAT-08
  - CONTACTCAT-09
  - CONTACTCAT-10
  - CONTACTCAT-11
  - CONTACTCAT-12
  - CONTACTCAT-13
  - CONTACTCAT-14
  - CONTACTCAT-15

must_haves:
  truths:
    - "Every Phase 80 behaviour has a requirement ID traceable from REQUIREMENTS.md to a plan and from a plan to an automated command."
    - "The requirements-coverage tooling resolves Phase 80 to a real ID list instead of a TBD placeholder."
    - "80-VALIDATION.md names the exact plan and task that proves each requirement, so a dropped decision is visible before execution rather than after."
  artifacts:
    - ".planning/REQUIREMENTS.md contains a `### Contact Categories` section with CONTACTCAT-01 through CONTACTCAT-15"
    - ".planning/REQUIREMENTS.md Traceability table contains fifteen Phase 80 rows"
    - ".planning/ROADMAP.md `## Requirements Coverage` table contains fifteen CONTACTCAT rows mapped to Phase 80"
    - ".planning/ROADMAP.md § Phase 80 carries a real `**Requirements**:` list and a `Plans:` list of eight plans"
    - "80-VALIDATION.md Per-Task Verification Map is re-keyed from D-* to CONTACTCAT-* and names real 80-NN task ids"
  key_links:
    - "REQUIREMENTS.md ID list -> ROADMAP.md Requirements Coverage table -> each plan's `requirements` frontmatter"
    - "80-VALIDATION.md Per-Task Verification Map -> the `<automated>` command on the task it names"
  prohibitions:
    - "No source file under QuestBoard.* is touched by this plan."
    - "No requirement is minted for a Deferred Idea (bulk assignment, drag-and-drop refiling, per-contact ordering, category icons/colours/descriptions, collapsible sections, tags/filtering)."
---

<objective>
Mint the `CONTACTCAT-*` requirement family for Phase 80 and wire it through every tracking
surface, then complete the phase's validation contract so each requirement names the plan and
task that proves it.

Phase 80 currently has no requirement IDs at all: `.planning/ROADMAP.md` says
`**Requirements**: TBD` and `.planning/REQUIREMENTS.md` has no Contact Categories section.
`80-VALIDATION.md` is therefore keyed by CONTEXT.md decision id (`D-04`, `D-17`, …) and its own
header says to re-key it once the minting plan lands. Without this plan the requirements-coverage
gate has nothing to check and every later plan's `requirements` field would be empty.

Purpose: make the rest of the phase traceable and auditable.
Output: fifteen requirement IDs defined, mapped, and bound to concrete automated commands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-contact-categories/80-VALIDATION.md
@.planning/phases/80-contact-categories/80-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan creates no source symbols. It creates the following documentation identifiers:

- Requirement IDs `CONTACTCAT-01` … `CONTACTCAT-15` (new ID family, new prefix)
- A `### Contact Categories` heading in `.planning/REQUIREMENTS.md`
- Fifteen new rows in the `.planning/REQUIREMENTS.md` `## Traceability` table
- Fifteen new rows in the `.planning/ROADMAP.md` `## Requirements Coverage` table
- A re-keyed Per-Task Verification Map and Validation Sign-Off in `80-VALIDATION.md`

The source symbols this **phase** produces are listed identically in every later plan; see
`80-02-PLAN.md` for the canonical list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mint CONTACTCAT-01..15 into REQUIREMENTS.md</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — read the whole file. `### Personal Cross-Board Event Agenda` (line 77) and `### Link Previews — Characters and Contacts` (line 102) show the exact section format; the `## Traceability` table (line 147) and the `**Coverage:**` counter block at the end of the file both need new entries.
    - `.planning/phases/80-contact-categories/80-CONTEXT.md` § `<decisions>` — D-01 through D-20, the source each requirement restates, and § `<deferred>`, which lists what must NOT get an ID.
    - `.planning/phases/80-contact-categories/80-RESEARCH.md` § `## Phase Requirements` — the decision-to-research-support table confirming every decision has a verified implementation path.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
Add a new section `### Contact Categories` to `.planning/REQUIREMENTS.md`, placed immediately
after the existing `### Link Previews — Characters and Contacts` section and before
`## Future Requirements`, so the new family lands after the last v9.0 feature family.

Use the file's existing item format exactly: `- [ ] **ID**: description`, unchecked, one line
per requirement, no trailing period. Write these fifteen verbatim:

- `CONTACTCAT-01`: A DungeonMaster-tier user can create a named category from a dedicated Manage Categories page reached from a button on the Contacts index, and the category belongs to the board it was created on
- `CONTACTCAT-02`: A contact belongs to exactly one category or to none, assigned from a single dropdown with a blank "— None —" option on the contact Create and Edit forms on both desktop and mobile
- `CONTACTCAT-03`: A DungeonMaster-tier user can rename and delete a category, and deleting a category that still holds contacts moves those contacts to Ungrouped rather than deleting them or blocking the delete
- `CONTACTCAT-04`: Category names are unique per board, compared case-insensitively, and a duplicate submission returns a validation message on the form rather than an unhandled server error
- `CONTACTCAT-05`: Every category read and write is scoped to the active board by the global query filter, a request with no active board resolves zero categories, and no application code path bypasses the filter
- `CONTACTCAT-06`: Only DungeonMaster-tier users can create, rename, delete, or reorder categories, enforced server-side so a player who guesses the URL is refused
- `CONTACTCAT-07`: A DungeonMaster-tier user can reorder categories with up and down controls on the Manage Categories page, and the Contacts index renders headings in that order rather than alphabetically
- `CONTACTCAT-08`: The Manage Categories page ships as both a desktop and a mobile view, and the mobile view is proven to be the one actually selected under a real mobile User-Agent
- `CONTACTCAT-09`: The Contacts index renders contacts under their category headings on both desktop and mobile, with contacts sorted alphabetically by name within each heading
- `CONTACTCAT-10`: Contacts with no category render under a synthetic "Ungrouped" heading that is pinned after every real category and is neither renameable nor orderable
- `CONTACTCAT-11`: A board with no categories renders the flat contact list exactly as it renders today, with no category headings at all including no Ungrouped heading
- `CONTACTCAT-12`: A category heading renders only when at least one contact beneath it is visible to the viewer, and the heading carries the category name alone with no contact count
- `CONTACTCAT-13`: A category name is stored with a 60-character cap and rendered as plain escaped text, never routed through the Markdown pipeline
- `CONTACTCAT-14`: A contact's category is shown on the contact Details view on both desktop and mobile, and a contact with no category shows no category line at all
- `CONTACTCAT-15`: On a board with no categories, the contact Create and Edit forms render the category select disabled with helper text linking to the Manage Categories page

Do not mint an ID for anything in CONTEXT.md's Deferred Ideas: bulk assignment, drag-and-drop
refiling, per-contact ordering within a category, category icons/colours/descriptions,
collapsible sections, and tags/filtering all belong to later phases.

Then append fifteen rows to the `## Traceability` table, after the last `EVTAGENDA-10` row, in
the existing `| ID | Phase | Status |` format with `Phase 80` and `Not started`.

Finally update the `**Coverage:**` block at the end of the file: `- v1 requirements: 60 total`
becomes `- v1 requirements: 75 total` and `- Mapped to phases: 60/60 ✓` becomes
`- Mapped to phases: 75/75 ✓`. Leave the `Unmapped: 0` line as is.

Write the file with CRLF line endings — this repository uses CRLF and REQUIREMENTS.md is
already CRLF.
  </action>

  <acceptance_criteria>
    - `grep -c 'CONTACTCAT-' .planning/REQUIREMENTS.md` outputs `30` (fifteen definitions plus fifteen traceability rows)
    - `.planning/REQUIREMENTS.md` contains the literal heading `### Contact Categories`
    - `grep -cE '^\| CONTACTCAT-[0-9]{2} \| Phase 80 \| Not started \|$' .planning/REQUIREMENTS.md` outputs `15`
    - `.planning/REQUIREMENTS.md` contains `- [ ] **CONTACTCAT-01**:` and `- [ ] **CONTACTCAT-15**:`
    - `.planning/REQUIREMENTS.md` contains `- v1 requirements: 75 total` and `- Mapped to phases: 75/75`
    - No deferred idea is minted: `grep -iEc 'CONTACTCAT-[0-9]{2}.*(bulk assign|drag-and-drop|collapsib|tag)' .planning/REQUIREMENTS.md` outputs `0`
    - `file .planning/REQUIREMENTS.md` still reports `CRLF line terminators`
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Repos/quest-board" &amp;&amp; test "$(grep -c 'CONTACTCAT-' .planning/REQUIREMENTS.md)" = "30" &amp;&amp; grep -q '### Contact Categories' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Mapped to phases: 75/75' .planning/REQUIREMENTS.md &amp;&amp; test "$(grep -cE '^\| CONTACTCAT-[0-9]{2} \| Phase 80 \| Not started \|$' .planning/REQUIREMENTS.md)" = "15" &amp;&amp; echo OK</automated>
  </verify>

  <done>
    Fifteen CONTACTCAT requirements are defined in their own section, listed in the traceability
    table against Phase 80, and the coverage counters reconcile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Map CONTACTCAT-01..15 into ROADMAP.md coverage and finalize the Phase 80 entry</name>

  <read_first>
    - `.planning/ROADMAP.md` — the `## Requirements Coverage` table starting at line 571 (rows run in phase order and currently end with the `EVTAGENDA-*` Phase 82 block), and the `### Phase 80: Contact Categories` entry near line 477 whose `**Requirements**:` and `**Plans**:` lines both read `TBD`.
    - `.planning/REQUIREMENTS.md` — as written by Task 1, so the IDs match exactly.
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
Two scoped edits to `.planning/ROADMAP.md`. Use targeted replacements; never rewrite the whole
file — it carries every other phase entry.

Edit one: insert fifteen rows into the `## Requirements Coverage` table in the file's exact
`| Requirement | Phase |` two-column format — `| CONTACTCAT-01 | Phase 80 |` through
`| CONTACTCAT-15 | Phase 80 |`, one per line in ascending ID order. Insert them **after** the
last `| LINKCARD-06 | Phase 79 |` row and **before** the first `| EVTAGENDA-01 | Phase 82 |`
row, so the table stays ordered by phase number.

Edit two: in the `### Phase 80: Contact Categories` entry, replace the line
`**Requirements**: TBD` with a comma-separated list of all fifteen IDs in ascending order, and
replace the line `**Plans**: TBD (run `/gsd-plan-phase 80`)` with `**Plans**: 8 plans`. Then,
directly beneath the `**Plans**:` line and before the `**Origin:**` paragraph, add a `Plans:`
block grouped by wave, matching the style used by the Phase 82 entry:

Wave 1 — `80-01-PLAN.md` (mint the CONTACTCAT requirement family and complete the phase
validation contract) and `80-02-PLAN.md` (ContactCategory entity, schema, migration, and the
test seeding helper). Wave 2 — `80-03-PLAN.md` (category repository, service, DI wiring, and
the unit suite). Wave 3 — `80-04-PLAN.md` (view models, AutoMapper wiring, and category CSS).
Wave 4 — `80-05-PLAN.md` (Manage Categories controller, desktop and mobile views, and the
management integration suite). Wave 5 — `80-06-PLAN.md` (grouped Contacts index on both
platforms plus the suppression and ordering suite). Wave 6 — `80-07-PLAN.md` (category
assignment on the four Create/Edit forms plus the cross-group isolation suite) and
`80-08-PLAN.md` (category on both Details views plus the real-User-Agent mobile render suite).
Each entry is an unchecked `- [ ] {file} — {description}` item, with waves 2 through 6 labelled
`*(blocked on Wave N-1 completion)*`.

Touch nothing else. In particular do not edit the `### Phase 81: Contact Tags and Filtering`
entry — Phase 81 stays `Requirements: TBD` on purpose.

`.planning/ROADMAP.md` is CRLF; keep it CRLF.
  </action>

  <acceptance_criteria>
    - `grep -cE '^\| CONTACTCAT-[0-9]{2} \| Phase 80 \|$' .planning/ROADMAP.md` outputs `15`
    - The coverage table stays phase-ordered: `grep -n 'LINKCARD-06 | Phase 79' .planning/ROADMAP.md` reports a smaller line number than `grep -n 'CONTACTCAT-01 | Phase 80' .planning/ROADMAP.md`, which in turn reports a smaller line number than `grep -n 'EVTAGENDA-01 | Phase 82' .planning/ROADMAP.md`
    - The Phase 80 entry no longer carries the placeholder: `awk '/^### Phase 80:/,/^### Phase 81:/' .planning/ROADMAP.md | grep -c 'TBD'` outputs `0`
    - The Phase 80 entry lists eight plans: `awk '/^### Phase 80:/,/^### Phase 81:/' .planning/ROADMAP.md | grep -c '^- \[ \] 80-0[1-8]-PLAN.md'` outputs `8`
    - Phase 81 is untouched: `awk '/^### Phase 81:/,/^### Phase 82:/' .planning/ROADMAP.md | grep -c 'Requirements\*\*: TBD'` outputs `1`
    - Every ID in the ROADMAP coverage table also exists in REQUIREMENTS.md: `comm -13 <(grep -o 'CONTACTCAT-[0-9]*' .planning/REQUIREMENTS.md | sort -u) <(grep -o 'CONTACTCAT-[0-9]*' .planning/ROADMAP.md | sort -u)` produces no output
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Repos/quest-board" &amp;&amp; test "$(grep -cE '^\| CONTACTCAT-[0-9]{2} \| Phase 80 \|$' .planning/ROADMAP.md)" = "15" &amp;&amp; test "$(awk '/^### Phase 80:/,/^### Phase 81:/' .planning/ROADMAP.md | grep -c 'TBD')" = "0" &amp;&amp; test -z "$(comm -13 <(grep -o 'CONTACTCAT-[0-9]*' .planning/REQUIREMENTS.md | sort -u) <(grep -o 'CONTACTCAT-[0-9]*' .planning/ROADMAP.md | sort -u))" &amp;&amp; echo OK</automated>
  </verify>

  <done>
    The roadmap maps all fifteen CONTACTCAT requirements to Phase 80, the Phase 80 entry lists
    its eight plans by wave, no ID exists in the roadmap that is missing from REQUIREMENTS.md,
    and Phase 81's placeholder is intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-key and complete the phase validation contract in 80-VALIDATION.md</name>

  <read_first>
    - `.planning/phases/80-contact-categories/80-VALIDATION.md` — read the whole file. The header blockquote, the Per-Task Verification Map, the Wave 0 Requirements checklist, and the Validation Sign-Off block are edited by this task; the Test Infrastructure table, Sampling Rate, Manual-Only Verifications, and the Conflict Carried Forward From Research note are already correct and must be left as written.
    - `.planning/phases/80-contact-categories/80-CONTEXT.md` § `<decisions>` — the D-* ids currently in the Requirement column, so the re-key maps each one to the right CONTACTCAT id.
    - `.planning/REQUIREMENTS.md` — as written by Task 1, so the CONTACTCAT descriptions and the map's Behavior column agree.
  </read_first>

  <files>.planning/phases/80-contact-categories/80-VALIDATION.md</files>

  <action>
Rewrite the `## Per-Task Verification Map` so its Requirement column names CONTACTCAT ids
instead of D-* ids and its Task ID / Plan / Wave columns name real plans. Drop the header
blockquote's now-stale sentence about the planner still having to mint the family, and keep the
rest of that blockquote.

Keep the existing nine-column header
`| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |`
and rewrite the rows as follows. The Secure Behavior and Test Type columns keep their current
wording; only Task ID, Plan, Wave, Requirement, Automated Command and File Exists change. Set
File Exists to `✅ created by {plan}` for every row and Status to `⬜ pending`.

- `80-07 T3` / `80-07` / wave 6 / `CONTACTCAT-05` — cross-group isolation — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategory_CrossGroup"`
- `80-07 T3` / `80-07` / wave 6 / `CONTACTCAT-05` — fail-closed filter, null active group returns zero categories — same command
- `80-06 T3` / `80-06` / wave 5 / `CONTACTCAT-12` — empty-heading suppression both directions — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategory_EmptyHeadingSuppression"`
- `80-06 T3` / `80-06` / wave 5 / `CONTACTCAT-07`, `CONTACTCAT-09`, `CONTACTCAT-10`, `CONTACTCAT-11` — ordering, alphabetical within, Ungrouped last, zero-category flat list — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactsIndex_CategoryOrdering"`
- `80-05 T3` / `80-05` / wave 4 / `CONTACTCAT-03` — delete orphans, asserted by reading the database directly — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategory_DeleteOrphans"`
- `80-05 T3` / `80-05` / wave 4 / `CONTACTCAT-04` — case-insensitive duplicate name surfaces as a validation message, not a 500 — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategory_DuplicateName"`
- `80-08 T2` / `80-08` / wave 6 / `CONTACTCAT-08` — Manage mobile view actually selected under a real mobile User-Agent — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategoryMobileRender"`
- `80-08 T2` / `80-08` / wave 6 / `CONTACTCAT-09`, `CONTACTCAT-10`, `CONTACTCAT-14` — grouped headings, the Ungrouped block, and the Details category line render on the mobile views under the same mechanism — same command
- `80-05 T3` / `80-05` / wave 4 / `CONTACTCAT-06` — every category write refused for a plain player; the policy sits at class level — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategoryManagement"`
- `80-06 T3` / `80-06` / wave 5 / `CONTACTCAT-13` — a category name containing markup is HTML-escaped in the heading and never routed through the Markdown service; 60-character cap enforced — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategory_NameRendersEscaped"`
- `80-02 T2`, `80-03 T3` / `80-02`, `80-03` / waves 1 and 2 / `CONTACTCAT-01`, `CONTACTCAT-02` — nullable FK plus optional navigation; the migration emits SetNull on the Category FK and NoAction on the Group FK — `dotnet test QuestBoard.UnitTests --filter "FullyQualifiedName~ContactCategoryRepository"`
- `80-08 T1` / `80-08` / wave 6 / `CONTACTCAT-14` — category name reaches both Details views after being added to the entity, the domain model, the view model and both AutoMapper profiles — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactDetails_Category"`
- Add one new row not present today: `80-07 T2` / `80-07` / wave 6 / `CONTACTCAT-15` — on a board with zero categories the Create and Edit forms render a disabled select with the Manage Categories link — `dotnet test QuestBoard.IntegrationTests --filter "FullyQualifiedName~ContactCategory_DisabledSelect"`

In `## Wave 0 Requirements`, tick every checklist item and append the owning plan and task in
brackets: the seeding helper is `80-02 T3`; the new management integration test class is
`80-05 T3`; the new Facts on the existing Contacts integration class are `80-06 T3` and
`80-07 T3`; cross-group and delete-orphan coverage is `80-07 T3` and `80-05 T3`; the new unit
test pair is `80-03 T3`; the ContactRepository include extension is `80-03 T3`. Leave the
framework-install line and both explanatory notes (the mobile-render mechanism note and the
`IgnoreQueryFilters()` scope note) exactly as written.

In `## Validation Sign-Off`, tick all seven boxes, including the Requirement-column re-key box,
and replace the approval line with
`**Approval:** signed by planner — mapped to 80-01 … 80-08 task ids`.

Leave `nyquist_compliant: true`, `status: ready` and `wave_0_complete: false` in the
frontmatter alone — `wave_0_complete` is flipped by execution, not by planning.

Write with CRLF line endings.
  </action>

  <acceptance_criteria>
    - `grep -c 'CONTACTCAT-' .planning/phases/80-contact-categories/80-VALIDATION.md` outputs at least `15`
    - The Requirement column no longer carries decision ids: `grep -cE '^\| 80-0[0-9] T[0-9].*\| D-[0-9]{2}' .planning/phases/80-contact-categories/80-VALIDATION.md` outputs `0`
    - The file contains the task references `80-02 T3`, `80-03 T3`, `80-05 T3`, `80-06 T3`, `80-07 T3`, and `80-08 T2`
    - The file contains the filter name `ContactCategory_DisabledSelect`
    - `grep -c '^- \[ \]' .planning/phases/80-contact-categories/80-VALIDATION.md` outputs `0` — every checklist box in the file is ticked
    - The file contains `**Approval:** signed by planner`
    - The `IgnoreQueryFilters()` scope note survives the rewrite: the file still contains `is test infrastructure, not an application path`
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Repos/quest-board" &amp;&amp; V=.planning/phases/80-contact-categories/80-VALIDATION.md &amp;&amp; test "$(grep -c '^- \[ \]' "$V")" = "0" &amp;&amp; grep -q '80-08 T2' "$V" &amp;&amp; grep -q 'ContactCategory_DisabledSelect' "$V" &amp;&amp; grep -q 'Approval:\*\* signed by planner' "$V" &amp;&amp; test "$(grep -c 'CONTACTCAT-' "$V")" -ge "15" &amp;&amp; echo OK</automated>
  </verify>

  <done>
    80-VALIDATION.md names a real plan and task for every requirement, its Requirement column is
    keyed to CONTACTCAT ids, every checklist box is ticked, and the sign-off is signed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → executor agents | These files are the instruction source for later plans; a wrong or missing requirement here silently removes a control from the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-01-01 | Repudiation | Requirement traceability | medium | mitigate | Every CONTACTCAT id appears in REQUIREMENTS.md, the ROADMAP coverage table, and at least one plan's `requirements` frontmatter; Task 2's acceptance criterion diffs the two ID sets so an id can never exist in one place only |
| T-80-01-02 | Tampering | Validation contract | medium | mitigate | The Per-Task Verification Map binds each requirement to a named task and a runnable command, so a later plan cannot quietly drop the cross-group isolation suite without leaving an unmapped row |
| T-80-01-03 | Information Disclosure | — | low | accept | This plan writes no code and touches no runtime surface; it cannot itself disclose data |
| T-80-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package manager runs in this phase. `80-RESEARCH.md` § Package Legitimacy Audit records "Not applicable — this phase introduces zero new NuGet packages, zero new npm packages, and zero new external dependencies of any kind." No install task exists in any Phase 80 plan, so there is nothing to gate |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `80-VALIDATION.md` all agree on the same fifteen ids.
- No ID appears in one tracking file and not the others.
- Every plan in this phase declares a non-empty `requirements` list drawn only from these fifteen ids.
- Phase 81's ROADMAP entry still reads `Requirements: TBD`.
</verification>

<success_criteria>
- Fifteen CONTACTCAT requirements defined, mapped to Phase 80 in both tracking files, and bound to a runnable command each.
- `80-VALIDATION.md` carries CONTACTCAT ids, real task ids, and a signed sign-off.
- No source file is touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/80-contact-categories/80-01-SUMMARY.md` when done
</output>
