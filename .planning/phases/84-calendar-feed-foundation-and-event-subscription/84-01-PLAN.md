---
phase: 84-calendar-feed-foundation-and-event-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md
autonomous: true
requirements:
  - CALFEED-01
  - CALFEED-02
  - CALFEED-03
  - CALFEED-04
  - CALFEED-05
  - CALFEED-06
  - CALFEED-07
  - CALFEED-08
  - CALFEED-09
  - CALFEED-10
  - CALFEED-11
  - CALFEED-12
  - CALFEED-13
  - CALFEED-14
  - CALFEED-15
  - CALFEED-16

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md carries a Calendar Feed section holding CALFEED-01 through CALFEED-16, each an unchecked one-sentence observable outcome"
    - "The Traceability table carries one row per CALFEED id, each mapped to Phase 84 with status Pending"
    - "ROADMAP.md's Phase 84 section names all sixteen CALFEED ids instead of TBD, and its Plans line and plan list name the eight plans of this phase grouped by wave"
    - "ROADMAP.md's Requirements Coverage table carries one CALFEED row per id mapped to Phase 84"
    - "84-VALIDATION.md's per-task verification map names real task ids from this phase's plans instead of TBD"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md
  key_links:
    - "Every CALFEED id minted here is claimed by at least one of plans 84-02 through 84-08 in its requirements frontmatter"
    - "Every task id written into 84-VALIDATION.md exists as a named task in the plan it cites"
---

<objective>
Mint the `CALFEED-*` requirement family for Phase 84, register it in all three ledgers that must agree (REQUIREMENTS.md body, REQUIREMENTS.md Traceability table, ROADMAP.md), and finish the validation contract by replacing its placeholder task ids with this phase's real ones.

Purpose: ROADMAP.md line 792 currently reads `**Requirements**: TBD`. Until the family exists, no other plan in this phase can carry a non-empty `requirements` field, and nothing downstream can check coverage. This plan is deliberately documentation-only so it runs in parallel with the tracer.
Output: sixteen registered requirement ids, a wave-grouped plan list on the roadmap, and a validation map keyed by real task ids.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-CONTEXT.md
@.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md
</context>

<recorded_skips>
## Spec-less probe fallback — recorded skip

The spec-less probe fallback ran its gate and skipped. No `SPEC.md` exists for Phase 84 (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`) and the phase had no requirement ids to probe, because they are being minted in this very plan. The gate's note, verbatim:

> `spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).`

`$COVERAGE` was empty. No probe-derived edge predicates and no probe-derived prohibitions were authored this run, and no `must_haves.prohibitions:` block appears in any Phase 84 plan because no prohibitions source exists.

Consequence, compensated for deliberately: neither the SPEC edge-coverage path nor the probe fallback contributed any `must_haves.truths` this phase. The `## UI Considerations` lift from `84-UI-SPEC.md` (plans 84-07 and 84-08) is therefore the only structured truth source, and it covers the UI state axis only. Every backend truth in plans 84-02 through 84-06 was derived directly from the ROADMAP goal and the eighteen CONTEXT.md decisions rather than from a resolved edge.

## API coverage — recorded skip

The api-coverage detector returned `detected: false`. No `COVERAGE.md` is required for this phase and no coverage matrix was fabricated.

## Schema-push gate — recorded skip

The schema-push gate found no Payload/Prisma/Drizzle/Supabase/TypeORM file patterns. This is an EF Core project whose migrations auto-apply on startup through `context.Database.Migrate()`, so no `[BLOCKING]` schema-push task exists. The migration added in plan 84-02 is ordinary work.
</recorded_skips>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan produces no source symbols. The full phase-wide symbol inventory — every class, interface, method, entity property, migration, options key, controller action, route, ViewModel, view, CSS class, package and test class Phase 84 creates — is listed in `84-02-PLAN.md`'s `<artifacts_this_phase_produces>` section, with per-plan detail in each of 84-03 through 84-08. Documentation artifacts produced here:

- `.planning/REQUIREMENTS.md` — new `### Calendar Feed — Foundation and Event Subscription` section holding `CALFEED-01` … `CALFEED-16`, plus sixteen new `## Traceability` rows and an updated coverage count
- `.planning/ROADMAP.md` — Phase 84 `**Requirements**` line, `**Plans**` line, `Plans:` list, and sixteen new `## Requirements Coverage` rows
- `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md` — per-task verification map rows keyed by real task ids
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mint the sixteen CALFEED requirements into REQUIREMENTS.md and its Traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read the `### Availability Surface Naming and Placement` section (currently the last section under `## v1 Requirements`, around line 90) for the exact bullet format, then the `## Traceability` table header and its final rows, then the `**Coverage:**` block at the end of the file
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-CONTEXT.md` — the `<decisions>` block, so each requirement traces to a real decision rather than invented scope
    - `.planning/ROADMAP.md` lines 789-826 — the Phase 84 goal and scope notes
  </read_first>
  <action>
Insert a new section `### Calendar Feed — Foundation and Event Subscription` into `.planning/REQUIREMENTS.md` under `## v1 Requirements`, immediately after the existing `### Availability Surface Naming and Placement` section and before `### Link Previews — Foundation and Quests`. Use the exact bullet shape the existing sections use but unchecked, because these are new: `- [ ] **CALFEED-NN**: <sentence>`.

Write these sixteen lines verbatim:

- `- [ ] **CALFEED-01**: A board member holds no calendar subscription until they press Add on their Profile page, and pressing Add mints exactly one new subscription carrying a 256-bit cryptographically random URL-safe address`
- `- [ ] **CALFEED-02**: A member can hold several calendar subscriptions at once, each separately named, separately addressed and separately revocable, so retiring one device leaves every other device working`
- `- [ ] **CALFEED-03**: Every subscription row on Profile shows its name, its full re-copyable address, the date it was created and when it was last fetched, and offers rename and delete`
- `- [ ] **CALFEED-04**: Deleting a subscription retires it as a tombstone rather than removing the row, so its address answers 410 Gone from then on while an address that never existed answers 404 Not Found`
- `- [ ] **CALFEED-05**: A subscription's address serves a valid text/calendar document to an anonymous caller that sends no cookie, holds no session and has no active board, with the address itself as the only credential`
- `- [ ] **CALFEED-06**: The feed carries exactly the events the subscription's owner holds a signup row on, drawn from every board they are still a member of, and never an event from a board they do not belong to`
- `- [ ] **CALFEED-07**: A row that survives the feed query's board predicate but falls outside the owner's membership set is dropped before it reaches the response and recorded as an error in the application log`
- `- [ ] **CALFEED-08**: A cancelled event never appears in the feed, on any board, for any subscription`
- `- [ ] **CALFEED-09**: Each calendar entry's title is the board name in square brackets followed by the event title, with "(maybe)" or "(declined)" appended when the owner answered that way and nothing appended otherwise`
- `- [ ] **CALFEED-10**: An event with a start time becomes a one-hour entry in floating local time with no timezone declared, and an event with no start time becomes a true all-day entry occupying exactly one day`
- `- [ ] **CALFEED-11**: Every calendar entry is marked transparent so a subscriber never reads as busy, and carries no description, no link and no alarm`
- `- [ ] **CALFEED-12**: Each calendar entry's identifier is unchanged across repeated fetches of the same occurrence and is namespaced by its source, so a phone updates an entry in place and a future quest source cannot collide with an event of the same numeric id`
- `- [ ] **CALFEED-13**: The feed covers a rolling window of recent past and upcoming months, recomputed on every fetch, with both bounds changeable through configuration and no code change`
- `- [ ] **CALFEED-14**: Both the desktop and the mobile Profile layout carry the subscription section, and every row offers a copy control, a webcal link and a scannable QR code for its address`
- `- [ ] **CALFEED-15**: The feed endpoint is rate limited per address and no application log line ever contains a full subscription address`
- `- [ ] **CALFEED-16**: Repeated fetching of one address updates its last-fetched timestamp at most once per throttle interval, so a hammered address cannot become a database write storm`

Then append sixteen rows to the `## Traceability` table at the end of the file, after the final `| CONTACTTAG-17 | Phase 81 | Not started |` row, one per id in numeric order, each exactly `| CALFEED-NN | Phase 84 | Pending |`.

Finally update the `**Coverage:**` block: `v1 requirements` rises from 99 to 115, and `Mapped to phases` from `99/99 ✓` to `115/115 ✓`. `Unmapped` stays 0.

Do not renumber, reword, re-check or re-uncheck any existing requirement. Do not add a requirement for anything in CONTEXT.md's `## Deferred Ideas` block — quests in the feed, a per-board feed URL, a never-answered marker, and renaming `EmailSettings:AppUrl` are all out of scope for this phase.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*CALFEED-' /mnt/Data/repos/quest-board-dnd/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*CALFEED-' .planning/REQUIREMENTS.md` outputs exactly `16`
    - `grep -c '^| CALFEED-[0-9][0-9] | Phase 84 | Pending |$' .planning/REQUIREMENTS.md` outputs exactly `16`
    - `grep -c '^### Calendar Feed — Foundation and Event Subscription$' .planning/REQUIREMENTS.md` outputs exactly `1`
    - For each of `01 02 03 04 05 06 07 08 09 10 11 12 13 14 15 16`, `grep -c "CALFEED-$n" .planning/REQUIREMENTS.md` is at least `2` (one body bullet plus one traceability row)
    - `.planning/REQUIREMENTS.md` contains `- v1 requirements: 115 total` and `- Mapped to phases: 115/115 ✓`
    - `grep -c 'EVTNAME-07' .planning/REQUIREMENTS.md` is unchanged at its pre-edit value (no existing requirement was disturbed)
  </acceptance_criteria>
  <done>All sixteen CALFEED ids exist as unchecked v1 requirements and as Pending Phase 84 traceability rows, and the coverage count reflects 115 total requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ROADMAP.md's Phase 84 TBD requirements, plan count and plan list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 750-800 — the completed Phase 83 entry, for the exact shape of a settled `**Requirements**` line, `**Plans**: N/N plans complete` line, and a wave-grouped `Plans:` list with `**Wave 1**` / `**Wave 2** *(blocked on Wave 1 completion)*` headings
    - `.planning/ROADMAP.md` lines 789-826 — the Phase 84 entry being edited
    - `.planning/ROADMAP.md` lines 695-720 — the `## Requirements Coverage` table rows, for the two-column `| ID | Phase N |` shape
  </read_first>
  <action>
Make four scoped edits to `.planning/ROADMAP.md`. Use targeted replacements only — never rewrite the whole file, because every other phase entry lives in it.

1. In the `### Phase 84: Calendar Feed Foundation and Event Subscription` section, replace the line `**Requirements**: TBD` with the comma-separated list in numeric order: `**Requirements**: CALFEED-01, CALFEED-02, CALFEED-03, CALFEED-04, CALFEED-05, CALFEED-06, CALFEED-07, CALFEED-08, CALFEED-09, CALFEED-10, CALFEED-11, CALFEED-12, CALFEED-13, CALFEED-14, CALFEED-15, CALFEED-16`

2. In the same section replace `**Plans**: 0 plans` with `**Plans**: 0/8 plans complete`. Phase 85's identical `**Plans**: 0 plans` line must be left untouched — anchor the replacement on the Phase 84 occurrence.

3. Replace the Phase 84 `Plans:` placeholder line `- [ ] TBD (run /gsd-plan-phase 84 to break down)` with the wave-grouped list below, matching Phase 83's heading style exactly. Phase 85's own `- [ ] TBD (run /gsd-plan-phase 85 to break down)` line must survive unchanged.

Write these lines, in this order, each exactly as given:

- `**Wave 1**`
- `- [ ] 84-01-PLAN.md — Mint the CALFEED requirement family into REQUIREMENTS.md and ROADMAP.md, and complete the phase validation contract (wave 1)`
- `- [ ] 84-02-PLAN.md — End-to-end tracer: subscription table and migration, the signup-rooted cross-board feed query with its second-layer re-check, a minimal RFC 5545 writer, and the anonymous token-authenticated feed endpoint (wave 1)`
- `**Wave 2** *(blocked on Wave 1 completion)*`
- `- [ ] 84-03-PLAN.md — Full RFC 5545 writer: timed and all-day branches, floating local time, transparency, board-prefixed titles with vote suffixes, source-namespaced stable identifiers, folding and escaping, plus the exact-byte unit suite (wave 2)`
- `- [ ] 84-04-PLAN.md — Feed hardening: configurable rolling window, tombstone status codes, throttled last-fetched write, per-address rate limiting and address-free logging (wave 2)`
- `**Wave 3** *(blocked on Wave 2 completion)*`
- `- [ ] 84-05-PLAN.md — Feed behaviour suite: log-capture harness, four two-group tenant isolation cases, every response code, the inclusion and exclusion rules, both window bounds and the fetch-time throttle (wave 3)`
- `- [ ] 84-06-PLAN.md — Subscription management plumbing: QR package and renderer, absolute address builder, view models, and the add, rename and revoke Profile POST actions (wave 3)`
- `**Wave 4** *(blocked on Wave 3 completion)*`
- `- [ ] 84-07-PLAN.md — The Calendar Subscription section on both Profile layouts, with copy, calendar-handoff link, QR modal, rename modal and delete confirm (wave 4)`
- `**Wave 5** *(blocked on Wave 4 completion)*`
- `- [ ] 84-08-PLAN.md — Both-layout markup and round-trip suite, the phase static guard, the real-device subscription check, and the requirement and roadmap ledger close-out (wave 5)`

4. Append sixteen rows to the `## Requirements Coverage` table, after the final `| CONTACTTAG-17 | Phase 81 |` row, each exactly `| CALFEED-NN | Phase 84 |` in numeric order.

Leave the Phase 84 goal line alone — it already carries real content and is not a placeholder. Leave every Phase 85 line alone.
  </action>
  <verify>
    <automated>grep -c 'CALFEED-' /mnt/Data/repos/quest-board-dnd/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Requirements\*\*: TBD' .planning/ROADMAP.md` outputs `1` (Phase 85's remains; Phase 84's is gone)
    - `grep -c '^\*\*Requirements\*\*: CALFEED-01, CALFEED-02' .planning/ROADMAP.md` outputs `1`
    - `grep -c '^\*\*Plans\*\*: 0/8 plans complete$' .planning/ROADMAP.md` outputs `1`
    - `grep -c '^| CALFEED-[0-9][0-9] | Phase 84 |$' .planning/ROADMAP.md` outputs exactly `16`
    - `grep -c '84-0[1-8]-PLAN.md' .planning/ROADMAP.md` outputs exactly `8`
    - `grep -c 'run /gsd-plan-phase 85 to break down' .planning/ROADMAP.md` outputs `1`
    - `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged at its pre-edit value (no phase entry was lost)
  </acceptance_criteria>
  <done>Phase 84's roadmap entry names all sixteen requirement ids, declares eight plans grouped into five waves, and the coverage table maps every CALFEED id to Phase 84 — with Phase 85's placeholders intact.</done>
</task>

<task type="auto">
  <name>Task 3: Key 84-VALIDATION.md's per-task map to this phase's real task ids</name>
  <files>.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md` — the `## Per-Task Verification Map` table and the `## Wave 0 Requirements` checklist
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-02-PLAN.md` — its `<tasks>` block, for the tracer and proof-test task names
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-03-PLAN.md`, `84-04-PLAN.md`, `84-05-PLAN.md`, `84-06-PLAN.md`, `84-08-PLAN.md` — their `<tasks>` blocks, for the remaining task names and positions
  </read_first>
  <action>
Rewrite the `## Per-Task Verification Map` table rows in `84-VALIDATION.md`, replacing every `TBD` in the Task ID, Plan and Wave columns with real values. Keep every existing column and the status vocabulary line beneath the table. Task ids follow the form `84-<plan>-T<n>` where `<n>` is the task's position in its plan's `<tasks>` block.

Rows to write, in this order:

| Task ID | Plan | Wave | Requirement | Threat Ref | Test Type | Automated Command |
|---|---|---|---|---|---|---|
| `84-02-T3` | `84-02` | 1 | CALFEED-05, CALFEED-06 | T-84-01 | integration, end-to-end | `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionFeedTests` |
| `84-03-T1` | `84-03` | 2 | CALFEED-10, CALFEED-11 | T-84-16, T-84-17 | unit (exact-byte) | `dotnet test QuestBoard.UnitTests --filter CalendarFeedWriterTests` |
| `84-03-T2` | `84-03` | 2 | CALFEED-09 | T-84-16 | unit (exact-byte) | `dotnet test QuestBoard.UnitTests --filter CalendarFeedWriterTests` |
| `84-03-T3` | `84-03` | 2 | CALFEED-12 | T-84-15 | unit (invariant) | `dotnet test QuestBoard.UnitTests --filter CalendarFeedWriterTests` |
| `84-04-T1` | `84-04` | 2 | CALFEED-13 | T-84-21 | unit (options validation) | `dotnet test QuestBoard.UnitTests --filter CalendarFeedOptionsValidationTests` |
| `84-04-T2` | `84-04` | 2 | CALFEED-13, CALFEED-16 | T-84-06 | unit (repository, throttle) | `dotnet test QuestBoard.UnitTests --filter CalendarSubscriptionRepositoryTests` |
| `84-04-T3` | `84-04` | 2 | CALFEED-04, CALFEED-15 | T-84-02, T-84-05 | build + integration | `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionFeedTests` |
| `84-05-T1` | `84-05` | 3 | CALFEED-15 | T-84-02, T-84-05 | integration (captured logs, rate limit) | `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionFeedTests` |
| `84-05-T2` | `84-05` | 3 | CALFEED-06, CALFEED-07 | T-84-01, T-84-23 | integration, two-group | `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionFeedTests` |
| `84-05-T3` | `84-05` | 3 | CALFEED-04, CALFEED-08, CALFEED-13, CALFEED-16 | T-84-04, T-84-06 | integration | `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionFeedTests` |
| `84-06-T3` | `84-06` | 3 | CALFEED-01, CALFEED-02, CALFEED-03 | T-84-07, T-84-26 | build + full suite | `dotnet test` |
| `84-08-T1` | `84-08` | 5 | CALFEED-03, CALFEED-14 | T-84-30, T-84-33 | integration (markup + round trip, both layouts) | `dotnet test QuestBoard.IntegrationTests --filter ProfileCalendarSubscriptionTests` |
| `84-08-T2` | `84-08` | 5 | CALFEED-14 | T-84-32, T-84-35, T-84-36 | integration (static guard) | `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionStaticGuardTests` |

Fill the `Secure Behavior` column from the requirement's own wording, keep `File Exists` as `❌ W0` for any row whose test file does not exist yet, and leave every `Status` cell as `⬜ pending`.

Then set `wave_0_complete: false` and leave `status: draft` and `nyquist_compliant: false` alone — `/gsd-validate-phase` owns those transitions, not this plan. Under `## Validation Sign-Off`, leave every box unchecked and `**Approval:** pending` as-is.

Also add two `## Wave 0 Requirements` entries the research pass did not anticipate, so the checklist matches the plan set: `QuestBoard.UnitTests/Extensions/CalendarFeedOptionsValidationTests.cs` and `QuestBoard.UnitTests/Repository/CalendarSubscriptionRepositoryTests.cs`, plus `QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs` and the `QuestBoard.IntegrationTests/Helpers/CapturingLoggerProvider.cs` harness the log-safety fact depends on.

Add one line under `## Manual-Only Verifications`' existing rows noting that all three manual verifications are discharged by task `84-08-T3`, a blocking `checkpoint:human-verify` in plan 84-08, so the manual gate has an owner rather than floating.
  </action>
  <verify>
    <automated>grep -c 'TBD' /mnt/Data/repos/quest-board-dnd/.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD' .planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md` outputs `0`
    - `grep -cE '\| `?84-0[2-8]-T[0-9]' .planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md` outputs at least `13`
    - The file contains `84-08-T3` in the `## Manual-Only Verifications` region
    - `grep -c 'CalendarFeedOptionsValidationTests\|CalendarSubscriptionRepositoryTests\|CalendarSubscriptionStaticGuardTests\|CapturingLoggerProvider' .planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md` outputs at least `4`
    - The file still contains `status: draft`, `nyquist_compliant: false` and `**Approval:** pending`
    - Every `Automated Command` cell names either `dotnet test QuestBoard.UnitTests --filter CalendarFeedWriterTests`, `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionFeedTests`, `dotnet test QuestBoard.IntegrationTests --filter ProfileCalendarSubscriptionTests`, or `dotnet test` — no watch-mode flag appears anywhere in the file
  </acceptance_criteria>
  <done>The validation contract's per-task map names real Phase 84 task ids, real requirement ids and real threat references, with no TBD left and the manual gate owned by a named checkpoint task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning ledger → downstream agents | Requirement ids minted here are consumed by execute-phase, verify-work and audit-milestone; a wrong or missing id silently removes a behaviour from every later coverage check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-11 | Tampering | `.planning/ROADMAP.md` whole-file rewrite | medium | mitigate | Task 2 mandates scoped replacements only and asserts the pre-edit `### Phase ` heading count is unchanged, so a clobbered sibling phase entry fails the task |
| T-84-12 | Repudiation | requirement ids that no plan claims | medium | mitigate | Every minted id appears in the `requirements` frontmatter of at least one of plans 84-02 … 84-08; the `<verification>` block below re-checks the union |
| T-84-13 | Information Disclosure | this plan writes no code and touches no user data | low | accept | Documentation-only; no runtime surface exists in this plan |

No threat in this plan reaches the configured `high` blocking severity. ASVS level 1.
</threat_model>

<verification>
- `grep -c '^- \[ \] \*\*CALFEED-' .planning/REQUIREMENTS.md` is `16`
- Every id in ROADMAP.md's Phase 84 `**Requirements**` line also appears in REQUIREMENTS.md and in the ROADMAP coverage table
- The union of the `requirements:` frontmatter across `84-01-PLAN.md` … `84-08-PLAN.md` equals exactly the sixteen minted ids, with no id absent and none invented:
  `grep -ho 'CALFEED-[0-9][0-9]' .planning/phases/84-*/84-0*-PLAN.md | sort -u | wc -l` outputs `16`
- `grep -c 'TBD' .planning/phases/84-*/84-VALIDATION.md` is `0`
- `dotnet build` still succeeds, because this plan touches no source file
</verification>

<success_criteria>
The `CALFEED-*` family exists in all three ledgers with consistent counts, Phase 84's roadmap entry names its eight plans in five waves, Phase 85's placeholders are untouched, and the validation contract is keyed to real task ids.
</success_criteria>

<output>
Create `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-01-SUMMARY.md` when done
</output>
