---
phase: 84
slug: calendar-feed-foundation-and-event-subscription
status: approved
shadcn_initialized: false
preset: none
created: 2026-09-17
---

# Phase 84 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope reminder:** this contract governs one thing only — a new "Calendar Subscription" section added to the existing Profile page, on both `Views/Account/Profile.cshtml` and `Views/Account/Profile.Mobile.cshtml`. The feed endpoint, the ICS writer, the token table, and the repository query are backend and out of scope here (see `84-RESEARCH.md`). Nothing in this document proposes a new page, a new palette, or a new component library — it detects and locks the design system this codebase already has.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — server-rendered Razor + Bootstrap 5, not a JS component framework; the shadcn gate does not apply |
| Preset | not applicable |
| Component library | Bootstrap 5 (cards, modals, forms, tables) with this codebase's `.modern-card` overlay theme (`QuestBoard.Service/wwwroot/css/modern-card.css`) |
| Icon library | Font Awesome (`fas fa-*`), already loaded app-wide |
| Font | Body: Bootstrap's default sans-serif stack. Headings inside `.modern-card`/`.modern-card-header`: `'Cinzel', serif` (fantasy display face), weight 600, already declared in `modern-card.css` — reused as-is, not redefined |

**Detected, not invented:** every value below was read directly from `modern-card.css`, `account.mobile.css`, `contacts.mobile.css`, and the existing `Profile.cshtml`/`Profile.Mobile.cshtml`/`ContactCategoryManagement/Manage(.Mobile).cshtml` views (Phase 80's category-management page, the closest existing precedent for a named, rename/delete-capable row list). Nothing here is a new design decision unless explicitly marked "New for this phase."

---

## Spacing Scale

Declared values (must be multiples of 4) — Bootstrap's rem-based spacers, already in universal use across this codebase:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps (`me-1`) |
| sm | 8px | Compact element spacing (`me-2`, `gap-2`) |
| md | 16px | Default element spacing (`mb-3`, card body padding uses 1.5rem = 24px, see `lg`) |
| lg | 24px | `.modern-card-body` padding (`padding: 1.5rem` per `modern-card.css:67`); section spacing (`mb-4`) |
| xl | 32px | Layout gaps between major page regions |
| 2xl | 48px | Empty-state vertical padding (`py-5` = 3rem, matches `ContactCategoryManagement/Manage.cshtml`'s "No Categories Yet" block) |
| 3xl | 64px | Not used by this phase |

**Exception (existing, cited, not new):** icon-only reorder/action buttons on the mobile layout carry `min-height: 44px; min-width: 44px` — a deliberate departure from the 4px grid for touch-target size, already codified in `contacts.mobile.css:194-197` (`.category-mgmt-reorder-btn`) and `mobile.css:9`. Every icon-bearing button this phase adds to the mobile row (Copy, Show QR) must meet the same 44px floor. Text-labelled full-width buttons (Rename, Delete) already clear it via Bootstrap's default button padding and do not need the explicit override.

---

## Typography

Detected from `modern-card.css`, `account.mobile.css`, and Bootstrap defaults already in force on Profile:

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / row text (address, meta, button labels) | 16px | 400 | 1.5 |
| Label / meta (created date, last-fetched, helper text, `.account-field-label`) | 14px (0.875rem) | 400 | 1.4 |
| Sub-heading (new "Calendar Subscription" section title within the Profile card — an `h4`/`h5`, matching `Events/Details.cshtml`'s in-card sub-headers) | 20px (1.25rem) | 600 | 1.2 |
| Heading (existing page-level `h2` "Profile" title — unchanged) | 24px (1.5rem), Cinzel | 600 | 1.2 |

Only two weights in use: **400 regular** (body/meta text) and **600 semibold** (headings, `.form-label`, per `modern-card.css:121-127`). No third weight is introduced.

---

## Color

Detected from `modern-card.css`, `account.mobile.css`, and the app's existing icon-color conventions (`Events/Index.cshtml`, `Series/Details.cshtml`, `Agenda/Index.cshtml`, `_Layout.Mobile.cshtml`):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | Fixed parchment/notice-board background image (`body::before`, `site.css`) showing through everywhere the card doesn't cover | Page background, both layouts |
| Secondary (30%) | `.modern-card` glass surface — `rgba(255,255,255,0.15)` with `backdrop-filter: blur(15px)` (desktop) / `.account-card-mobile` — `rgba(255,255,255,0.15)` blur (mobile) | The Profile card itself; the new subscription section renders inside the same card, not a separate surface |
| Accent (10%) | `#6f42c1` (`.text-purple`) | **Reserved for exactly one element in this phase:** the new sub-section's heading icon (`fa-rss text-purple`), matching the same accent already used for every other calendar/event-identity header icon in this codebase (`Events/Index.cshtml`'s `fa-calendar-check text-purple`, `Series/Details.cshtml`'s `fa-repeat text-purple`, `Agenda/Index.cshtml`'s `fa-calendar-days text-purple`, `_Layout.Mobile.cshtml`'s My Agenda nav entry). Do not extend this accent to buttons, links, or any other element in this section — it marks "this is part of the calendar/events feature family," nothing else. |
| Destructive | Bootstrap `btn-danger` (`#dc3545` family, `.modern-card .alert-danger` uses `rgba(220,53,69,0.9)`) | The Delete (revoke) button only |

Existing supporting contextual colors already in use elsewhere on this exact page and its siblings, reused where noted below (not new accents, not part of the 10%): `btn-warning` (gold/filled — Add actions, matching `ContactCategoryManagement`'s "Add Category" button), `btn-primary` (blue gradient — default confirm actions), `text-muted` (meta text).

---

## Visuals

Focal points are declared here rather than left to be inferred from the Copywriting Contract — the checker flagged their absence as the one non-blocking gap in the first pass.

| State | Focal point | How the hierarchy is carried |
|-------|-------------|------------------------------|
| Empty state | The **Add Subscription** button | It is the only filled, coloured control in the sub-section; the explainer prose above it is body-weight muted text. Nothing else in the sub-section competes for the first glance. |
| Populated state | The **list of subscription rows** | The rows are the content; the section heading recedes to a label. Within a row, the address field is the widest element and reads first, with its Copy button adjacent — the address and the means of copying it are one visual unit. |
| QR modal | The **QR code itself** | At ~240×240px it dominates the modal body. The restated Copy button and webcal link sit beneath it as secondary paths, not peers. |
| Rename modal | The **Name input** | Single field, pre-filled and focused on open; the confirm button is the only filled control. |

**Within-row action hierarchy:** Copy is the expected path and sits adjacent to the address. Show QR Code, Rename, then Delete follow, with the destructive action last and the only one in `btn-danger`. No action in a row is icon-only on desktop; on mobile the Copy and Show QR controls may reduce to icon-only buttons at the 44×44px floor, and those two only.

**Accent discipline:** the `text-purple` accent is spent on exactly one element — the section heading's `fa-rss` icon (see `## Color`). It is deliberately *not* used to draw the eye to a focal point; filled-versus-muted weight does that work, matching how the rest of this application already establishes hierarchy.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Section heading | "Calendar Subscription" (singular — the feature; the list beneath it may hold several named subscriptions) |
| Primary CTA (empty state) | **Add Subscription** — verb + noun, matching the established "Add Category" CTA shape from Phase 80's Manage Categories page |
| Empty state heading | "No calendar subscriptions yet" |
| Empty state body | "Get this board's schedule onto your phone's own calendar app. The address below acts like a password — anyone who has it can see your events, dates, and times, and it never expires on its own. Press Add Subscription to create one; nothing is generated until you do." |
| Populated-row created label | "Created {MMM d, yyyy}" (matches this codebase's existing `CreatedAt.ToString("MMM d, yyyy")` convention, e.g. `Contacts/Details.Mobile.cshtml`) |
| Populated-row last-fetched, never fetched | "Never fetched yet" — muted, not bold, not styled as an error or warning. Must not read as "broken." |
| Populated-row last-fetched, has fetched | "Last fetched {MMM d, yyyy h:mm tt}" (matches `note.CreatedAt.ToString("MMM d, yyyy h:mm tt")` convention from `Contacts/Details.Mobile.cshtml`) |
| Rename modal heading | "Rename Subscription" |
| Rename field label | "Name" (placeholder: "e.g. My Phone") |
| Copy button, idle | "Copy" (icon: `fa-copy`) |
| Copy button, just-copied feedback | Label swaps to "Copied!" (icon: `fa-check`, color shifts to the existing `btn-success` green) for **2 seconds**, then reverts to "Copy" / `fa-copy`. No toast, no page reload — this is a per-button, self-contained state change, since the row already renders inline and a shared toast container is unnecessary machinery for a same-page confirmation. |
| webcal link | "Open in Calendar App" (icon: `fa-mobile-screen-button`; `href="webcal://..."`) |
| Show QR trigger | "Show QR Code" (icon: `fa-qrcode`) — opens a modal, does not render the QR inline in the row (see UI Considerations, QR presentation) |
| QR modal heading | "Scan to Subscribe" |
| QR modal body copy | "Point your phone's camera at this code to add {subscription name} to your calendar app." |
| QR image accessible text | `aria-label="QR code encoding the {subscription name} calendar subscription address"` — **never** the literal address string. A sighted user already sees the plaintext address in the row; a screen-reader user must not have the bearer token narrated aloud from an image alt attribute. |
| Rename affordance | "Rename" (icon: `fa-pen`) — opens a small modal (reusing this codebase's existing `show.bs.modal` + `event.relatedTarget` idiom, the same pattern `Shop/Index.cshtml` and `_CharacterSelectModal.cshtml` already use), not a navigation to a separate Edit page. The subscription section lives entirely on Profile; navigating away and back for a one-field rename would contradict D-15's "section, not a page." |
| Destructive action label | "Delete" (icon: `fa-trash`) — **the visible label is literally "Delete,"** per 84-CONTEXT D-07/D-08: the UI action is named Delete even though the underlying operation is a revoke-with-tombstone. Do not rename the button to "Revoke" — that would create a second name for one action with no user-facing benefit. |
| Destructive confirmation | Delete: `confirm('Delete "{name}"? Any device using this address will silently stop receiving updates the next time it checks — this cannot be undone. You can always create a new subscription afterward.')` — matches this codebase's existing native-`confirm()` pattern (`ContactCategoryManagement/Manage.cshtml`'s delete-category confirm), and states the specific, otherwise-invisible consequence (a device goes quietly stale) rather than a generic "are you sure." |
| Error state (mint/rename/revoke failure) | Reuse the existing server-rendered `_Toasts` partial / validation-summary pattern already wired into Profile's POST actions elsewhere in this codebase — "Couldn't {add/rename/delete} this subscription. Try again, and if it keeps happening, let a Dungeon Master know." No new error-surface mechanism is introduced. |
| Forbidden claim (duration) | No copy anywhere in this section may state or imply that the board or the calendar entry knows how long an event runs. 84-CONTEXT D-02's one-hour block is invented for calendar-client rendering only; do not write "your events show their real length," "one-hour sessions," or similar. |
| Forbidden claim (refresh speed) | No copy may promise a specific refresh interval ("updates every hour," "syncs within minutes"). Per `84-RESEARCH.md` Pitfall 2, every refresh-cadence signal is client-controlled and undocumented by the vendors (LOW confidence). If any copy addresses this at all, it must read like: "Your calendar app decides how often to check for updates — this can take anywhere from a few minutes to about a day," never a specific promised number. |
| Forbidden claim (`X-WR-CALNAME`) | No copy may promise the subscription will be named or renamed automatically inside the reader's calendar app (Apple Calendar is reported not to honour this reliably — Pitfall 1). The Name field is this app's own list label, shown here on Profile — not a guarantee about what the phone displays. |

---

## UI Considerations

Probed on the closed 8-category element/state axis over 9 described surfaces. **58 applicable considerations: 37 resolved (32 explicit, 5 backstop), 21 dismissed with reasons, 0 unresolved.** Element kinds were confirmed against the heuristic classifier, not taken from it: `E1` and `E2` gained `static-content` and `E9` was reclassified from unclassified to `nav`, raising 5 considerations the cue-match alone would have missed.

Empty-state and error-state **copy** lives in `## Copywriting Contract` above; the rows below reference it rather than restating it.

| # | Element / surface | Kinds |
|---|-------------------|-------|
| E1 | The Calendar Subscription section itself (explainer + Add control) | interactive-control, static-content |
| E2 | The subscription list (rows: name, address, created, last-fetched) | list-collection, static-content |
| E3 | The address field inside a populated row | form, list-collection, static-content |
| E4 | The Copy button | form, media, interactive-control, static-content |
| E5 | The Add Subscription form | form |
| E6 | The Rename modal | form, list-collection, static-content |
| E7 | The QR code modal | media, interactive-control, static-content |
| E8 | The Delete button | list-collection, interactive-control |
| E9 | The "Open in Calendar App" `webcal://` link | nav |

### Resolved considerations

| Element | Category | Status | Resolution |
|---------|----------|--------|------------|
| E1 | loading | 🧪 backstop | Add, Rename and Delete are plain form POSTs followed by a redirect; the whole-page reload is the only loading affordance and no spinner or skeleton is introduced for this section. |
| E1 | error | ✅ covered | A failed add, rename or delete surfaces through the existing server-rendered toast / validation-summary pattern already wired into Profile's POST actions; no new error mechanism is introduced. |
| E1 | overflow | ✅ covered | The explainer prose sits in the normal flow of `.modern-card-body` with no fixed height and no clipping; on a narrow viewport it wraps and grows the card rather than scrolling inside a bounded region. |
| E1 | long-text | ✅ covered | The explainer copy is fixed by the Copywriting Contract, so its length is bounded by the contract itself; it wraps and is never truncated. |
| E2 | empty | ✅ covered | Zero subscriptions renders the Copywriting Contract's empty-state heading and body plus the Add Subscription CTA; nothing is minted on page load. |
| E2 | loading | 🧪 backstop | The list is server-rendered with the page; mutations are form POSTs followed by a redirect, so the page reload is the only loading affordance. |
| E2 | error | ✅ covered | List-level failures surface through the existing toast / validation-summary pattern; no bespoke list error state. |
| E2 | populated | ✅ covered | Desktop renders stacked table rows matching `ContactCategoryManagement/Manage.cshtml`'s table shape; mobile renders stacked cards matching `Manage.Mobile.cshtml`'s `.category-mgmt-row` shape. |
| E2 | partial | ✅ covered | Each row's last-fetched cell renders independently — "Never fetched yet" versus a timestamp — with no shared or aggregate status line implying all rows are in the same state. |
| E2 | overflow | ✅ covered | The list is uncapped and unpaginated: it grows in normal document flow with however many subscriptions exist. No per-user ceiling and no pagination control are introduced. |
| E2 | zero-one-many | ✅ covered | One list container renders 0, 1 and N rows; there is no separate single-subscription layout branch. |
| E2 | long-text | ✅ covered | The name input carries `maxlength=60`, matching `ContactCategory.Name`'s existing convention; the row label truncates with `text-overflow: ellipsis` if a 60-character name still exceeds the row width on mobile. |
| E3 | error | ✅ covered | If the address cannot be rendered the row itself has failed; that surfaces through the section's toast / validation-summary error surface, not a field-level error state. |
| E3 | populated | ✅ covered | A `readonly` (never `disabled`) monospace field at 14px, present in the DOM and fully selectable but visually hidden by default. **Amended 2026-09-18 by operator decision during UAT:** the address is a bearer credential with no expiry, and this phase already refuses to let it reach a log line at all; rendering it permanently on a page that gets screen-shared or screenshotted contradicted that posture and was the row's largest element for no reader benefit. Copy, Show QR Code and Open in Calendar App are the paths to it. The field is revealed only by the E4 fallback below. |
| E3 | overflow | ✅ covered | The container bounds the width and absorbs the 80-plus-character token with either `overflow-x: auto` plus `white-space: nowrap`, or `text-overflow: ellipsis` — planner's choice between the two. Page-level horizontal overflow is prohibited either way. |
| E3 | long-text | ✅ covered | Token length is fixed by the generator, so the bounded-width container is sufficient; no truncation of the address text itself is permitted that would break copy-selection. |
| E4 | loading | ✅ covered | The clipboard write is synchronous; the 2-second "Copied!" label-and-icon swap is the entire in-flight-and-done affordance. No spinner. |
| E4 | error | ✅ covered | When the clipboard API is unavailable or denied, the button reveals the hidden `readonly` address field, focuses and selects its full contents, and swaps its label to "Press Ctrl+C" for 2 seconds instead of "Copied!". The field stays revealed afterwards — the person is mid-copy, and re-hiding it under them would be hostile. |
| E5 | empty | ✅ covered | The section's empty state *is* the unfilled form: explainer copy plus a single Add Subscription button. No fields are presented before minting. |
| E5 | loading | ✅ covered | On submit the Add button is disabled and its label swaps to "Adding…", re-enabled only by the redirect-driven page reload. A server-side guard additionally rejects a second mint for the same user within a short window, so a JS-disabled or replayed request cannot mint two bearer tokens from one intent. |
| E5 | error | ✅ covered | A failed mint surfaces through the existing toast / validation-summary pattern and no subscription row appears. |
| E5 | partial | 🧪 backstop | The Add form submits no user-supplied fields, so there is no partially-filled state; a freshly minted subscription therefore carries a system-assigned default name until the reader renames it. Verification: the default-name string is not fixed by this contract — assert a freshly minted row renders a non-empty, non-placeholder label on both layouts. |
| E6 | empty | ✅ covered | The modal opens pre-filled with the subscription's current name via the `show.bs.modal` + `event.relatedTarget` idiom; submitting an empty name is a validation failure, not an accepted rename. |
| E6 | loading | 🧪 backstop | Rename is a form POST followed by a redirect; the page reload is the only loading affordance. |
| E6 | error | ✅ covered | A failed rename surfaces through the existing validation-summary / toast pattern; the modal does not invent its own error surface. |
| E6 | populated | ✅ covered | The single Name input renders pre-filled with the current name; the "e.g. My Phone" placeholder shows only when the field is empty. |
| E6 | overflow | ✅ covered | A 60-character name scrolls horizontally within the native input; the modal body neither grows nor gains a scroll region of its own. |
| E6 | zero-one-many | ✅ covered | One modal instance is reused for every row, populated from the triggering button's data attributes; no per-row modal markup is emitted. |
| E6 | long-text | ✅ covered | `maxlength=60` on the Name input caps the value at source, matching `ContactCategory.Name`'s existing convention. |
| E7 | loading | ✅ covered | The `SvgQRCode` output is rendered server-side into the modal markup in the same response as the row, so there is no fetch and no loading state. Row count is small by construction (D-16 mints nothing automatically), so inline rendering per row is affordable. |
| E7 | error | ✅ covered | If QR generation fails the modal still renders its heading, the address, the Copy button and the "Open in Calendar App" link, so every delivery path except the QR stays reachable; the QR region is omitted rather than rendering a broken image. |
| E7 | populated | ✅ covered | Inline SVG sized to approximately 240×240px on both layouts, inside the Bootstrap modal body with no scroll region of its own. |
| E7 | overflow | ✅ covered | At 240×240 the SVG fits the modal body at both layouts' modal widths; it must scale down rather than introduce horizontal scroll on the narrowest supported viewport. |
| E7 | long-text | ✅ covered | The modal body copy interpolates the subscription name, which is capped at 60 characters and wraps; the QR's `aria-label` describes the code's purpose and never the address string. |
| E8 | loading | 🧪 backstop | Delete is a plain form POST followed by a redirect; the page reload is the only affordance. Verification: confirm Delete is a form POST consistent with the category-management delete, not a `fetch()`-based call. |
| E8 | error | ✅ covered | A failed revoke surfaces through the existing toast / validation-summary pattern and the row remains in the list. |
| E9 | error | ✅ covered | Handler absence cannot be detected from the page, so muted helper copy sits next to the link: "If nothing happens, copy the address and add it in your calendar app manually." The link ships on both layouts. |

### Dismissed considerations (reasons are the audit trail)

| Element | Category | Reason |
|---------|----------|--------|
| E3 | empty | A row exists only once a subscription has been minted, and minting always produces an address; an empty address field is unreachable by construction. |
| E3 | loading | The address is server-rendered into the row markup in the same response as the row itself; there is no asynchronous fetch that could be in flight. |
| E3 | partial | An address is an atomic opaque string; there is no partial or half-populated form of it. |
| E3 | zero-one-many | Exactly one address per subscription row, always; there is no zero or many case for this field. |
| E4 | empty | The Copy button exists only inside a populated row; there is no data-less form of it. |
| E4 | populated | A button carries no populated content state; its labels are enumerated in the Copywriting Contract. |
| E4 | partial | A clipboard write either succeeds or fails; there is no partial copy. |
| E4 | overflow | Fixed one-word label with an icon; it cannot exceed its container. |
| E4 | long-text | All three labels ("Copy", "Copied!", "Press Ctrl+C") are fixed by this contract and bounded. |
| E5 | long-text | The Add form has no text input; the name is supplied afterwards through the Rename modal, where the 60-character cap applies. |
| E6 | partial | Single-field form; there is no partial completion state to specify. |
| E7 | empty | The QR always encodes an existing subscription address; a QR with no payload is unreachable. |
| E8 | empty | The Delete button exists only inside a populated row. |
| E8 | populated | A button carries no populated content state. |
| E8 | partial | A revoke either commits or does not; there is no partial revoke state in the UI. |
| E8 | overflow | Fixed one-word label with a trash icon. |
| E8 | zero-one-many | One Delete control per row; the count follows the row count already covered by the list's zero-one-many resolution. |
| E8 | long-text | Fixed label fixed by the Copywriting Contract. |
| E9 | loading | Activating the link hands off to the operating system's default calendar handler; nothing loads in the page, so there is no in-page loading state to specify. |
| E9 | overflow | Fixed label "Open in Calendar App" with an icon; it wraps within the row's action area and cannot exceed the viewport. |
| E9 | long-text | Fixed label fixed by the Copywriting Contract. |

### Open-axis considerations (prose-owned, outside the closed taxonomy)

These sit on the open UX axis — accessibility breadth and touch interaction — and are contract content rather than probe output. They are carried forward from the authored spec and lift by the same rule.

| Concern | Element(s) | Status | Resolution |
|---------|------------|--------|------------|
| QR accessibility | QR code SVG | ✅ covered | `aria-label` describes the QR's purpose without embedding the token text (Copywriting Contract, "QR image accessible text") — a visual credential must carry a text alternative that does not read the secret aloud. |
| Touch target | Mobile row action buttons (Copy, Show QR icon buttons) | ✅ covered | 44×44px floor per the existing `.category-mgmt-reorder-btn` precedent in `contacts.mobile.css` (see the Spacing Scale exception). |
| Focus order | A row carrying Copy / webcal / Show QR / Rename / Delete | ✅ covered | Natural DOM order, no `tabindex` overrides — see "Row focus order" below, matching this codebase's `row-nav-link` convention of never reordering focus via script. |

<!-- Status vocabulary (locked by probe-core projectTruths):
     ✅ covered   → a plain truth string lifted into must_haves.truths
     🧪 backstop  → a flat scalar { statement, verification: backstop }; at verify time, no explicit
                    evidence → insufficient_spec → human_needed (never a silent pass, #1154)
     ⚠ unresolved → an explicit planner assumption (surfaced, never silently dropped)
     Dismissed rows carry a reason and are NOT lifted as truths; the reason is the audit trail.
     Rows are REPLACED (not appended) on a probe re-run — idempotent. -->

### Address field presentation (D-06)

The full subscription address must be visible in the row at all times once the subscription exists — not hidden behind a reveal click (that would contradict D-06's "stays visible ... at any time"). On a narrow phone layout, an 80+ character bearer token cannot wrap or blow out the row:

- Render the address in a `readonly` (not `disabled`) monospace text field or styled `<code>` block, `font-size: 14px`, inside a fixed-width container with `overflow-x: auto; white-space: nowrap;` (horizontal scroll within the field, not page-level overflow) or CSS `text-overflow: ellipsis` if scroll is judged too fiddly on a touchscreen — planner's call between the two, either satisfies "stays visible and selectable."
- The field must remain focusable and fully selectable (`readonly` inputs are; do not use `disabled`, which is not focusable and cannot be selected or copied via keyboard).
- The Copy button is the primary, expected path (Copywriting Contract), but a manually-selectable field is the fallback for any browser or assistive-tech path where the clipboard API is unavailable.

### QR presentation (D-14)

- The QR is rendered **inside a modal**, triggered by a "Show QR Code" button per row — not inline in every row by default. 84-CONTEXT D-15 already accepts that "a subscription table with per-row rename and delete controls plus a QR code will dominate" the Profile page; gating the QR behind a per-row trigger is how this contract keeps that accepted cost from compounding across several rows (D-05: "a reader may hold several"). This does not violate D-06, since D-06 concerns the *address text*, not the QR's visual encoding of it — the address itself stays in the row markup and selectable, though hidden by default per the amended field above.
- Inline SVG (`SvgQRCode` per `84-RESEARCH.md`) sized to approximately 240×240px inside the modal on both layouts — large enough for a phone camera to focus on a laptop screen at arm's length, small enough not to need its own scroll region inside a Bootstrap modal body.
- The modal also restates the Copy button and the "Open in Calendar App" (`webcal://`) link alongside the QR, so all three of D-14's delivery paths are reachable from one place once opened — the row itself only needs the one "Show QR Code" trigger plus the always-visible Copy button next to the address field.

### Row focus order (accessibility)

For a populated row carrying Copy, Show QR, Rename, and Delete: order these controls left-to-right in the DOM exactly as they read visually — Copy adjacent to the address field it copies, Show QR next, then Rename, then Delete last (destructive actions trail, matching `ContactCategoryManagement/Manage.cshtml`'s Rename-before-Delete ordering). Do not use `tabindex` to reorder focus; this codebase's established `row-nav-link` convention relies on DOM order matching visual and focus order everywhere else, and this row should not be the exception.

### Rename affordance (decision, not left to the planner)

A small Bootstrap modal per row, reusing the existing `show.bs.modal` + `event.relatedTarget` idiom already established in this codebase (`Shop/Index.cshtml`, `ShopManagement/Index.cshtml`, `_CharacterSelectModal.cshtml`) — not a full-page navigation to a separate Edit view. The category-management precedent (a dedicated `Edit` page) is a heavier pattern appropriate to a page whose sole purpose is management; the subscription section lives inside Profile alongside unrelated content, so leaving and returning to rename one field would be worse UX here than the modal this codebase already knows how to build.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — this is a server-rendered Razor/Bootstrap app, not a shadcn-consuming frontend |
| third-party | none | not applicable |

No component registry is in play for this phase. The one new dependency this phase introduces — QRCoder (`SvgQRCode` renderer) — is a server-side .NET NuGet package for rendering the QR markup, not a UI component registry block, and its legitimacy/version verification is already covered in `84-RESEARCH.md`'s Package Legitimacy Audit (verdict: OK, approved, with a `checkpoint:human-verify` flagged before install). Nothing further is required here.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-09-17 — gsd-ui-checker returned UI-SPEC VERIFIED (5 PASS, 1 non-blocking FLAG on Dimension 2 Visuals). The FLAG was closed by adding the `## Visuals` section above, which declares focal points per state instead of leaving them inferential.
