---
phase: 84-calendar-feed-foundation-and-event-subscription
plan: 08
type: execute
wave: 5
depends_on:
  - 84-05
  - 84-07
files_modified:
  - QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs
  - QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - CALFEED-01
  - CALFEED-02
  - CALFEED-03
  - CALFEED-14

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Zero subscriptions renders the empty-state heading and body plus the Add Subscription control, and nothing is minted on page load."
    - statement: "The list is server-rendered with the page; mutations are form POSTs followed by a redirect, so the page reload is the only loading affordance."
      verification: backstop
    - "List-level failures surface through the existing toast and validation-summary pattern; there is no bespoke list error state."
    - "The desktop layout renders stacked table rows matching the category-management table's shape, and the mobile layout renders stacked cards matching its card-row shape."
    - "Each row's last-fetched cell renders independently — either never-fetched wording or a timestamp — with no shared or aggregate status line implying every row is in the same state."
    - "The list is uncapped and unpaginated, growing in normal document flow with however many subscriptions exist; no per-member ceiling and no pagination control are introduced."
    - "One list container renders zero, one and many rows; there is no separate single-subscription layout branch."
    - "The name input caps at sixty characters, and a row label truncates with an ellipsis if a sixty-character name still exceeds the row width on the mobile layout."
    - statement: "The Add form submits no member-supplied fields, so there is no partially-filled state and a freshly minted subscription carries a system-assigned default name until the reader renames it; assert a freshly minted row renders a non-empty, non-placeholder label on both layouts."
      verification: backstop
    - "A real phone subscribed to a real minted address shows the member's events in its own calendar application, confirmed by a person rather than inferred from markup"
  artifacts:
    - QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs
    - QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs
  key_links:
    - "Every markup fact requests Profile twice — once with a default user agent and once with a real mobile user agent — because the mobile view is selected purely from that header and viewport emulation cannot exercise it"
    - "The static guard walks up from the test assembly's base directory to locate the view and stylesheet files, following the path-resolution helper the existing contrast guard already uses"
    - "The ledger close-out runs only after the real-device checkpoint has been approved"
---

<objective>
Close the phase: prove the section renders on both layouts under a real mobile user agent, prove add, rename and delete round-trip end to end, guard the contracts no server-side test can otherwise see, confirm a real phone actually subscribes, and close the ledgers.

Purpose: every prior plan proved one layer. This one proves the two things that only show up at the seams — that both layouts really got the feature, and that a real calendar application really consumes the feed. The first is provable automatically and this codebase has three recorded failures where it was not; the second is not provable automatically at all and needs a person with a phone.
Output: two new test classes, an approved real-device confirmation, and closed requirement and roadmap ledgers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-CONTEXT.md
@.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-UI-SPEC.md
@.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md
@.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-06-SUMMARY.md
@.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-07-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `QuestBoard.IntegrationTests.Tests.ProfileCalendarSubscriptionTests` — new test class with private helpers `GetProfileAsync`, `GetMobileProfileAsync`, `MintSubscriptionAsync`, `PostAddAsync`, `PostRenameAsync`, `PostRevokeAsync`, `CountOccurrences`
- `QuestBoard.IntegrationTests.Tests.CalendarSubscriptionStaticGuardTests` — new test class with a `ResolveRepoFile` path-walking helper
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` — ledger close-out edits only, no new ids

Symbols created elsewhere in this phase are inventoried in `84-02-PLAN.md`'s corresponding section.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Both-layout markup facts and the add, rename and delete round trip</name>
  <files>QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Tests/AgendaMobileRenderTests.cs` — the mobile user-agent constant, the `GetMobileAsync` request helper, and the class comment explaining why viewport emulation cannot exercise a view selected from a request header
    - `QuestBoard.IntegrationTests/Tests/ContactCategoryContrastGuardTests.cs` — how a fact asserts structurally against rendered markup rather than assuming a selector reaches an element
    - `QuestBoard.IntegrationTests/Helpers/AntiForgeryHelper.cs` — how an existing test extracts a token from a rendered page and posts it back, which every mutation fact below needs
    - `QuestBoard.IntegrationTests/Helpers/AuthenticationHelper.cs` — `CreateAuthenticatedClientAsync`, since Profile requires an authenticated member
    - `QuestBoard.Service/Views/Account/Profile.cshtml` and `Profile.Mobile.cshtml` — as shipped, for the exact strings, ids, classes and data attributes to assert on
    - `QuestBoard.Domain/Interfaces/ICalendarSubscriptionService.cs` — the mint path a fact uses to arrange a populated list without going through the page
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-UI-SPEC.md` `### Resolved considerations` — the E2 and E5 rows this suite is the verification for
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-CONTEXT.md` — D-16, which the mints-nothing-on-load fact is the direct check for: an address is a password with no expiry, so minting one for every member on a page load they did not make for that purpose is the wrong default; and D-15, whose both-layouts requirement the parity theories enforce
  </read_first>
  <action>
Create `QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` as an `IClassFixture<WebApplicationFactoryBase>` suite. Write a class comment stating that every markup fact runs twice — once with the default user agent and once with a real mobile user agent — because the mobile view is selected purely from that header, and that this codebase has shipped a control on one layout and not the other more than once, which is why the parity facts exist rather than being assumed.

Add the mobile user-agent constant and a `GetProfileAsync(HttpClient client, bool mobile)` helper that requests `/Account/Profile`, optionally attaching that header, and returns the status and the rendered markup. Add `PostAddAsync`, `PostRenameAsync` and `PostRevokeAsync` helpers that fetch Profile, extract the antiforgery token from the relevant form, and post to the matching action — every mutation fact must go through a real form post so the antiforgery path is exercised. Add a `CountOccurrences(string haystack, string needle)` helper for the row-count facts.

Write the facts, and write each one as a theory over the two layouts wherever the assertion is layout-independent, so parity is structural rather than a second hand-written copy:

**Empty state.**
- `Profile_RendersTheEmptyState_WhenTheMemberHoldsNoSubscription` — assert the empty-state heading, the contract's empty-state body text and the Add Subscription control are all present, on both layouts.
- `Profile_MintsNothingOnLoad` — request Profile three times for a fresh member, then assert through a direct service read that the member still holds zero subscriptions. Nothing is generated until Add is pressed, and a page load is not a press.

**Populated state and its shape.**
- `Profile_RendersOneRowPerSubscription` — a theory over zero, one and three minted subscriptions asserting the row-marker occurrence count matches, on both layouts. Assert the same container markup serves all three counts: there is no separate single-subscription branch, so the marker that identifies a row must be the same string in every case.
- `Profile_RendersTheDesktopTableShape_AndTheMobileCardShape` — assert the desktop markup contains the category-management table classes and the mobile markup contains the row class instead, and that neither contains the other's shape.
- `Profile_RendersEachRowsFetchStateIndependently` — mint two subscriptions, fetch one of the two addresses so only it has a fetch time, then assert the page carries both the never-fetched wording and a timestamp, and carries no single aggregate status line covering both rows.
- `Profile_RendersNoPaginationControl` — with ten minted subscriptions, assert all ten rows render and that no pagination or show-more control appears. The list is deliberately uncapped: there is no per-member ceiling.
- `Profile_CapsTheNameInputAtSixtyCharacters` — assert the rename input carries the sixty-character limit attribute, on both layouts.
- `Profile_TruncatesAnOverlongNameOnTheMobileLayout` — assert the mobile row label element carries the truncating class whose rule the static guard in task 2 pins.

**The freshly-minted default name.**
- `Profile_RendersANonEmptyDefaultName_ForAFreshlyMintedSubscription` — press Add through a real form post, then assert the rendered row label is non-empty, is not whitespace, is not the rename field's example placeholder text, and equals the default the service assigns. This is the design contract's one deliberately unspecified string, settled in the plumbing plan; this fact is what stops it drifting back to empty.

**The round trip.**
- `AddSubscription_CreatesExactlyOneSubscription_AndShowsIt` — post Add once, assert the redirect, follow it, and assert one row and one success notice.
- `AddSubscription_RefusesAnImmediateSecondSubmit` — post Add twice in immediate succession and assert the member still holds exactly one subscription and the second response carried the failure notice. A form submit can be double-clicked, retried by a browser or replayed, and each extra mint is a second credential that never expires.
- `RenameSubscription_ChangesTheLabel` — post a rename and assert the new name renders and the old one does not.
- `RenameSubscription_RefusesAnEmptyName` — post a whitespace-only name and assert the stored name is unchanged and a failure notice rendered.
- `RenameSubscription_DoesNothing_ForAnotherMembersSubscription` — mint for member A, post a rename as member B using A's subscription id, and assert A's name is unchanged and B's response reveals nothing about whether that id exists.
- `RevokeSubscription_RemovesTheRowFromTheList_ButKeepsTheAddressAnswering` — post Revoke, assert the row is gone from Profile, then fetch the address and assert it answers with the retired status rather than the unknown status. Delete is a retirement, and this fact is where the two halves of that decision meet.
- `RevokeSubscription_DoesNothing_ForAnotherMembersSubscription` — the mirror of the rename ownership fact.
- `Mutations_AreRejectedWithoutAnAntiforgeryToken` — post each of the three actions with no token and assert none of them succeeds.

Assert on the presence and absence of specific strings and markers rather than on whole-page snapshots. Keep the file at CRLF. Put no requirement id, phase number or plan number in any comment or string literal.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet test QuestBoard.IntegrationTests --filter ProfileCalendarSubscriptionTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.IntegrationTests --filter ProfileCalendarSubscriptionTests` exits 0 with at least 20 passing tests and 0 failures
    - `grep -c 'iPhone; CPU iPhone OS' QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` outputs at least `1` — the mobile view is exercised by a real user agent
    - `grep -cE 'Theory|InlineData' QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` outputs at least `4` — layout parity is structural rather than hand-copied
    - `grep -c 'No calendar subscriptions yet' QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` outputs at least `1`
    - `grep -c 'Never fetched yet' QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` outputs at least `1`
    - `grep -c 'maxlength' QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` outputs at least `1`
    - `grep -cE 'RefusesAnImmediateSecondSubmit|DoesNothing_ForAnotherMembersSubscription|AreRejectedWithoutAnAntiforgeryToken' QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` outputs at least `4`
    - `grep -c 'RendersANonEmptyDefaultName' QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` outputs `1`
    - `dotnet test` exits 0 for the whole solution
    - `file QuestBoard.IntegrationTests/Tests/ProfileCalendarSubscriptionTests.cs` reports CRLF line terminators
  </acceptance_criteria>
  <done>Both layouts are proven to carry the section under their own selection mechanism, the list is proven uncapped and per-row independent, and add, rename and delete are proven to round-trip with ownership, replay and antiforgery all refused correctly.</done>
</task>

<task type="auto">
  <name>Task 2: The static guard for contracts no server-side test can observe</name>
  <files>QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs</files>
  <read_first>
    - `QuestBoard.IntegrationTests/Tests/ContactCategoryContrastGuardTests.cs` — the full file, especially its `ResolveCssPath` directory-walking helper and its class comment about what a server-side suite provably cannot see
    - `QuestBoard.IntegrationTests/Controllers/CalendarButtonStyleTests.cs` — the sibling static-guard precedent from the previous phase
    - `QuestBoard.Service/Views/Account/Profile.cshtml` and `Profile.Mobile.cshtml` — as shipped
    - `QuestBoard.Service/wwwroot/css/site.css` and `account.mobile.css` — the appended prefixed rule blocks this guard pins
    - `CLAUDE.md` — the rule forbidding planning and tracking references in source, and the note that a prior phase existed solely to strip them back out
  </read_first>
  <action>
Create `QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs`. Write a class comment explaining what it defends, in the spirit of the existing contrast guard: these are contracts that a passing integration suite cannot see, because a server-side test reads markup and not computed styles, and reads code and not conventions.

Add a `ResolveRepoFile(params string[] segments)` helper that walks up from the test assembly's base directory until the joined path exists, failing with a message naming the attempted path — the same approach the contrast guard already uses, generalised beyond stylesheets so it can reach view files and source directories.

Write these facts:

**Copy parity between the layouts.** For each string in the copywriting contract — the section heading, the empty-state heading, the empty-state body, the Add label, the created and never-fetched and last-fetched wordings, the copy labels including the copied and manual-copy variants, the calendar-handoff label, its fallback sentence, the show-code label, the rename heading and its example placeholder, the delete label and the full destructive confirm text, and the code modal's heading and body — assert the string appears in both view files. Drive it from a single array of strings in one theory so adding a string to the contract means adding one array entry rather than a new fact.

**The destructive confirm text is byte-identical.** Extract the confirm argument from both view files and assert the two extracted strings are equal. Two hand-maintained copies of a sentence drift; this is what stops it.

**No forbidden claim.** Assert neither view file contains a duration claim, a refresh-interval promise, or a claim that the reader's calendar application will be named or renamed automatically. Match case-insensitively on the specific shapes the copy contract forbids. Comment why each is forbidden: the one-hour block is invented for rendering and the board does not know how long an event runs; every refresh signal is chosen by the reading application and undocumented by its vendor; and at least one major client is reported not to honour the calendar-name property.

**The mobile touch-target floor exists and reaches its element.** Assert `account.mobile.css` contains the icon-button rule with both the minimum height and the minimum width at 44 pixels, and assert structurally that the mobile view actually applies that class to its icon-only controls — a rule nothing selects is not a mitigation.

**The truncation rule exists and reaches its element.** Same two-part assertion for the row-label ellipsis rule.

**The code sizing rule exists in both stylesheets.** Assert each of the two stylesheets carries the vector sizing rule with its maximum width, since the two layouts load different stylesheets and a rule present in only one would leave one layout unsized.

**No planning or tracking reference reached the source.** Walk every `.cs`, `.cshtml` and `.css` file under the five project directories and assert none contains a requirement id of this phase's family, a phase reference, a plan reference or a review-finding reference. Write the search patterns as data rather than as literals embedded in prose, and comment why this guard exists: these references go stale the moment a phase closes, and a previous phase existed solely to hunt them back out of this codebase.
    <!-- planner-discipline-allow: CALFEED- -->
    <!-- planner-discipline-allow: Phase 84 -->

Keep the file at CRLF, and make sure the guard does not fail on itself — its own search patterns live in this test file, so exclude the test file that holds them from its own walk and say so in a comment.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionStaticGuardTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionStaticGuardTests` exits 0 with at least 8 passing tests and 0 failures
    - `grep -c 'ResolveRepoFile' QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs` outputs at least `2`
    - `grep -c 'min-height: 44px' QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs` outputs at least `1`
    - `grep -c 'max-width: 240px' QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs` outputs at least `1`
    - The copy-parity fact is a single theory over an array: `grep -c 'MemberData\|InlineData' QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs` outputs at least `1`
    - `dotnet test` exits 0 for the whole solution
    - `grep -rlE 'CALFEED-[0-9]' QuestBoard.Domain QuestBoard.Repository QuestBoard.Service --include='*.cs' --include='*.cshtml' --include='*.css' | wc -l` outputs `0`
    - `file QuestBoard.IntegrationTests/Tests/CalendarSubscriptionStaticGuardTests.cs` reports CRLF line terminators
  </acceptance_criteria>
  <done>Every copy string is proven present on both layouts, the destructive confirm text is proven byte-identical between them, no forbidden claim survives in either, each scoped style rule is proven to exist and to reach the element it targets, and no planning reference reached the source tree.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Subscribe a real phone to a real address</name>
  <what-built>
The whole feature is built and green: the token table and its migration, the signup-rooted cross-board query with its membership pin and second-layer re-check, the full RFC 5545 writer, the anonymous rate-limited endpoint with its retired-versus-unknown split and its throttled fetch timestamp, and the Calendar Subscription section on both Profile layouts with copy, calendar-handoff link, scannable code, rename and delete.

What is green is every layer the server can observe. What no automated test in this repository can observe is the only thing the phase actually promises: that a phone's own calendar application polls the address on its own schedule and renders the events. The integration suite runs against an in-memory database and never drives a real calendar client's poller or renderer.
  </what-built>
  <how-to-verify>
Run the application against a real database, sign in as a member who belongs to at least one board and holds at least one signup row on a future event, and:

1. Open Profile on a desktop browser. Confirm the Calendar Subscription section is present, the empty state reads correctly, and pressing Add Subscription produces exactly one row with a non-empty name, an address, a created date and the never-fetched wording.
2. Press Copy. Confirm the label swaps to Copied for about two seconds and that pasting elsewhere yields the full address.
3. Press Show QR Code. Confirm the code renders at roughly 240 by 240, that the modal also offers Copy and the calendar-handoff link, and that the modal body names the subscription.
4. On an iPhone, scan the code with the camera and follow the handoff. Confirm iOS Calendar offers to subscribe and that after subscribing the events appear. Note what iOS names the calendar — the phase does not promise it will use the published name, so record what it actually does rather than judging it.
5. In Google Calendar, use "From URL" with the plain web form of the address. Confirm the events appear.
6. In both clients confirm: a timed event shows as a one-hour block at the right wall-clock hour; an event with no time shows as a single-day all-day entry and not one spanning two days; and the entries do **not** mark you busy — check your own availability in the client and confirm the calendar reads as free.
7. Confirm each entry's title opens with the board name in square brackets, and that an event you answered maybe or declined carries its marker.
8. Reload Profile and confirm the last-fetched time is now populated.
9. Press Delete on that subscription, confirm the warning text names the subscription and states that a device will silently stop receiving updates, accept it, and confirm the row disappears from Profile. Then open the same address in a browser and confirm it reports the resource as gone rather than as never having existed.
10. Open Profile on a real phone browser — not a desktop with a narrowed window — and confirm the whole section is present there too, that the icon-only copy and show-code controls are comfortably tappable, that the address field scrolls inside its own box rather than pushing the page sideways, and that Rename and Delete both work.
11. Paste one live feed response into an iCalendar conformance validator and record any complaint. Treat a complaint as a defect in the writer.

Two things to watch for and report rather than fix in this phase: how long a change to an event takes to reach each client (record the observed delay; no copy promises a number, and both vendors choose their own interval), and whether an edited event ever stays stale after several confirmed refreshes — the writer emits a constant sequence number because there is no modified timestamp on an event, and a confirmed stale entry would reopen that as its own piece of work.
  </how-to-verify>
  <resume-signal>Type "approved" when a real phone and a real calendar client both show the events correctly, or describe what did not work</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Close the requirement and roadmap ledgers</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the `### Calendar Feed — Foundation and Event Subscription` section and the sixteen `| CALFEED-NN | Phase 84 | Pending |` traceability rows
    - `.planning/ROADMAP.md` — the Phase 84 entry's `**Plans**` line and its wave-grouped plan list, and the completed Phase 83 entry directly above it for the exact closed form
    - `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-01-SUMMARY.md` through `84-07-SUMMARY.md` — confirm each plan actually completed before ticking its line
  </read_first>
  <precondition>Task 3's real-device checkpoint has been approved. Do not close the ledgers on automated evidence alone — the phase's central promise is only observable on a real phone.</precondition>
  <action>
Make scoped edits only. Never rewrite either file whole: `ROADMAP.md` holds every other phase and `REQUIREMENTS.md` holds every other requirement family.

In `.planning/REQUIREMENTS.md`:
- Change each of the sixteen `- [ ] **CALFEED-NN**:` bullets to `- [x] **CALFEED-NN**:`, leaving every word of the sentence untouched.
- Change each of the sixteen `| CALFEED-NN | Phase 84 | Pending |` traceability rows to `| CALFEED-NN | Phase 84 | Complete |`.
- Leave the coverage block's counts alone — they count requirements, not completions, and were already updated when the family was minted.

In `.planning/ROADMAP.md`:
- Change the Phase 84 `**Plans**: 0/8 plans complete` line to `**Plans**: 8/8 plans complete`.
- Change each of the eight `- [ ] 84-0N-PLAN.md` lines to `- [x] 84-0N-PLAN.md`, leaving the descriptions and the wave suffixes untouched.
- Leave the wave headings, the goal, the requirements line, the origin note, the scope notes, the decisions block and the risks block untouched.
- Leave every Phase 85 line untouched, including its `**Requirements**: TBD` and its own `- [ ] TBD` plan placeholder.

Then add one line to the end of the Phase 84 risks block recording, in plain language, what the real-device check actually observed about refresh latency and about whether any entry went stale — the two things the research flagged as low-confidence and the checkpoint was asked to record rather than promise. If the checkpoint reported neither, say that no stale entry was observed and give the observed latency, so a future support question has an answer to point at.

Do not touch `84-VALIDATION.md`'s frontmatter: the validation lifecycle's status and compliance flags are owned by the validation pass, not by this plan.
  </action>
  <verify>
    <automated>cd /mnt/Data/repos/quest-board-dnd && grep -c '^- \[x\] \*\*CALFEED-' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*CALFEED-' .planning/REQUIREMENTS.md` outputs exactly `16`
    - `grep -c '^- \[ \] \*\*CALFEED-' .planning/REQUIREMENTS.md` outputs `0`
    - `grep -c '^| CALFEED-[0-9][0-9] | Phase 84 | Complete |$' .planning/REQUIREMENTS.md` outputs exactly `16`
    - `grep -c '^| CALFEED-[0-9][0-9] | Phase 84 | Pending |$' .planning/REQUIREMENTS.md` outputs `0`
    - `grep -c '^\*\*Plans\*\*: 8/8 plans complete$' .planning/ROADMAP.md` outputs `1`
    - `grep -c '^- \[x\] 84-0[1-8]-PLAN.md' .planning/ROADMAP.md` outputs exactly `8`
    - `grep -c 'run /gsd-plan-phase 85 to break down' .planning/ROADMAP.md` outputs `1` and `grep -c 'Requirements\*\*: TBD' .planning/ROADMAP.md` outputs `1`
    - `grep -c '^### Phase ' .planning/ROADMAP.md` is unchanged from its pre-edit value
    - `grep -c 'status: draft' .planning/phases/84-calendar-feed-foundation-and-event-subscription/84-VALIDATION.md` outputs `1` — the validation lifecycle was not touched
    - `dotnet test` exits 0 for the whole solution
  </acceptance_criteria>
  <done>All sixteen requirements read complete in both the body and the traceability table, all eight plans are ticked with the plan count closed, Phase 85's placeholders survive intact, and the real-device observations about refresh latency and staleness are recorded where a future support question will find them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence → shipping decision | Every layer below this plan is provable on the server; the phase's central promise is only observable on a real device, so the boundary is crossed by a person rather than a test |
| one layout → the other | Two hand-maintained copies of the same markup and the same script; nothing but a parity assertion keeps them equal |
| source tree → planning references | A convention rather than a compiler rule, and one this codebase has already had to clean up once |
| planning ledgers → downstream audits | Requirement and plan states here are read by later verification and milestone audits; a premature close hides an unfinished behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-33 | Denial of Service | a layout shipping without the feature | high | mitigate | Copy parity asserted from a single string array across both view files, the destructive confirm text compared byte for byte, and every markup fact run twice — once under a real mobile user agent, because the mobile view is selected purely from that header |
| T-84-30 | Spoofing | cross-site request forgery against the three mutations | high | mitigate | A fact posts each action with no token and asserts none succeeds, so the protection is proven at runtime and not merely present in markup |
| T-84-07 | Elevation of Privilege | acting on another member's subscription | high | mitigate | Two facts — one for rename, one for retirement — post another member's subscription id and assert nothing changed and nothing about that id was revealed |
| T-84-26 | Spoofing | a replayed Add minting a second never-expiring credential | high | mitigate | A fact posts Add twice in immediate succession and asserts exactly one subscription exists afterwards, so the server-side guard is proven rather than assumed from the disable-and-relabel affordance |
| T-84-34 | Repudiation | closing the ledgers on automated evidence alone | high | mitigate | Task 4 carries an explicit precondition that the real-device checkpoint is approved first, and the checkpoint is a blocking gate placed before it in the task order |
| T-84-35 | Tampering | a scoped style rule that exists but selects nothing | medium | mitigate | Each style guard is a two-part assertion: the rule exists in the stylesheet, and the view actually applies that class to the element it is meant to protect. A rule nothing selects is not a mitigation |
| T-84-36 | Information Disclosure | planning references left in shipped source | medium | mitigate | A guard walks every source, view and stylesheet file under the project directories for requirement, phase, plan and review references, excluding only the test file that holds the search patterns. A previous phase existed solely to strip these back out |
| T-84-32 | Repudiation | forbidden copy claims surviving in either layout | medium | mitigate | Case-insensitive negative assertions over both view files for duration claims, refresh-interval promises and calendar-naming promises, with each prohibition's reason recorded beside it |
| T-84-25 | Information Disclosure | what an in-memory harness and a markup suite cannot prove | medium | accept | Recorded in both suites' class comments and discharged by the blocking real-device checkpoint, which covers the renderer, the poller, the free/busy treatment, the all-day boundary and a conformance validator pass. Refresh latency and any stale-entry behaviour are recorded as observations rather than promised, per the research's own confidence ratings |

Threats at `high` or above are all dispositioned `mitigate` with named, checkable controls. ASVS level 1.
</threat_model>

<verification>
- `dotnet test QuestBoard.IntegrationTests --filter ProfileCalendarSubscriptionTests` exits 0 with at least 20 facts
- `dotnet test QuestBoard.IntegrationTests --filter CalendarSubscriptionStaticGuardTests` exits 0 with at least 8 facts
- `dotnet test` exits 0 for the whole solution
- No source, view or stylesheet file under the project directories contains a requirement id, phase reference, plan reference or review-finding reference
- All sixteen requirements read complete in both ledgers and all eight plans are ticked, with Phase 85's placeholders intact
- The real-device checkpoint was approved before the ledger close-out ran
</verification>

<success_criteria>
The section is proven present and equal on both layouts under their own selection mechanisms, add, rename and delete are proven to round-trip with ownership, replay and forgery all refused, every style and copy contract that a server-side test cannot see is guarded, a real phone has actually subscribed and rendered the events, and the ledgers are closed on that evidence rather than ahead of it.
</success_criteria>

<output>
Create `.planning/phases/84-calendar-feed-foundation-and-event-subscription/84-08-SUMMARY.md` when done
</output>
