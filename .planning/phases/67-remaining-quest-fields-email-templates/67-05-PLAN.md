---
phase: 67-remaining-quest-fields-email-templates
plan: 05
type: execute
wave: 2
depends_on: ["67-01", "67-02", "67-03", "67-04"]
files_modified: []
autonomous: false
requirements: [QUESTMD-02, QUESTMD-03, EMAILMD-01]
user_setup: []

must_haves:
  truths:
    - "Rewards edits with the Markdown toolbar/preview/hint on all 6 write forms, desktop and 320px mobile, toolbar in one row with no overflow"
    - "Rewards renders as formatted HTML on Quest Details, Quest Manage (collapsible), and QuestLog Details — desktop and mobile"
    - "Recap edits with the same editor on EditRecap (desktop + mobile) and renders as formatted HTML on QuestLog Details (desktop + mobile), with readable (black) editor/Preview text"
    - "Markdown-rendered Rewards/Recap show no doubled spacing; the untouched Original Quest Description on QuestLog still preserves line breaks"
  artifacts: []
  key_links: []
---

<objective>
Operator verification checkpoint for Phase 67. All code lands in plans 67-01…67-04 (Wave 1); this plan runs the full automated gate, then pauses for a human to confirm the live write→read loop for Rewards and Recap across desktop and mobile — mirroring Phase 66's 66-07 checkpoint. This is where the two known Phase-66 CSS cascade concerns (the `.modern-card` editor-text-color leak and the 320px toolbar one-row fit) are re-checked for the new Rewards/Recap editors specifically, since Phase 66 found the color leak only during human verification.

Purpose: QUESTMD-02, QUESTMD-03, and EMAILMD-01 are behavior-shaped and view-heavy; automated build/tests prove the wiring and email output, but the rendered visual result (toolbar fit, black editor text, no doubled spacing, formatted output) needs a human eye at real breakpoints.
Output: Operator sign-off (or a list of issues to fold into a gap-closure pass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md
@.planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate — build + email render tests + markdown unit tests</name>
  <files></files>
  <read_first>
    - .planning/phases/67-remaining-quest-fields-email-templates/67-01-SUMMARY.md
    - .planning/phases/67-remaining-quest-fields-email-templates/67-02-SUMMARY.md
    - .planning/phases/67-remaining-quest-fields-email-templates/67-03-SUMMARY.md
    - .planning/phases/67-remaining-quest-fields-email-templates/67-04-SUMMARY.md
  </read_first>
  <action>
    Confirm all four Wave-1 plans landed and the solution is green before asking the operator to look at anything. Build the whole solution (`QuestBoard.slnx`) and run the email render tests plus the markdown service unit tests. If the build fails due to locked output files, the app is likely running under the Visual Studio debugger — ask the operator to stop it (Shift+F5) and retry, per CLAUDE.md.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.slnx -c Debug && dotnet test QuestBoard.IntegrationTests/QuestBoard.IntegrationTests.csproj --filter "FullyQualifiedName~MarkdownRenderTests" && dotnet test QuestBoard.UnitTests/QuestBoard.UnitTests.csproj --filter "FullyQualifiedName~MarkdownServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build QuestBoard.slnx` succeeds.
    - The three `*MarkdownRenderTests` classes (QuestFinalized, SessionReminder, WaitlistPromoted) all pass.
    - `MarkdownServiceTests` passes (confirms the shared sanitizing pipeline is unregressed).
  </acceptance_criteria>
  <done>Solution builds; all email render tests and markdown unit tests are green; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies the live Rewards + Recap write→read loop (desktop + mobile)</name>
  <files></files>
  <read_first>
    - .planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md (Spacing Scale note on re-confirming the 320px one-row toolbar for Rewards/Recap; Typography note on re-checking editor/Preview text color inside `.modern-card`)
    - .planning/phases/66-quest-description-editor-rendering-proof-of-concept/66-07-SUMMARY.md (the two carried-forward CSS findings: `.modern-card` gold-text leak fix, and the accepted 44px-tall/~30-34px-wide mobile touch-target deviation)
  </read_first>
  <action>
    Start the app, then present the numbered verification steps below to the operator and PAUSE for their confirmation. This task changes no code; it gates Phase 67 closure on a human confirming the rendered result at both desktop and a true 320px mobile viewport. If the operator reports a scoped, operator-directed CSS/markup issue, it may be fixed in-session (mirroring 66-07); anything larger is routed to a gap-closure pass.
  </action>
  <what-built>
    Plans 67-01…67-04 wired the shared Markdown editor into Quest Rewards (6 write forms, incl. the new Follow-Up mobile field) and Quest Recap (EditRecap desktop + mobile), rendered both fields as formatted HTML on Quest Details, Quest Manage (new collapsible Rewards section), and QuestLog Details (incl. the new mobile Rewards section), added the `.markdown-content { white-space: normal }` pre-wrap override, and switched the Session Reminder + Waitlist Promoted emails to render Description as HTML.
  </what-built>
  <how-to-verify>
    Run the app (`dotnet run --project QuestBoard.Service`, or start it as usual). Verify at desktop width AND at a true 320px mobile viewport (the app uses server-side User-Agent detection — use a real mobile UA or device emulation that changes the UA, not just a resized desktop window, so the `.Mobile.cshtml` views actually load):

    1. REWARDS EDITOR (write): On Quest Create, Edit, and Create Follow-Up — desktop and mobile — the Rewards field shows the EasyMDE toolbar (Bold, Italic, Heading, List, Link, Blockquote + Preview), an info-icon tooltip on hover/tap ("Supports Markdown formatting. Leave a blank line between paragraphs."), and NO red asterisk. Type some Markdown; toggle Preview; confirm the rest of the toolbar disables while Preview is active. On the Follow-Up MOBILE form specifically, confirm the Rewards field now exists (it was missing before) and starts blank.
    2. TOOLBAR FIT (mobile, 320px): For BOTH a Rewards form and the Recap form, confirm the toolbar fits in one row with no horizontal scroll/overflow. (Buttons being ~30-34px wide rather than 44px is the known, accepted Phase-66 deviation — not a new defect.)
    3. EDITOR TEXT COLOR: In the Rewards editor (on Create/Edit, nested in `.modern-card`) and the Recap editor (EditRecap, nested in `.modern-card`), confirm the raw editing text AND the Preview pane text render black-on-white and readable — not gold/low-contrast. (This is the Phase-66 `.modern-card` leak; the fix is global CSS and should apply automatically here — confirm it does.)
    4. REWARDS RENDER (read): Save a quest with Markdown Rewards (a heading, a bullet list, some bold). Confirm formatted HTML on: Quest Details (desktop + mobile), Quest Manage's new "Rewards" collapsible (fa-coins icon, collapsed by default, expands on click; desktop + mobile), and QuestLog Details (desktop + mobile — including the newly added mobile Rewards section). Confirm a quest with BLANK Rewards shows no Rewards section anywhere (no empty collapsible on Manage).
    5. RECAP RENDER (read): Save a Recap with Markdown via EditRecap (desktop + mobile). Confirm formatted HTML on QuestLog Details (desktop + mobile). Confirm a quest with no recap still shows the plain-text "No recap has been written for this quest yet." line.
    6. NO DOUBLED SPACING / NO REGRESSION: Confirm the rendered Rewards/Recap boxes do NOT show large doubled vertical gaps between lines. Then confirm the untouched "Original Quest Description" block on QuestLog Details (desktop + mobile) STILL preserves its line breaks (it is out of scope and must keep pre-wrap) — type a multi-line description with single Enters on a quest and check it still shows those line breaks in the QuestLog Original Description box.

    (Email real-client rendering is NOT part of this checkpoint — it is Phase 71. The email HTML-rendering behavior is already proven by 67-04's passing render tests.)
  </how-to-verify>
  <verify>
    <human-check>Operator confirms items 1-6, or reports specific issues to triage.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms items 1-6 above, OR reports specific issues.
    - Any issue the operator raises is captured for a gap-closure pass; a "fix now" CSS/markup issue may be corrected in-session if scoped and operator-directed (mirroring how 66-07 handled its findings).
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 67, or describe the issues to fold into gap closure.</resume-signal>
  <done>Operator has signed off on the live Rewards + Recap loop, or issues are captured for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Verification-only plan — introduces no code and no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05 | (verification only) | live app review | accept | No code changes. The XSS/sanitization mitigations (T-67-01, T-67-02) are delivered and tested in 67-02/67-03/67-04; this plan only confirms rendered behavior. |
| T-67-SC | Tampering (supply chain) | dependencies | accept | No installs; no new dependency surface. |
</threat_model>

<verification>
- Automated gate (Task 1) green: solution builds, all `*MarkdownRenderTests` and `MarkdownServiceTests` pass.
- Operator sign-off (Task 2) on the live Rewards + Recap loop across desktop and mobile.
</verification>

<success_criteria>
- Phase 67 success criteria confirmed live: QUESTMD-02, QUESTMD-03, EMAILMD-01 all observably satisfied.
- Any operator findings are triaged (fixed in-session if scoped, else routed to gap closure).
</success_criteria>

<output>
Create `.planning/phases/67-remaining-quest-fields-email-templates/67-05-SUMMARY.md` when done, recording the operator's sign-off and any findings.
</output>
