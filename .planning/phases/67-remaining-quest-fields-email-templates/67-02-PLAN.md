---
phase: 67-remaining-quest-fields-email-templates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - QuestBoard.Service/Views/Quest/Details.cshtml
  - QuestBoard.Service/Views/Quest/Details.Mobile.cshtml
  - QuestBoard.Service/Views/Quest/Manage.cshtml
  - QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml
  - QuestBoard.Service/wwwroot/css/markdown-content.css
autonomous: true
requirements: [QUESTMD-02]
user_setup: []

must_haves:
  truths:
    - "Quest Rewards renders as formatted HTML on Quest Details (desktop and mobile)"
    - "Quest Manage shows a collapsed-by-default Rewards section (desktop and mobile), omitted entirely when Rewards is blank"
    - "Markdown-rendered Rewards no longer shows doubled vertical spacing from the old pre-wrap rule, while untouched plain-text Description blocks that share the same box class keep their line-break preservation"
  artifacts:
    - path: "QuestBoard.Service/Views/Quest/Details.cshtml"
      provides: "Rewards rendered via Html.Markdown() (inline pre-wrap removed)"
      contains: "Html.Markdown(Model.Quest.Rewards)"
    - path: "QuestBoard.Service/Views/Quest/Manage.cshtml"
      provides: "New collapsible Rewards section (D-01), fa-coins icon, questRewardsCollapse id"
      contains: "questRewardsCollapse"
    - path: "QuestBoard.Service/wwwroot/css/markdown-content.css"
      provides: "white-space: normal override scoped to .markdown-content wrapper"
      contains: "white-space: normal"
  key_links:
    - from: "QuestBoard.Service/Views/Quest/Details.cshtml"
      to: "IMarkdownService (via Html.Markdown extension)"
      via: "Html.Markdown(Model.Quest.Rewards)"
      pattern: "Html\\.Markdown\\(Model\\.Quest\\.Rewards\\)"
    - from: "QuestBoard.Service/Views/Quest/Manage.cshtml"
      to: "Bootstrap collapse component"
      via: "data-bs-target=\"#questRewardsCollapse\""
      pattern: "questRewardsCollapse"
---

<objective>
Render Quest **Rewards** as formatted HTML everywhere it displays in the `Quest` area — Quest Details (desktop + mobile) and a new collapsible Rewards section on Quest Manage (desktop + mobile, decision D-01) — by swapping raw `@Model.Quest.Rewards` output for `@Html.Markdown(...)`, and add the single CSS override that stops the shared parchment boxes' `white-space: pre-wrap` from doubling the spacing of Markdown block output.

Purpose: Completes the read-side of QUESTMD-02 within the `Quest` controller's views (QuestLog's read views are handled in 67-03). The Manage-page Rewards section mirrors Phase 66's Description collapsible so Manage doesn't show Description-but-not-Rewards.
Output: Four updated views + one CSS rule; Rewards now renders with headings/lists/bold/blockquotes styling identical to Description.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md
@.planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md

<interfaces>
<!-- The read-side helper. It already wraps output in .markdown-content — do NOT add another wrapper div. -->

From QuestBoard.Service/Extensions/HtmlHelperExtensions.cs:
```csharp
internal static IHtmlContent Markdown(this IHtmlHelper html, string? markdown);
// Renders markdown -> sanitized HTML via IMarkdownService (MarkdownRenderTarget.Web),
// returns: <div class="markdown-content">{rendered}</div>
```

Phase 66's already-shipped Manage-page Description collapsible (the exact pattern to copy for Rewards) lives in `Views/Quest/Manage.cshtml` (card > card-header > btn-link toggle with `data-bs-toggle="collapse"` + chevron > `#questDescriptionCollapse.collapse` > card-body with `@Html.Markdown(Model.Description)`). The mobile analog is in `Manage.Mobile.cshtml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render Rewards via Html.Markdown() on Quest Details (desktop + mobile) and add the pre-wrap override</name>
  <files>QuestBoard.Service/Views/Quest/Details.cshtml, QuestBoard.Service/Views/Quest/Details.Mobile.cshtml, QuestBoard.Service/wwwroot/css/markdown-content.css</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Details.cshtml (Rewards block: `<div class="quest-description-box" style="white-space: pre-wrap;">@Model.Quest.Rewards</div>`; Description above it already uses `@Html.Markdown(...)`)
    - QuestBoard.Service/Views/Quest/Details.Mobile.cshtml (Rewards block uses `.quest-description-mobile`, `fa-coins` icon, raw `@Model.Quest.Rewards`)
    - QuestBoard.Service/wwwroot/css/markdown-content.css (the existing `.markdown-content { font-size...line-height }` rule you will extend)
    - QuestBoard.Service/wwwroot/css/quests.css (lines ~813-835 — confirm `.quest-description-box` and `.recap-display-box` both carry class-level `white-space: pre-wrap`; you are NOT editing this file, but you must understand why the fix goes on `.markdown-content` instead)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md (the "Pre-wrap CSS scoping (important)" note under Claude's Discretion)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-PATTERNS.md (Flagged Discrepancy 1)
  </read_first>
  <action>
    In `Details.cshtml`: change the Rewards render from raw `@Model.Quest.Rewards` to `@Html.Markdown(Model.Quest.Rewards)`, and delete the inline `style="white-space: pre-wrap;"` attribute on that `.quest-description-box` div. Keep the surrounding `@if (!string.IsNullOrWhiteSpace(Model.Quest?.Rewards))` guard, the `.quest-description-box` class, the `<h5>` heading, and the `fa-coins` icon unchanged.

    In `Details.Mobile.cshtml`: change the Rewards render from raw `@Model.Quest.Rewards` to `@Html.Markdown(Model.Quest.Rewards)`. Keep the `.quest-description-mobile` wrapper, the `fa-coins` icon, and the blank-guard unchanged. (No pre-wrap removal is needed here — `.quest-description-mobile` in `quests.mobile.css` has no `white-space: pre-wrap` rule.)

    In `markdown-content.css`: add `white-space: normal;` to the existing `.markdown-content { ... }` rule (the block with `font-size: 1rem; font-weight: 400; line-height: 1.6;`). Rationale to preserve in a plain-language comment: Markdown output is real block-level HTML; the `.quest-description-box` / `.recap-display-box` parent boxes set `white-space: pre-wrap` (an inherited property) to preserve newlines in the old plain-text era, which would now preserve the insignificant newlines Markdig emits between `<p>`/`<ul>`/`<blockquote>` and double the vertical spacing. Overriding to `normal` on the `.markdown-content` wrapper collapses that whitespace for rendered Markdown only, while raw-text Description blocks that share those box classes (with no `.markdown-content` child) keep their pre-wrap untouched. Do NOT edit the `white-space` rules in `quests.css` — those classes are shared with out-of-scope plain-text Description blocks (`QuestLog/Details.cshtml` Original Description, `QuestLog/Details.Mobile.cshtml` Original Description) that must keep line-break preservation until a later phase migrates them.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds.
    - `Details.cshtml` contains `@Html.Markdown(Model.Quest.Rewards)` and no longer contains `style="white-space: pre-wrap;"` on the Rewards box.
    - `Details.Mobile.cshtml` contains `Html.Markdown(Model.Quest.Rewards)` and no longer renders `@Model.Quest.Rewards` raw in the Rewards block.
    - `markdown-content.css` contains `white-space: normal;` inside the `.markdown-content` base rule.
    - `quests.css` is unchanged — `git diff --name-only` does NOT list `QuestBoard.Service/wwwroot/css/quests.css`, and both `.quest-description-box` and `.recap-display-box` still contain `white-space: pre-wrap` there.
  </acceptance_criteria>
  <done>Rewards renders as formatted HTML on both Details views; the pre-wrap doubling is fixed at the `.markdown-content` level without touching the shared box classes that out-of-scope Description depends on.</done>
</task>

<task type="auto">
  <name>Task 2: Add the collapsible Rewards section to Quest Manage (desktop + mobile) — D-01</name>
  <files>QuestBoard.Service/Views/Quest/Manage.cshtml, QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml</files>
  <read_first>
    - QuestBoard.Service/Views/Quest/Manage.cshtml (lines ~37-51: the existing Description collapsible section — copy its structure)
    - QuestBoard.Service/Views/Quest/Manage.Mobile.cshtml (the existing Description collapsible section using `questDescriptionCollapseMobile` inside `.dm-manage-section-card`)
    - QuestBoard.Service/Views/Quest/_QuestSection.cshtml (the canonical collapse pattern D-01 references)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-CONTEXT.md (decision D-01)
    - .planning/phases/67-remaining-quest-fields-email-templates/67-UI-SPEC.md (Copywriting Contract row "Manage-page Rewards section header")
  </read_first>
  <action>
    In `Manage.cshtml`: immediately after the existing Description collapsible card (the one targeting `#questDescriptionCollapse`), add a new Rewards collapsible card that copies the Description card's structure verbatim, changing only: title text `Rewards`, header icon `fas fa-coins text-warning` (NOT `fas fa-scroll` — that is Description's icon), collapse target id `questRewardsCollapse` (used in both `data-bs-target`, `aria-controls`, and the target div's `id`), body content `@Html.Markdown(Model.Rewards)`. The section is collapsed by default (`class="collapse"` with no `show`). Wrap the ENTIRE new card in `@if (!string.IsNullOrWhiteSpace(Model.Rewards)) { ... }` so it is omitted outright when Rewards is blank (Description has no such guard because it is `[Required]`; Rewards is optional). `Model` here is a `Quest`, so the property is `Model.Rewards`.

    In `Manage.Mobile.cshtml`: apply the same addition immediately after the mobile Description collapsible section, using collapse target id `questRewardsCollapseMobile` and matching the mobile file's existing section container conventions (`.dm-manage-section-card` rather than `.card modern-card`, whichever the mobile Description section uses). Same blank-guard, same `fa-coins` icon, same `@Html.Markdown(Model.Rewards)` body, collapsed by default.
  </action>
  <verify>
    <automated>dotnet build QuestBoard.Service/QuestBoard.Service.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds.
    - `Manage.cshtml` contains `questRewardsCollapse`, a `fas fa-coins` icon on the new section header, `@Html.Markdown(Model.Rewards)`, and an enclosing `!string.IsNullOrWhiteSpace(Model.Rewards)` guard.
    - `Manage.Mobile.cshtml` contains `questRewardsCollapseMobile`, `fas fa-coins`, `@Html.Markdown(Model.Rewards)`, and the same blank-guard.
    - The new sections use `class="collapse"` without `show` (collapsed by default) on both files.
    - No existing `questDescriptionCollapse` / `questDescriptionCollapseMobile` block was altered.
  </acceptance_criteria>
  <done>Both Manage views show a collapsed-by-default, blank-guarded Rewards section rendered as Markdown, using the fa-coins icon and the correct per-breakpoint collapse ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored Rewards text → rendered HTML on Details/Manage pages | User-authored Markdown is converted to HTML and injected into the page via `Html.Markdown()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering / Information Disclosure (stored XSS) | `Html.Markdown(Model.Quest.Rewards)` / `Html.Markdown(Model.Rewards)` on Details + Manage | mitigate | `Html.Markdown()` routes through `IMarkdownService.RenderToHtml(..., Web)`, which sanitizes with HtmlSanitizer (blocks `<script>`, `javascript:` URLs, generic-attribute injection) — the exact pipeline unit-tested in Phase 65 (261 tests green) and already live for Description. The returned `HtmlString` wraps only sanitizer output, never raw user input. No new sanitization code. |
| T-67-SC | Tampering (supply chain) | client assets | accept | No package installs this plan; CSS-only + view edits. |
</threat_model>

<verification>
- `dotnet build QuestBoard.Service/QuestBoard.Service.csproj` is green.
- Rewards renders via `Html.Markdown()` on Details (desktop + mobile) and inside a new collapsible on Manage (desktop + mobile).
- `quests.css` untouched; `.markdown-content` gains `white-space: normal`.
- Visual confirmation of "no doubled spacing" on Rewards and "Description on QuestLog still preserves line breaks" is performed in 67-05.
</verification>

<success_criteria>
- Rewards renders as formatted HTML on Quest Details and Quest Manage across desktop and mobile.
- The Manage Rewards section is collapsed by default and hidden when empty (D-01).
- The pre-wrap doubling fix is scoped to `.markdown-content` and does not regress out-of-scope plain-text Description.
</success_criteria>

<output>
Create `.planning/phases/67-remaining-quest-fields-email-templates/67-02-SUMMARY.md` when done.
</output>
